# Solde #829 — l'emprise d'une masse se dérive du plan

date: 2026-07-26

VERIFIE: j'ai mesuré la dérivation sur la carte RÉELLE de l'utilisateur (`la-diligence-projet.json`, exportée depuis son éditeur, qui ne déclare AUCUNE masse) — 12 masses dérivées, 0 case de plancher sans toit ; j'ai lancé `npx tsc --noEmit` (0 erreur) et la suite complète (13 636 verts) ; j'ai chronométré la construction sans navigateur (buildScene 30 ms, buildRoofs 28 ms, buildWalls 77 ms, buildFloors 6 ms) pour écarter une régression de coût ; et j'ai regardé moi-même la capture isométrique — pans à croupes nets, faîtages continus, façade à colombage lisible.

## Restes

- Décrochements aux jointures entre masses dérivées adjacentes de même niveau : chaque masse reçoit sa propre croupe au lieu que les masses voisines fusionnent en une nappe continue -> ce ticket RESTE OUVERT après ce commit (le lot livre la dérivation, pas la fusion des nappes)
- Le ratio d'aires entre versants (critère du DoD d'origine) ne vaut que pour une masse CONVEXE ; sur une emprise en anneau il vaut 1,45 sans que ce soit un défaut de la formule -> RAS : critère requalifié, le versant sud est structurellement plus court
- Règle acquise à conserver : plusieurs masses `gable` valent mieux qu'une grande masse `hip` sur une emprise irrégulière (le `hip` est fait pour du convexe ; sur un L ou un anneau son axe médian est ramifié) -> RAS : consignée sur le ticket et appliquée à la migration de l'Arène

## Réfutation

verdict: PARTIEL — la dérivation elle-même n'a pas été réfutée (0 case sans toit sur la carte utilisateur, mesuré), mais j'ai constaté MOI-MÊME sur capture que la silhouette reste imparfaite aux jointures, et l'agent avait signalé un bug de pool de cases partagé entre corps (deux corps dérivaient chacun la couverture complète) qu'il a corrigé en cours de lot. Le ticket reste ouvert sur ce reste.
