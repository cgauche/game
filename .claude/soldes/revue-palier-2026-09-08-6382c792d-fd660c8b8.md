# Revue adversariale de PALIER — 13 commits de substance, 1 fermeture, 3 sessions, 2026-09-08
verdict: PARTIEL
Revue rendue par un juge en lecture seule (mandat : réfuter) sur la fenêtre `6382c792d..fd660c8b8` — arbre `.wt-gates-D` ÉPINGLÉ à `fd660c8b8b349ffe27df2f6048e1fabdd26ac94b` (= `git rev-parse origin/main`), `git status --porcelain` VIDE au moment de la mesure. Contrôle positif d'épinglage : `git show HEAD:scripts/raw/reconciliation-stock.json` existe (stock né en D2, dans l'arbre jugé), `src/scenes/diligence/diligence-mobilier-spatial.test.ts` ABSENT de HEAD et PRÉSENT à la base (retrait de D0c effectivement dans l'arbre jugé), `.claude/soldes/1691.md` suivi. Mesure indépendante du palier (sonde `mesureDuPalier`, `scripts/guards/lib/revuePalier.mjs:178`) : `{"compte":13,"tete":"6382c792d","chemin":".claude/soldes/revue-palier-2026-09-07-1cac27bb6.md"}` — 15 commits dans la fenêtre, 13 touchent `src`/`scripts` (les deux autres, `658c1330a` et `d91e3b46b`, sont des régénérations `docs/`). Le CONTENU tient : les huit commits ouverts au hasard sont exacts, y compris les retraits de fichiers et les cardinaux annoncés, et la CI est verte de bout en bout (12 runs, 12 succès, dont HEAD). Ce qui fuit est le RÉGIME, dans trois directions : un commit de substance publié SANS ticket, sans gate chiffrée et sans regard adversarial (`947b3b7a1`) ; un cardinal en dur posé sur une population vivante par la session voisine, le jour même où cette session en purgeait six (`relief-identite-matieres.test.ts:59`) ; et le REPLI du détecteur de stock qui, privé d'image post, ne voit pas naître un stock JSON de 13 entrées.

## Fermetures
- **Par commit : UNE.** `git log 6382c792d..fd660c8b8` × `/(fixes|closes|corrige|ferme)\s+#(\d+)/gi` = 1 occurrence, `corrige #1691` dans `5ead0d66f`. Solde présent DANS l'INDEX du commit de fermeture (`git show --stat 5ead0d66f` : `.claude/soldes/1691.md | 25 ++`), amendé ensuite par `c12a2be9b` (le fix post-suite complète). Conformité MESURÉE par le validateur du dépôt, pas jugée à l'œil : `validateSolde(git show fd660c8b8:.claude/soldes/1691.md, '2026-09-07')` → `{"ok":true,"problems":[],"refuted":false}` (`scripts/hooks/solde-ticket-guard.mjs`, export `validateSolde`). Le solde porte ses sections `## Restes` (chaque reste routé : « corrigé dans ce commit » avec site `src/gameIso/builders/floors.ts:166`, « inventaire #1686 », « inventaire #1463 »), `## Recette visuelle` (`public/qc/1691-relief/d-01-inspector-relief.png`) et `## Réfutation` (verdict CONFIRMÉ, juge de design + juge de diff).
- **Hors commit : ZÉRO fermeture illégitime.** `gh api 'search/issues?q=repo:cgauche/game+is:issue+closed:>=2026-09-07'` = 2 items : **#1691** clos `2026-09-07T20:11:42Z` (postérieur au commit `5ead0d66f` du 19:51 et à sa publication — c'est le closer de publication, régime `.github/workflows/ci.yml` job `fermetures`, conforme) et **#1690** clos `2026-09-07T12:24:19Z`, ANTÉRIEUR à la base `6382c792d` (2026-09-07 15:03:59Z) : palier précédent, hors fenêtre.

## Commits vérifiés (8, 3 sessions)
| sha | session | annonce vérifiée | preuve à HEAD |
|---|---|---|---|
| `947b3b7a1` | coop (sans ticket) | « resynchroniser les invites au retour de l'hote » : une ligne dans `RoomGuest`, un test | `src/net/relay.ts:322` `this.onReconnected?.();` dans la branche `env.evt === 'host-up'` (le diff n'ajoute QUE cette ligne) ; câblage réel `src/state/netFlow.ts:334` `rg.onReconnected = () => guest?.rejoin();` ; couverture `src/net/relay.test.ts:198` « invité assis pendant la coupure hôte : host-up rétablit campagne, snapshot et intents » et `:248` |
| `5ead0d66f` + `c12a2be9b` | #1691 (session voisine) | matière de relief en DONNÉE, `SCHEMA_PROJET` 7→8, 4 projets en `schema: 8`, `floors.ts:166,201` LISENT | `src/data/schemas/defs-scenes/projet.ts:34` `export const SCHEMA_PROJET = 8;` ; `src/state/worldMap.ts:592` `PROJECT_MIGRATIONS`, entrée `695: 7: (doc) => {` ; les QUATRE paquets portent `"schema": 8` (`arene`, `barge-du-sel`, `diligence`, `loup-et-saumure`) ; `src/gameIso/builders/floors.ts:166` `id: scene.reliefDefaults.pilier` et `:201` `id: solidBlock ? terrainMatiere(terrain)! : scene.reliefDefaults[part]` — aucun littéral |
| `056d287b8` | #1709 C3s | `readCorpus` LÈVE par BASE sur 0 fichier | `scripts/guards/lib/sourceCorpus.mjs:78-86` (`const vide = parBase.findIndex(...)` puis `throw`) — le refus est bien PAR BASE et précède toute `readFileSync` ; sonde de non-régression sur les 11 couples base×options réellement appelés dans `src/**` : `src` 1947, `src`+tests 3599, `src/engine` 161, `src/ui` 327, `src/ui`+`src/gameIso` 1336, `src/state`+`src/ui` 512, `src/state`(.ts) 185, `src/data/schemas/defs`(.ts,tests) 128, `src/scenes`(.json,tests) 4, `src/ui/styles`(.css,tests) 36, `src`(.css,tests) 38 — **aucune base vide, aucun appelant ne peut lever** |
| `5756d2d2c` | #1709 D2 | `raw:reconcile` peut rougir ; 13 trous durs stockés ; double sens ; stock vu par le cliquet de plage | `scripts/raw/reconcile.mjs:448` `if (neuves.length || perimees.length || pivot.length) process.exitCode = 1` ; sonde en mémoire (aucune écriture) : stock 13 entrées = `ADE I 2, EDO 10, EDOC 9, EDOC 10, MCLB 2, MDG 3, MDG 6, MSRC 10, NADJ 4, NADJ 14, PDT 8, PDT 9, B2 LDB 38`, trous mesurés sur l'arbre = 13, écarts `{neuves:0, perimees:0, pivot:0}` ; **mutations rejouées** : stock + entrée fantôme `ZI 99` → `perimees: ['ZI 99 — fantome (x)']` ; trou neuf `ZI 98` → `neuves: ['ZI 98 … · src/y.ts:2']` ; `estPorteurDeStock('scripts/raw/reconciliation-stock.json')` = **true**, et `croissanceDesStocks(diff de 5756d2d2c, {lirePostImage})` = **`reconciliation-stock.json +13`**, couvert par la ligne `CLIQUET:` du message (`croissancesNonCouvertes` = `[]`) |
| `56182764e` + `fd660c8b8` | #1709 D0b-T2 / D0c | trois tests-photographies SUPPRIMÉS, garde générique par glob | affirmation de RETRAIT vérifiée par `git cat-file -e <base>:<f>` vs `<tête>:<f>` : `src/scenes/diligence/diligence-mobilier-spatial.test.ts` PRÉSENT→ABSENT, `src/scenes/barge-du-sel/barge-du-sel-projet.test.ts` PRÉSENT→ABSENT, `src/scenes/loup-et-saumure/loup-et-saumure-projet.test.ts` PRÉSENT→ABSENT — et absents du DISQUE (`ls src/scenes/diligence` = `diligence-projet.json`, `edo-ch1-calibration-voyage.test.ts`) ; la garde générique énumère par glob (`src/scenes/bundled-projects.test.ts:31` `entry.name.endsWith('-projet.json')`), donc **couvre les 4 paquets livrés y compris ceux modifiés par #1691** et tout paquet futur |
| `0ae41122f` | #1709 D0b-T1 | `validateScene` juge le contenu, un paquet livré ne part avec aucune erreur | `src/state/validateScene.ts:54` `export function validateScene(project: Scene[], worldMap?)` ; contrôle exécuté : `npx vitest run src/scenes/bundled-projects.test.ts src/state/mobilier-implantation.test.ts src/state/validateScene-contenu.test.ts` → `{"numTotalTestSuites":13,"numPassedTestSuites":13,"numFailedTests":0,"numTotalTests":77,"success":true}` — **0 erreur `validateScene` sur les 4 paquets à HEAD** |
| `a474ca986` | #1709 F | `build` = `gen && vite build`, le typage jugé par SA gate | `package.json:17` `"build": "npm run gen && node scripts/lancer-local.mjs vite -- vite build"` (plus aucun `tsc`) ; `package.json:31` `"typecheck:brut": "… tsc --noEmit --incremental false"` ; `.github/workflows/ci.yml:52` `run: npm run typecheck` — le typage reste jugé en CI, aucun trou |
| `2639287cd` | #1709 B3 | trois fixtures cuisent par la primitive de mémo canonique | `src/state/sceneMemo.ts:11` `export function memoByRef<K extends object, V>` existe et est la primitive citée ; le diff ne touche que quatre fichiers de test (`sceneMeshes`, `sceneTint`, `props-volumiques`, `modele-forme`) — **chiffre NON re-mesuré** : « 12,6 → 2,1 s » est un temps de tests, mon rejeu isolé de `props-volumiques.test.ts` mesure 22,9 s de mur (démarrage `gen` + vite compris), donc ni confirmé ni réfuté |

Gates annoncées au message (15 messages relus) : **5 sans aucune mention de gate** — `658c1330a` et `d91e3b46b` (docs, admissible), `947b3b7a1` (SUBSTANCE, voir Réfutations), `2639287cd` et `5756d2d2c` (qui chiffrent en revanche leurs suites et leurs mutations dans `JUGE:`/`REFUTATION:`). Onze des treize commits de substance portent `JUGE:` et/ou `REFUTATION:` ; les deux exceptions sont `947b3b7a1` (aucun) et `d91e3b46b` (docs).

## CI
12 runs sur la fenêtre (`gh run list --workflow=ci.yml --branch main`), **12 verts / 0 rouge = 100 %**, tête comprise : `fd660c8b8` run **34199093426** `success` 2026-09-08T07:23:33Z. `main` est laissé VERT à HEAD — le défaut central du palier précédent (`main` CI-rouge) n'est pas reproduit. **Trois commits n'ont AUCUN run propre** (`947b3b7a1`, `5ead0d66f`, `c12a2be9b`) : poussés en lot, seule la tête du lot est jugée — dont le commit de FERMETURE `5ead0d66f`, qui n'a donc jamais eu de verdict CI à son propre sha.

## Stocks nominatifs
Rejeu de `croissancesNonCouvertes` sur les 15 commits (sonde 2 en fin de revue) : **une seule croissance**, `c12a2be9b` → `scripts/gates/ecrivainsAtteints.test.mjs +1`, **couverte** par le `CLIQUET:` de son message. `5756d2d2c` déclare `CLIQUET: scripts/raw/reconciliation-stock.json +13` et la croissance correspond EXACTEMENT (+13) lorsqu'on fournit l'image post, comme le fait la porte réelle.
**Action (β) de la revue précédente : FAITE.** La porte a posteriori ne lit plus `HEAD` seul : `scripts/hooks/stocks-nominatifs.test.mjs:18` importe `croissancesDeLaPlage, raisonDeRefusDePlage, SHA_NUL` de `scripts/guards/lib/plageStock.mjs`, prend le début de plage de l'événement de push (`:44-50`, `GITHUB_EVENT_PATH → before`) et lit l'image par `:431` `lirePostImage: (f) => gitOuNull('show', \`${apres}:${f}\`)` — un push en lot ne la contourne plus.

## Dérive
- **Un commit de substance HORS régime** : `947b3b7a1` (`src/net/relay.ts` + `src/net/relay.test.ts`, 136 insertions) porte un message d'UNE ligne, sans accents, **sans ticket**, sans gate chiffrée, sans `JUGE:`/`REFUTATION:`. Il est licite au garde par construction — « un commit sans AUCUN ticket n'entre jamais dans ce mécanisme (périmètre tranché #591) », `scripts/hooks/solde-ticket-guard.mjs:28-30` — mais il ENTRE dans le palier (`commitsDeSubstanceDepuis` compte `src`), donc le régime compte un commit qu'il n'a jamais regardé. C'est le trou nommé par la doctrine, exercé.
- **Cardinal en dur posé le jour où le palier en purgeait six** : `src/gameIso/builders/relief-identite-matieres.test.ts:59` `expect(livrees.length).toBeGreaterThan(40);` — le fichier a par ailleurs un plancher DÉRIVÉ correct (`plancherScenes()`, `:29`), ce `40` littéral est un chiffre sur une population vivante (scénarios + scènes de campagne), exactement la classe que `c9606d110` (« six CARDINAUX VIVANTS deviennent des PROPRIÉTÉS ») et `2639287cd` (`PLANCHER_INSTANCES = 100` réfuté) éliminaient le même jour. Session voisine, même palier : la leçon n'a pas traversé.
- **Migration C3a partielle, reste non chiffré** : `8aee8a926` migre dix-neuf gardes vers `readCorpus` et NOMME un reste honnête (« Sept gardes restent VERTES sur corpus VIDE »), mais **108 fichiers `*.test.ts(x)` de `src/` marchent encore l'arbre par `readdirSync`** (`grep -rln readdirSync --include=*.test.ts --include=*.test.tsx src/ | wc -l` = 108) : ce stock-là n'est chiffré nulle part, donc il ne décroît sous le regard de personne.
- **Fan-out** : 1 fermée (#1691), 0 créée dans la fenêtre → solde net négatif, régime « fan-out ≤ 1 » tenu.

## Réfutations (fichier:ligne + sha)
1. **`947b3b7a1`** — commit de substance publié sans ticket, sans gates annoncées, sans réfutation, et sans run CI propre (`src/net/relay.ts:322`). Le contenu est JUSTE (vérifié ci-dessus) ; c'est la PORTE qui n'a rien vu. Le périmètre #591 (`scripts/hooks/solde-ticket-guard.mjs:28-30`) rend l'esquive légale : il suffit de ne citer aucun ticket.
2. **`relief-identite-matieres.test.ts:59` (`5ead0d66f`)** — `toBeGreaterThan(40)` sur `testScenarios.length + Σ campagnes.scenes.length`. Un retrait légitime de scénarios rougit ce test sans faute ; le plancher dérivé de la ligne 29 le rend redondant.
3. **Angle mort MESURÉ du détecteur de stock, hors CI** — `croissanceDesStocks(diff)` **sans** `lirePostImage` rend `[]` sur `5756d2d2c` alors que le fichier NAÎT avec 13 entrées (`git show --name-status` : `A scripts/raw/reconciliation-stock.json`) ; avec l'image, `+13`. Cause : le repli textuel `estEntreeDeStock(t.texte)` (`scripts/guards/lib/stocksNominatifs.mjs:331-349`) ne reconnaît pas une entrée JSON multi-lignes, et `entreesDeStock` n'a d'AST que pour les dialectes qu'il parse. La porte de plage fournit l'image, donc la CI tient — mais tout appelant qui juge un diff sans image (diagnostic, sonde, revue) SOUS-COMPTE en silence, et un stock JSON qui NAÎT est le cas le moins couvert.
4. **Chiffre non re-mesurable en l'état** : `2639287cd` annonce « 12,6 → 2,1 s » pour `props-volumiques.test.ts` ; un rejeu isolé mesure 22,9 s de mur (`gen` + démarrage vite inclus) et la sortie compressée ne rend pas le temps de tests. Non réfuté, non confirmé — un chiffre de perf annoncé sans la commande exacte qui le rend n'est pas vérifiable par un tiers.
5. **Nom de CE fichier : la porte refuserait celui que le brief demande.** `nomDArchiveDeRevue` (`scripts/guards/lib/revuePalier.mjs:66-72`) rend, pour ce contenu, **`revue-palier-2026-09-08-6382c792d-fd660c8b8.md`** ; la graphie « base seule » n'est acceptée que pour les archives ANTÉRIEURES (`nomsDArchiveAcceptes`, `:81-88` : « Seule une revue NEUVE est tenue au nom courant »), et `problemesDeRevueNeuve` (`scripts/hooks/solde-ticket-guard.mjs:1052`) poserait « la revue s'appelle …, son contenu la nomme … ». **À stager sous `.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md`**, pas sous le nom du brief.

## Synthèse
Le palier est SAIN sur le fond et malade sur la bordure. Sur le fond : huit commits ouverts au hasard dans les trois sessions tiennent leurs annonces à l'octet — retraits de fichiers effectifs, `schema: 8` sur les quatre paquets, `floors.ts` sans littéral, `readCorpus` qui lève par base sans qu'aucun appelant réel puisse le déclencher, treize trous durs stockés et un cliquet à double sens que la mutation fait mordre dans les deux sens, `build` délesté de `tsc` sans perdre la gate `typecheck`, et `main` vert de bout en bout, ce qui solde le défaut central du palier précédent ; l'unique fermeture (#1691) est adossée à un solde que le validateur du dépôt accepte, avec ses restes routés et sa recette visuelle. Sur la bordure, trois fuites de régime se répondent : un commit de substance a traversé le palier sans ticket, donc sans solde, sans juge et sans run CI propre — l'esquive est légale par le périmètre #591 et vient d'être exercée pour de vrai ; le commit de fermeture lui-même n'a jamais eu de verdict CI à son sha, poussé en lot ; et pendant que cette session convertissait six cardinaux vivants en propriétés, la session voisine en reposait un (`toBeGreaterThan(40)`) à côté d'un plancher dérivé qui le rendait inutile, preuve que la leçon ne circule pas entre sessions d'un même palier autrement que par une revue comme celle-ci. S'y ajoute un angle mort mesuré du détecteur de stock hors du chemin CI : une naissance de stock JSON est invisible au repli textuel, et c'est précisément la forme qu'a prise le stock neuf de ce palier.

**Actions routées** : (α) un commit de substance sans ticket exige au minimum un `JUGE:` ou `REFUTATION:` au message — étendre le déclencheur du garde au-delà du ticket cité, ou décider explicitement que le périmètre #591 est maintenu et l'écrire dans le régime (#1679, L2) ; (β) `relief-identite-matieres.test.ts:59` : remplacer `toBeGreaterThan(40)` par le seul plancher dérivé `plancherScenes()` (#1691 rouvert au titre du reste, ou lot du chantier scènes de #1709) ; (γ) `estEntreeDeStock` : reconnaître une entrée-objet JSON multi-lignes, ou faire du repli sans image un REFUS nommé plutôt qu'un zéro silencieux (`stocksNominatifs.mjs:331-349`, #1709) ; (δ) chiffrer au message le stock résiduel des gardes qui marchent l'arbre à la main (108 fichiers) pour qu'il décroisse sous cliquet (#1709, suite de C3a) ; (ε) publier commit par commit, ou accepter explicitement qu'un lot ne fasse juger que sa tête — le commit de fermeture `5ead0d66f` n'a pas de run à son sha (#1679, L2). Rien à rouvrir sur le fond : la fermeture de la fenêtre est solidement soldée.

## Sondes (à promouvoir en test committé)
**Sonde 1 — le nom d'archive d'une revue NEUVE, et la mesure du palier** (lecture seule) :
```js
import { mesureDuPalier, nomDArchiveDeRevue, fenetreDeRevue } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/guards/lib/revuePalier.mjs'
import { validateRevuePalier } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/hooks/solde-ticket-guard.mjs'
console.log('MESURE', JSON.stringify(mesureDuPalier(process.cwd())))
const brouillon = '# Revue adversariale de PALIER — 13 commits de substance, 1 fermeture, 3 sessions, 2026-09-08\nverdict: PARTIEL\nfenetre `6382c792d..fd660c8b8` ' + 'x'.repeat(2000)
console.log('NOM ATTENDU', nomDArchiveDeRevue(brouillon))
console.log('VALIDE', JSON.stringify(validateRevuePalier(brouillon, '2026-09-08')))
```
Sortie : `MESURE {"compte":13,"tete":"6382c792d","chemin":".claude/soldes/revue-palier-2026-09-07-1cac27bb6.md"}` · `NOM ATTENDU revue-palier-2026-09-08-6382c792d-fd660c8b8.md` · `VALIDE {"ok":true,"problems":[]}`.

**Sonde 2 — angle mort du repli : une NAISSANCE de stock JSON est invisible sans image post** (lecture seule ; à promouvoir en cas de `scripts/hooks/stocks-nominatifs.test.mjs`) :
```js
import { execFileSync } from 'node:child_process'
const M = await import('file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/guards/lib/stocksNominatifs.mjs')
const ROOT = 'C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D'
const git = (...a) => execFileSync('git', a, { cwd: ROOT, encoding: 'utf8', maxBuffer: 1 << 28 })
const diff = git('show', '--format=', '-U0', '5756d2d2c')
const post = (f) => git('show', `fd660c8b8:${f}`)
console.log('SANS image :', JSON.stringify(M.croissanceDesStocks(diff).map(c => c.fichier + ' +' + c.net)))
console.log('AVEC image :', JSON.stringify(M.croissanceDesStocks(diff, { lirePostImage: post }).map(c => c.fichier + ' +' + c.net)))
console.log('entrées AST du JSON :', M.entreesDeStock(post('scripts/raw/reconciliation-stock.json'), 'scripts/raw/reconciliation-stock.json').length)
```
Sortie : `SANS image : []` · `AVEC image : ["scripts/raw/reconciliation-stock.json +13"]` · `entrées AST du JSON : 13`.

**Sonde 3 — double sens du cliquet `raw:reconcile`, sans écrire un octet** (lecture seule ; à promouvoir en complément de `scripts/raw/reconcile.test.mjs`) :
```js
process.chdir('C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D')
const M = await import('file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/raw/reconcile.mjs')
const stock = M.lireStock(); const entrees = M.trousDurs(M.computeReconciliation({}))
console.log('arbre :', JSON.stringify(M.ecartsTrousDurs(entrees, stock)).slice(0, 120))
console.log('stock fantôme →', M.ecartsTrousDurs(entrees, { ...stock, 'ZI 99': { sites: ['src/x.ts:1'], lot: 'x', date: '2026-01-01', quoi: 'fantome' } }).perimees.map(x => x.cle ?? x))
console.log('trou neuf →', M.ecartsTrousDurs([...entrees, { cle: 'ZI 98', livre: 'ZI', chap: 98, sites: ['src/y.ts:2'] }], stock).neuves.map(x => x.cle ?? x))
```
Sortie : `arbre : {"neuves":[],"perimees":[],"pivot":[]}` · `stock fantôme → [ 'ZI 99 — fantome (x)' ]` · `trou neuf → [ 'ZI 98 … · src/y.ts:2' ]`.

**Sonde 4 — aucun appelant de `readCorpus` ne peut lever (refus du vide, C3s)** (lecture seule) :
```js
import { readCorpus } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/guards/lib/sourceCorpus.mjs'
process.chdir('C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D')
for (const [dirs, o] of [[['src'],{}],[['src'],{tests:true}],[['src/engine'],{}],[['src/ui'],{}],[['src/ui','src/gameIso'],{}],[['src/state','src/ui'],{}],[['src/state'],{exts:['.ts']}],[['src/data/schemas/defs'],{exts:['.ts'],tests:true}],[['src/scenes'],{exts:['.json'],tests:true}],[['src/ui/styles'],{exts:['.css'],tests:true}],[['src'],{exts:['.css'],tests:true}]])
  try { console.log(dirs.join('+'), JSON.stringify(o), '=', readCorpus(dirs, o).length) } catch (e) { console.log('LEVE', dirs.join('+'), e.message.slice(0, 120)) }
```
Sortie : 11 lignes, aucune `LEVE` (1947 / 3599 / 161 / 327 / 1336 / 512 / 185 / 128 / 4 / 36 / 38).
