# Solde #1713 — 2026-09-16

VERIFIE: arbitrage utilisateur du 2026-09-16 (verbatim au ticket : « Oui, supprimer (Recommandé) ») qui REMPLACE celui du 2026-09-08 (« Le job joue les gates », pris sous le régime du justificatif local, sans objet depuis #1776 : plus de justificatif, ruleset `main` actif, un push émis par `GITHUB_TOKEN` ne déclenche pas de run CI, bypass de l'intégration Actions refusé par GitHub — HTTP 422 collé au ticket) ; prémisse « aucun lecteur » mesurée par DEUX méthodes — `git grep -n -E "docs/decisions|issues:export|export-issues|issues\.json" origin/main` hors archives : 30 sites, tous exclusions de scan (`memoryLinks.mjs` HORS_SCAN, `check-plans-anchors.mjs` CORPUS_HISTORIQUES + `continue`, `check-doc-refs.mjs`, `manualDocsStock.mjs`, migration 1699), descriptions (`build-reprise.mjs`, `toutes.mjs:112`, `ruleset-main.mjs`, `pre-push.mjs`, `canari.yml:103`) ou l'export lui-même ; contre-sonde du juge sur l'arbre entier (`grep -rn` suivi ou non, mot nu `decisions`, chemins construits) : zéro site dans `src/`, `server/`, `.claude/skills`, `.claude/agents`, `knip.json`, workflows restants ; le seul LECTEUR effectif du fichier était `scripts/hooks/stocks-nominatifs.test.mjs:126` (parse de tout `.json` suivi) ; miroir figé au 2026-09-01 (runs 34214352782 du 09-08 et 34958974341 du 09-15 rouges). Diff relu en entier par l'orchestrateur : 18 fichiers + 2 suppressions sous `docs/decisions/` (−22 623 lignes), aucune pierre tombale (juge : 8 commentaires réécrits relus, tous vrais après suppression ; « aucun workflow ne commet sur `main` » vérifié sur `ls .github/workflows/`), `CORPUS_HISTORIQUES = ['.claude/soldes/']` reste nécessaire ET exercé (`.claude/soldes/925.md:6` cite `docs/decisions/issues.md:342`, garde verte), `HORS_SCAN` sans `issues.json`, `docs/superpowers/` toujours présent sur `origin/main` (exemptions voisines non orphelines), tableau des workflows de `docs/reprise-apres-pause.md` dérivé du dossier réel (5 → 4 lignes) ; contre-grep sur l'INDEX après `git add` par chemins : 0 ligne (exit 1). Gates du périmètre, sorties brutes et codes réels (codeur puis juge, méthodes distinctes) : `test:docs` 76/76, `test:hooks` 951/951 (deux fois), `test:ops` 243/243, lint 0 avertissement, `docs:check` OK (deux fois : 27 générateurs rejoués puis « à jour, 26 frais »), `node scripts/docs/check-plans-anchors.mjs` exit 0 sans l'exemption et son banc 7/7 (aucune fixture ne citait `docs/decisions/`), `ops:ruleset -- --dry-run` : corps identique, aucun `bypass_actors` ; `vitest` `memory-links-guard` 11/11 + `comment-poison-guard` (84 au total) ; `ecrivainsAtteints.test.mjs` 3/3 après la correction de `toutes.mjs:112`. Suite complète jouée par le run CI de `chantier/1713` (régime #1776). Passe de réfutation de fermeture par un juge (lecture seule), verdicts ci-dessous.

## Restes
- `scripts/gates/toutes.mjs:112` déclarait « LIT docs/ sur un site seulement » : mesuré FAUX par le juge (sonde `fs` sur `test:hooks` : `docs/.sources-lues.json` est lu par le banc de `scripts/git-hooks/`, `pre-commit.mjs:272`) -> corrigé dans ce commit scripts/gates/toutes.mjs:112 (deux sites nommés, mesure datée)
- renvois `check-doc-refs.mjs l.46-49` périmés dans `check-plans-anchors.mjs` (le commentaire visé vivait en l.66-69) -> corrigé dans ce commit scripts/docs/check-plans-anchors.mjs:43 (les deux renvois ont disparu avec les blocs réécrits)
- `scripts/migrations/2026-09-14-1699-source-chemins-ascii.mjs:97` : `'docs/decisions/'` reste dans `ARCHIVES_DATEES`, liste exécutée par le filtre l.390 -> RAS : la migration est un script DATÉ rejoué sur l'arbre de sa date (`migrations:replay:head`), sa liste décrit les archives du 2026-09-14 ; sur un arbre sans `docs/decisions/` le préfixe est un no-op sans effet ni faux, et l'éditer changerait un artefact figé (même statut que `docs/plans/`)
- grief 10 de la revue de palier 2026-09-09 : la porte de `main` ne voit que `ci.yml` (`coursesCi.mjs:75` défaut `'ci.yml'`, `pre-push.mjs:150` sans surcharge ; ruleset limité à `build` + `migrations`) — l'INSTANCE (`export-issues.yml` rougissant chaque mardi sans canal) disparaît avec ce lot, la CLASSE (`deps-report.yml` qui rougirait avant son étape d'issue, tout workflow neuf) survit -> #1779

## Réfutation
verdict: PARTIEL — juge de réfutation de fermeture (2026-09-16, lecture seule, `.wt-1713` indexé sur d5449edb2) : lentille 1 « aucun lecteur » TIENT (contre-sonde arbre entier, chemins construits compris) ; lentille 2 `toutes.mjs:112` FRAGILE → cardinal « un site » RÉFUTÉ par sonde `fs` (deux sites : `docs/raw` et `docs/.sources-lues.json`), corrigé dans ce commit ; pierres tombales TIENT (8 commentaires relus) ; lentille 3 exemptions orphelines TIENT (`docs/superpowers/` présent), migration 1699 FRAGILE non bloquante (RAS motivé ci-dessus) ; lentille 4 `check-plans-anchors` sans exemption TIENT (exit 0, banc 7/7, `CORPUS_HISTORIQUES` exercé) ; lentille 5 fermeture TIENT sur le ticket (supersession de l'arbitrage du 09-08 fondée sur un mécanisme mort prouvé au ticket, le titre parle du push refusé et l'émetteur du push disparaît sans lecteur orphelin) et FRAGILE sur la classe → #1779 ouvert AVANT la fermeture ; lentille 6 docs générés TIENT (`docs:check` rejoué par le juge, exit 0). Verdict global du juge : fermeture NON RÉFUTÉE sous un défaut corrigé dans le geste.
