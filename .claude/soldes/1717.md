# Solde #1717 — 2026-09-11

VERIFIE: l'attendu du ticket (le rouge intermittent de `dataset-save-parse.test.ts` sur la clé `etats` a sa cause nommée, pas seulement constatée) confronté à l'arbre du worktree `gates/F2` — cause établie par un juge en lecture seule (canari `afterEach` sur 608 fichiers, un seul pollueur : `etat-test-penalty.test.ts` réordonnant les clés d'une entrée VIVE, SEED de `resetData()` capturé après le mutateur), reproduction déterministe par paire (`--no-file-parallelism`, exit 1 → exit 0 après correction), site corrigé par la porte d'écriture déclarée (`setDataset` + restauration de l'objet d'origine, identité et ordre des clés vérifiés à l'octet), et VERROU PAR CONSTRUCTION : le détecteur du seam d'écriture reconnaît désormais la mutation d'une entrée résolue (`delete`, affectation, méthode mutante, `Object.assign`) par une passe AST à résolveurs DÉRIVÉS du source — deux autres tests mutateurs trouvés et corrigés par la même porte, zéro faux positif en production ; mutation témoin (le `delete` remis) → garde rouge nommée ET paire rouge ; 142 tests verts, `typecheck:fast` 0, `eslint` 0, pre-commit 0 ; diff lu par l'orchestratrice ; gates locales complètes jouées en série avant le push.

## Restes
- La seconde occurrence du ticket (`skills-screen-attention.test.tsx`, React « Should not already be working » sous charge, une fois) n'est pas un magasin partagé mais un `act()`/une racine react-dom en vol dans le worker jsdom partagé, hypothèse distincte avec sa sonde nommée -> #1724
- Le gel profond du dataset au setup (verrou plus large) est RÉFUTÉ par mesure : `setDataset` fait `arr.splice` en place, un dataset gelé casse la porte de production `CodexEdit.save` -> RAS : le verrou retenu est la garde structurelle du seam, qui refuse le geste au SITE (chez le coupable, jamais chez la victime)
- Une signature d'ordre des clés par test sur tout `src/data` (17,8 ms/test, ~350 s CPU sur la suite) -> RAS : prohibitif, mesuré ; la garde du seam rend le détecteur inutile

## Réfutation
verdict: CONFIRMÉ — juge d'enquête (lecture seule, 40 min) : magasin nommé (`ARRAYS.etats` vif et le SEED de `resetData()`), un seul pollueur sur 608 fichiers par canari, reproduction déterministe par paire, gel du dataset réfuté par mesure ; juge de diff (30 min) : 0 faux positif de production sur 3 619 fichiers, restauration par identité vérifiée, UNE réfutation (le vocabulaire dérivé manquait les résolveurs exportés en `const` — 16, dont `findPropById` — et laissait passer une violation réelle dans `foyer-de-lampe.test.ts`) corrigée avec 48 résolveurs et le site par la porte, quatre angles morts mesurés dont trois couverts et un écrit.
