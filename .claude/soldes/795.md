# Solde #795 — garde mapQC de scene + reachability jouable (props par etage)

date: 2026-07-24

VERIFIE: reachableCells->walkNeighbors->isWalkable inclut deja entityBlockedAt (props solides) depuis #791, donc la reachability du harnais est DEJA jouable et props-aware par etage ; le garde de scene (unreachableDescriptiveZones(buildDiligenceScene, start)=[] + une case z1 dans reachableCells) est ajoute dans floorplan.test.ts et passe (9/9). Harnais sur la scene meublee = 0/37.

## Restes

- Patron de garde generalisable a toute carte (doc map-authoring) -> RAS : mapQC + le garde de floorplan.test.ts sont le patron reutilisable, deja documente au § harnais QC de docs/map-authoring.md (#778)

## Réfutation

verdict: CONFIRMÉ
Le deuxieme item du DoD (reachability jouable comptant le blocage par props par etage) etait DEJA satisfait par #791 (entityBlockedAt z-aware dans isWalkable) — prouve par le harnais qui, sur la scene meublee, comptait 4 pieces sealed par le mobilier same-floor (donc il VOIT bien le blocage par props). Le garde de scene qui ECHOUE si une piece nommee est isolee est en place (floorplan.test.ts), verrouille La Diligence a 0 isolee.
