# Solde #507 — l'assembleur d'extraction n'émet plus de sections manuelles d'état (2026-07-16)

VERIFIE: relancé moi-même les gardes dans le worktree (citation-graphy-guard exit 0 — 4 classes à zéro dont la nouvelle prose d'état ; test:raw 144/144 ; test:hooks 88/88 ; docs:check 3 étages OK) ; relu le rendu du codeur : fixture 2-topics avec `codeMap` + `faithful:false` volontairement présents en entrée → la sortie assemblée ne contient ni « Carte code → règle » ni « Bilan de fidélité » ni claim d'état, champ `**Implémente :** (non implémenté)` en placeholder nu début de ligne, `parseFiche` y détecte 2 champs / 0 anomalie ; grep de contrôle des deux fichiers du pipeline (aucun générateur `Carte code`/`Bilan`/`codeMap` restant) ; suite complète 909 fichiers / 10 490 tests verte, typecheck 0, lint 0.

## Restes
- Le comportement des AGENTS de synthèse du workflow (respect du placeholder STRUCT) n'est vérifiable qu'à la prochaine extraction réelle -> RAS : toute fiche fraîche dont le champ dévierait du placeholder diverge du généré et fait échouer `build-implemente --check` (docs:check, CI + pre-commit) — la classe est gardée en aval quel que soit le comportement amont.
- Champs `faithful`/`issues` subsistent dans le retour du workflow (métadonnée de vérification RAW, plus consommée par l'assembleur) -> RAS : sans effet sur la sortie, utile au juge de fidélité ; n'est pas un claim d'état d'implémentation.

## Réfutation
verdict: CONFIRMÉ
La passe adversariale de la fermeture #434 (juge, même session) avait elle-même signalé ce vecteur (« l'assembleur régénère la maladie ») — la présente vague le ferme ; j'ai attaqué la fermeture sur pièces : fixture d'assemblage avec les champs interdits VOLONTAIREMENT présents en entrée (ils ne fuient pas), grep des deux fichiers du pipeline, et vérification que le filet aval (`build-implemente --check`) attrape le seul maillon non testable (agents de synthèse). Le DoD de #507 (« l'assembleur n'émet plus ni Carte code → règle ni claim d'état ; une extraction d'essai le prouve ; docs:check vert ») est couvert : l'extraction d'essai = l'assemblage de la fixture (le workflow multi-agents complet serait une dépense sans gain de preuve, le maillon agent étant gardé en aval).
