# Solde #996 — 2026-09-08

VERIFIE: le DoD du ticket confronté à l'arbre f87b17867 + train E (worktree `.wt-gates-A`, `git status` vide) et rejoué sur `.wt-gates-B1` (c4a0854cf) — (1) `src/state/state-purity.test.ts` est ABSENT de HEAD (`git show --stat c4a0854cf` : 80 lignes supprimées, plus aucune allowlist par nom de fichier nulle part : la relation `src/state → src/ui` vit dans `eslint.config.js` sous `no-restricted-imports` core avec `allowTypeImports: true`, le type-only passe par PARSING, aucune liste) ; (2) mutation du DoD rejouée ce soir : `src/state/revealStep.ts:10` passé de `import type { IconId } from '../ui/icons'` à `import { IconId } from '../ui/icons'`, `npx eslint src/state/revealStep.ts` → exit 1, « '../ui/icons' import is restricted from being used by a pattern. Pureté de couche (#1709, CLAUDE.md règle 3) : src/state n'importe rien de src/ui à l'EXÉCUTION … no-restricted-imports » ; fichier remis par `git checkout --`, arbre propre ; (3) l'angle mort restant est ÉNONCÉ : `src/eslint-purete-de-couche.test.ts:27-28` (« Reste hors de portée, comme pour toute police d'accès : la recopie qui ne passe par aucun import, et `require()` ») et le docblock d'`eslint.config.js:60-75` dit ce qui passe (formes élidées à la compilation, `TSImportType` inline) et pourquoi deux règles ; (4) le filet `src/eslint-purete-de-couche.test.ts` mesure la config RÉSOLUE (`calculateConfigForFile` + `lintText`) sur 13 fichiers réels et 12 formes — un bloc qui ne matche plus rien rougit en nommant la couche.

## Restes
- Allowlist indexée par FICHIER (`revealStep.ts`, `combatManeuvers.ts`, `roster.ts`) dans `state-purity.test.ts` -> corrigé par c4a0854cf eslint.config.js:83
- Un import RUNTIME de `../ui/` dans un fichier blanchi type-only passait -> corrigé par c4a0854cf eslint.config.js:100
- L'angle mort restant de la garde à ÉNONCER dans son docblock -> corrigé par c4a0854cf src/eslint-purete-de-couche.test.ts:27
- `require()` et la recopie sans import restent hors de portée -> RAS : limite de toute police d'accès par imports (doctrine « police d'accès aveugle à la recopie »), énoncée au site cité ci-dessus, aucun lint ne la ferme
- Trois relations à allowlist vide (`engine → state`, `engine → ui`, `state → gameIso`) acceptent désormais le type-only -> RAS : 0 site mesuré pour chacune, le contrat devient la pureté d'EXÉCUTION que les titres des trois tests disaient déjà (« importe RUNTIME »), jugé et dit au message de c4a0854cf ; refuser le type-only serait un trompe-l'œil, l'inline `import('x').T` équivalent passant partout
- `src/data` hors du périmètre ESLint : `data-purity.test.ts` reste un test vitest -> RAS : périmètre de la config (`ignores: 'src/data/**'`), nommé au message de c4a0854cf ; la garde vitest y tient le même contrat

## Réfutation
verdict: CONFIRMÉ — juge de DESIGN C3b (2026-09-07, lecture seule) : « pureté → lint » TIENT sous trois conditions, toutes portées par c4a0854cf (doublage `no-restricted-syntax` pour l'import dynamique que la règle core ne visite pas ; filet sur la config RÉSOLUE ; chaque message porte la réf) ; juge de DIFF C3b-2 (2026-09-08, lecture seule sur `.wt-gates-B1`) : TIENT — config résolue sur 13 fichiers réels, table de 12 formes rejouée, mutation « bloc muet » rouge ; un BLOQUANT de rendu (« 3 relations relâchées type-only, pas 1 ») corrigé au message et re-jugé ; c'est ce juge qui a constaté que #996 était soldé PAR CONSTRUCTION (sa définition : « n'exempter QUE les `import type`, vérifiables par parsing, aucune liste ») et exigé sa fermeture. Mutation du DoD (`revealStep.ts:10` → import runtime → rouge) rejouée par l'orchestratrice le 2026-09-08 à 23 h 39, sortie citée dans VERIFIE. Non joué par les juges : la suite complète et les gates — jouées avant le push de c4a0854cf (22/22 en 474,6 s, CI verte).
