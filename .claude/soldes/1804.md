# Solde #1804 — ops:publier — l'étape de pilotage passe par GraphQL (`gh issue view --json`, publier.mjs:1040), refusé HTTP 403 en session Claude Code : le commentaire de ticket est posé à la main — 2026-09-18

VERIFIE: les deux appels du ticket sont morts et leur remplacement est éprouvé sur l'API RÉELLE, pas seulement en test. Sonde de la PRÉMISSE, rejouée dans ce conteneur : `gh issue view 1804 --repo cgauche/game --json state,comments` → « HTTP 403: GitHub GraphQL is not available from Claude Code sessions; use the REST API », et `gh api graphql -f query='query{viewer{login}}'` rend le même refus — donc aussi `gh issue list`, `gh label list`, toute sous-commande CLI servie par GraphQL. Sonde de la ROUTE NEUVE, `lireTicket` appelée avec le vrai `gh` sur quatre numéros : `#1803 ok=true etat=closed commentaires=2` (dernier corps = le solde de #1803, le train l'avait posé à la main), `#1804 ok=true etat=open commentaires=0`, `#1813 ok=true etat=open commentaires=0`, et `#235 ok=false raison=#235 est une pull request, pas un ticket`. La pose, elle, a déjà fait ses preuves : le commentaire de pilotage de #1803 a été posé le 2026-09-18 par exactement l'argv que `poserCommentaire` construit (`gh api repos/<o>/<r>/issues/<N>/comments -X POST -F body=@<fichier>`). Le cliquet de source a été éprouvé À L'EXÉCUTION sur onze variantes appendues à une COPIE de `publier.mjs` puis rejouées par `node --test` — sept qui doivent mordre (`gh(['issue','view'])`, la même en doubles quotes, `gh([\`api\`,\`graphql\`])`, `gh(['project','item-list'])`, `gh(['pr','view'])`, la fermeture REST `-X PATCH -f state=closed`, sa variante `--method PATCH`) et quatre qui doivent passer (`const LABELS = ['label','bug']`, un commentaire de FIN de ligne, un bloc `/* … */` multiligne, une ligne neutre) : 11/11 conformes, 0 écart. Gates sur l'arbre final : `test:ops` 268/268, `test:hooks` 1014/1014, `test:agents` 24/24, `test:docs` 83/83, `lint` rc 0, `docs:check` rc 0, `agents:check` rc 0. La suite Vitest n'est pas rejouée LOCALEMENT et le dit : `vite.config.ts:71` borne `include` à `src/**`, `server/src/**` et `scripts/map/**` — aucun fichier de ce diff n'y entre ; la CI de branche la joue et `ops:publier` attend son verdict avant le fast-forward.

## Restes
- La CLASSE dépasse `publier.mjs` : `scripts/ops/board.mjs:527,735` (`gh api graphql`, Projects v2 — API GraphQL-SEULEMENT, donc SANS route de remplacement), `scripts/ops/fermer-depuis-main.mjs:115,129` et `scripts/ops/fermetures-non-citees.mjs:136` (`gh api --paginate`, refusé dès la page suivante), tous trois derrière un `npm run ops:*` que `CLAUDE.md` § Pile et commandes présente comme un geste de session ; `signaler-rouge.mjs` et `deps-report.mjs` en sont ÉCARTÉS, mesurés CI-seulement (`deps-report.yml:40`, `canari.yml:244`) -> #1813
- `.claude/skills/orchestrer-des-agents/SKILL.md` prescrivait à TOUT agent `gh issue view <N> --json title` pour déréférencer un pointeur — la commande même que ce ticket prouve refusée -> corrigé dans ce commit (.claude/skills/orchestrer-des-agents/SKILL.md:62)
- `.claude/credo.md` faisait foi sur `gh label list` pour dédupliquer par label, refusé HTTP 403 comme les autres (mesuré) -> corrigé dans ce commit (.claude/credo.md:11)
- Le CÂBLAGE de l'étape `pilotage` reste non couvert : `jouer` appelle `lireTicket(numero, racine)` sans passer la couture `appel`, là où `ci`/`push-branche`/`ff-main` sont exercés -> RAS : défaut PRÉ-EXISTANT — l'étape n'était pas couverte avant non plus — et l'ouvrir demande d'injecter dans `jouer` une couture qu'aucune des neuf étapes ne porte, geste de socle qui déborde ce ticket
- `docs/raw/reconciliation.md` se réécrit à chaque `docs:build` POSIX (chemins Windows committés) : sorti du commit, jamais poussé -> RAS : défaut PRÉ-EXISTANT déjà porté par #1801, dont le titre le décrit à l'octet ; l'y router une seconde fois créerait un doublon

## Réfutation
verdict: CONFIRMÉ après réfutation — juge adversarial de DIFF (2026-09-18, lecture seule, arbre épinglé `b345bf37a`) : **un BLOQUANT vérifié et quatre mineurs, tous corrigés avant commit**. BLOQUANT — la migration AVEUGLAIT le cliquet « la SOURCE du train ne porte AUCUN geste de fermeture » (`publier.test.mjs:412` d'alors) sans le remplacer : ce cliquet ne reconnaissait que la FORME `gh issue close`, que la migration rend de toute façon impossible, pendant que la fermeture s'écrit désormais `gh api <chemin> -X PATCH -f state=closed` — forme déjà cataloguée dans le dépôt (`scripts/hooks/solde-ticket-guard.test.mjs:1815`) et vue par AUCUN des deux cliquets, sonde à l'appui. Le juge a de plus percé le cliquet neuf sur trois contournements (doubles quotes, littéraux de gabarit, `gh project` — GraphQL-only, non couvert) et l'a pris en défaut sur trois faux positifs (`['label','bug']`, commentaire de fin de ligne, ligne de bloc sans `*` : la neutralisation maison ne traitait que les lignes ENTIÈREMENT commentaires). Correction : les DEUX cliquets fusionnent en un CONTRAT POSITIF sur l'argv, lu sur `codeSeul` (`scripts/guards/lib/commentPoison.mjs:210`, la primitive du dépôt pour « le code sans ses commentaires ») au lieu d'une regex maison — `argv[0]` doit valoir `api`, `argv[1]` ne peut pas valoir `graphql`, toute méthode explicite doit valoir `POST`, et aucun argument ne porte `state=closed` ; une borne `argvs.length >= 3` refuse qu'un extracteur cassé rende le cliquet vert en ne lisant plus rien. MINEUR 3 — la pagination se bornait au compteur `comments` du ticket, LU DANS UN APPEL ANTÉRIEUR : sonde du juge, 101 commentaires réels dont le 101ᵉ porte la marque contre un compteur à 100, « corps lus : 100 · marque trouvée : false · le train REPOSTE : true ». Correction : c'est la LISTE qui dit où elle s'arrête (une page incomplète est la dernière), le compteur n'est plus consulté du tout — une source de vérité au lieu de deux — et `PLAFOND_PAGES` ne sert que d'anti-emballement, son dépassement REFUSANT la lecture plutôt que de la tronquer. MINEUR 4 — `repos/<o>/<r>/issues/<N>` sert AUSSI les pull requests (`gh api repos/cgauche/game/issues/235` → `{"pr":true}`) là où l'ancienne route refusait : `lireTicket` refuse désormais une charge portant `pull_request`, avant toute lecture de commentaires. MINEUR 5 — la classe hors `publier.mjs` est routée (#1813) et ses deux prescriptions VIVANTES corrigées ici. Griefs explicitement essayés par le juge et ABANDONNÉS, cités pour ne pas les rejuger : `-F body=@<fichier>` (RÉFUTÉ à la source de `cli/cli` 2.45.0, `pkg/cmd/api/fields.go:154-161` — la branche `@` lit le fichier AVANT toute conversion de type et rend une chaîne, jamais un base64 ; `-f` aurait envoyé le littéral `@/tmp/…`) ; « le compteur `comments` ne compte pas la même chose que `/comments` » (RÉFUTÉ par mesure sur #1680 30/30, #1388 23/23, #1736 7/7) ; « une page demandée en trop est une erreur » (RÉFUTÉ : `page=99` → rc 0, `[]`) ; injection d'argv par `numero` (RÉFUTÉ : `numerosCites` rend `String(Number(...))`, `gh()` spawne sans `shell: true`) ; équivalence de `etat` (RÉFUTÉ : REST rend `open`/`closed`, GraphQL `OPEN`/`CLOSED`, `.toLowerCase()` s'applique des deux côtés — les TROIS décisions de `jouer` sont préservées à la lettre) ; poison de commentaire dans les blocs neufs (RÉFUTÉ par exécution de `commentPoison.mjs` : `{"tombes":[],"excuses":[],"vocab":[]}`) ; exports morts (RÉFUTÉ : `knip-exports-ratchet` rc 0, « aucun nouveau ») ; duplication d'une primitive REST existante (ABANDONNÉ : `fermetures-non-citees.mjs:146` et `ruleset-main.mjs:80` bâtissent leur chemin avec leur propre `gh` sous des contraintes différentes — `execFileSync` de runner contre `spawnSync` borné en temps ; extraire ici serait du couplage, et c'est précisément le geste que #1813 porte).
