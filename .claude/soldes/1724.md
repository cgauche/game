# Solde #1724 « Suite jsdom : skills-screen-attention rougit sous charge (React « Should not already be working », act() d'un fichier voisin en vol) » — 2026-09-12

VERIFIE: l'attendu du ticket (le mutateur nommé et corrigé au site — un `unmount` manquant, un `act` non attendu — et la barrière de `test-setup.ts` REFUSE une racine laissée montée, rouge chez le coupable, pas chez la victime) confronté au diff du worktree `.wt-gates-A` sur `ae8ae88e0` — grounding mesuré : la barrière DOM existante n'observait que `document.body.children` et ignorait une racine `createRoot` montée sur un conteneur jamais attaché et tout état `act()` ; le dépôt monte tout à la main (aucun `@testing-library/react`, quatre-vingts `createRoot(` dans quarante-quatre fichiers) ; les sentinelles `[diag]` sont des greps de sortie qui comptent des lignes sans nommer de fichier ; la victime `skills-screen-attention.test.tsx` est elle-même propre ; BARRIÈRE posée dans le socle (`src/test-setup.ts`) : compte pris sur le prototype des racines react-dom (`render` inscrit le fichier courant, `unmount` le retire, import paresseux pour la partition `node`, racine-sonde jamais rendue), verdicts purs `messageRacineMontee`/`messageActEnVol`, lecture et VIDAGE du registre en tête de l'`afterEach` avant toute barrière qui peut lancer (bloquant du juge : sinon le test suivant du même fichier était accusé — témoin forgé + mutation du `clear()` → deux rouges au lieu d'un, remise par hash), instrumentation FAIL-LOUD si `ReactCurrentActQueue` est introuvable (nomme `react.version` et ce qu'il faut recâbler ; banc de câblage réel : un `act(async)` non attendu ouvre la file, attendu la ferme), l'enveloppe de `React.act` pour nommer l'ouvreur MESURÉE IMPOSSIBLE (`TypeError: Cannot redefine property: act` sur l'objet de module servi par vitest — banc qui verrouille cette portée) donc le volet « act en vol » dit au JSDoc qu'il ne nomme que le fichier courant et se déclare UNE fois par file ouverte ; mesuré aussi : le registre est par FICHIER (vitest ré-évalue `setupFiles` par fichier même sous `isolate: false`) ; angles morts dits au code : `hydrateRoot` (prototype distinct, zéro usage), racines montées en `beforeAll`/corps de module (non comptées sur le premier fichier, effacées ensuite) ; COUPABLES nommés par la barrière sur la suite complète — exactement les cinq lignes du stock de résidus DOM `scripts/guards/lib/domResiduStock.mjs` (fuite de nœud et fuite de racine sont le même défaut vu par deux bouts) : `SeatAssignmentsField.test.tsx` (aucun `afterEach`), `Inspector.test.tsx` et `editor-enregistre-repasse-parseProject.test.tsx` (démontages partiels), `CharacterSheet.test.tsx` et `RollLine-second-read.test.tsx` (un porteur `root` unique réassigné) — CORRIGÉS par UN helper `src/monterRacine.testkit.ts` (convention `*.testkit.ts` mesurée ; `monterRacine` retient la liste des montages, `demonterRacines` les rend tous en ordre inverse ; pas d'`afterEach` propre, raison dite), plus aucun `createRoot` ni démontage manuel résiduel dans les cinq (juge), assertions intactes ; les cinq lignes quittent le stock, `MAX_DOM_RESIDU` 12 → 7 en ÉGALITÉ ; test de la barrière `src/racine-montee-barriere.test.tsx` sans cardinal sur le registre partagé (`toContain`) ; preuve par MUTATION : témoin qui rend une racine sur un conteneur détaché sans démonter (invisible à la barrière de nœuds) → rouge nommant son fichier ; instrumentation débranchée → le témoin passe et le test de la barrière rougit ; suite complète VERTE (`React coincé 0` contre 27, `act hors act` 1574 → 395), deux passes à `WFRP_TEST_COEURS=16` à mémoire pleine sans reproduire le rouge d'origine ; deux gardes rougies par le geste et recalées sans les affaiblir (`comment-poison-guard`, `vi-mock-isolate-guard`) ; dix fichiers vitest, eslint et `typecheck:fast` rejoués par l'orchestratrice ; juge de diff : TIENT SOUS CORRECTIONS, trois bloquants et les souhaitables portés.

## Restes
- Le rouge d'origine (« Should not already be working » chez `skills-screen-attention`, vu une fois sous charge) -> RAS : non reproduit sur deux passes à pleine charge après le lot ; la cause de classe (racines laissées montées par cinq fichiers, réveillées pendant un voisin) est nommée par la barrière et fermée au site, et toute récidive rougira désormais chez le fichier qui rend
- Le volet « act en vol » ne sait nommer que le fichier COURANT (l'objet de module `react` servi par vitest n'est pas redéfinissable, mesuré) -> RAS : dit au JSDoc, déclaré une fois par file ouverte, un banc verrouille cette portée (si l'interop change, il rougit et l'ouvreur redevient nommable)
- Treize avertissements « act hors act » subsistent, tous dans `src/ui/CharacterSheet.test.tsx` -> RAS : ce sont des mises à jour hors `act` signalées par la sentinelle du lanceur (avertissements React), pas une file d'`act` ouverte ni une racine laissée montée — hors de l'objet du ticket, comptés par `[diag]` à chaque run
- `src/vi-mock-isolate-guard.test.ts` lit le socle par le littéral `beforeEach((` -> RAS : l'instrumentation vit dans un hook séparé enregistré en premier, le hook synchrone reste intact ; la sonde reste à casse d'octets, dite au rendu
- `hydrateRoot` et les racines montées en `beforeAll`/corps de module échappent à la barrière -> RAS : angles morts DITS au JSDoc, zéro usage de `hydrateRoot` mesuré

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 2026-09-12) : câblage réel de l'instrumentation vérifié (test qui monte une vraie `createRoot` détachée), cinq sites migrés sans résidu, stock 12 → 7 exact, 8/8 fichiers verts ; TROIS BLOQUANTS mesurés — registre non vidé quand la barrière de nœuds lance avant (rouge chez le suivant), détecteur d'`act` muet si l'interne React disparaît (sonde : `current` = `[]` après un `act(async)` non attendu, `null` au repos), volet « act » nommant la victime — portés par le codeur (vidage inconditionnel prouvé par témoin + mutation, fail-loud + banc, portée dite + déclaration unique après mesure de l'impossibilité) ; souhaitables portés (`.testkit.ts`, `toContain`, égalité du plafond, angles morts `hydrateRoot`/`beforeAll` dits) ; le claim « section RACINES MONTÉES du lanceur » corrigé (sortie de sonde du codeur, pas du lanceur). Non joué par le juge : la suite complète (jouée par le codeur : verte) et les gates — jouées sur la tête du lot avant le push.
