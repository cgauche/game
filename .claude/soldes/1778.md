# Solde #1778 « MapSpec : wallAppearances — une grille walled ne sait poser qu'une STRUCTURE là où elle veut un LOOK » — 2026-09-17

VERIFIE: le DoD relu clause par clause — (1) « `wallAppearances` existe dans `MapSpec`, compilé par `buildScene`, et une grille de test rend des `WallSeg` avec `appearance` posée, `structure` absente » : livré sous la forme GÉNÉRALE jugée (design FRAGILE → cinq corrections, commentaire 5719235492) `MapSpec.wallLegend: Record<char, WallOverlay>` — un char d'arête écrit `structure` et/ou `appearance` (`WallOverlay` sur `WallSeg`, `state/scene.ts`, liste unique `WALL_OVERLAY_KEYS` : N+1 propriété = deux lignes, type + `wallSegSchema`) ; golden `state/mapSpec.test.ts` (trois formes d'overlay en ensembles, apparence seule ⇒ `structure` absente) ; `wallStructures` (0 scène) supprimé sans alias ; (2) « `docs/map-authoring.md` ne liste plus `wallStructures` NI `wallAppearances` parmi les champs sans exemple mesuré » : `wallLegend?` y porte son exemple mesuré (`src/scenes/opera/floorplan.ts`) et la liste des orphelins ne cite plus que `music?`, `knownUnsupportedFloor?`, `seatAssignments?`, `restZones?` ; la doc suit désormais les `extends` d'une interface (`champsInterface`, `WallSpec` aurait perdu ses deux champs hérités) ; (3) `npm run docs:check` vert. Au-delà du DoD, la classe entière : `zonesFromSeeds` reçoit la même table (sinon le zonage fuit — contrat général `state/asciiMap.test.ts`, câblage de l'opéra testé), validation d'ids à la compilation `assertAuthoredIds` (arêtes, `cells`, `elevate.parapet`, `wallLegend` ; auparavant un `console.warn` DEV au rendu, rien en test ; 40 scénarios + campagne + vitrine + opéra se construisent), round-trip `sceneToAscii` sur l'overlay entier (l'apparence posée au `<select>` de l'Inspector survit à l'export→réimport, perdue en silence auparavant ; diagonale porteuse nommée), `edgeWalls` mort supprimé. Scène du dépôt : l'opéra, seule scène `walled` — `OPERA_WALL_LEGEND = { w: { appearance: 'mur-en-bois' } }` passée à tous les lecteurs des deux grilles, 57 refends entre loges voisines des deux flancs de l'étage en bois (cloison en L 32|31 comprise, trou de trois arêtes levé par le juge), look d'authoring maison révisable (le folio 39 ne donne aucun matériau), tout le reste du bâti en mur nu (contrat #1180 réécrit en positif, témoin `wall:0,1,E,1` nu). Gates locales (régime machine du 2026-09-17 : périmètre seul, la CI joue le reste) : typecheck 0, périmètre 11 fichiers / 380 tests verts, `map:check opera` 0 partout, `docs:check` 0. Recette au kit CDP (port 5243, scénario `opera-plan`) : 57 arêtes `mur-en-bois` lues sur la scène, trois captures, console vide. Juges : design FRAGILE (5 corrections), diff train 1 TIENT (1 bloquant doc + 4 souhaitables levés au train 3), diff train 2 FRAGILE (cloison en L levée au train 4) ; commit `069f34c5b` (sha local, réécrit à la publication).

## Restes
- Cloisons diagonales `/`/`\` : `patchWall` admet une `appearance` sur une diagonale mais la grille `walled` n'a aucun char de légende pour une diagonale (`parseWalledAscii`, `state/asciiMap.ts`) — l'export la nomme désormais perdue (warning + `notRestored`) au lieu de la taire -> inventaire #1680 : légende d'arête des diagonales, un site, aucune scène n'en pose
- Souhaitable du juge non retenu : `puitsRim` reçoit la table sans la lire (son flood ne lit que `tiles`) -> RAS : choix consigné au JSDoc de `OPERA_WALL_LEGEND` (aucun lecteur de la grille sans table), câblage inerte mais uniforme

## Recette visuelle
capture: public/qc/soldes/1778-opera-plan-loges-gauche-z1.png — le groupe dans une loge du flanc gauche (5,13,z1) : refends de loge en planches brunes, enveloppe et refend côté couloir en mur nu, parterre visible sous le puits
capture: public/qc/soldes/1778-opera-plan-loges-droite-z1.png — flanc droit (38,13,z1) : cloisons de loge en planches contreventées (`mur-en-bois`), garde-corps du puits, enveloppe nue
capture: public/qc/soldes/1778-opera-plan-galerie-nue-z1.png — galerie (21,44,z1) : témoin sans légende d'arête, tous les murs en mur nu

## Réfutation
verdict: CONFIRMÉ — Tentée sur pièces : (a) « le look est un `wallAppearances` déguisé, la classe n'est pas corrigée » — une seule table, un seul type `WallOverlay`, parse/recopie/export itèrent sur `WALL_OVERLAY_KEYS` ; ajouter une propriété = la liste + le schéma zod, aucun site énuméré (grep) ; (b) « le repli `-`/`|` du round-trip perd l'apparence » — sonde du juge sur une grille sans arête nue avec collision de glyphe : 17 segments identiques avant/après ; (c) « `assertAuthoredIds` rougit une scène existante » — 40 scénarios + campagne + vitrine + opéra construits, 0 KO ; (d) « la loi des refends en bois a des trous » — le juge en a trouvé trois (cloison en L 32|31), levés ; re-sonde : plus aucune arête nue non-porte entre deux pièces de loge, 57 chars `w` = 57 arêtes = 57 éléments rendus en bois (faces `structure` de `buildWalls`, méthode indépendante) ; (e) « le zonage a bougé avec l'ASCII » — diff de chars `|→w` 5 et `-→w` 52 seulement, rez identique, 25 pièces et cases par pièce inchangées ; (f) « la recette n'est qu'une lecture d'état » — `__wfrp.goto` observe, le rendu est capturé à trois positions et relu à l'œil (planches contreventées vs mur nu), console vide ; (g) « les restes sont plus d'un » — zéro ticket routé, un inventaire sur l'épic de socle, un RAS consigné.
