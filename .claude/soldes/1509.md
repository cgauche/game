# Solde #1509 « SOCLE empreinte DÉRIVÉE des props volumiques » — clôturé le 2026-09-26

VERIFIE: mesuré le 2026-09-26 par l'orchestrateur sur la branche `chantier/1509`.

**Gates** : `npx tsc --noEmit --incremental false` → exit 0 ; `npx vitest run` → 1736 fichiers verts sur 1737 (24269 tests) ; seul rouge, une ligne du stock `désormais` rendue périmée par la réécriture du commentaire de `ui-ratchets.test.ts`, purgée (`legacyVocabStock.mjs`), puis `comment-poison-guard` + `ui-ratchets` verts (132 tests) ; `npm run docs:check` → exit 0 sur l’index du commit.

**Le DoD, par lot :**
- Lots L1 à L8 : trains A, B et C de la vague « décor 3D », tableau du commentaire d'audit de #1509 du 2026-09-26, puis soldes 1 à 6.
- **L1-L3 (LdV mémoïsée, empreinte locale partagée, `propFootTiles(+facing)`)** :
  - l'empreinte d'un meuble volumique est DÉRIVÉE de son corps tourné par `empreinteDuProp` (`src/data/props.types.ts`) ;
  - son cache est une WeakMap recette × places × `cap|mpt` (`props.types.ts:395-410`), testée à `props-integrity.test.ts:410` ;
  - `gameiso-purity` interdit l'appel à `buildPropVolumes` depuis `state`.
- **L4 (dérivation active, `span` au cap)** :
  - `OrigineDecor` est une union discriminée : seul un décor d'ENTITÉ porte `entId` et `span` ; la branche non-entité les ferme par le type (`span?: never`) ;
  - le halo et le chrome ne relisent plus aucun `span` hors de l'union.
- **L5 (contrats de catalogue)** : la liste blanche `IDS` est morte, et le contrat corps/empreinte est écrit en positif.
- **L6 (Palette et Inspecteur)** :
  - l'éditeur montre le contour de sélection et la marque de sol sur l'empreinte entière (`entiteFootTiles`, `src/state/spawn.ts`) ;
  - le chip Empreinte donne 2×1 ou 1×2 selon le cap ;
  - le monde 3D est plein cadre à FROID, car toute classe de mise en page du plateau tient par la feuille de son RENDEUR (table `MISES_EN_PAGE`, `src/gameIso/stage/anim-css-branchee.test.ts:39`).
- **L7 (`SAVE_VERSION`)** : fait au train B.
- **L8 (meuble N+1 à zéro ligne de code)** :
  - `src/gameIso/catalog/meuble-multicase-neuf.test.ts` traverse 12 coutures ;
  - chacune est prouvée par une mutation (tableau du solde 6 : `entityAt`, `seatSlotsOf`, `validatePropCatalog`, `validateScene`, halo, empreinte, occupation, index, picking, ligne de vue, vision) ;
  - `validateScene` et `validatePropCatalog` portent leur PRÉMISSE (le cap `NE` produit l'erreur, `:156` ; les abords posés sur leur siège produisent 3 erreurs).
  - La murale (`529d0405b`) a touché du code : écart déjà consigné au ticket.

**Recette joueur** (kit CDP, console à 0 erreur) :
- pose d'une murale 2×1 à l'éditeur ;
- contour sur 2 cases aux caps S et E ;
- corps visible à froid en Gabarit ET en Isolée ;
- redimensionnement ;
- campagne intacte.

## Restes
- Le manifeste disait que `gameStage` possédait la surface du monde -> corrigé par `src/data/primitives.manifest.json:874` (le concept dit les ANIMATIONS, le fichier est l'unique importeur `MondeDeCampagne`) et `docs/primitives.md` régénéré.
- Tombale « l'échec ne se replie plus … sur un second peintre » -> corrigé par `src/gameIso/stage/SansWebgl.tsx:7`.
- Tombale « anciens voiles … commit C5a » -> corrigé par `src/gameIso/stage/stage-sun-wiring.test.tsx:329`.
- Règles de MISE EN PAGE `.pastille-entite` et `.plaque-nom` restées dans `anim.css`, tenues par le hasard de l'hôte -> corrigé par `src/gameIso/stage/PastilleEntite.tsx:34` et `src/gameIso/stage/PlaquesDeNom.tsx:25` (feuilles propres), gardées par `src/gameIso/stage/anim-css-branchee.test.ts:39`.
- Angle mort `classList.add` / `setAttribute('class')` du scan -> corrigé par `src/gameIso/stage/anim-css-branchee.test.ts:24`. Ces formes sont couvertes et testées fail-closed ; `classList.toggle`, la concaténation et le nom tiré d'une variable sont déclarés.
- Volet « toute classe animée réclamée a sa règle », vide par construction (les classes réclamées sont un sous-ensemble des classes déclarées) -> corrigé par `src/gameIso/stage/anim-css-branchee.test.ts:20` : volet retiré, angle mort DÉCLARÉ hors de portée d'un scan textuel.
- Une seule mutation prouvée sur cinq des coutures N+1 ; `validateScene` sans prémisse -> corrigé par `src/gameIso/catalog/meuble-multicase-neuf.test.ts:156` (prémisse `NE`), avec onze mutations rouges puis vertes.
- Capture `1509-L6-10`, prise avant le correctif du canevas et dont le nom ne le disait pas -> corrigé par `public/qc/soldes/1509-L6-14-corps-a-froid-isolee-cap-S.png` (retirée ; la vue Isolée à froid est committée en L6-14 et L6-15).
- Galerie `public/qc-decor.html`, qui portait encore la règle `.iso-stage` d'`anim.css` -> corrigé par `public/qc-decor.html:6764` (régénérée : seules restent les animations scopées sous `.iso-stage`).
- `docs:check` lisait une LISTE À LA MAIN de feuilles CSS (toute feuille neuve exigeait sa ligne) -> corrigé par `scripts/docs/check-doc-refs.mjs:155` (corpus = toutes les `.css` de `src/`, contre-épreuve rouge puis verte).
- Les feuilles neuves `plaque-nom.css` et `pastille-entite.css` n'avaient AUCUN statut, et le cliquet (xiv) ne voyait que `src/ui`. `anim.css` et `iso-stage.css` étaient à la fois partagées et de primitive -> corrigé par `src/ui/ui-ratchets.test.ts:600` : (xiv) balaie toutes les `.css` de `src/` et refuse le double statut ; `SHARED_CSS_FILES` est dérivé de `FEUILLES_PARTAGEES` ; `plaquesDeNom` et `pastilleEntite` entrent au manifeste avec leur `css`. Trois mutations rouges. Coût assumé : les deux feuilles de primitive de `gameIso` quittent le cliquet de fuite (xiii), comme toute feuille de primitive de `src/ui/styles`, et relèvent de §5.2 de `primitive-owners-guard`, dont les `poseurs` de `gameStage` sont des répertoires.
- Les 6 lignes `désormais` de `src/ui/ui-ratchets.test.ts`, fichier touché -> RAS : lignes préexistantes à la base, stock du lot `L7 désormais` de #1486 (sonde `stock-origine.mjs` : 283/283 préexistantes, 0 neuve).
- Chrono ×297 de la sonde de parité, sans test -> RAS : `gameiso-purity` interdit l'appel qui le coûtait, et la clé du cache est testée (`props-integrity.test.ts:410`).

## Réfutation
verdict: PARTIEL — passe de réfutation de CLÔTURE du 2026-09-26 (un juge, lecture seule, HEAD `6f005fc02`, devenu `44c3cf5a4` après correction du message).

Tiennent :
- les quatre restes de la passe précédente sont soldés :
  - preuve N+1 ;
  - canevas recetté après correctif (contre-épreuve « canevas seul ») ;
  - `01-menu.png` retiré ;
  - `span` fermé par le type ;
- aucun TROISIÈME rendu de `.iso-stage` n'existe hors tests ;
- le test jsdom `editeur-monde-volumique` n'affirme rien sur la mise en page.

Bloquants (tous deux corrigés au solde 6, dans ce commit) :
- le manifeste disait faux ;
- deux tombales avaient été amputées de leur seul motif détecté.

Non bloquants, tous corrigés au solde 6 :
- mutations non prouvées une à une ;
- règles de mise en page gardées par aucun volet ;
- capture trompeuse ;
- galerie générée périmée ;
- deux affirmations excessives du message de commit, rectifiées par amendement avant publication (« cinq mutations », « Gabarit » seul, extension de la garde de poison).

Deux passes précédentes, PARTIEL ×2 (sondes non promues, poison, preuve N+1 incomplète, `span` ouvert, canevas non recetté), soldées par les soldes 2 à 5. La recette du canevas a RÉFUTÉ le test jsdom qui passait sans CSS.

## Recette visuelle
capture: public/qc/soldes/1509-L6-04-selection-cap-S.png
capture: public/qc/soldes/1509-L6-05-selection-cap-E.png
capture: public/qc/soldes/1509-L6-06-canevas-a-froid.png
capture: public/qc/soldes/1509-L6-11-corps-a-froid-cap-S.png
capture: public/qc/soldes/1509-L6-12-corps-a-froid-cap-E.png
capture: public/qc/soldes/1509-L6-14-corps-a-froid-isolee-cap-S.png
capture: public/qc/soldes/1509-L6-15-corps-a-froid-isolee-cap-E.png
capture: public/qc/soldes/1509-L6-13-campagne.png
capture: public/qc/soldes/1509-murale-rot0.png
capture: public/qc/soldes/1509-murale-top.png
