# Solde #1734 « Outillage de recette : realKey(chaîne) vs realKeyDown/Up(objet) et __wfrp.scenario muet sur une scène vide — les deux restes « sans ticket » de 1687.md » — 2026-09-14

VERIFIE: le DoD relu clause par clause contre le diff du worktree `.wt-1732` — (1) UNE forme d'argument pour la famille `realKey*` : la TOUCHE `{ key, code?, windowsVirtualKeyCode?, modifiers? }` (celle qui porte les modificateurs), dérivée en UNE fonction `champsCDP` (`scripts/recette/lib.mjs:923-939`) consommée par `realKey`/`realKeyDown`/`realKeyUp` ; le refus de forme est NOMMÉ (`:926-930`, la forme attendue dans le message ; le juge avait mesuré un `TypeError … reading 'length'` anonyme) ; `realKey(session, 'Escape')` n'existe plus, aucun `typeof === 'string'` ; les 3 appelants migrés (`lib.mjs` `shot`, `perf-scenario.mjs:66`, `intentions-portee.mjs:165` via `frapperTouche`, alias conservé parce qu'il a un appelant) ; `git grep -nE "realKey\((session|s), *'"` sur `src scripts docs` → 0 (rejoué par l'orchestratrice) ; tests `scripts/recette/lib.test.mjs:361-420` (6 neufs : une forme, `char` émis pour un caractère, 5 formes refusées par `/TOUCHE/`, alias = même fonction), mutations (chaîne réacceptée → 3 rouges nommés ; garde retirée → le `TypeError` d'origine), remises à l'octet ; (2) `__wfrp.scenario(id)` (`src/state/devtools.ts:444-470` `runScenario`) JETTE sur un id inconnu (l'erreur porte l'id et la liste des ids) et sur une scène chargée VIDE (`scene` nul ou `scene.entities.length === 0`, la population que lisent le rendu et les sondes `entities`/`visibleCount` ; `loadProject` charge synchronement, `store.ts:2205`, le contrôle lit bien la scène NEUVE), et la mémoire d'onglet ne s'écrit qu'APRÈS les deux refus (`:469` ; écrite avant, `resumeLastScenario()` rejouait en boucle un scénario qui jette — mesuré par le juge) ; les 8 appels par `evaluate` des scripts de recette (`capture-jeu:132`, `perf-scenario:110`, `intentions-portee:128,209`, `hud-clickables:189`, `console-pont-formes:261`, `tons-lumiere-1245:53`) reçoivent l'exception nommée ; `buildApi(scenarios = testScenarios)` (`:440`) est le seam par lequel le test forge un scénario vide dans une liste LOCALE — le tableau importé, compté par `bake-retention.test.ts:245`, `wallIndex.test.ts:44`, `scenarios.test.ts:13`, n'est pas muté ; tests `src/state/devtools.test.ts:533-564`, mutations (throw → chaîne ; mémoire avant chargement ; contrôle de plateau retiré) → rouges nommés, remises à l'octet ; (3) `docs/recette-navigateur.md:147-148,382` décrit la forme unique et les deux refus au présent, `npm run docs:check` vert (un premier rouge : la forme d'appel `realKey()` en backtick est lue comme un symbole de `src/`, retirée) ; `.claude/soldes/1687.md` non réécrit. Runners rejoués par l'orchestratrice : `npx vitest run src/comment-poison-guard.test.ts src/state/devtools.test.ts src/scenes/test-scenarios/scenarios.test.ts` 198/198 ; par le codeur : `node --test scripts/recette/lib.test.mjs` 44/44, eslint 0, `typecheck:fast` 0 ; suite complète et gates jouées sur la tête du lot avant le push.

## Restes
- `scripts/recette/perf-scenario.mjs:66` envoyait `key: 'KeyE'` (un `code` employé comme `key`, ce qu'aucun clavier n'émet) — anomalie OBSERVÉE par le codeur, hors des deux tickets ; le listener lit `e.code` (`src/ui/useGameKeyboard.ts:36`), aucun listener `keypress`/`beforeinput`/`input` dans `src` ne mord le `char` désormais émis -> corrigé dans ce commit (scripts/recette/perf-scenario.mjs:66)
- Aucune recette navigateur rejouée (les scripts `scripts/recette/*.mjs` exigent Chrome + serveur de dev) : la famille est prouvée par test unitaire pur sur session factice, et le changement CDP de KeyE (codes virtuels 0 → 69, `char` émis) n'est mesuré que par le contrat émis -> RAS : le contrat CDP (`key`/`code`/codes virtuels/`char`, ordre `rawKeyDown`/`char`/`keyUp`) est comparé avant/après par le juge de diff ; la prochaine recette réelle (#1505 ou toute livraison d'écran) traverse `perf-scenario.mjs`

- `__wfrp.screen('editor')` charge la scène-FIXTURE et non la scène active (piège documenté `docs/recette-navigateur.md:148`, listé par la revue de palier du 2026-09-13 parmi trois restes d'outillage) — hors des deux restes que ce ticket porte, sans porteur jusqu'ici ; consigné le 2026-09-14 en commentaire comme 5ᵉ piège du harnais `__wfrp` sur le ticket qui porte les quatre autres -> #1296

## Réfutation
verdict: CONFIRMÉ — juge de réfutation du 2026-09-14 (« TIENT » sur les trois clauses du DoD) : clause 1 (`git grep realKey` sur `src scripts docs .claude` : toutes les formes sont la TOUCHE, zéro chaîne nue ; `frapperTouche` préexiste au diff ; binding `pov-turn-r` keyé par `code` `KeyE`, `keybindings.ts:243,248,446` — même déclenchement, code virtuel désormais juste), clause 2 (`devtools.test.ts:536-537` et `:545-563` ; le `throw` remonte au recetteur par `lib.mjs:360-361` sur les 8 appels par `evaluate`), seam `buildApi(scenarios)` (un seul appelant passe un registre — le test ; `window.__wfrp` reçoit le registre réel `devtools.ts:1839` ; `resumeLastScenario` rejoue par la closure du même `buildApi`), clause 3 (`.claude/soldes/1687.md` hors du `git status`). Les deux restes « sans ticket » de `.claude/soldes/1687.md:11` (asymétrie des helpers, silence de `scenario`) sont ceux que ce solde ferme. Angle mort du juge : aucune preuve navigateur.
