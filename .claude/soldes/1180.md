# Solde #1180 « wallApp crénelle par ALTITUDE : tout mur baseH>1 devient fortifié — l'opéra a 1260 merlons intérieurs » — 2026-09-16

VERIFIE: le DoD relu clause par clause — (1) « L'apparence fortifiée (parapet/merlons) ne se déclenche plus par la hauteur seule : elle vient d'une donnée d'apparence du mur (structure/style authoré), la hauteur restant de la géométrie » : `wallApp(seg)` = `appearance` → `structure` → `plain` (`src/gameIso/catalog/structures/index.ts`), le paramètre de cote a disparu de la signature ; la loi façade→`wallApp` n'a plus qu'UN site, `edgeAppearance` (`src/gameIso/builders/roofs.ts`), relu par `wallGeometry` et par les fermetures d'architecture ; `baseH` ne sert plus qu'aux faces et extrémités (`walls.ts`). Mesuré sur les ÉLÉMENTS RENDUS de 118 scènes (vitrine, 89 scénarios, 4 JSON de campagne — sondes du codeur et du juge, méthodes différentes) : seul l'opéra changeait, 462 éléments (420 arêtes nues de l'étage + 42 coutures de nappes) passent `mur-en-pierre` → `plain`, toute autre scène rend à l'identique élément par élément ; les 124 `mur-en-pierre` restants sont AUTHORÉS (structure d'arête, `elevate.parapet`/`cells.wall` du siège : 60 éléments `crest:`). Mutation collée : la cote remise dans `edgeAppearance` fait rougir la garde de l'opéra sur exactement 462 offenseurs. (2) « L'étage de l'opéra rend des murs d'intérieur (lambris/pierre lisse), plus des créneaux » : capture `public/qc/soldes/1180-opera-plan-galerie-z1.png` — loge de flanc à l'étage, murs lisses ocre sans merlon ; le siège garde ses créneaux (capture `1180-siege-explore-remparts.png`, données `crest`). Le troisième chemin de `wallApp` était le SEUL site du dépôt où une cote décidait d'une identité d'apparence (juge de design du 2026-09-16, 5 sondes) ; `fortified` (`defs/structures.ts:27`) reste hors lot : ses 2 porteurs ont une apparence homonyme, le lire serait une tautologie. Design jugé TIENT (juge de design), diff jugé FRAGILE puis FRAGILE sur un seul témoin de couture à id dérivé, recalé en forme stable (juge de diff, deux passages) ; commits de lot `6c1a1cfb9` (noyau) et 8e625f818 (corrections : garde sur les éléments rendus sans cardinal vivant, mutualisation, contrat de `edgeAppearance`).

## Restes
- Une grille `walled` ne sait poser qu'une STRUCTURE (destructible) là où un auteur veut un LOOK : `MapSpec` n'a pas de `wallAppearances` (symétrique de `wallStructures`, `mapSpec.ts:209/686`) — le DoD est atteint sans lui (`plain` = l'intérieur lisse du rez, goût arbitré à #1771) -> #1778
- `fortified` (`src/data/schemas/defs/structures.ts:27-28`, « rendu, pas règle ») n'a aucun lecteur de rendu (seul lecteur : fiche du Compendium, `registry.ts:2009`) ; ses 2 porteurs (`porte-de-ville`, `mur-en-pierre`) ont une apparence homonyme -> inventaire #1680 : champ de rendu sans lecteur, à lire par une apparence ou à retirer avec ses 2 porteurs, jamais par une cote
- Le juge de design avait mesuré 462 éléments changés, le codeur 420 : le chantier avait été taillé sur `02b38d88b`, avant la publication de #1771 (corps de l'opéra, 42 coutures) ; rebasé sur `d5449edb2`, la garde voit les 462 -> RAS : écart de base, pas de mesure ; les deux comptes sont exacts sur leur arbre
- La famille des PIGNONS (`buildRoofs`, matière dans `faces[].material`) est lue par la garde mais VIDE à l'opéra (toit dérivé en croupes, aucune fermeture de comble) -> RAS : la loi est prouvée sur le site unique par le contrat de `edgeAppearance` (`roofs.test.ts`) et « la matière suit le mur, pas la hauteur » (`roofs.test.ts:832`)
- Prose du test de l'opéra : « loges à 4 m » (faux depuis #1771, la galerie est à `ETAGE_M`) -> corrigé par 8e625f818 src/scenes/opera/floorplan.test.ts:17
- JSDoc de `wallCellIndexOf` (`src/gameIso/builders/roofs.ts:826-828`) : cardinal vivant en PROSE (« 65 scènes livrées (37 scénarios + 4 campagnes, 3 143 murs) »), hors du hunk du lot (granularité = le site, fiche `game-garde-exemption-au-site-jamais-au-fichier`) -> inventaire #1680 : classe « mesure chiffrée à la main dans un doc vivant », un site nommé, à remplacer par une garde ou une prose sans chiffre

## Recette visuelle
capture: public/qc/soldes/1180-opera-plan-galerie-z1.png — kit CDP du 2026-09-16 (port 5219, Chromium propre, le navigateur Playwright MCP restant un verrou machine) : `scenario('opera-plan')`, `ready()`, `goto({x:2,y:30,z:1})` : loge de flanc de l'étage, l'allié est dessiné, les murs de l'étage sont LISSES (ocre, plinthe et arase, aucun merlon) ; le rez se voit par l'ovale à droite. `walls()` → `{"(sans structure)":983}` : rien n'est authoré, tout rend `plain`.
capture: public/qc/soldes/1180-opera-plan-salle-verte.png — `goto({x:3,y:4,z:0})` : le rez rend comme à #1771 (banc, table, coiffeuses, murs lisses) — la loi ne change rien sous 1 m.
capture: public/qc/soldes/1180-siege-explore-remparts.png — `scenario('siege-explore')`, `ready()`, briefing fermé par un CLIC RÉEL sur « Terminer » (`clickButtonByText`) : l'enceinte garde ses merlons (chemin de ronde à 4 m, `elevate`/`cells.wall` → `crest`), `walls()` → `{"porte-de-ville":2}` ; console 0 erreur 0 avertissement sur les trois scènes.

## Réfutation
verdict: CONFIRMÉ — passe de réfutation de la fermeture sur pièces (orchestratrice, 2026-09-16) : (a) « le lot dégrade un rempart VOULU quelque part » → mesuré deux fois par méthodes différentes (sonde codeur sur 118 scènes, sonde juge sur scénarios + campagnes) : 0 élément changé hors opéra ; les remparts du siège sont des DONNÉES (`crest:` 60, `elevate.parapet`), capture à l'appui ; (b) « la garde ne prouve rien » → mutation : cote remise → 462 offenseurs nommés, trois tests rouges ; cardinaux vivants retirés des assertions (fiche `feedback-test-jamais-un-cardinal-vivant…`), témoins nommés ; (c) « les 42 coutures de toit sont oubliées » → la garde porte sur `buildWalls` (arêtes + coutures) et `buildRoofs` (pignons), témoin de couture nommé ; (d) « la loi existe encore en deux endroits » → `edgeAppearance` est le site unique, `segAppearance` ne fait plus que résoudre la clé ; grep `!seg.structure && !seg.appearance` : 1 site ; (e) « la recette n'a pas été jouée en joueur » → kit CDP canonique, `__wfrp` pour le setup et l'observation, clic RÉEL sur « Terminer », console 0/0 ; le clavier n'a pas été frappé (rotation de caméra non exposée, #1568) ; (f) « la vague ouvre plus qu'elle ne ferme » → ce lot ferme #1180 et ouvre #1778 : delta 0 ; sur la vague, fermés #1771 + #1180, ouverts #1777 + #1778. Juge de diff (agent Opus, deux passages, 7 lentilles) : 2 bloquants levés au commit 8e625f818, 1 témoin recalé, 9 attaques écartées par mesure (grep du site unique, appelants, cycle d’import, famille des pignons couverte sur fixture).
