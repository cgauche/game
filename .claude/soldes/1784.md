# Solde #1784 — 2026-09-17

VERIFIE: le contrat du ticket (« Un train détaché n'ouvre AUCUNE fenêtre sur Windows, quel que soit le nombre de ses enfants et sans que chaque spawner du dépôt ait à y penser (règle générale, pas N `windowsHide`) : le détachement donne au train une console CACHÉE dont ses enfants héritent […], le pid reste imprimé, le journal reste `node_modules/.cache/publication/chantier_<N>.log`, l'arrêt reste possible par pid ») confronté au code et à des sondes Windows RÉELLES (codeur puis juge, harnais indépendants). Cause mesurée : sous win32 `spawn({ detached: true })` pose `DETACHED_PROCESS` (libuv), le train n'a aucune console et chaque enfant console en alloue une visible — variante d'origine (`spawn` détaché + `windowsHide`, `de6e7de21` `publier.mjs:1041-1045`) : 9 consoles neuves pour 15 commandes (codeur) ; 8 fenêtres Windows Terminal visibles + 3 PseudoConsoleWindow pour 10 enfants (juge, `EnumWindows`+`IsWindowVisible`) ; `windowsHide` est ignoré avec `DETACHED_PROCESS` (variante sans : identique). Correctif au SEUL site de détachement du train, `lancerDetache` (`scripts/ops/publier.mjs:190`) : sous win32 `powershell.exe -NoProfile -NonInteractive -Command "(Start-Process -FilePath '<node>' -ArgumentList … -WindowStyle Hidden -PassThru).Id"` (`:215`) — 0 fenêtre neuve, petits-enfants `git`/`node` compris (juge : `BASE=19 NEUFS=0` ; remesure après corrections `BASE=32 NEUFS=0`), main rendue en 253-328 ms, pid = le NODE du train (`Get-Process -Id` → `node.exe`, parent mort), survie prouvée (train de 17-20 s après la mort du lanceur), `process.kill(pid)` l'arrête, `WFRP_PUBLIER_ENFANT` et `cwd` hérités, journal UTF-8 sans BOM écrit par le train et ses petits-enfants ; branche POSIX inchangée (`detached: true`, `stdio: ['ignore', fdLog, fdLog]`, `unref`). Variantes écartées PAR MESURE : `-RedirectStandardOutput/-Error` retient le powershell 16,14 s ; `detached:false + windowsHide:true` (CREATE_NO_WINDOW) : 0 console mais le train meurt avec son lanceur. Griefs du juge corrigés dans le geste : `citerArgv` (`:154`, PURE, token `CommandLineToArgvW`) cite chaque élément avant la citation PowerShell — sondes du juge rejouées : `arg avec espace` arrive en UN argument (avant : trois), un script sous `dossier avec espace/` démarre et journalise (avant : pid rendu, journal vide = faux vert) ; `filetDuTrainEnfant` (`:238`) branché à l'entrée de `main()` (`:1113`) sur `WFRP_PUBLIER_LOG` passé par le parent (`:1155`) : une chute avant `ouvrirLog` s'écrit dans le journal avec sa ligne `PUBLICATION: rouge moteur` (sonde bout-en-bout, train détaché réel). Tests `publier.test.mjs` 77/77 (`citerArgv` 7 cas `:627`, win32 en contrat de FRAGMENTS `:646`, apostrophe `:674`, chemin à espace `:688`, pid illisible `:708`, POSIX `:715`, filet `:744`/`:762`), mutations rejouées rouges (identité sur `citerArgv` : 4 ; filet débranché : 2 ; branche win32 court-circuitée : 4 ; `detached:false` : 1), scanners de poison à zéro, `windowsHide`/`Start-Process` n'existent plus qu'à `publier.mjs`/son test. `docs/reprise-apres-pause.md:34-35` (« détaché du harnais, et imprime son `pid` et son `log` ») reste vrai. Gates locales limitées au périmètre (consigne machine de l'utilisateur) : lint, typecheck (aucun `.ts` touché) et suites complètes jouées par la CI de branche.

## Restes
- `-ArgumentList` sans citation Win32 : un argument ou un chemin à espace est éclaté, un script au chemin espacé meurt en silence avec un pid annoncé (juge L4) -> corrigé dans ce commit (scripts/ops/publier.mjs:154)
- un train né puis mort avant `ouvrirLog` est invisible sous win32, sa console étant cachée (juge L3) -> corrigé dans ce commit (scripts/ops/publier.mjs:238)
- « Le SEUL site de détachement du dépôt » était faux, `scripts/gates/toutes.mjs:700` détache ses gates sous POSIX (juge L6) -> corrigé dans ce commit (scripts/ops/publier.mjs:175)
- le CÂBLAGE du filet dans `main()` (3 lignes, `:1111-1114`) n'a pas de test unitaire, `main()` n'étant pas testable sans jouer un train -> RAS : couvert par la sonde bout-en-bout du codeur (train détaché réel, chute avant `ouvrirLog` lue dans le journal) et par le contrat de `filetDuTrainEnfant` (`publier.test.mjs:744`)
- dépendance nouvelle sous win32 : `powershell.exe` (Windows PowerShell 5.1) -> RAS : composant système de Windows 11, mesuré présent (`/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe`) ; `pwsh` absent de cette machine, donc le mauvais choix
- « aucune fenêtre sous Windows » n'est pas dit par `docs/reprise-apres-pause.md` -> RAS : la phrase de ce doc est de la prose du générateur (`build-reprise.mjs:377-386`) qui reste vraie ; le fait vit au commentaire de `lancerDetache` avec ses mesures, pas dans une prose qui pourrait mentir

## Réfutation
verdict: PARTIEL — juge de diff (2026-09-17, lecture seule, huit lentilles, sondes Windows jetables au scratchpad) : L1 cause TIENT (fait utilisateur REPRODUIT : 8 fenêtres Terminal + 3 PseudoConsole ; correctif 0 fenêtre, petits-enfants compris) ; L2 survie/arrêt/pid TIENT (pid = `node.exe` orphelin, `process.kill` l'arrête) ; L3 journal FRAGILE (stdout/stderr propres du train perdus dans la console cachée ; chemin nominal complet via `journaliser` et `fdLog` des étapes `:698/:860/:865`) → corrigé (filet) ; L4 citation RÉFUTÉE (argument à espace éclaté, chemin espacé = faux vert mesuré) → corrigée (`citerArgv`, sondes rejouées) ; L5 tests TIENT (contrats positifs, mutations rouges, aucune tombale), oracle exact fragile → refait en fragments ; L6 source unique TIENT, commentaire « seul site du dépôt » faux → corrigé ; L7 poison TIENT (scanners à zéro, doc vraie) ; L8 `powershell.exe` TIENT (5.1 présent, `pwsh` absent). Fermeture PARTIELLE en l'état → les deux bloquants et les deux non-bloquants corrigés dans ce commit, mutations et sondes rejouées sur l'arbre corrigé.
