# Solde #1685 — 2026-09-03 (#1657 trains B3-1, B3-1b, B3-3, épic #1463)

VERIFIE: les deux Tests que #1685 nommait (Résistance des Blessures critiques, `critResistValue` ; cycle quotidien des maladies, `restResistVal`) ne se calculent plus NULLE PART hors `testValue` : `critResistValue`, `resolveAmputation`, `restResistVal`, `calmeVal` = 0 occurrence dans `src/` (`grep`), les nœuds `test` sont FABRIQUÉS (`noeudDeRangee`/`noeudAmputation`, B3-1/B3-1b) ou AUTHORÉS avec leurs ids vérifiés au `Source/` (`symptoms.json` ×3 : Blessé LDB 20 l.145 et Toxine l.212 = Résistance, ver de carie MSRC 16 l.90 = Endurance ; `maladies.json` ×1 : pneumonie EDOC 08 l.104 = Résistance ; Cauchemars LDB 21 l.95 = Calme) et joués par la porte canonique (`routeTriggeredTest` en combat ; étapes de nuit `rollStep` → `monoStep` → `openSequence`/nuit interactive/`runCascadeImmediate` journalisé hors combat) ; un nœud `test` sans `skill` ni `characteristic` ne CHARGE plus (`flowTestSchema.superRefine`, 131/131 nœuds conformes sur `src/data` + `src/scenes`), ne COMPILE plus (`TestIds` en union) et ne se JOUE plus (`rollLine` jette) ; la garde `flowTestEngineRoll` est BLOQUANTE (baseline VIDE) et l'assertion inverse du registre exige un routeur par nœud ; sonde du juge sur le chemin réel (`openRest` → `restSleep`) : héros 2× Sonné + ver de carie → base 40 NUE, cible 30 (« Sonné −20 », « Vers de carie −10 », +20 Accessible) là où la valeur maison donnait 72 ; `crit-test-porte` (vi) : héros 2× Sonné E35 → cible 15 (Accessible : 35) contre 35/55 avant. « Décision utilisateur requise (influence RNG) » RÉFUTÉ par l'utilisateur le 2026-09-02 (« Attends, c'est quoi ce délire avec la creation du ticket 1685, en quoi ca demande une décision de ma part ? ») : le livre dit la règle (LDB 16 l.125 « -10 à tous les Tests »), l'invariance RNG n'est l'invariant que des trains de FORME.

## Restes
- `calmeValue` (`src/engine/psychology.ts:391`, FM + avances de Calme hors `testValue`) survit sur 13 sites de psychologie de COMBAT (Peur/Terreur/Frénésie, `turnHooks`, `rollFlowSpecs`, 2 modales), même moule que `combatFlow.ts:5814` et `combatManeuvers.ts:649/652`, extinction du seam portée par cette session avec #1064/#1096 -> #1437
- Les 3 rouleurs eager de `sleepParty` (`restFlow.ts` : `restRecovery`, `rollContraction`, `nightmareCheck`) roulent désormais à la valeur de la porte mais sans fenêtre -> RAS : dette déjà portée par le ticket ouvert #1101 (déclarés `kind:'dette'` au registre `rollSeamWhitelist`), hors périmètre de #1685 qui ne visait que la VALEUR

## Réfutation
verdict: CONFIRMÉ — juge de design B3 (§1 : 3 calculs hors `testValue` nommés ; cardinal RÉFUTÉ par le juge de diff B3-3 qui en mesure 5 avec `calmeVal`/`nightmareCheck`, morts en B3-3, et le codeur en mesure un 6ᵉ hors lot, `calmeValue`, routé ci-dessus) ; juges de diff B3-1 (11 claims), B3-1b (12 claims, 1 bloquant d'escalade soldé), B3-3 (13 claims : C2 RAW au Source verbatim, C3 valeur de la porte CONFIRMÉE à la sonde sur le chemin réel, C6 fail-open `test: {}` et testkit divergent = 2 bloquants soldés par construction et par foyer unique `applyDiseaseEnd`, C11 cardinaux du rejeu dérivés par la pile = bloquant soldé, C12 `calmeVal` soldé, XOR du refine RÉFUTÉ par le codeur : 5 nœuds RAW portent Compétence ET Caractéristique) ; recettes en joueur B3-1b (amputation, −20 Sonné à l'écran) et B3-2.
