# Solde #925 — 2026-09-11

VERIFIE: l'attendu du ticket (le garde de graphie lit le corpus UNE fois et s'enchaîne dans une gate existante, sous 5 s) confronté à l'arbre du worktree `gates/925` — le CLI joué par `spawnSync` AVANT (`git show HEAD:` déposé dans le worktree) et APRÈS, sortie identique à l'octet sur l'arbre propre (3 622 o) et sur un arbre fautif à neuf familles (4 840 o, exit 1 des deux côtés), durée 17,5 s → 3,0 s (juge) puis 3,9 / 4,0 / 4,2 s après corrections (codeur, machine partagée) ; l'ensemble de fichiers lu extrait du blob HEAD et joué (3 743 + 30, identique) ; `package.json` chaîne le CLI dans `raw:check-code-refs` au dialecte du dépôt et `ci.yml` appelle le script npm (inchangé) ; `node --test` sur `citation-graphy-guard`, `ecrivainsAtteints`, `toutes`, `justificatif` verts (105 tests) ; le juge a muté les douze câblages de la passe un par un et trouvé le seul site non gardé (`detecteUnknownAbbr` × fiches), corrigé et re-muté rouge (50/51) ; le gel du mémo muté → rouge nommé ; pre-commit 0 sur les quatre fichiers ; diff lu par l'orchestratrice ; les gates locales complètes jouées en série avant le push.

## Restes
- `docs/decisions/issues.md:342` (dump GÉNÉRÉ des issues) porte l'état de #925 au moment de son ouverture (« le CLI est ROUGE, 10 folios nus ») alors que le CLI est vert sur l'arbre -> RAS : doc dérivé du registre GitHub, régénéré par son générateur, jamais édité à la main
- Une porte de relâchement du mémo de passe (à l'image de `viderCorpus` de `sourceCorpus.mjs`) n'existe pas : une écriture dans un dossier scanné pendant un même process rendrait un verdict périmé muet -> RAS : le CLI est un process par run et le harnais crée un dossier jetable par cas (`mkdtempSync`), aucun appelant ne réécrit un dossier scanné ; la licéité est écrite au code
- Sur un `check-code-refs` rouge, le rapport de graphie n'est pas imprimé (un défaut par run) -> RAS : propriété de tous les chaînages `node a && node b` du dépôt (`docs:check:brut`), la CI et les gates rejouent après correction
- Le compte « 4 137 lectures / 0 écriture » de l'enregistreur de lectures n'a pas été rejoué par le juge -> RAS : `ECRIT_LU.lit` de la gate porte les préfixes `src/`, `docs/raw/`, `Source/`, `scripts/raw/` qui couvrent tout chemin que le CLI lit (lecture du code par le juge), et `ecrivainsAtteints` cliquète le volet `ecrit`

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 45 min) : l'écart `readCorpus` est un FAIT mesuré par sonde (refus du vide par base, pas de `descendre`, `.d.ts` exclu), verdict à l'octet et ensemble lu rejoués par lui, `ecrivainsAtteints` +1 tracé au chaînage (import préexistant à HEAD, écriture derrière `isMain`), UNE réfutation (câblage `unknownAbbr` × fiches non gardé, site créé par le refactor) corrigée avec mutation rejouée rouge, trois souhaitables portés (mémo gelé, raison du lecteur écrite au code, en-tête au présent).
