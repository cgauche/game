# Solde de fermeture — #1633 (concepts d'ENVELOPPE au lexique, scoping de classerValeur, 2 colonnes) — 2026-09-01

Lot unique (grounding lecteur → design écrit dans mon fil → juge de DESIGN (AMENDER, 4 amendements bloquants) → codeur Opus → juge de DIFF), sur les 4 chemins structures libérés par la session voisine (41409fc3e).

VERIFIE: le DoD du ticket confronté point par point à l'arbre — (1) concepts d'ENVELOPPE au lexique : 4 concepts de strate `Document` (ouverture, cloture, narratif, condition) par NOYAU de clés requises, sans `champs` ni `site` (un concept champ-keyé vole toute forme sous son nom — prouvé par la sonde A promue), signature cible = projection du noyau, portes nommées (`ouvertureSchema`, `clotureSchema`, `formeNarratif` exporté, `conditionSchema`) ; (2) scoping PROPRE de classerValeur : `Contexte.dataset` propagé à `statutDe(site)` (socle réutilisable, 0 changement de comportement mesuré), et surtout les DEUX dérivations séparées (`CONCEPTS_CLASSABLES` toutes strates / `CLES_DE_VALEUR` strate Valeur seule) — sans quoi 44 pions de scène perdaient leur tell de document (sonde C promue) ; (3) portes MULTIPLES nommées par forme : en commentaire de chaque concept, résidu de `condition` NOMMÉ (le noyau `{expr,kind}` ne capte que la variante `flag` — toutes les formes `cond` hors flag restent hors strate, motif `kind` polysémique) ; (4) re-baseline en 2 colonnes SANS table neuve : les 13 lignes captées (12 hors-strate + diligence `ouverture` orpheline « clé réservée ») entrent dans `STRUCTURES_CIBLES` (27→34 : noyau nu ET noyau+… par porte à optionnels, précédent `monnaie`), `PLAFOND_HORS_STRATE` 1157→1145 EXACT (mutation ±1 rouge), orphelines 98→97 lignes (plafond 105→97 — 7 places de mou préexistantes resserrées) ; (5) bumps 1146→1147 / 1147→1149 SOLDÉS (commentaires recalés, pas de tombale) ; en plus : la sonde D de plage-bornes-contrat ne recopie plus un littéral (408 — rouge à HEAD depuis les trains L-ref voisins), elle DÉRIVE la répartition du stock ; mes contre-passes : structures-contrat + slots-contrat + plage-bornes verts, tsc FULL 0, et la liste DÉRIVÉE par grep de tous les tests important structuresStock/slotsStock/grammaireStock rejouée avant push.

## Restes
- DoD-3 « AST des defs → 0 » : 71 lignes STRUCTURES_REDECLARATIONS restent (ce lot n'en retire aucune — vérifié : les 4 concepts n'ajoutent aucune redéclaration) -> #1654
- Résidu de `condition` (toutes les formes `cond` hors variante flag : spells/domains/trappings/traits/etats — `kind` polysémique) -> RAS : NOMMÉ au lexique avec son motif, hors périmètre du ticket (un noyau `{kind}` capterait tout le dépôt — refus mesuré, sonde B)
- Dérive L1d 57→59 (alsoIn creatures/species, stock STRUCTURES_FORMES divergente) -> RAS : dette L1d, portée par #1469 — ce ticket ne la couvre pas (confirmé au grounding : forme connue, cible nommée, lot L1d)
- Renumérotation du doc structures (orphelines §3.15 → §3.19) -> RAS : aucun .md ni garde ne cite « §3.15 » (grep du périmètre), les citations sont par ligne
- deps:exports (knip) aveugle aux exports utilisés dans leur propre fichier (ignoreExportsUsedInFile) — formeNarratif mort passait la garde -> #1655
- Le doc structures-donnees régénéré par le codeur sur l'ARBRE (régénéré SUR L'INDEX par regen5 au staging — l'arbre ne portait que ce lot et le micro-lot #1620 (i), aucun WIP voisin, sa vague `de` code en worktree) -> corrigé dans ce commit
- Rouge transitoire silhouette-corps.test.tsx sous suite complète (14/14 vert seul — charge machine à 100 % de mémoire, classe anti-wedge #1619, pas un défaut de HEAD) -> RAS : flake de charge mesuré, hors de ce lot, la famille #1619 le borne

## Réfutation
verdict: CONFIRMÉ
Juge de DESIGN avant codeur : socle CONFIRMÉ (concept N+1 = une entrée de tableau ; projeteValeur réunit les buckets ; la route « clé réservée » n'avait rien à contourner — mon design décrivait un problème inexistant), 4 amendements bloquants tous exécutés (condition par noyau, dérivations séparées, 1145/97, pas de site sur les concepts, pas de table neuve). Le codeur a ensuite réfuté 4 points du design jugé PAR LA MESURE et je les endosse : call-site AST sans dataset ; résidu de condition bien plus large que « when {kind} ×4 » ; 7 signatures cible et non 4 ; « 28 objets » faux → 44 pions de scène ; + 7 places de mou préexistantes au plafond des orphelines. Juge de DIFF : COMMITTABLE — réfutation ÉCHOUÉE sur le fond : scan complet AVEC/SANS les 4 concepts = formes +13/−0 (ZÉRO vol : `when {rule}` de spells/talents reste `reference`), invisibles −12, orphelines −1 (diligence ouverture), `expr` requis par la seule variante `flag` (mecanique.ts:149) confirmé au Source, double projection = précédent réel `monnaie` ET forcée par l'égalité lexique⇄stock, 44 pions de scène confirmés au chiffre près (le « 28 » du juge de design était faux), cliquets EXACTS (1145/97/34, 7 places de mou purgées), socle neutre (run sans Document = état HEAD à l'identique), 71 redéclarations inchangées, 60 tests poison verts, tsc FULL 0. Un grief soldé (formeNarratif exporté sans consommateur — et `knip` aveugle aux exports utilisés dans leur propre fichier, `ignoreExportsUsedInFile`), deux durcissements pris (sonde D en CO-OCCURRENCE — la présence isolée d'un nombre ne mordait pas sur collision ; sonde « avec/sans » promue).
