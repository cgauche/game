# Solde #1693 — 2026-09-09

VERIFIE: l'attendu du ticket confronté à l'arbre — (1) QUI appelle `bodyPlan` : mesuré — `src/ui/compendium/CreaturePreview.tsx:20-21` (`resolveRender(a?.species, findCreatureById(name)?.traits, name)`), monté par `CodexEntry.tsx:155` et `CodexEdit.tsx:875` dès que `item.appearance` est truthy ; les catégories `mutations`, `traits` et `psychologie` (`registry.ts`) posent un `appearance` SANS `previewRef` → `name` = libellé FR, jamais un id de créature → branche diag de `bodyPlan.ts:190-195` pour les 73 entrées à `appearance` de ces catégories, pas seulement « Pattes d'animaux » ; (2) la DONNÉE n'est PAS à corriger : `mutations.json` (30 `appearance`) et `traits.json` portent des FRAGMENTS de surcouche (`features`/`parts`/`colors`/`eyes`, jamais `species`), consommés au rendu réel par `combatantVisuals.ts:23-39` sur le rig d'un PORTEUR — la COUTURE est corrigée : un fragment se prévisualise sur un porteur par DÉFAUT déclaré en donnée (`registry.ts` `previewPorteur` posé par les builders `traitItem` et `mutations`, foyer `apercuPorteur.ts` `PORTEUR_PAR_DEFAUT = DEFAULT_RACE_ID` importé de `bodyPlan`), `CreaturePreview` reçoit `porteur` et le résout par la déclaration existante (`a?.species ?? porteur`, zéro branche) ; (3) console à 0 erreur sur Mutations (recette), et une GARDE structurelle `codex-apercu-porteur.test.ts` rejoue la résolution de l'aperçu pour TOUTES les entrées à `appearance` de TOUTES les catégories du registre — contrôle positif : `previewPorteur` retiré → 73 rouges nommés.

## Restes
- `psychologie` et `traits` n'ont aujourd'hui AUCUN `appearance` en donnée : leur déclaration de porteur sert l'authoring (aperçu live de `CodexEdit`) et un fragment posé un jour, sans geste -> RAS : le clivage se lit à la donnée, `previewPorteur` n'est posé que sur un item à fragment (0 champ inerte, mesuré)
- Le diag `[bodyPlan]` reste DEV-only et par `diagOnce` : un aperçu monté hors DEV avec un libellé non résolu se tairait -> RAS : la garde structurelle rejoue la résolution en test, indépendamment du canal DEV

## Recette visuelle
capture: public/qc/1693-apercu-porteur/01-codex-mutation-pattes.png
capture: public/qc/1693-apercu-porteur/02-atelier-trait-a-distance.png
capture: public/qc/1693-apercu-porteur/03-codex-creature-loup.png
capture: public/qc/1693-apercu-porteur/03-codex-creature-gobelin.png
capture: public/qc/1693-apercu-porteur/04-codex-psychologie.png
Codex Mutations « Pattes d'animaux » + 5 autres : aperçu rendu sur le porteur bipède par défaut, console 0 erreur (avant : `[bodyPlan] … aucune espèce résolue`) ; Atelier Traits « À distance » en édition : apparence live « (par défaut : Humain) », 0 erreur ; Créatures Loup (quadrupède) et Gobelin (bipède) INCHANGÉES ; Psychologie : aucun aperçu ne se monte, 0 erreur.

## Réfutation
verdict: CONFIRMÉ — grounding (lecteur) : la donnée est canonique, la couture confond entité autonome et fragment porté ; codeur : écart de design assumé et mesuré (le rendu était déjà bon, seul le diagnostic était faux : `resolveRender` retombait déjà sur `DEFAULT_RACE_ID` en criant) ; juge de diff : ACCEPTE-SOUS-CORRECTIONS (rendu créatures inchangé : 628 résolutions et 73 SVG identiques ; `traitItem` lit la déclaration de SA catégorie ; `previewPorteur` seulement sur un item à fragment ; sonde SVG promue en garde) ; recette navigateur VERTE (4 cas, 0 message `[bodyPlan]` sur tout le parcours).
