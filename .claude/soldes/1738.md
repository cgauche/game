# Solde #1738 — 2026-09-16

VERIFIE: le contrat du ticket (« une modification limitée à une fiche documentaire non exécutable ne lance ni suite moteur/UI, ni build du jeu, ni suite complète indirecte via le job de fermeture ») confronté à `main` PUBLIÉE (eedd1892a, rebase du a95c1a9df de branche, + 0dc3cba27) et à deux runs GitHub mesurés — (1) push PRODUIT (le lot lui-même, `ci.yml` touché) : run 35111946613, step `classer` sur le runner « ref=refs/heads/chantier/1738 base=0b3631840 fichiers=55 produit=true — .github/workflows/ci.yml : hors DOCUMENTAIRE », les 24 gates + `migrations:replay` jouées, vert en 625 s (`origin/main` présent au runner, `merge-base` = tête de `main`) ; (2) push DOCUMENTAIRE jetable (branche `chantier/1738-sonde`, une fiche `.claude/memory/env-veille-machine.md` portant `[[fiche-inexistante]]`, contre-exemple nommé AVANT exécution au design v2) : run 35113266971, `classer` « base=0dc3cba27 fichiers=1 produit=false — 1 fichier(s), tous sous DOCUMENTAIRE », steps `test:runner`, `test:recette`, `gen`, `typecheck`, `lint`, `npm test`, `build`, `raw:catalogs`, `raw:*` (8), `server ci`, `server:typecheck` SKIPPÉS (16), rejeu de `migrations` SKIPPÉ avec le job VERT (check requis rendu), `build` ROUGE par `test:hooks` « not ok 212 — ARBRE RÉEL : aucun lien mort dans .claude/memory/ — env-veille-machine.md:15 [[fiche-inexistante]] », `fermetures` non déclenché ; durée du run 2 min 10 s (15:08:09 → 15:10:19) contre 625 s pour le push produit — la régression documentaire représentative EST détectée, sans un seul test produit ni build ; branche supprimée après mesure ; (3) ce solde lui-même est un push documentaire réel (`.claude/soldes/1738.md` seul) : son run est la troisième mesure, portée au commentaire de pilotage. Mécanisme relu en entier par l'orchestrateur : `scripts/gates/classerPush.mjs` (module feuille, base = `merge-base origin/main` hors `main`, `before`/`SHA^` sur `main`, fetch explicite si `origin/main` manque, diff vide ou fichier hors `DOCUMENTAIRE` → produit), polarité `!= 'false'` sur 16 steps sautables + 3 `CI_SEULEMENT` produit + le rejeu de `migrations` (checkout en profondeur 0, `id: install`, jamais `needs: build`), 8 gates jouent toujours parce que leur `lit` chevauche un chemin documentaire (`agents:check`, `test:agents`, `test:hooks`, `test:ops`, `test:docs`, `deps:unused`, `docs:check`, `docs:empreinte`) ; deux `lit` sous-déclarés remesurés (`test:agents` par sonde fs, `docs:empreinte` par lecture — `git ls-files -s`) et la classe gardée (`classerPush.test.mjs` cas c1 sur `corpusParGate`, mutation : 170 constats) ; les deux gardes documentaires de vitest migrées en node:test sans perte (11/19 et 7/8 cas/assertions, juge) ; `ECRIT_LU.test.lit` remesuré par sonde fs sur `npm test` (24 200 lectures, 0 documentaire). Gates : `test:hooks` 979, `test:docs` 83, `test:agents` 24, `test:ops` 243, `test:runner` 52, gates 63/63, lint, typecheck (sortie vide), `npm test` 1 503 fichiers / 21 463 tests, `docs:check` et `docs:empreinte` OK, `ops:ruleset -- --dry-run` identique. Deux juges (design, diff) et un train de corrections, verdicts ci-dessous.

## Restes
- points 3 et 5 du « Travail borné » (autorité du commit final, suppression des justificatifs — #1776 : plus aucune gate locale, `justificatif.mjs` et ses tables supprimés) -> corrigé par 50b1a3e92 scripts/ops/publier.mjs:9
- `docs/*.md` générés et `docs/raw/` classés PRODUIT : le push le plus fréquent (`chore(docs): docs dérivés`) ne gagne rien -> RAS : conservateur à raison, `npm test` confronte le code aux docs dérivés (`toutes.mjs`, raison de `test`, `data-atlas-complete`, `index-moteur-ratchet`…) ; un généré qui bouge peut changer un verdict produit
- `src/ui/registry-id-branch-guard.test.ts:156` : la garde n'exempte qu'au FICHIER (cardinal `RAW_KNOWN`), pas au site ; `classerPush.mjs` y entre pour `ref === REF_TRONC` -> RAS : un seul site dans un module de 170 lignes, la raison est écrite au patron des voisines ; la doctrine « exemption au site » vaut pour les gardes qui en ont le mécanisme, celle-ci n'en a pas et n'est pas le sujet de ce lot
- `tsconfig.tsbuildinfo` suivi par git (artefact d'incrémental de `tsc`, cite encore `src/memory-links-guard.test.ts`) -> #1781

## Réfutation
verdict: PARTIEL — juge de design (2026-09-16, lecture seule) : 12 corrections dont 2 RÉFUTATIONS (base du diff = incrément du push → trou sur run annulé puis push documentaire ; `npm test` « joue toujours » par deux gardes vitest lisant `.claude/memory/` et `CLAUDE.md`), 10 adoptées, 2 tranchées au ticket (migration des deux gardes en node:test là où vit leur lib `.mjs` ; condition par step conservée et confrontée par la garde). Juge de diff (lecture seule, neuf lentilles) : fermeture RÉFUTÉE en l'état sur L1 — `test:agents` lit `CLAUDE.md`, `AGENTS.md`, `.claude/settings.json`, `.codex/hooks.json` (sonde fs, `compat.test.mjs:160-180`) avec `lit = ['scripts/agents/']`, donc sautée sur le push qui touche ces fichiers → corrigé (`lit` remesuré, step joue toujours) et la CLASSE gardée (`corpusParGate` : aucune gate sautable ne nomme un chemin documentaire, mutation à 170 constats) ; L2 FRAGILE (`origin/main` au runner → fetch explicite + cas de garde sur clone `--single-branch`, et mesuré présent au run 35111946613) ; L3 polarité TIENT ; L4 garde bidirectionnelle TIENT par mutation (typecheck, test:hooks) ; L5 migration TIENT (11/19 et 7/8 cas/assertions, racines réelles, `test:hooks` 977 → 979, `test:docs` 83) ; L6 `refusDeCouverture` TIENT ; L7 doc dérivée TIENT (`docs:check` rejoué par le juge) ; L8 FRAGILE (`branche` → `ref`, exemption au fichier faute de mécanisme au site) ; L9 DoD mesuré ENSUITE sur GitHub : produit 35111946613 (tout joue, 625 s), documentaire 35113266971 (rien de produit, rouge sur le lien mort, 130 s). Écarts assumés : docs dérivés et `docs/raw/` restent produit.
