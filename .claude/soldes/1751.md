# Solde #1751 — les outils ops se jouent depuis n'importe quel worktree (Lot 1 de #1750) — 2026-09-14

VERIFIE: l'orchestrateur a relu les trois livraisons (A `gitPorte.mjs`/`chantier.mjs`/`worktrees.mjs` + consommateurs, B `publier.mjs`/`verrou.mjs`/docs, C corrections du juge) fichier par fichier et contre-grepé les claims : `git grep -- "--git-common-dir" scripts/` → seuls `gitPorte.mjs` (primitive + JSDoc), son test, et `git-destructive-guard.test.mjs` (teste git) — les deux résolveurs manuscrits (`justificatif.mjs:150`, `pre-commit.mjs:60`) consomment `arbrePrincipal` ; `child_process` absent de `port-dev.mjs` (chargé par `vite.config.ts:7`) ; `vue.tete &&` et `journal.tete ?? null` → 0 dans `publier.mjs`, `vue.tete == null` → à faire dans `planDeReprise` ET `etatDeLEtape` ; `process.kill(pid, 0)` → un seul site (`verrou.mjs:28`, `run.mjs` consomme l'export) ; `ROTATIONS_GARDEES`/`aSupprimer` → 0, `purgerPerimes` importé ; `CLASSES` = 7 avec `tenu` ; inventaire RÉEL depuis `.wt-1751` : `.wt-1751` classé `tenu`, l'arbre principal `principal` ; runners rejoués par l'orchestrateur sur le cumul A+B+C : `node --test` ops + guards/lib + git-hooks + test + ecrivainsAtteints + toutes.test → 0 échec, eslint 0, `docs:check` vert, `--etapes` sans effet ; juge de design (Opus) puis juge de diff (Opus) sur le cumul, leurs réfutations corrigées (C) et contre-vérifiées ; la publication RÉELLE de ce lot est jouée par `ops:publier --detache` depuis `.wt-1751`, sortie collée au ticket.

## Restes
- DoD « `ops:publier --detache` lancé pendant qu'une suite série tourne ATTEND » : la sonde du verrou est prouvée par sa décision PURE (`verdictDeSondeDuVerrou`, 5 cas, mutation → rouge) et le lecteur de tenant (`lireTenant`/`tenantVivant`, mutation → rouge) ; la boucle impure n'est exercée par un train réel que si une suite tierce tient le verrou au moment du train -> RAS : mesuré au train de ce lot si l'occasion se présente (log au ticket), sinon la ligne d'attente `[publier] gates — verrou tenu par PID … : sonde k` est le contrat, testée sur sa décision
- DoD « `ops:worktrees -- --purger` depuis un worktree candidat le NOMME » : `.wt-1751` n'est `propre+fusionné` qu'après sa publication -> RAS : la classe `tenu` prime sur `propre+fusionné` dans `classerWorktree` (test positif `worktrees.test.mjs`, mutation → 3 rouges) et l'inventaire réel depuis `.wt-1751` la rend ; le `--purger` réel se joue par la session propriétaire après publication
- `estArbrePrincipal` défini deux fois (`scripts/port-dev.mjs:67` et `scripts/hooks/solde-ticket-guard.mjs:2049`, défauts divergents `RACINE` vs `process.cwd()`) -> #1750 : hook de SESSION, autre sémantique, nommé par le juge de design, Lot 3 (le commit et les hooks)
- Le `npm ci` racine n'est pas un prérequis d'`ECRIT_LU` (aucune gate ne le déclare ; `resoudreOutilLocal` le mesure à la préflight) -> RAS : `EQUIPEMENT_RACINE` reste le seul équipement non dérivé, dit au JSDoc de `chantier.mjs`
- Un autre processus (shell, `vite dev`) tenant un worktree candidat n'est pas vu par la classe `tenu` (qui ne connaît que ce processus : script et cwd) -> RAS : le refus de git (EPERM) reste la barrière, sans force, et la re-mesure post-purge NOMME tout dossier encore présent (`natureDuChemin`, remove rouge OU vert)
- Le log rotationné vit sous `node_modules/.cache/publication/`, effacé par le `npm ci` d'`ops:chantier` -> RAS : dit au JSDoc d'`ouvrirLog` et dans `docs/reprise-apres-pause.md` — le log est une trace de travail, la preuve d'une publication est sa sortie collée au ticket (DoD)

## Réfutation
verdict: CONFIRMÉ — juge de design (Opus, 2026-09-14) a attaqué le design en 8 lentilles : TIENT AVEC CORRECTIONS, 22 corrections dont 21 intégrées (résolution `--path-format=absolute` + `normaliserRacine` pour les comparaisons, placement dans `gitPorte.mjs` hors de `port-dev.mjs`, deux résolveurs manuscrits retirés, `prune` inconditionnel mesuré en dépôt jetable, classe `tenu` dans `classerWorktree`, `lireTenant` exporté et tenant vivant exigé, borne 60 min mesurée sur #1736, règle de tête UNIQUE au HEAD vivant — défaut antérieur nommé —, rotation bornée par `purgerPerimes`, test « depuis un WORKTREE : refus » réécrit en contrat positif, langue) et 1 RÉFUTÉE par contre-grep de l'orchestrateur (`EQUIPEMENTS` dérivée d'`ECRIT_LU` depuis `3dc9500a7`) ; juge de diff (Opus) sur le cumul A+B : TIENT AVEC CORRECTIONS, 9 points tous corrigés en C sur pièces (tombale de `chantier.mjs`, commentaire faux « dépôt nu rend `.` » — mesuré : chemin absolu sous `--path-format=absolute`, test à injection impossible remplacé par un dépôt nu RÉEL, échappatoire `null` de la tête fermée dans les deux fonctions, `ctx?.tete` sans appelant sans ctx, copie `estVivant` de `run.mjs` supprimée, re-mesure post-purge de TOUS les `remove`, borne du verrou distinguée de l'orphelin, `.sources-lues.json` régénéré) sauf le 8ᵉ (« `--check` écrit ») NON REPRODUIT par C (hash identique avant/après, l'unique écriture `build-all.mjs:544` est gardée par `if (!check)`) ; les deux écarts des codeurs au brief (casse conservée dans la valeur d'`arbrePrincipal` — fixtures tmpdir à majuscules sur `ubuntu-latest` —, détail fusionné sur « déjà faite » pour `ci.dejaFaite`) jugés JUSTES par le juge de diff sur mesure.
