# Solde de réfutation — commit 3b du lot L2 #1548 (2026-08-30)

## Réfutation

Deux passes adversariales AVANT ce commit, non demandées par l'utilisateur :
- **Juge de design** (2 passes, 2026-08-30) : design du lot attaqué avant le premier codeur — 3 RÉFUTÉ / 5 FRAGILE en passe 1 (périmètre-ledger contredit, grammaire insuffisante pour les `choice` réels, baisse chiffrée fausse de 45 %), contre-passe sur le design v2 : dispatchable après re-scope (périmètre par CONCEPT, `{random}` sorti de L2, commit 0 réduit à `ids-nus`).
- **Juge de cumul #2** (2026-08-30, commits 2+3a commités + 3b en arbre) : fix RAW VÉRIFIÉ AU SOURCE (MDG 13 l.491 « Test étendu de Force Accessible (+20) », MDG 12 Panne de Vapeur, MSRC 7 l.78 « Test d'Initiative » — le repli silencieux roulait ces Tests sur la DEXTÉRITÉ, prouvé statiquement) ; classification plat/emboîté 12/12 vérifiée def+donnée ; comptabilité recomptée indépendamment (FORMES 587, REDECL 105, SLOTS 331 = plafonds exacts) ; cliquet SLOTS remonté jugé LÉGITIME (angle mort `champDuPath` préexistant, structures-lexique.mts:116) ; verdict PAS EN L'ÉTAT → 2 bloquants corrigés : `steam-breakdown.json` spec « Ingénieur »→`ingenieur` ×4 (bug moteur MESURÉ par le juge : 30 vs 70, les avances du mécano tombaient — sonde promue en garde par extension de `spec-pool-contrat.test.ts`, liste décroissante 4→0, mutation rouge/vert prouvée) et filet `grantCareerSkill` porté sur le walk de `[...opDatasets, ...talents]` (mutation : op fantôme dans traits.json = rouge).

## Juge-Vision

Recette navigateur dédiée (recetteur, serveur PID 30964 servant CET arbre, 12 captures dans `%TEMP%\recette-1548-lot3b\01-codex-feu-eteint.png` … `12-punchausen.png`) : Codex steam-breakdown (Test de Force/Métier (Ingénieur) aux bons libellés, zéro id brut), chips `grantCareerSkill` (« Art (Au choix) », « Divertissement (Chant) »), Vouivre optionnels, atelier CodexEdit (restart pré-rempli, sélecteur Compétence OU Caractéristique fonctionnel, aucun formulaire vide), activités d'interlude (« Punchausen » : difficulté PAR VOIE correcte, pré-jet 23 construit). Console : 0 erreur, 0 warning sur tout le parcours. Aucun changement visuel voulu ni constaté — le lot est une migration de graphie, les libellés résolvent à l'identique.

Restes de recette rattachés à la vague : outil `__wfrp` pour exercer `optionals`/swap au spawn (→ commit 5, spawn.ts dans son périmètre) ; une ligne à `docs/recette-navigateur.md` sur le chemin d'écriture des captures Playwright MCP (→ commit de solde).

## Réfutation — commit 3c (2026-08-30)

Juge de diff dédié sur les 174 fichiers en arbre : TIENT au Source sur 3 fronts (Mendier = usage de Charme LDB 09 l.95-97 — l'annulation de la cure de catalogue était la bonne décision ; Escalade/Saut LDB 15 l.55/76 avec magnitude du bug mesurée 30 vs 40 ; Savoir (Guerre) LDB 08 l.1085 vs NADJ 16 l.25, prose verbatim intacte) ; comptabilité recomptée tight (FORMES 557, ORPHELINES 106, OPS 400, SLOTS 340 = plafonds exacts, hausse SLOTS auditée ligne à ligne) ; filet exhaustif-par-construction mordant sur mutation (10 remises à plat + 1 spec frère = rouges). Son bloquant D1 (verrou RAW de corruptionExposure perdu — rabotage silencieux vers Résistance) corrigé en ALPHABET AU SCHÉMA (`z.enum(TESTS_DE_CORRUPTION)`, patron charKeySchema — la garde #842 avait mordu la première forme en membership, preuve que les cliquets travaillent) ; L5 XOR testMatch posé ; l'extracteur `Implémente` corrigé à la CAUSE (portée par profondeur — il attribuait les réfs RAW au dernier id emboîté, défaut ANTÉRIEUR au lot, 18 fiches régénérées plus vraies que HEAD, test menteur réécrit).

## Juge-Vision — commit 3c

Aucun écran JOUEUR ne change (migration de graphie ; opSummary GAGNE des libellés là où il crachait des ids). Surface AUTEUR touchée : le `<select>` de corruption de GameOpEditor affichait `[object Object]` dans l'état intermédiaire du lot — attrapé par le codeur des correctifs AVANT commit, corrigé (options dérivées de l'alphabet, lecture `.id`), contrats de composant verts (GameOpEditor.seeds, opRows, 161/161). La recette navigateur du lot (12 captures, console 0/0) couvrait GameOpEditor/CodexEdit ; les surfaces 3c (FlowEditor/EffectList RefField simplifiés, TavernGameModal) rendent par les mêmes primitives, vérifiées par leurs tests de composant.
