# Solde #1776 — 2026-09-16

VERIFIE: le DoD du ticket confronté à l'état PUBLIÉ de `main` (75692d80c) et au diff d'alignement — (1) ruleset `main` ACTIF lu au serveur : `gh api repos/cgauche/game/rulesets/23537150` → `enforcement: active`, `required_status_checks` [`build`, `migrations`] non strict, `non_fast_forward`, `deletion`, `bypass_actors: []`, `current_user_can_bypass: never` ; le bypass de l'intégration GitHub Actions promis au corps est REFUSÉ par GitHub sur un dépôt personnel (HTTP 422 « Actor GitHub Actions integration must be part of the ruleset source or owner organization »), verbatim porté en en-tête de `scripts/ops/ruleset-main.mjs` et au commentaire de #1713 ; sonde du hook `jugerPush` sur `refs/heads/main` avec et sans `GITHUB_ACTIONS=true` → refus IDENTIQUES (« aucun run CI sur <sha> »), aucune exonération locale ; (2) la CI joue sur `chantier/1776` : runs 35074632674 (rouge — la porte a REFUSÉ la passe 1 : deux écrivains non déclarés, invisibles en local car non suivis), 35076311100 (vert, 9 min 18), 35081205652 (vert, 8 min 30) ; hook `pre-push` mesuré par `jugerPush` sur la plage réelle : 3,5 s à 3 commits, 4,8 s à 10, 6,5 s à 20, 9,6 s à 40 (le coût est `plageStock`, refus préexistant de #1679) ; (3) `ops:publier` (log `chantier_1776.log` lu en entier) : preflight → derives → rebase → docs → push-branche → ci → ff-main → pilotage → fin, AUCUNE étape `gates`, aucun verrou machine ; temps machine locale (docs 33-59 s, derives 61 s, push 3-7 s) séparés de l'attente CI (561 s, 528 s) par `attenteCiSecondes` ; un tronc qui a bougé pendant l'attente (#1771) a relancé le rebase (conflit `PLAFOND_OCTETS` résolu à la mesure 26455) ; (4) #1771 (ancien régime) et #1776 ont publié sans s'attendre : #1776 a poussé sa branche pendant les gates de #1771 et n'a attendu que SA CI ; (5) `git grep -E "justificatif|wfrp-justificatifs|justifie\.mjs|portePush" -- scripts src .claude docs package.json .github` hors soldes/decisions/plans → 0, 9 fichiers supprimés (`git show --stat 50b1a3e92`) ; (6) `test:hooks` 951/951, `test:ops` 243/243, `test:docs` 76/76, lint, `docs:check` vert après régénération, run CI de `main` 35082033206 vert. Passe de réfutation de fermeture par un juge (lecture seule), verdicts repris ci-dessous.

## Restes
- « deux chantiers publiés dans la même heure » : écart réel entre l'entrée de #1771 (10:43) et celle de #1776 (~11:54) = 71 min, deux heures de calendrier -> RAS : la clause visait l'absence d'attente croisée, tenue et mesurée (aucune étape gates au log, seul coût du croisement = un rebase) ; la lettre « même heure » était une formulation de DoD, pas un invariant
- clause « pre-push < 5 s » : tenue jusqu'à ~10 commits (4,8 s), 6,5 s à 20, 9,6 s à 40 -> RAS : le coût est le scan `plageStock` du refus 2, préexistant (#1679, 2026-09-03), linéaire en commits (~0,15 s/commit), sans rapport avec le régime livré ; mesure collée ici pour la prochaine lecture
- canari sous sa nouvelle forme (step `rule-suites` retiré) : le cron du lundi n'a pas encore tourné -> RAS : run `workflow_dispatch` 35083899333 lancé le 2026-09-16 sur `main`, verdict au commentaire de pilotage v4 ; le cron du 2026-09-21 le rejoue
- bot d'`export-issues.yml` refusé par le serveur (aucun bypass posable sur un dépôt personnel) -> RAS : état PRÉEXISTANT à ce lot (le bot était déjà refusé par l'ancien pre-push, #1713 ouvert depuis le 2026-09-03), seule sa cause change ; la mesure du 422 et les deux voies (DeployKey en bypass, ou export hors `main`) sont commentées à #1713 le 2026-09-16, décision utilisateur
- #1738 NON capturé : son contrat vise aussi les JOBS de la CI (la suite et le build jouent sur un push de fiche seule, `ci.yml` sans filtre de chemins) et son point 4 est intact (`fermetures` exige `build` pour une fermeture documentaire) -> #1738

## Réfutation
verdict: PARTIEL — juge de réfutation de fermeture (2026-09-16, lecture seule, `.wt-1776` à 75692d80c + diff d'alignement) : #1776 FRAGILE — DoD 1 TIENT sur le ruleset (lecture serveur collée) et le diff d'alignement (sonde `GITHUB_ACTIONS` sans effet, contre-grep `bypass|GITHUB_ACTIONS|15368` à 0 hors mentions voulues), RÉFUTÉ sur `docs:check` (pied périmé après la dernière retouche de source → régénéré avant commit, vert) ; DoD 2 FRAGILE (4,1 s sur 3 commits, marge nulle → remesuré à 4 tailles ci-dessus) ; DoD 3 TIENT (log entier, séparation structurelle `attenteCiSecondes`) ; DoD 4 RÉFUTÉ À LA LETTRE (71 min) tenu à l'esprit ; DoD 5 TIENT (grep 0, 9 suppressions) ; DoD 6 partiel (canari non mesuré → dispatch lancé) ; le job `fermetures` fermera bien sur `corrige #1776` (`fermetures.mjs:14`). #1738 : capture RÉFUTÉE (contrat sur les jobs, `ci.yml` sans filtre de chemins, `needs: build`) → reste ouvert, re-cadré. Décision d'orchestrateur consignée : le refus non fast-forward exempte `chantier/**` (un écrivain, rebase par le train).
