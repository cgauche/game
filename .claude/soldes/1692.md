# Solde #1692 — 2026-09-08 (lot 2 ; lot 1 du 2026-09-07)

VERIFIE: l'attendu du ticket confronté à l'arbre après le lot 2 — (1) une GARDE STRUCTURELLE dérivée de `DATASET_KEYS` (importé d'`overrides.ts`, jamais recopié) refuse nominativement tout index construit au niveau module (`new Map(`/`new Set(`/`Object.fromEntries(`/`.reduce(`) sur une clé d'`ARRAYS`, sur `src/**` non-test en entier, contrôle positif par injection dans une copie ; (2) les 52 index figés mesurés par le juge de design (44 dans `src/data/index.ts`, 8 hors — `engine/conditions.ts`, `engine/psychology.ts`, `engine/qualities/registry.ts`, `engine/traits/registry.ts`, `engine/traits/dispatch.ts`, `engine/travelStages.ts`, `ui/creator/CharacterCreator.tsx`) sont des LECTEURS VIFS par UNE primitive `indexParId`/`indexParChamp` mémoïsée par la VERSION du dataset, que le seam de mutation (`setDataset`/`setObjectDataset`, `overrides.ts`) bump dans le module FEUILLE `src/data/versionDataset.ts` (patron `idsVivants.ts`, aucun cycle) ; (3) coût mesuré : Map figée 8,06 ns → version 10,96 ns par lecture, chemin chaud (bake, tour IA) < 1 % ; le témoin de CONTENU d'`indexDesTerrains`/`defsGlobaux` (91,4 ns à N=123, O(N)) CONVERGE vers la version ; `.find` à l'appel (`findLightToneById`, `matieresDe`, mesure `index.ts:2425-2426`) reste un choix sur mesure ; (4) contrat de fraîcheur POSITIF sur des datasets ÉDITABLES (cas qui échouaient AVANT le lot, sonde 4 du juge : label de trait édité → `findTraitById`/`traitLabelById`/`knownTraitId` à jour ; trapping neuf vu ; `worldBakeDeps` porte la dep neuve) ; (5) seam UNIQUE en écriture : les deux tests qui mutaient hors seam (`creation.test.ts`, `stage-tons-lumiere.test.tsx`, commentaire faux corrigé) passent par `setDataset`, garde structurelle sur `push/splice/pop/…` des bindings d'`ARRAYS` hors `overrides.ts`.

## Restes
- `PROP_BY_ID` (`index.ts:2714`) figé à l'import, lu par bake (`builders/props.ts`, `sceneMeshes.ts:345,358`) -> corrigé dans ce commit : src/data/index.ts:2724
- Prémisse du ticket « pas un bug vivant, un piège » -> RAS : réfutée par le juge de design — 43 des 52 index portent sur un dataset ÉDITABLE au Codex, staleness prouvée en exécution (`traits`, `trappings`) ; le contrat de fraîcheur du lot verrouille ces cas
- `LOOKUP_VERSION` / `invalidateCodexLookup` du Codex (`registry.ts:617-642`, abonnés React `useSyncExternalStore`, rollback à double bump de `CodexEdit.save`) -> inventaire #1689 : mécanisme vif rival à faire converger vers `abonnerAuxDatasets` du seam, dans un train qui porte les abonnés React
- Index sur des datasets non éditables PAR LEUR DEF (`props`, `lightTones`, `structureAppearances`, `MERCHANT_ARCHETYPES`, `lieuxServices`, `reseauRoutier` — `edit: { none }`) -> RAS : même primitive vive (un dataset qui devient éditable un jour ne demande rien), aucun contrat Codex à écrire, le clivage se lit au def
- `traitByLabel` exporté vers `src/engine` (`traits/dispatch.ts:19,37`, `Map` par label hors `src/data`) -> corrigé dans ce commit : src/engine/traits/dispatch.ts:29
- `WEATHER_TABLE` (`engine/travelStages.ts:82-86`) : snapshot exporté justifié « compat/tests » sans tag d'entérinement utilisateur, doublon de `weatherFromRoll` -> corrigé dans ce commit : src/engine/travelStages.ts:85
- 26 VUES DÉRIVÉES figées de la même classe (`FABRICATION_ATOUTS`, `siegeEngines`, `CORE_AXIS_IDS`, `CULT_IDS`, `OUPS_TABLE`, vues d'éditeur/galerie/créateur, boucles d'enregistrement de tables de `tavernFlow`/`travelFlow`), trouvées par le juge de diff n°1 hors du motif initial de la garde -> corrigé dans ce commit : scripts/guards/lib/bindingsVifs.mjs:315 (motif élargi + migration de la classe entière vers `memoParVersion`)
- Deux qualités au libellé « Immobilisante » dans `qualities.json` (`immobilisante` LDB 62 l.256 ; `immobilisante-fixe` = note d'arme du Filet lesté AA 08 l.310 promue en qualité), trouvaille du juge de diff n°2 vérifiée au Source, pré-existante -> #1710
- Une table de jeu de taverne ou de saison SUPPRIMÉE au Codex reste enregistrée (`registerTableStep` écrase, ne retire jamais ; `tavernFlow`/`travelFlow` mémoïsés par version) -> inventaire #1463 : purge du reste vivant à l'édition, hors du périmètre « index vif » (aucun désenregistrement n'existe)
- 46 index figés dans des `.test.ts` (fixtures locales) hors périmètre de la garde d'index (non-test), asymétrie avec la garde d'écriture (tests compris), déclarée en tête de la garde -> RAS : un test qui fige une fixture ne sert aucun écran ; la garde d'écriture, elle, couvre les tests parce qu'un `push` hors seam y falsifie la version
- 7 plans EXÉCUTÉS `docs/superpowers/plans/2026-06-07-qualite-objet-*.md` citant `qualities/registry.ts` supprimé, et `2026-06-08-marchand-lot2.md` (#371 lot 2, livré) -> RAS : les 8 fichiers sont supprimés dans ce commit (politique `docs/` : un plan exécuté se supprime, git porte l'histoire ; aucun doc vivant ne les liait)
- 45 plans datés restent sous `docs/superpowers/plans/`, dossier hors `docs:check` -> inventaire #1679 : politique `docs/` (« supprimés une fois exécutés ») non appliquée à ce dossier, à trier par le chantier de régime
- `src/gameIso/catalog/structures/index.ts:9` `MAP` figé sur `structureAppearances` (hors `ARRAYS` et hors `OBJECTS` : aucun chemin de mutation, `edit: { none }`) et `MERCHANT_ARCHETYPES`/`lieuxServices`/`reseauRoutier` -> RAS : hors seam, aucune staleness possible ; le jour où le dataset entre au seam, la garde le verra (vocabulaire dérivé d'`ARRAYS`)
- Console à l'Atelier du Codex : ouvrir un Trait ou une Mutation en édition émet `[bodyPlan] « … » : aucune espèce résolue` (un aperçu d'apparence tente de résoudre un rig pour une entrée qui n'en porte pas), reproductible, pré-existant (recette) -> inventaire #1689 : prévisualisation d'apparence montée sur une catégorie sans rig, à borner au def
- `CompendiumScreen.tsx:99` referme l'édition dès que « Enregistrer » change le `label` de l'entrée en cours (l'effet réagit à son propre changement) — friction d'auteur, pré-existante (recette) -> inventaire #1689 : dépendance d'effet sur `selected?.label`, à re-clé par `id`
- `knownTraitId` re-sluguait le LIBELLÉ résolu (`slugId(def.label)`, `traits/dispatch.ts`) : après une édition au seam, l'id rendu n'existait pas — gravé par le premier test de fraîcheur du lot, refusé par la garde `label-logic` au pre-commit -> corrigé dans ce commit : src/engine/traits/dispatch.ts:29
- `QUALITY_DESC` (`engine/qualities/describe.ts`) : Record keyé par LIBELLÉ, figé à l'import, dans `src/engine` ; invisible à la garde #142 (construction tolérée, lecture par variable) -> corrigé dans ce commit : src/engine/qualities/describe.ts:2 (mort ; règle neuve `label-keyed-index` dans `labelLogic.mjs`, ses deux sites préexistants `weaponGroup.ts`/`combatLog.ts` migrés, stock à zéro)
- Tables de monde dérivées de la donnée (météo/saison, jeux de taverne, nature de mutation par seuil) enregistrées par EFFET DE BORD d'un mémo lu au seul site d'ouverture : suite complète rouge (`de-monde-surface.test.ts`) après le lot -> corrigé dans ce commit : src/state/cascade.ts:252 (registre STATIQUE + FAMILLES vives, lecture unique `tableStepDef`/`tableStepIds`, `tableStepDefs` mort)
- `src/data/mutations.ts` : `TABLE_BY_ID`/`ROWS_BY_TABLE`/`MUTATION_TABLE_IDS`/`BY_ID` figés à l'import sur `mutationTables`/`mutations` ÉDITABLES (réfuté par le juge de diff : fourchette éditée non servie, table neuve introuvable), invisibles à la garde d'index parce que le dataset y est atteint par l'IMPORT JSON DIRECT -> corrigé dans ce commit : src/data/mutations.ts:37
- `src/engine/miscast.ts` `RUNTIME_ROWS` figé à l'import : `rollMiscast` jouait les ops d'avant une édition alors que `miscastRowAt` servait la version vive ; la clé de seam (`miscastMinor`) est un DOCUMENT de `miscast.json`, l'entrée du seam est un APPEL (`miscastEntries(…)`) → binding fantôme, jamais surveillé -> corrigé dans ce commit : src/engine/miscast.ts:340
- Garde d'index : l'entrée de seam à valeur d'APPEL (`miscastMinor: miscastEntries(…)`, `criticalsTete: critiqueEntries(…)`) posait un binding fantôme -> corrigé dans ce commit : scripts/guards/lib/bindingsVifs.mjs:56 (`bindingSurveille` : appel → la CLÉ, assertion positive + injection)
- Garde d'index : un module qui atteint un dataset par l'IMPORT JSON DIRECT puis un ALIAS nu (`const X = json as T[]`) échappe encore au vocabulaire — 6 sites hors périmètre mesurés (`engine/clock.ts:122`, `engine/trauma.ts:116,428`, `engine/travel.ts:41,45,57`), contre-vérifiés : `CUMUL_FICHES`, `TRAVEL_VEHICLES`, `TRAVEL_MODE_LABEL`, `CAMPAIGN_START` (calendrier = datasets du seam) sont des DÉRIVÉS figés sur des datasets ÉDITABLES -> corrigé dans ce commit : scripts/guards/lib/bindingsVifs.mjs:100 (la garde suit l'import JSON direct et l'alias nu ; 0 fautif à l'arrivée, 11 dérivés migrés en accesseurs dont `campaignStart()`, `travelVehicles()`, `cumulFiches()`, `diseaseDefs()`)
- Partition de `TENUE_DEFS` (code généré, pas un dataset) par un prédicat devenu vif (`gameIso/rig/parts/tenues/index.ts:20-42`) reste figée à l'import, invisible par construction -> RAS : déclaré dans l'en-tête de la garde, pas un dataset
- Une table RETIRÉE de la donnée rend une cascade PERSISTÉE sur son id irrésoluble : refus NOMMÉ de `lireEnTable`, fail-fast sous l'arbitrage utilisateur du 2026-08-17 (saves reset, pas migration), test de reprise -> RAS : arbitrage cité au site (src/state/cascade.ts:376)
- `src/state/label-logic-guard.test.ts:189,421` exemptent `TRAVEL_VEHICLES.map(...)` par CHAÎNE VERBATIM — une migration de ce binding devra re-trier l'exemption -> inventaire #1463 : exemption par verbatim à convertir en exemption structurelle au site
- Recette de la cascade météo (persona joueur) : le bouton `.worldmap-btn` de l'`ExplorationDock` sert QUATRE fonctions (Possessions, Carnet, Dossier navire, Carte du monde) sans distinction autre que le `title` ; les cartouches de lieu de la carte du monde sont en `pointer-events:none` (seul le médaillon clique) ; `Escape` dans le menu système remonte d'un niveau au lieu de reprendre ; les chips du Compendium portent un compteur accolé sans séparateur accessible (« Météo de voyage4 »), aucun `data-testid` -> inventaire #1689 : affordances d'ouverture indistinctes et chips sans sélecteur stable, à traiter par la primitive de dock/chip
- Une cascade EN ATTENTE interdit toute navigation vers le Compendium par les contrôles réels (aucun bouton menu/compendium pendant `pendingCascade`) — friction de recette et de joueur (éditer une table de monde pendant une étape ouverte) -> inventaire #1689 : ouverture du Codex en consultation pendant une cascade, à arbitrer (lecture seule ou édition)
- `setScreen('compendium')` (`store.ts:1919`) ne pose pas `compendiumReturn` (seul `openCodex()` `store.ts:1922-1931` le fait, défaut `'menu'`) : « ← Retour » du Compendium atterrit au MENU PRINCIPAL et perd l'écran de cascade — asymétrie des deux chemins d'entrée, pré-existante (recette) -> inventaire #1689 : une seule entrée du Codex qui pose son retour
- « ✎ Atelier » du Compendium est un TOGGLE de mode rendu comme un onglet (recette : deux tentatives perdues) -> inventaire #1689 : affordance de mode vs onglet, `Tabs`/`MenuToggle`

## Recette visuelle
capture: public/qc/1692-index-vif/02-editeur-libelle-vif.png
capture: public/qc/1692-index-vif/03-psycho-libelle-vif.png
capture: public/qc/1692-index-vif/04-galerie.png
capture: public/qc/1692-index-vif/05-statusbar.png
capture: public/qc/1692-index-vif/06-meteo-etape-avant-edit.png
capture: public/qc/1692-index-vif/06b-meteo-j2-avant-edit.png
capture: public/qc/1692-index-vif/07-meteo-j2-apres-edit.png
capture: public/qc/1692-index-vif/08-codex-meteo-refus-couverture.png
Preuve PRODUIT du lot : un libellé de Trait édité au Codex (« Animosité (ÉDITÉ) ») apparaît dans le sélecteur de traits optionnels de l'éditeur SANS rechargement (`02`), une psychologie éditée apparaît dans le champ des mutations (`03`) — avant le lot, ces sélecteurs servaient l'index de l'import jusqu'au rechargement. Galerie et barre de statut rendues sans erreur (`04`, `05`). Console : 0 erreur liée au mécanisme ; 7 erreurs `[bodyPlan]` pré-existantes (reste ci-dessus). Correctif du registre de tables (recette « Journée de route », scénario « Voyage & temps long », Dés fixés) : l'étape « Météo de l'Étape » affiche les rangées VIVANTES de `weather.json` (`06`, `06b`), et une fourchette éditée au Compendium pendant que l'étape du jour 2 est en attente se reflète dans la grille « FIXER LE DÉ » sans rechargement (`07`, édition COHÉRENTE de deux bornes) ; une borne éditée SEULE est REFUSÉE par le garde de couverture d100 du schéma et l'état d'avant est repris (`08`, message de refus à l'écran) — la première recette avait joué cette édition refusée et conclu ROUGE à tort ; console 0 erreur. Test committé `src/state/meteo-codex-vif.test.ts` (refus → reprise ; édition cohérente → table vive).

## Réfutation
verdict: CONFIRMÉ — juge de DESIGN (lecture seule, `juge-design-1692.md`) : TIENT-SOUS-CORRECTIONS sur l'invariant v1 — prémisse du ticket réfutée (43 bugs vivants, 2 staleness prouvées en exécution), cycle `overrides.ts` ↔ `index.ts` démontré (module feuille exigé), 2 écrivains hors seam trouvés, coût mesuré des trois régimes (sonde 1e6 lectures), périmètre de garde élargi à `src/**`, cardinaux corrigés (121 clés, 52 index, N=123/25) ; v2 porte ses 9 corrections. Juge de DIFF n°1 (`juge-1692-L2`, rendu en réponse) : ACCEPTE-SOUS-CORRECTIONS — recensement indépendant 71 → 0 hors tests, typecheck FULL, 305 + 20 + 12 + 67 tests, perf reproduite (9,33 → 11,50 ns), knip ROUGE (export mort `abonnerAuxDatasets`), 14 vues dérivées figées non gatées, `.reduce` absent du motif, `QUALITIES` par label dans `src/engine`, dep réelle de `worldBakeDeps` non assertée — toutes portées (26 vues migrées, motif élargi, `qualities/registry.ts` mort, `findQualityByLabel` dans `src/data`). Juge de DIFF n°2 sur les corrections : ACCEPTE-SOUS-CORRECTIONS — réf morte `OUPS_MISFIRE`, commentaire « homonymie de Source » FAUX (vérifié au Source : LDB 62 l.256 unique définition, AA 08 l.310 note d'arme) → réécrit, doublon de modélisation ticketé #1710 ; 6 cécités du détecteur mesurées par injection (flèche sœur, IIFE, déstructuration, `export default`, affectation nue, accesseur vif RE-FIGÉ) → toutes couvertes avec contrôle positif, un vrai positif trouvé et migré (`EffectList.tsx` `SPELL_GROUPS`) ; mutation isolante de `worldBakeDeps` committée ; stock résiduel 46 fixtures de test déclaré. Recette navigateur (`recette-1692-L2`, 9 captures `public/qc/1692-index-vif/`) : VERT — la fraîcheur se voit à l'écran au clic réel. Ce que les juges n'ont pas joué : la suite complète et les gates — jouées avant le push.
