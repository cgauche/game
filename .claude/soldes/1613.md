# Solde #1613 — 2026-09-04 (L2 T1b, programme #1679)

VERIFIE: les trois points du DoD confrontés à l'arbre. (1) « Hook `pre-push` … exporte HEAD dans un dossier temporaire » : `scripts/git-hooks/pre-push.mjs:163-200` (P1.4) — pour chaque ref, la plage poussée (`<sha distant>..<sha local>`, ou l'arbre ENTIER du sha quand la ref distante est neuve) est confrontée aux chemins qui arment le rejeu (`CHEMINS_QUI_ARMENT_LE_REJEU` = le `PERIMETRE` IMPORTÉ de `replay.mjs:108` + `scripts/migrations`, jamais recopié) ; armé → `rejeuSurExport` (fonction importée, pas un `npm run` spawné) → refus nommant chaque rouge et la commande qui le rejoue ; non armé → « replay sauté » DIT et aucun export fabriqué (assertion sur `os.tmpdir()/wr/`). (2) « Le même script est invocable à la main (`npm run migrations:replay:head`) » : `package.json:15` → `scripts/migrations/replay-head.mjs`, argument `<sha>` optionnel (défaut HEAD), export par index TEMPORAIRE (`GIT_INDEX_FILE` + `read-tree` + `ls-files -z` + `checkout-index --stdin`) donc l'ARBRE du sha et jamais l'index ni le working tree (test : un fichier modifié non commité n'apparaît pas dans l'export), effacement dans un `finally` sous garde de préfixe. (3) « La skill d'orchestration ajoute le gate à sa checklist pré-push » : `.claude/skills/orchestrer-des-agents/SKILL.md:150-153`, une phrase, au présent. Le piège que le ticket nommait est MESURÉ et c'est pire que « sortie vide » : hors dépôt, `git diff --exit-code -- <6 chemins>` ne se plaint pas, il bascule en `--no-index`, compare les deux premiers chemins et prend le reste en pathspec — aucun n'existant, il rend **0**, soit « rien n'a bougé » sur un arbre pourtant réécrit (`rev-parse --is-inside-work-tree` y rend 128) ; d'où la 3e voie de mesure, l'EMPREINTE (`scripts/migrations/lib/empreinteRejeu.mjs`), et le fail-loud `estUnDepot` (`replay.mjs:119`, `replay.mjs:201`). Budget MESURÉ sur ce worktree, `npm run migrations:replay:head`, trois runs : à froid export 7,5 s / rejeu 15,7 s / empreinte 0,29 s / total 27,7 s ; à chaud 4,4+10,5+0,14 = 17,6 s puis 3,2+11,2+0,16 = 16,8 s — au-dessus des « ~10-15 s » estimés au ticket, l'écart étant le rejeu lui-même (88 migrations), pas la mesure. Non-régression du mode CI : `npm run migrations:replay` EN PLACE, 88 migrations, « rien n'a bougé » + « aucun fichier NEUF », exit 0. Un juge de DIFF a ensuite attaqué la porte sur 7 sondes : 2 bloquants et 6 dits, tous REPRODUITS puis corrigés (voir Restes), et ses sondes A, D, E et F sont promues en tests.

## Restes
- Le dossier des migrations n'avait aucun critère HONNÊTE de sélection : sous `f !== 'replay.mjs'`, `replay-head.mjs` s'est fait REJOUER comme une migration (rejeu RÉCURSIF de 15,7 s, exit 0) ; sous le seul préfixe daté qui l'a remplacé, une migration NON idempotente mal nommée (`corrige-props.mjs`) était SAUTÉE en silence — « 0 migration(s) », verdict VERT (juge de diff, sonde A). Préfixe daté POUR jouer + liste nominative `MODULES_DE_LA_PORTE` POUR excuser, tout le reste ROUGE et nommé -> corrigé dans ce commit (scripts/migrations/replay.mjs:167)
- Deux rejeux CONCURRENTS sur le même sha (un `pre-push` pendant un `migrations:replay:head` à la main, deux worktrees au même commit) se partageaient le dossier `wr/<sha8>` : `EBUSY rmdir` pour le second, faux rouge « DOCUMENT DISPARU » dans l'ordre inverse (juge de diff, sonde D) -> corrigé dans ce commit (scripts/migrations/replay-head.mjs:121)
- La garde d'effacement testait un préfixe de CHAÎNE : `…/wr-sauvegarde` et `…/wrangler-cache` passaient (juge de diff, sonde E) -> corrigé dans ce commit (scripts/migrations/replay-head.mjs:103)
- Une entrée FICHIER du périmètre rendait un `disparus` FANTÔME — `blobsDe` la voyait, `fichiersDe` non (juge de diff, sonde F) -> corrigé dans ce commit (scripts/migrations/lib/empreinteRejeu.mjs:54)
- ANGLE MORT de l'empreinte, dit et non corrigé : `git hash-object` applique les filtres d'entrée (un fichier CRLF y rend le sha de sa version LF — mesuré sans `.gitattributes`, sous `* text=auto eol=lf` ET sous `* -text`), donc une migration qui ne changerait QUE des fins de ligne est invisible -> RAS : c'est EXACTEMENT l'angle mort de `git diff` dans le dépôt sous `* text=auto eol=lf`, la 3e voie ne l'élargit pas ; il est NOMMÉ au JSDoc d'`empreinteDe`, pas tu
- Budget mesuré 16,8-27,7 s contre « ~10-15 s » au ticket -> RAS : l'écart est le rejeu des 88 migrations (10,5-15,7 s), pas l'export (3,2-7,5 s) ni l'empreinte (0,14-0,29 s) ; la porte ne se paie qu'au PUSH, une fois par sha poussé (mémoïsé), et seulement si la plage touche le périmètre — sauf première poussée d'une BRANCHE NEUVE, où l'arbre entier tient lieu de plage et arme donc toujours le rejeu, ce que la note du hook DIT
- Aucun lien `node_modules` dans l'export -> RAS : mesuré, aucune des 88 migrations n'importe hors `node:` et hors chemin relatif, et le rejeu complet des 88 passe sur un export SANS `node_modules` (mesuré : `existsSync(node_modules)` faux dans l'export, 88/88 à l'exit attendu, `migrations:replay:head` vert) — un lien serait une dépendance inventée
- Voie d'export `git archive | tar` -> RAS : mesurée équivalente à l'octet (528 fichiers du périmètre, 0 divergence) mais plus lente (3,9 s contre 3,1 s), avec un tarball intermédiaire de 121 Mo et le piège `--force-local` de GNU tar sur `C:\…` ; l'alternative et sa raison vivent dans l'en-tête de `replay-head.mjs`

## Réfutation
verdict: CONFIRMÉ — la preuve est la fixture ROUGE, écrite et jouée AVANT tout code de mesure (`ERR_MODULE_NOT_FOUND` sur `empreinteRejeu.mjs`), puis SIX mutations nominatives, chacune rouge puis remise à l'octet (blake3 confrontés au préimage). Premier tour : (a) fail-loud `estUnDepot` débranché → le test hors dépôt rougit en imprimant le faux vert exact « rien n'a bougé » ; (b) `comparer` privé de sa détection de fichiers neufs → 2 rouges, dont un rejeu qui déclarait « 1 fichier identique » alors qu'une migration venait de créer `src/data/neuf.json` ; (c) porte P1.4 du pre-push débranchée → 2 rouges, le refus devenant une chaîne vide. Second tour, après le juge de DIFF, sur les tests promus de ses propres sondes : (d) `MODULES_DE_LA_PORTE` retiré du filtre → « un `.mjs` SANS préfixe daté est ROUGE » rougit, la sortie ne nommant même plus `corrige-props.mjs` ; (e) PID retiré du dossier d'export → « deux rejeux CONCURRENTS » rougit sur l'`EBUSY rmdir` d'origine ; (f) séparateur retiré de la garde d'effacement → « REFUSE tout voisin de `…/wr` » rougit sur `Missing expected exception: …\wr-sauvegarde`. Le juge avait raison sur les 8 points : les 2 bloquants étaient deux FAUX VERTS de la porte elle-même (une migration jamais jouée déclarée verte ; un export effacé sous les pieds d'un voisin), et sa sonde G a confirmé la prémisse du train (`git diff --exit-code` hors dépôt = 0, `rev-parse` = 128). Réfutation retournée sur un point, mesurée : sa sonde E recopiait l'ANCIENNE expression de la garde au lieu d'appeler `effacerExport`, donc elle ne pouvait pas voir la correction — la garde est désormais exportée et mesurée par appel, pas par recopie. Gates : `node --test` (empreinte + pre-push) 38/38, `test:hooks` 546/546, `migrations:replay` en place 88 vert sur périmètre vierge, `migrations:replay:head` vert, typecheck 0, lint 0, deps:unused (exports morts 11, aucun nouveau), check-doc-refs 32 docs, vitest comment-poison + portable-paths + grammaire-guard verts.
