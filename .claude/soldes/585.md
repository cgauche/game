# Solde #585 — graphie des réfs RAW gelée, source unique des acronymes, 5 familles à zéro

VERIFIE: les 5 familles de `citation-graphy-guard` mesurées à 0 sur l'arbre publié (ch. cosmétique 1371→0, réf sans chapitre 91→0, folio nu 242→0, abréviation inconnue verrouillée, multi-folios à cheval verrouillée) ; `check-code-refs` 0 réf morte ; `test:raw` 180/180 ; `docs:check` 4 sous-gardes vertes ; `tsc` 0 erreur ; suite complète relancée par l'orchestrateur ; spot-checks au Source de mon propre chef (folio 310→ch76 vs 312→ch77, Encombrement ch61, localisations créatures ch76).

## Restes
- Folios nus des fiches `docs/raw/**` (197 refs, hors perimetre du scan qui ne couvre que `src/`) ; le DoD de #585 rattache explicitement le volet Atlas au parseur partage -> #454
- Résidus d'ancres `data-folio` non résolvables (#522 en laissait 146) : le nouveau scan multi-folios les traite en « indéterminé » plutôt que d'échouer -> RAS : choix documenté en commentaire dans la garde, une dette d extraction n est pas une faute de graphie
- Dette de graphie preexistante trouvee dans le geste (`ops.ts:652`, 2 sorts sur 3 hors du chapitre cite) -> corrigé dans ce commit
- Échec `part-view-format.test.ts` (plafond de stock des tenues) -> RAS : préexistant sur origin, aucun fichier tenues dans ce diff, appartient à la session rig

## Juge
Juge adversarial sur le chapitrage des 242 folios : 10/10 classes de divergence verbatim≠ancre CONFIRMÉES au texte (dans 5 cas l'ancre siège en fin du chapitre PRÉCÉDENT — la conversion mécanique aurait fabriqué des réfs fausses), échantillon 10/10 des résolutions par ancre justes, pureté du diff PROPRE (word-diff : tokens ajoutés = numéros de chapitre seuls, aucun statbloc `Savoir (…)`, aucun jalon `#T2/#T3`, aucun toponyme). Verdict PARTIEL sur un seul point : la classe multi-folios non traitée — corrigée depuis (6 occurrences + 1 dette) ET gatée par un scan neuf avec test rouge/vert.

## Juge-Vision
Sans objet — aucun écran modifié : le lot ne touche que des commentaires de code et des fiches Atlas ; le Compendium affiche les mêmes valeurs qu'avant (`bookAbr()` lisait déjà les acronymes retenus comme canon).

## Réfutation
verdict: PARTIEL — le juge a réfuté la complétude (classe multi-folios `p.X/Y` enjambant deux chapitres, invisible des gardes car `check-code-refs` ne valide que les `l.`), et le codeur du lot correctif a réfuté MON PROPRE BRIEF sur `localisation.ts` (le folio est 310→ch76, pas 312 qui est le statblock Ogre en ch77) — preuve `folioRange` à l'appui. Les deux réfutations ont été intégrées avant fermeture, la classe est désormais gatée à zéro.

Date : 2026-07-18
