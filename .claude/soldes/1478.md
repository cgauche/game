# Solde #1478 « Recette navigateur : deux helpers __wfrp manquants — ready() (fin de cuisson du monde volumique, piège « Chargement... ») et editorOpen(id) (ouvrir un projet d'éditeur sans dialogue OS) » — 2026-09-15

VERIFIE: les deux manques du ticket, plus le troisième que la recette de #1644 exigeait (`roofCut`), livrés par `61ad56a64` et MESURÉS en joueur le 2026-09-15 (recetteur sur le worktree `.wt-1478-1644-recette-opera`, serveur de dev sur le port dérivé 5179, kit `scripts/recette/lib.mjs`) — (1) `__wfrp.ready()` : après `scenario('opera-plan')` → `✓ monde prêt — scène « opera-staatsoper », voile tombé après 4777 ms` et 0 `[data-voile]` dans le DOM à la résolution ; rejoué sur un monde déjà prêt → `voile tombé après 1 ms` ; `screen('menu')` puis `ready(1500)` → `✗ __wfrp.ready : aucun monde monté après 1500 ms — aucune scène volumique n'a signalé son entrée en scène` (rejet NOMMÉ, jamais un timeout muet) ; le rendez-vous vit en couche state (`src/state/entreeEnScene.ts`, 0 import), signalé par `GameStage3D` depuis SA vérité du voile ; (2) `__wfrp.editorOpen()` sans id rend les trois familles (`{projets:[], campagnes:[4], scenarios:[41]}`) ; `editorOpen('opera-plan')` → `✓ scénario de test « Opéra — plan meublé (Staatsoper) » ouvert`, sélecteur « Scène active » = « Théâtre Staatsoper », panneau « Contenu du plan (445) », AUCUNE modale de fichier OS ; `editorOpen('la-diligence')` → `✓ campagne « L'Ennemi Intérieur — chapitre 1 : la route d'Altdorf » ouverte (copie)` ; id inconnu → `✗ … introuvable — projets : (aucun) | campagnes : … | scénarios : …` ; la commande `ouvrir(id)` passe par le pont canonique `src/state/editeurBridge.ts` et la MÊME voie que la modale « Ouvrir » (`loadSaved`/`loadBuiltin`/`loadScenario`), `loadSaved` rendant le REFUS de `parseProject` (juge de diff B2) ; (3) `__wfrp.roofCut(false)` sur `diligence` (bâtiment à toit, entré au clavier réel `KeyS`/`KeyA` de (17,2) à (10,17)) → `lève-toit OFF — toits, façades et décors de toit restent en place, hors le disque local de perçage autour de chaque héros`, capture : toit de tuiles sur tout le bâtiment, le héros visible dans son seul disque de perçage ; `roofCut(true)` → retour pixel pour pixel à l'état initial ; les deux drapeaux de recette (`debugRoofCut`, `debugLabels`) sortent de la save et du snapshot coop par l'ensemble nommé `HORS_SAVE` (juge de diff B1, défaut préexistant de `debugLabels` corrigé dans le geste) ; `docs/recette-navigateur.md` porte les trois rangées d'outillage et cinq pièges vécus ; console à 0 erreur sur les 10 sessions de sonde ; typecheck full 0, vitest 90 fichiers / 1363 tests, `npm run gen` idempotent, `docs:check` vert sur `61ad56a64`.

## Restes
- Le rejet d'`editorOpen(id inconnu)` est une RÉSOLUTION en `✗ …` et non une exception : un script de recette n'a pas besoin de `try/catch`, `docs/recette-navigateur.md` le dit à la rangée du helper -> RAS : c'est le contrat commun des helpers `__wfrp` (retour texte `✓`/`✗`, jamais un throw), mesuré par la recette et conforme à la doc
- `__wfrp.ready()` résout aussitôt quand le MÊME id de scène est rejoué (l'armement du voile est `[scene.id]`, le voile ne se relève pas) : limite DITE au JSDoc de `src/state/devtools.ts` et au message de `61ad56a64` -> RAS : un rejeu du même id ne recuit pas le monde, il n'y a rien à attendre ; la scène de recette dédiée `opera-plan` se rejoue depuis le menu
- Le zoom à la molette (`Input.dispatchMouseEvent{type:'mouseWheel'}` via CDP) ne traverse pas le listener de zoom de la caméra, et aucun helper `__wfrp` ne pilote le zoom : les captures des zones du rez restent cadrées au zoom par défaut -> inventaire #1680 : friction de recette mesurée le 2026-09-15 (2 essais, capture identique avant/après), à porter par le lot de recette du socle qui touchera la caméra, avec le geste réel documenté ou un helper de setup

## Recette visuelle
capture: public/qc/soldes/1478-diligence-roofcut-off.png — recetteur du 2026-09-15 (worktree `.wt-1478-1644-recette-opera`, port 5179) : scénario `diligence`, groupe entré dans l'auberge au CLAVIER réel jusqu'en (10,17), puis `__wfrp.roofCut(false)` — le toit de tuiles et les façades restent en place au-dessus du groupe, seul le disque local de perçage (#1176 M3) laisse voir le héros ; l'état `roofCut(true)` (défaut) et son retour sont identiques pixel pour pixel (captures `diligence-roofcut-on.png` / `-on-retour.png` du recetteur, non versionnées : même image que la baseline). Seconde preuve versionnée : `public/qc/soldes/1478-editeur-opera-plan.png` — l'éditeur ouvert par `editorOpen('opera-plan')`, scène active « Théâtre Staatsoper », sans dialogue OS. Console à 0 erreur.

## Réfutation
verdict: CONFIRMÉ — juge de diff adversarial (2026-09-15, six lentilles) : B1 (drapeaux de recette persistés en save et snapshot coop) et B2 (`ouvrir` rendant `✓` sur un document refusé) RÉFUTÉS puis corrigés en train court, mutations rouge→vert rejouées ; juge de la revue de palier `abedb5fd4..61ad56a64` (lentille « architecture à rebours » sur le lot de tête) : les claims « `entreeEnScene.ts` sans import de `src/gameIso` », « deux seuls sites de résolution du dégagement gardés par `debugRoofCut` », « pont canonique, jamais un shim », « `HORS_SAVE` = 4 clés » attaqués et TOUS écartés par re-mesure (`git grep`/`git show` à la tête) ; la recette en joueur a exercé chaque helper dans ses trois issues (succès, rejeu, rejet nommé) sans trouver de cas où le retour texte ment.
