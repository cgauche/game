# Solde #600 — réfs fausses BLANCHIES par la normalisation : garde de classe + corrections au texte

VERIFIE: contre-contrôle orchestrateur — `source.book` des 2 talents DÉJÀ corrects sur origin (contredit la prémisse du ticket, issue du juge #454 qui lisait une fiche périmée : book = nuits-agitees / ennemi-dans-l-ombre-compagnon, jamais LDB) ; vrai reste soldé (`empreint-d-ulgu` page 90→88, folio prouvé au texte data-folio 88 couvre la l.47) ; `AA 10 p.124→125` prouvé (folio 124 = tableau munitions, « Arme d'équipe » en 125) ; garde `check-entity-in-chapter` détecte réellement (test 12/12, stock 0) ; grep « INCORRECT/source incorrecte » = zéro après purge de la pierre tombale talents.md:1577 ; tsc 0, check-code-refs/check-entity exit 0, rebase sur origin (#606) sans conflit, page 88 préservée.

## Restes
- Garde `check-entity-in-chapter` limitée aux TALENTS (le cas prouvé) ; l'étendre aux sorts/créatures/qualités est un chantier séparé -> RAS : périmètre assumé, la classe est gardée là où elle a mordu
- Annotations de commentaire re-rendues dans folioRatchetStock.mjs (audit-folios --stock) -> RAS : re-rendu d'outil canonique, commentaires seuls, aucun impact sur le Set ni les tests (signalé par le juge comme bruit)

## Juge
Juge adversarial PARTIEL → corrigé : les 4 claims techniques CONFIRMÉS (book déjà juste, page 88, AA 10 p.125, garde réelle non décorative — normalisation saine, test rouge/vert prouve la détection) ; un seul défaut trouvé (pierre tombale survivante talents.md:1577 affirmant une source « LDB INCORRECT » désormais fausse) → purgée, grep de non-régression à zéro. Ratchet 122→121 vérifié légitime (page corrigée ne viole plus), pas un masquage.

## Réfutation
verdict: PARTIEL — le juge a attaqué (1) la justesse des folios (page 88 / AA 10 p.125, tous deux prouvés au texte), (2) le caractère réel de la garde (a planté un cas nom-absent → détecté ; la tolérance articles ne fait pas matcher deux talents différents), (3) la purge de la note « INCORRECT » (INCOMPLÈTE : 1 résidu faux survivant) → corrigé avant fermeture. Le fond tient, le résidu de poison est purgé.

Date : 2026-07-19
