# Solde #1791 — 2026-09-18

VERIFIE: le contrat du ticket (quatre canaux scalaires d'`ActiveEffect` — `skillMods`, `moveScale`, `moveMod`, `maxWeaponHands` — doublaient le canal canonique `passive`, trois émis sans `src` ni `label` donc invisibles à `sourceSuspended`) est corrigé à la source : les quatre champs sont SUPPRIMÉS de `types.ts`, les quatre `case` de `applyOps` poussent l'op authorée dans `passive` (sans ses champs de durée, `sansDuree`), `passiveMods` n'a plus qu'un lecteur d'effet (`e.passive`, `src: effectRef(e)`, `label`), la durée « intrinsèque sinon contexte » vit dans une seule fonction `durationFromOp` (5 sites), `SAVE_VERSION` 49 → 50 (bump + rejet, politique `saves.ts:17-25`), test `active-effect-passive.test.ts` (7 cas, mutation « `src` retiré » → 2 rouges), diff moteur relu par l'orchestrateur, sonde du juge « Peau d'écorce et d'os » rejouée (0 Round à FM < 10, VDM 15 l.406).

## Restes
- Le « minimum de 1 Round » (AA 07 l.113) reste un plancher PAR TYPE D'OP (`durationFromOp(…, 1)` aux sites `moveScale`/`maxWeaponHands`), pas une propriété de la formule de l'entrée ; même artefact dans `resolveWindow` (`ops.ts:188-189`) -> #1797
- « migration des effets sauvés » annoncée à l'ouverture (ticket) -> RAS : ce dépôt n'a pas de chaîne de migration de sauvegarde (arbitrage utilisateur 2026-08-17, `saves.ts:17-25`) — bump + commentaire daté + rejet à `readSlot`, test du pas 49 → 50 ajouté.
- Grief 2 du ticket (« un `skillMod` avec `durationRounds` propre est ignoré ») -> RAS : `skillMod`/`moveMod` n'ont pas de champ de durée dans leur type (`ops.ts:940`, `:1000`) et aucune donnée n'en porte ; la répétition du patron de durée (5 sites, 3 formes) est, elle, corrigée dans ce commit (src/engine/ops.ts:1342).
- `saves-flow.test.ts:344` portait un cardinal vivant `toBe(49)` (codeur) -> corrigé dans ce commit (src/state/saves-flow.test.ts:344)
- Garde « tout `PassiveMod` porte un `src` » envisagée au ticket -> RAS : quatre émetteurs sans entité au Codex la rougiraient à dessein (séquelles, Faim, Soif, Ivresse, `trauma.ts:964-1003`, repli `CHAR_PENALTY_KIND_LABEL`) ; l'invariant de CE lot est structurel (un seul lecteur d'effet, `src` posé au seul site) et prouvé par mutation.

## Réfutation
verdict: PARTIEL — juge (2026-09-18, lecture seule, diff du lot, sondes `applyOps` jouées sur la base ET sur le lot) : (1) RÈGLE INVENTÉE — le plancher `Math.max(1, …)` généralisé à `charMod`/`light` octroyait 1 Round à `peau-d-ecorce-et-d-os` là où le livre en donne 0 (VDM 15 l.406 sans minimum ; « minimum de 1 » écrit seulement pour Aux Armes, AA 07 l.113) → plancher en PARAMÈTRE explicite, posé aux seuls sites qui l'avaient avec la réf nue, test ajouté (0 Round / 1 Round) ; (2) `charClockMin` « strictement équivalent » FAUX — une horloge héritée de `ctx.defaultUntilTime` imprimait une durée (voire `until` brut sans `now`) au lieu du fragment « hors combat » → alimenté par la seule échéance INTRINSÈQUE, test de journal ajouté ; (3) l'op stockée dans `passive` transportait `durationRounds` non résolu → `sansDuree`, assertion ajoutée ; (4) docstring de `passiveMods` « kind `intrinseque` » pour un sort → `magique`. Résiste : aucun consommateur résiduel des champs supprimés (`git grep` src/scripts/docs), pas de double compte, limite mesurée de `suspension.ts` toujours vraie, test neuf à valeurs discriminantes, bump de save conforme, docs sans canal scalaire.
