# Solde #1493 — 2026-09-04 (L2 T2, programme #1679)
VERIFIE: #1493 et #1614 sont le MÊME rapport ouvert deux fois par `canari.yml`, et la cause est fermée par ce commit. Les deux titres sont identiques à l'octet (`Canari rouge — environnement ou suite cassés`, mesuré par `gh issue view <n> --json title`). Les deux CORPS, en revanche, ne le sont PAS — ils portent chacun le lien de LEUR run : #1493 (créée le 2026-08-24T07:08:39Z) porte `Run: https://github.com/cgauche/game/actions/runs/32700173502`, #1614 (2026-08-31T13:16:21Z) porte `Run: …/33396001399`. #1614 n'est donc pas un sur-ensemble de #1493 : rien n'a été recopié par ce commit, et le RENDU du train demande à l'orchestrateur de reporter le lien du run 32700173502 dans #1614 avant la fermeture (une écriture GitHub n'appartient pas au codeur). Le doublon lui-même est mesuré et daté : 9 issues de titre `Canari rouge` existent, 2 ouvertes (#1493, #1614) et 4 fermées le 2026-08-30 sous le label `duplicate` (#906, #1039, #1229, #1352) — six fois le même rapport. La CAUSE : `canari.yml` faisait `gh issue create` à CHAQUE run rouge, sans jamais chercher une issue ouverte, avec un repli sans label parce que le label `canari` n'existe pas sur le dépôt (`gh label list --search canari` rend une liste VIDE, mesuré le 2026-09-04) ; et le canari était rouge à chaque run (5/5), 4 fois sur `npm audit --audit-level=high` d'un stock connu. Ce commit ferme les deux volets : le label est créé idempotemment (`gh label create canari --force`), l'issue SURVIVANTE est la plus ANCIENNE ouverte dont le titre commence par `Canari rouge` — elle est COMMENTÉE, la création n'ayant lieu que si aucune n'est ouverte (`.github/workflows/canari.yml`, step « Résumé du canari ») —, et les vulnérabilités ne rougissent plus le canari par elles-mêmes : `scripts/ops/audit-stock.mjs` juge l'ÉCART à un stock nominatif daté de 7 entrées (7 jaunes, 0 rouge à la mesure du 2026-09-04). Contrat verrouillé par `scripts/ops/canari.test.mjs` (10 tests : issue survivante triée par `createdAt`, création conditionnée, label idempotent, aucun `npm audit` brut). Après ce commit, #1493 étant fermée, la « plus ancienne ouverte » que le workflow trouvera est #1614 — la règle et le survivant coïncident.
## Restes
- Le lien du run 32700173502, porté par le corps de #1493 et absent de celui de #1614, doit être reporté sur le survivant « Canari rouge — environnement ou suite cassés » (titre exact recollé de `gh issue view 1614 --json title`), qui porte désormais le canari idempotent posé par ce train — écriture GitHub, donc geste de l'orchestrateur -> #1614
## Réfutation
verdict: PARTIEL — verdict SOUMIS AU JUGE de diff (le brief du train demandait « À JUGER » ; `validateSolde` n'admet que CONFIRMÉ, PARTIEL ou RÉFUTÉ, et PARTIEL est le plus proche du fait). La preuve tenue par ce train est la MESURE (les deux corps confrontés à l'octet, le label absent du dépôt, les 4 doublons déjà labellisés, les 5 runs rouges et leur step de mort) et la MUTATION des portes neuves (résumé privé d'un `id` → rouge nominatif ; `gh label create` privé de `--force` → rouge ; recherche de l'issue survivante retirée → rouge). Ce que ce solde N'A PAS prouvé, et qui appartient au juge : que le canari REDEVIENDRA vert — la première exécution réelle du workflow réécrit ne peut avoir lieu qu'après le push (cron du lundi 6 h ou `workflow_dispatch`), et aucune des mesures locales ne remplace ce run.
