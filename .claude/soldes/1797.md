# Solde #1797 — 2026-09-18

VERIFIE: le contrat du ticket (le « minimum de 1 Round » d'AA 07 l.113 / LDB 18 l.88 était un plancher PAR TYPE D'OP au moteur, `durationFromOp(…, 1)` aux sites `moveScale`/`maxWeaponHands`, et d'autres planchers de durée sans réf vivaient dans `ops.ts`, `magic.ts`, `combatFlow.ts`) est corrigé à la règle : la grammaire `Formula` porte un nœud `{minimum, of}` (résolution, validité, espérance, schéma zod, humanisation « (minimum de N) », éditeur avec formes « Minimum » et « Somme de termes » récursives, lexique et stock des structures), les deux entrées « Choc violent au bras » de `criticals.json` reçoivent leur borne par migration datée idempotente, et TOUS les planchers de durée du moteur sont retirés (`durationFromOp` Rounds et horloge, `resolveWindowDuration`, op `condition`, `castPenalty` ×4, `durationClockMinutes` de `magic.ts`, durée de zone et `scheduleRespawn` de `combatFlow.ts`) ; `git grep "Math.max(1" -- src/engine src/state` ne rend plus aucune durée ; diff relu par l'orchestrateur ; recette navigateur de l'éditeur sur la donnée migrée.

## Restes
- Miracle « Aux Innocents les Mains Pleines ! » (Criminalité urbaine l.657, « DR Heures (minimum 1) ») : entrée STUB en donnée (`spells.json:14884-14902`, `duration: null`, aucune étape) — la borne s'écrira dans sa formule quand la durée sera modélisée -> RAS : dette de DONNÉE déjà portée par le doc généré `docs/sorts-implementation.md` (entrée non mécanisée), pas de moteur ; la grammaire sait désormais l'exprimer.
- Aura de démence du jabberslythe (ADE 07 l.321, « minimum une heure ») : aucune entrée en donnée -> RAS : rien à migrer, la grammaire sait désormais l'exprimer.
- Défaut de fenêtre de Détermination `w === undefined → { rounds, left: 1 }` (`ops.ts:193`) -> RAS : c'est la valeur maison « jusqu'à la fin du Round » (LDB 16 l.117), un défaut, pas un plancher sur une formule.
- Clé `atLeast` demandée au brief -> RAS : collision avec quatre concepts existants (`flowCore.ts:133/145/153`, `ops.ts:862`, `traumas.ts:19`) et refus du stock hors-strate ; `minimum` ne nomme que ce concept.
- Zone de sort : la Surincantation de Durée n'y ajoutait pas le BONUS de Rounds (chemin parallèle sans `overcastDurationParts`, révélé par le retrait du plancher) -> corrigé dans ce commit (src/state/combatFlow.ts:5966)
- Recette : l'op qui porte la règle (`maxWeaponHands`, et `moveScale`) n'avait AUCUN formulaire dédié — repli JSON brut, la borne n'était pas éditable aux clics (règle stricte 2) ; `charMod` éditait sa durée par un `NumberField` qui perdait toute formule -> corrigé dans ce commit (src/ui/editor/GameOpEditor.tsx:747) : `DureeRoundsField`, source unique des trois ops à durée de Rounds seule.
- Recette : la phrase mécanique de la fiche taisait la durée de `maxWeaponHands`/`moveScale` (« ne peut manier que des armes à 1 main(s) ») alors que la Description à côté dit « (minimum de 1) » -> corrigé dans ce commit (src/ui/compendium/humanize.ts:74) : `pendantRounds`, source unique du fragment.
- Recette 360 px : trois menus de type d'op `.eff-add-menu` dépliés débordent de 9 px (pré-existant, mesuré par `verdictDebordement`) -> #1798

## Recette visuelle
Recette navigateur en joueur (Chrome headless CDP, clics et clavier réels, console 0 erreur), 2026-09-18, sur l'arbre final :
- capture: public/qc/soldes/1797-01-fiche-minimum-de-1.png — Compendium › fiche « Choc violent au bras » : la durée rendue porte « (minimum de 1) », verbatim des deux livres.
- capture: public/qc/soldes/1797-02-atelier-minimum-somme.png — Atelier › Éditer › op `maxWeaponHands` : forme « Minimum » (borne 1) et, imbriquée, « Somme de termes » avec ses deux termes (1d10 ; Bonus d'Endurance × −1) — la donnée migrée se relit entière.
- capture: public/qc/soldes/1797-03-borne-modifiee-2.png — saisie clavier réelle de la borne (2) : somme inchangée, donnée valide au schéma.
- capture: public/qc/soldes/1797-04-sortie-minimum-conserve-somme.png — quitter « Minimum » pour « Somme de termes » conserve la somme (symétrie de sortie), retour à « Minimum ».
- capture: public/qc/soldes/1797-05-mobile-360-minimum.png — même écran à 360 px, sans débordement.

## Réfutation
verdict: PARTIEL — juge (2026-09-18, lecture seule, diff du lot, sondes `npx tsx` base/lot, rejeu de la migration hors dépôt) : (1) MAJEUR demi-migration — `magic.ts:459` (`durationClockMinutes`, 245 sorts à formule variable) et `combatFlow.ts:5964` (durée de zone) gardaient le même plancher sans réf → retirés, zone alignée sur `overcastDurationParts` (source unique, LDB 47 l.15), `scheduleRespawn` (même classe) retiré, tests BFM 0 → 0 minute et zone Surincantée ×3 + bonus ; (2) MAJEUR éditeur — pas de forme `sum` : la donnée migrée affichait « Dés » et un champ VIDE, toute élection l'écrasait → forme « Somme de termes » récursive, test réécrit sur la donnée réelle (relecture entière, geste clavier, `safeParse`) ; (3) quitter « Minimum » jetait la formule → repart de `of` ; (4) borne sans contrainte → `z.number().int().nonnegative()`, `min` propagé ; (5) « (minimum 1) » → « (minimum de 1) » (verbatim). Résiste : verbatims de migration exacts, corpus VF exhaustif (deux clauses de durée, toutes deux migrées), aucune donnée ne bascule ≤ 0, expiration saine (`left ≤ 0` retiré à la frontière de Round), `min: 1` de la règle optionnelle ENFORCÉ par `NumberField`, `formulaSinSchema` hors de portée, espérance sans consommateur de durée, migration prouvée aller + idempotence, cliquet `STRUCTURES_CIBLES` 39 → 40 cohérent.
