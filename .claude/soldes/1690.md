# Solde #1690 — 2026-09-07 (lot 3 ; lots 1-2 du 2026-09-06)

VERIFIE: les trois lots du ticket confrontés à l'arbre — (1) lot 1 : inventaire (25 terrains, pas 24 — ticket corrigé), invariant, juge de design ; (2) lot 2 (1cac27bb6, CI verte) : 0 fichier TS de terrain (`src/state/terrain/defs/` supprimé, `_registry.generated.ts` mort), `src/data/terrains.json` par `document('terrains','entite')`, façade vive `src/state/terrain/index.ts`, `layerSchema.tiles: z.array(idDe('terrain'))`, dump unique prouvé par golden `TerrainMeta` ×25 et hash volumique `FaceSurface` ; (3) lot 3 (ce commit) : catégorie Codex « Terrains » (Monde), `edit.dataset` (formulaire d'atelier, `overlayProp` en sélecteur `props`), dégradés SVG VIFS (`defsGlobaux()` mémoïsée par témoin de contenu, 81 importateurs), `maison` par entrée, dette d'exposition `DETTES_EXEMPT` −1 ; DoD « `TerrainMeta` et le rendu à l'identique » re-mesurée APRÈS le lot 3 : sonde `FaceSurface` 66 scènes / 95 121 faces, hash global `8b4c2d024bad922c…` identique à la base du lot 2. Trouvaille du socle traitée dans le geste : l'arbitrage `maison` d'une entrée (exigé par `document()` sur tout dataset sans folio) n'était affiché sur AUCUNE fiche du Codex — `depuisEnveloppe` le projette désormais (15 catégories / 102 items, dont `axes` qui court-circuitait l'enveloppe), rendu UNE fois dans `CodexEntry` ; et une rangée `couleur` de socle (pastille peinte + hex) fait VOIR la teinte et la rampe.

## Restes
- Onglet Codex « Terrains » éditable au geste (lot 3 du ticket) -> corrigé dans ce commit : src/ui/compendium/registry.ts:2045
- Dégradés SVG assemblés une fois à l'import (écart dit au lot 2, `sprites.ts` `DEFS`) -> corrigé dans ce commit : src/gameIso/sprites.ts:83
- `detail` inféré `kind:'object'` à l'atelier (écart dit au lot 2, piste `dedicatedFieldKeys`) -> RAS : `kind:'object'` est le sous-formulaire récursif ÉDITABLE (`CodexEdit.tsx:571`) ; l'y mettre le retirerait du formulaire — la piste du lot 2 était fausse
- Sous-clés de la recette `detail` (`seedScope`, `tintVar`, `perM2`, `hM`) en clés nues à l'atelier, identique sur `materials.json` (même `detailRecipeSchema`) : `MetaChamp` ne nomme que le PREMIER niveau d'un document (`registry.ts:236-248`) -> inventaire #1689 : limite du socle des libellés, préexistante au lot, mesurée sur 2 datasets
- Repeint de l'AFFINE après une édition Codex des `stops` : `EditorCanvas.tsx:292` (`useMemo` sans le dataset dans ses deps) et `GlobalSvgDefs` (`App.tsx:76`, sans abonnement) ne se réémettent qu'au MONTAGE — la recette voit le repeint au changement d'écran, pas par abonnement -> inventaire #1689 : réactivité des écrans au dataset édité, même classe que le témoin de contenu de la façade
- Atelier : suppression d'une entrée sans affordance, « + Entrée » muet tant que le 1er offset est vide (`RecordTextField`, `CodexEdit.tsx:1819`), refus mêlant messages Zod anglais et ligne FR (`validate.ts:23`), « Enregistrer » = téléchargement du JSON (`CodexEdit.tsx:603`) -> inventaire #1689 : préexistants au lot (aucun de ces fichiers n'est dans son diff), relevés par la recette
- Titre d'une section UNIQUE jamais peint en lecture (« Rendu » du terrain) : `TabbedEntry.tsx:60` masque la barre à un onglet, le sous-titre « Arrêts du dégradé » reste visible -> RAS : régime préexistant de toutes les fiches à section unique, la recette n°2 le mesure au DOM
- « 24 terrains » dans le titre et l'état du ticket -> RAS : la mesure dit 25, corrigée au lot 1 (commentaire du ticket) et dans la fiche mémoire

## Recette visuelle
capture: public/qc/1690-codex/c-01-herbe-swatch.png
capture: public/qc/1690-codex/c-02-gallery-swatch.png
Les deux captures ci-dessus sont postérieures à la DERNIÈRE retouche d'écran du lot (`.swatch`). Les états antérieurs sont documentés par les rapports de recette sous `node_modules/.cache/` (recette n°1 : 28 images `0x-*.png` — liste, fiche `herbe`, `sous-bois`, formulaire, plan repeint, refus, création, mobile ; recette n°2 + vision : 6 images `b-*.png` — note « Arbitrage maison », règle optionnelle, mobile), toutes sous `public/qc/1690-codex/`.

## Réfutation
verdict: CONFIRMÉ — trois passes de juge en lecture seule et trois recettes navigateur sur l'arbre du lot 3. Juge de diff n°1 (`juge-1690-L3.md`) : ACCEPTE-SOUS-CORRECTIONS — fraîcheur de `defsGlobaux()` NON réfutée (`overrides.ts:323-326` splice en place, l'atelier remplace l'entrée par un objet neuf), coût NON réfuté (`sceneMeshes.ts:819/1298` derrière les mémos de bake), 0 site de l'ancienne const `DEFS`, cliquets tous MESURÉS ; exigences B1 (docs manuscrits nommant `DEFS`), B2 (`docs:check` rouge), B3 (titre de test promettant l'écran) portées. Recette n°1 (`recette-1690-L3.md`, 28 captures) : 25 fiches, libellés FR, prop par LIBELLÉ, refus nominatif à la création, repeint du plan sans rechargement, 0 erreur console — et trois trouvailles de SOCLE (`maison` invisible sur toute fiche, couleur en hex nu, atelier) → correctifs C1-C6. Juge n°2 (`juge-1690-L3b.md`) : ACCEPTE-SOUS-CORRECTION — sonde de gel « re-hash privé de `maison` = empreinte de HEAD » catégorie par catégorie : 14 gels déplacés, 14 expliqués par le SEUL ajout de `maison`, 0 clé perdue, `axes` sans `source` (aucun badge par effet de bord) ; note `maison` visible sur TOUS les onglets (hors `TabbedEntry`) ; dette de repeint au montage VRAIE (`EditorCanvas.tsx:292`, `GlobalSvgDefs`) ; E1 cardinal faux corrigé à la mesure (172 entrées portent `maison`, 101 de premier rang + 1 imbriquée atteignent une fiche, 71 muettes = props 41 + actions 30 SANS catégorie Codex, aucune projection manuelle restante) ; S1 pastille décorative `aria-hidden` ; S2 UNE primitive `.swatch` (`components.css`) remplace `.codex-pastille` et `.gallery-swatch-color` (0 occurrence), `.terrain-swatch` conservée = bouton d'outil de palette, concept différent. Recette n°2 + juge vision (`recette-1690-L3b.md`) : VERT, note et pastilles composent `.codex-sec`/`.codex-kv` existants, aucune classe ad hoc ; écarts nommés préexistants (titre de section unique, empilement `--font-display` + small-caps de `.section-label`). Recette n°3 (`recette-1690-L3c.md`) : `.swatch` peinte au Codex et à la galerie. DoD re-mesurée après le lot : sonde `FaceSurface` 66 scènes / 95 121 faces, hash `8b4c2d024bad922c…` identique. Ce que les juges n'ont PAS joué : la suite complète et les gates — jouées par `npm run gates` avant le push (justificatifs sous `node_modules/.cache/gates/`).
