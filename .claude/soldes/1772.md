# Solde #1772 — 2026-09-15

VERIFIE: le DoD du ticket confronté au diff de `scripts/gates/toutes.mjs` et `scripts/gates/toutes.test.mjs` sur `.wt-1772` (base origin/main 9c8da035a) — (1) N rouges dans des lanes distinctes rendus en UN run : tests « DEUX rouges dans DEUX lanes distinctes sont rendus par UN SEUL run » et « DEUX rouges dans la MÊME lane sont tous deux JOUÉS et rendus » (deux lignes `— ROUGE (exit N)`, exit 1, `doesNotMatch(/sautée/)`) ; (2) `--serie` rend les mêmes verdicts : test « --serie rend les MÊMES verdicts que les lanes : deux rouges, deux lignes, exit 1 » (`principal` entier rejoué avec `--serie`, 4,9 s), morsure d'équivalence de `lanesAJouer` inchangée ; (3) les cas légitimes de « sautée » tiennent : réécriture de l'arbre (test « une gate de la phase SÉRIE qui réécrit l'arbre est REFUSÉE » + assertion neuve : l'écrivain suivant s'imprime `sautée — 0.0 s — <fautif> a réécrit l'arbre — coût inconnu`, jamais « déjà justifiée »), signal (`arreterSurSignal` → `process.exit(130)`) ; le prérequis absent n'est PLUS une cause d'arrêt (chaque gate teste SES prérequis, `prerequisAbsents`) : test « un PRÉREQUIS absent ne fait sauter AUCUNE gate — ni sa lane, ni les autres » ; (4) rien n'est ajouté au chemin vert : `poser` ne fait plus qu'un `verdicts.set` + journal, la garde morte de `jouerLane` est retirée, `code` comptait déjà tout verdict non vert (résumé) ; (5) `docs/reprise-apres-pause.md` ne cite pas l'arrêt (grep 0). Rejoué par l'orchestrateur : `node --test scripts/gates/toutes.test.mjs` 33/33, eslint 0 ; mutations du codeur : armement remis dans `poser` → 3 tests rouges nommés ; `break` remis → l'assertion « déjà justifiée » rougit seule ; restaurations à l'octet (blake3).

## Restes
- En `--serie`, la phase 1 est sautée et les écrivains passent dans la lane unique : un écrivain qui réécrit l'arbre n'y est vu que par la photo finale, sans « sautée » — préexistant, mode diagnostic -> RAS : `--serie` est le mode de MESURE (coût plein, rien ne tourne à côté), pas le mode de publication ; le verdict final reste juste (photo de fin)
- Le premier rouge est désormais nommé au RÉSUMÉ, après le mur complet (~300-440 s), au lieu de ~50 s quand un rouge rapide coupait tout -> RAS : arbitrage du ticket, mesuré — un passage de publication économisé vaut plus que 4 minutes de premier retour, et la lane la plus longue (`test`) bornait déjà le mur de chaque passage
- `docs/reprise-apres-pause.md` (GÉNÉRÉ) ne décrit pas la politique d'arrêt des gates -> RAS : l'en-tête de `toutes.mjs` (§ « UN ROUGE NE COUPE RIEN ») et la liste fermée de `arret` la portent, au seul foyer

## Réfutation
verdict: CONFIRMÉ — juge de diff (2026-09-15, lecture seule, `.wt-1772`, 7 lentilles sur le cumul #1772 + #1773) : lentille 1 TIENT (réécriture d'arbre → écrivains restants et lanes « sautée », signal → exit 130, EXPIRÉE → verdict + exit 1 + arbre tué au plafond, `justifie.mjs:57` n'écrit un justificatif qu'au code 0, aucune dépendance croisée entre gates — sonde : aucune entrée `ecrit` dans `ECRIT_LU`, aucun écrivain hors phase 1) ; lentille 2 TIENT (0 consommateur de la graphie « sautée »/`arret` dans `pre-push.mjs`, `publier.mjs`, `reprise-apres-pause.md` : `publier` ne lit que le code de sortie et les justificatifs) ; lentille 6 TIENT (les 4 tests neufs spawnent de vraies gates et lisent code + résumé). Souhaitable retenu : le coût du premier retour est dit au message de commit et au solde. Aucun bloquant sur ce lot.
