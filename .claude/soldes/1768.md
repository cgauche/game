# Solde #1768 — 2026-09-15

VERIFIE: le DoD du ticket confronté à l'arbre `.wt-1768-board` (base origin/main 3113daad1) et au Project GitHub réel — (1) `npm run ops:board -- --liste` joué par l'orchestrateur depuis le worktree : 31 lignes (`En cours=2 Dormant=7 Ouvert=2 Fusionné=18 Fermé=2`, 14 anomalies nommées : 3 worktrees détachés, 11 branches sans ticket dérivable), base `origin/main` après `fetch`, `#1768 Ouvert` / `#1727 En cours +1 / −17` / `#1392 Fusionné` / `#1751 Fermé` (worktree encore posé) ; (2) `npm run ops:board -- --creer` joué le 2026-09-15 : Project « Chantiers » (#1, `PVT_kwHOAGp6Ic4BZr9a`) lié à `cgauche/game`, champ intégré `Status` réécrit en `En cours/Dormant/Ouvert/Fusionné/Fermé` (mutation `updateProjectV2Field`), champs `Branche`/`Worktree`/`Avance`/`Dernier commit` créés, 31 items posés — relu par `gh project field-list 1` et `gh project item-list 1` (`Dormant=7 · En cours=2 · Fermé=2 · Fusionné=18 · Ouvert=2`, `#1768 Ouvert | chantier/1768-board | +0 / −0 | 2026-09-15 | …/.wt-1768-board (tenu)`) ; second `npm run ops:board` immédiat = aucun geste (`ajoutés 0 · mis à jour 0 · archivés 0`) ; (3) `node --test scripts/ops/board.test.mjs` → 37/37 rejoués par l'orchestrateur, dont les sondes du juge promues en tests (valeur vide effacée par `--clear`, second passage sans geste, archivage d'un item non mesuré à issue CLOSED, citation chaînée `refs #A #B`, `Ouvert` à 0 commit sans preuve de publication, base paramétrée : câblage `main...` → rouge) ; `npx eslint` sur les deux fichiers → 0 ; (4) `CLAUDE.md:114` (et son miroir `AGENTS.md`) porte `ops:board [-- --liste|--creer]`, `package.json` porte `ops:board`.

## Restes
- La VUE « Board » (colonnes groupées par `Status`) n'est pas exposée par l'API Projects v2 -> RAS : réglage UNIQUE dans l'UI GitHub (Layout → Board, Group by → Status), dit par l'outil à la sortie de `--creer` (`MOT_DE_LA_VUE`, `scripts/ops/board.mjs`)
- 14 anomalies à la mesure (3 worktrees détachés dont `.codex/worktrees/914b`, 11 branches sans ticket dérivable dont 9 `worktree-agent-*` à 0 d'avance) -> RAS : c'est le stock de NETTOYAGE, imprimé nommément par l'outil à chaque run ; le geste est `npm run ops:worktrees -- --purger`, pas le board
- Le pied d'empreinte de `docs/consommateurs-de-champs.md` alterne entre deux valeurs à corps constant (24 chemins lus hors racine) : `docs:check` rouge au hasard sur un arbre propre, préexistant au lot -> #1769
- `chantier/1478-1644-recette-opera` sort `#1478 Ouvert` et `#1644 Fusionné` sur la MÊME branche -> RAS : la preuve de publication est mesurée PAR TICKET (citation par un commit de la base), pas par branche — c'est la règle posée au design ; une branche à deux tickets dit deux états s'ils diffèrent

## Réfutation
verdict: CONFIRMÉ — juge de DIFF (2026-09-15, lecture seule, worktree `.wt-1768-board`) : idempotence RÉFUTÉE (valeur vide jamais posée : une branche supprimée restait affichée à vie), citation chaînée `refs #A #B` perdue (#1644 : 8 citations, 0 retenue), `Fusionné` à 0 commit sur un chantier fraîchement ouvert (#1768, #1392), mot d'instruction « Group by → Statut » sur un champ nommé `Status`, `synchroniser` sans test — CINQ bloquants, tous corrigés dans le train suivant, chacun avec la sonde du juge promue en test (`board.test.mjs` 29 → 37) ; souhaitables corrigés (replis `?? ''` devenus anomalies nommées, morts `titre`/`fetchLu`/`principal` purgés, `jourDe` au jour du committer, en-tête ancré aux shas `e6c11db77`/`3113daad1`). Rejoué par l'orchestrateur après le train : 37/37, eslint 0, `--liste` réel, `--creer` réel puis second run sans geste (VERIFIE). Non joué par le juge : les gates du train — jouées UNE fois par `ops:publier` sur la tête avant le push.
