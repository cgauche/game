# Solde #1779 — 2026-09-17

VERIFIE: le contrat du ticket (« chaque workflow du dépôt est dans l'un de deux états, MESURÉ par une garde — (a) vu par la porte […], ou (b) se nomme lui-même en rougissant (patron `canari.yml`), ou (c) figure dans une liste blanche NOMMÉE avec sa raison […]. Un workflow neuf qui n'est dans aucun des trois états fait rougir la garde ») confronté à `main` PUBLIÉE (`966e1a451`, `d1524e669`, `b05623f34`, run CI 35257386119 vert) et aux trois points du DoD, chacun MESURÉ. (1) « garde verte sur les quatre workflows » : `scripts/gates/workflowsDuDepot.test.mjs:37` (`verdict({ cwd: RACINE })` vide) et `:41` (`mesurerEtat` rend sur chaque YAML réel exactement l'état déclaré au registre `scripts/gates/workflowsDuDepot.mjs`), joués par `test:hooks` (994/994, les deux tests neufs SUIVIS par git donc listés par `testsParGate`). (2) « un workflow ajouté sans état déclaré fait rougir la garde, test committé » : `:116` (copie jetable de `.github/workflows/` + `neuf.yml` → constat « n'est déclaré dans AUCUN état »), et les mutations voisines `:128` (step signaleur retiré d'un `autosignale`), `:141` (`on: push` ajouté à un `manuel`), `:154` (se signale sans être déclaré), `:170` (déclaré absent du disque). (3) « un `workflow_dispatch` de `deps-report.yml` forcé en échec produit une issue nommant le run » : mesuré sur GitHub, sans knob dans le code — branche jetable `chantier/1779-sonde` créée par l'API depuis `main` (b05623f34), `deps-report.yml` y reçoit `- run: exit 1` AVANT `npm ci` ; `gh workflow run deps-report.yml --ref chantier/1779-sonde` → run 35258885513 : `failure Run exit 1`, `skipped Run npm ci`, `success Se nommer en rougissant` ; issue #1785 « Rapport de dépendances rouge » créée par `github-actions`, corps « Run https://github.com/cgauche/game/actions/runs/35258885513 : job `failure` » — le run est NOMMÉ. Second temps : un deuxième rouge (run 35259339195, sonde mal formée : `run: echo "SONDE #…"` lu par YAML comme un commentaire) a été COMMENTÉ sur la survivante #1785 et non re-créé (contrat « survivante = plus ancienne ouverte » tenu au runner) ; troisième dispatch VERT (994fc0809, run 35259478157, tous steps `success`) → le signaleur a commenté « job `success` » puis FERMÉ #1785 (closedAt 2026-09-17T18:33:12Z) : rouge → ouverture, rouge → commentaire, vert → fermeture, les trois gestes de `decider` mesurés sur le chemin réel ; branche jetable supprimée (`gh api -X DELETE …/git/refs/heads/chantier/1779-sonde`), aucune issue parasite (le rapport mensuel de `deps-report.mjs` ne commence pas par le préfixe). Mesure de l'état sur ce qui s'EXÉCUTE : `corpsRun` (`workflowsDuDepot.mjs:130`, corps shell des `run:` commentaires ôtés) et `joueSurRouge` (`:158` : `always()`, `!cancelled()` ou `failure()` non nié, jamais en conjonction avec `success()`), sondes du juge promues en tests (`:57` `!failure()` et `success() && !cancelled()` ne valent pas ; `:66` chemin du signaleur en commentaire ne vaut pas ; `:72` les trois formes vraies valent) ; `porte` confrontée aux args réellement passés à `gh` par `coursesCi` (`:182`, `coursesCi.mjs:76` lit `PORTE`) et au lecteur `gatesDeCi` (`:103`) ; aucun `gh issue` en ligne dans un YAML (`:81`, morsure `:89`). Signaleur unique `scripts/ops/signaler-rouge.mjs` (label `--force` à description FIXE `DESCRIPTION_LABEL`, survivante, commentaire, fermeture sur vert, création sur rouge, `stdio[0] = 'ignore'` sur chaque `gh`), appelé par `canari.yml` (qui garde son `exit 1`) et `deps-report.yml` ; `signaler-rouge.test.mjs` 10 cas (`test:ops` 250/250), `canari.test.mjs` réduit à la délégation. `docs/reprise-apres-pause.md:185-188` (généré) : colonne « État » lue du registre, `build-reprise.mjs` abandonne sur un `.yml` sans entrée. Gates du lot au dernier rejeu : test:hooks 994/994, test:ops 250/250, typecheck (sortie vide), lint, docs:check OK ; CI de branche verte après `d1524e669` (fixtures `banc`, run 35257386119). Juge de diff (lecture seule, neuf lentilles) et train de corrections, verdict ci-dessous.

## Restes
- mesure `autosignale` SYNTAXIQUE (`!failure()`, `success() && !cancelled()`, chemin du signaleur en commentaire → faux-vert, juge L1) -> corrigé par 966e1a451 scripts/gates/workflowsDuDepot.mjs:130
- description du label partagé `canari` réécrite à chaque run selon le préfixe appelant (juge L2) -> corrigé par 966e1a451 scripts/ops/signaler-rouge.mjs:28
- littéral `'ci.yml'` résiduel dans `build-reprise.mjs` qui importe déjà le registre (juge L4) -> corrigé par 966e1a451 scripts/docs/build-reprise.mjs:184
- « jamais un `gh issue create` en ligne dans un YAML » n'était mesuré par rien, et `gatesDeCi.mjs` n'était confronté à `PORTE` par aucun test (juge L4/L5) -> corrigé par 966e1a451 scripts/gates/workflowsDuDepot.test.mjs:81
- `push:` indenté à 3 espaces SOUS `workflow_dispatch:` est mesuré `manuel` (juge L1 cas G) -> RAS : YAML valide = clé imbriquée sous `workflow_dispatch`, pas un déclencheur ; GitHub ne le lit pas comme un `on: push`
- `deploy.yml` déclaré `manuel` reste hors de toute porte -> RAS : le ticket le nomme comme liste blanche avec sa raison (CLAUDE.md § Pile, « prod — sur demande explicite SEULEMENT »), raison portée au registre et à `docs/reprise-apres-pause.md:187`

## Réfutation
verdict: PARTIEL — juge de diff (2026-09-16, lecture seule, neuf lentilles, sondes de mutation hors arbre sur copie jetable) : L1 RÉFUTÉ — trois faux-verts reproduits (`if: !failure()`, `if: success() && !cancelled()`, chemin du signaleur en commentaire seul → `autosignale=true`, `verdict()` 0 constat) : la garde signait un workflow qui ne se nommerait JAMAIS sur rouge → corrigé (`corpsRun` + `joueSurRouge`, sondes promues en tests A/B/C, mutation rejouée rouge) ; L2 fidélité du signaleur à `canari.yml` l.241-266 TIENT, une dérive nommée (description du label partagé) → corrigée (description fixe) ; L3 `deps-report.yml` TIENT sur pièces, `job.status` non mesurable localement → mesuré ENSUITE sur GitHub (runs 35258885513 rouge → #1785 créée, 35259339195 rouge → commentée, 35259478157 vert → fermée) ; L4 FRAGILE (littéral `'ci.yml'` dans `build-reprise.mjs`, `gatesDeCi.mjs` non confronté à `PORTE`) → corrigés ; L5 tests TIENT (quatre cas migrés à force égale, aucune tombale), trou de couverture nommé (`gh issue create` en ligne) → gardé (`:81`, morsure `:89`) ; L6 doc dérivée TIENT, nit « Porte » → « État » corrigé ; L7 poison TIENT ; L8 architecture TIENT (l'état `manuel` est indécidable sans déclaration : le registre est contre-mesuré, pas un smell) ; L9 DoD 1 et 2 TENUS, DoD 3 NON MESURÉ au moment du jugement → mesuré ci-dessus. Fermeture PARTIELLE en l'état → les quatre griefs corrigés dans `966e1a451`, DoD 3 prouvé sur le chemin réel.
