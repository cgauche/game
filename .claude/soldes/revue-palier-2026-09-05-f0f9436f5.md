# Revue adversariale de PALIER — 35 commits de substance, 5 fermeture(s), 3 sessions, 2026-09-05

verdict: PARTIEL — le dispositif a réellement soldé cinq trouvailles du palier n°4 (le palier se MESURE et ne se compte plus, le log de dérogation DIT qu'il enregistre des tentatives, `croissanceDesStocks` est fail-loud, l'archive de revue est nommée par date+base, les fermetures passent par la PUBLICATION via `github-actions[bot]`), la CI est verte sur les 25 runs de la fenêtre sans une seule dérogation, et le chantier #1686 tient ses bancs, son typecheck FULL et l'idempotence de `gen` — mais la porte qui garde les FERMETURES est aveugle sur le chemin réel : trois des cinq soldes de la fenêtre (#1507, #1624, #1686) sont REFUSÉS par `validateSolde` sur l'arbre committé, #1686 routait CINQ tickets neufs pour un plafond de UN (décision prise à la lecture de cette revue : #1686 RESTE OUVERT, son solde est retiré du commit), la porte de stock neuve produit un FAUX +1 mesuré (cardinal 12 → 12) qui inscrit un delta inexistant au registre des cliquets, et 79 % des commits de substance n'ont toujours aucun check-run.

Fenêtre : f0f9436f5..714df53da

**Arbre ÉPINGLÉ** : worktree `.wt-1624`, `git rev-parse HEAD` = `e849d1e9a7d4efecccadbf92a3f2b806d45e4c6d`, `git status --porcelain` = **2 fichiers modifiés, tous deux posés par l'orchestrateur pour le commit à venir** : `.claude/soldes/1686.md` (4 restes routants convertis en `inventaire #1463`) et `scripts/hooks/stocks-nominatifs.test.mjs` (mention `#1686` retirée d'un commentaire, exigée par le garde de dette). Contrôles POSITIFS du travail jugé : `src/data/materials.json` existe (16 entrées, discriminant `domain`), `src/data/schemas/grammaire/meta.ts` exporte `libelleDeValeur`/`valeursDuChamp`, `src/ui/compendium/registry.ts` porte la catégorie `materials`, `scripts/guards/lib/plageStock.mjs` exporte `croissancesDeLaPlage`, `scripts/guards/lib/stocksNominatifs.mjs:284` porte le `TypeError` fail-loud. 35 commits de substance (`git log --oneline f0f9436f5 .. e849d1e9a (tête de MESURE : l'arbre non publié de l'orchestrateur ; après rebase, la fenêtre publiée est f0f9436f5..714df53da) -- src scripts | wc -l` = 35), 59 au total.

**⚠ L'ARBRE JUGÉ N'EST PAS SUR `main`.** `git merge-base --is-ancestor e849d1e9a origin/main` → **NON**. `origin/main` (et `gh api repos/cgauche/game/commits/main`) = `714df53dac7bd5788843bdb5c1a6f64a558878d0` (« Lot A C2 », poussé 2026-09-05T18:52Z, CI verte 18:58Z run 33985674090), soit **un commit APRÈS `226a764b6`** sur lequel les deux commits de tête ont été rebasés. Le brief de cette revue posait « main distant = 226a764b6 » : c'est FAUX au moment de la mesure. Les deux commits de tête (`7b476acd3`, `e849d1e9a`) sont NON PUBLIÉS et ne sont pas fast-forward — un rebase sur `714df53da` est requis avant tout push, et les docs dérivés devront être régénérés une fois de plus.

## Ce qui a TENU sous attaque (réfutations tentées, échouées — à ne pas rouvrir)

**A. Trouvaille 8 du palier n°4 (compteur faux) : SOLDÉE À LA CAUSE.** `.git/wfrp-palier.compteur` vaut encore 38, mais **plus aucun script ne le lit** (`grep -rn 'wfrp-palier.compteur' scripts/ .claude/settings.json` → 0 occurrence). Le palier se MESURE désormais : le garde rend « Palier atteint : **35** commits de substance depuis `f0f9436f5` », chiffre identique au mien à l'unité près. Le fichier est un résidu mort, pas une source.

**B. Trouvaille 9 (dérogation ambiguë) : SOLDÉE.** Chaque ligne porte maintenant `"etat":"tentative"` (`scripts/git-hooks/pre-push.mjs:383`) et le commentaire `pre-push.mjs:41` le dit en clair (« le log de dérogations compte donc des tentatives, et chaque ligne le DIT »). Posé par `c07a0e24d` puis `5475b9565`, dans cette fenêtre.

**C. Trouvaille 5 (signature positionnelle silencieuse) : SOLDÉE, et dans les termes exacts réclamés.** `scripts/guards/lib/stocksNominatifs.mjs:284-289` lève un `TypeError` si `diffU0` n'est pas une chaîne, et le JSDoc (`:277-281`) cite nommément la revue n°4 et son faux zéro. La classe entière de faux zéros est morte.

**D. Trouvailles 2 (θ/μ) : SOLDÉES et VÉRIFIÉES sur le chemin réel.** L'archive de revue est nommée par date+base (`revue-palier-2026-09-04-2c11fdd9a.md`, stable au rebase). Et la fermeture suit la PUBLICATION : les **6** fermetures depuis 2026-09-04 (`gh api search/issues … closed:>=2026-09-04`) sont TOUTES posées par `github-actions[bot]` avec `commit_id` nul, après un `build` vert sur `main` — plus aucun sha orphelin ne ferme un ticket.

**E. Aucune dérogation, aucun push sur rouge.** `.git/wfrp-justificatifs/derogations.log` = 6 entrées, la plus récente `2026-09-04T12:02:57Z` (`f3d23dfed`), donc **0 dans la fenêtre**. Les 25 runs `CI` de `main` de la fenêtre sont TOUS `success` — le premier rouge en remontant est `0acda4517`, hors fenêtre. Le durcissement réclamé par la trouvaille 1 (η) existe désormais dans le dispositif, pas seulement dans les messages : `verdictCi` refuse `sans-ancetre` (`pre-push.mjs:34,110,139,186-193`) — aucun commit de l'histoire de HEAD porté par une course VERTE ⇒ refus.

**F. Le chantier #1686 tient ses mesures d'ingénierie.** `npm run typecheck` FULL sur `e849d1e9a` : **exit 0**, VERT. `npm run gen` joué DEUX fois : `IDS_PAR_DATASET ← 4099 ids / 80 datasets [inchangé]`, `git status --porcelain` **identique avant et après les deux passes** (idempotence prouvée, pas alléguée). Quatre bancs nommés par les lots (`materials-identite`, `codex-edit-charge-discriminee`, `valeurs-de-champ`, `registry-enveloppe`) : **11 suites / 25 tests, `success: true`, 0 échec**.

**G. Le lot #1679 L3 (peer) tient ses trois claims, vérifiés au code.** (i) « le pre-push lit les courses UNE fois » : `coursesCiDeMain` n'est appelé qu'en `pre-push.mjs:339`, **hors** de la boucle des refs (qui se ferme en 319) ; le second site (`:332`) est la relecture À LA DEMANDE. (ii) « un justificatif par (gate, clé, propreté) » : `npm run gates -- --liste` nomme `typecheck.9c3c2e8565d4….sale.json` et rend un plan cohérent — 22 gates, 20 justifiées sur le contenu `9c3c2e8565d4`, 2 à produire (`test:hooks`, `test`). (iii) « la porte de stock lit une IMAGE » : elle lit bien deux images — mais elle les lit MAL (trouvaille 4).

**H. Poison : rien de neuf dans `src`/`scripts`, à une exception.** Scan des lignes de commentaire AJOUTÉES sur les 35 commits (`git diff f0f9436f5 e849d1e9a -- src scripts | grep '^+'`, filtré familles b/c) → 9 correspondances, **8 faux positifs relus un par un** (usages techniques de « temporaire » : fichiers de fixture sous `os.tmpdir()`, écriture atomique ; « plus tard » d'une promesse ; `-F` champ de formulaire ; une excuse RÉGULIÈRE, tagguée `[entériné 2026-09-01]` avec verbatim utilisateur). La 9ᵉ est une vraie tombale — trouvaille 6.

## Trouvailles

**1. BLOQUANT — trois des cinq soldes de la fenêtre sont REFUSÉS par le garde qui est censé les avoir laissés passer.** Sonde : `validateSolde(<contenu à e849d1e9a>, <jour>, { issuesFermees: [N] })`, la fonction MÊME que le driver appelle (`solde-ticket-guard.mjs:1155`).

| solde | `ok` | problèmes |
|---|---|---|
| `#1493` | **true** | 0 |
| `#1540` | **true** | 0 |
| `#1507` | **false** | 2 dispositions invalides (`-> ÉCARTÉE par l'utilisateur…`, `-> RAS, mesuré au commit 30dd38716` — la virgule au lieu du deux-points tue la forme `RAS : <justification>`) |
| `#1624` | **false** | 5 dispositions invalides (`-> non jouable :…`, `-> hors de ce ticket…`, `-> écartés dès le ticket…`, **`-> #1686.`** — le POINT final casse `DISPOSITION_RE`, `-> à ajouter à docs/…`) |
| `#1686` | **false** | **6** : « 5 restes ROUTÉS vers un ticket neuf (plafond 1) » + 5 dispositions invalides |

Trois fermetures ont donc été committées avec une preuve que le garde en vigueur refuse. Cause structurelle mesurée : le garde est un hook PreToolUse de session (`.claude/settings.json:72-76`, matcher `Bash|PowerShell|mcp__lean-ctx__ctx_shell`), et non un hook git — il ne juge que les commits passés par l'outil d'une session où il est câblé. `git config core.hooksPath` = `scripts/git-hooks`, et **aucun des hooks git n'appelle `solde-ticket-guard`** (`grep -rn 'solde-ticket-guard' scripts/git-hooks/` → 0). **Attendu** : la porte de solde descend dans `scripts/git-hooks/pre-commit` (ou au pre-push sur la plage poussée), sans quoi « fermeture = solde conforme » reste une convention de session, pas un verrou par construction. Les soldes #1507 et #1624 se corrigent en un geste (grammaire des dispositions) — fait dans le commit qui porte cette revue.

**2. BLOQUANT — la correction en cours de `#1686` ne passait TOUJOURS pas, et le geste employé CONTOURNAIT le plafond au lieu de le respecter.** Deux faits mesurés :
- (a) **La version de l'arbre échouait encore** : `validateSolde` sur `.claude/soldes/1686.md` tel qu'il était → `ok=false`, 1 problème : *« item sans disposition valide (ligne 1 du bloc) : "- Ligne de DoD … -> #1690 (ticket frère, arbitrage user …) — la ligne reste OUVERTE là-bas, nommément." »*. `DISPOSITION_RE` n'admet AUCUNE prose après `#N`.
- (b) **Le geste était une évasion, pas une réduction.** Le diff convertissait quatre `-> #1691/#1692/#1693/#1694` en `-> inventaire #1463 : ticket #169X ouvert le 2026-09-05`. `restesRoutants` tombait de **5 à 1** — mais `ROUTANT_RE = /->\s*#\d+/` (`:676`) ne regarde que le jeton qui suit `->`, et la disposition `inventaire` est documentée à la ligne d'à côté comme *« écart PORTÉ à un programme (**aucun ticket neuf émis**) »* (`:672`). Contrôle GitHub : `#1690 #1691 #1692 #1693 #1694` existent, `state=open`, créés le 2026-09-05, et **aucun ne cite #1463 dans son corps**. Le fan-out réel de cette fermeture était **5**, contre la doctrine verbatim *« fan-out ≤ 1 par commit »* (`.claude/memory/user-regime-une-session-par-chantier-2026-09-01.md:3`, décision utilisateur 2026-09-01). **Décision prise à cette revue** : #1686 RESTE OUVERT (le commit du lot 3a-2 est `refs`, le solde est retiré de l'arbre et s'écrira à la fermeture, quand #1690 aura posé la ligne de DoD terrain) ; les quatre tickets de trouvailles restent des tickets de leur classe. **Attendu au dispositif** : `ROUTANT_RE` doit compter tout `#N` NEUF d'un item, quelle que soit la disposition qui le précède, sinon le plafond ne mesure plus rien.

**3. BLOQUANT — `test:hooks` est ROUGE sur `e849d1e9a`, et le geste annoncé (« deux lignes CLIQUET au message ») ne le rendra vert que s'il AMENDE `7b476acd3`.** `npm run test:hooks` (718 tests) : `not ok 672 - CLIQUET stocks : la PLAGE POUSSÉE ne fait grossir aucun stock en silence`, avec l'attendu littéral `[]` contre l'obtenu `['7b476acd3 scripts/migrations/lib/1686-structure-material-portes.test.mjs +1', '7b476acd3 src/data/field-consumers.test.ts +1']`. Le refus est **attribué à `7b476acd3`** : un CLIQUET posé au message d'un commit SUIVANT laisse la porte rouge. Et ce sont bien les **seuls** deux refus de la plage réellement poussable : `croissancesDeLaPlage({avant:'714df53da', apres:'e849d1e9a'})` → **2 refus, tous deux sur `7b476acd3`**. (Sur la fenêtre entière, la porte en trouve 8 — voir trouvaille 5.) Fait : les deux commits de tête sont refondus en un seul dont le message porte les CLIQUET.

**4. BLOQUANT — la porte de stock neuve (`a527272d3`, posée il y a six heures) produit un FAUX +1 : elle exige un CLIQUET mensonger sur un stock dont le cardinal N'A PAS BOUGÉ.** `src/data/field-consumers.test.ts` dans `7b476acd3` : diff parfaitement symétrique (3 lignes retirées, 3 ajoutées, mêmes numéros 231/233/234), et **cardinal du stock mesuré à 12 AVANT comme APRÈS**. La porte rend pourtant `{ ajoutees: 3, retirees: 2, net: +1 }`. Cause isolée par réduction minimale : le lecteur de PRÉ-image ne reconnaît pas l'entrée
`['Amputation', 'timing', 'src/engine/critical.ts:327, src/ui/compendium/registry.ts:719'],`
alors qu'il reconnaît sa version simplifiée `['Amputation', 'timing', 'src/engine/critical.ts:327'],` sur le POST-image — la virgule INTERNE au troisième littéral suffit. Conséquence de doctrine : **toute simplification d'une entrée de stock est comptée comme une croissance**, et le seul moyen de passer la porte est d'écrire au registre des cliquets un `+1` FAUX. **Attendu** : corriger `entreesDeStock`/`lignesDEntrees` pour que la reconnaissance soit SYMÉTRIQUE entre les deux images, et promouvoir la sonde de réduction ci-dessous en test. Le CLIQUET `field-consumers.test.ts +1` du commit porteur dit qu'il paie un faux positif nommé, jamais une croissance.

**5. Six croissances de stock NON DÉCLARÉES dans la fenêtre échappent définitivement à la porte, parce qu'elle a gagné la vue APRÈS leur push.** `croissancesDeLaPlage({avant:'f0f9436f5', apres:'e849d1e9a'})` → **8 refus** :

| sha | fichier | net | déclaré |
|---|---|---|---|
| `c07a0e24d` | `scripts/gates/ecrivainsAtteints.test.mjs` | 1 | **2** (écart) |
| `c07a0e24d` | `scripts/hooks/solde-ticket-guard.test.mjs` | 1 | `null` |
| `2f5115980` | `scripts/guards/lib/structuresStock.mjs` | 2 | `null` |
| `ac9da118b` | `scripts/ops/workflows-joues.test.mjs` | 3 | `null` |
| `946ccfe84` | `scripts/migrations/lib/1686-ardoise-portes.test.mjs` | 2 | `null` (fichier CRÉÉ par le commit) |
| `39989b6d3` | `scripts/migrations/lib/1686-materials-portes.test.mjs` | 4 | `null` (fichier CRÉÉ par le commit) |
| `7b476acd3` | `1686-structure-material-portes.test.mjs` | 1 | `null` (fichier CRÉÉ) |
| `7b476acd3` | `src/data/field-consumers.test.ts` | 1 | `null` (**faux positif**, trouvaille 4) |

Les quatre premiers sont PUBLIÉS et ne seront jamais déclarés (leur push a précédé `a527272d3`, 2026-09-05 14:05). Les trois lignes « fichier CRÉÉ » sont une **seconde classe de faux positifs** : un stock qui naît à N n'a pas « grandi » de N, il n'existait pas. **Attendu** : la porte distingue la CRÉATION d'un porteur de stock de sa CROISSANCE (sinon tout banc de portes neuf naîtra rouge) ; et le cas `c07a0e24d` (`declare: 2` pour un `net: 1`) montre qu'un CLIQUET peut sur-déclarer sans que rien ne le dise.

**6. Une pierre tombale ÉCHAPPE au motif que le lot 3a-1 vient d'ajouter — et elle a été introduite DANS la fenêtre.** `src/ui/CascadeTableMode.test.tsx:147` (posé par `f39356d5b`, `git log -S`) : `// La raison ne vit plus dans un \`title\` natif (invisible à l'arbre a11y, inatteignable au doigt) :` — rappel d'ancien état, famille (c), tolérance ZÉRO. Le garde passe : sonde `tombstonesIn` → **`[]`** sur cette ligne, contre `["n'est / ne vit plus ici (site quitté)"]` sur le témoin positif de la fixture. `NO_MORE_HERE_RX` (`commentPoison.mjs:296-299`) exige le mot littéral **`ici`** ; « ne vit plus **dans un `title` natif** » sort par le côté, alors même que la ligne nomme un artefact de code entre back-ticks. **Attendu** : le motif couvre `ne vit / n'est plus <dans|sur|à> <artefact>` (le discriminant back-tick existe déjà dans les familles voisines) ; le commentaire est reformulé au présent dans le commit porteur de cette revue.

**7. `test:hooks` porte un test FLAKY, et c'est EXACTEMENT celui qui a coûté deux hotfixes au palier n°4.** `scripts/gates/toutes.test.mjs:110` — « un enfant qui dépasse son plafond est EXPIRÉ, et son ARBRE tombe avec lui ». Deux exécutions du même arbre : passe n°1 → `# fail 1` ; passe n°2 → `# fail 2`, avec `not ok 26 … ENOENT: no such file or directory, open '…\gates-timeout-yOmIbx\temoin.txt'` (`toutes.test.mjs:133`). Le test suppose que le petit-fils (intervalle 200 ms) a écrit AU MOINS une fois avant le plafond de 1500 ms ; sous charge la prémisse tombe et `readFileSync` explose. **Circonstance à dire honnêtement** : mes propres mesures parallèles (typecheck FULL + vitest + `gen`) chargeaient la machine — ce qui est précisément le régime des lanes de `npm run gates`. **Attendu** : la prémisse temporelle s'ATTEND (`attendreQue(existsSync(temoin))` avant le plafond, patron déjà nommé au #1442), jamais ne se suppose. Un gate dont le verdict dépend de la charge n'est pas un gate.

**8. 27 des 34 commits de substance publiables (79 %) n'ont AUCUN check-run — 4ᵉ palier consécutif et pire qu'au précédent (76 %).** `gh api repos/cgauche/game/commits/<sha>/check-runs --jq .total_count` sur les 35 (`7b476acd3` non publié : `422 No commit found`) : **7 à 3** (`226a764b6`, `56af53b19`, `946ccfe84`, `af8873c42`, `81b624128`, `b05b16170`, `d608418ad`), **27 à 0**. Parmi les non jugés : `c07a0e24d` (breaking, ferme #1493, refonte du canari et des fermetures), `e1f606503` (train de la porte des jets #1508), `5dd0eedce` et `39989b6d3` (les deux plus gros trains de #1686), et les cinq commits du train L3. **Attendu** : inchangé depuis trois paliers — soit le push par lot cesse pour les trains cassants, soit le régime cesse d'affirmer que chaque commit passe les gates.

**9. Deux commits de substance de la fenêtre n'ont ni type conventionnel, ni `refs #`, ni fermeture — et datent du 18 août.** `8954ad355` « Spike de faisabilité : découpe adressée du Source » (2026-08-18T13:10, +415 lignes sur `scripts/source/`) et `25b02d5f0` « Spike v2 … : correctifs du juge + adresses de cellule + empreintes » (2026-08-18T16:20, +371 lignes, touche `package.json`). Ce sont les **seuls** commits de la fenêtre sans `refs #<n>` (sonde sur les 59 messages). Greffés par rebase d'un vieux worktree, ils sont entrés sur `main` en portant du code de production sans rattachement de traçabilité. **Attendu** : rien à défaire (le code est repris par le train C1 `a7ae5d209` de #1389/#1388) ; à retenir qu'aucune porte ne refuse un commit sans référence de ticket, et qu'un rebase de spike ancien fait entrer du code hors régime.

**10. Le détecteur de fermetures sans solde est TOUJOURS aveugle aux fermetures hors commit — 5ᵉ palier — et son stock n'a pas décru d'une unité en 35 commits.** `grep -c 'gh api\|search/issues' scripts/hooks/fermetures-sans-solde.test.mjs` = **0**. Cardinal du stock nominatif : **118 à `f0f9436f5`, 118 à `e849d1e9a`**, alors qu'il « ne peut que DÉCROÎTRE ». Atténuations MESURÉES et réelles : (a) les soldes #1659 et #1673 réclamés au palier n°4 SONT écrits et suivis par git (`.claude/soldes/1659.md`, `1673.md`, portés par `c07a0e24d`, en-tête « Solde A POSTERIORI ») ; (b) les 6 fermetures de la fenêtre passent toutes par la publication. Reste un trou nommé : **`#1614` a été fermé le 2026-09-04T19:40Z sans solde propre** — doublon de canari absorbé par `#1493` (le commentaire de fermeture le dit), mais aucune disposition de solde ne le porte et le stock ne l'enregistre pas.

**11. L'arbre PRINCIPAL décroche encore : 51 commits de retard (38 au palier précédent).** `C:\Users\gauch\PhpstormProjects\Foundry\Game` est sur `599979762` ; `git rev-list --count 599979762..714df53da` = **51**. Progrès à créditer, et il est net : les débris signalés au palier n°4 ont été PURGÉS (`nul`, `.tmp-tsc-truth.mjs`, `TODO-vague-1388.md`, `TODO-vague-1624.md` : absents), et les 15 docs dérivés périmés ne sont plus modifiés. Ce qui reste est de la mémoire vivante (15 fiches `.claude/memory/` modifiées, 11 non suivies) — pas des débris. **Attendu** : `git pull --ff-only` avant le prochain commit depuis cet arbre. `git worktree list | wc -l` = **20**, inchangé.

## Sorties brutes

| # | commande | exit | résultat retenu |
|---|---|---|---|
| 1 | `git rev-parse HEAD` ; `git status --porcelain` | 0 | `e849d1e9a7d4…` ; `M .claude/soldes/1686.md`, `M scripts/hooks/stocks-nominatifs.test.mjs` |
| 2 | `git rev-list --count f0f9436f5 .. e849d1e9a (tête de MESURE : l'arbre non publié de l'orchestrateur ; après rebase, la fenêtre publiée est f0f9436f5..714df53da)` ; `… -- src scripts \| wc -l` | 0 | **59** total ; **35** de substance |
| 3 | `git merge-base --is-ancestor e849d1e9a origin/main` | 1 | **PAS ANCÊTRE** — `origin/main` = `714df53da` |
| 4 | `gh api repos/cgauche/game/commits/main --jq .sha` | 0 | `714df53dac7bd5788843bdb5c1a6f64a558878d0` (Lot A C2, 18:52Z) |
| 5 | `node fenetre.mjs` (fermetures + refs + CLIQUET) | 0 | 5 fermetures : `#1686` `#1540` `#1624` `#1507` `#1493` ; **2 commits sans `refs #`** ; 46 lignes `CLIQUET:` |
| 6 | `git diff --name-status f0f9436f5 e849d1e9a -- .claude/soldes` | 0 | `A` 1493, 1507, 1540, 1624, **1659**, **1673**, 1686, revue-palier-2026-09-04 |
| 7 | `node valide.mjs` (`validateSolde` sur les 5) | 0 | `#1493 ok` · `#1540 ok` · **`#1507 ✗2`** · **`#1624 ✗5`** · **`#1686 ✗6`** ; arbre `#1686 ✗1` |
| 8 | `node routants.mjs` (`restesRoutants`) | 0 | HEAD `#1686` = **5** routants (plafond 1) ; arbre = 1, mais **4 tickets NEUFS** `#1691 #1692 #1693 #1694` déclarés ouverts sous `inventaire` |
| 9 | `gh api …/issues/{1690..1694}` | 0 | 5 × `state=open`, 5 × `body` ne cite PAS `#1463` |
| 10 | `gh api search/issues closed:>=2026-09-04` / `created:>=2026-09-04` | 0 | **6 fermés** (tous par `github-actions[bot]`) / **9 créés**, tous ouverts |
| 11 | `gh api …/actions/runs?branch=main` | 0 | 25 runs dans la fenêtre, **25 success, 0 failure** ; `714df53da` = success |
| 12 | `gh api …/commits/<sha>/check-runs` ×34 | 0 | **27 à 0**, 7 à 3 (`7b476acd3` = 422, non publié) |
| 13 | `cat .git/wfrp-justificatifs/derogations.log` | 0 | 6 entrées, **0 dans la fenêtre**, chacune `"etat":"tentative"` |
| 14 | `npm run typecheck` (FULL) | **0** | `[gate] typecheck VERTE sur e849d1e` |
| 15 | `npm run gen` ×2 puis `git status --porcelain` | 0 | `4099 ids / 80 datasets [inchangé]` ; **status identique aux deux passes** |
| 16 | `npx vitest run` 4 bancs #1686 | 0 | `numTotalTests: 25`, `numFailedTests: 0`, `success: true` |
| 17 | `npm run test:hooks` (×2) | — | passe 1 : `# pass 717 / # fail 1` ; passe 2 : `# pass 716 / # fail 2` (`not ok 26` **flaky**, `not ok 672` **déterministe**) |
| 18 | `node plage.mjs` (`croissancesDeLaPlage f0f9436f5 .. e849d1e9a (tête de MESURE : l'arbre non publié de l'orchestrateur ; après rebase, la fenêtre publiée est f0f9436f5..714df53da)`) | 0 | **8 refus** / 59 commits ; sur `714df53da..e849d1e9a` : **2 refus**, tous `7b476acd3` |
| 19 | `node fauxpositif.mjs` | 0 | `field-consumers.test.ts` : `{ajoutees:3, retirees:2, net:+1}` — **cardinal réel 12 → 12** |
| 20 | `node asymetrie.mjs` (réduction minimale) | **1** | `retirees:0, net:+1` sur une entrée SIMPLIFIÉE — asymétrie pré/post image ISOLÉE |
| 21 | `node tombale.mjs` (`tombstonesIn`) | 0 | `[]` sur `CascadeTableMode.test.tsx:147` ; témoin positif → `["n'est / ne vit plus ici"]` |
| 22 | `grep -c 'gh api\|search/issues' …/fermetures-sans-solde.test.mjs` ; cardinal STOCK | 0 | **0** ; **118 → 118** (inchangé sur 35 commits) |
| 23 | `grep -rn 'wfrp-palier.compteur' scripts/ .claude/settings.json` | 1 | **0 occurrence** — le compteur (38) n'est plus lu, le palier se MESURE (35) |
| 24 | `grep -rn 'solde-ticket-guard' scripts/git-hooks/` | 1 | **0** — la porte de solde n'est PAS un hook git |
| 25 | `npm run gates -- --liste` | 0 | 22 gates, 20 justifiées sur `9c3c2e8565d4`, 2 à produire (`test:hooks`, `test`) |
| 26 | `git -C <arbre principal> rev-list --count 599979762..714df53da` ; `status` | 0 | **51** de retard ; 15 M + 11 `??`, **tous `.claude/memory/`** ; débris du palier n°4 PURGÉS |
| 27 | poison : `git diff f0f9436f5 e849d1e9a -- src scripts \| grep '^+' \| grep -iE '<familles b/c>'` | 0 | 9 lignes, **8 faux positifs**, 1 vraie tombale (trouvaille 6) |

### Sondes à promouvoir en test committé

**`asymetrie.mjs`** — la porte de stock ne compte pas une SIMPLIFICATION comme une croissance (trouvaille 4 ; ÉCHOUE aujourd'hui, `exit 1`) :

```js
const { croissanceDesStocks } = await import(pathToFileURL('scripts/guards/lib/stocksNominatifs.mjs').href)
const AVANT = "const RECOUVRES = [\n  ['Amputation', 'timing', 'src/engine/critical.ts:327, src/ui/compendium/registry.ts:719'],\n];\n"
const APRES = "const RECOUVRES = [\n  ['Amputation', 'timing', 'src/engine/critical.ts:327'],\n];\n"
const diff = ['--- a/src/data/field-consumers.test.ts', '+++ b/src/data/field-consumers.test.ts', '@@ -2 +2 @@',
  "-  ['Amputation', 'timing', 'src/engine/critical.ts:327, src/ui/compendium/registry.ts:719'],",
  "+  ['Amputation', 'timing', 'src/engine/critical.ts:327'],"].join('\n')
const r = croissanceDesStocks(diff, { lirePostImage: () => APRES, lirePreImage: () => AVANT })
assert.deepEqual(r, [], 'une entrée SIMPLIFIÉE (1 retirée / 1 ajoutée, cardinal inchangé) ne fait pas grossir le stock')
// obtenu : [{ ajoutees: 1, retirees: 0, net: 1 }] — le PRÉ-image ne reconnaît pas l'entrée à virgule interne
```

**`valide.mjs`** — tout solde COMMITTÉ passe le garde qui prétend le garder (trouvaille 1 ; ÉCHOUE sur #1507, #1624, #1686) :

```js
const { validateSolde } = await import(pathToFileURL('scripts/hooks/solde-ticket-guard.mjs').href)
for (const [n, jour] of FERMETURES_DE_LA_PLAGE) {            // (sha du solde, date de son en-tête)
  const c = execFileSync('git', ['show', `${SHA}:.claude/soldes/${n}.md`], { encoding: 'utf8' })
  const r = validateSolde(c, jour, { issuesFermees: [Number(n)] })
  assert.ok(r.ok, `#${n} : solde committé REFUSÉ par le garde — ${r.problems.join(' ; ')}`)
}
```

**`routants.mjs`** — le plafond compte les tickets NEUFS, pas les jetons après `->` (trouvaille 2 ; ÉCHOUE sur `#1686`) :

```js
const { restesRoutants } = await import(pathToFileURL('scripts/hooks/solde-ticket-guard.mjs').href)
const c = readFileSync('.claude/soldes/1686.md', 'utf8')
const neufs = new Set((c.match(/ticket #(\d+) ouvert/gi) || []).map(s => s.match(/#\d+/)[0]))
assert.ok(restesRoutants(c).length + neufs.size <= 1,
  `fan-out réel ${restesRoutants(c).length + neufs.size} > 1 — un "inventaire #<épic>" n'émet AUCUN ticket neuf (solde-ticket-guard.mjs:672)`)
// obtenu : 1 routant + 4 tickets neufs (#1691 #1692 #1693 #1694) = 5
```

**`tombale.mjs`** — la famille (c) couvre « ne vit plus <dans|sur|à> <artefact> », pas seulement « ici » (trouvaille 6) :

```js
const { tombstonesIn } = await import(pathToFileURL('scripts/guards/lib/commentPoison.mjs').href)
assert.ok(tombstonesIn("// Le FICHIER, lui, ne vit plus ici : il est DÉRIVÉ du def porteur.").length, 'témoin POSITIF')
assert.ok(tombstonesIn("// La raison ne vit plus dans un `title` natif (invisible à l'arbre a11y)").length,
  'src/ui/CascadeTableMode.test.tsx:147 — tombale à artefact back-tiqué, échappe à NO_MORE_HERE_RX')
```

Fichiers chargés : `scripts/hooks/solde-ticket-guard.mjs` (`:660-698`, `:761-763`, `:840-880`, `:935-947`, `:973-1003`, `:1122-1175`), `scripts/guards/lib/stocksNominatifs.mjs` (`:265-345`), `scripts/guards/lib/plageStock.mjs`, `scripts/guards/lib/commentPoison.mjs` (`:296-299`, `:411`), `scripts/git-hooks/pre-push.mjs` (`:34`, `:41`, `:110`, `:139`, `:186-193`, `:314-343`, `:383`), `scripts/gates/toutes.test.mjs:110-140`, `scripts/hooks/fermetures-sans-solde.test.mjs`, `scripts/hooks/stocks-nominatifs.test.mjs:461-475`, `.claude/settings.json:61-116`, `src/ui/CascadeTableMode.test.tsx:143-151`, `.claude/soldes/{1493,1507,1540,1624,1659,1673,1686}.md`, `.claude/memory/user-regime-une-session-par-chantier-2026-09-01.md`.
