# Solde #1800 — 2026-09-18

VERIFIE: le contrat du ticket (le cliquet (xii) gelait le NOMBRE de classes par module de domaine — 24 hausses / 17 baisses en 60 jours — sans voir l'identité posée en module d'écran ni le `style=` inline) est corrigé à l'arbitrage utilisateur du 2026-09-18 (A1 « un module CSS d'écran ne déclare que du PLACEMENT ; toute IDENTITÉ vit dans une primitive ; le nombre de classes n'est plus compté » ; A2 « seule forme légale de `style=` : un objet dont toutes les clés sont des variables CSS consommées par une classe ») : (xii), `CLASS_SELECTOR_BASELINE` et `DOMAIN_CSS_MODULES` sont supprimés ; la couche LAYOUT existe (`src/ui/Layout.tsx` : `Stack`/`Row`/`Grid`/`Split`, `src/ui/styles/layout.css` en liste fermée, tokens `--sp-*`) ; les classes `.row-flex`/`.panel-grid`/`.span-2`/`.layout-sidebar`/`.master-detail` sont mortes et leurs 50 consommateurs composent les primitives ; les gardes (xxi) (identité et espacement hors échelle en module d'écran) et (xxii) (`style=` hors variable CSS) tiennent des stocks NOMINATIFS amorcés à 2339 / 910 / 100 sites et décroissants-seulement (`scripts/guards/lib/cssCouchesStock.mjs`, régénérateur `scripts/ui/regen-css-couches-stock.mts`), prouvées par mutation ; l'écran de référence `TestScenariosScreen` se rend à zéro CSS écrit, zéro inline ; diff relu par l'orchestrateur, jugé deux fois, recetté au navigateur.

## Restes
- Sticky de `Split` collé sur le PREMIER enfant quel que soit `side`, et `align-items: start` implicite qui effondrait le canevas de la carte du monde (juge, M1/M3) -> corrigé dans ce commit (src/ui/styles/layout.css:105) : colonne bornée désignée par sa position selon `side`, prop `align` de `Split`.
- `WorldMapView.test.tsx` réécrit pour matcher une règle de `layout.css` étrangère à l'écran (juge, M2) -> corrigé dans ce commit (src/ui/WorldMapView.test.tsx:93) : quatre `it` mesurés à la règle de l'écran (`world-meta.css`) et au contrat de `Split`.
- Slot détail de `MasterDetail` passé en `Stack gap="md"` sur ses 11 consommateurs (juge, M4) -> corrigé dans ce commit (src/ui/MasterDetail.tsx:119) : conteneur neutre.
- Lexer `reglesCss` avalant la règle qui suit une at-rule DÉCLARATION (`@import`, `@charset`) — `.btn:focus-visible` de `styles.css` invisible à (xx) (juge, M5) -> corrigé dans ce commit (scripts/guards/lib/cssCouches.mjs:74) : preuve à `ui-ratchets.test.ts` « (xx) preuve — une règle posée APRÈS une at-rule DÉCLARATION reste vue ».
- `build-primitives.mjs` recopiait la liste des feuilles partagées sans `layout.css` (juge, mineur 1) -> corrigé dans ce commit (scripts/docs/build-primitives.mjs:21) : `FEUILLES_PARTAGEES` importée.
- `className="btn small row"` manuscrit (juge, mineur 2) -> corrigé dans ce commit (src/ui/creator/CharacterCreator.tsx:341) : `<Row as="button">`, `button` admis dans `Balise`.
- `sticky` posé sans mandat sur `PortView`/`ShipDossier` (juge, mineur 3) -> corrigé dans ce commit (src/ui/PortView.tsx:66).
- Recette 1440 : `.worldmap-layout` redéclarait `grid-template-columns` sous ≥901 px, battue par `.split[data-aside='md']` — colonne bloquée à 270 px au lieu de `min(400px, 40vw)` (recetteur) -> corrigé dans ce commit (src/ui/styles/world-meta.css:135) : l'écran pose `--aside`, la variable que `Split` consomme, sans `aside` prop ; test `WorldMapView.test.tsx` réécrit sur cette règle.
- Galerie : sélection du spécimen keyée par son `label` (préexistant, révélé par le garde `labelLogic` au commit) -> corrigé dans ce commit (src/ui/gallery/registry.tsx:998) : `id` stable DÉCLARÉ sur chacun des 50 spécimens (unicité gardée au chargement), `DesignGallery` compare l'`id`.
- `docs/charte-ui.md` citait `{...grow}`/`{...pushEnd}`/`{...spanFull}` dans des backticks, lus comme des classes mortes par `check-doc-refs` (pré-commit) -> corrigé dans ce commit (docs/charte-ui.md:198).
- Écarts au plan assumés (code réel) : `forwardRef` sur les quatre primitives (`MasterDetail` mesure le rail), `Balise` étendue de `span`/`label`/`button`, repli `--aside` codé dans `.split`, `style` toujours rendu par `NotchGauge`/`LifeBar` (`initial` = repli du `var()`), `cssCouches.test.mjs` en `node:test` joué par `test:hooks` (`vite.config.ts` n'inclut pas `scripts/guards/**`), entrée `layout` sans champ `css` (feuille PARTAGÉE, (xiv) réécrite) -> RAS : chacun vérifié par le juge sur le code, aucun n'affaiblit une garde.
- Migration de l'IDENTITÉ des 24 modules d'écran vers leurs primitives (stock 2339) et recalage des espacements (stock 910), par famille -> inventaire #1811 : épic ouvert le 2026-09-18, cinq lots #1806 (Combat, 935 décl.), #1807 (Monde, 455), #1808 (Atelier, 351), #1809 (Fiche, 323), #1810 (Créateur, 331) ouverts, stocks 2339 / 910 / 100 à décroître jusqu'à 0.
- `src/gameIso/**/*.tsx` : 21 sites `style=` hors périmètre de (xxii) -> #1805
- Espacements hors échelle de `gallery.css`/`party.css` laissés au stock (codeur) -> RAS : le stock nominatif est le mécanisme prévu, décroissance aux lots #1808/#1809.
- `candidate-master-detail` (`PartyScreen.tsx`, `party.css`) : nom voisin d'une classe morte, préexistant, classe d'écran distincte (juge) -> RAS : rien de mort n'y survit, l'identité de `party.css` va au stock (#1809).
- Recette : aucun scénario de test n'a de lieu à `poi`, l'onglet Plan du hub n'est exerçable que via une campagne ; deux pièges du kit (clic CDP à côté d'un bouton icône-seule, `.click()` qui réussit SOUS une modale) -> RAS : consigné en commentaire daté du 2026-09-18 sur le ticket du kit de recette #1561 (frictions 5-7), hors périmètre de ce lot.
- Recette : `Row` d'actions à ≥2 boutons non observée à 360 sur la fiche testée (héroïne sans option secondaire) -> RAS : `flex-wrap: wrap` de `.row` mesuré, et le bouton `Row as="button"` du créateur recetté à 360.

## Recette visuelle
Recette navigateur en joueur (Chrome headless CDP, clics et clavier réels, console 0 erreur à toutes les largeurs, aucun débordement horizontal), 2026-09-18, sur l'arbre final :
- capture: public/qc/soldes/1800-01-tests-scenarios-360.png — écran de référence à 360 px, une colonne, cartes `.panel.sunken`, texte tronqué `.clamp`.
- capture: public/qc/soldes/1800-01-tests-scenarios-700.png — 700 px.
- capture: public/qc/soldes/1800-01-tests-scenarios-900.png — 900 px.
- capture: public/qc/soldes/1800-01-tests-scenarios-1440.png — 1440 px, `Grid min="lg"` à 3 colonnes.
- capture: public/qc/soldes/1800-01b-tab-sur-lancer.png — `Tab` jusqu'à « Lancer », `Entrée` ouvre le scénario.
- capture: public/qc/soldes/1800-02-cityhub-plan-700.png — hub de ville, onglet Plan, `MasterDetail aside="lg"` empilé.
- capture: public/qc/soldes/1800-02-cityhub-plan-900.png — liste + détail côte à côte.
- capture: public/qc/soldes/1800-03-compendium-masterdetail-1440.png — Compendium, slot détail neutre, liste défilant en interne.
- capture: public/qc/soldes/1800-03-compendium-masterdetail-560.png — empilé.
- capture: public/qc/soldes/1800-04-shipdossier-split-1440.png — dossier du navire, `Split aside="md"` 270 px, aside en flux.
- capture: public/qc/soldes/1800-04-shipdossier-split-700.png — une colonne.
- capture: public/qc/soldes/1800-05-sessionend-grid-1440.png — fin de séance, `Grid min="md"` réagit à son conteneur.
- capture: public/qc/soldes/1800-05-sessionend-grid-560.png — idem à 560.
- capture: public/qc/soldes/1800-06-fiche-row-1440.png — fiche, `Row` d'actions.
- capture: public/qc/soldes/1800-06-fiche-row-360.png — idem à 360.
- capture: public/qc/soldes/1800-07-worldmap-1440.png — carte du monde, `Split side="end" align="stretch"` : canevas pleine hauteur, aside à droite en flux.
- capture: public/qc/soldes/1800-07-worldmap-900.png — une colonne.
- capture: public/qc/soldes/1800-07-worldmap-700.png — une colonne.
- capture: public/qc/soldes/1800-08-creator-bouton-360.png — créateur, `<Row as="button" className="btn small">`.
- capture: public/qc/soldes/1800-08-creator-bouton-1440.png — idem à 1440.

## Réfutation
verdict: PARTIEL — juge de design sur le plan (2026-09-18, lecture seule) : 9 majeurs mécaniques (amorçage du stock impossible, clé `famille` perdue, ligne `CLIQUET:` unique, `.menu-card` à 560 px, consommateurs de `MasterDetail` hors périmètre, `city-hub` cassé au lot 1, `.row-flex` redéfinie dans `creator.css`, plages de `base.css` destructrices, cascade tuant `pad`) → tous corrigés au plan avant exécution ; juge du diff (2026-09-18, lecture seule, sondes `node`, `npx vitest run ui-ratchets`) : 5 majeurs (sticky sur le mauvais enfant + test travesti + canevas effondré sur la carte du monde, slot détail de `MasterDetail` en flex, lexer avalant `@import`) et 6 mineurs → tous corrigés dans ce commit, mutations rejouées (`;` du lexer retiré → `not ok 5` + (xx) rouge ; `side`/`align` retirés → 2 rouges). Résiste : DoD greps à 0, 47/47 `ui-ratchets` sur le corpus réel, comptes 2339 / 910 / 100 = 3349 lignes d'entrées, `cssCouches.test.mjs` joué en CI par `test:hooks`, clés nominatives sans numéro de ligne, détection inline complète (expr, spread, ternaire, `{{}}`, commentaires), catalogue de la charte complet ((xiii) à 0 sur `layout.css`), écran de référence conforme au caractère près.
