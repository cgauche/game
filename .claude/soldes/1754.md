# Solde #1754 « Hook `enterine-guard` : la branche `Write` juge le contenu entier et redemande une validation pour des tags `[entériné]` déjà sur disque » — 2026-09-14

VERIFIE: le DoD relu clause par clause contre le diff du worktree `.wt-1508-t3b` — (1) `scripts/hooks/enterine-guard.mjs` branche `Write` : tags du contenu écrit MOINS tags du fichier `tool_input.file_path` lu sur disque, en MULTIENSEMBLE (une 2ᵉ occurrence ou une date changée compte comme un tag de plus), fichier inexistant ou illisible → `ask` comme avant ; branche `Edit` strictement inchangée (diff nul sur ses lignes) ; (2) banc NEUF `scripts/hooks/enterine-guard.test.mjs`, 7 contrats, fixtures sous `os.tmpdir()` : Write plein-fichier d'un fichier portant déjà son tag → silence ; Write ajoutant un 2ᵉ tag → ask ; Write d'un fichier neuf avec tag → ask ; Edit qui introduit → ask ; Edit dont l'`old_string` portait le tag → silence ; stdin illisible → silence ; message du credo 6b et câblage `Write|Edit` dans `.claude/settings.json` et `.codex/hooks.json` assertés ; (3) `npm run test:hooks` rejoué depuis le worktree après rebase sur `abedb5fd4` : 898/898 (le banc entre dans `test:hooks`, `package.json:25`, et dans le cliquet `scripts/gates/ecrivainsAtteints.test.mjs` +1 avec sa mesure d'écriture — `os.tmpdir()` seulement) ; (4) sonde `sonde-hooks-ask.mjs` (les 5 hooks `Write|Edit` du dépôt rejoués en `Write` plein-fichier sur les 35 fichiers du train #1508 T3b-1) : 4 `ask` avant → 0 après, aucun autre hook ne redemande ; (5) émetteur NOMMÉ avant tout code (fiche `feedback-demande-de-validation-est-un-defaut`, #1728) : le hook du dépôt, pas le classificateur du harnais ni le train #1736 — `.claude/settings.json` et les agents n'avaient pas bougé sur `origin/main` entre `212a8714b` et la sonde ; (6) arbitrage utilisateur du 2026-09-14 (3ᵉ verbatim de `user-doctrine-chantier-outille-sans-reinventer.md`, porté sur le ticket) : « il est important d'offrir des outils qui ne demande pas de ma part une validation de la creation ou la modification d'un fichier » — le fix est la règle « juger le DELTA contre le disque, `ask` seulement pour un fait NOUVEAU ».

## Restes
- Le canal `ctx_patch` (`op: create`, chemin porté en `path`, pas en `file_path`) n'est gaté par AUCUN des hooks `PreToolUse` `Write|Edit` du dépôt (préexistant, nommé par le juge hors lot) : un tag `[entériné]` neuf posé par `ctx_patch` passe sans `ask` -> inventaire #1750 : classe « hook Write = contenu entier » (Lot 3), le canal `ctx_patch` s'y ajoute comme angle mort mesuré, à porter par ce lot avec les autres hooks.
- Résidus jugés du côté SÛR (un déplacement d'un tag dans le fichier est silencieux — même multiensemble ; une différence de casse ou un chemin relatif hors dépôt → `ask`) -> RAS : le hook ne peut plus demander ce qu'il a déjà validé, et ce qu'il demande encore est un fait nouveau ou non lisible ; contrats du banc.
- `probe-setscan.mts` à la racine du worktree (sonde non suivie d'une autre session, hors périmètre) -> RAS : jamais stagée, jamais touchée.

## Réfutation
verdict: CONFIRMÉ — juge de diff du 2026-09-14 (Opus, lecture seule) : COMMITTABLE — multiensemble mesuré sur 7 sous-cas (déplacement silencieux ; duplication, date changée, casse → `ask` ; chemin relatif hors dépôt → `ask` : résidus du côté sûr), branche `Edit` strictement inchangée, câblage asserté sans chemin absolu de machine, cliquet `ecrivainsAtteints` motivé et vrai (écriture bornée à `os.tmpdir()`) ; griefs mineurs non bloquants tous hors lot (canal `ctx_patch`, sonde étrangère) et routés ci-dessus.
