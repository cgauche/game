# Solde #1389 « [#1388 · Lot A] Socle découpe & résolution : lib de prod, garde CI de re-résolution, chemin app, pilote psychology » — 2026-09-14

VERIFIE: les sept items du « À livrer » et les trois lignes du DoD, confrontés au diff du worktree `.wt-1388-A` sur `61b90d8c8` — (1) SCHÉMA : l'adresse est un champ de la grammaire (`src/data/schemas/grammaire/prose.ts`, forme `DescRef` déclarée en `src/data/source/decoupe.ts:79`, `desc` et `descRef` EXCLUSIFS, refus porté par `resoudreProse` `scripts/source/resoudre.mjs:22`), `source:{book,page}` survit à côté (les 9 entrées de `src/data/psychology.json` portent les deux) ; (2) RÉSOLUTION CÔTÉ APP : le navigateur ne résout rien — le plugin Vite `wfrp:prose-source` (`scripts/source/prose-source-plugin.mjs`) MATÉRIALISE la prose dans le module JSON servi, prouvé sur la donnée RÉELLE par `src/data/source/prose-source.test.ts` (« PROSE MATÉRIALISÉE — 1 module(s) adressé(s), 9 adresse(s) sur le disque », 16/16) ; (3) GARDE CI de re-résolution : `src/data/prose-resolution.test.ts` re-résout TOUTES les adresses des deux racines, empreintes comprises, tolérance ZÉRO sans fichier de stock (17/17, « PROSE ADRESSÉE — 9 adresse(s) sur src/data (122 .json) + src/scenes (4 .json), 1 chapitre(s) distinct(s) ») et elle liste EXACTEMENT les consommateurs impactés, code de rupture par entrée ; (4) MONTAGE PLAFONNÉ : `MIN_FRAGMENT = 40` / `MAX_FRAGMENTS = 3` (`decoupe.ts:110-112`), unicité et chevauchement refusés à la résolution, les quatre codes du volet D éprouvés sur fixture ; (5) RENDU inchangé, aucun composant de prose neuf dans le diff ; (6) GARDES QUI LISENT `desc` : la garde PAR FAMILLE `src/data/psychology-verbatim.test.ts` est SUPPRIMÉE par C5 (`695be73cc`, vérifié `git log --diff-filter=D`) et remplacée par la garde générique — solde de gardes +1 générique / −1 par famille, 0 gate ajoutée à `ci.yml` ; (7) OUTIL DE RÉPARATION, l'objet de ce commit : `scripts/source/reparer-adresses.mjs` relocalise une adresse cassée par son TEXTE D'ORIGINE (`git show <--depuis>:<chapitre>`, patron `reanchor-split.mjs:42-45`) et rend quatre verdicts — `RECALÉE` (un seul emplacement ET l'adresse neuve re-rend le texte d'origine à l'octet, `reparer-adresses.mjs:144`), `AMBIGUË` (emplacements listés), `PERDUE`, `IRRÉCUPÉRABLE` — `--apply` n'écrivant QUE les `RECALÉE` ; trois définitions hissées plutôt que recopiées : le walk des adresses (`scripts/source/adresses.mjs`, désormais SEULE définition — `prose-resolution.test.ts` et `prose-source.test.ts` ont perdu leurs walks locaux, `grep` de walk local dans les tests de `src/data` = 0), l'ambiguïté des blocs (`findAllRuns` dans `decoupe.ts:455`, dont `findRuns` est la dérivée, zéro appelant changé) et la réécriture textuelle ancrée (`scripts/source/reecriture-ancree.mjs`, `remplacerAncre`/`compterAncre`/`jsonIndente`, que la migration `scripts/migrations/2026-09-05-1389-psychology-desc-vers-descref.mjs:141` consomme à la place de sa copie) ; SORTIES MESURÉES sur l'arbre : `node scripts/source/reparer-adresses.mjs` → « ADRESSES — 9 inventoriée(s), 0 cassée(s), origine lue à `HEAD` » exit 0, `--dataset psychology` idem, la migration rejouée → « 9 entrée(s), 0 adressée(s), 9 sautée(s) / Remplacements écrits : 0 » exit 0, et rejouée sur le `psychology.json` d'AVANT (`git show 2998720aa:`, md5 `2f92991e…`) elle rend md5 `5f912624…` = l'octet exact de l'arbre (fichier remis à l'identique, `git status` propre) ; garde du geste : `src/data/source/reparer-adresses.test.ts` monte un DÉPÔT GIT jetable (`instanceDeDepot`), corrige le chapitre, et prouve les issues sur fixture PUIS le câblage de production sans injection (10/10), cinq mutations rouges remises à la main par hash ; ÉCRITURE D'UN DOCUMENT COMPACT (bloquant relevé par le juge de diff : sur `[\n  { "id": …, "descRef": {…} }\n]`, `--apply` écrivait un JSON INVALIDE et rendait « Écritures : 1 … reste 0 » exit 0) — TROIS filets, chacun à sa place : `remplacerAncre` ré-indente sur le BLANC DE TÊTE de la ligne porteuse et non sur le préfixe entier (`reecriture-ancree.mjs:39`), `--apply` RE-PARSE le document avant d'écrire et nomme l'échec sans toucher un octet (`reparer-adresses.mjs:255`), et `adressesDuDepot` LÈVE nommément sur un `.json` illisible (`adresses.mjs:64`) au lieu de l'avaler — le ré-jugement d'après-écriture n'est donc plus aveugle (`ré-jugement impossible après écriture : document illisible : …` entre dans `echecs`, exit 1) ; la migration C5 passe par la même primitive et sa sortie reste md5-identique ; `npx vitest run src/data/source src/data/prose-resolution.test.ts src/data/prose-inline-contrat.test.ts` 81/81 exit 0, `npm run typecheck:fast` 0 erreur, `npx eslint` 8 fichiers 0, `node scripts/docs/check-doc-refs.mjs` « OK (32 docs vivantes) » ; DoD : le geste humain est DOCUMENTÉ (`docs/ajouter-un-livre-source.md` §7, six pas + table « ce que chaque outil voit et ne voit PAS » + les deux précédents `850ae3199` et `8d7465698`, sujets vérifiés au `git log`), la carte des modules ne ment plus (`docs/architecture.md` gagne son entrée `scripts/source/`), et la porte joueur (Codex › États psychologiques, prose rendue depuis le livre) a été recettée au C5 le 2026-09-07 puis re-vue en recette courte le 2026-09-14 par l'orchestratrice — je ne l'ai pas rejouée moi-même.

## Restes
- Item 6 non porté par ce lot : le folio de la découpe qui valide `source.page` (`scripts/guards/lib/folioIntegrity.mjs` + `src/data/book-source-integrity.test.ts` lisent encore la `desc`), `src/data/variants-integrity.test.ts` (à adapter au PREMIER porteur à variantes adressé — `psychology.json` n'en a aucun, l'adapter à vide serait une garde sans cas) et `tavern-desc-verbatim` (prose RAW recopiée dans `src/engine/tavernGame.ts`, hors racines de donnée) -> #1390
- `citationCoverage.isCitedItem` et `npm run raw:implemente` -> RAS : mesuré le 2026-09-04, aucun des deux ne lit `desc` (une adresse EST une citation, ils travaillent sur `source`/les réfs RAW) — rien à adapter
- Stock de prose INLINE restant : 2617 nœuds sur 53 types (`src/data/prose-inline-contrat.test.ts`, baseline décroissante `src/data/schemas/grammaire/prose-inline.ts`) -> RAS : c'est le DÉNOMINATEUR de la campagne d'adressage, tenu par sa propre baseline décroissante, et le pilote n'en migre qu'une famille par construction
- `scripts/source/adresses.d.mts` créé en plus du périmètre du brief (qui prescrivait `@ts-expect-error`) -> RAS : convention du dépôt pour un `.mjs` lu depuis `src/**` (`scripts/guards/lib/lister.d.mts` et 30 autres) — sans lui l'inventaire arrivait en `any` dans deux tests et y rouvrait des `implicitly has an 'any' type` ; `depotGabarit.mjs`, qui n'a pas de `.d.mts`, garde bien son `@ts-expect-error`

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 2026-09-14) : RÉFUTÉ sur un point puis CONFIRMÉ après correction — le juge a rejoué lui-même l'outil (9/0), le test, la mutation « preuve à l'octet » (rouge 98/102 car.), `ancresDObjet` sur guillemets échappés, imbrication, `"descRef" :` et doublons (refus nommés), les précédents `850ae3199`/`8d7465698` au `git show`, et a TROUVÉ par sonde sur dataset COMPACT que `remplacerAncre` prenait tout le préfixe de ligne pour indentation (JSON invalide écrit) tandis que l'inventaire avalait l'illisible (bilan « reste 0 » exit 0) — corrigé par trois filets (blanc de tête seul ; `JSON.parse` avant `writeFileSync` ; `adressesDuDepot` lève et le ré-jugement le pousse dans `echecs`), cas compact + câblage réel sans injection ajoutés (10/10), mutations en paire rouges ; la garde du geste est éprouvée par MUTATION sur son câblage réel — relocalisation débranchée (`emplacementsDe` → `[]`) : trois rouges dont « expected [ 'PERDUE', 'PERDUE' ] to deeply equal [ 'RECALÉE', 'RECALÉE' ] » ; `--apply` élargi des `RECALÉE` aux `cassees` : deux rouges sur les cas AMBIGUË et IRRÉCUPÉRABLE ; preuve à l'octet neutralisée : « expected 'RECALÉE' to be 'IRRÉCUPÉRABLE' » sur le chapitre RÉ-HABILLÉ — les trois remises à l'identique par hash (`c2b0779c…`), suite re-verte 6/6. Non joué par moi : les gates complètes, jouées sur la tête du lot avant le push.
