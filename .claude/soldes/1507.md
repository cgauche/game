# Solde #1507 — 2026-09-04

VERIFIE: le socle du ticket (recettes volumiques authorées en MÈTRES, `radiusM`, coutures d'échelle nominatives comptées par `src/state/echelle-de-scene.test.ts`, `metresPerTile` par défaut = 2 = RAW LDB 15 l.12) est posé sur main par `30dd38716` (ligne 1 de #1680) ; le ticket ne restait ouvert que sur la question de GOÛT de l'échelle de la Diligence (64 × 76 m à 2 m/case). L'utilisateur l'a tranchée le 2026-09-04, question posée avec ses assertions, réponse verbatim « A — 2 m/case, RAW » (commentaire #1507) ; `docs/campagne-authoring.md` le consigne dans la référence vivante. Aucun code ne change.

## Restes
- Option B (auberge deux fois plus petite : hors RAW, Mouvement 4 = 4 m, portées doublées en cases, toits repensés, contrat « un meuble tient dans sa case » sauté) -> RAS : écartée par l'utilisateur, jamais un arbitrage tacite.
- Le rapport hauteur/largeur des 22 recettes prouvait déjà l'authoring à 2 m/case (un coffre serait plus haut que long à 1 m/case) -> RAS : mesuré au commit `30dd38716`.

## Réfutation
verdict: CONFIRMÉ — la fermeture ne repose sur aucun changement de code : elle consigne un arbitrage utilisateur cité verbatim et daté, conforme à la règle « tout arbitrage consigné porte sa citation », et l'échelle en vigueur est celle que `echelle-de-scene.test.ts` verrouille (21 sites nommés, aucun `?? 2` recopié).
