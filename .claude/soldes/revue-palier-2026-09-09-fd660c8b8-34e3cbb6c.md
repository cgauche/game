# Revue de palier — fenêtre fd660c8b8..34e3cbb6c — 2026-09-09

verdict: PARTIEL

8 lentilles de juge lancées sur les faits de leur angle, 8 rendues, 23 trouvailles distinctes, 19 confirmées après réfutation, 4 écartées, 71 points vérifiés qui tiennent. 16 agents joués : 8 de lentille, 8 de réfutation (un par lentille à trouvailles, plafond 12).

## Trouvailles confirmées

1. **#996 — le commit FERMANT localise la preuve du point 3 du DoD là où elle n'est pas : « l'angle mort restant (`require()`, recopie sans import) est énoncé dans la config à côté de la règle » est FAUX à l'octet** (lentille fermetures-soldes) — Message de 9df43457b (fermeture de #996) : « l'angle mort restant (`require()`, recopie sans import) est énoncé dans la config à côté de la règle ». Mesure : `grep -n -i "recopie|require(" eslint.config.js` → UNE seule occurrence, `eslint.config.js:256`, qui appartient au bloc « MUR DE L'ORDRE TOTAL » (`require('fs').readdirSync`), sans rapport avec la pureté de couche ; `sed -n '60,75p' eslint.config.js | grep -i "require|recopie"` → vide (le docblock de pureté ne dit que ce qui PASSE et pourquoi deux règles). L'énoncé réel vit ailleurs : `src/eslint-purete-de-couche.test.ts:27-28` (« Reste hors de portée, comme pour toute police d'accès : la recopie qui ne passe par aucun import, et `require()` »), ce que le solde `.claude/soldes/996.md:3` dit correctement. Le DoD du ticket exigeait l'énoncé « dans son docblock » : il est tenu (docblock du filet), mais le commit qui ferme le ticket oriente le lecteur vers un fichier où le texte n'existe pas. → attendu : Le message de fermeture cite le SITE réel de la preuve (`src/eslint-purete-de-couche.test.ts:27-28`) — ou la config porte effectivement l'angle mort à côté de la règle, comme annoncé. · réfutation tentée : Arbre épinglé : HEAD = 34e3cbb6c (`git log --oneline -1`, worktree C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624) ; commit fermant #996 = 9df43457b, présent dans l'histoire (contrôle positif : `git log --oneline -3 9df43457b` rend son message qui contient bien la phrase incriminée).

RE-MESURE (PowerShell, lecture seule) :
`Select-String -Path eslint.config.js -Pattern "recopie|require\(|angle mort|purete|pureté|no-restricted-imports"` → aucune occurrence de `require(` ni de « recopie » dans un bloc de pureté ; la seule ligne parlant de `require(` est eslint.config.js:256 (bloc « MUR DE L'ORDRE TOTAL », `require('fs').readdirSync`), sans rapport avec la pureté de couche.
`Select-String -Path eslint.config.js -Pattern "angle|aveugl|hors de port|recopi|CommonJS"` → 57 (dialecte de parse), 88 (« Import DYNAMIQUE … hors de portée de `no-restricted-imports` » — c'est le volet COUVERT par la 2e règle, pas l'angle mort), 105, 174, 233, 251 : aucune mention de l'angle mort `require()`/recopie.
Lecture verbatim du docblock de pureté (eslint.config.js:60-75) : il énonce le critère structurel, les formes élidées qui PASSENT, et pourquoi DEUX règles — rien sur `require()` ni sur la recopie.
L'énoncé réel vit bien ailleurs : src/eslint-purete-de-couche.test.ts:22-28, verbatim « CE QUE LA POLICE NE VOIT PAS … Reste hors de portée, comme pour toute police d'accès : la recopie qui ne passe par aucun import, et `require()`. »

Donc la phrase du commit 9df43457b est matériellement fausse sur le SITE (« dans la config à côté de la règle »).

NON BLOQUANTE : le DoD demandait l'énoncé « dans son docblock » (docblock de la GARDE) — tenu à src/eslint-purete-de-couche.test.ts:27-28 — et le solde .claude/soldes/996.md le localise CORRECTEMENT (point (3) : « `src/eslint-purete-de-couche.test.ts:27-28` … et le docblock d'`eslint.config.js:60-75` dit ce qui passe … et pourquoi deux règles » ; ligne « Restes » : « -> corrigé par c4a0854cf src/eslint-purete-de-couche.test.ts:27 »). La fermeture de #996 tient ; le défaut est une imprécision de PROSE du message de fermeture, qui envoie un lecteur vers un fichier où le texte n'existe pas.

```
cd "/c/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624"; grep -n -i "recopie\|require(" eslint.config.js; sed -n '60,75p' eslint.config.js | grep -n -i "require\|recopie"   # → seule ligne 256 (bloc ordre total) ; docblock de pureté : vide
```
2. **#1692 — le volet « coût mesuré » du DoD est tenu EN PROSE seule : les chiffres (8,06 ns → 10,96 ns, « chemin chaud < 1 % ») ne sont adossés à aucune sonde committée, donc non re-mesurables** (lentille fermetures-soldes) — DoD du ticket : « … au choix mesuré par le coût réel (sonde sur le chemin chaud) ». Solde `.claude/soldes/1692.md:3` point (3) : « Map figée 8,06 ns → version 10,96 ns par lecture, chemin chaud (bake, tour IA) < 1 % ». Recherche du support dans l'arbre : `git grep -l "10,96|8,06" -- src scripts .claude` → un SEUL fichier, `.claude/soldes/1692.md` (le solde lui-même). `git show --stat a8d16ade0` n'apporte aucun banc de perf (les fichiers neufs sont `src/data/versionDataset.ts`, `index-vif-guard.test.ts`, `seam-ecriture-guard.test.ts`, `fraicheur-datasets.test.ts`) : la sonde de coût a été jetée avec son transcript. → attendu : Le chiffre qui justifie le choix de régime (index vif mémoïsé plutôt que `.find` à l'appel) est porté par une sonde/banc committé et rejouable — ou le solde dit que la mesure est indicative et non re-mesurable, plutôt que de l'annoncer comme mesure du DoD. · réfutation tentée : Arbre épinglé : HEAD = 34e3cbb6c. Contrôle positif du travail jugé : a8d16ade0 (`corrige #1692`) existe et pose bien src/data/versionDataset.ts (`memoParVersion`/`indexParId`/`indexParChamp`, lu verbatim), src/data/index-vif-guard.test.ts, seam-ecriture-guard.test.ts, fraicheur-datasets.test.ts.

DoD RE-VÉRIFIÉ À LA SOURCE (pas au brief) : `gh issue view 1692 --json body` → « … ou en index invalidé par version (patron `invalidateCodexLookup`/`LOOKUP_VERSION`…), **au choix mesuré par le coût réel (sonde sur le chemin chaud)** ».

MESURE D'ABSENCE :
`git grep -n -e "10,96" -e "8,06" -- src scripts .claude docs` → UNE seule ligne, `.claude/soldes/1692.md:3` (le solde lui-même).
`git grep -n -e "ns par lecture" -e "chemin chaud" -- .claude docs src scripts` → même unique ligne.
`git show --stat --format="" a8d16ade0` (136 fichiers) → aucun banc/sonde de perf ; `git ls-files | Select-String "perf|bench|banc"` → seuls scripts/recette/perf-scenario.mjs et src/gameIso/stage/banc-volumique.* (sans rapport) ; `git log fd660c8b8..34e3cbb6c --name-only --diff-filter=A -- scripts src` → aucun fichier de sonde/banc né dans la fenêtre. Le dépôt SAIT committer un banc quand la mesure porte un choix (précédent cf50eee98 : « banc de perf en RATIO naïf/index > 5 sur la même passe ») — l'écart de régime est donc mesurable, pas théorique.

SONDE (à promouvoir en banc, exécutée hors de l'arbre jugé, node, copie FIDÈLE de `memoParVersion` de src/data/versionDataset.ts, N=300, 20 M lectures, 3 rodages) :
fichier scratchpad `bench-index-vif.mjs` ; corps : `const VERSIONS=new Map(); const versionDuDataset=(c)=>VERSIONS.get(c)??0; function memoParVersion(cle,calcul){const cles=typeof cle==='string'?[cle]:cle;let version='',valeur;return()=>{const v=cles.map(versionDuDataset).join('/');if(v!==version){valeur=calcul();version=v;}return valeur;};} … const FIGE=new Map(entrees.map(e=>[e.id,e])); const index=memoParVersion('props',()=>new Map(entrees.map(e=>[e.id,e]))); const vif=(id)=>(id==null?undefined:index().get(id));` + boucle `process.hrtime.bigint()`.
SORTIE : `figee ns/lecture 9.34` · `vif ns/lecture 40.69` · `delta ns 31.36 ratio 4.36`.
Le delta que je mesure (+31,4 ns, ×4,4) est un ordre de grandeur au-dessus du delta annoncé au solde (+2,9 ns, ×1,36) : sans banc committé, personne ne peut dire lequel des deux protocoles est le bon — ce qui EST la trouvaille. (L'absolu reste petit, donc « < 1 % du chemin chaud » n'est pas réfuté ; il est simplement invérifiable.)

NON BLOQUANTE : la fermeture de #1692 repose sur l'attendu principal (garde structurelle + migration + contrats de fraîcheur), tous committés et rejouables ; le volet « coût mesuré » est un qualificatif de CHOIX. La fermeture tient, mais le point (3) du solde .claude/soldes/1692.md est une affirmation non re-mesurable présentée comme mesure du DoD.

```
cd "/c/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624"; git grep -n -l "10,96\|8,06" -- src scripts .claude   # → .claude/soldes/1692.md seul
```
3. **1e7f4f830 — le cliquet `justificatif.mjs +1` déclare 1 là où la table grandit de 3, et déclarer le VRAI compte aurait été REFUSÉ : 14 des 15 entrées de `RAISON_CLE_COMPLETE` sont invisibles au détecteur** (lentille stocks-et-cliquets) — Sonde (sortie verbatim) : « RAISON_CLE_COMPLETE @1e7f4f830 : 15 gates déclarées, 1 vues comme ENTRÉES par le détecteur » / « avant : 12 gates -> après : 15 (croissance RÉELLE = 3 entrées) » / « détecteur net = [ 1 ] » / « si le message avait déclaré +3 : [{"net":1,"declare":3}] » (non couvert ⇒ refus, `couvert = k.n === c.net`, scripts/guards/lib/stocksNominatifs.mjs:428). Entrées ajoutées : scripts/guards/lib/justificatif.mjs:95 (`test`), :101 (`test:ops`), :102 (`deps:unused`) ; seule est vue celle dont un littéral vaut exactement `src/memory-links-guard.test.ts:81` (le motif NOMME exige un match plein, stocksNominatifs.mjs:135-140). La règle écrite exige pourtant « `+N` compte les ENTRÉES du stock — ses éléments » (stocksNominatifs.mjs:446). → attendu : Le `+N` doit compter les entrées réelles ; ici la convention force à écrire le chiffre du détecteur (1) au lieu de la croissance vraie (3), et 14/15 entrées de cette table peuvent grossir sans qu'aucun CLIQUET ne soit exigé. · réfutation tentée : CONFIRMÉ. Arbre épinglé `34e3cbb6c` (`git log --oneline -1`, worktree C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624). Sonde rejouée (fichier C:\Users\gauch\AppData\Local\Temp\claude\C--Users-gauch-PhpstormProjects-Foundry-Game\4407a64f-b0ad-4d3d-b30f-ffca252025d6\scratchpad\p1.mjs, importe `scripts/guards/lib/stocksNominatifs.mjs`), sortie verbatim :
  PRE gates: 12 agents:check,test:hooks,test:docs,docs:check,docs:empreinte,test:raw,raw:coverage,raw:reconcile,raw:check-refs,raw:check-code-refs,raw:check-folio-continuity,raw:reanchor
  POST gates: 15 test,test:ops,deps:unused,agents:check,…
  entrees VUES par le detecteur: 1 test@97
  total entrees detectees dans le FICHIER: 1
  croissance: [{"fichier":"scripts/guards/lib/justificatif.mjs","ajoutees":1,"retirees":0,"net":1,"exemples":["test:"]}]
  non couvertes (message REEL): []
  non couvertes SI +3: [{"net":1,"declare":3}]
Donc : croissance RÉELLE = 3 entrées (12→15), détecteur = 1, et déclarer `+3` serait REFUSÉ (`couvert = k.n === c.net`, stocksNominatifs.mjs:428). Cause matérielle vérifiée à la lecture : seule la raison de `test` porte un littéral qui matche `NOMME` en PLEIN (`…src/memory-links-guard.test.ts:81`, justificatif.mjs:102) ; les 14 autres raisons citent des chemins AU MILIEU d'une phrase (`justificatif.mjs:103,107,108,109`) que `NOMME` (stocksNominatifs.mjs:106, ancré `^…$`) ne voit pas. La règle écrite exige bien « `+N` compte les ENTRÉES du stock — ses éléments » (stocksNominatifs.mjs:446) : la convention et le détecteur se contredisent sur ce fichier.
NUANCE re-mesurée et à porter au crédit du commit : le message NE MENT PAS, il dit lui-même l'écart — « la table `RAISON_CLE_COMPLETE` gagne trois gates (`test`, `test:ops`, `deps:unused`), comptées +1 net par le détecteur ».
NON BLOQUANTE : `1e7f4f830` est `refs #1709`, il ne porte ni fermeture ni solde ; les deux seules fermetures de la fenêtre (#996 @9df43457b, #1692 @a8d16ade0, `faits.fermetures`) ne dépendent pas de ce cliquet.

```
import { execFileSync } from 'node:child_process'
import { entreesDeStock, croissanceDesStocks, croissancesNonCouvertes } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/scripts/guards/lib/stocksNominatifs.mjs'
const cwd = 'C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624'
const g = (a) => { try { return execFileSync('git', a, { cwd, encoding: 'utf8', maxBuffer: 1e9 }) } catch { return null } }
const f = 'scripts/guards/lib/justificatif.mjs'
const post = g(['show', '1e7f4f830:' + f])
const lignes = post.split('\n')
const ent = new Set(entreesDeStock(post, f).map(e => e.ligne))
const debut = lignes.findIndex(l => l.startsWith('export const RAISON_CLE_COMPLETE'))
let gates = 0, vues = 0
for (let i = debut + 1; i < lignes.length && !lignes[i].startsWith('}'); i++) {
  if (/^  (?:'[^']+'|[A-Za-z_]\w*):/.test(lignes[i])) { gates++; if (ent.has(i + 1)) vues++ }
}
console.log(`RAISON_CLE_COMPLETE @1e7f4f830 : ${gates} gates déclarées, ${vues} vues comme ENTRÉES par le détecteur`)
const base = g(['show', '1e7f4f830^:' + f])
const lb = base.split('\n'); const db = lb.findIndex(l => l.startsWith('export const RAISON_CLE_COMPLETE'))
let gb = 0; for (let i = db + 1; i < lb.length && !lb[i].startsWith('}'); i++) if (/^  (?:'[^']+'|[A-Za-z_]\w*):/.test(lb[i])) gb++
console.log(`avant : ${gb} gates -> après : ${gates} (croissance RÉELLE = ${gates - gb} entrées)`)
const diff = g(['show', '--format=', '-U0', '--no-renames', '1e7f4f830'])
const images = { lirePostImage: (x) => g(['show', '1e7f4f830:' + x]), lirePreImage: (x) => g(['show', '1e7f4f830^:' + x]) }
console.log('détecteur net =', croissanceDesStocks(diff, images).filter(c => c.fichier === f).map(c => c.net))
console.log('si +3 :', JSON.stringify(croissancesNonCouvertes({ diff, message: 'CLIQUET: ' + f + ' +3 — trois gates passent sous la clé complète' }, images).map(c => ({ net: c.net, declare: c.declare }))))
```
4. **a8d16ade0 — CLIQUET-TAMPON : `+0` sur un fichier que le commit ne touche pas, et son motif « aucun stock nominatif ne grandit » est FAUX pour son propre commit (+1 mesuré)** (lentille stocks-et-cliquets) — Message : « CLIQUET: scripts/gates/ecrivainsAtteints.test.mjs +0 — aucun stock nominatif ne grandit ». (a) `git show --stat --format= a8d16ade0 -- scripts/gates/ecrivainsAtteints.test.mjs` → sortie VIDE : le fichier nommé n'est pas modifié par ce commit (ni par aucun de la plage : `git log fd660c8b8..34e3cbb6c -- ...` vide). (b) `+0` ne peut jamais couvrir : `croissanceDesStocks` filtre `net > 0` (stocksNominatifs.mjs:391). (c) sonde par commit : « == a8d16ade0 / CROISSANCE src/data/index-vif-guard.test.ts net=1 (+1/-0) / DECLARE scripts/gates/ecrivainsAtteints.test.mjs +0 » — le commit fait naître `const HORS_PERIMETRE = new Set(['src/data/overrides.ts', 'src/data/versionDataset.ts'])` (2 chemins, sous-comptés 1). Éteint deux commits plus loin (25b8e306d, entrées 1 → 0, mesuré). → attendu : Un CLIQUET nomme le fichier qui grandit avec son compte réel ; une ligne `+0` sur un fichier étranger au diff est un tampon et son affirmation globale (« aucun stock ne grandit ») doit être vraie du commit qui la porte. · réfutation tentée : CONFIRMÉ sur pièces (arbre `34e3cbb6c`). (a) Message verbatim : « CLIQUET: scripts/gates/ecrivainsAtteints.test.mjs +0 — aucun stock nominatif ne grandit ; `deps:unused` 11 exports morts gelés inchangés… » (`git log --format="%h%n%B" fd660c8b8..34e3cbb6c`). (b) Sonde par commit (scratchpad\p2.mjs, `croissanceDesStocks`+`cliquetsDuMessage`+`croissancesNonCouvertes` sur chaque sha de fd660c8b8..34e3cbb6c), sortie verbatim :
  == a8d16ade0
     CROISSANCE src/data/index-vif-guard.test.ts net=1 (+1/-0) ex=["const HORS_PERIMETRE = new Set(['src/data/overrides.ts', 'src/data/versionDataset.ts']);"]
     DECLARE    scripts/gates/ecrivainsAtteints.test.mjs +0  touche_par_ce_commit=false
     NON-COUVERT [{"f":"src/data/index-vif-guard.test.ts","net":1,"declare":null}]
Le fichier nommé par le cliquet n'est PAS dans `--name-only` du commit (mesuré `touche_par_ce_commit=false`) ; il est pourtant un PORTEUR (`estPorteurDeStock('scripts/gates/ecrivainsAtteints.test.mjs') = true`, mesuré). Un `+0` ne peut jamais couvrir quoi que ce soit : `croissanceDesStocks` filtre `net > 0` (stocksNominatifs.mjs:391) — c'est donc bien un TAMPON, et son affirmation globale est fausse pour son propre commit (+1 net mesuré, 2 chemins sous-comptés 1, angle mort « plusieurs entrées sur UNE ligne » documenté stocksNominatifs.mjs:52).
POURQUOI LA PORTE N'A RIEN VU (re-mesuré, `faits.derogations.dansLaFenetre = []` — aucune dérogation) : la porte de PLAGE agrège. Sur `fd660c8b8..34e3cbb6c` la croissance nette de `src/data/index-vif-guard.test.ts` est 0 — entrées 1 @a8d16ade0 → 0 @25b8e306d → 0 @HEAD (mesuré), `HORS_PERIMETRE` absent de HEAD. Le défaut est donc RÉEL au commit et ÉTEINT à l'arbre.
NON BLOQUANTE, dit explicitement : a8d16ade0 EST la fermeture de #1692 (`corrige #1692`, `solde:true`), mais le DoD fermé porte sur l'index vif, pas sur le cliquet ; la croissance non couverte a été éteinte deux commits plus loin DANS la même fenêtre (25b8e306d, `refs #1692`), et la porte de plage sortait verte. Ce qui reste est un défaut d'hygiène de déclaration, pas une fermeture invalidée.

```
import { execFileSync } from 'node:child_process'
import { croissanceDesStocks, cliquetsDuMessage } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/scripts/guards/lib/stocksNominatifs.mjs'
const cwd = 'C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624'
const g = (a) => { try { return execFileSync('git', a, { cwd, encoding: 'utf8', maxBuffer: 1e9 }) } catch { return null } }
const shas = g(['rev-list', '--reverse', '--no-merges', 'fd660c8b8..34e3cbb6c']).trim().split('\n')
for (const sha of shas) {
  const message = g(['show', '-s', '--format=%B', sha]) ?? ''
  const diff = g(['show', '--format=', '-U0', '--no-renames', sha]) ?? ''
  const images = { lirePostImage: (f) => g(['show', sha + ':' + f]), lirePreImage: (f) => g(['show', sha + '^:' + f]) }
  const cr = croissanceDesStocks(diff, images), cl = cliquetsDuMessage(message)
  if (cr.length || cl.length) {
    console.log('== ' + sha.slice(0, 9))
    for (const c of cr) console.log(`   CROISSANCE ${c.fichier} net=${c.net} (+${c.ajoutees}/-${c.retirees})`)
    for (const k of cl) console.log(`   DECLARE    ${k.fichier} +${k.n}`)
  }
}
```
5. **ff6771b90 — le corps du commit annonce « STOCK NOMINATIF GELÉ de 48 entrées », le fichier committé en porte 47** (lentille stocks-et-cliquets) — `git show -s --format=%B ff6771b90 | grep -o "STOCK NOMINATIF GELÉ de [0-9]* entrées"` → « STOCK NOMINATIF GELÉ de 48 entrées » ; comptage du tableau `const STOCK` au même sha : `git show ff6771b90:src/data/schemas/grammaire/records-de-libelles.test.ts | sed -n '/^const STOCK/,/^\];/p' | grep -cE "^  '"` → 47 (le détecteur donne aussi net=47, et la ligne CLIQUET dit bien +47). → attendu : Le compte d'un stock annoncé dans le message doit être celui du fichier — un chiffre de prose faux devient la référence des paliers suivants. · réfutation tentée : CONFIRMÉ. Arbre `34e3cbb6c`. Sonde (scratchpad\p3.mjs), sortie verbatim :
  T4 STOCK@ff6771b90 = 47 lignes-entree ; entreesDeStock(fichier) = 47
  T4 STOCK@7c818901a = 30 ; T4 STOCK@34e3cbb6c = 21 ; T4 STOCK@HEAD = 21
  T4 message dit : [ 'STOCK NOMINATIF GELÉ de 48 entrées à migrer au train B' ]
Le compte du fichier `src/data/schemas/grammaire/records-de-libelles.test.ts` et le compte du détecteur concordent à 47 ; la ligne de cliquet du même message dit bien `+47` (« CLIQUET: src/data/schemas/grammaire/records-de-libelles.test.ts +47 — stock NOMINATIF GELÉ … »), et la croissance mesurée du commit est net=47. Seule la PROSE dit 48 — vraisemblablement l'estimation de design « ~32-48 Records UI » reprise à tort dans la même phrase.
CE QUI ATTÉNUE (re-mesuré, contre l'`attendu` de la trouvaille « devient la référence des paliers suivants ») : le faux 48 n'a PAS propagé — le palier suivant repart du bon compte : 7c818901a dit « Stocks : `records-de-libelles` 47 → 30 » et 34e3cbb6c « 30 → 21 » (mesuré : 30 puis 21 dans le fichier).
NON BLOQUANTE : ff6771b90 est `refs #1694, refs #1686`, aucune fermeture ni solde ; les deux fermetures de la fenêtre (#996, #1692) sont étrangères à ce stock. Reste un chiffre de prose faux dans un message immuable, à ne pas citer.

```
cd C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624 && for s in ff6771b90 7c818901a 34e3cbb6c; do n=$(git show $s:src/data/schemas/grammaire/records-de-libelles.test.ts | sed -n '/^const STOCK/,/^\];/p' | grep -cE "^  '"); echo "STOCK @$s = $n"; done   # → 47 / 30 / 21
```
6. **PARAPHRASE RAW plus ÉTROITE que le Source : le commentaire ajouté restreint aux « Projectiles » une règle que LDB 48 donne à TOUS les Sorts du Domaine** (lentille poison-des-diffs) — Ajouté par la fenêtre (`git diff fd660c8b8 HEAD -- src/data/schemas/defs/domains.ts` : ligne en `+`) — `src/data/schemas/defs/domains.ts:20` :
`/** Matière de PA ignorée par les Projectiles du Domaine (LDB 48). */`
Source cité, VERBATIM :
- `Source/Warhammer v4 - Livre de base version corrigée/48 - Magie des Couleurs.md:104-106` (Domaine des Cieux) : « Les Sorts d'Arcane issus du Domaine des Cieux […] Les Sorts infligeant des / Dégâts ignorent les PA des armures en métal »
- `…/48 - Magie des Couleurs.md:588` (Domaine des Ombres) : « De plus, les Sorts lancés depuis le Domaine des Ombres ignorent tous les PA non magiques. »
Les DEUX seules valeurs de l'enum (`metal`, `nonMagic`, `src/data/domains.json:666,778`) sont donc portées par une règle qui vise « les Sorts infligeant des Dégâts » / « les Sorts lancés depuis le Domaine », jamais les seuls Projectiles ; le `alsoIn` de la donnée elle-même (`src/data/domains.json:662`) recopie « Les Sorts qui infligent des Dégâts ignorent les Points d'Armure des armures en métal ». Le rétrécissement n'est pas neutre : c'est ce que le moteur applique (`src/engine/domainAttributes.ts:60` `domainMissileMods`, appelé sur la seule mitigation de Projectile), et le commentaire neuf donne à ce rétrécissement l'AUTORITÉ d'une réf RAW. → attendu : Le commentaire porte la réf NUE (CLAUDE.md règle 6a) — pas d'énoncé de règle ; et l'écart de portée entre la règle (tout Sort du Domaine infligeant des Dégâts) et le code (Projectiles seuls) est instruit au ticket, pas masqué par un descripteur de schéma. · réfutation tentée : Arbre épinglé 34e3cbb6c (`git log --oneline -1`, .wt-1624). MATÉRIEL : `src/data/schemas/defs/domains.ts:20` = `/** Matière de PA ignorée par les Projectiles du Domaine (LDB 48). */`, ligne en `+` de `git diff fd660c8b8 HEAD -- src/data/schemas/defs/domains.ts` (avec `missileBypassSchema = enumNomme({ metal, nonMagic })`). SOURCE re-lu moi-même (Read, pas le brief) : `Source/Warhammer v4 - Livre de base version corrigée/48 - Magie des Couleurs.md` l.104-106 (Cieux) « Les Sorts d'Arcane issus du Domaine des Cieux […] Les Sorts infligeant des / Dégâts ignorent les PA des armures en métal » ; l.399 (Métal) « Les Sorts infligeant des Dégâts ignorent les PA des armures métalliques, et infligent un bonus de Dégâts égal au nombre de PA de l'armure métallique portée » ; l.588 (Ombres) « les Sorts lancés depuis le Domaine des Ombres ignorent tous les PA non magiques ». Les trois porteurs mesurés en donnée sont exactement ces trois Domaines (`src/data/domains.json:665-667` cieux `metal`, `727-729` metal + `bonusFromBypass`, `777-778` ombres `nonMagic`) — donc les DEUX valeurs de l'enum sont portées par une règle « tout Sort infligeant des Dégâts / tout Sort du Domaine », jamais « les Projectiles ». Le commentaire énonce donc une règle (règle 6a : le commentaire porte la réf NUE) et l'énonce plus étroite que le Source, avec la caution d'une réf RAW. NUANCE mesurée (à charge de l'auteur de la trouvaille, pas contre elle) : le rétrécissement n'est PAS né du commentaire — le champ s'appelle `missile`, et le seul consommateur `domainMissileMods` (`src/engine/domainAttributes.ts:60`) n'est appelé qu'en `evaluateMissile`/`magicDeviationEligible` (`src/engine/magic.ts:1030,1074` — sonde `ctx_search regex domainMissileMods` : 9 occurrences, 4 fichiers, aucun autre appelant) ; l'écart de portée règle↔moteur préexiste et reste à instruire au ticket. Non bloquante : le commit porteur 34e3cbb6c ne porte que `refs #1694, refs #1686`, aucune fermeture ni solde de la fenêtre n'en dépend.

```
PowerShell, arbre 34e3cbb6c :
`cd C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624; git diff fd660c8b8 HEAD -- src/data/schemas/defs/domains.ts`  → la ligne `+/** Matière de PA ignorée par les Projectiles du Domaine (LDB 48). */`
ctx_shell : `grep -n "armures en métal" Source/'Warhammer v4 - Livre de base version corrigée'/*.md` → `48 - Magie des Couleurs.md:106: Dégâts ignorent les PA des armures en métal…` ; `grep -n "Domaine des Ombres" -A4 "48 - Magie des Couleurs.md"` → l.588 « les Sorts lancés depuis le Domaine des Ombres ignorent tous les PA non magiques ».
```
7. **PIERRE TOMBALE ajoutée dans un banc neuf : le commentaire nomme « l'ancien banc », ses TROIS exemptions nominatives et son allowlist — tous morts à HEAD** (lentille poison-des-diffs) — `src/eslint-purete-de-couche.test.ts:22-28` (fichier CRÉÉ par la fenêtre, commit c4a0854cf), VERBATIM :
« CE QUE LA POLICE NE VOIT PAS, et pourquoi : les formes ÉLIDÉES à la compilation […] ne créent aucune / arête d'exécution — c'est le critère STRUCTUREL qui remplace les trois exemptions NOMINATIVES de / l'ancien banc (`state/combatManeuvers.ts`, `state/roster.ts`, `state/revealStep.ts`, tous trois / des `import type … from '../ui/…'`) et l'allowlist `engine/types.ts` (une référence inline vers / `gameIso/rig/appearance`). »
Les artefacts nommés sont RÉVOLUS : `ls src/engine-purity.test.ts src/state-purity.test.ts src/gameiso-purity.test.ts` → « No such file or directory » (×3) ; l'allowlist `engine/types.ts` n'existe plus (le commit c4a0854cf l'annonce lui-même comme tombée). CLAUDE.md règle 6(c) : « La pierre tombale (« déplacé vers X », rappel de l'ancien état) : à supprimer à vue, git porte l'historique », « (c) tolérance ZÉRO, sans liste d'exception ».
POURQUOI LA GARDE NE L'A PAS VU (angle mort mesurable, `scripts/guards/lib/commentPoison.mjs`) : la famille tombale n'a que ces motifs sur « ancien » — `l:363` `/anciennement/i`, `l:375` `/\bl['’]ancien(?:ne)?s?\s+chemin\b/i` (le mot `chemin` EXIGÉ), `l:377` `/\bremplac\w+\s+l['’]ancien/i` (adjacence exigée — ici on lit « remplace les trois exemptions NOMINATIVES **de** l'ancien banc »), `l:400` `/\(ancien(?:ne)?s?\b/i` (parenthèse exigée), `l:401` `ANCIEN_IDENT_EOL_RX` = `'\\b[Aa]ncien(?:ne|s|nes)?\\s+' + IDENT_CODE + '…$'` (identifiant de CODE exigé — « banc » n'en est pas un). Aucun ne mord. → attendu : Le commentaire dit le critère STRUCTUREL courant (formes élidées = aucune arête d'exécution) sans nommer le banc mort ni ses exemptions défuntes ; et la famille tombale gagne le motif « l'ancien <nom commun> » que ce site exhibe. · réfutation tentée : Arbre 34e3cbb6c. Texte relu à la source : `src/eslint-purete-de-couche.test.ts:22-28` « CE QUE LA POLICE NE VOIT PAS […] c'est le critère STRUCTUREL qui remplace les trois exemptions NOMINATIVES de / l'ancien banc (`state/combatManeuvers.ts`, `state/roster.ts`, `state/revealStep.ts`, tous trois / des `import type … from '../ui/…'`) et l'allowlist `engine/types.ts` (une référence inline vers / `gameIso/rig/appearance`) ». Fichier CRÉÉ par la fenêtre : `git diff --stat fd660c8b8 HEAD` → `src/eslint-purete-de-couche.test.ts | 122 +++…` (100 % ajout). Artefacts nommés RÉVOLUS, contrôle positif/négatif : `Test-Path src/eslint-purete-de-couche.test.ts` → True ; `Test-Path src/engine-purity.test.ts` / `state-purity.test.ts` / `gameiso-purity.test.ts` → False, False, False. C'est bien un rappel d'ancien état (CLAUDE.md règle 6c, tolérance ZÉRO). ANGLE MORT DE LA GARDE re-mesuré par sonde EXÉCUTÉE (pas par lecture de regex) — `node scratchpad/sonde-tombales.mjs` :
```js
import { readFileSync } from 'node:fs';
import { scanTombstones, estFichierScanne } from 'file:///…/.wt-1624/scripts/guards/lib/commentPoison.mjs';
for (const rel of ['src/eslint-purete-de-couche.test.ts','src/data/fraicheur-datasets.test.ts','src/data/schemas/grammaire/valeurs.ts','src/data/schemas/defs/enums-nommes.test.ts','src/data/schemas/defs/domains.ts'])
  console.log(rel,'| scanne=',estFichierScanne(rel),'| tombales=',scanTombstones(rel, readFileSync(ROOT+rel,'utf8')).length);
```
Sortie : les 5 fichiers `scanne= true`, `tombales= 0` — la famille tombale (y compris la famille (e) « vocabulaire de l'ancien état », `commentPoison.mjs:405-415`) ne mord sur AUCUN. Non bloquante : le solde #996 (9df43457b) est substantiel (règle eslint en place, trois bancs vitest absents, rejeu de mutation cité) ; le poison est un défaut de contenu livré, il n'invalide pas la fermeture.

```
ctx_shell (lecture seule) : `cd .wt-1624 && ls src/engine-purity.test.ts src/state-purity.test.ts src/gameiso-purity.test.ts` → 3× « No such file or directory » ; `grep -n "ancien" scripts/guards/lib/commentPoison.mjs` → l.363/375/377/400/401 (les cinq motifs ci-dessus) ; `grep -n "ANCIEN_IDENT_EOL_RX =" -A6 scripts/guards/lib/commentPoison.mjs` → `'\\b[Aa]ncien(?:ne|s|nes)?\\s+' + IDENT_CODE + '[ \\t]*(?:\\*+/)?[ \\t]*$'`.
```
8. **Trois autres commentaires AJOUTÉS rappellent l'état d'AVANT le lot (famille tombale), tous sous le même angle mort de la garde** (lentille poison-des-diffs) — 1) `src/data/fraicheur-datasets.test.ts:4-6` : « Ces cas ÉCHOUAIENT avant le lot : les accesseurs lisaient un index construit une fois à l'import, / si bien qu'après une édition au Codex (`setDataset`…) l'entrée éditée / gardait son ancien libellé et une entrée NEUVE n'existait pour personne. »
2) `src/data/schemas/grammaire/valeurs.ts:56-57` : « elle ne peut plus être partielle / comme l'était le Record d'affichage qu'elle remplace. »
3) `src/data/schemas/defs/enums-nommes.test.ts:3-4` : « C'est la couverture que les / Records de libellés côté UI portaient avant leur mort (`grammaire/records-de-libelles.test.ts`) ».
Aucun n'est pris par la famille tombale : le motif « avant » n'existe qu'en `/(comme avant\s*:|avant\s*:\s*«)/i` (`commentPoison.mjs:390`) et `/\(avant\s*:/i` (`l:394`) — « avant le lot : », « comme l'était », « avant leur mort » passent tous. → attendu : Le contrat d'un banc s'énonce au PRÉSENT (ce qui doit tenir), jamais par le défaut d'hier — git porte l'historique (CLAUDE.md règle 6c) ; ces trois en-têtes se reformulent en contrat positif. · réfutation tentée : Arbre 34e3cbb6c, trois sites relus verbatim : (1) `src/data/fraicheur-datasets.test.ts:4-6` « Ces cas ÉCHOUAIENT avant le lot : les accesseurs lisaient un index construit une fois à l'import, […] l'entrée éditée gardait son ancien libellé et une entrée NEUVE n'existait pour personne. » ; (2) `src/data/schemas/grammaire/valeurs.ts:56-57` « elle ne peut plus être partielle comme l'était le Record d'affichage qu'elle remplace. » ; (3) `src/data/schemas/defs/enums-nommes.test.ts:3-4` « C'est la couverture que les Records de libellés côté UI portaient avant leur mort (`grammaire/records-de-libelles.test.ts`) ». Tous ajoutés par la fenêtre : `git diff --stat fd660c8b8 HEAD` → `fraicheur-datasets.test.ts | 257 +++` (créé), `enums-nommes.test.ts | 114 +++` (créé), `valeurs.ts | 76 ++++++` (ajouts). Angle mort re-mesuré par la MÊME sonde exécutée que ci-dessus : `scanTombstones` rend 0 sur les trois. RÉSERVE À CHARGE (mesurée, ne renverse pas le verdict) : la réf `grammaire/records-de-libelles.test.ts` du site (3) n'est PAS pendante — `Test-Path src/data/schemas/grammaire/records-de-libelles.test.ts` → True (c'est le banc de stock décroissant) ; seule la formule « avant leur mort » est du rappel d'ancien état. Non bloquante : ces sites vivent dans des commits `refs #1692/#1694`, aucun ne porte le solde d'une fermeture de la fenêtre.

```
PowerShell : `Select-String -Path <scratchpad>\added-comments.txt -Pattern "d.sormais|remplac|anciennement|avant "` sur les 1 136 commentaires AJOUTÉS extraits de `git diff -U0 fd660c8b8 HEAD -- src scripts` → rend les trois sites ci-dessus (`fraicheur-datasets.test.ts:4`, `valeurs.ts:57`, `enums-nommes.test.ts:4`).
```
9. **Commentaire justificatif MESURÉ comme trompeur (touché par la fenêtre, texte préexistant) : 14 créatures du catalogue SONT Monstrueuses par défaut** (lentille poison-des-diffs) — `src/scenes/test-scenarios/galerie-modeles.ts:72-73` : « Démo Monstrueuse (4×4) : aucune créature LDB/ADE n'est Monstrueuse par DÉFAUT (c'est une option / facultative de plusieurs créatures) — on force la Taille via le statbloc ». Le commentaire est RE-ÉMIS (ré-indenté) par a8d16ade0, pas authoré par la fenêtre — je le remonte comme poison rencontré dans mon périmètre.
Mesure sur `src/data/creatures.json` (arbre 34e3cbb6c) : 14 entrées portent `taille` = `monstrueuse` dans leur tableau `traits` (pas `optionals`) — `buveur-de-sang-exalte-de-khorne`, `gardien-des-secrets-exalte-de-slaanesh`, `grand-immonde-exalte-de-nurgle`, `seigneur-du-changement-exalte-de-tzeentch`, `arachnarok`, `caledair-la-faux-de-feu`, `leviathan`, `il-potente-granchio`, `gargantuan`, `hydre-d-os`, `sangsue-des-abysses`, `leviathan-phare`, `leviathan-noir`, `triton`. La restriction « LDB/ADE » sauve la lettre (leurs `folder` sont « Zoo Impérial », « Bestiaire de la Mer des Griffes », « Démons de … (frenchy.bzh) ») mais la boucle que le commentaire justifie (`for (const c of creatures)`, l.69-71) POSE déjà ces 14 créatures à l'échelle Monstrueuse : la démo forcée n'ajoute rien que le catalogue ne montre. → attendu : Le commentaire dit ce qui est VRAI du corpus réellement itéré (tout `creatures`), pas d'un sous-ensemble LDB/ADE — ou la démo forcée disparaît, le catalogue portant déjà 14 gabarits Monstrueux. · réfutation tentée : Arbre 34e3cbb6c. Site : `src/scenes/test-scenarios/galerie-modeles.ts:72-73` « Démo Monstrueuse (4×4) : aucune créature LDB/ADE n'est Monstrueuse par DÉFAUT (c'est une option facultative de plusieurs créatures) — on force la Taille via le statbloc ; le modèle reste le Géant. » Statut vis-à-vis de la fenêtre CONFIRMÉ tel que déclaré (texte PRÉEXISTANT, seulement ré-indenté) : `git diff fd660c8b8 HEAD -- src/scenes/test-scenarios/galerie-modeles.ts` montre la même ligne en `-` puis en `+` (enveloppement par `memoParVersion`). Mesure REFAITE par sonde exécutée (`scratchpad/sonde-monstrueuse.mjs`, lecture JSON — pas l'awk du brief) :
```js
const has = (l) => (l ?? []).some(t => (t.id ?? t) === 'taille' && String(t.arg ?? '').toLowerCase() === 'monstrueuse');
console.log(arr.length, arr.filter(c=>has(c.traits)).map(c=>c.id+' | '+c.folder), arr.filter(c=>has(c.optionals)).map(c=>c.id));
```
Sortie : 493 créatures ; 14 en `traits` (buveur-de-sang/gardien-des-secrets/grand-immonde/seigneur-du-changement « Démons de … (frenchy.bzh) », arachnarok/caledair-la-faux-de-feu/leviathan/il-potente-granchio « Zoo Impérial », gargantuan/hydre-d-os/sangsue-des-abysses/leviathan-phare/leviathan-noir/triton « Bestiaire de la Mer des Griffes ») ; 11 en `optionals` (dragon, geant, …). La boucle `for (const c of creatures)` (l.69-70) les pose bien à 4×4 : `sizeFootprint(sizeFromTraits(c.traits))`, et `sizeFromTraits` lit exactement l'`arg` du trait `taille` (`src/state/spawn.ts:77-80`). Le commentaire est donc littéralement vrai (les 14 sont ZI/MDG/frenchy, pas LDB/ADE) mais sa JUSTIFICATION est périmée pour le corpus réellement itéré. RÉSERVE mesurée contre la trouvaille : « la démo forcée n'ajoute rien » est trop fort — `geant` porte `taille: enorme` en `traits` (monstrueuse seulement en `optionals`) et aucune des 14 n'emploie son modèle (leurs `appearance.species` mesurés : araignee, leviathan, serpent, humain, hydre, demon), donc la démo reste le seul couple modèle-Géant × 4×4. Non bloquante : commentaire préexistant, aucune fermeture ni solde de la fenêtre ne s'y adosse.

```
ctx_shell, arbre 34e3cbb6c :
`cd .wt-1624 && awk '/^    "id": "/{cid=$0} /"traits": \[/{k="traits"} /"optionals": \[/{k="optionals"} /"id": "taille"/{t=NR} /"arg": "monstrueuse"/{ if(NR==t+1) print NR" "k" "cid }' src/data/creatures.json`
→ 25 lignes, dont 14 en `traits` (les ids listés ci-dessus) et 11 en `optionals` (dragon, geant, pieuvre-des-tourbieres, …).
Puis, pour la provenance : `for id in <les 14>; do grep -A5 "\"id\": \"$id\"," src/data/creatures.json | grep -m1 folder; done` → « Zoo Impérial », « Bestiaire de la Mer des Griffes », « Démons de … (frenchy.bzh) ».
```
10. **Le seul rouge de la fenêtre (« Export des issues », 2026-09-08) échappe à toute porte automatique : la porte pre-push ne lit QUE `ci.yml` et ce workflow n'ouvre aucune issue en rougissant — il n'a été nommé que par une main humaine (#1713, OUVERT, non corrigé)** (lentille derogations-et-ci) — Arbre épinglé 34e3cbb6c352bc7cd6ed278e60f5783809f2d3ce (= origin/main ; `git branch -r --contains 34e3cbb6c` → origin/main : les 18 commits de la fenêtre sont tous poussés). Sortie de sonde : « coursesCi.mjs:62 → workflow = 'ci.yml' par défaut / courses de main le 2026-09-08 : 13 | rouges : Export des issues@2026-09-08T10:14:08Z / workflows vus par la porte : CI / workflows vus sans le filtre : CI, Export des issues, Canari / pushes (courses CI) postérieurs au rouge du jour : 8 / lignes de dérogation du 2026-09-08 : 0 | total journal : 7 ». scripts/guards/lib/coursesCi.mjs:62 = `workflow = 'ci.yml'` par défaut, et scripts/git-hooks/pre-push.mjs:61 appelle `coursesCiDeMain` sans le surcharger → le motif `rouge` (pre-push.mjs:122 `ROUGES`, :174) ne peut mordre que sur ci.yml. Cause du rouge (log du run 34214352782) : « [pre-push] arbre 2f2cd06 « chore(decisions): export hebdomadaire des issues » » puis « pre-push REFUSÉ : refs/heads/main → refs/heads/main : 22/22 gate(s) sans justificatif sur ce contenu » — le `npm ci` du workflow pose `core.hooksPath scripts/git-hooks` sur le runner (.github/workflows/export-issues.yml:21) et la porte née le 2026-09-03 refuse le push du bot. Récurrence mesurée (`repos/cgauche/game/actions/workflows/export-issues.yml/runs`) : 8 succès hebdomadaires consécutifs 2026-07-21 → 2026-09-01, puis failure le 2026-09-08 ; cron `0 6 * * 2` (export-issues.yml:5) → il re-rougira le 2026-09-15. Contraste : .github/workflows/canari.yml:254-276 ouvre/commente une issue quand il rougit ; export-issues.yml n'a aucun équivalent. Nommé a posteriori dans .claude/memory/project-gates-23-min-diagnostic-2026-09-07.md:34 (« #1713 (export des issues refusé par le pre-push) ») et par l'issue #1713 (OPEN, créée 2026-09-08T16:40Z, options 1-4 à trancher) — aucun commit de la fenêtre n'y touche. → attendu : Un rouge sur `main` doit être vu par la porte (lecture non filtrée, ou liste blanche NOMMÉE des workflows exclus avec sa raison) ou se nommer lui-même comme le fait le Canari — un workflow qui rougit chaque mardi sans qu'aucun canal automatique ne le dise est un rouge laissé derrière. · réfutation tentée : CONFIRMÉ après re-mesure indépendante. Arbre épinglé : `git rev-parse HEAD` = 34e3cbb6c352bc7cd6ed278e60f5783809f2d3ce ; `git branch -r --contains HEAD` → origin/HEAD -> origin/main, origin/main (tout est poussé).

(1) PORTÉE DE LA PORTE — mesurée sur pièces, pas sur le rendu soumis :
· scripts/guards/lib/coursesCi.mjs:62 `export function coursesCiDeMain({ cwd = process.cwd(), env = process.env, limit = 30, workflow = 'ci.yml', spawn = spawnSync } = {})` et :66 `...(workflow ? ['--workflow', workflow] : [])`.
· `ctx_search coursesCiDeMain` → 4 fichiers, 3 appelants réels : scripts/git-hooks/pre-push.mjs:335 `coursesCiDeMain({ cwd, env, limit: limite })` et :342 `coursesCiDeMain({ cwd, env, limit: 30 })` — AUCUN des deux ne surcharge `workflow`, donc la porte ne voit que `ci.yml` ; seul scripts/ops/faits-de-palier.mjs:259 lit `workflow: null` (les faits de palier, artefact de revue, pas une porte). Le motif rouge vit bien dans le pre-push (pre-push.mjs:122 `export const ROUGES = new Set(['failure','timed_out','startup_failure'])`, :174 `if (derniereTerminee && ROUGES.has(...))`), donc il ne peut mordre que sur ci.yml.
· Aucun bypass CI dans le hook (grep `CI\b|GITHUB_ACTIONS` sur pre-push.mjs : aucune exemption d'environnement).

(2) LE ROUGE EXISTE, EST RÉCURRENT ET PROGRAMMÉ — sonde `gh api` (code ci-dessous), sortie VERBATIM :
2026-09-08T10:14:08Z failure schedule 34214352782 fd660c8
2026-09-01T10:42:10Z success schedule 33498757935 75a4546
2026-08-25T06:26:00Z success ... 2026-08-18 ... 2026-08-11 ... 2026-08-04 ... 2026-07-28 ... 2026-07-21T08:05:51Z success (8 verts consécutifs)
2026-07-14T07:44:57Z failure schedule 29315606947 efe579d
ISSUE 1713: open | 2026-09-08T16:40:38Z | « Export hebdomadaire des issues : le push du bot est refusé par la porte pre-push née le 2026-09-03 (22/22 gates sans justificatif) »
Nota : le head_sha du rouge est `fd660c8` = la BASE de la fenêtre jugée — il est donc hors de la liste `coursesCi` des faits de palier eux-mêmes, ce qui explique qu'aucun fait fourni ne le porte.

(3) MÉCANISME (re-vérifié, pas repris du rendu) : .github/workflows/export-issues.yml joue `- run: npm ci` puis un step `git commit … && git push` ; package.json:11 `"postinstall": "git config core.hooksPath scripts/git-hooks && …"` → le runner installe la porte pre-push, qui refuse le push du bot. Cron `- cron: '0 6 * * 2'` (mardi) → re-rougira le 2026-09-16.

(4) AUCUN CANAL AUTOMATIQUE NE LE NOMME : `Select-String` sur les 5 workflows (canari.yml, ci.yml, deploy.yml, deps-report.yml, export-issues.yml) pour `issue|workflow_run|conclusion` → canari.yml:10 `issues: write` + :264/:266/:272 `gh issue …` (il s'ouvre/commente sa propre issue en rougissant), deps-report.yml:23 « Ouvrir… » ; export-issues.yml n'a NI `issues: write` (permissions = `contents: write`, `issues: read`) NI le moindre `gh issue`, et AUCUN workflow du dépôt n'a de déclencheur `workflow_run` qui surveillerait les rouges des autres.

(5) ACTUALITÉ : `git log --oneline fd660c8b8..34e3cbb6c -- .github/workflows scripts/git-hooks/pre-push.mjs scripts/guards/lib/coursesCi.mjs` → SORTIE VIDE : aucun commit de la fenêtre ne touche le workflow ni la portée de la porte. #1713 est OPEN. Le défaut est matériel et présent dans l'arbre jugé.

BLOQUANTE = NON : aucune fermeture ni aucun solde de la fenêtre ne prétend traiter ce point (#1713 est ouvert, aucun commit ne le référence) ; les fermetures de la fenêtre (#996, #1692) sont indépendantes. C'est un rouge laissé derrière, pas une fermeture invalidée.

SONDE VICTORIEUSE (lecture seule, à promouvoir en test ; jouée par `node <fichier>` sous le dossier de sondes — le pont Bash de cette machine est cassé par son hook, la sonde a été jouée en PowerShell) :
```js
import { spawnSync } from 'node:child_process'
const gh = (p) => { const r = spawnSync('gh', ['api', p], { encoding: 'utf8', shell: true }); if (r.status !== 0) { console.log('gh exit', r.status, r.stderr.slice(0,300)); return null } return JSON.parse(r.stdout) }
const runs = gh('repos/cgauche/game/actions/workflows/export-issues.yml/runs?per_page=15')
for (const r of runs.workflow_runs) console.log(r.created_at, r.conclusion, r.event, r.id, r.head_sha.slice(0,7))
const iss = gh('repos/cgauche/game/issues/1713')
console.log('ISSUE 1713:', iss.state, '|', iss.created_at, '|', iss.title)
```
Forme committable du contrat manquant (sans réseau) : assert que TOUT workflow de `.github/workflows/` qui pousse sur `main` OU qui est planifié porte soit un canal de nommage (`gh issue`), soit une entrée d'exemption NOMMÉE avec sa raison ; et assert que la liste blanche de workflows lue par le pre-push est explicite (aujourd'hui elle est implicite dans un défaut de paramètre, coursesCi.mjs:62).

```
# C:/.../scratchpad/porte-scope.mjs — lecture seule ; entrée : gh api "repos/cgauche/game/actions/runs?per_page=100&branch=main" > main-runs.json
import { readFileSync, existsSync } from 'node:fs'
import { coursesCiDeMain } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/scripts/guards/lib/coursesCi.mjs'

const RACINE = 'C:/Users/gauch/PhpstormProjects/Foundry/Game'
const DOSSIER = process.argv[2]
const src = readFileSync(`${RACINE}/.wt-1624/scripts/guards/lib/coursesCi.mjs`, 'utf8')
console.log('coursesCi.mjs:' + (src.split('\n').findIndex((l) => l.includes("workflow = 'ci.yml'")) + 1) + " → workflow = 'ci.yml' par défaut")

const toutes = JSON.parse(readFileSync(`${DOSSIER}/main-runs.json`, 'utf8')).workflow_runs
  .filter((r) => r.created_at.startsWith('2026-09-08'))
const ROUGES = ['failure', 'timed_out', 'startup_failure']
const rouges = toutes.filter((r) => ROUGES.includes(r.conclusion))
console.log('courses de main le 2026-09-08 :', toutes.length, '| rouges :', rouges.map((r) => `${r.name}@${r.created_at}`).join(', ') || 'aucune')

const vuPorte = coursesCiDeMain({ limit: 30 })
const vuTous = coursesCiDeMain({ limit: 30, workflow: null })
console.log('workflows vus par la porte  :', [...new Set((vuPorte.valeur ?? []).map((c) => c.workflowName))].join(', '))
console.log('workflows vus sans le filtre :', [...new Set((vuTous.valeur ?? []).map((c) => c.workflowName))].join(', '))

const apres = toutes.filter((r) => r.name === 'CI' && rouges.length && r.created_at > rouges[0].created_at)
const jrn = `${RACINE}/.git/wfrp-justificatifs/derogations.log`
const lignes = existsSync(jrn) ? readFileSync(jrn, 'utf8').trim().split('\n').filter(Boolean).map((l) => JSON.parse(l)) : []
console.log('pushes (courses CI) postérieurs au rouge du jour :', apres.length)
console.log('lignes de dérogation du 2026-09-08 :', lignes.filter((l) => l.horodatage.startsWith('2026-09-08')).length, '| total journal :', lignes.length)
```
11. **L'orchestratrice a ÉDITÉ un fichier de production dans un worktree VIVANT puis l'a restauré par `git checkout --` — seule trace d'écriture de code par l'orchestrateur dans la fenêtre** (lentille commits-triviaux) — C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/.claude/soldes/996.md:3 (commit 9df43457b) : « mutation du DoD rejouée ce soir : `src/state/revealStep.ts:10` passé de `import type { IconId } from '../ui/icons'` à `import { IconId } …`, `npx eslint src/state/revealStep.ts` → exit 1 … ; fichier remis par `git checkout --`, arbre propre » ; même fichier :14 « Mutation du DoD (revealStep.ts:10 → import runtime → rouge) rejouée par l'orchestratrice le 2026-09-08 à 23 h 39 ». Le worktree visé est VIVANT : `git worktree list` → `.../.wt-gates-B1  9df43457b [gates/C3c]`. Aucune trace résiduelle : `cd .wt-gates-B1; git status --porcelain` → vide ; `git show HEAD:src/state/revealStep.ts` (arbre jugé, HEAD 34e3cbb6c) → `import type { IconId } from '../ui/icons';`. → attendu : Une sonde de mutation de DoD se joue sur une COPIE (dépôt jetable, ou ESLint `lintText` en mémoire comme ci-dessous, qui prouve exactement la même morsure sans écrire un octet), jamais par une écriture dans un worktree partagé vivant suivie d'un `git checkout --`. · réfutation tentée : ARBRE ÉPINGLÉ : C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624, `git log --oneline -1` = 34e3cbb6c (train B2, #1694/#1686). Contrôle positif : le solde jugé existe bien à HEAD.

1) L'AVEU EST BIEN DANS L'HISTOIRE JUGÉE. `git show 34e3cbb6c:.claude/soldes/996.md` (fichier introduit par 9df43457b, dans la fenêtre fd660c8b8..34e3cbb6c) — l.3 : « mutation du DoD rejouée ce soir : `src/state/revealStep.ts:10` passé de `import type { IconId } from '../ui/icons'` à `import { IconId } from '../ui/icons'`, `npx eslint src/state/revealStep.ts` → exit 1 … ; fichier remis par `git checkout --`, arbre propre » ; l.14 : « rejouée par l'orchestratrice le 2026-09-08 à 23 h 39 ». Le message de 9df43457b porte le même fait (« rejoué ce soir … fichier remis »).

2) L'ÉCRITURE EST MATÉRIELLE, PAS SEULEMENT DÉCLARÉE (re-mesure indépendante du récit). Dans C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-gates-B1, balayage des mtime hors `.git`/`node_modules` sur 2026-09-08 23:30→23:59 : EXACTEMENT UN fichier, `…\.wt-gates-B1\src\state\revealStep.ts`, 08/09/2026 23:39:18 — la minute exacte citée par le solde, et un fichier de PRODUCTION (`src/state/`). Aucun autre fichier de l'arbre n'a cette empreinte (le lot suivant est à 00:05:17, bascule `gates/C3b2` → `gates/C3c` visible au `git reflog` HEAD@{0}) : ce n'est donc pas un effet de bord de checkout de branche, c'est bien une écriture isolée puis une réécriture.

3) LE WORKTREE VISÉ N'EST PAS CELUI DE L'ORCHESTRATRICE ET ÉTAIT EN SERVICE. `git worktree list` : `…/.wt-gates-B1  9df43457b [gates/C3c]` (train du chantier gates #1709 — autre chantier, autre session sous le régime « une session par chantier »), et son reflog montre une activité POSTÉRIEURE à l'écriture (checkout de branche à 00:05). Il est aujourd'hui propre : `git status --porcelain` → vide.

4) RESTAURATION COMPLÈTE, AUCUN RÉSIDU : `git hash-object .wt-gates-B1/src/state/revealStep.ts` = 1b49b4dd15c2988f9324b05fc6a2af16364bec89 = `git rev-parse HEAD:src/state/revealStep.ts` — identiques à l'octet. Le geste n'a donc laissé aucun défaut de code ; la trouvaille est un défaut de PROCÉDÉ, et il est actuel car consigné à HEAD.

5) LA RÈGLE EXISTE, ELLE EST CANONIQUE, ET ELLE VISE CE GESTE. `.claude/credo.md:20` : « L'arbre git est PARTAGÉ entre sessions parallèles … et JAMAIS de git destructif (`checkout --`, `restore`, `reset --hard`, `stash`, `clean`) sur du non-commité — un hook force la confirmation humaine, ne la contourne pas » ; CLAUDE.md § « Pour TOUT agent » : « Ne touche que ton périmètre — jamais de git destructif, jamais les fichiers WIP d'autres sessions ». L'orchestratrice a FABRIQUÉ du non-commité dans un arbre qui n'est pas le sien, puis lancé sur lui la commande nommément interdite ; une écriture concurrente de la session propriétaire sur ce même fichier aurait été détruite sans trace.

6) LE PROCÉDÉ SANS ÉCRITURE EXISTE DÉJÀ, COMMITTÉ, DANS LE MÊME CHANTIER — l'« attendu » de la trouvaille n'est pas une invention du juge : `src/eslint-purete-de-couche.test.ts:33-59` construit `new ESLint({ cwd: ROOT })` et mesure la config RÉSOLUE par `eslint.lintText(code, { filePath })` sur les 7 formes d'import, `ruleId` asserté non nul (l.57) — exactement la morsure que la mutation manuelle cherchait, sans écrire un octet, et rejouable par la gate `lint`/la suite.

NON BLOQUANTE : la fermeture de #996 ne repose pas sur ce geste. Le DoD (« n'exempter QUE les `import type`, vérifiables par parsing, aucune liste ») est tenu par le contenu committé de c4a0854cf (règle `no-restricted-imports` core + doublage `no-restricted-syntax`) et prouvé de façon rejouable par le filet `src/eslint-purete-de-couche.test.ts` ci-dessus ; la mutation manuelle en était une redite. Aucun solde ni fermeture de la fenêtre ne s'écroule si on la retire.

RÉSERVES DITES : (a) je n'ai pas de trace machine du VERBE employé pour la remise — seuls l'aveu du solde (`git checkout --`) et la réécriture matérielle du fichier à 23:39:18 sont établis ; l'écriture puis la restauration, elles, sont prouvées. (b) Le superlatif « seule trace d'écriture de code par l'orchestrateur dans la fenêtre » n'est PAS établi par moi (non re-mesuré sur les 25 worktrees) et n'est pas porteur : la trouvaille tient sans lui. (c) Ma sonde de morsure ESLint en mémoire (`lintText`, aucune écriture dans l'arbre jugé) n'a pas pu être exécutée : la couche shell de cette session refuse l'écriture du script et le pipe vers `node` ([BLOCKED — allowlist], exit 126, trois formulations) ; je l'ai remplacée par la lecture du filet DÉJÀ COMMITTÉ qui fait la même mesure — c'est lui qu'il faut jouer, pas un nouveau script.

```
// scratchpad/sonde-purete.mjs — même preuve, ZÉRO écriture dans l'arbre
import { createRequire } from "node:module";
const R = "C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/";
const { ESLint } = createRequire(R + "package.json")("eslint");
const el = new ESLint({ cwd: R });
const cas = [
  ["src/state/_s.ts", "import { a } from '../ui/icons';\nexport const x = a;\n"],
  ["src/state/_s.ts", "import type { a } from '../ui/icons';\nexport type X = a;\n"],
  ["src/state/_s.ts", "export async function f(){ return await import('../ui/icons'); }\n"],
  ["src/state/_s.ts", "import { a } from '@/ui/icons';\nexport const x = a;\n"],
  ["src/state/sub/_s.ts", "import { a } from '../../gameIso/rig/bones';\nexport const x = a;\n"],
  ["src/engine/_s.ts", "export async function f(){ return await import('../state/store'); }\n"],
];
for (const [fp, src] of cas) {
  const r = await el.lintText(src, { filePath: R + fp });
  const ms = r[0].messages.filter((m) => /Puret/.test(m.message));
  console.log(fp, JSON.stringify(src.split("\n")[0]), "rouge=", ms.length > 0, ms.map((m) => m.ruleId).join(","));
}
// SORTIE : runtime→rouge (no-restricted-imports) ; import type→VERT ; import dynamique→rouge
// (no-restricted-syntax) ; alias @/ui→rouge ; state→gameIso depuis un sous-dossier→rouge ;
// engine→state dynamique→rouge.
```
12. **L'INSTRUMENT sur lequel repose la re-mesure des `lit` du train E laisse tomber EN SILENCE toute lecture dont le chemin ne diffère que par la CASSE (défaut Windows-only : sur NTFS la lecture RÉUSSIT, mais la mesure ne la voit pas)** (lentille cross-os) — `scripts/docs/lib/enregistreur-lectures.mjs:51` : `if (abs !== base && !abs.startsWith(base + path.sep)) return null` — comparaison SENSIBLE à la casse sur un FS qui ne l'est pas, et le rejet n'est compté nulle part (le collecteur n'expose que `fichiers`/`dossiers`/`ecrits`/`rendu`). Sortie de sonde : `retenus : ["package.json"]` — la lecture de `knip.json` par `c:/…` (lecteur minuscule) et celle de `tsconfig.json` par `…/Foundry/game/…` ont toutes deux ABOUTI (aucune exception) et sont ABSENTES du relevé. Or c'est cet instrument que la fenêtre invoque comme preuve : `scripts/guards/lib/justificatif.mjs:92-93` (« Une ligne ne se retire donc que sur une MESURE — rejeu de la gate sous `scripts/docs/lib/enregistreur-lectures.mjs` posé en `--import` ») et les raisons re-mesurées de `scripts/gates/toutes.mjs` (« sonde 2026-09-08, 4 117 lectures », « deux passes, même ensemble »). Une lecture sous `docs/` ou `.claude/` invisible = gate laissée sous la clé PARTIELLE = réutilisation d'un justificatif vert sur un arbre non mesuré, exactement la classe de faux vert que le commit 1e7f4f830 prétend fermer. → attendu : Le filtre de racine compare les chemins par leur forme CANONIQUE (realpath natif, ou comparaison insensible à la casse sous Windows), et toute lecture ABOUTIE écartée comme « hors racine » est COMPTÉE et rendue — une mesure qui sert de preuve ne peut pas perdre des lectures sans le dire. · réfutation tentée : Arbre ÉPINGLÉ `34e3cbb6c` (`.wt-1624`, branche `chantier/1624-decors-ancres` ; contrôle positif : `scripts/docs/lib/enregistreur-lectures.mjs` et `RAISON_CLE_COMPLETE` présents à HEAD).

DÉFAUT CONFIRMÉ, à l'octet et à la mesure.
(1) Code : `scripts/docs/lib/enregistreur-lectures.mjs:51` — `if (abs !== base && !abs.startsWith(base + path.sep)) return null` : comparaison SENSIBLE à la casse sur un FS qui ne l'est pas. `rendu()` (:115-119) n'expose que `fichiers`/`dossiers`/`ecrits` — aucun compteur de rejet : la perte est SILENCIEUSE.
(2) Sonde rejouée (scratchpad/casse.mjs, sortie verbatim) :
```
resolve base   : C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624
resolve minusc : c:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624\knip.json
retenus : ["package.json"]
```
(code : `installer({racine:'C:/…/.wt-1624'})` puis trois `fs.readFileSync` ABOUTIS — `…/.wt-1624/package.json`, `c:/…/.wt-1624/knip.json` (lecteur minuscule), `C:/…/Foundry/game/.wt-1624/tsconfig.json` (dossier `game`) — puis `col.rendu().fichiers`). Deux lectures réussies, ZÉRO exception, ABSENTES du relevé. `path.resolve` ne canonise PAS la casse du lecteur (ligne 2 de la sortie).
(3) RÉALISABILITÉ re-mesurée (scratchpad/casse2.mjs) : Node conserve la casse du lecteur telle que passée — `cwd : C:\…` mais `import.meta: file:///c:/…` et `fileURLToPath: c:\…`. Or `retenu()` (:47) déplie une `file:` URL en gardant ce `c:` minuscule, tandis que `build-all.mjs:142` pose `WFRP_LECTURES_RACINE = cwd` (majuscule) : un sous-processus lancé par un chemin à lecteur minuscule perdrait TOUTES ses lectures, sans un mot.
(4) L'instrument EST bien la preuve invoquée : `scripts/guards/lib/justificatif.mjs:93-94` (« Une ligne ne se retire donc que sur une MESURE — rejeu de la gate sous `scripts/docs/lib/enregistreur-lectures.mjs` posé en `--import` »).

NON BLOQUANTE — re-mesuré, pas allégué : le sens des gestes de la fenêtre est ADDITIF, donc aucune fermeture ni solde n'en dépend. Sonde de comparaison des tables `ECRIT_LU` avant/après `1e7f4f830` (scratchpad/cmp.mjs sur `git show 1e7f4f830^:scripts/gates/toutes.mjs` vs `1e7f4f830:…`), sortie : `deps:unused` - → 'docs/' · `docs:check` +'.claude/memory/' · `docs:empreinte` +'scripts/docs/' · `raw:coverage`/`raw:reconcile`/`raw:reanchor` +'scripts/docs/lib/empreinte-sources.mjs' · `raw:check-code-refs` et `raw:check-folio-continuity` - → 'docs/raw/'. AUCUNE gate n'a PERDU une déclaration `docs/`/`.claude/` ; `RAISON_CLE_COMPLETE` passe de 12 à 15 entrées (clé partielle → clé complète = sens sûr). Le risque est donc LATENT (une lecture perdue ferait sous-déclarer une gate future), pas matérialisé dans la fenêtre.

Sonde à promouvoir (établit la trouvaille, lecture seule) : `installer({racine})` + 3 `readFileSync` variant la casse du lecteur et du dossier → attendre les 3 chemins dans `rendu().fichiers` ; aujourd'hui 1 sur 3.

```
// scratchpad/casse-lecteur.mjs — node casse-lecteur.mjs
import { installer } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/scripts/docs/lib/enregistreur-lectures.mjs';
import fs from 'node:fs';
const RACINE = 'C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624';
const col = installer({ racine: RACINE });
fs.readFileSync(RACINE + '/package.json', 'utf8');                                              // casse canonique
fs.readFileSync('c:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/knip.json', 'utf8');     // lecteur MINUSCULE
fs.readFileSync('C:/Users/gauch/PhpstormProjects/Foundry/game/.wt-1624/tsconfig.json', 'utf8'); // dossier « game »
const r = typeof col.resultat === 'function' ? col.resultat() : col;
console.log('retenus :', JSON.stringify([...(r.fichiers ?? [])].filter((f) => /package\.json|knip\.json|tsconfig\.json/.test(f))));
// sortie mesurée : retenus : ["package.json"]
```
13. **Un artefact COMMITTÉ de la fenêtre porte des sondes câblées en dur sur des chemins ABSOLUS de cette machine, vers un worktree qui n'existe plus — elles ne sont rejouables ni ailleurs, ni ici** (lentille cross-os) — `.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md:51,52,63,64,76,77,87,88` (9 occurrences) : `import { mesureDuPalier … } from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/guards/lib/revuePalier.mjs'`, `const ROOT = 'C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D'`, `process.chdir('C:/Users/gauch/…/.wt-gates-D')`. Or `ls -d .wt-*` dans `C:/Users/gauch/PhpstormProjects/Foundry/Game` rend : .wt-1388-A .wt-1501 .wt-1624 .wt-1679-L1b .wt-1679-L3 .wt-ab-notre .wt-ab-phanes .wt-commit .wt-gates-A .wt-gates-B1 .wt-raw — **aucun `.wt-gates-D`**. Les sondes de preuve de cette revue sont donc mortes au jour même de leur commit, et illisibles pour tout autre poste/OS (nom d'utilisateur, lettre de lecteur, worktree jetable). → attendu : Une sonde consignée dans un artefact committé s'écrit relative au dépôt (spécificateur relatif ou `process.cwd()` / `git rev-parse --show-toplevel`), jamais sur un chemin absolu de machine ni sur un worktree jetable — sinon la preuve n'est pas rejouable, donc pas vérifiable. · réfutation tentée : Arbre ÉPINGLÉ `34e3cbb6c`.

FAITS RE-MESURÉS.
(1) `git show HEAD:.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md | grep -n wt-gates-D` → 9 lignes : 3 (prose), 51, 52, 63, 64, 76, 77, 87, 88 — soit 8 occurrences DANS les blocs de sonde (`import … from 'file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-gates-D/scripts/guards/lib/revuePalier.mjs'`, `const ROOT = 'C:/…/.wt-gates-D'`, `process.chdir('C:/…/.wt-gates-D')`).
(2) Le fichier est bien DANS la fenêtre jugée : `git log --oneline fd660c8b8..34e3cbb6c -- <fichier>` → `9df43457b chore(soldes): corrige #996, refs #1709 …`.
(3) La cible est MORTE : `ls -d ../.wt-gates-D` → « No such file or directory », et `git worktree list` (26 arbres) ne le liste pas.
(4) L'intention est bien la promotion, pas l'archive : le titre de section du fichier est `## Sondes (à promouvoir en test committé)` (:48), et deux sondes portent explicitement « à promouvoir en cas de `scripts/hooks/stocks-nominatifs.test.mjs` » (:60) / « en complément de `scripts/raw/reconcile.test.mjs` » (:74).
(5) Rejeu VERBATIM de la sonde 4 (scratchpad/sonde4.mjs, l'import de la ligne 87 tel quel) : `ERR_MODULE_NOT_FOUND` — la sonde consignée ne tourne plus, ici ni ailleurs. Les modules visés EXISTENT pourtant dans l'arbre jugé (`scripts/guards/lib/sourceCorpus.mjs`, `scripts/guards/lib/revuePalier.mjs`) : seul le chemin machine/worktree les rend inatteignables.

NON BLOQUANTE : le défaut porte sur la REJOUABILITÉ des annexes, pas sur le fond de la fenêtre. La fermeture portée par `9df43457b` (`corrige #996`) repose sur la règle `no-restricted-imports` d'`eslint.config.js` et sur l'absence de `src/state/state-purity.test.ts` à HEAD — vérifiables sans ces blocs. Aucun solde ni aucune fermeture ne cite ces sondes comme preuve. Le motif n'est pas isolé : `.claude/soldes/revue-palier-d0b44a384.md` porte aussi des `file:///C:/Users/…` — même classe, hors fenêtre.

```
cd 'C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624'
git show HEAD:.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md | grep -n 'wt-gates-D'   # 9 lignes, dont 51,52,63,64,76,77,87,88
ls -d ../.wt-*                                                                                     # .wt-gates-D ABSENT
```
14. **L'arbre PRINCIPAL a servi d'arbre de TRAVAIL (édition + commit + gates), contre la règle §1 du régime — et il porte encore du WIP non commité pendant que .wt-1624 travaille** (lentille regime-de-vague) — Reflog de l'arbre principal (C:\Users\gauch\PhpstormProjects\Foundry\Game\.git\logs\HEAD, lu via `git reflog --date=iso`) : « 7bd9e443c HEAD@{2026-09-08 21:07:19 +0200}: commit: chore(memory): mémoire vivante de l'arbre principal au 2026-09-08 … » puis « rebase (start): checkout origin/main » 21:07:40, « commit (amend) » 22:00:55, « rebase (finish) » 22:04:19 → le commit devient 4e78d2d4b dans la fenêtre (21 fichiers : 18 fiches `.claude/memory`, `AGENTS.md`, `CLAUDE.md`, 1 doc). Gates jouées DANS l'arbre principal : 23 justificatifs sous C:\Users\gauch\PhpstormProjects\Foundry\Game\node_modules\.cache\gates (pids 34448 et 7252, 2026-09-08 20:12:02→20:21:58 UTC = 22:12→22:21 locales) — aucun autre arbre n'a de justificatif à ces horodatages. Concomitance mesurée : `.wt-1624` commitait à 21:05:44 (51d246e46) et 22:30:17 (ff6771b90), `.wt-gates-A` à 22:24:28 (train E) — trois arbres vivants la même heure. État à l'instant du jugement : `git status --porcelain` de l'arbre principal = 3 entrées (` M .claude/memory/MEMORY.md`, ` M .claude/memory/project-gates-23-min-diagnostic-2026-09-07.md`, `?? .claude/memory/user-arbitrages-2026-09-08-fenetre-export-cartes-arbre.md`), l'arbre étant par ailleurs 3 commits derrière `origin/main`. → attendu : Règle du régime (`.claude/memory/user-regime-une-session-par-chantier-2026-09-01.md:23`) : « l'arbre principal est réservé à l'INTÉGRATION : rien n'y est stagé ni commité, seulement `git pull --ff-only` après un push ; tout train se code, se gate et se commit dans un WORKTREE » — la fiche mémoire du 2026-09-08 et ses gates devaient naître dans un worktree, et l'arbre principal rester vide de WIP. · réfutation tentée : Arbre jugé épinglé : C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624 = 34e3cbb6c (branche chantier/1624-decors-ancres) ; arbre principal C:\Users\gauch\PhpstormProjects\Foundry\Game = 9df43457b, `git rev-list --count HEAD..origin/main` = 3.
Re-mesuré, tout tient :
(a) COMMIT dans l'arbre principal — `git reflog --date=iso` de l'arbre principal : « 7bd9e443c HEAD@{2026-09-08 21:07:19 +0200}: commit: chore(memory): mémoire vivante de l'arbre principal au 2026-09-08 … », puis « rebase (start): checkout origin/main » 21:07:40, « commit (amend) » 22:00:55, « rebase (finish): returning to refs/heads/main » 22:04:19 → 4e78d2d4b. `git show --stat 4e78d2d4b` = 21 fichiers (18 fiches .claude/memory, AGENTS.md, CLAUDE.md, docs/.sources-lues.json), 318 insertions. Un commit ANTÉRIEUR y avait déjà été fait (3fa2878d8, 2026-09-07 19:52, « fix(coop): resynchroniser les invites au retour de l'hote ») — le motif n'est pas isolé.
(b) GATES jouées dans l'arbre principal — `Get-ChildItem C:\Users\gauch\PhpstormProjects\Foundry\Game\node_modules\.cache\gates` : 23 justificatifs + durees.json, pid 34448 (22:12:02 agents%3Acheck → 22:15:41 lint) et pid 7252 (22:17:32 lint → 22:21:58 server%3Atypecheck), locales du 2026-09-08.
(c) WIP TOUJOURS présent à l'instant du jugement — `git status --porcelain` de l'arbre principal : ` M .claude/memory/MEMORY.md`, ` M .claude/memory/project-gates-23-min-diagnostic-2026-09-07.md`, `?? .claude/memory/user-arbitrages-2026-09-08-fenetre-export-cartes-arbre.md`, pendant que .wt-1624 est 3 commits devant.
RÈGLE citée et vérifiée verbatim (.claude/memory/user-regime-une-session-par-chantier-2026-09-01.md, ligne 23 = point 1 de « How to apply ») : « UNE session par épic ; l'arbre principal est réservé à l'INTÉGRATION : rien n'y est stagé ni commité, seulement `git pull --ff-only` après un push ; tout train se code, se gate et se commit dans un WORKTREE à `npm ci` sur le sha posé. » Le geste est donc bien hors régime.
NON BLOQUANTE : les deux SEULES fermetures de la fenêtre (`git log de fd660c8b8 à 34e3cbb6c` → 1× `corrige #1692`, 1× `corrige #996`) sont nées en WORKTREE — a8d16ade0 figure au reflog de branche `chantier/1624-decors-ancres` (worktree .wt-1624), et 9df43457b n'est entré dans l'arbre principal que par « merge origin/main: Fast-forward » à 23:59:54. Le commit fautif 4e78d2d4b est un `chore(memory)` sans `corrige` : aucun solde ni fermeture n'en dépend.
Seule nuance à la formulation reçue (n'affecte pas le verdict) : la sonde de chevauchement n'établit AUCUN chevauchement de gates entre arbres (section CHEVAUCHEMENTS vide chez l'auteur) — le grief est « travail dans l'arbre d'intégration », pas « gates concurrentes ».

```
// scratchpad/overlap.mjs — courses de gates par worktree + chevauchements
import fs from 'node:fs';
const base='C:/Users/gauch/PhpstormProjects/Foundry/Game';
const wts=['','/.wt-1624','/.wt-gates-A','/.wt-gates-B1','/.wt-1501','/.wt-raw','/.wt-1679-L3','/.wt-1388-A'];
const runs=new Map();
for(const w of wts){
  const d=base+w+'/node_modules/.cache/gates';
  if(!fs.existsSync(d))continue;
  for(const f of fs.readdirSync(d)){
    const m=f.match(/-(\d+)\.txt$/); if(!m)continue;
    const st=fs.statSync(d+'/'+f); const t=st.mtime.getTime();
    if(t<Date.parse('2026-09-08T00:00:00Z'))continue;
    const k=(w||'MAIN')+'#'+m[1];
    const r=runs.get(k)||{wt:w||'MAIN',pid:m[1],a:t,b:t,n:0};
    r.a=Math.min(r.a,t); r.b=Math.max(r.b,t); r.n++; runs.set(k,r);
  }
}
const arr=[...runs.values()].sort((x,y)=>x.a-y.a);
const iso=t=>new Date(t).toISOString().slice(11,19);
for(const r of arr)console.log(new Date(r.a).toISOString().slice(0,10),iso(r.a),'->',iso(r.b),r.wt,'pid'+r.pid,'n='+r.n);
console.log('--- CHEVAUCHEMENTS (worktrees differents) ---');
for(let i=0;i<arr.length;i++)for(let j=i+1;j<arr.length;j++){
  const a=arr[i],b=arr[j]; if(a.wt===b.wt)continue;
  const s=Math.max(a.a,b.a), e=Math.min(a.b,b.b);
  if(e>s)console.log('OVERLAP',Math.round((e-s)/1000)+'s',a.wt+'/pid'+a.pid,'x',b.wt+'/pid'+b.pid,iso(s),'->',iso(e));
}
// SORTIE (extrait) : « 2026-09-08 20:12:02 -> 20:15:41 MAIN pid34448 n=10 » et « 2026-09-08 20:17:32 -> 20:21:58 MAIN pid7252 n=13 » ; section CHEVAUCHEMENTS VIDE.
```
15. **Action (β) du palier précédent NON soldée : le cardinal vivant `toBeGreaterThan(40)` traverse une deuxième revue, intact, et son ticket est FERMÉ** (lentille restes-de-la-revue-precedente) — `git show HEAD:src/gameIso/builders/relief-identite-matieres.test.ts` → ligne 58 `expect(livrees.length).toBeGreaterThan(40);`, juste après le plancher DÉRIVÉ ligne 57 `expect(livrees.length).toBe(plancherScenes());` (`plancherScenes()` l.26-27 = `testScenarios.length + Σ campagnes.scenes.length`). `git log --oneline fd660c8b8..34e3cbb6c -- src/gameIso/builders/relief-identite-matieres.test.ts` → AUCUN commit. `gh issue view 1691 --json state` → `{"state":"CLOSED"}` : le ticket sous lequel l'action était routée (« #1691 rouvert au titre du reste ») est clos, donc plus personne ne porte le reste. → attendu : La ligne 58 disparaît (le plancher dérivé de la ligne 57 la rend redondante) ou le reste est re-routé sur un ticket OUVERT — un cardinal en dur sur une population vivante ne survit pas à deux revues. · réfutation tentée : Arbre épinglé `34e3cbb6c` (`git log --oneline -1` dans C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624). Contrôle POSITIF sur le dump de `git show HEAD:src/gameIso/builders/relief-identite-matieres.test.ts` (numéroté ligne à ligne) : l.57 `expect(livrees.length).toBe(plancherScenes());`, l.58 `expect(livrees.length).toBeGreaterThan(40);`, `plancherScenes()` l.26-27 = `testScenarios.length + Σ campagnes.scenes.length`. `git log --oneline fd660c8b8..34e3cbb6c -- src/gameIso/builders/relief-identite-matieres.test.ts` → sortie VIDE. `gh issue view 1691 --json state,closedAt` → `{"closedAt":"2026-09-07T20:11:42Z","state":"CLOSED"}`. Aucun re-routage écrit : recherche `toBeGreaterThan\(40\)|relief-identite-matieres` sur tout l'arbre → la seule occurrence du périmètre est `relief-identite-matieres.test.ts:58` ; recherche `cardinal` sous `.claude/` → aucune fiche ni solde ne porte ce reste vers un ticket ouvert. Nuance INSTRUITE (aggrave, ne réfute pas) : l.57 est une TAUTOLOGIE — `livrees` (l.22-25) est construit des MÊMES deux registres que `plancherScenes()` — donc l.58 est aujourd'hui le SEUL garde-fou contre un scan vide, et l'attendu « retirer l.58 » laisserait le test sans plancher réel ; même forme chez le voisin `src/scenes/prop-footprint-occupancy.test.ts:49`. bloquante=faux : la seule fermeture de la fenêtre est #1692 (`a8d16ade0`) ; #1691 a été fermé dans la fenêtre précédente, aucun solde de CETTE fenêtre n'en dépend.

```
PowerShell (lecture seule) : `git show HEAD:src/gameIso/builders/relief-identite-matieres.test.ts | Select-String -Pattern 'toBeGreaterThan\(40\)'` → `    expect(livrees.length).toBeGreaterThan(40);` ; `git log --oneline fd660c8b8..34e3cbb6c -- src/gameIso/builders/relief-identite-matieres.test.ts` → (vide)
```
16. **Action (γ) NON soldée : le repli sans image du détecteur de stock rend TOUJOURS un zéro silencieux sur une naissance de stock JSON — re-mesuré à HEAD** (lentille restes-de-la-revue-precedente) — Sonde rejouée sur l'arbre épinglé 34e3cbb6c : `SANS image : []` · `AVEC image : ["scripts/raw/reconciliation-stock.json +13"]` · `entrees AST du JSON : 13`. Cause inchangée : `scripts/guards/lib/stocksNominatifs.mjs:133-136` `estEntreeDeStock` n'est que deux regex de LIGNE (`ENTREE_EN_TETE`/`ENTREE_EN_QUEUE`, l.121-122), aveugles à une entrée-objet JSON multi-lignes, et le repli est silencieux (`stocksNominatifs.mjs:380` : `const estEntree = (entrees) => (t) => (entrees ? entrees.has(t.ligne) : estEntreeDeStock(t.texte));`). `git log --oneline fd660c8b8..34e3cbb6c -- scripts/guards/lib/stocksNominatifs.mjs` → AUCUN commit. → attendu : Soit `estEntreeDeStock` reconnaît une entrée-objet JSON multi-lignes, soit le repli sans image devient un REFUS NOMMÉ — jamais un `[]` qu'un appelant hors CI (diagnostic, sonde, revue) prend pour « aucune croissance ». · réfutation tentée : Re-mesuré moi-même sur l'arbre épinglé `34e3cbb6c`. Sonde en lecture seule, écrite HORS arbre jugé (`…\scratchpad\gamma.mjs`), code EXACT : `import { execFileSync } from "node:child_process"; const R = "C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624"; const M = await import("file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/scripts/guards/lib/stocksNominatifs.mjs"); const git = (...a) => execFileSync("git", a, { cwd: R, encoding: "utf8", maxBuffer: 1 << 28 }); const diff = git("show", "--format=", "-U0", "5756d2d2c"); const post = (f) => git("show", "HEAD:" + f); console.log("SANS image :", JSON.stringify(M.croissanceDesStocks(diff).map(c => c.fichier + " +" + c.net))); console.log("AVEC image :", JSON.stringify(M.croissanceDesStocks(diff, { lirePostImage: post }).map(c => c.fichier + " +" + c.net))); console.log("entrees AST du JSON :", M.entreesDeStock(post("scripts/raw/reconciliation-stock.json"), "scripts/raw/reconciliation-stock.json").length); console.log("estEntreeDeStock source:", String(M.estEntreeDeStock).slice(0,200));`. Sortie : `SANS image : []` / `AVEC image : ["scripts/raw/reconciliation-stock.json +13"]` / `entrees AST du JSON : 13` / `estEntreeDeStock source: function estEntreeDeStock(ligne) { const l = String(ligne ?? ''); return ENTREE_EN_TETE.test(l) || ENTREE_EN_QUEUE.test(l); }` — `EXIT=0` lu par `$LASTEXITCODE` sans tube. Cause lue au fichier : `scripts/guards/lib/stocksNominatifs.mjs:131-136` (deux regex de LIGNE définies l.122-123, aveugles à une entrée-objet JSON multi-lignes) ; `git log --oneline fd660c8b8..34e3cbb6c -- scripts/guards/lib/stocksNominatifs.mjs` → sortie VIDE. bloquante=faux : le repli ne se joue que si l'IMAGE ne se lit pas — le chemin CI/pre-commit passe `lirePostImage` et VOIT le `+13` — aucune fermeture ni solde de la fenêtre ne repose dessus.

```
node (lecture seule, fichier écrit sous le dossier de sondes) : import de `scripts/guards/lib/stocksNominatifs.mjs` de l'arbre jugé ; `const diff = git('show','--format=','-U0','5756d2d2c')` ; `const post = (f) => git('show', 'HEAD:'+f)` ; puis `console.log('SANS image :', JSON.stringify(M.croissanceDesStocks(diff).map(c => c.fichier + ' +' + c.net)))`, `console.log('AVEC image :', JSON.stringify(M.croissanceDesStocks(diff, { lirePostImage: post }).map(c => c.fichier + ' +' + c.net)))`, `console.log('entrees AST du JSON :', M.entreesDeStock(post('scripts/raw/reconciliation-stock.json'), 'scripts/raw/reconciliation-stock.json').length)`. Sortie : `SANS image : []` / `AVEC image : ["scripts/raw/reconciliation-stock.json +13"]` / `entrees AST du JSON : 13`, EXIT=0 (lu par `$LASTEXITCODE`, sans tube).
```
17. **Action (δ) NON soldée : le stock des gardes qui marchent l'arbre à la main n'est chiffré nulle part et n'est sous aucun cliquet** (lentille restes-de-la-revue-precedente) — Comptage à HEAD : 106 fichiers `src/**/*.test.ts(x)` contiennent `readdirSync` (108 au palier précédent) — décroissance INCIDENTE, non déclarée. Aucun message de la fenêtre ne prononce le mot : `git log fd660c8b8..34e3cbb6c --format=%H%n%B | Select-String readdirSync` → 0 occurrence. Aucun stock/cliquet ne le porte : sur `scripts/**`, les seuls fichiers de famille stock/ratchet contenant `readdirSync` (`scripts/hooks/soldes-stock.test.mjs`, `scripts/ops/knip-exports-ratchet.test.mjs`) l'UTILISENT pour marcher l'arbre, aucun ne le compte. → attendu : Le reste est chiffré au message (ou porté par un stock nominatif décroissant) pour qu'il décroisse sous le regard de quelqu'un — un reste non chiffré ne décroît que par accident, comme ici (−2 sans cause écrite). · réfutation tentée : Re-mesuré aux DEUX bornes (l'auteur ne donnait que HEAD), arbre épinglé `34e3cbb6c` : `git grep -l readdirSync fd660c8b8 -- "src/*.test.ts" "src/*.test.tsx"` → **108** fichiers ; le même sur `HEAD` → **106** — la décroissance de 2 est réelle. Non chiffrée : `git log fd660c8b8..34e3cbb6c --format=%B` + `Select-String readdirSync` → **0** occurrence (compté sans tube d'exit code). Sous aucun cliquet : recherche `walker|readdirSync` sur `scripts/**/*.mjs` — les seuls fichiers de famille stock/cliquet qui contiennent `readdirSync` l'UTILISENT pour marcher l'arbre, aucun ne le compte ; la liste `PORTEURS` de `scripts/guards/lib/stocksNominatifs.mjs:87-93` (5 motifs) ne déclare aucun stock de walkers. bloquante=faux : aucune fermeture ni solde de la fenêtre ne s'appuie sur ce chiffre.

```
PowerShell (lecture seule) : `(Get-ChildItem -Recurse -Path src -Include *.test.ts,*.test.tsx | Select-String -Pattern 'readdirSync' -List | Measure-Object).Count` → `106` ; `git log fd660c8b8..34e3cbb6c --format=%H%n%B | Select-String -Pattern 'readdirSync' | Measure-Object | Select-Object -ExpandProperty Count` → `0`
```
18. **Action (ε) NON soldée : 8 des 11 commits de substance de la fenêtre n'ont AUCUN run CI à leur sha, et le régime n'a pas écrit qu'un lot ne fait juger que sa tête** (lentille restes-de-la-revue-precedente) — Faits `coursesCi` (provenance `gh`) croisés au drapeau `substance` des faits — sans run propre : `7c818901a` (train B1 #1694), `1e7f4f830` (train E #1709), `ff6771b90` (train A #1694), `774cc7b97`, `3d5d5d73f`, `9c1b91929`, `25b8e306d`, `a8d16ade0` ; avec run `success` : `34e3cbb6c`, `c4a0854cf`, `26340a52a`. Aucun commit de la fenêtre n'écrit la décision ; la recherche dans les commentaires de #1679/#1709 ne rend que le ROUTAGE du 2026-09-08 (« (ε) publier commit par commit ou accepter qu'un lot ne fasse juger que sa tête »). → attendu : Soit la publication se fait commit par commit, soit le régime écrit noir sur blanc qu'un lot ne fait juger que sa tête — le statu quo laisse 8 commits de substance sans verdict CI sur leur propre contenu. · réfutation tentée : Re-mesuré à la SOURCE et non repris du fichier de faits : `gh run list --limit 60 --json headSha,conclusion,status,workflowName,createdAt` (couvre 2026-09-04 → 2026-09-08, donc toute la fenêtre). Shas de la fenêtre portant un run CI : `34e3cbb6c`, `7d7cd5a4e`, `9df43457b`, `f87b17867`, `4e78d2d4b`, `51d246e46`, `c4a0854cf`, `26340a52a` — tous `success`. Croisé au drapeau `substance` : SANS run propre = `7c818901a`, `1e7f4f830`, `ff6771b90`, `774cc7b97`, `3d5d5d73f`, `9c1b91929`, `25b8e306d`, `a8d16ade0` → **8/11**, exactement la liste soumise. Rien d'écrit au régime : `git diff --name-only fd660c8b8..34e3cbb6c -- .claude docs` ne touche aucun doc de régime (fiches mémoire + docs dérivés seulement) ; recherche `que sa t[êe]te|run CI à son sha|commit par commit` sous `.claude/` → le seul hit pertinent est le ROUTAGE de la revue précédente (`.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md:46`). bloquante=faux, motivé CONTRE l'auteur : la seule fermeture de la fenêtre (`a8d16ade0`, `corrige #1692`) a été publiée dans un lot dont la tête `51d246e46` porte un run CI `success`, or le régime ÉCRIT (CLAUDE.md § Commandes) fait suivre la fermeture de la PUBLICATION — `fermer-depuis-main <before>..<sha>` « après un `build` vert sur `main` », sur la PLAGE. Ce qui manque est le verdict CI par commit, pas la porte de fermeture.
19. **Action (α) NON tranchée : le périmètre #591 est intact à l'octet et aucune décision de régime n'est écrite** (lentille restes-de-la-revue-precedente) — `git show HEAD:scripts/hooks/solde-ticket-guard.mjs` l.25-27 : « Le déclencheur reste le TICKET explicitement rattaché (fermeture ou `ref #N`) — un commit sans AUCUN ticket n'entre jamais dans ce mécanisme (périmètre tranché #591, 2026-07-17). » ; `git log --oneline fd660c8b8..34e3cbb6c -- scripts/hooks/solde-ticket-guard.mjs` → AUCUN commit. Le seul écrit de la fenêtre sur le sujet est le ROUTAGE (commentaire #1709 : « (α) déclencheur du garde au-delà du ticket cité, ou périmètre #591 écrit dans le régime (#1679 L2) ») — une action routée, pas une décision. → attendu : L'une des deux branches de (α) est exécutée : le déclencheur du garde s'étend au-delà du ticket cité, OU le maintien du périmètre #591 est écrit dans le régime avec sa raison — le trou reste ouvert et il a déjà été exercé une fois (`947b3b7a1`). · réfutation tentée : Arbre épinglé `34e3cbb6c`. `git show HEAD:scripts/hooks/solde-ticket-guard.mjs` — l.26-27 verbatim : « Le déclencheur reste le TICKET explicitement rattaché (fermeture ou `ref #N`) — un commit sans / AUCUN ticket n'entre jamais dans ce mécanisme (périmètre tranché #591, 2026-07-17). » ; `git log --oneline fd660c8b8..34e3cbb6c -- scripts/hooks/solde-ticket-guard.mjs` → sortie VIDE. J'ai cherché à réfuter par une décision écrite ailleurs : `git diff --name-only fd660c8b8..34e3cbb6c -- .claude docs` ne touche aucun doc de régime, et les 27 occurrences de `#591` de l'arbre vivent toutes dans le garde, ses tests et la ligne de ticket `docs/decisions/issues.md:1354` — aucune n'est une décision de maintien motivée. Précision instruite : le trou est un CONTRAT verrouillé par des tests positifs (`scripts/hooks/solde-ticket-guard.test.mjs:1068-1071` « aucun ticket rattaché … → silence (#591) », `:1138-1140` pour `evaluateJuge`), pas un oubli d'implémentation. bloquante=faux : les 18 commits de la fenêtre portent tous un `refs #N`/`corrige #N` — le trou n'a PAS été exercé ici, aucune fermeture ni solde de cette fenêtre n'en dépend.

```
PowerShell (lecture seule) : `git show HEAD:scripts/hooks/solde-ticket-guard.mjs | Select-Object -Skip 24 -First 10` ; `git log --oneline fd660c8b8..34e3cbb6c -- scripts/hooks/solde-ticket-guard.mjs` → (vide)
```

## Écartées par réfutation

- ~~c4a0854cf — 4 entrées de stock nominatif migrent de fichiers SURVEILLÉS vers `eslint.config.js`, qui n'est pas un porteur : toute exemption future y échappe au cliquet~~ (lentille stocks-et-cliquets) — écartée : RÉFUTÉ sur sa claim porteuse (« migrent vers `eslint.config.js` »). Arbre `34e3cbb6c`. Les chiffres bruts de la trouvaille sont exacts — je les reproduis — mais ils ne portent PAS le verdict.
MESURÉ (scratchpad\p3.mjs) : `src/state/state-purity.test.ts : avant=3 entrees apres=ABSENT` avec `entrees avant : ["'combatManeuvers.ts',","'roster.ts',","'revealStep.ts',"]` ; `src/engine/engine-purity.test.ts : avant=1 entrees apres=ABSENT` (l'entrée est l'allowlist inline `allow: new Set(['types.ts'])`, engine-purity.test.ts:36 @c4a0854cf^) ; `estPorteurDeStock('eslint.config.js') = false`.
CONTRE-MESURE QUI RÉFUTE LA MIGRATION : aucune des exemptions n'a atterri dans `eslint.config.js`. `git show HEAD:eslint.config.js | grep -n "combatManeuvers\|roster\|purity"` → SORTIE VIDE : `combatManeuvers.ts` et `roster.ts` n'y existent à aucun octet ; `types.ts` non plus. Les 7 noms de fichiers AJOUTÉS par c4a0854cf (mesurés : rollSeam.ts, revealStep.ts ×2, flowOutcomes.ts, rollFlowSpecs.ts, encounterPsychFlow.ts) sont l'inverse d'exemptions — lus dans le diff, ce sont trois blocs qui RÉ-IMPOSENT la pureté à des fichiers déjà exemptés d'AUTRES règles : « Les deux MINTEURS de `src/state` : exemptés des verrous de marque …, JAMAIS de la pureté de couche — sans ce bloc, la règle du dessus les ferait sortir du radar de l'import dynamique » et « Les trois goulots du canal d'issue restent SOUS la pureté de couche » (eslint.config.js, blocs ajoutés par c4a0854cf). Les 14 autres noms de fichiers préexistaient à c4a0854cf (mesuré : `avant` = 14 noms, dont rollSeam/revealStep/rollRowBuild/i18n/ownership/…).
La dette nominative a donc été ABOLIE, pas déplacée — ce que le commit annonce et que la mesure confirme, et ce que la fermeture #996 (9df43457b) rejoue par mutation. RÉSIDU VRAI mais hors du titre : `eslint.config.js` n'est effectivement pas dans `PORTEURS` (stocksNominatifs.mjs:87-93) et porte 21 littéraux de chemin ; une exemption nominative FUTURE y échapperait au cliquet. C'est un angle mort de périmètre à instruire, pas la migration alléguée.
- ~~`knip-exports-baseline.json` : 11 exports morts gelés, INCHANGÉS sur toute la fenêtre et depuis le 2026-08-17 — et le fichier n'est pas un porteur de stock~~ (lentille stocks-et-cliquets) — écartée : RÉFUTÉ comme DÉFAUT (les faits bruts sont exacts, l'accusation ne tient pas). Arbre `34e3cbb6c`. Faits reproduits (scratchpad\p3.mjs) : dernière touche `eb1da9826 2026-08-17` (#1318 E2, 173 → 11) ; contenu HEAD = 6 fichiers / 11 symboles (setLocale, LandMarketState, PortState, PendingSeaActivities, StairCell, StairFlightPlan, StairStep, minFlightCells, setEdgeWall, lowerLayerMode, lowerLayerOpacity) ; `estPorteurDeStock('knip-exports-baseline.json') = false`.
CE QUI RÉFUTE L'`attendu` « il lui faut un solde nommé » : il EN A UN, écrit à côté du cliquet. `scripts/ops/knip-exports-ratchet.mjs:12-21` : « STOCK RÉSIDUEL (2026-08-17, #1318 E2 — 173 → 11) : chaque survivant est justifié NOMINATIVEMENT » — `setLocale` = commutateur du seam i18n (→ #320) ; `PortState`/`LandMarketState`/`PendingSeaActivities` = « ANGLE MORT de knip, pas des morts » (import de type INLINE que knip ne compte pas comme consommation) — soit 3 des 11 qui ne sont PAS des exports morts ; les 7 de `src/ui/editor/**` = hors périmètre de la passe E2. 1+3+7 = 11, le compte est couvert entrée par entrée.
CE QUI RÉFUTE « sa croissance ne demanderait aucun CLIQUET » comme trou ouvert : la porte principale de ce stock N'EST PAS celle des porteurs mais son propre cliquet — `knip-exports-ratchet.mjs:4-5` « toute APPARITION échoue, toute entrée assainie doit être RETIRÉE de la baseline (`--sync` ne sait que retirer, jamais ajouter) », implémenté par `compare()` (:45-57, `nouveaux` ⇒ échec) ; la baseline est déclarée lue par la gate `deps:unused` (`scripts/gates/toutes.mjs:136,182`). Une croissance ne peut donc venir que d'une édition MANUELLE du JSON — angle mort réel mais mineur, pas « une dette non gardée ».
ENFIN, hors accusation : le stock est ACTIVEMENT suivi dans la fenêtre — le cliquet d'a8d16ade0 en rend compte et NOMME une extinction du palier (« l'export `abonnerAuxDatasets` posé “pour un train futur” a été SUPPRIMÉ : knip le voyait mort »). Un stock documenté, gardé et stable sur une fenêtre d'UN jour n'est pas un défaut du palier. NON BLOQUANTE.
- ~~Le ticket porteur de la vague #1686 n'est pas tenu à jour : 5 commits « refs #1686 » posés dans la fenêtre, ticket inchangé depuis le 2026-09-07 — l'état vit dans une fiche mémoire, pas au ticket~~ (lentille regime-de-vague) — écartée : Le FAIT brut tient (#1686 figé) mais la trouvaille se réfute sur son cœur — « l'état vit dans une fiche mémoire, pas au ticket » est FAUX, et l'auteur cite lui-même la preuve qui le contredit.
Mesure (arbre épinglé .wt-1624 @ 34e3cbb6c) : `gh issue view 1686 --json comments --jq '.comments[] | [.createdAt, …]'` → 9 commentaires, le dernier 2026-09-07T12:16:36Z (« État après #1690 (2026-09-07) »), rien depuis. Exact.
MAIS `gh issue view 1694 --json comments` rend TROIS commentaires de pilotage, un PAR TRAIN posé, dans les minutes suivant chaque push :
• 2026-09-08T20:45:28Z « **Train A — POUSSÉ** (de 4e78d2d4b à f87b17867, `refs #1694`, ticket OUVERT jusqu'au train B) — le SOCLE. »
• 2026-09-08T22:25:48Z « **Train B1 — POUSSÉ** (de 9df43457b à 7d7cd5a4e, `refs #1694`, ticket OUVERT jusqu'au train B2) »
• 2026-09-08T23:27:47Z « **Train B2 — POUSSÉ** (de 7d7cd5a4e à 34e3cbb6c, `refs #1694`, ticket OUVERT jusqu'au train B3) — la GRAMMAIRE. »
Or les commits de la fenêtre portent `refs #1694, refs #1686` (ff6771b90, 7c818901a, 34e3cbb6c) : #1694 EST le ticket de ces trains. L'auteur produit « #1694 maj 2026-09-08T23:27:48Z » comme CONTRE-EXEMPLE vertueux sans voir qu'il s'agit du ticket des trains qu'il juge non pilotés — sa propre mesure réfute sa conclusion. De même #1692 : fermé par a8d16ade0 (`corrige #1692`), ticket CLOSED updatedAt 2026-09-08T19:26:06Z.
Règle invoquée, relue au canon (même fiche, ligne 27, point 5) : « Pilotage de l'épic réécrit à chaque train posé » — c'est fait, sur le ticket du train ; et #1686 n'est pas l'épic (titre : « Matières du monde … vague de #1463 » — l'épic est #1463, #1686 est la vague, #1691/#1692/#1694 ses tickets de train). Aucune règle du canon (fiche du régime, CLAUDE.md, credo) n'exige un commentaire sur le ticket-PARENT à chaque train d'un ticket-enfant déjà piloté.
Reste un résidu vrai mais mineur, non retenu comme défaut : #1686 (OPEN) n'a pas reçu de récapitulatif depuis le 2026-09-07, l'état de ses descendants ayant en plus été écrit en fiche (f87b17867) — mais ce n'est pas « à la place » du ticket, c'est EN PLUS de trois commentaires de ticket.
- ~~Trace de worktree menteuse : le train #1694/#1692 se code dans `.wt-1624` sur la branche `chantier/1624-decors-ancres`, dont le ticket #1624 est CLOS et sans rapport~~ (lentille regime-de-vague) — écartée : Les FAITS bruts se re-mesurent et tiennent : `git worktree list` → « C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624   34e3cbb6c [chantier/1624-decors-ancres] » ; `gh issue view 1624` → state CLOSED, « Décors ANCRÉS au bâtiment en volume … », updatedAt 2026-09-04T22:08:44Z ; le reflog de branche (`git reflog chantier/1624-decors-ancres`) du 2026-09-08/09 ne porte que des commits `refs #1692` puis `refs #1694, refs #1686` — aucun travail #1624.
Ce qui RÉFUTE la trouvaille, c'est son « attendu » : il n'existe dans le canon AUCUNE règle exigeant que le worktree ou sa branche nomme le chantier porté. Vérifié : `grep -n "worktree" .claude/credo.md` → 0 occurrence ; la fiche du régime .claude/memory/user-regime-une-session-par-chantier-2026-09-01.md ligne 23 n'exige que « tout train se code, se gate et se commit dans un WORKTREE à `npm ci` sur le sha posé » (rien sur le nom) ; .claude/memory/game-agents-worktree-isolation-shared-branch.md ne pose que l'isolation, la pile de stash partagée et les gates en différentiel — aucune convention de nommage. L'attendu est donc fabriqué par l'auteur, pas cité au canon (règle du repo : une affirmation de règle se vérifie à la source avant verdict).
Et la prémisse « trace menteuse » ne tient pas non plus à la mesure : la branche n'est pas un fil de travail #1624 recyclé mais un pointeur rebasé sur origin/main à chaque train (`git reflog chantier/1624-decors-ancres` : « rebase (finish) … onto c4a0854cf » 21:02:41, « onto 4e78d2d4b » 22:32:12, « onto 9df43457b » 00:13:07), et `git log chantier/1624-decors-ancres` = l'histoire de main. La traçabilité du régime se lit au COMMIT (chaque commit de la fenêtre nomme son ticket : `refs #1692`, `corrige #1692`, `refs #1694, refs #1686`) et au TICKET (#1694 : trois commentaires de train horodatés 20:45 / 22:25 / 23:27 le 2026-09-08) : rien n'est perdu ni masqué par le nom du répertoire. Défaut d'HYGIÈNE cosmétique au plus (répertoire-conteneur réutilisé), pas une infraction au régime — et sans effet sur une fermeture (les deux `corrige` de la fenêtre, #1692 et #996, sont nés en worktree et sont vérifiables au message de commit).

## Points tenus

- fermetures-soldes : Arbre épinglé : HEAD = 34e3cbb6c (worktree .wt-1624, `git log --oneline -1`), contrôle positif du travail jugé : `.claude/soldes/996.md` et `.claude/soldes/1692.md` présents dans l'arbre, `src/data/index-vif-guard.test.ts` et le bloc de pureté d'`eslint.config.js:60-102` existent.
- fermetures-soldes : #996 — le solde PART AVEC le commit fermant : `git show --stat 9df43457b` liste `.claude/soldes/996.md` (14 l.) + `.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md` dans le MÊME commit ; le sujet cite `corrige #996`.
- fermetures-soldes : #1692 — idem : `git show --stat a8d16ade0` liste `.claude/soldes/1692.md` (32 l.) ; sujet `corrige #1692`.
- fermetures-soldes : Les deux commits fermants sont PUBLIÉS (`git branch -r --contains 9df43457b` et `a8d16ade0` → `origin/main`), donc la fermeture suit bien la publication, régime du CLAUDE.md.
- fermetures-soldes : Fermetures hors commit : `gh` ne rend aucune fermeture non citée dans la fenêtre (fait du harnais, non contesté après lecture des deux issues : #996 et #1692 sont CLOSED avec le solde POSTÉ en commentaire — 2026-09-08 22:09:01 et 19:26:05 — par la route de publication).
- fermetures-soldes : #996 DoD point 1 (mutation `revealStep.ts` en import runtime → garde ROUGE) : REJOUÉ par sonde ESLint sur la config résolue — ORIGINAL 0 erreur, MUTÉ 1 erreur `no-restricted-imports` l.10 « Pureté de couche (#1709…) : src/state ».
- fermetures-soldes : #996 DoD point 2 (aucun blanchiment par NOM DE FICHIER) : `src/state/state-purity.test.ts` absent de HEAD (`git ls-tree -r HEAD | grep purity` → seul `src/data/data-purity.test.ts`) ; la pureté vit en `no-restricted-imports`+`no-restricted-syntax` (eslint.config.js:76-102, 211-229, 294-335) et n'exempte que des formes élidées à la compilation ; les trois goulots du canal d'issue restent sous la pureté par le bloc l.328-335. Aucune perte de couverture : l'ancienne garde excluait déjà les `*.test.ts` (`git show c4a0854cf^:src/state/state-purity.test.ts`).
- fermetures-soldes : La gate qui joue cette pureté existe et couvre le périmètre : `package.json:86` `lint:brut` = `eslint . --max-warnings 0`, appelée par `.github/workflows/ci.yml:54`.
- fermetures-soldes : #1692 DoD (garde structurelle « aucun index figé à l'import sur une clé d'ARRAYS ») : détecteur mordu par contrôle POSITIF sur les index NOMMÉS par le ticket — PROP_BY_ID, LIGHT_LEVEL_BY_ID, domainById, allAxes/AXIS_BY_ID réinjectés → 1 fautif chacun ; contrôle NÉGATIF sur l'arbre réel (src/** non-test hors seam) → 0 fautif.
- fermetures-soldes : #1692 DoD (les index existants passent en lecture VIVE) : `PROP_BY_ID`→`propParId` (index.ts:2723-2724), `LIGHT_LEVEL_BY_ID`→`niveauLumiereParId` (2701), `domainById`→`domaineParId` (2741), `AXIS_BY_ID`→`axeParId` (2755), tous via `indexParId`/`memoParVersion` de `src/data/versionDataset.ts`.
- fermetures-soldes : #1692 — la 5e ligne du tableau du ticket (`src/gameIso/catalog/structures/index.ts:9`, MAP figée) est encore figée MAIS légitimement hors périmètre : `structureAppearances` (index.ts:2402) n'est ni dans `ARRAYS` (overrides.ts:159-253) ni dans `OBJECTS` — aucun chemin `setDataset`, donc aucune staleness ; le solde le dit et le mesure (1692.md l.18).
- stocks-et-cliquets : Arbre épinglé : HEAD = 34e3cbb6c (train B2 #1694) ; contrôle positif = `src/data/schemas/grammaire/records-de-libelles.test.ts` présent avec `const STOCK` de 21 entrées. ⚠ Arbre SALE (index chargé : `.claude/soldes/1694.md` ajouté, docs dérivés + src modifiés — session vivante) : toutes mes mesures sont prises sur les OBJETS COMMITTÉS via `git show`, jamais sur le working tree.
- stocks-et-cliquets : Balayage per-commit des 18 commits de fd660c8b8..34e3cbb6c par `croissanceDesStocks` + `cliquetsDuMessage` : 4 croissances seulement (eslint-purete-de-couche.test.ts +2, index-vif-guard.test.ts +1, records-de-libelles.test.ts +47, justificatif.mjs +1) — trois couvertes au compte exact, la quatrième (a8d16ade0) éteinte en cumulé par 25b8e306d, ce qui explique le `refus: []` des faits.
- stocks-et-cliquets : `records-de-libelles.test.ts:STOCK` DÉCROÎT sur deux paliers, mesuré : 47 (ff6771b90) → 30 (7c818901a) → 21 (34e3cbb6c) ; aucune borne chiffrée : le test exige `eteints === []` (lignes 218-222), donc une entrée éteinte DOIT être retirée — pas de plafond.
- stocks-et-cliquets : `valeurs-de-champ.test.ts:VOCABULAIRES_SANS_LIBELLES` DÉCROÎT sur deux paliers, mesuré par AST : 159 → 140 → 130 ; assertion d'égalité stricte au stock déclaré (ligne 275), plus un contrat positif `jumeauxMuets === []`.
- stocks-et-cliquets : c4a0854cf `CLIQUET: src/eslint-purete-de-couche.test.ts +2` : compte exact (2 entrées mesurées), motif > 20 car., et le contenu est bien le CONTRAT (`COUCHES` = 2 couches amont, eslint-purete-de-couche.test.ts:36-39), pas une exemption.
- stocks-et-cliquets : 1e7f4f830 : la croissance de `RAISON_CLE_COMPLETE` va dans le sens SÛR (clé partielle → clé complète, plus de rejeux) et chaque entrée porte sa lecture mesurée et datée — le grief porte sur le COMPTE et la cécité du détecteur, pas sur la direction.
- stocks-et-cliquets : c4a0854cf n'introduit AUCUN nom de fichier exempté nouveau : l'ensemble DISTINCT des chemins nommés dans `eslint.config.js` est identique avant/après (10 chemins, sonde) ; la hausse 14 → 21 n'est que la répétition des mêmes noms dans les blocs qui leur rendent la règle (eslint.config.js:314 puis :323, :212 puis :331).
- stocks-et-cliquets : Aucune baseline ni aucun stock JSON n'a bougé dans la fenêtre : `git diff --stat fd660c8b8..34e3cbb6c -- "*baseline*.json" "scripts/raw/*-stock.json" "scripts/hooks/*.json"` rend une sortie vide — aucune liste d'exemptions JSON neuve.
- stocks-et-cliquets : Aucun processus laissé vivant : seules des exécutions `node` ponctuelles (sondes du scratchpad) et des lectures `git` en lecture seule ; aucune écriture sous l'arbre jugé.
- poison-des-diffs : Arbre ÉPINGLÉ : `git log --oneline -1` dans C:\Users\gauch\PhpstormProjects\Foundry\Game\.wt-1624 rend **34e3cbb6c** (train B2 #1694) ; contrôle POSITIF du travail jugé : `src/data/schemas/grammaire/valeurs.ts:61` porte bien `specsSourceSchema = enumNomme({…})` à 16 valeurs, et les trois anciens bancs `src/{engine,state,gameiso}-purity.test.ts` sont ABSENTS de HEAD (`ls` → No such file), donc la fenêtre fd660c8b8..34e3cbb6c est bien celle mesurée.
- poison-des-diffs : Corpus de la lentille MESURÉ, pas survolé : 1 136 lignes de COMMENTAIRE ajoutées par `git diff -U0 fd660c8b8 HEAD -- src scripts` (parseur de hunks, sortie dans le dossier de sondes) — filtrées ensuite sur les vocabulaires excuse / tombale / réf RAW.
- poison-des-diffs : Famille EXCUSE : aucun `TODO`/`FIXME`/« pour l'instant »/« pour le moment »/« temporaire »/« provisoire »/« en attendant » ajouté dans `src/**` ni `scripts/**` sur la fenêtre (scan des 1 136 commentaires ajoutés) — un seul commentaire porte le mot DETTE (`src/state/combatManeuvers.ts:524`) et il NOMME la dette, la mesure (« 20 entrées de `maneuvers.json` portent toutes un `label` non vide ») et déclare le chemin INERTE : c'est un signalement, pas une autorisation.
- poison-des-diffs : Libellés du train B/B2 repris VERBATIM sur les deux vocabulaires que j'ai contre-vérifiés : `git diff fd660c8b8 HEAD -- src | Select-String "^-.*(Progression maximale|PA métalliques)"` rend les MÊMES chaînes côté suppression (`plus2: 'Progression maximale (M+2)'…`, `metal: 'PA métalliques', nonMagic: 'PA non magiques'`) — la migration Record→nœud n'a pas réécrit ces libellés.
- poison-des-diffs : Table RAW `naval-progression` confrontée au Source : `Source/WH - V4 - La Mer de Griffe/13 - Navigation maritime.md` l.68-75 porte bien la table PROGRESSION à CINQ bandes (4 ou plus / 1 à 3 / –2 à 0 / –3 à –4 / –5 ou moins) — le commentaire ajouté `src/data/schemas/defs/naval-progression.ts:10` (« Les 5 modes… MDG 13 l.68-75 ») est exact.
- poison-des-diffs : Aucun cliquet RELEVÉ dans la fenêtre : le seul mouvement de plafond est une BAISSE justifiée (`registry-id-branch-guard` sur `src/engine/trauma.ts` 8 → 7, commit 774cc7b97) ; `git diff … | Select-String "^[+-]const (PLAFOND|RAW_CEILING|MAX_|STOCK)"` ne rend aucune hausse.
- poison-des-diffs : Réfs RAW ajoutées vérifiées au Source pour les deux que j'ai pu trancher : LDB ch.48 l.106 (Cieux) et l.588 (Ombres) existent bien et disent une règle de Domaine sur les PA ; MDG ch.13 l.68-75 existe bien.
- derogations-et-ci : Arbre épinglé et contrôle POSITIF : HEAD = 34e3cbb6c (train B2 #1694) = origin/main ; les 18 commits fd660c8b8..34e3cbb6c sont tous sur origin/main (git branch -r --contains).
- derogations-et-ci : Dérogations DANS la fenêtre = 0 : le journal réel C:/Users/gauch/PhpstormProjects/Foundry/Game/.git/wfrp-justificatifs/derogations.log ne porte que 7 lignes, la plus récente 2026-09-06T05:56:41Z (fichier non modifié depuis le 06/09) — le fait `derogations.dansLaFenetre: []` / `horsFenetre: 7` est confirmé au fichier.
- derogations-et-ci : Les 7 dérogations hors fenêtre ont chacune une raison mesurée ET une suite : #1→#2 (« ce push en est le correctif » de docs:check cross-OS), #3/#4 (banc weather d'une autre session) → #6 « corrige le rouge de main (weather-boucle.test.tsx:161) », #5 (test:hooks, « corrigé par ce push »), #7 (« correctif du rouge de main lui-même », grammaire.test.ts:1038) ; les doublons #3/#4 sur c3692d0f9 sont un fait déjà documenté à pre-push.mjs:40.
- derogations-et-ci : Aucun push sur CI rouge dans la fenêtre : les 16 courses non vertes de ci.yml sur main (100 dernières) sont toutes ≤ 2026-09-05T21:30 (dernière : 1658c946a), et la course suivante (2026-09-06T06:13, de8424de5) est verte — le rouge dérogé a bien été corrigé.
- derogations-et-ci : Chaque push de la fenêtre a une CI verte sur sa tête : 8 courses `CI` completed/success (26340a52a, c4a0854cf, 51d246e46, 4e78d2d4b, f87b17867, 9df43457b, 7d7cd5a4e, 34e3cbb6c) ; les 10 commits sans course sont des commits INTERMÉDIAIRES d'un même push (GitHub ne déclenche `on: push` que sur la tête), régime assumé et déclaré par pre-push.mjs:4-5 (« les cinq premiers portent sur la TÊTE de chaque ref poussée, l'unité que la CI juge »).
- derogations-et-ci : La CI verte de HEAD n'est pas un faux vert : run 34290738632, jobs `migrations`/`build`/`fermetures` tous success, et le job `build` a réellement joué les 22 gates (agents:check, test:hooks, test:ops, test:runner, test:docs, deps:unused, test:recette, typecheck, lint, npm test, build, docs:check, docs:empreinte, raw:*, server:typecheck) — 0 step skipped.
- derogations-et-ci : La porte pre-push est bien installée dans l'arbre jugé (git config core.hooksPath = scripts/git-hooks ; scripts/git-hooks/pre-push + pre-push.mjs présents) et des justificatifs frais existent sous .git/wfrp-justificatifs (clés 501f7956e…/02eabdfe…, écrits 08/09 23:48-23:58).
- derogations-et-ci : LIMITE de mon angle, dite explicitement : le journal de dérogations vit sous `.git/` (non commité, par machine, partagé entre worktrees via --git-common-dir) — un push émis depuis un autre clone/une autre machine, ou un `git push --no-verify`, ne laisserait aucune trace ici ; aucun indice d'un tel contournement dans la fenêtre (toutes les têtes poussées ont une CI verte), mais je ne peux pas le RÉFUTER.
- commits-triviaux : Arbre épinglé : HEAD = 34e3cbb6c (branche `chantier/1624-decors-ancres`, worktree .wt-1624). ATTENTION : l'arbre porte un WIP d'une autre session (60 fichiers stagés, dont `src/data/schemas/grammaire/valeurs.ts`) — toutes mes mesures de contenu ont été refaites sur les blobs `git show HEAD:…` quand le fichier était sale.
- commits-triviaux : Aucun code n'a été glissé dans un commit `chore` de la fenêtre : `51d246e46` = 12 fichiers `docs/` seuls, `4e78d2d4b` = `.claude/memory/` + `CLAUDE.md`/`AGENTS.md` + `docs/.sources-lues.json`, `9df43457b` = `.claude/soldes/` seuls, `7d7cd5a4e`/`44f91c4cf`/`f87b17867` = 1 fichier dérivé chacun (`git show --stat`).
- commits-triviaux : Deux commits de la fenêtre touchent du CODE sans aucune ligne `JUGE:`/`REFUTATION:` (774cc7b97 sur `src/ui/registry-id-branch-guard.test.ts`, 3d5d5d73e sur `src/engine/travel.ts`) — les deux ont été re-mesurés et sont EXACTS, pas des retouches de confort.
- commits-triviaux : Le cliquet abaissé par 774cc7b97 (`RAW_KNOWN['src/engine/trauma.ts']` 8 → 7) est la MESURE, pas un décrément de confort : sonde `scanRawIdEqualities` sur `src/engine/trauma.ts` à HEAD → 7 sites nommés (L261, L399, L415, L720, L904, L1196, L1220), détecteur principal → 0. Le plafond global `RAW_CEILING` est dérivé (`registry-id-branch-guard.test.ts:244`), aucun cardinal n'est écrit à la main.
- commits-triviaux : L'affirmation de RETRAIT de 3d5d5d73e (« `travelModeLabel` n'avait AUCUN lecteur ») tient : `git grep -n 'travelModeLabel\b' HEAD -- src scripts` → 0 ; à `3d5d5d73e~1` la seule occurrence était sa définition (`src/engine/travel.ts:68`) — purge complète, pas d'orphelin.
- commits-triviaux : Le remplacement des trois gardes vitest de pureté par ESLint (c4a0854cf, qui SUPPRIME `state-purity.test.ts` et `gameiso-purity.test.ts`) ne perd rien : sonde ESLint sur la config RÉSOLUE → runtime `state→ui` rouge, `import type` vert, import DYNAMIQUE rouge (`no-restricted-syntax`), alias `@/ui` rouge, `export … from` rouge, `import '…'` de side-effect rouge, `state→gameIso` (y compris depuis un sous-dossier) rouge, `engine→state`/`engine→ui` rouge. Le fichier supprimé nommé `gameiso-purity` gardait en fait la relation `state→gameIso` (lu à `git show c4a0854cf~1:src/state/gameiso-purity.test.ts`), couverte par `AVALS_STATE` — aucune couche amont orpheline.
- commits-triviaux : Les tests réécrits par le correctif de gates 1e7f4f830 sont des contrats POSITIFS, pas des travestissements : `pre-push.test.mjs` remplace « commit docs/ seul → passe » par « la gate qui ne lit pas docs/ passe, celle qui le LIT est REJOUÉE » + un test neuf pour le cas légitime (sha différent, arbre identique) ; `solde-ticket-guard.test.mjs:1488` remplace une sonde qui ÉCRIVAIT `.repoRoot-probe-tmp` dans le magasin partagé par trois assertions de LECTURE. La tautologie résiduelle de `justificatif.test.mjs` (le test itère `Object.keys(RAISON_CLE_COMPLETE)`) est bornée par `assert.ok(nommees.length > 0)` et l'appartenance est mesurée ailleurs (`toutes.test.mjs`), limite que le commit ÉNONCE (accord table↔table, jamais la vérité de `lit`).
- commits-triviaux : Les migrations de test du train #1692 (`cascade-table-step.test.ts`, `de-monde-surface.test.ts`) RENFORCENT au lieu d'accommoder : le fail-fast passe de `/non enregistrée/i` à un motif qui exige l'id ET les deux causes, et un contrat positif neuf est ajouté (table retirée de la donnée → refus nommé, dataset restauré en `finally`).
- commits-triviaux : Les stocks bougés par les trains A/B (`records-de-libelles.test.ts`, `valeurs-de-champ.test.ts`) sont NOMINATIFS et décroissants (entrées retirées une à une), aucun cardinal vivant asserté.
- commits-triviaux : Le libellé FR inventé par 34e3cbb6c pour `weaponsMelee`/`weaponsRanged` est bien DÉRIVÉ, pas inventé au doigt : `git show HEAD:src/data/schemas/grammaire/valeurs.ts` → « Armes de mêlée » / « Armes à distance », identiques à `src/data/schemas/defs/trappings.ts:25-26` (`melee`/`ranged`).
- commits-triviaux : Aucun processus laissé vivant : les seules sondes jouées sont trois scripts `node` ponctuels sous le dossier de sondes (terminés) ; aucune écriture sous l'arbre jugé, aucune suite ni gate jouée.
- cross-os : Arbre ÉPINGLÉ : HEAD = 34e3cbb6c (train B2 #1694), base fd660c8b8, 312 fichiers dans la fenêtre. ⚠ l'arbre est SALE au moment de la mesure (36 fichiers `src/`+`scripts/` modifiés/indexés par une session voisine, dont valeurs.ts, mecanique.ts, records-de-libelles.test.ts) : toutes mes preuves de contenu sont prises sur `git show HEAD:` ou sur l'arbre git, jamais sur le working tree.
- cross-os : Encodage des 312 fichiers de la fenêtre, mesuré sur les blobs de HEAD : 0 BOM, 0 CRLF, 0 séquence non-NFC, 0 U+FFFD (sonde `enc.mjs`, `git show HEAD:<f>` en Buffer). Aucun piège d'encodage cross-OS introduit malgré les libellés FR accentués/apostrophés d'`enumNomme`.
- cross-os : Fins de ligne verrouillées au dépôt : `.gitattributes:6` `* text=auto eol=lf`, alors que `core.autocrlf=true` vient du gitconfig SYSTÈME (`file:C:/Program Files/Git/etc/gitconfig`) — le foot-gun Windows est neutralisé par attribut, pas par config locale.
- cross-os : Clés de justificatif (`clesDeContenu`, scripts/guards/lib/justificatif.mjs:74) INDÉPENDANTES de la conversion de fins de ligne : sonde `cles.mjs` sur HEAD, `git -c core.autocrlf=true|false|input hash-object --stdin` rend cleTree 6ebdb909b7c005632b9d2e18bd3ad2bfbfb2f321 et cleComplete 0b7d3989e1b4256f557231a690df1ae1050bcbe8 dans les 3 cas (6765 entrées, 5995 dans la clé partielle) — un justificatif écrit sous Windows vaut pour le même contenu sous Linux.
- cross-os : Zéro import relatif mal casé : 4 330 modules `src/`+`scripts/`+`server/`, 20 683 spécificateurs relatifs résolus contre l'arbre git (sensible à la casse) → 0 divergence de casse ; contrôle POSITIF joué (`./Dice` et `../Engine/dice` sont VUS comme mal casés, candidat `src/engine/dice.ts`). Le retrait de `src/engine/qualities/registry.ts` ne laisse aucun import fantôme résolu par insensibilité NTFS.
- cross-os : Le nouveau vocabulaire de gardes `scripts/guards/lib/bindingsVifs.mjs:439` liste par `listerArbre` (lister.mjs, ordre total par unités de code, jamais `localeCompare`) — l'ordre du système de fichiers n'entre pas dans le verdict des deux gardes #1692.
- cross-os : `src/data/versionDataset.ts` : le témoin de fraîcheur est un COMPTEUR (`Map<string, number>`, +1 par écriture au seam), aucune horloge, aucun aléa, aucune identité d'objet — la mémoïsation `memoParVersion` est reproductible d'une machine à l'autre.
- cross-os : Les nouveaux détecteurs `records-de-libelles.test.ts` / `valeurs-de-champ.test.ts` marchent l'arbre par `readdirSync` non trié mais rendent une liste `sort()` par unités de code et comparent des ensembles : leur verdict ne dépend pas de l'ordre du FS ; leurs chemins rapportés sont POSIX (`src/<rel>`), identiques Windows/Linux.
- cross-os : `docs/.sources-lues.json` (dérivé regénéré dans la fenêtre) ne porte que des chemins relatifs POSIX triés — aucune trace de machine, d'horodatage ou de `node_modules`.
- cross-os : L'enregistreur de lectures normalise bien `path.sep` → `/` (enregistreur-lectures.mjs:52) : les prefixes `lit` déclarés en POSIX dans `scripts/gates/toutes.mjs` sont comparables sur les deux OS ; `horsCle` (justificatif.mjs:43) replie aussi les antislashs.
- regime-de-vague : Arbre ÉPINGLÉ : `.wt-1624` HEAD = 34e3cbb6c (branche `chantier/1624-decors-ancres`), contrôle positif = commit de tête « train B2 refs #1694 » présent, `origin/main` = 34e3cbb6c ; toutes les mesures ci-dessous sont prises sur cet arbre et sur `.git/{logs,worktrees}` partagés.
- regime-de-vague : Un seul codeur LOURD à la fois : sonde `overlap.mjs` sur les justificatifs `node_modules/.cache/gates/*-<pid>.txt` des 8 arbres → 19 courses de gates depuis le 2026-09-08, ZÉRO chevauchement inter-worktree (section « CHEVAUCHEMENTS » vide) ; les courses s'enchaînent (18:34→18:40 .wt-1624, 18:40→18:48 .wt-gates-B1, 18:48→18:55 .wt-1624, UTC).
- regime-de-vague : Aucun fan-out d'agents : sonde `agents.mjs` sur `.git/worktrees/agent-*/logs/HEAD` → aucune entrée de reflog ≥ 2026-09-08 dans les 9 worktrees d'agent (dernières activités 2026-08-2x/09-05).
- regime-de-vague : Un train = un worktree : #1692 et #1694 entièrement dans `.wt-1624`, #1709 D0d/E + solde #996 dans `.wt-gates-A` (branches gates/D0d→gates/E), #1709 C3b-2 dans `.wt-gates-B1` (gates/C3b2) — aucun train réparti sur deux arbres (reflogs par worktree).
- regime-de-vague : Rebase sur `origin/main` AVANT commit final puis gates dans le worktree : ex. `.wt-1624` rebase 20:32:12 UTC → commit ff6771b90 20:30 UTC → course de gates 22 justificatifs 20:35:46→20:44:16 UTC ; `.wt-gates-A` rebase 21:35:44 → commit 9df43457b 21:45 → gates 21:48:58→21:58:35 UTC.
- regime-de-vague : Mémoire non écrasée malgré des commits mémoire concurrents dans deux arbres (28a23356f dans `.wt-1624`, 4e78d2d4b dans l'arbre principal) : `git show HEAD:.claude/memory/MEMORY.md` cite encore `project-1692-index-vif-livre-2026-09-08` ET `budget-horloge-agents-2026-09-08`.
- regime-de-vague : Pilotage à jour sur les tickets porteurs des trains : #1709 mis à jour 2026-09-08T22:02:38Z (après train E/#996), #1694 mis à jour 2026-09-08T23:27:48Z (≈9 min après le train B2 de 23:18:55Z).
- regime-de-vague : Portes de push : `derogations.dansLaFenetre` = [] (journal du pre-push), aucune fermeture hors commit dans la fenêtre, CI verte sur tous les shas poussés qui portent une course (34e3cbb6c, 7d7cd5a4e, 9df43457b, f87b17867, 4e78d2d4b, 51d246e46, c4a0854cf, 26340a52a).
- restes-de-la-revue-precedente : Arbre ÉPINGLÉ : `git log --oneline -1` = 34e3cbb6c (train B2 #1694) dans C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624 ; contrôle positif d'épinglage : `src/data/schemas/grammaire/valeurs.ts` (socle `enumNomme` né dans la fenêtre) présent à HEAD, `scripts/raw/reconciliation-stock.json` présent ; les deux fichiers mesurés (`relief-identite-matieres.test.ts`, `stocksNominatifs.mjs`) sont INTACTS dans l'arbre sale (`git status --porcelain -- <les deux>` = vide), mes mesures valent donc pour HEAD.
- restes-de-la-revue-precedente : Revue précédente PRÉSENTE et lisible (`.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md`, verdict PARTIEL, actions α-ε) — le cas « pas de revue précédente » n'est pas celui-ci.
- restes-de-la-revue-precedente : Action (ε₁) soldée sur son instance nommée : l'unique commit de FERMETURE de la fenêtre (`9df43457b`, `corrige #996`) porte un run CI `success` à SON sha, contrairement à `5ead0d66f` du palier précédent.
- restes-de-la-revue-precedente : Réfutation 5 du palier précédent (nom d'archive) SOLDÉE : la revue est committée sous `revue-palier-<date>-<base>-<tête>.md`, la graphie exigée par `nomDArchiveDeRevue`, portée par `9df43457b`.
- restes-de-la-revue-precedente : Les cinq actions ont bien été ROUTÉES publiquement (commentaire #1709) : les restes ci-dessus sont des restes d'EXÉCUTION, pas des oublis de traçage.
- restes-de-la-revue-precedente : Le symptôme (α) ne récidive pas : les 18 commits de la fenêtre citent tous un ticket (`refs #`/`corrige #`) — aucun commit de substance sans ticket dans ce palier.
- restes-de-la-revue-precedente : Le stock `readdirSync` a décru 108 → 106, donc dans le bon sens, mais sans cause écrite ni cliquet (voir trouvaille δ).
