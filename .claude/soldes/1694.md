# Solde #1694 — 2026-09-09 (train B3 ; trains A, B1, B2 poussés les 2026-09-08/09)

VERIFIE: l'attendu du ticket confronté à l'arbre — (1) le socle décrit la PROFONDEUR : le libellé d'une valeur d'enum vit SUR LE NŒUD (`enumNomme` = `z.enum(Object.keys(valeurs)).meta({ valeurs })`, `grammaire/valeurs.ts`), lu par `valeursDe`/`libelleDeValeur` (`grammaire/meta.ts`, déroulé unique `noyauEnum` composé par `optionsEnum`) à toute profondeur, dans `champs` comme dans `options.rangee` ; couverture et ordre vrais par construction (les 3 verrous de `verifieValeurs` sont morts avec `MetaChamp.valeurs`, `valeursDuChamp`, `ENUMS_DE_DOCUMENT`) ; `inferFields` rend le `select` nommé en profondeur (26 éditeurs dédiés reçoivent leur nœud, `nestedCols` aussi) ; (2) les 8 Records nichés du ticket + `SPECS_SOURCE_LABEL` (foyer `grammaire/valeurs.ts`, pas `SPEC_SOURCES` d'engine : cycle) + `ATTACK_LABEL` (mort d'engine, lu au site par `src/state`) sont migrés, plus 30 Records hors ticket trouvés par le détecteur structurel ; (3) le stock `valeurs-de-champ.test.ts` est passé du premier niveau (58 champs) à TOUS les vocabulaires (187 : 120 muets, 52 nommés, jumeaux muets 0) ; (4) le cliquet des Records UI (`records-de-libelles.test.ts`, détecteur structurel : clés ⊆ options d'un vocabulaire, valeur = texte FR, forme `{clé: 'texte'}` et `{ id, label }[]`) est né GELÉ à 47 et descend à 9 — pas 0 : le juge de design a recalibré l'attendu (« cliquet = stock nominatif gelé, pas 0 ») parce que les restes sont un SECOND AXE d'affichage ou une homonymie, pas des doublons du libellé canonique.

## Restes
- `src/engine/combat.ts:1302` décidait l'Esquive sur un LIBELLÉ FR (`rangeBandName(...) === 'Bout portant'`), invisible à `label-logic-guard` (retour d'appel comparé à un littéral) -> corrigé dans ce commit : src/engine/combat.ts:1302
- `src/state/combatFlow.ts:5926` `worstCorruptionExposure` compare `worst.toLowerCase() === 'modérée'` (rang keyé par le même texte `:5924`) : logique par LIBELLÉ sur l'argument du trait `corruption`, révélée par la garde étendue (`label-call-literal`), gelée au stock à 1 — la migrer exige un vocabulaire d'ids pour `t.arg` (donnée + authoring + lecteurs) -> inventaire #1463 : l'argument du trait `corruption` porte un libellé, pas un id
- Les 9 lignes du stock GELÉ `records-de-libelles.test.ts`, d'une autre nature que le doublon d'un libellé canonique : second axe d'affichage sur un nœud nommé (`LOC_SHORT`, `ZONE_OF_LOC`, `humanize:FIELD`/`REL_PLAYER`/`table`/`negTable`, et le jumeau `<option>` Vue/Ouïe de `GameOpEditor.tsx:860-863` sur `senseSchema`, forme JSX invisible au détecteur), homonymie (`NATURE_INFLUENCE` = ids de Compétence) et libellé FR fabriqué DANS ENGINE pour une `ModLine` (`SIZE_LABEL`, `RANGE_BANDS` — `combat.ts:743,754` ; engine ne peut pas lire la grammaire, le contrat de `ModLine` doit porter l'id) — un nœud ne porte qu'une table -> #1714
- `OP_LABEL` (`GameOp.op` = `looseObject.superRefine` sur `OP_DEFS`, pas un `z.enum`), `ATTACK_KIND_LABELS` (`z.string()` surensemble), `LAYER_LABEL` (`keyof Layers`), `DETAIL_TEXT_LABEL` (clés d'un `z.record`) -> RAS : pas des enums, mesurés par le juge de design, hors du foyer
- `docs/structures-donnees.md` n'imprime pas la classe déclarée (« enum nommé » présent au relevé, aucune colonne) -> inventaire #1463 : colonne de classe déclarée dans `build-structures.mts`
- Aperçu de rig `[bodyPlan]` monté à l'Atelier d'un Trait (« À distance »), groupes d'onglets du Compendium non documentés, onglets de catégorie et sous-onglets de fiche homonymes (`role="tab"`), « Atelier » toggle rendu comme un onglet, `.worldmap-btn` ×4, aucune inspection des combattants en JEU (`inspectEnabled` n'a de bouton que dans l'éditeur), manœuvre à coût d'Avantage invisible sous le seuil (`combatManeuvers.ts:236`) au lieu d'une case fermée avec raison -> inventaire #1689
- `pendingCascade` hors convention `roll()/confirm()` du harnais de recette, `fight()` sans acquittement du Round 1, `fastForward()` muet sur le pending bloquant -> inventaire #1679 : outillage de recette
- `hull.propulsion`/`travel.medium` (vehicles) lus par la logique seule, `drunkenness.outcome` fait non éditable par design -> RAS : aucun affichage, aucun libellé à porter

## Recette visuelle
capture: public/qc/1694-enum-nomme/01-codex-manoeuvres-groupes.png
capture: public/qc/1694-enum-nomme/02-atelier-manoeuvre-select-kind.png
capture: public/qc/1694-enum-nomme/03-atelier-regle-optionnelle-select.png
capture: public/qc/1694-enum-nomme/04-atelier-competence-select.png
capture: public/qc/1694-enum-nomme/05-codex-phenomenes.png
capture: public/qc/1694-enum-nomme/07-atelier-activite-outcomes.png
capture: public/qc/1694-enum-nomme/08-fiche-navire-greement.png
capture: public/qc/1694-enum-nomme/09-combat-manoeuvre-libelle.png
capture: public/qc/1694-enum-nomme/10-atelier-talent-specssource.png
capture: public/qc/1694-enum-nomme/11-editeur-condition-selects.png
capture: public/qc/1694-enum-nomme/12-codex-maladie-severite.png
capture: public/qc/1694-enum-nomme/13-codex-trigger-libelle.png
capture: public/qc/1694-enum-nomme/14a-codex-traumatisme-oeil-perdu.png
capture: public/qc/1694-enum-nomme/14b-codex-traumatisme-oreille-perdue.png
capture: public/qc/1694-enum-nomme/14c-codex-critique-tete-localisation.png
capture: public/qc/1694-enum-nomme/17a-codex-gabarits-de-coque.png
capture: public/qc/1694-enum-nomme/16a-editeur-toit-profil-deux-panneaux.png
capture: public/qc/1694-enum-nomme/16b-editeur-facade-type-clocheton.png
Trains A/B1/B2 : selects et faits du Codex, de l'Atelier et de l'éditeur de scène libellés depuis le nœud (Manœuvres, Règles optionnelles, Compétences, Phénomènes arcaniques, bandes d'issue d'Activités, Dossier du navire « Cogue · Voile », Traits `specsSource` 16 options, `ConditionEditor`, sévérité de symptôme, déclencheur d'effet) — aucun pixel changé, console 0 erreur liée. Train B3 : Codex Traumatismes « perd la vue »/« perd l'ouïe » (`senseSchema`, forme articulée), catégorie « Critiques — Tête » (`critTableSchema`), gabarits de coque (7 tailles, `shipSizeSchema`), bandeau météo du hub de ville, palette d'entités de l'éditeur (« Départ héros »/« Personnage »/« Décor », `entityKindSchema`), orientation « Sud » (`dir8Schema`) ; NON prouvés à l'écran : la modale de Corruption (`exposureLevelSchema`/`chaosAlignSchema` — aucun scénario de test ne pose une corruption prête, `combatEnd` refusé pendant la cascade de Surprise : preuve par verbatim mécanique du juge et lecture de `CorruptionModal.tsx`) et le cap sur `ShipSheet`. Trouvaille de recette traitée dans le geste : `Inspector.tsx:675` gardait un second `<select>` de profil de toit en dur (« Deux pans »/« Croupe »…) divergent du nœud — unifié sur `roofProfileSchema` (`16a` : les deux panneaux rendent les mêmes libellés) ; `Inspector.tsx:533` select de feature de façade en dur sans `belfry` (affordance morte) → `facadeFeatureKindSchema` nommé, « Clocheton » offert (`16b`).

## Réfutation
verdict: CONFIRMÉ — juge de DESIGN (`juge-design-1694.md`, TIENT-SOUS-CORRECTIONS, 10 corrections portées dans l'invariant v2 : le foyer est le nœud pour une raison de FORME, `.meta()` natif zod 4.4.3 > WeakMap, `specsSource` en grammaire, stock par vocabulaire jamais par nœud, cliquet gelé et non 0, `enfantsDe` retenue) ; juges de DIFF par train (budget 30 min chacun, tous ACCEPTE-SOUS-CORRECTIONS, corrections portées) : train A — agrégation par vocabulaire en OU blanchissant 2 jumeaux muets (garde par nœud, `mutationKindSchema` partagé), refus `.extract` non structurel (garde ajoutée), `MOT_CAPITALISE` (critère structurel des tables de segments) ; train B1 — verbatim prouvé mécaniquement 19/19, demi-migration `WATER_APPLIES_TO` (le stock mentait), détecteur élargi à `{ id, label }[]` ; train B2 — verbatim 9 + 5 paires, libellés neufs DÉRIVÉS (pas inventés), exhaustivité TS prouvée par mutation, 210 nœuds reproduits, 5 corrections de forme ; train B3 — verbatim et ordre 12/12, identité des nœuds partagés prouvée à l'exécution, refus `ModLine` fondés, logique par libellé FR trouvée et corrigée avec extension de la garde. Recettes navigateur par train (VERT ; deux cas inconclus en B1 rejoués et prouvés : gréement du navire, manœuvre au Codex — le repli de libellé du journal de combat reste non prouvé à l'écran, dette préexistante `combatManeuvers.ts:522` inchangée).
