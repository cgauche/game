# Revue de palier — fenêtre 9d66de88f..993107240 — 2026-09-18

verdict: PARTIEL

Fenêtre `9d66de88f..993107240` (20 commits, 10 de substance, **8 fermetures toutes posées par le bot**) jugée en lecture seule depuis l'arbre épinglé `C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1806` — `git rev-parse HEAD` → `c3c4e4b9a2bd8ed09bfa3f061b2b87b499c6da9d`, un commit `wip(migrations) refs #1812` **hors fenêtre et écarté du jugement** ; la tête jugée `993107240` est son parent direct (`git log --oneline -3` collé). Contrôle POSITIF que le travail jugé est bien là, pas une absence de marqueur : `src/ui/Layout.tsx`, `src/ui/styles/layout.css`, `scripts/guards/lib/cssCouchesStock.mjs` (3394 lignes), `scripts/guards/lib/cssCouches.mjs`, `scripts/hooks/bootstrap-conteneur.mjs` présents et **exécutables** — `npx vitest run src/ui/ui-ratchets.test.ts` → **47/47 verts**, `npx vitest run src/ui/compendium/codex-edit-cases-a-cocher.test.tsx` → **5/5 verts**, `node --test scripts/ops/publier.test.mjs` → **87/87**. Le WIP hors fenêtre touche `structuresStock.mjs` et onze migrations : toute mesure de cliquet ci-dessous est donc prise par `git diff 9d66de88f 993107240` ou `git show 993107240:<fichier>`, jamais sur le disque. **Le cumul est le plus dense des six paliers et il progresse sur quatre axes mesurés** : les 8 fermetures portent toutes leur solde suivi, toutes posées par `github-actions[bot]` (REST collé), aucune à la main ; les **8 courses CI de `main`** couvrant les têtes de train de la fenêtre sont **toutes `success` à `run_attempt = 1`** — zéro relance ; `JUGE:`+`REFUTATION:` est à **10/10** des commits de substance ; le trailer `Claude-Session` remonte de 2/11 à **7/10** ; et le grief 3 du palier précédent reçoit son geste dans un commit DÉDIÉ qui cite la revue (`da0a35c73`, `STRUCTURES_DEFAUT` 28 → **27**, mou consommé). Ce qui ne tient pas est concentré et lourd d'un cran : **la tête publiée laisse `main` avec un Canari ROUGE** (`plans-dates=failure` — le plan daté de #1800 survit à la fermeture de #1800, issue #1815 ouverte automatiquement à 18:46:44Z), **le doc généré des doctrines date du 2026-09-04 un arbitrage utilisateur du 2026-09-18**, et **le grep que `fcaa6ef8d` donne POUR PREUVE de son invariant est réfuté à deux sites**. La classe « cliquet déplacé sans ligne `CLIQUET:` » (grief 4 du palier précédent) RÉCIDIVE sur quatre sites, et quatre griefs hérités persistent au caractère près, dont `ECRIT_LU['test'].lit` sans `scripts/` que la fenêtre elle-même AGGRAVE.

## Sortie brute de l'instrument

`npm run ops:faits-de-palier -- --base 9d66de88f --tete origin/main` — digest sans valeur réécrite :

```
KEYS base | tete | depuis | chainage | faitsChemin | commits | fermetures | stocks
     | fermeturesHorsCommit | auditStock | coursesCi | revuePrecedente | provenance
== base :: "9d66de88f"   == tete :: "origin/main"   == depuis :: "2026-09-18"   == chainage :: "vérifié"
== commits substance = 10 / total 20
== fermetures :: [#1804 → 35909cc18 · #1800 → faf3fc0ce · #1803 → b49f1b27c · #1797 → fcaa6ef8d
                  · #1791 → 5e6e88691 · #1792 → 74b952861 · #1716 → fac9288a1 · #1789 → fac9288a1]
                  (solde:true sur les HUIT)
== stocks :: {"refus":[],"notes":[],"commits":20,"plage":"9d66de88f..origin/main","indisponible":null}
== fermeturesHorsCommit :: { "disponible": true, "valeur":
     "[fermetures] fenêtre depuis 2026-09-18 · baseline 12 entrée(s)
      - aucune fermeture non citée dans la fenêtre
      **Aucun écart à la baseline.**" }
== auditStock :: "stock 5 entrée(s), observé 5 paquet(s) >= high … aucun écart au stock daté"
                 (les 5 mêmes JAUNES du 2026-09-04, routées à #1726)
== coursesCi :: 8 shas → [CI success] (993107240, 5c35a9501, b345bf37a, 2feac3dfb, 8ef2ff641,
                 7193fa723, 272176da9, 2d23e308c) ; 12 shas → "courses":[]
                 + 993107240 → [Canari "in_progress", conclusion null]  ← voir grief 1
== provenance :: commits/fermetures/stocks/chainage = script · fermeturesHorsCommit/coursesCi = gh
                 · auditStock = npm audit · revuePrecedente = git
```

`fermeturesHorsCommit` passe de `disponible:false` à `disponible:true`. **Ce n'est pas un geste** : `git log --oneline 9d66de88f..993107240 -- scripts/ops/fermetures-non-citees.mjs` → **vide**, `TITRE_CANARI = 'Canari rouge'` intact (`:45`). Le vert vient du `--depuis 2026-09-18`, qui fait sortir #1785 (fermée le 2026-09-17) de la fenêtre. Le Canari du MÊME sha, dont la fenêtre part du 2026-09-11, rend toujours le rouge, verbatim de son log : `- #1785 [NEUVE] … **1 écart(s) à la baseline :**`. Voir grief 7.

Annotations et sessions, mesurées (`git show -s --format=%B` par sha) :

```
da0a35c73 JUGE=1 REFUT=1 VISION=0 CLIQUET=0 SESSION=0
74b952861 JUGE=1 REFUT=1 VISION=1 CLIQUET=0 SESSION=1
21917783d JUGE=1 REFUT=1 VISION=0 CLIQUET=0 SESSION=1
5e6e88691 JUGE=1 REFUT=1 VISION=0 CLIQUET=0 SESSION=1
fcaa6ef8d JUGE=1 REFUT=1 VISION=1 CLIQUET=1 SESSION=1
5e11b2380 JUGE=1 REFUT=1 VISION=0 CLIQUET=0 SESSION=1
b49f1b27c JUGE=1 REFUT=1 VISION=0 CLIQUET=1 SESSION=0
faf3fc0ce JUGE=1 REFUT=1 VISION=1 CLIQUET=3 SESSION=1
5c35a9501 JUGE=1 REFUT=1 VISION=0 CLIQUET=0 SESSION=1
35909cc18 JUGE=1 REFUT=1 VISION=0 CLIQUET=1 SESSION=0
```

**10/10** portent `JUGE:` ET `REFUTATION:` (10/11 au palier précédent, 6/9 avant). **7/10** portent `Claude-Session` (2/11 au palier précédent) — voir grief 8.

## Fermetures

| # | commit qui ferme | solde suivi | restes routés | cible | verdict du solde | fermeture posée |
|---|---|---|---|---|---|---|
| #1716 | `fac9288a1` | `1716.md` | 1 + 1 `inventaire #1680` | `-> #1794` **open** | CONFIRMÉ | bot 2026-09-18T07:45:48Z |
| #1789 | `fac9288a1` | `1789.md` | 1 + 1 `inventaire #1680` | `-> #1795` **open** | CONFIRMÉ | bot 07:45:50Z |
| #1792 | `74b952861` | `1792.md` | 1 | `-> #1796` **open** | PARTIEL | bot 08:17:16Z |
| #1791 | `5e6e88691` | `1791.md` | 1 | `-> #1797` (fermée DANS la fenêtre) | PARTIEL | bot 08:45:51Z |
| #1797 | `fcaa6ef8d` | `1797.md` | 1 | `-> #1798` **open** | PARTIEL | bot 11:30:02Z |
| #1803 | `b49f1b27c` | `1803.md` | 1 | `-> #1804` (fermée DANS la fenêtre) | CONFIRMÉ après réfutation | bot 14:11:37Z |
| #1800 | `faf3fc0ce` | `1800.md` | 1 + 1 `inventaire #1811` | `-> #1805` **open** | PARTIEL | bot 15:32:07Z |
| #1804 | `35909cc18` | `1804.md` | 1 | `-> #1813` **open** | CONFIRMÉ après réfutation | bot 18:38:25Z |

Sondes collées :

```
$ git ls-tree -r --name-only 993107240 -- .claude/soldes | grep -E "(1716|1789|1791|1792|1797|1800|1803|1804)\.md"
  → les HUIT présents et SUIVIS

$ for n in 1791 1792 1797 1800 1803 1804 1716 1789 ; gh api repos/cgauche/game/issues/$n \
    --jq '[.number,.state,.state_reason,.closed_at,(.closed_by.login // "-")]|@tsv'
1791 closed completed 2026-09-18T08:45:51Z github-actions[bot]
1792 closed completed 2026-09-18T08:17:16Z github-actions[bot]
1797 closed completed 2026-09-18T11:30:02Z github-actions[bot]
1800 closed completed 2026-09-18T15:32:07Z github-actions[bot]
1803 closed completed 2026-09-18T14:11:37Z github-actions[bot]
1804 closed completed 2026-09-18T18:38:25Z github-actions[bot]
1716 closed completed 2026-09-18T07:45:48Z github-actions[bot]
1789 closed completed 2026-09-18T07:45:50Z github-actions[bot]

$ grep -o -- "-> [a-z]* *#[0-9]*" .claude/soldes/179{1,2,7}.md .claude/soldes/180{0,3,4}.md
1791 -> #1797 · 1792 -> #1796 · 1797 -> #1798 · 1800 -> inventaire #1811 ET -> #1805
1803 -> #1804 · 1804 -> #1813
```

**Les huit fermetures tiennent sur la forme** : solde suivi dans l'arbre, `verdict:` nommé, ≤ 1 reste routé (`#1800` en porte 2, dont un `inventaire`, même porte de service qu'au palier précédent), cible **OPEN** pour les cinq restes ouverts, et **zéro `gh issue close`** : les huit sont posées par `github-actions[bot]`. Les six cibles de routage neuves portent leurs dépendances **dans le CORPS**, mesurées une par une :

```
$ gh api repos/cgauche/game/issues/<N> --jq '.body' | grep -iE "bloqu|débloque|prérequis"
#1796  **Bloqué par** : rien (#1792 publié pose l'immunité de la boîte). **Débloque** : rien. …
#1798  **Bloqué par** : rien. … **Prérequis partagé avec** : #1689 (épic UI : primitives d'écran).
#1805  **Bloqué par** : #1800 (lot 1 …). **Débloque** : rien. **Prérequis partagé avec** : les lots 2-6 …
#1811  **Bloqué par** : #1800 … **Débloque** : la suppression du plan daté docs/plans/2026-09-18-1800-couche-layout.md …
#1813  Bloqué par : rien. Prérequis partagé avec #1804 (mêmes coutures REST, à extraire une fois #1804 publié).
#1806..#1810  **Bloqué par** : #1800 … (+ lot 2 pour #1809, lot 3 pour #1810) …
```

Le SENS est porté dans les deux directions, jamais laissé au rétro-lien. Réserve : #1813 écrit ses dépendances **en clair, sans gras, à la ligne 62** et non en tête comme les cinq autres — la forme n'est pas normée, la substance y est.

## Stocks et cliquets

`refus: []`, `notes: []` sur 20 commits ; `auditStock` : 5 entrées, 5 paquets `>= high`, aucun écart au stock daté — les mêmes cinq JAUNES du 2026-09-04, routées à #1726.

Tout ce qui a BOUGÉ sur un cliquet dans la fenêtre, mesuré (`git diff --numstat 9d66de88f 993107240`) :

| porteur | delta réel | ligne `CLIQUET:` ? | motif |
|---|---|---|---|
| `scripts/guards/lib/cssCouchesStock.mjs` | **création, 3349 entrées** | **OUI**, `faf3fc0ce +3349` | amorçage des trois couches CSS |
| `scripts/guards/lib/cssCouches.mjs` | création, `FEUILLES_PARTAGEES` | **OUI**, `faf3fc0ce +4` | registre de socle, source unique |
| `src/ui/ui-ratchets.test.ts` | 310+/616− | **OUI**, `faf3fc0ce +1` | `SHARED_CSS_FILES` dérivée |
| `scripts/guards/budget-contexte.mjs` | `PLAFOND_OCTETS` 26455 → **26459** | **OUI**, `35909cc18 +4` | pointeur credo `gh label list` → route REST |
| `scripts/hooks/bootstrap-conteneur.test.mjs` | +1 | **OUI**, `b49f1b27c +1` | une réponse par constat |
| `src/data/structures-contrat.test.ts` | `STRUCTURES_CIBLES` 39 → **40** | **OUI**, `fcaa6ef8d +1` | cible `formule \| minimum,of` |
| `src/data/structures-contrat.test.ts` | `STRUCTURES_DEFAUT` 28 → **27** | n/a (BAISSE) | grief 3 du palier précédent soldé |
| `scripts/guards/lib/structuresStock.mjs` | `entrees: 62 → **66**` | **NON** (`faf3fc0ce`) | grief 4 |
| `src/ui/compendium/registry-enveloppe.test.ts` | `primitives.manifest.json#0 (62) → **(66)**` | **NON** (`5c35a9501`) | grief 4 |
| `scripts/migrations/2026-08-27-l1b-2d-manifests-label.mjs` | `attendu: 62 → **66**` | **NON** (`5c35a9501`) | grief 4 |
| `scripts/migrations/2026-08-28-l1b-11b-entite-type.mjs` | `TOTAL_ATTENDU 837 → **841**` | **NON** (`5c35a9501`) | grief 4 |

Le `+3349` est **exact à l'unité**, contre-compté sur l'objet :

```
$ awk '/^export const CSS_IDENTITE_ECRAN_RATCHET/{s="ident";next} \
       /^export const CSS_ESPACEMENT_RATCHET/{s="espace";next} \
       /^export const STYLE_INLINE_RATCHET/{s="inline";next} \
       /^\];/{s=""} s&&/^  \{ fichier/{c[s]++} \
       END{for(k in c) print k,c[k]; print "TOTAL",c["ident"]+c["espace"]+c["inline"]}' \
      scripts/guards/lib/cssCouchesStock.mjs
ident 2339 · espace 910 · inline 100 · TOTAL 3349
```

**La hausse de 3349 porte sa contrepartie, et elle est mesurable** : (a) le cliquet (xii) et `CLASS_SELECTOR_BASELINE`/`DOMAIN_CSS_MODULES` sont **supprimés** (`grep -rl` sur `src/ scripts/ docs/` → les deux noms ne survivent que dans le plan daté) ; (b) le stock est **décroissant-seulement, sans plafond**, et il porte un contrat de VIE — `cssCouchesStock.mjs:21-24` : « la garde échoue (a) sur tout site NEUF … (b) sur toute entrée dont le site a disparu » : une entrée morte ROUGIT, ce n'est pas une liste d'absolution ; (c) l'exemption est au **SITE** (`<sélecteur> :: <prop> :: <valeur>`, `<balise>[.classe] :: <clés>`), jamais au fichier ; (d) les trois angles morts sont DITS en tête (`:30-33`, substitution à compte égal, ré-ordinalisation, renommage) ; (e) la résorption est ticketée en sept issues **OPEN** portant leurs dépendances (#1811, #1805, #1806-#1810). Le `+4` de `budget-contexte.mjs` est exact au caractère : `gh label list` (13 octets) → `gh api …/labels` (17 octets, `…` sur 3 octets UTF-8), et le credo porte bien la nouvelle rédaction (`.claude/credo.md:11`, `grep "gh label list" .claude/credo.md` → **aucune ligne**).

## Griefs de la revue précédente — suivi, un par un

| grief du 2026-09-18 (`cd9bb3299..9d66de88f`) | état | mesure |
|---|---|---|
| 1 — `CLIQUET:` citant `296965162`, référence inexistante | **SANS RÉCIDIVE sur la forme, mais voir grief 4** | aucune ligne `CLIQUET:` de la fenêtre ne cite d'empreinte ni de SHA : les six posées nomment un chemin + un delta, toutes **exactes** au contre-comptage (table ci-dessus). Geste reçu sur la classe « identifiant non relu » |
| 2 — solde #1789 « 22 options » vs 23 mesuré | **RÉSORBÉ** | corrigé au solde avant clôture, #1789 fermée par le bot le 2026-09-18T07:45:50Z. Aucun cardinal d'écran nu dans les six soldes neufs |
| 3 — `STRUCTURES_DEFAUT` relevé 27 → 28 sans contrepartie | **GESTE REÇU, exemplaire** | `git diff 9d66de88f 993107240 -- src/data/structures-contrat.test.ts` → `-['STRUCTURES_DEFAUT', …, 28]` / `+['STRUCTURES_DEFAUT', …, 27]`, dans un commit **dédié** (`da0a35c73`) dont le corps cite la revue : « Trouvé par la revue de palier `cd9bb3299..9d66de88f` (grief 3) », avec `JUGE:` et `REFUTATION:` sur l'attaque « redescendre un plafond publié casse `main` ». C'est la première fois qu'un grief de palier est soldé par un commit qui le NOMME |
| 4 — cliquets déplacés sans ligne `CLIQUET:` | **RÉCIDIVE, 4 sites** | voir grief 4 ci-dessous |
| 5 — `ECRIT_LU['test'].lit` omet `scripts/` | **PERSISTANT ET AGGRAVÉ, 4ᵉ palier** | voir grief 6 |
| 6 — trailer `Claude-Session` 9/11 absents | **REDRESSÉ** | 3/10 absents (`da0a35c73`, `b49f1b27c`, `35909cc18`). Trajectoire 8/10 → 5/10 → 3/9 → 9/11 → **3/10**. Voir grief 8 |
| 7 — exemption au FICHIER portant un cardinal | **PERSISTANT, 4ᵉ palier** | `grep -n "classerPush.mjs" src/ui/registry-id-branch-guard.test.ts` → `161:  'scripts/gates/classerPush.mjs': 1,`. Intacte. Point positif : la fenêtre n'ajoute **aucune** exemption au fichier — le stock neuf de 3349 entrées est nominatif AU SITE |
| 8 — `-> inventaire #N` hors plafond ; `--diff-filter=D` absent (#1777) | **PERSISTANT, atténué (2 → 1)** | `grep -o "inventaire #[0-9]*"` sur les six soldes neufs → **un seul**, `1800.md -> inventaire #1811`. Et `grep -n "diff-filter" scripts/guards/lib/revuePalier.mjs` → **une seule ligne, `:118`, `--diff-filter=A`** : une archive de revue reste supprimable sans porte, #1777 **OPEN** |

## Griefs de ce palier

**Grief 1 — la tête publiée `993107240` laisse `main` avec un Canari ROUGE : le plan daté de #1800 survit à la fermeture de #1800, et le rouge est CAUSÉ par la fenêtre.** L'instrument a photographié le Canari `in_progress` ; il a depuis conclu.

```
$ gh api "repos/cgauche/game/actions/runs?branch=main&per_page=30" \
    --jq '.workflow_runs[]|select(.name=="Canari")|[.head_sha[0:9],.run_attempt,.status,.conclusion,.created_at]|@tsv'
993107240	1	completed	failure	2026-09-18T18:36:21Z
75692d80c	1	completed	success	2026-09-16T10:14:30Z

$ gh api repos/cgauche/game/actions/runs/35381156775/jobs --jq '.jobs[]|[.name,.conclusion]|@tsv'
build	failure
$ …/logs → "18_Plans datés — ancre Ticket encore OUVERTE.txt" :
docs:check-plans — 1 violation(s) :
  docs/plans/2026-09-18-1800-couche-layout.md:3  [plan exécuté, à supprimer]  #1800 fermée(s) —
  la politique docs/ veut la SUPPRESSION du plan (git porte l'historique)
Un plan exécuté se SUPPRIME (git porte l'historique) ; une citation de plan mort se RETIRE de la ligne.
##[error]Process completed with exit code 1.
→ "### Mesures ROUGES\n- plans-dates=failure" · "issue ouverte" · "canari ROUGE : 1 mesure(s)"

$ gh api "repos/cgauche/game/issues?state=open&per_page=100" --jq '.[]|select(.title|test("[Cc]anari"))|[.number,.created_at,.title]|@tsv'
1815	2026-09-18T18:46:44Z	Canari rouge — environnement ou suite cassés
```

Le plan est **ajouté par la fenêtre** (`2941cae86`, `docs/plans/2026-09-18-1800-couche-layout.md`) et #1800 est **fermée par la fenêtre** (bot, 15:32:07Z) : la garde qui exige une ancre OUVERTE ne pouvait que rougir. Le lot le SAVAIT — le corps de #1811 écrit « **Débloque** : la suppression du plan daté `docs/plans/2026-09-18-1800-couche-layout.md` » — et a publié quand même. C'est le premier Canari rouge depuis au moins le 2026-09-16 (la course précédente, `75692d80c`, est verte) et il a ouvert une issue de bruit, #1815, dont le titre est précisément celui que #1793 décrit comme empoisonnant l'instrument de palier. Les 8 courses `CI` sont vertes : c'est le workflow `Canari`, joué à part, qui porte le rouge — un juge de diff ne pouvait pas le voir, un juge de palier oui.

→ attendu : un plan daté se RÉ-ANCRE sur le ticket qui le fait vivre (#1811 est OPEN et le revendique) ou se SUPPRIME dans le train qui ferme son ancre ; la porte `check-plans-anchors.mjs` doit être jouée **avant** la publication, pas seulement au Canari nocturne. `fichier:ligne` : `docs/plans/2026-09-18-1800-couche-layout.md:3` ; `scripts/docs/check-plans-anchors.mjs` ; run 35381156775. **BLOQUANT — `main` est rouge au moment de cette revue.**

**Grief 2 — `docs/doctrines.md` date du 2026-09-04 un arbitrage utilisateur du 2026-09-18, et c'est la fenêtre qui l'a fait mentir.** Le doc GÉNÉRÉ dont CLAUDE.md dit qu'il porte les doctrines « verbatim daté » :

```
$ git diff 9d66de88f 993107240 -- docs/doctrines.md | grep -E "^[-+].*primitives-comme-les-donnees"
- **user-doctrine-ui-coherente-par-primitives-comme-les-donnees** (2026-09-04, 4 verbatims) :
  « N'oublie pas les concepts lié a l'UI et les primitives. … »
+ **user-doctrine-ui-coherente-par-primitives-comme-les-donnees** (2026-09-04, 7 verbatims) :
  « Un module CSS d'écran ne déclare que du PLACEMENT (grille, gap, largeur, position) ; toute
    IDENTITÉ … vit dans une primitive. Le nombre de classes n'est plus compté »
$ git show 993107240:docs/doctrines.md | grep -c "Arbitrages (2026-09-18"   → 0
```

L'extrait BASCULE sur l'arbitrage **A1 du 2026-09-18** (`#1800`) et la date reste **2026-09-04**, celle d'un verbatim différent, antérieur de quatorze jours. La cause est GÉNÉRALE et lisible : `scripts/docs/build-doctrines.mjs:151` prend la date « du PARAGRAPHE du verbatim », et `paragrapheDe` découpe sur `\n\s*\n` (`:140`) ; or la fiche empile ses quatre verbatims datés **sans ligne blanche** (`.claude/memory/user-doctrine-ui-coherente-par-primitives-comme-les-donnees.md:8-11`), donc `DATE_ISO.exec` rend la PREMIÈRE date du bloc. Le générateur documente pourtant son propre contrat en tête : « La DATE est celle du paragraphe du verbatim ». Toute fiche à plusieurs verbatims datés collés est touchée. L'enjeu n'est pas cosmétique : le credo réserve la CÉRÉMONIE datée à l'arbitrage utilisateur, et c'est ce doc qui la rend.

→ attendu : `paragrapheDe`/`dateDe` prennent la date de la LIGNE du verbatim retenu (chaque verbatim de fiche est une ligne `**Titre (AAAA-MM-JJ)** : « … »`), pas la première date du bloc ; garde : une fiche-fixture à deux verbatims datés collés doit rendre la date du verbatim EXTRAIT. `fichier:ligne` : `scripts/docs/build-doctrines.mjs:140,151` ; `docs/doctrines.md:38` ; `.claude/memory/user-doctrine-ui-coherente-par-primitives-comme-les-donnees.md:8-11`. **MAJEUR — un doc vivant, généré, qui ment sur une date d'arbitrage utilisateur.**

**Grief 3 — le grep que `fcaa6ef8d` donne POUR PREUVE de son invariant est réfuté à deux sites, dont un plancher de durée en ROUNDS sans réf.** Le corps de `fcaa6ef8d` (#1797, FERMÉE) annonce : « TOUS les planchers de durée du moteur sont retirés — `git grep "Math.max(1" -- src/engine src/state` ne rend plus aucune durée. » Rejoué sur la tête de fenêtre :

```
$ git grep -nE "(dur|duration|duree|minutes|rounds)[A-Za-z]* = Math\.max\(1" 993107240 -- src/engine src/state
993107240:src/engine/disease.ts:193:   const dur = Math.max(1, opts?.duration != null ? opts.duration * MINUTES_PER_DAY : rollDiseaseTime(def.duration, rng));
993107240:src/state/combatFlow.ts:5565: const durationMult = Math.max(1, extras?.durationMult ?? 1);
993107240:src/state/combatManeuvers.ts:440: const dur = Math.max(1, bonus(effectiveChar(attacker, 'endurance')));
993107240:src/state/riverVoyageFlow.ts:605: const rounds = Math.max(1, be); // plancher : au moins UNE tentative …
993107240:src/state/tavernFlow.ts:1623: const rounds = Math.max(1, seq.round);
```

Deux de ces cinq sont bien des DURÉES au sens du ticket. `src/state/combatManeuvers.ts:440` est le plus net : la zone de fumée de `souffle-fumee` est posée `rounds: dur` (`:446`) avec `dur = Math.max(1, BE)`, alors que le commentaire du site (`:438`) dit « la zone bloque les Lignes de vue pendant **BE Rounds** » — **aucun minimum au livre n'y est cité**, exactement le motif que #1797 qualifie de règle inventée et dit avoir éradiqué (un BE de 0 rend 1 Round au lieu de 0, le jumeau littéral de la « Peau d'écorce » que la passe de réfutation du même ticket a fait corriger). `src/engine/disease.ts:193` plancher à 1 minute une durée de maladie, sans réf. `combatFlow.ts:5565`, `riverVoyageFlow.ts:605` et `tavernFlow.ts:1623` sont des normalisations de multiplicateur et des comptes de tentatives : hors classe, dits ici pour ne pas les rejuger. Le défaut n'est pas la survivance de deux sites dans un moteur de cette taille — c'est qu'ils sont **dans le périmètre littéral de la preuve citée**, et que la fermeture repose sur cette preuve.

→ attendu : la borne de `souffle-fumee` migre dans la FORMULE (`{minimum, of}`, la grammaire que le même lot vient de poser) avec sa réf, ou le plancher tombe ; un grep donné pour preuve dans un message de fermeture est rejoué tel quel avant le commit. La classe ne se ferme pas par un site : un test « aucun plancher de durée sans réf dans `src/engine`+`src/state` » est ce que le lot aurait dû laisser. `fichier:ligne` : `src/state/combatManeuvers.ts:440,446` ; `src/engine/disease.ts:193` ; `git show -s --format=%B fcaa6ef8d`. **MAJEUR — #1797 est fermée sur une preuve fausse.**

**Grief 4 — quatre cliquets ont bougé sans la moindre ligne `CLIQUET:` : la classe du grief 4 du palier précédent RÉCIDIVE, et trois des quatre sont le MÊME nombre recopié.** Mesuré au `git diff 9d66de88f 993107240` (table du § Stocks) :

```
$ git diff 9d66de88f 993107240 -- scripts/guards/lib/structuresStock.mjs | grep "^[-+].*entrees:"
-  { … document: "primitives.manifest.json", chemin: "(entrées)", entrees: 62, … }
+  { … document: "primitives.manifest.json", chemin: "(entrées)", entrees: 66, … }
   ← faf3fc0ce, qui pose pourtant TROIS lignes CLIQUET: (cssCouchesStock, cssCouches, ui-ratchets)

$ git diff 9d66de88f 993107240 -- src/ui/compendium/registry-enveloppe.test.ts
-  "primitives.manifest.json#0 (62)",
+  "primitives.manifest.json#0 (66)",
$ git show 993107240:scripts/migrations/2026-08-27-l1b-2d-manifests-label.mjs | grep -n "attendu: 66"
41:  { fichier: 'src/data/primitives.manifest.json', entrees: (d) => d, attendu: 66 },
$ git show 993107240:scripts/migrations/2026-08-28-l1b-11b-entite-type.mjs | grep -n "TOTAL_ATTENDU"
97:const TOTAL_ATTENDU = 841;
   ← les trois par 5c35a9501, mesuré CLIQUET=0
```

Les quatre nombres sont JUSTES — `git show 993107240:src/data/primitives.manifest.json | grep -c '"id"'` → **66**, à l'unité. Le défaut est le CANAL : `git log --grep=CLIQUET -- src/ui/compendium/registry-enveloppe.test.ts` ne rend rien pour un gel qui vient de bouger, et le palier suivant ne pourra pas le lire. Les raisons existent et sont bonnes (commentaires datés au site, prose du message), mais elles ne sont pas là où la revue regarde. C'est la RÉCIDIVE exacte du grief 4 du 2026-09-18, dont l'attendu était : « une ligne `CLIQUET:` par FICHIER dont un cran bouge, plafond compris — ou une garde de pré-commit ». Le geste attendu n'a pas été posé, et la fenêtre a pourtant produit six lignes `CLIQUET:` parfaites : ce n'est pas l'ignorance de la règle, c'est l'absence de porte.

→ attendu : la garde de pré-commit déjà décrite au palier précédent — pour tout fichier du corpus « cliquet/stock/baseline/cardinal gelé » modifié par un commit, exiger une ligne `CLIQUET:` nommant ce chemin (le corpus est énumérable, `stocksNominatifs.mjs` le connaît). `fichier:ligne` : `scripts/guards/lib/structuresStock.mjs` (`entrees` 62→66, `faf3fc0ce`) ; `src/ui/compendium/registry-enveloppe.test.ts`, `scripts/migrations/2026-08-27-l1b-2d-manifests-label.mjs:41`, `scripts/migrations/2026-08-28-l1b-11b-entite-type.mjs:97` (`5c35a9501`, `CLIQUET=0`). **MOYEN, RÉCIDIVE.**

**Grief 5 — la dette de synchronisation créée par #1800 n'entre dans AUCUN solde de la fenêtre, et son ticket naît SEPT MINUTES après la fermeture qu'il aurait dû grever.** Une primitive de plus au manifeste force **quatre** recalages manuels du même cardinal (grief 4), soit exactement le smell que le credo nomme : « Une garde de synchronisation est un smell : une seule source de vérité. » Le lot l'a vécu — `5c35a9501` existe parce que la CI de branche a rougi dessus (« CI de branche rouge, course 35358034541 ») — et ne l'a pas consigné :

```
$ git log --oneline 9d66de88f..993107240 --grep="1812"    → (vide)
$ git grep -n "1812" 993107240 -- .claude/soldes           → (vide)
$ gh api repos/cgauche/game/issues/1812 --jq '[.number,.state,.created_at,.title]|@tsv'
1812  open  2026-09-18T15:39:10Z  Migrations et test gèlent le CARDINAL du manifeste des primitives :
      trois recalages par primitive neuve (62→66 au #1800)
   ← ouverte 15:39:10Z ; #1800 fermée par le bot à 15:32:07Z
```

Le solde `1800.md` route `-> #1805` et `-> inventaire #1811` ; il ne porte pas cette classe. Conséquence mesurable : la lecture de palier, qui compte les restes AU SOLDE, ne voit pas une dette que le lot a créée et découverte lui-même. Et le ticket sous-compte : il annonce « **trois** recalages », les mesures ci-dessus en rendent **quatre** (deux migrations, `structuresStock.mjs`, `registry-enveloppe.test.ts`).

→ attendu : une dette découverte PENDANT le lot entre au solde du lot, avec son `-> #N`, avant la fermeture — sinon la fermeture porte un solde faux ; et le corps de #1812 se recale sur quatre sites nommés. `fichier:ligne` : `.claude/soldes/1800.md` (restes) ; corps de #1812. **MOYEN.**

**Grief 6 — `ECRIT_LU['test'].lit` omet toujours `scripts/`, 4ᵉ palier consécutif, et la fenêtre AGGRAVE la fausseté de cette déclaration.** Le grief n'est plus théorique :

```
$ sed -n '211,221p' scripts/gates/toutes.mjs
211:  test: {
220:    lit: ['src/', 'server/src/', 'scripts/map/', 'docs/', 'Source/', '.gitattributes', 'vite.config.ts'],

$ git grep -n "from '\.\./\.\./scripts/" 993107240 -- src/ui/ui-ratchets.test.ts
:5  import { readCorpus } from '../../scripts/guards/lib/sourceCorpus.mjs';
:6  import { estFichierVitest } from '../../scripts/guards/lib/fichierVitest.mjs';
:7  import { FEUILLES_PARTAGEES, reglesCss } from '../../scripts/guards/lib/cssCouches.mjs';   ← NEUF (#1800)
:16 } from '../../scripts/guards/lib/cssCouchesAudit';                                         ← NEUF (#1800)
:21 } from '../../scripts/guards/lib/cssCouchesStock.mjs';                                     ← NEUF (#1800)
:22 import { cleDeSite, ecartDuVolet } from '../../scripts/guards/lib/stock.mjs';

$ git grep -l "scripts/guards" 993107240 -- 'src/**/*.ts' 'src/**/*.tsx' | wc -l   → 20+ fichiers de test
```

La lane `test` lit donc `scripts/guards/lib/` à plus de vingt endroits, dont **trois imports neufs posés par cette fenêtre même**, pendant que sa déclaration `lit` ne mentionne que `scripts/map/`. Une déclaration d'écrivains/lecteurs qui se trompe de périmètre est une garde qui ment sur ce qu'elle protège.

→ attendu : `scripts/guards/lib/` (a minima) entre dans `ECRIT_LU['test'].lit`, ou la déclaration se DÉRIVE des imports réels au lieu d'être recopiée. `fichier:ligne` : `scripts/gates/toutes.mjs:220`. **MOYEN, 4ᵉ palier.**

**Grief 7 — le vert de l'instrument sur `fermeturesHorsCommit` est un effet de FENÊTRE, pas un geste ; trois réserves d'outillage restent intactes.** Mesures :

```
$ git log --oneline 9d66de88f..993107240 -- scripts/ops/fermetures-non-citees.mjs   → (vide)
$ grep -n "TITRE_CANARI" scripts/ops/fermetures-non-citees.mjs
45:export const TITRE_CANARI = 'Canari rouge'
$ logs du Canari 35381156775, fichier "fermetures.md" :
  [fermetures] fenêtre depuis 2026-09-11 · baseline 12 entrée(s)
  - #1785 [NEUVE] 2026-09-17T18:33:12Z par github-actions[bot] · completed · canari
  **1 écart(s) à la baseline :** … ROUGE FERMETURE NEUVE hors baseline …
$ grep -c "run_attempt" scripts/ops/faits-de-palier.mjs        → 0
$ grep -n "diff-filter" scripts/guards/lib/revuePalier.mjs     → 118: '--diff-filter=A'
$ gh api …/issues/1793 · 1777 → les DEUX open
```

Le détecteur qui rougit sur son propre signaleur est **inchangé au caractère près** ; il rend vert ici seulement parce que `--depuis 2026-09-18` exclut #1785. Et l'issue **#1815**, ouverte par le Canari de cette fenêtre (grief 1), porte exactement le titre `Canari rouge` que #1793 décrit comme le poison : le prochain palier repartira rouge sur l'instrument, pour une fermeture que la fenêtre a fabriquée. Réserves d'outillage reconduites : `faits-de-palier.mjs` ne lit toujours pas `run_attempt` (ici sans conséquence — vérifié à l'API, les 8 courses sont à `run_attempt = 1`), et `revuePalier.mjs:118` ne regarde que les ajouts d'archive (#1777).

→ attendu : #1793 au prochain lot d'outillage (exemption dérivée du registre des workflows, pas clouée au titre) ; `coursesCi` remonte `run_attempt` ; `revuePalier` regarde aussi `D`. `fichier:ligne` : `scripts/ops/fermetures-non-citees.mjs:45` ; `scripts/ops/faits-de-palier.mjs` ; `scripts/guards/lib/revuePalier.mjs:118`. **MOYEN pour l'instrument.**

**Grief 8 — le trailer `Claude-Session` manque encore sur 3 des 10 commits de substance, et les trois sont des commits d'OUTILLAGE.** Mesure collée au § Instrument : absents sur `da0a35c73` (#1716, cliquet), `b49f1b27c` (#1803, bootstrap conteneur) et `35909cc18` (#1804, pilotage REST) ; présents sur les sept autres, tous du canal `Claude Fable 5.1`. Le redressement est net (9/11 absents → 3/10) et il vient du canal de publication ; les trois manquants sont d'autres sessions. Le motif reste celui nommé aux deux paliers précédents : le trailer dépend du rédacteur, pas de l'outil.

→ attendu : inchangé — le trailer se pose par `ops:publier`, comme `Co-Authored-By` ; à défaut, la porte de commit refuse un commit de substance sans trailer de session. `fichier:ligne` : `git log 9d66de88f..993107240 --format='%h %(trailers:key=Claude-Session,valueonly)'`. **MINEUR.**

## Ce qui résiste

**Les fermetures.** Huit tickets fermés, huit soldes SUIVIS dans l'arbre à la tête de fenêtre, huit `verdict:` nommés (4 CONFIRMÉ dont 2 « après réfutation », 4 PARTIEL — la passe adverse concède des BLOQUANTS réels sur #1803 et #1804, pas des points de disposition), **zéro fermeture à la main** : les huit sont posées par `github-actions[bot]`, REST collé. Les six cibles de routage neuves sont OPEN et portent leurs dépendances dans le CORPS, dans les DEUX sens (« Bloqué par » / « Débloque » / « Prérequis partagé avec »), y compris la chaîne interne des lots 2-6 (#1809 bloqué par le lot 2, #1810 par le lot 3) : c'est la première fenêtre où la grammaire de dépendance est tenue sur l'intégralité des restes.

**La CI de publication.** Huit courses `CI` sur `main` pour les têtes de train de la fenêtre, **toutes `success`, toutes à `run_attempt = 1`** (API collée) : zéro relance sur huit trains publiés en une journée. Le rouge du palier est sur un autre workflow, et il est de CONTENU, pas de flakiness.

**Le socle du lot #1800, mesuré et non pris sur parole.** `+3349` exact à l'unité (2339/910/100, comptage awk collé) ; l'exemption est au SITE ; le stock porte un contrat de VIE qui rougit sur toute entrée morte ; les angles morts sont DITS en tête du fichier ; la contrepartie est la SUPPRESSION du cliquet (xii) et de ses deux baselines, contre-grepée — `row-flex`, `panel-grid`, `span-2`, `layout-sidebar`, `test-scenarios.css`, `DOMAIN_CSS_MODULES`, `CLASS_SELECTOR_BASELINE` ne survivent **que dans le plan daté**, aucun résidu dans `src/` ni `scripts/` ; `.master-detail`/`-detail` ne sont plus des sélecteurs (seul `.master-detail-list` vit, consommé par `MasterDetail.tsx:110`). `npx vitest run src/ui/ui-ratchets.test.ts` → **47/47 verts**.

**L'interaction #1792 × #1800, sondée et saine.** La garde de cascade des cases DÉRIVE sa liste de feuilles de `styles.css` (`codex-edit-cases-a-cocher.test.tsx:19` : `[...STYLES.matchAll(/@import '\.\/styles\/([^']+)'/g)]`), donc `layout.css` — inséré en 4ᵉ position par #1800 — y entre **automatiquement** ; et `grep -nE "input|!important" src/ui/styles/layout.css` → **aucune ligne** : la couche neuve ne peut pas réécraser la boîte 18×18. Rejoué : **5/5 verts**.

**Le lot #1804, prouvé.** `node --test scripts/ops/publier.test.mjs` → **87/87**, dont le contrat POSITIF sur l'argv (`gh` par `api` seulement, jamais `graphql`, ne ferme rien). L'interaction #1803 × #1804 est saine : le constat `gh` du hook de conteneur est `gh --version` (`bootstrap-conteneur.mjs:81`), pas une route GraphQL refusée en session.

**Les docs vivantes, trois sondes ciblées, deux exactes.** `docs/reprise-apres-pause.md:111-112` annonce « 15 scripts déclarés dans `.claude/settings.json` … sur 17 fichiers `.mjs` hors test sous `scripts/hooks/` » — recompté sur la tête de fenêtre : **15** chemins `scripts/hooks/*.mjs` distincts dans `settings.json`, **17** `.mjs` hors `.test.mjs` sous `scripts/hooks/` : exact aux deux, et `SessionStart` est déclaré au tableau (`:166`). `docs/primitives.md:60` porte la couche de placement (`Stack/Row/Grid/Split`, `src/ui/Layout.tsx`, garde `(xxi)/(xxii)`) et `docs/charte-ui.md` la catalogue (`:196` `.stack` (`Stack`), `:62-63` le réflexe) sans résidu de (xii) (`grep "(xii)\|nombre de classes\|DOMAIN_CSS" docs/charte-ui.md` → aucune ligne). La troisième sonde est le grief 2.

**L'arbitrage utilisateur, consigné dans les formes.** `.claude/memory/user-doctrine-ui-coherente-par-primitives-comme-les-donnees.md:10-11` porte le Cap et les deux arbitrages A1/A2 en **verbatim, datés 2026-09-18, avec leur ticket** — la cérémonie est tenue à la fiche ; c'est le RENDU généré qui se trompe de date (grief 2), pas la consignation.

**Et un précédent qui compte** : `da0a35c73` est le premier commit de l'histoire de ce dépôt à solder un grief de palier en le NOMMANT dans son corps (« Trouvé par la revue de palier `cd9bb3299..9d66de88f` (grief 3) »), avec sa propre passe de réfutation sur l'attaque évidente. La boucle revue → geste se ferme, mesurée.

## Non couvert par ce jugement

(a) `npm test`, `npm run gates`, `build`, `typecheck`, `lint`, `docs:build` **non rejoués** (consigne machine) : quatre exécutions ciblées seulement, toutes vertes et collées. (b) **Aucun contrôle visuel** : les captures `public/qc/soldes/1792-*.png` (4), `1797-*.png` (5) et `1800-*.png` (20) annoncées par les trois `JUGE-VISION:` sont prises pour vraies, jamais ouvertes. (c) Les **mutations annoncées** (couleur en module d'écran → rouge, `@media` n'abrite rien, `min-height` sans `!important` → 2 rouges, borne ignorée → 3 rouges) ne sont pas rejouées. (d) Le **rejeu des migrations** (`migrations:replay:head`) n'est pas joué : les quatre cardinaux sont contre-comptés statiquement sur l'objet. (e) Les mesures de conteneur de #1803 (`git fetch --unshallow` 22,9 s, `apt-get install gh`) ne sont pas reproductibles depuis cette machine : **non vérifiées**. (f) Le contenu GitHub des tickets n'est recroisé que sur `number/state/state_reason/closed_at/closed_by/title/body`. (g) Le WIP `c3c4e4b9a` est hors fenêtre et n'est pas jugé ; ses fichiers (`structuresStock.mjs`, migrations) ont été lus par `git show 993107240:` pour éviter toute contamination.

## Verdict

verdict: PARTIEL

**Huit tickets fermés en un jour, huit soldes suivis, huit courses CI vertes à la première tentative, `JUGE:`/`REFUTATION:` à 10/10, le trailer de session qui remonte de 2/11 à 7/10, six lignes `CLIQUET:` toutes exactes au contre-comptage, un stock de 3349 entrées amorcé au SITE avec contrat de vie, angles morts dits, contrepartie grepée et sept tickets de résorption OPEN portant leurs dépendances dans les deux sens, et — première du dépôt — un commit dédié qui solde un grief de palier en le nommant** : le cumul de cette fenêtre est le plus dense et le mieux instrumenté des six paliers. Ce qui le fait basculer en PARTIEL tient en trois mesures et une récidive : **(1)** la tête publiée laisse `main` avec un **Canari ROUGE** — `plans-dates=failure`, `docs/plans/2026-09-18-1800-couche-layout.md:3` survit à la fermeture de #1800, rouge CAUSÉ par la fenêtre, connu d'avance (le corps de #1811 l'écrit), premier rouge depuis le 2026-09-16, et il a ouvert l'issue de bruit **#1815** dont le titre est précisément le poison que #1793 décrit ; **(2)** `docs/doctrines.md` — le doc généré dont la raison d'être est le verbatim DATÉ — bascule son extrait sur l'arbitrage utilisateur A1 du **2026-09-18** en gardant la date **2026-09-04**, par une cause générale lisible à `build-doctrines.mjs:140,151` ; **(3)** le grep que `fcaa6ef8d` donne POUR PREUVE (« aucune durée ne reste plancherée ») est réfuté sur `combatManeuvers.ts:440`, où la zone de fumée porte `Math.max(1, BE)` en Rounds sans réf de minimum — le jumeau exact de la règle inventée que la passe de réfutation du même ticket avait fait corriger, et #1797 est fermée là-dessus ; **(4)** la classe « cliquet déplacé sans ligne `CLIQUET:` » RÉCIDIVE sur quatre sites (`structuresStock.mjs` 62→66 par un commit qui pose pourtant trois `CLIQUET:`, `registry-enveloppe.test.ts`, deux migrations), l'attendu du palier précédent — une porte de pré-commit — n'ayant reçu aucun geste. S'y ajoutent une dette de synchronisation créée et vécue dans le lot mais absente de tout solde (#1812, ouverte sept minutes APRÈS la fermeture de #1800, et qui sous-compte trois recalages là où il y en a quatre), et quatre réserves héritées inchangées : `ECRIT_LU['test'].lit` sans `scripts/` — **aggravée**, la fenêtre ajoutant trois imports neufs de `scripts/guards/lib/` dans la lane `test` —, l'exemption au FICHIER portant un cardinal (`registry-id-branch-guard.test.ts:161`, 4ᵉ palier), `--diff-filter=A` seul (#1777 OPEN) et `run_attempt` toujours illisible à l'instrument. Le vert neuf de `fermeturesHorsCommit` n'est pas un geste : le script est intouché, seule la fenêtre a bougé — et le Canari du même sha le prouve en rendant encore le rouge #1785.
