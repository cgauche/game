# Solde #1771 « opera-plan : le rez sous la galerie reste COUVERT — aucune masse déclarée par le plan ASCII, clearedSpace ne dégage rien » — 2026-09-16

VERIFIE: le DoD relu clause par clause — (1) question 1 (masse émise par le générateur ASCII ou plancher dégagé par lui-même) tranchée « ni l'un ni l'autre : le bâti est une DONNÉE authorée, l'opéra est une INSTANCE du cas canonique `zones-pieces.ts`/Diligence » (design daté au ticket, commentaire 5693359969 ; aucun socle touché, donc pas de workflow `juge-design-socle` — la lentille « architecture à rebours » du juge de diff du 2026-09-16 l'a attaquée et rend TIENT : N+1 = une entrée `architecture` + une légende, `roomZoneIds` dérivés, aucune couture moteur) ; question 2 tranchée par l'utilisateur (AskUserQuestion du 2026-09-16 avec captures, verbatim consigné au ticket, commentaire 5694410559 : « Étage retiré en bloc » — la pièce occupée et tout le rez se lisent, l'étage disparaît entier, même loi que la Diligence ; toit dérivé : « Oui, il ne se voit qu'à l'extérieur ») ; (2) `goto({x:3,y:4,z:0})` sur `opera-plan` montre la salle verte MEUBLÉE (banc, table, coiffeuses des pièces voisines) sans silhouette — capture `public/qc/soldes/1771-opera-plan-salle-verte.png` ; (3) couturières `c25` (`goto` (32,26), les cases des meubles refusent le pas : tables, tabourets, portants, lampes visibles) et réserves `r27` (`goto` (36,37) : tonneaux, caisses, coffre) — captures `1771-opera-plan-coulisses.png`, `1771-opera-plan-reserves.png` ; (4) `viewLevel(0)` en iso : son JSDoc (`src/state/devtools.ts`) et la rangée de `docs/recette-navigateur.md` disent qu'il n'isole l'étage qu'en vue du dessus (mesuré : `MondeDeCampagne.tsx` ne filtre `activeZ` que sous `planVue` ; rejoué à la recette : « ✓ étage affiché : 0 » sans changement du monde peint) ; (5) recette du 2026-09-16 par le kit CDP du dépôt (`scripts/recette/lib.mjs`, port 5234, Chromium propre — le navigateur Playwright MCP est un verrou MACHINE tenu par une session voisine, recetteur bloqué 15 min à l'ouverture), `__wfrp` pour le setup et l'observation (`scenario`, `ready` voile tombé après 5-6 s, `goto`, `pickTileAt`), console 0 erreur 0 avertissement sur les deux passes ; goût du mobilier des pièces du rez (#1644 parqué) : jugé sur les trois captures — meubles posés au sol, adossés aux murs, aucun bloque un passage nommé au DoD, aucune remarque de goût de l'utilisateur au-delà des créneaux de l'étage (#1180). Cause unique mesurée puis levée en DONNÉE : `floorplan.ts` déclare le corps `opera-staatsoper` (2 storeys) et 25 pièces de la légende du plan NADJ (folio 38/39, lue sur l'image du PDF VF) ; `clearedSpace` en salle verte : `roomlessCells` 4200 → 0, `zoneCells` 84, `overheadCells` 2100 (`floorplan.test.ts`, 17 tests dont 6 neufs). Commit de lot `765e1d111`.

## Restes
- 12 pièces de la légende de l'ÉTAGE sans zone (10 aires closes de flanc pour 9 loges attendues ; balcons, galerie, bar, salons fondus dans l'aire ouverte de 1140 cases) et 8 cloisons du plan absentes de l'ASCII (le calque les compense par découpe ; pathfinding et murs peints restent faux) → un allié à l'étage hors « Loge royale » dégage l'EMPRISE entière (1452/1488 cases nues, loi `clearedSpace`, capture `1771-opera-plan-galerie-z1.png`) -> #1179
- Murs de l'étage rendus CRÉNELÉS (capture `1771-opera-plan-galerie-z1.png`, remarque de l'utilisateur du 2026-09-16 : « les murs là c'est des créneaux non ? ») : apparence de mur fortifiée par ALTITUDE, défaut préexistant que la galerie à 5 m rend plus visible -> inventaire #1680 : #1180 ouvert (« l'opéra a 1260 merlons intérieurs »), lot suivant de la séquence des restes
- Légende du plan = IMAGE dans le PDF VF, aucun texte dans l'extraction Markdown (`08 - Une nuit a l'Opera.md` l.22→51, folios 38-39 absents) : les 25 libellés sont cités par réf nue `NADJ 08 folio 39 — légende du plan (image)`, vérifiés par l'orchestratrice sur l'image (fitz 260 dpi) ; `Source/` non édité (les réfs `NADJ 08 l.<n>` de `docs/raw/combat.md` tiennent) -> inventaire #1388 : classe « légende d'image non extraite », un cas mesuré
- Galerie 4 → 5 m et rampes de 5 rangées (`ETAGE_M = SCENE_M + METRES_PER_LEVEL`, `RAMPE_RANGEES`) : défaut préexistant révélé par le corps (galerie à 3 m au-dessus des planches) -> corrigé par 765e1d111 src/scenes/opera/floorplan.ts:26
- `validateScene` mesurait l'empilement sous des colonnes OUVERTES adoptées par la masse (puits de rampe : Δ 3 m entre 2 et 5 m) -> corrigé par 765e1d111 src/state/validateScene.ts:313
- Grief 2 de la revue de palier `3113daad1..02b38d88b` (cardinal vivant 40/89 de `scenarios-contrat.test.ts`, mon train) -> corrigé par 765e1d111 src/data/schemas/defs-scenes/scenarios-contrat.test.ts:35
- Recetteur (agent) bloqué : le navigateur Playwright MCP est un singleton MACHINE (`ms-playwright-mcp/mcp-chrome-*`) tenu par la session voisine, aucune file ni mode isolé depuis le brief -> RAS : recette jouée par le kit CDP canonique `scripts/recette/lib.mjs` (Chromium propre), fiche mémoire `env-playwright-mcp-verrou-machine-kit-cdp` posée dans ce commit
- `walls()` rend toujours `{"(sans structure)":983}` : les arêtes de l'opéra n'ont pas de Structure nommée -> RAS : hors DoD (la Structure d'arête porte l'opacité et le rendu, pas le dégagement ; le corps `architecture` est la donnée qui manquait)

## Recette visuelle
capture: public/qc/soldes/1771-opera-plan-salle-verte.png — kit CDP du 2026-09-16 (port 5234) : `scenario('opera-plan')`, `ready()` (voile tombé après 6383 ms), `goto({x:3,y:4,z:0})` : le héros est DESSINÉ (corps, halo de sélection), banc et table de la salle verte visibles, coiffeuses et miroirs des vestiaires voisins, rideaux de la scène à droite ; l'étage a disparu en bloc (loi du couvercle, arbitrage utilisateur). `pickTileAt` au centre de la case → `{tile:{x:3,y:4,z:0}, via:'sol'}`.
capture: public/qc/soldes/1771-opera-plan-coulisses.png — `goto({x:32,y:26,z:0})` (la case (31,25) de `c25-table-1` refuse le pas : `pickTileAt` y rend `via:'meuble', geste.entId:'c25-table-1'`) : tables, tabourets, lampes de la pièce, portants de costumes de la pièce voisine, tonneaux de la réserve en bas, fauteuils du parterre à gauche.
capture: public/qc/soldes/1771-opera-plan-reserves.png — `goto({x:36,y:37,z:0})` ((35,35) porte `r27-tonneaux-1`, `via:'decor'`) : tonneaux, caisses, coffre, établi ; escalier des seigneurs et statue au premier plan.
capture: public/qc/soldes/1771-opera-plan-salle-verte-roofcut-off.png — même case, `roofCut(false)` : le toit DÉRIVÉ (ardoise, `hip` 5,4°, 2100 cases) coiffe tout le théâtre et le héros repasse en silhouette — preuve que la masse existe et que c'est la loi de dégagement qui la retire.
capture: public/qc/soldes/1771-opera-plan-galerie-z1.png — `goto({x:2,y:30,z:1})` (loge de flanc non zonée) : l'allié dégage l'emprise entière, le rez se voit par l'ovale ; murs crénelés (#1180), loges sans zone (#1179).

## Réfutation
verdict: PARTIEL — passe de réfutation de la fermeture sur pièces (orchestratrice, 2026-09-16) : (a) « la clause PORTE de #1644 (les scènes réelles rendent les volumes) reste contredite » → LEVÉE au rez sur les trois pièces du DoD (captures), l'étage hors loge royale reste sur l'emprise — dit, routé #1179, verdict PARTIEL ; (b) « le lot pose des libellés sans citation de ligne du Source » → la légende est une image, aucune ligne n'existe (sonde du codeur : texte des pages PDF 40-41 = folio + filigrane ; extraction Markdown sans texte entre les spans page-38-0 et page-41-0), les libellés sont vérifiés par un second lecteur (moi) sur l'image rendue à 260 dpi, réf nue `NADJ 08 folio 39` ; (c) « `validateScene` est relâché » → juge de diff L1 : le test qui verrouille le contrôle traverse des cases à plancher réel (`validateScene.test.ts:159`), `realFloorAt` est déclarée partagée (`sceneEdit.ts:687-692`), le seul cas relâché est une colonne OUVERTE (puits de rampe, Δ 3 m) ; (d) « la recette n'a pas été jouée en joueur » → jouée par le kit CDP canonique avec `__wfrp` pour le setup et `pickTileAt` pour l'observation, console 0/0 ; le clavier réel n'a pas été frappé (rotation de caméra non rejouée : `rotateCam` n'est pas exposé par `__wfrp`, la capture « cran 1 » a été retirée) — friction d'outillage déjà inventoriée sur #1568 ; (e) « le lot ouvre plus qu'il ne ferme » → la vague ferme #1771 et ouvre #1777 (grief de palier sur mon propre commit), delta net 0. Juge de diff (agent, 7 lentilles) : 2 bloquants corrigés et vérifiés par mutation, 3 souhaitables faits, 8 attaques écartées par mesure.
