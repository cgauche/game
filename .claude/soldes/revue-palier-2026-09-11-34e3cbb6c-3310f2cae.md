# Revue de palier — fenêtre 34e3cbb6c..3310f2cae — 2026-09-11

verdict: PARTIEL

8 lentilles jugées par UN agent juge unique (décision utilisateur 2026-09-11, verbatim : « Oula arrete avec ce workflow, je n'ai pas assez d'utilisation pour survivre a ca » — le workflow multi-agents `revue-palier` n'est plus joué) sur 35 commits (25 de substance) et 5 fermetures : 5 trouvailles confirmées, 4 écartées par re-mesure, 24 points tenus, aucune trouvaille bloquante.

## Trouvailles confirmées

1. **L'épic #1686 a été fermé alors que son reste explicitement routé #1715 était OUVERT, contre la doctrine utilisateur du 2026-09-01 (« un reste routé en ticket n'est pas un solde »)** (lentille fermetures-soldes-DoD) — `gh issue view 1715 --json createdAt,closedAt` → `createdAt 2026-09-09T03:02:43Z`, `closedAt 2026-09-09T14:00:01Z` ; `gh issue view 1686 --json closedAt` → `2026-09-09T03:20:10Z` : l'épic se ferme 17 minutes APRÈS la naissance de son ticket de restes, et 11 h AVANT sa clôture. Le commit fermant `98172ff56` porte lui-même la section « **Restes** … → **#1715** », et `.claude/soldes/1686.md` affirme au même instant « (3) ZÉRO catalogue TS de matière » alors que les 7 ids de matière en dur y vivaient encore : `git grep -n roofMaterial 98172ff56 -- src/gameIso/catalog` → `defs/{chapelle,forge,manoir,tour}.ts:7: roofMaterial: 'toit-ardoise'`, `defs/{maison,taverne}.ts:7: 'tuile'`, `defs/echoppe.ts:7: 'chaume'` (et `DEFAULT_ROOF_DEFAULTS` = 4 occurrences dans `sceneEdit.ts`, `plafond` encore dans `materials.json`). Fiche `.claude/memory/feedback-epic-ne-se-ferme-jamais-sur-tickets-ouverts.md:20` (verbatim user 2026-09-01) : « Pas de demi-migration j'ai dit, ça sert à quoi si on ferme l'épique sans avoir terminé ? » → attendu : un épic ne se ferme qu'une fois ses restes ÉTEINTS (ici : après #1715), ou son solde dit que la clause n'est pas tenue au lieu de l'affirmer tenue. · NON bloquante, instruit : les 7 sites sont morts à HEAD (`git grep roofMaterial 3310f2cae -- src/gameIso/catalog/buildings/defs` → aucun fichier), `plafond` = 0 dans `materials.json`, `DEFAULT_ROOF_DEFAULTS` a quitté `sceneEdit.ts` — aucun reste ne survit au palier, et la lecture stricte de la clause (« catalogue de MATIÈRES ») peut s'entendre tenue : c'est l'ACTE de fermeture qui viole la doctrine, pas l'état d'arrivée.

```js
// sonde s26.mjs (lecture seule) — état de la clause au commit fermant ; se joue depuis la racine du dépôt
import {spawnSync} from 'node:child_process';
const R = process.env.WFRP_ROOT ?? process.cwd();
const g=(...a)=>spawnSync('git',a,{cwd:R,encoding:'utf8',maxBuffer:1e9});
for (const rev of ['98172ff56','3310f2cae']) {
  console.log(rev, g('grep','-n','roofMaterial',rev,'--','src/gameIso/catalog/buildings/defs').stdout.trim() || '(aucun)');
  console.log('  plafond:', g('grep','-c','plafond',rev,'--','src/data/materials.json').stdout.trim() || '0');
  console.log('  DEFAULT_ROOF_DEFAULTS:', g('grep','-n','DEFAULT_ROOF_DEFAULTS',rev,'--','src/state/sceneEdit.ts').stdout.trim().split('\n').filter(Boolean).length);
}
// sortie : 98172ff56 → 7 lignes roofMaterial, plafond 1, DEFAULT_ROOF_DEFAULTS 4 ; 3310f2cae → (aucun), 0, 0
```

2. **Action (γ) NON soldée : le repli silencieux du détecteur de stocks traverse une TROISIÈME revue sans une ligne de code** (lentille restes-de-la-revue-precedente) — `git log --oneline de 34e3cbb6c à 3310f2cae -- scripts/guards/lib/stocksNominatifs.mjs` → sortie VIDE ; `estEntreeDeStock` et ses deux regex de ligne (`ENTREE_EN_TETE`) sont intacts à HEAD. Le worktree annoncé pour le train D3 (`.wt-gates-B1`, branche `gates/D3`) est à `3310f2cae` — il n'a encore produit AUCUN commit : le train est en vol, pas soldé. Aggravant mesuré : c'est ce détecteur qui fonde le fait « 0 refus de stock sur la plage » du harnais — un zéro silencieux sur une naissance de stock JSON y est indiscernable d'une absence de croissance. → attendu : soit `estEntreeDeStock` lit une entrée-objet JSON multi-lignes, soit le repli sans image devient un REFUS NOMMÉ ; tant qu'il est en vol, le « 0 refus » du palier se lit « 0 refus SOUS la couverture du détecteur ».

3. **Poison famille (b) ajouté par la fenêtre et invisible au détecteur canonique : une dette de lot annoncée sans tag `[entériné]`** (lentille poison-des-diffs) — `src/gameIso/stage/AreteOverlay.tsx:16`, verbatim ajouté : « Une seule chaîne d'activation, donc, et le nom lisible **en attendant l'infobulle partagée (lot 3)**. » CLAUDE.md règle 6(b) range l'excuse-report (« épargné pour l'instant ») en dette signalée, non autorisée sans tag. Le détecteur canonique ne mord pas : `untaggedExcuseMatch("… le nom lisible en attendant l'infobulle partagée (lot 3).")` → `null`, alors que le TÉMOIN positif mord (`untaggedExcuseMatch("épargné pour l'instant, on verra plus tard")` → `["pour l'instant", …]`). Balayage du corpus vivant (`readCorpus(['src','scripts'])` + `extractComments`) : 5 occurrences de « en attendant », dont 2 de sens report (`AreteOverlay.tsx`, `src/scenes/test-scenarios/96-presets-edo.ts`), 3 de sens runtime. → attendu : le site se reformule (ce que le code FAIT, sans annoncer le lot suivant) ou porte son tag ; et « en attendant/dans l'attente » entre dans la famille (b) du détecteur, ou est inscrit à `GARDE.angleMort` avec sa mesure.

```js
// sonde s6.mjs — le détecteur canonique interrogé avec son témoin positif ; se joue depuis la racine du dépôt
import {untaggedExcuseMatch, tombstonesIn} from './scripts/guards/lib/commentPoison.mjs';
for (const [n,t] of [
  ['AreteOverlay:16',"Une seule chaîne d'activation, donc, et le nom lisible en attendant l'infobulle partagée (lot 3)."],
  ['temoin-excuse',"épargné pour l'instant, on verra plus tard"],
  ['temoin-tombale',"déplacé vers src/autre.ts"],
]) console.log(n, JSON.stringify(untaggedExcuseMatch(t)), JSON.stringify(tombstonesIn(t)));
// AreteOverlay:16 null [] | temoin-excuse ["pour l'instant",…] [] | temoin-tombale null ["déplacé(e)(s) vers/dans (code)"]
```

4. **La trouvaille 12 du palier précédent (perte SILENCIEUSE des lectures dont le chemin ne diffère que par la casse) est intacte à l'octet — l'instrument qui justifie les clés de gates reste aveugle** (lentille cross-os-et-determinisme) — `git log --oneline de 34e3cbb6c à 3310f2cae -- scripts/docs/lib/enregistreur-lectures.mjs` → VIDE ; `scripts/docs/lib/enregistreur-lectures.mjs:51` porte toujours `if (abs !== base && !abs.startsWith(base + path.sep)) return null` (comparaison sensible à la casse sur NTFS, rejet non compté). Aucun ticket ne le porte : `git grep -n 'enregistreur-lectures' -- .claude/soldes` ne rend que l'archive du palier précédent. → attendu : le reste est routé sur un ticket OUVERT ou corrigé (comparaison canonique + compteur de rejets rendus) — un défaut d'INSTRUMENT de preuve ne peut pas traverser deux paliers sans porteur.

5. **La classe « sondes câblées sur des chemins ABSOLUS de cette machine » se reproduit dans un artefact committé PAR la fenêtre** (lentille cross-os-et-determinisme) — `git log --diff-filter=A --name-only de 34e3cbb6c à 3310f2cae -- .claude/soldes` → 6 fichiers ; les 5 soldes de tickets en portent ZÉRO, mais `.claude/soldes/revue-palier-2026-09-09-fd660c8b8-34e3cbb6c.md` en porte **25** (`C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1624/scripts/guards/lib/stocksNominatifs.mjs`, `…/.wt-1624`, …) : les sondes qui établissent les trouvailles du palier précédent ne sont rejouables ni sur une autre machine, ni depuis l'arbre principal. → attendu : une sonde archivée cite la racine par variable (`process.env.WFRP_ROOT` / chemin relatif au dépôt), jamais un worktree de la machine du jour. · Les deux sondes de CETTE archive, rendues par le juge avec la racine absolue de la machine, ont été récrites par l'orchestratrice à la racine relative avant archivage.

## Écartées par réfutation

- ~~Cliquets déclarés au mauvais compte dans la fenêtre~~ — écartée : re-mesure par l'instrument canonique (`croissanceDesStocks` de `scripts/guards/lib/stocksNominatifs.mjs`, joué commit par commit sur `git show -U0 <sha>`) → 3 croissances réelles (`ecrivainsAtteints.test.mjs +1` ×2, `labelLogic.mjs +1`), toutes couvertes, et 14 `CLIQUET:` déclarés soit un SUR-ensemble du mesuré (sens sûr). Le fait `stocks` du harnais (0 refus) tient — sous la réserve de la trouvaille 2.
- ~~Les deux `chore(tests)` de recalage de cliquet sont du code d'orchestrateur qui accommode la mesure~~ — écartée : `a6ce13fe1` ABAISSE (`'editor/Inspector.tsx': 22 → 21`, motif écrit « la pente et le comble d'un corps composent `NumberField` ») et `e6e0ce8ea` fait baisser un stock (`structures` quitte les champs étrangers, 5 → 4) plus une empreinte re-mesurée dont le motif nomme la cause (`taille`/`maison` exigés, `maison` 2 → 24) — deux recalages dans le sens sûr, aucun plafond relevé.
- ~~Un commit de substance sans ticket dans la fenêtre (régime α)~~ — écartée : sonde sur les 35 commits (fichiers `src/`/`scripts/` × présence de `refs|corrige|closes|fixes #N`) → 0 commit de substance sans ticket, et 0 sujet sans `#`. La décision α elle-même est tracée avec verbatim et date (commentaire #1679 du 2026-09-11T05:28:50Z).
- ~~Action (β) non soldée (le cardinal vivant `toBeGreaterThan(40)`)~~ — écartée : `git show HEAD:src/gameIso/builders/relief-identite-matieres.test.ts` ne contient plus ni `toBeGreaterThan(40)` ni `plancherScenes` ; `3126b00f7` porte le geste et RÉFUTE la prémisse du palier précédent (le plancher dérivé était une tautologie) au profit d'une non-vacuité par gisement.

## Points tenus

- fermetures-soldes-DoD : arbre ÉPINGLÉ `git log --oneline -1` → `3310f2cae` (branche `main`) ; contrôle POSITIF du travail jugé : `src/engine/structures.ts:125 structureEnduranceMult` et `.claude/soldes/1715.md` présents.
- fermetures-soldes-DoD : les 5 commits fermants citent leur ticket en `corrige #N` et EMPORTENT leur solde (`git show --name-only` : `.claude/soldes/{1715,1688,1686,1693,1694}.md`) ; les 5 fichiers existent à HEAD ; les 5 issues sont CLOSED.
- fermetures-soldes-DoD : DoD de #1715 tenu point par point dans l'arbre — (1) les 7 defs TS ont disparu et `src/data/buildings.json` porte 7 `roofMaterial` ; (2) `toitureEffective` exportée de `state/sceneEdit.ts`, `DEFAULT_ROOF_DEFAULTS` réduit à une semence de `state/scene.ts` ; (3) `"plafond"` = 0 occurrence dans `materials.json`.
- fermetures-soldes-DoD : DoD de #1694 tenu — les 8 Records nommés au ticket ET `SPECS_SOURCE_LABEL` rendent 0 occurrence sous `src/` (`git grep`), les seuls restes `*ATTACK_LABEL` sont `FREE_ATTACK_LABEL` (primitive canonique, sans rapport).
- fermetures-soldes-DoD : DoD de #1693 tenu par une garde POSITIVE et non par la prose : `src/ui/compendium/codex-apercu-porteur.test.ts:45` (« tout aperçu rig résout son rendu, jamais par diagnostic »), la sonde du juge y est promue (`:91`).
- fermetures-soldes-DoD : PRÉMISSE de #1688 vérifiée au Source, pas au brief — `Source/WH - V4 - Aux Armes/10 - L'ARTILLERIE ET LES DÉGÂTS INFLIGÉS AUX STRUCTURES.md` l.98, verbatim : « Le MJ doit déterminer la Taille de la Structure attaquée et compter son Bonus d'Endurance une fois supplémentaire par catégorie de Taille au-dessus de celle de l'attaquant. Donc, un mur de pierre Énorme (avec un BE de 6) attaqué par un humain Moyen serait considéré comme ayant un Bonus d'Endurance de 18 (6 fois 3). Les armes de siège ignorent ces restrictions. » — l'implémentation `1 + max(0, écart)` et le banc `structureEnduranceMult(epee, mur, 'moyenne') === 3` collent à l'exemple RAW, l'exemption siège est testée (`=== 1`).
- fermetures-soldes-DoD : aucune fermeture hors commit (`gh`, baseline 12 entrées, « aucun écart »), fait non contesté après lecture des 5 issues.
- stocks-et-cliquets : chaque `CLIQUET:` de la fenêtre porte un motif de plus de 20 caractères qui NOMME la cause (mesuré sur les 14 déclarations) ; aucune liste d'exemptions nominative neuve n'apparaît — les croissances sont des PÉRIMÈTRES de scan (`SCAN_DIRS`, `DIRS`) ou des tables de couches, dites comme telles.
- stocks-et-cliquets : le seul stock nominatif qui naît (`labelLogic.mjs`, `LABEL_CALL_LITERAL_STOCK`) naît AVEC sa règle et nomme son unique entrée préexistante (`src/state/combatFlow.ts:5926`).
- poison-des-diffs : corpus MESURÉ, pas survolé — 2 279 lignes de COMMENTAIRE ajoutées par `git diff 34e3cbb6c 3310f2cae -- src scripts` (parseur de hunks) ; famille TOMBALE : 0 site confirmé (24 candidats par regex large, tous réfutés par lecture — « déplacement » y est le geste de jeu, « désormais » y qualifie un contrat au présent ou un motif de cliquet ; le détecteur canonique `tombstonesIn` rend `[]` sur chacun, témoin positif mordant).
- poison-des-diffs : aucun test de la fenêtre ne verrouille un comportement faux sur les deux périmètres re-mesurés — les trois bancs de `structures.test.ts` dérivent du verbatim AA 10 l.98, et les bancs de `3310f2cae` remplacent des attentes chiffrées sur le contenu de la Diligence par des attentes DÉRIVÉES.
- derogations-et-ci : 0 dérogation dans la fenêtre (journal du pre-push, 7 entrées toutes hors fenêtre, 0 illisible) — fait re-lu, non contesté.
- derogations-et-ci : aucune course non verte depuis le 2026-09-09 — `gh run list --limit 60` → 20 courses sur la période, 20 `success`, 0 échec ; la tête `3310f2cae` est verte. Régime ε respecté : 15 des 35 shas n'ont pas de run, aucun n'est une tête de push identifiée comme rouge.
- commits-triviaux : aucun code n'est glissé dans un commit `chore(memory)`/`chore(docs)` de la fenêtre (les 10 commits non-substance ne touchent que `.claude/` et `docs/`).
- commits-triviaux : 3 commits touchant du code ne portent aucune trace de juge dans leur message (`a6ce13fe1`, `7cc1e73d7`, `e6e0ce8ea`) ; les trois sont rattachés à un ticket et leur contenu est vérifié ci-dessus (recalages dans le sens sûr, retrait d'une entrée sans émetteur) — signalé, non retenu comme défaut.
- cross-os-et-determinisme : balayage des 9 954 lignes AJOUTÉES sous `src/`+`scripts/` → 0 chemin Windows absolu, 0 `new Date()`/`Date.now()`, 0 `Math.random(`, 0 `toLocaleString` ; les 2 seules occurrences de `localeCompare` sont les ASSERTIONS du filet qui vérifient que le mur l'exclut des tests et le garde chez les générateurs.
- cross-os-et-determinisme : les 2 `readdirSync` ajoutés hors tests sont les deux migrations datées (`2026-09-09-1715-…:72`, `2026-09-10-1687-…:69`), qui écrivent un fichier PAR scène — l'ordre de listing n'entre dans aucun dérivé.
- regime : régime α tracé avec son verbatim daté (#1679, 2026-09-11T05:28:50Z, « Tout commit de substance cite un ticket… ») et régime ε de même (« Je ne comprends pas, en toute logique ce qui est important c'est que le tout soit vert… ») — décisions écrites, porte pre-commit à venir (train R1), donc non encore mesurable au code.
- regime : l'arbre PRINCIPAL n'a plus servi d'arbre de travail de CODE — `git status --porcelain` ne rend que 3 fiches `.claude/memory/` modifiées et un fichier `Source/` non suivi ; aucun WIP sous `src/`/`scripts/` pendant que `.wt-gates-B1` (gates/D3) et `.wt-1624` travaillent. Amélioration mesurée par rapport à la trouvaille 14 du palier précédent.
- restes-de-la-revue-precedente : action (δ) soldée sur son périmètre — la marche brute de l'arbre a QUITTÉ les tests de `src/**` (sonde : 85 fichiers `src`+`scripts` portent encore `readdirSync|opendirSync|globSync`, dont 16 tests, dont 14 sous `scripts/**` et 2 sous `src/` qui ne l'écrivent qu'en COMMENTAIRE ou en littéral de sonde : `src/data/field-consumers.test.ts:128`, `src/eslint-ordre-total-et-purete.test.ts`). Le résidu `scripts/**` n'est ni chiffré ni sous cliquet : périmètre non couvert par le mur, dit ici.
- restes-de-la-revue-precedente : trouvaille 2 du palier précédent (coût de `memoParVersion` annoncé sans banc) SOLDÉE dans la fenêtre par `253bce69d` (`perf(data): refs #1692 — le coût … était ANNONCÉ sans banc`).
- NON MESURÉ — `auditStock` : indisponible sur la plage (advisory neuve `vitest GHSA-82fw-gwwq-j7x9` + `sharp GHSA-rgj7-g3m4-5g8c` font sortir le script en rouge avant son verdict) ; l'état du stock de vulnérabilités n'a donc PAS été jugé ce palier.
- NON MESURÉ — appariement push ⇄ commits : sans reflog distant le juge n'a pas pu reconstruire les LOTS poussés ; « la tête de chaque push est verte » n'est établi que par l'absence totale de course non verte depuis le 2026-09-09, pas par l'identification des têtes.
- NON MESURÉ — fan-out d'agents et « un seul codeur par train » : aucune trace mesurable dans git (les 25 worktrees listés sont un état, pas un journal d'occupation) ; seul l'entrelacement des dates a été relevé (#1709 et #1715/#1687 progressent le même jour, en worktrees distincts, ce que le régime autorise).
- NON MESURÉ — aucune suite de tests ni gate n'a été jouée (interdit du brief) : tous les verdicts ci-dessus reposent sur des sondes de fichier isolé, des lectures git et les détecteurs canoniques importés un à un.
