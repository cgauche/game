# Solde #1792 — 2026-09-18

VERIFIE: le contrat du ticket (les cases à cocher de l'atelier posées dans un `<label class="dr">` se rendent en rectangle plat 44 px de large, sans coche lisible, parce que `.dr input { width: 44px }` de `editor.css` écrase la boîte 18×18 des contrôles custom de `base.css`) est reproduit au témoin `public/qc/soldes/1695-03-op-aveugle-porte.png` et corrigé à la SOURCE : toute la boîte du contrôle (`width`/`height`/`min-*`/`max-*`/`padding`/`box-sizing`/`flex`) est `!important` dans la règle globale case/radio de `base.css`, si bien qu'aucune déclaration de boîte d'un module sur un `input` non typé (règles `.x input { width }` de `src/ui/styles/`, `padding` et `min-height` tactile génériques de `base.css`) ne peut plus la déformer ; l'exclusion `:not([type='checkbox'])` de `codex-edit.css` (contournement local du même défaut) et la règle `.menu-toggle input` (devenue inerte) sont retirées ; deux gardes de CLASSE `src/ui/ui-ratchets.test.ts` (xx) ; charte mise à jour ; mesure en recette 18×18 sur trois écrans.

## Restes
- DoD du ticket « sélecteur typé `.dr input[type='number']` » (formulation de l'ouverture) -> RAS : remplacé par l'immunité à la source, qui ferme la classe entière au lieu du seul site `.dr` ; les deux contournements existants (`codex-edit.css:18`, `creator.css:138`) prouvaient que la classe était déjà connue site par site.
- Cible TACTILE des cases/radios : 18 px sous `pointer: coarse` alors que `.btn` 40 / `input` 38 ; avant ce commit la case y rendait 44×38 (déformée), pas plus grande (juge, grief 1) -> #1796
- `.skill-adv-grid`/`.skill-adv`/`.skill-adv input` (`combat-ui.css`) sans aucun consommateur TSX (juge, note hors périmètre) -> corrigé dans ce commit (src/ui/ui-ratchets.test.ts:372)
- `.menu-toggle input { flex: none; margin: 0 }` battu par l'immunité et doublon de `base.css` (juge, grief 2) -> corrigé dans ce commit (src/ui/styles/base.css:308)
- Première recette : 22×18 mesuré, le `padding: 8px 10px` générique de `input, select, textarea` (`base.css`) débordait la boîte de 18 px (recetteur) -> corrigé dans ce commit (src/ui/styles/base.css:306)

## Recette visuelle
Recette navigateur en joueur (Chrome headless CDP, clics et Space réels, console 0 erreur), 2026-09-18, rejouée sur l'arbre FINAL après les corrections du juge et du recetteur ; `getBoundingClientRect()` lu en `evaluate` séparé de l'action :
- capture: public/qc/soldes/1792-01-op-condition-cases-18px.png — Compendium › Magie › Sorts › Transmutation de Chamon › Atelier › Éditer › op « Aveuglé » : cases « porté par la source », « chaque Round », « par DR » mesurées 18×18 (témoin 1695-03 : 44×18).
- capture: public/qc/soldes/1792-02-op-condition-case-cochee.png — « chaque Round » cochée au clic réel (`checked` true), décochée par Space (`checked` false), focus réel conservé ; le libellé du bloc suit (« Aveuglé/Round »).
- capture: public/qc/soldes/1792-03-menu-options-cases.png — menu principal › Options › Confort « Dés fixés » : 18×18 (la case du menu système `.menu-toggle` est prouvée par la sonde de cascade du juge, `MenuCard.tsx`).
- capture: public/qc/soldes/1792-04-mobile-360-op-condition.png — même op à 360 px : 18×18, aucune déformation au responsive.

## Réfutation
verdict: PARTIEL — juge (2026-09-18, lecture seule, diff du lot, sonde de cascade jsdom avant/après) : la prémisse tient (44 → 18 px mesuré par `getComputedStyle`, aucune variante légitime de redimensionnement de case/radio dans tout le CSS, retrait de l'exclusion `codex-edit` prouvé sans effet de bord sur `range`/`color`/texte, témoin authentique) ; cinq griefs, tous corrigés dans ce commit : (1) MAJEUR `@media (pointer: coarse) { input, select { min-height: 38px } }` de `base.css` échappait à l'immunité (`min-*` clampe hors cascade) → `min-width`/`min-height`/`max-width`/`max-height` `!important` ajoutés ; (2) `.menu-toggle input` devenu inerte → supprimé ; (3) garde (xx) = test de TEXTE (regex sur la forme du sélecteur, valeurs figées) → réécrite en deux gardes de classe : parse tolérant de la règle case/radio exigeant `!important` sur CHAQUE propriété de boîte, et scan de tous les `.css` de `src/ui` exigeant que toute propriété de boîte posée sur un `input` non typé soit couverte par l'immunité (mutation « `min-height` sans `!important` » → 2 rouges, restauré → 32 verts) ; (4) phrase de charte qui autorisait à supprimer l'exclusion LÉGITIME de `creator.css:138` (`label:has(...)`, layout de rangée) → bornée aux déclarations de boîte, exclusion nommée ; (5) cardinaux vivants (« 17 règles », « 45 cases ») dans un commentaire et un titre de test → retirés. Recetteur (première passe, même jour) : 22×18 mesuré sur les trois écrans, `padding` générique non couvert → `padding: 0 !important` + `box-sizing: border-box !important`, propriétés ajoutées à la garde, recette rejouée : 18×18.
