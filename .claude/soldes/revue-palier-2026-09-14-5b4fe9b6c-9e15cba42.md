# Revue de palier — fenêtre 5b4fe9b6c..9e15cba42 — 2026-09-14

verdict: PARTIEL

7 lentilles jugées par UN juge unique, lecture seule, arbre épinglé au worktree `.wt-1736` dont `git rev-parse HEAD` = `9e15cba42be0bd637f4fb12d603c817606af9db9` (le lot stagé #1736 est resté hors de tout verdict : toutes les mesures de contenu passent par `git show`/`git grep <rev>`, jamais par le disque). 16 commits (10 de substance selon l'instrument, dont une fusion), **2 fermetures seulement** : #1389 (`bc473e4c8`) et #1711 (`812da89f1`) — les deux CLOSED/COMPLETED, chacune emportant son solde DANS le commit fermant (12 et 13 lignes), chacune portant `## Restes` ET `## Réfutation`, chacune routant ses restes vers un ticket OUVERT (#1390, #1727) ; leurs DoD tiennent au contrôle POSITIF dans l'arbre (les trois définitions hissées de #1389 existent et sont consommées, walks locaux à 0 ; #1711 : `assertAgainstBaseline`/`countsByChapterRef` → 0 sous `scripts`, `folio-gaps-stock.json` = 76 entrées, plus aucune baseline de COMPTE hors le reste déclaré). 6 trouvailles confirmées, aucune bloquante : la plus lourde est que la lentille `fermetures-hors-commit` est NON MESURABLE par l'instrument canonique de ce palier (il est borné par une DATE, pas par la plage de shas) et que la mesure « captures citées » de la revue précédente compte SA PROPRE sonde. 5 points écartés par re-mesure, 14 tenus. Archivée par le commit qui ferme #1736 (l'outil de publication) ; ses sondes vivent au scratchpad de la session, non recopiées ici pour ne pas nourrir la classe de la trouvaille 2.

## Trouvailles confirmées

1. **La lentille `fermetures-et-CI / fermetures hors commit` est NON MESURABLE : le fait canonique est ROUGE, et son rouge est un artefact de BORNAGE** (lentille derogations-et-ci) — `faits-palier-5b4fe9b6c.json` rend `fermeturesHorsCommit.disponible: false`, raison = `fermetures-non-citees.mjs --depuis 2026-09-12` sorti en erreur sur trois fermetures « citées par AUCUN commit et sans solde » : #1699, #1732, #1734 (fermées le 2026-09-14 par `github-actions[bot]`). Re-mesuré à la main : les trois sont fermées par des commits qui existent, mais **au-delà de la tête jugée** — `6ba23c74f` « corrige #1699 », `c31571a72` « corrige #1732 », `54e6cdcad` « corrige #1734 » — et chacun emporte son solde (`.claude/soldes/1699.md` 12 lignes, `1732.md` 14, `1734.md` 12, présents sur `origin/main`). L'outil est borné par `--depuis <date>` alors que la revue est bornée par `<base>..<tête>` : toute fermeture publiée après la tête rend le fait indisponible. → attendu : le fait `fermeturesHorsCommit` se borne à la PLAGE DE SHAS de la fenêtre (ou la baseline s'écrit au même geste que le palier) ; sinon chaque revue héritera d'un rouge qui ne parle pas d'elle. · NON bloquante (les trois fermetures sont régulières, mesurées ici à la main).

2. **La mesure « capture citée par un solde » de la revue précédente compte SA PROPRE SONDE : la prétendue aggravation 48 → 50 est un faux positif intégral** (lentille restes-de-la-revue-precedente) — sonde rejouée aux deux bouts : `5b4fe9b6c {soldes:113, total:48, dansGit:0, surDisque:0}` → `9e15cba42 {soldes:116, total:50, dansGit:1, surDisque:1}`. Le delta est porté par UN seul fichier, `.claude/soldes/revue-palier-2026-09-13-e1ae5618c-5b4fe9b6c.md` (0 → 2), et les deux « captures » sont le littéral de regex de son bloc de code (le mot-clef suivi de deux-points, dans le code de la sonde) et un backtick isolé — ce backtick est d'ailleurs l'unique « capture » qui existe « dans git ». La revue précédente s'était déjà comptée (+2 en texte) ; elle se compte maintenant en CODE. Le stock RÉEL de citations mortes n'a pas bougé dans la fenêtre (aucun solde neuf ne cite de capture). → attendu : la sonde exclut les blocs de code et les fichiers de revue avant de compter, sinon l'instrument produit la dérive qu'il dénonce ; et la classe se ferme sur le fond (capture versionnée, ou champ de capture des soldes qui cesse d'affirmer une preuve inouvrable). · NON bloquante.

3. **Les restes (1) et (4) de la revue précédente traversent un TROISIÈME palier sans bouger DANS la fenêtre — ils ne sont soldés qu'au-delà de la tête** (lentille restes-de-la-revue-precedente) — mesuré à l'octet : `git rev-parse 5b4fe9b6c:src/gameIso/stage/AreteOverlay.tsx` = `9e15cba42:…` = `b7e9d6928950f6a31e7cf5b3054deca855ba3b7a`, et la tête porte encore « le nom lisible en attendant l'infobulle partagée (lot 3) » (lignes 15-17 du blob) ; `.claude/soldes/1687.md` contient toujours `sans ticket` (1 occurrence) à `9e15cba42`. Les deux sont ticketés (#1732, #1734) puis fermés le 2026-09-14 à 08:55 par `c31571a72` et `54e6cdcad`, tous deux HORS fenêtre — le premier élargissant même la famille (b) du détecteur (`ATTENTE_DE_X`), ce qui était l'attendu écrit. → attendu : rien de plus sur le fond ; mais la fenêtre jugée est bien le troisième palier consécutif où ces restes n'ont pas bougé, ce que seul le découpage sauve. · NON bloquante.

4. **`92e44a0c2` dépose dans `Source/` le SEUL binaire VO du dossier — une carte WFRP *5e* (Cubicle 7, anglais, 4,5 Mo) — corps de commit VIDE, aucun `refs #N`, aucune ligne de doc** (lentilles regime et poison-des-diffs) — `git show --name-only 92e44a0c2` → `Source/WHFRP 5e - [Cubicle 7 Entertainment] - Warhammer Fantasy Roleplay 5e - Endpaper Map [OEF][2026-09-01].jpg`, sujet « présente au disque depuis le 2026-09-01 » et corps vide ; `git ls-tree -r 9e15cba42 Source/` ne rend que deux images au total ; `git grep -i "Endpaper" -- docs scripts src` → **aucun site**, `docs/sources-vf.md` ne la nomme pas, `gh issue list --search "WFRP 5e in:title"` → `[]`. Or `CLAUDE.md` § Sources VF : « Tout est en français sous `Source/` », et un périmètre s'établit « par PASSAGE, documenté dans `docs/sources-vf.md` ». Le fichier est toujours là, sans doc, sur `origin/main`. → attendu : une entrée de `docs/sources-vf.md` qui dit ce que cette pièce EST et ce qu'elle n'autorise pas (une carte n'est pas une règle, et 5e n'est pas la v4 du projet), ou son retrait ; un binaire de 4,5 Mo n'entre pas dans le dossier de vérité citable sans porteur. · NON bloquante.

5. **Un ROUGE DE CI sur `main` à l'intérieur de la fenêtre, causé par un verdict branché sur un code `errno` que les gates locales (Windows) ne peuvent pas voir** (lentille derogations-et-ci) — `coursesCi` : `6e76f75d6` = CI `failure` (run 34815975288, `test:hooks` 851/853, `gitPorte.test.mjs:42`) ; toutes les autres courses de la fenêtre sont `success`. Le régime a fonctionné : UNE dérogation journalisée dans la fenêtre (`9e15cba42`, motif `rouge`, `shaCause 6e76f75d6`, « gates locales 22/22 vertes » — l'état `tentative` est par construction, le hook s'exécute avant le transfert, `scripts/git-hooks/pre-push.mjs:38`), un commentaire de sonde CI posé sur #1729, et un correctif code-agnostique par injection de `nature` dans l'heure. Classe résiduelle mesurée à la tête : `git grep -E "\.code\s*===\s*'E[A-Z]+'" -- scripts` → 2 fichiers seulement, tous deux « fichier absent » (stable cross-OS). → attendu : rien à réparer sur ce site ; la classe « un verdict de garde branché sur un code d'erreur système » reste sans porte et ne se voit qu'en CI, après publication sur `main`. · NON bloquante.

6. **#1728 : quatre commits de la fenêtre s'y réfèrent et le ticket porte ZÉRO commentaire** (lentille regime) — `git log 5b4fe9b6c..9e15cba42 --grep='#1728'` → `ddca9c829` (purge mémoire 388 → 136 fiches), `07e821e71` (rouges de gates laissés par la purge), `7231d1388` (migrations recalées), `a768ba74e` (fiche mémoire) ; `gh issue view 1728` → OPEN, `comments: 0`, corps intact du 2026-09-13. L'état du chantier (purge atterrie, deux rectificatifs, ce qui reste du budget de contexte mesuré) ne vit que dans les messages de commit. → attendu : un commentaire d'état à chaque atterrissage, comme #1508 et #1729 le font correctement dans la même fenêtre. · NON bloquante.

## Écartées par re-mesure

- ~~Les cinq `CLIQUET:` de `de83569a5` déguisent une dette en conversion~~ — écartée, mesurée des deux côtés : `rollSeamWhitelist.mjs` `hitModifiers.ts` **2 → 1** (`mesuré : d10×2` → `d10×1`) et `combatFlow.ts` **59 → 58** (même liste `mesuré`, `d10×2` → `d10×1`) : deux DÉCROISSANCES réelles, cohérentes au détail du dé ; `mecanique.ts` `OPS_NON_TYPEES` **124 → 123** entrées, `domeWard` quittant la liste (ligne 102 avant) pour un `z.strictObject` typé (`mecanique.ts:90` après) ; `slotsStock.mjs` `spells.json|ops` **205 → 206** et `structuresStock.mjs` +1 signature — les trois hausses sont la contrepartie EXACTE de l'op typée (la donnée gagne une op). `stocks.refus: []`, `notes: []` sur la plage complète.
- ~~#1711 laisse derrière lui des `*-baseline.json` de COMPTE~~ — écartée : `git ls-tree -r 9e15cba42 scripts/raw scripts/guards | grep baseline` → **deux fichiers seulement**, `scripts/raw/empty-folios-baseline.json` (reste DÉCLARÉ et routé → #1727 OPEN) et `scripts/guards/lib/decisions-baseline.json`, qui est NOMINATIF par construction (entrées `{fichier, motif, ancre, raison, date}` et `estPorteurDeStock('scripts/guards/lib/decisions-baseline.json') === true`, `scripts/hooks/stocks-nominatifs.test.mjs:85`). `assertAgainstBaseline|countsByChapterRef` → 0 hit sous `scripts`, `folio-gaps-stock.json` = 76 entrées `"fichier"`.
- ~~Poison (excuse sans tag, pierre tombale, test-tombale) introduit par la fenêtre~~ — écartée : sur **6 098 lignes ajoutées** (`scripts` + `src` + `.claude`), la passe sur `ancien|autrefois|avant ce|en attendant|pour l'instant|legacy|déplacé vers|provisoire|temporaire|à terme|jusqu'à ce que|épargné|exception assumée|TODO|FIXME` rend **5 hits, tous légitimes** : « fichier temporaire posé dans le périmètre » (contrat d'un lister), « jamais recopiées de l'ancienne adresse » et « le texte que l'ancienne… » (l'outil de réparation PARLE d'adresses anciennes, c'est son sujet), « il n'y a pas d'ancienne forme à » (grammaire), et `src/ui/compendium/codex-edit-prose-adressee.test.tsx:121` qui décrit un FLUX d'écran (« l'écran perd sa prose … jusqu'à ce que chapitre + section + blocs soient ressaisis »), pas une dette. Zéro test neutralisé : aucun `.skip(`/`.todo(`/`describe.skip` ajouté.
- ~~Les contrats annoncés par le train T3a ne sont que des affirmations de message~~ — écartée, rejoués sur l'arbre épinglé : `npx vitest run src/state/combat/sauvegardes-projectile-magique.test.ts src/scenes/test-scenarios/22-dome.test.ts` → **exit 0, 2 fichiers / 13 tests**.
- ~~La trouvaille 4 de la revue précédente (« l'état du chantier ne vit que dans des commits locaux, la tête n'est pas poussée ») tient encore~~ — écartée sur les deux volets : la tête EST publiée (`git merge-base --is-ancestor 9e15cba42 origin/main` → **OUI**, `origin/main` = `1b3d794cb`), et l'état vit sur les tickets — #1508 porte « ## T3a POSÉ … (de83569a5, main = de83569a5, 2026-09-14) », #1729 porte la sonde CI du run 34815975288, #1463 son pilotage v38. Seul #1728 y échappe (trouvaille 6).

## Tenus

- fermetures-soldes-DoD : les 2 fermetures de la fenêtre sont CLOSED/COMPLETED (`gh issue view` : #1389 le 2026-09-14T00:01:37Z, #1711 le 2026-09-13T22:35:06Z), chacune par SON commit, chacune emportant son solde dans ce même commit (`git show --stat bc473e4c8 -- .claude/soldes/1389.md` → 12 lignes ; `812da89f1 -- .claude/soldes/1711.md` → 13 lignes). Aucune fermeture posée hors commit dans la plage.
- fermetures-soldes-DoD : les deux soldes portent `## Restes` ET `## Réfutation` ; les restes sont ROUTÉS (#1390 **OPEN**, #1727 **OPEN**) ou motivés `-> RAS` avec leur mesure (`citationCoverage`, prose inline 2 617 nœuds, `.d.mts` de convention ; échange en place, ré-ordinalisation, ré-extraction Marker, `raw:check-entity-in-chapter` hors `ci.yml`).
- fermetures-soldes-DoD : DoD de #1389 tenu au contrôle POSITIF dans l'arbre — les trois définitions hissées EXISTENT et sont CONSOMMÉES : `scripts/source/adresses.mjs` (+ son `.d.mts`), `src/data/source/decoupe.ts:455` `export function findAllRuns`, `scripts/source/reecriture-ancree.mjs` importée par `scripts/migrations/2026-09-05-1389-psychology-desc-vers-descref.mjs:44` ET par `scripts/source/reparer-adresses.mjs:42` (la copie de la migration est bien morte) ; `git grep readdirSync -- src/data/prose-resolution.test.ts src/data/source/prose-source.test.ts` → **0** (les walks locaux ont disparu comme annoncé).
- fermetures-soldes-DoD : DoD de #1711 tenu au contrôle POSITIF (voir « Écartées » : 76/76, 0 copie d'`assertAgainstBaseline`, plus aucune baseline de compte non déclarée).
- stocks-et-cliquets : `stocks.valeur = { refus: [], notes: [], commits: 15 }` sur `5b4fe9b6c..9e15cba42` — aucune croissance non déclarée par l'instrument, et les cinq `CLIQUET:` du seul commit qui en porte sont re-mesurés ci-dessus (deux baisses réelles, trois hausses de contrepartie).
- derogations-et-ci : **1 dérogation dans la fenêtre**, motivée, nominative, avec son `shaCause` — 7 hors fenêtre, 0 illisible ; 12 courses CI sur les shas de la fenêtre, **1 seule rouge** (`6e76f75d6`, trouvaille 5), corrigée par le sha suivant, lui-même `success`.
- poison-des-diffs : les bancs ajoutés sont des contrats POSITIFS joués (13/13 rejoués pour T3a) et les mutations décrites dans les messages portent sur du CODE, pas sur des attentes ; aucun test neutralisé.
- regime : chaînage `vérifié`, 16 commits, aucun trou ; tête publiée sur `origin/main` ; les commits de substance citent tous `refs`/`corrige #N` — sauf `92e44a0c2` (trouvaille 4), que l'instrument classe non-substance.
- regime : l'état des chantiers vit SUR les tickets pour 3 des 4 chantiers de la fenêtre (#1508, #1729, #1463) — correction effective de la trouvaille 4 de la revue précédente ; #1728 reste l'exception (trouvaille 6).
- auditStock : disponible et VERT — 5 entrées / 5 paquets ≥ high observés, « aucun écart au stock daté », les 5 JAUNE portant chacune sa date d'entrée (2026-09-04) et son échéance numérotée (#1726, à la clôture de #1679). Aucun mouvement depuis le palier précédent, aucune advisory neuve sans porteur.
- restes-de-la-revue-precedente : reste (2) captures → n'a pas bougé en réel, et la prétendue aggravation est un artefact de sonde (trouvaille 2) ; restes (1) et (4) → n'ont pas bougé dans la fenêtre, soldés juste au-delà (trouvaille 3) ; reste (3) rouge `cdb57b518` du 2026-09-11 → hors fenêtre, aucun ticket ouvert ne le nomme (`gh issue list --search cdb57b518` ne rend que #1687 CLOSED et l'épic #1679), la fenêtre ayant en revanche NOMMÉ et corrigé son propre rouge.
- LIMITE DITE — l'arbre épinglé porte un lot stagé hors fenêtre (#1736, 20 chemins sous `scripts/guards`, `scripts/hooks`, `scripts/gates`, `scripts/ops`) : aucune mesure de contenu n'a lu le disque, tout passe par `git show`/`git grep <rev>`. Seule exception assumée : le rejeu vitest de deux bancs `src/**`, aucun des chemins modifiés n'étant dans leur graphe.
- NON MESURÉ — la lentille `fermetures hors commit` par l'instrument (trouvaille 1) ; elle a été suppléée à la main sur les trois numéros signalés.
- NON MESURÉ — « un codeur par train », fan-out d'agents, recettes navigateur revendiquées par le message de `de83569a5` (14 sessions, 23 captures `.playwright-mcp/recette-1508-t3a-ter/`) : aucune trace vérifiable en lecture seule dans git, et les captures citées relèvent précisément de la classe de la trouvaille 2.
- MUTATIONS SUR DISQUE : aucune. Aucune commande git écrivante (`add/commit/checkout/restore/reset/stash/clean` : zéro) ; `gh` en lecture seule (`issue view`, `issue list`) ; les trois sondes `.mjs` vivent sous le scratchpad de session ; le seul runner joué (`npx vitest run`) est un lecteur.
