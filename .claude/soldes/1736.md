# Solde #1736 — le train de publication en UN outil du dépôt (`scripts/ops/publier.mjs`) — 2026-09-14

VERIFIE: l'orchestrateur a relu les trois livraisons (A `publier.mjs` 750 l., B `chantier.mjs`/`worktrees.mjs`, C corrections) fichier par fichier et contre-grepé les claims : toutes les mutations git du train passent par `ctx.git` gardée par `commandeInterdite` (sites `rebase`/`--abort`/`add --`/`commit -F … --`/`push HEAD:main`, aucun `execFileSync('git'`), `git grep "issue', 'close"` → 0 dans `publier.mjs`, `RACINES_AGENTS`/`journal.gatesJouees` → 0 après C, `ROUGES`/`ANNULEE`/`urlOrigineAcceptee` définis une fois (`coursesCi.mjs:31,35`, `gitPorte.mjs:221`) sans alias ni re-export ; le juge de design (5 bloquants, 6 fragilités) a été contre-vérifié au code (`SOURCES_LUES` `build-all.mjs:97`, `agents:check` `pre-commit.mjs:380-388`, `--check` ciblé `build-all.mjs:450-464`, `managedRoots` `compat-core.mjs:189`) avant le premier codeur ; runners rendus BRUTS par les codeurs : `node --test` ciblés 67/67, `npm run test:ops` 177/177, `npm run test:hooks` 864/864, `npm run test:agents` 21/21, `ecrivainsAtteints` + `pre-push.test` 41/41, eslint 0 sur 9 fichiers, `npm run docs:check` vert, `node scripts/ops/publier.mjs --etapes` sans effet, `npm run agents:sync` joué par l'orchestrateur avant le commit ; preuves par mutation rendues pour chaque test neuf (chaîne `refs #A #B` → 4 rouges, `prune` sur absent → 2 rouges, `docs.dejaFaite`/`journalInitial`/`gatesRejouees`/options globales → 4 rouges, remises à l'octet par empreinte) ; le train réel (préflight sur arbre sale → `PUBLICATION: rouge preflight`, arbre inchangé, exit 1) mesuré par le juge de diff ; les gates complètes sont jouées par le train sur la tête de ce lot, et la PREMIÈRE PUBLICATION RÉELLE par l'outil est ce lot lui-même (log collé au ticket).

## Restes
- Le DoD nommait « première publication RÉELLE jouée par l'outil (les lots #1732/#1734 et T0a #1727) » : ces lots ont été publiés par leurs sessions AVANT l'outil (tronc `1b3d794cb`, commits `c31571a72`/`54e6cdcad`/`de5fcc474`) -> RAS : la première publication réelle par l'outil est CE lot, depuis `.wt-1736` (`npm run ops:publier -- --detache`), sa sortie est collée au ticket
- Le nettoyage des 27 worktrees orphelins (7 `propre+fusionné` mesurés par l'outil : `Game-1388`, `Game-astra-reprise`, `.wt-raw`, `.wt-1624`, `.wt-1728-L1`, deux `.claude/worktrees/agent-*`) -> RAS : `npm run ops:worktrees -- --purger` est prêt et ne retire que ces sept ; le geste s'exerce sur l'arbre PRINCIPAL, partagé par d'autres sessions vivantes — il revient à la session propriétaire de l'arbre principal, l'inventaire est collé au ticket
- `inventaire` de `worktrees.mjs` joue `fetchOrigin` (mutation de refs) même sans `--purger` -> RAS : dit en en-tête (scripts/ops/worktrees.mjs:16), un verdict de fusion exige `origin/main` frais et l'inventaire l'imprime sans lui s'il est indisponible
- « rien à publier » (`origin/main..HEAD` vide) est rendu ROUGE, exit 1 -> RAS : une invocation sans commit à publier n'est pas une publication échouée mais un usage invalide, la ligne `PUBLICATION: rouge rebase — rien à publier` le nomme (scripts/ops/publier.mjs:478)
- La bascule `agents:sync` sur `agents:check` rouge (étape `docs`) n'a pas de test unitaire, elle vit dans le `jouer` impur -> RAS : jugée par le train réel de cette publication (le lot modifie `CLAUDE.md`), log au ticket

## Réfutation
verdict: CONFIRMÉ — juge de design (Opus, 2026-09-14) a attaqué le train contre les portes réelles (`pre-commit`, `pre-push`, `toutes.mjs`, `docs-rebuild`) et la reprise après kill à chaque étape : 5 bloquants (`SOURCES_LUES` non dérivé, `agents:check` à chaque commit, fail-fast de `build-all`, journal keyé par branche sautant CI et pilotage au 2ᵉ lot, rebase résiduel) intégrés avant code ; juge de diff (Opus) a confirmé les 11 corrections sur pièces et RÉFUTÉ trois points corrigés dans le geste : `numerosCites` perdait les chaînes `refs #A #B` (72/300 sujets réels) avec un test qui verrouillait la perte, `--purger` ne jouait jamais `prune` pour les `absent` qu'il prescrivait, `managedRoots` (littéral `compat-core.mjs:189`) était exportable ; l'orchestrateur a réfuté `docs.dejaFaite` (keyé sur `journal.tete`, réécrit par le rebase du lot suivant) et la survie de `reprises`/`gatesJouees` d'un lot à l'autre — un run sans `--reprendre` part d'un journal neuf.
