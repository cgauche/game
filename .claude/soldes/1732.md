# Solde #1732 « AreteOverlay.tsx:16 « en attendant l'infobulle partagée (lot 3) » — lot 3 clos, infobulle en production, « en attendant » hors du détecteur de poison (deux paliers sans porteur) » — 2026-09-14

VERIFIE: le DoD relu clause par clause contre le diff du worktree `.wt-1732` — (1) `src/gameIso/stage/AreteOverlay.tsx:16-17` ne décrit plus d'état futur : le commentaire dit ce que le peintre FAIT (le nom de l'arête lisible au survol, rendu par le `<title>` natif porté par le seul trait hit-testable), le `<title>` reste (`:222`) ; `CodexRef tooltipOnly` non adopté pour une raison GÉOMÉTRIQUE mesurée (la 1ʳᵉ raison « `<span>` HTML non montable dans un `<g>` SVG » était fausse, réfutée par le juge : `src/gameIso/stage/PastilleEntite.tsx:85-98` monte `<CodexRef … tooltipOnly>` dans un `<foreignObject>` parce que la pastille EST une boîte `x/y/width/height`) : la prise d'une arête est un `<line>` d'angle quelconque à `strokeWidth={arete.largeurPrise}` et `pointerEvents="visibleStroke"` (`AreteOverlay.tsx:199-222`), un `foreignObject` exige une boîte alignée sur les axes qui couvrirait la boîte englobante du segment et casserait le hit-test par TRAIT ; et une arête n'est pas une entrée du Codex (`category/id`, `CodexRef.tsx:110,122`) ; (2) la famille (b) du détecteur `scripts/guards/lib/commentPoison.mjs:494-521` gagne le motif `ATTENTE_DE_X` : une des trois locutions d'attente (« en attendant », « dans l'attente d… », « en attente d… ») suivie DANS LA MÊME PHRASE d'un MARQUEUR DE DETTE (`MARQUEUR_ATTENTE` : primitive, partagée, mutualisé, canonique, factorisé, lot N, #N, migration, refonte) — deux réfutations du juge ont fixé le motif : la 1ʳᵉ version (déterminant) matchait 6/6 phrases de vocabulaire de FLUX et laissait fuir 3 vraies dettes ; la 2ᵉ (fenêtre à la ligne) n'attrapait PAS le site d'origine, dont le marqueur tombe à la ligne suivante d'un bloc enroulé ; la fenêtre est la PHRASE (`SUITE_DE_PHRASE`, `:520` : traverse le retour de ligne avec son préfixe ` * `/`//`, s'arrête à `.` `;` `!` `?`) ; sonde de l'orchestratrice sur `git show 2a27b8b60:src/gameIso/stage/AreteOverlay.tsx` → `scanExcuses` rend 1 trouvaille ligne 16 ; corpus de la garde (`src` + `scripts`) : 0 faux positif, le test de corpus fait foi ; banc `src/comment-poison-guard.test.ts:459-502` : verbatim deux-lignes du site d'origine (1 trouvaille, ligne de la locution), 6 positifs, 1 cas `[entériné]` neutralisé, 6 contrôles négatifs de flux (verbatims `fr.ts:1722`, `silhouette-sans-corps.test.tsx:185` compris), 2 négatifs de phrase suivante, 3 négatifs sur les verbatims légitimes du corpus (`worldMaterials.ts:97`, `plage-bornes-contrat.test.ts:263`, `96-presets-edo.ts:200`) ; mutation (fenêtre ramenée à la ligne) → test du site d'origine rouge, remise à l'octet (blake3 égal) ; angle mort NOMMÉ au JSDoc (`:513-514`) ; vérifié par l'orchestratrice : motif, JSDoc, banc relus au worktree, `npx vitest run src/comment-poison-guard.test.ts src/state/devtools.test.ts src/scenes/test-scenarios/scenarios.test.ts` 198/198, eslint 0 (codeur) ; suite complète et gates jouées sur la tête du lot avant le push.

## Restes
- Le motif ne voit pas une dette d'attente dite sans mot de chantier (périphrase, nom propre de module : « en attendant que `AreteOverlay` sache le faire ») -> RAS : angle mort de VOCABULAIRE mesuré et écrit au JSDoc du motif (`commentPoison.mjs` § ANGLE MORT) ; un détecteur ne mesure que sa couverture, la revue de palier lit ces sites à l'œil, comme elle a lu celui-ci
- La frontière de phrase de `SUITE_DE_PHRASE` est LEXICALE : une ponctuation de CODE entre la locution et le marqueur (`.` d'un chemin `stock.mjs` ou d'une abréviation `cf.`, décimal `1.5`, `;` séparant deux propositions) ferme la fenêtre — 4 dettes forgées sur 7 s'évadent (réfutation du 2026-09-14) ; le juge qualifie de défaut de design (critère lexical pour une unité syntaxique), pas de rustine à ajouter -> #1737
- Le ticket demandait de « dire si la composition `CodexRef … tooltipOnly` est possible sans fork » : mesuré, la voie `<foreignObject>` de `PastilleEntite.tsx:85-98` exige une boîte alignée sur les axes et casserait le hit-test par TRAIT de `AreteOverlay.tsx:199-222` -> RAS : la mesure est consignée au VERIFIE ci-dessus et au message de commit, le `<title>` natif reste

## Recette visuelle
capture: public/qc/1732-arete-survol.png — recetteur du 2026-09-14 depuis le worktree `.wt-1732` (serveur de dev sur le port dérivé 5188, éteint ensuite) : scénario `diligence` (seed 42), 6 arêtes-porte peintes, la première SURVOLÉE par un vrai `Input.dispatchMouseEvent` CDP (`survoler` de `scripts/recette/lib.mjs`) — l'accent de survol du peintre `AreteOverlay.tsx` est visible sur le seuil, et le `<title>` natif de ce trait lu dans le DOM (hors closure-sync) vaut « Entrée intérieure » : c'est le nom au survol que le commentaire au présent décrit (le tooltip natif ne se peint pas en headless, la preuve du nom est le texte du `<title>`). Console à 0 erreur. Le diff ne change que deux lignes de COMMENTAIRE : la capture prouve que ce que le commentaire affirme est vrai, pas un changement de rendu.

## Réfutation
verdict: CONFIRMÉ — juge de réfutation du 2026-09-14 (« TIENT » sur les trois clauses du DoD) : clause 1 (le fichier entier balayé `attendant|attente|TODO|plus tard|futur|pour l'instant|lot \d` → 0 hit, `<title>` réel `:222`), clause 2 (`scanExcuses` sur `git show 2a27b8b60:…AreteOverlay.tsx` → 1 trouvaille ligne 16 ; sur le fichier actuel → `[]` ; scan de corpus des deux racines inclus dans la suite verte 118/118), clause 3 (périmètre : un seul fichier de `src/gameIso` au diff). Grief instruit : angle mort de ponctuation de code → #1737 et JSDoc.
