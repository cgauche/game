# Solde #1721 « Enregistreur de lectures : les lectures dont le chemin differe par la casse sont rejetees en silence » — 2026-09-12

VERIFIE: l'attendu du ticket (comparaison canonique des chemins dite au code, compteur de lectures REJETÉES rendu avec la mesure, tests sur dépôt jetable : casse différente comptée, hors racine compté comme rejet) confronté au diff du worktree `.wt-gates-A` sur `10fff1c3b` — la comparaison d'octets vivait en DEUX copies identiques (`enregistreur-lectures.mjs:51` cité par le ticket ET `enregistreur-hooks.mjs:71-73`, thread des hooks, même bug pour la même raison), mesuré par le codeur et vérifié par l'orchestratrice au diff ; les deux volets consomment désormais UNE primitive feuille `scripts/docs/lib/chemin-mesure.mjs` (`canoniser` : `fs.realpathSync.native` rend la casse du DISQUE et suit jonctions et noms 8.3, repli sur la forme résolue pour un chemin absent, `ENOENT` rattrapé ; `relatifSousRacine` : décision « hors racine » sur `path.relative`, qui compare déjà sans la casse sur win32 — `..`, `..\`, ou chemin absolu d'un autre disque, le piège du préfixe `docs`/`docsx` tenu, sondé par le juge) ; la racine est canonisée UNE fois à l'installation, mémo chemin → canonique (0,048 ms par appel natif, double traversée `openSync` + `readFileSync`) ; le compteur `cheminsRejetes` compte des CHEMINS distincts par processus, canonisés (deux casses du même fichier hors racine = 1), sommés entre processus — unité dite aux trois sites et au `.d.mts` — rendu AVEC la mesure : `fusionnerLectures` le somme et `build-all.mjs` le DIT au refus de sources insuffisantes (« N chemin(s) lu(s) hors racine, écarté(s) de la mesure ») et imprime une ligne nommant le générateur dès qu'il est > 0 ; le pied signé des docs dérivés ne change pas ; tests neufs sur dépôt jetable (`enregistreur-lectures.test.mjs`) — casse différente COMPTÉE sous la casse du disque, hors racine compté rejeté et dédupliqué par casse, refus à trois arguments ; mutation (comparaison d'octets remise, puis suffixe du refus retiré + rejet non canonisé) → rouges nommés, remise à l'octet ; mesure sur un vrai générateur (`build-index-moteur.mjs` : 166 fichiers, 6 dossiers, 0 écrit, 0 rejeté, set identique avant/après mémo) ; `node --test` cinq fichiers consommateurs 91/91, eslint 0 (`.d.mts` compris), `typecheck:fast` 0, rejoués par l'orchestratrice dans le worktree ; juge de diff : TIENT SOUS CORRECTIONS, deux bloquants (compteur rendu à personne, `.d.mts` menteur) et cinq souhaitables, tous portés puis rejoués.

## Restes
- Le thread des hooks (`enregistreur-hooks.mjs`) ne rend aucun compteur de rejets (format `.hooks.jsonl` = chemins retenus seulement) -> RAS : sa DÉCISION est corrigée par la même primitive (plus aucune lecture perdue par la casse) ; l'absence de compteur est dite au code (`empreinte-sources.mjs:112-113`) et le thread principal, qui porte toutes les lectures des générateurs joués par `--import`, rend le sien
- Deux autres comparaisons « sous la racine » par octets, hors périmètre : `scripts/guards/lib/arbreImbrique.mjs:26`, `scripts/migrations/replay-head.mjs:115` -> RAS : jugées HORS CLASSE sur pièces (juge de diff) — la première est une terminaison de remontée d'ancêtres dont `courant` dérive par `dirname` de `resolve(racineAbs, …)` et porte donc les octets de la racine (la casse ne peut pas diverger ; un `realpathSync` par ancêtre sur des chemins stagés parfois absents serait faux), la seconde est un garde d'effacement fail-CLOSED (une divergence de casse y REFUSE de supprimer, polarité inverse de la perte silencieuse de l'enregistreur)
- Une source lue via une JONCTION pointant hors de la racine devient un chemin hors racine après `realpathSync.native` : comptée rejetée, pas retenue -> RAS : direction de la perte DITE au JSDoc de `chemin-mesure.mjs` ; mesuré par le juge : `.claude/memory` n'est une jonction ni sur l'arbre principal ni sur le worktree (`lstat().isSymbolicLink() === false`), aucune perte aujourd'hui, et le compteur rendu la nommerait
- La ligne de journal de `build-all.mjs` (générateur nommé quand le compte est > 0) n'a pas de test : la produire exigerait un vrai générateur lisant hors racine -> RAS : le chemin de REFUS qui porte le même compte est verrouillé et muté (`enregistreur-lectures.test.mjs:88-90`), la ligne de journal en est la simple impression
- L'import de `installer` par le test fait entrer `enregistreur-lectures.mjs` dans le graphe statique de `test:hooks` : +1 entrée au stock des écrivains atteints (`ecrivainsAtteints.test.mjs`) -> RAS : l'écriture est la sienne propre (`<WFRP_LECTURES_SORTIE>.<pid>.json` sous `os.tmpdir()`, derrière la porte d'environnement), déclarée avec sa mesure et sa date, `CLIQUET:` porté au message ; `ECRIT_LU` inchangé (rien de neuf n'est écrit dans l'arbre)

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 47 min, 2026-09-12) : canonisation juste et prouvée (91/91 ; mutation par copie hors arbre → un seul rouge nommé « set rendu [] (attendu la casse du DISQUE) »), piège du préfixe et cas inter-disques tenus par les trois clauses de `relatifSousRacine` (sonde `path.relative`), coût mesuré (0,048 ms/appel), chemin inexistant rattrapé, jonction sondée sur une jonction réelle et sur les deux arbres ; DEUX BLOQUANTS — le compteur n'était rendu à aucun consommateur de production (17 occurrences, toutes en lib et tests ; `build-all.mjs:499` ne lisait que `fichiers`/`dossiers`) et `empreinte-sources.d.mts` déclarait encore trois champs — portés par le codeur (refus et journal de `build-all`, `.d.mts` à jour) ; cinq souhaitables portés (une unité `cheminsRejetes`, rejet canonisé, mémo, direction de la perte par jonction au JSDoc, `restaurer()` et le `CLIQUET` nommés au message) ; deux sites hors périmètre jugés hors classe avec leurs raisons. Non joué par le juge : `docs:build` réel avec rejets en vie, et les gates — jouées sur la tête du lot avant le push.
