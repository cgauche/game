# Solde #1720 « Stocks nominatifs : un renommage pur d'un porteur est refuse comme une croissance +N par les deux portes » — 2026-09-12

VERIFIE: l'attendu du ticket (un renommage pur rend `net = 0` aux deux portes par appariement des entrées retirées sous l'ancien chemin et ajoutées sous le nouveau ; un test sur dépôt jetable le verrouille pour les deux portes, patron `plageStock.test.mjs` « NAÎT puis GRANDIT ») confronté au diff du worktree `.wt-gates-A` sur `e560eb0ed` — PRÉMISSE DU TICKET CORRIGÉE PAR LA MESURE (sonde du codeur sur dépôt jetable, re-mesurée par le juge sur sept cas) : la porte au COMMIT ne refusait pas `+6`, elle était AVEUGLE — `git diff --numstat` sans `--no-renames` replie le renommage en un chemin `scripts/guards/lib/{aStock.mjs => bStock.mjs}` qu'aucun motif de porteurs ne voit et qu'aucun pathspec n'égale, verdict `null` ; un `git mv` suivi de N entrées de plus passait donc MUET au commit (mesuré : renommage + 1 → commit `null`, push `+7`) ; au PUSH, le diff d'une suppression porte `+++ /dev/null`, les entrées du porteur supprimé n'étaient comptées nulle part et le renommage n'était qu'un porteur neuf de N entrées ; UNE règle dans le socle `scripts/guards/lib/stocksNominatifs.mjs` `croissanceDesStocks`, dont les deux portes héritent : les AJOUTS sont attribués au post-chemin (`+++`), les RETRAITS au PRÉ-chemin (`--- a/…`), l'un et l'autre lus dans la seule ZONE D'EN-TÊTE (drapeau posé par `diff --git `, levé au premier `@@`) parce qu'une ligne de CONTENU retirée qui commence par `-- ` est une ligne de diff `--- …` — la première version du geste la lisait comme un en-tête et neutralisait tous les retraits suivants (faux `+1` sur un stock qui a MAIGRI, régression mesurée par le juge sur un diff produit par git, verrouillée par ce cas exact) ; puis `apparierLesDeplacements` : une entrée qui quitte un porteur DISPARU (post-chemin `/dev/null`) et reparaît à l'identique (texte, un pour un) ailleurs ne compte nulle part, BORNE dite au JSDoc comme un conservatisme fail-closed (entre deux porteurs VIVANTS le retrait reste créditable à son site, une redistribution délibérée se dit par `CLIQUET:` chez le receveur, d'un porteur supprimé rien ne reste à créditer) ; côté commit, `diffDuCommit` expose `cheminsSansRenommage()` (`--numstat --no-renames`, un renommage déplié en ses deux bouts) et `analyzeDiffDuCommit(raw, chemins)` en dérive `fichiers`/`touchesSrc`/`touchesUi` pour TOUTES les portes (la demi-migration relevée par le juge — solde `corrigé dans ce commit <nouveau>:N` refusé « ABSENT de ce que ce commit emporte », porte d'écran aveugle sur un renommage d'UI — est fermée), `totalLines` restant sur le numstat replié (le VOLUME écrit, un renommage n'écrit rien), la relecture dépliée n'étant payée qu'en présence d'un repli ; table des cas mesurée par le juge, IDENTIQUE aux deux portes : renommage pur → net 0 ; renommage + 1 → `+1` sur le nouveau chemin, exemple = l'entrée ajoutée ; renommage − 1 → aucun refus (décroissance non créditée, dite) ; transfert entre deux porteurs vivants → `+1` (borne) ; split d'un porteur supprimé sur deux neufs → net 0 ; fichier créé → `+N` sans rien attribuer à `/dev/null` ; diff binaire → rien ; tests sur dépôts jetables réels (`git mv`) : `plageStock.test.mjs` (push : pur, +1, split, −1, ligne `-- ` retirée sur diff git réel) et `solde-ticket-guard-driver.test.mjs` (commit par le DRIVER réel : pur, +1, split, −1, renommage d'UI + solde citant le nouveau chemin accepté et preuve visuelle réclamée) ; mutations remises à l'octet par hash : appariement débranché → les deux portes rougissent en nommant `+3` ; lecture des porteurs remise sur le numstat replié → trois rouges, le driver montrant à l'octet le chemin `{ancien => nouveau}` ; `--- ` lu hors zone d'en-tête → le cas du juge rougit au compte près ; `node --test` quatre fichiers 293/293, eslint 0, rejoués par l'orchestratrice ; juge de diff : TIENT SOUS CORRECTIONS, deux bloquants et quatre souhaitables portés.

## Restes
- Le corps du ticket disait « refusé `+6` par les deux portes » -> RAS : prémisse corrigée par la mesure (la porte au commit était aveugle, pas refusante — défaut plus grave, couvert par le même geste et son contrat positif « renommage + 1 = +1 aux deux portes »)
- L'appariement se fait par TEXTE d'entrée, sans provenance : un porteur supprimé et un ajout fortuit du même chemin ailleurs s'annuleraient ; un REFORMATAGE d'entrée (guillemets, entrée déplacée et modifiée) échappe à l'appariement et se recompte `+1` (la réindentation, non : `trim()`) -> RAS : angles morts DITS au JSDoc du socle, mesurés par le juge ; borné au porteur DISPARU, l'appariement ne peut pas servir à faire grandir un porteur vivant
- Le déclencheur de la relecture dépliée est le littéral ` => ` dans le numstat : un nom de fichier qui le contiendrait paierait un `git diff` de plus -> RAS : jamais un verdict faux, seulement une lecture de plus, dit au code
- Le contournement historique `CLIQUET: +N` sur un renommage (précédent C3c-1, fiche `game-train-chirurgical-portes-sur-l-index`) -> RAS : faux par construction, plus jamais nécessaire — le cas est net 0 aux deux portes

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 2026-09-12) : sept cas sondés sur dépôts jetables aux deux portes, verdicts identiques (l'invariant « une seule règle » tient), prémisse (a) confirmée (chemin replié invisible aux porteurs) ; DEUX BLOQUANTS mesurés — une RÉGRESSION (ligne de contenu `-- ` retirée lue comme en-tête : HEAD `[]`, geste `+1`) et une DEMI-MIGRATION (`cheminsSansRenommage()` câblé à un seul consommateur : solde sur renommage refusé, `touchesUi` faux) — portés par le codeur et verrouillés par des tests sur diffs produits par git et par le driver réel ; quatre souhaitables portés (raison de la borne réécrite en conservatisme fail-closed, angle mort du reformatage dit, split et −1 verrouillés) ; branche morte `diff --git ` de `ENTETE_INERTE` purgée. Non joué par le juge : les mutations A/B du codeur telles quelles (couvertes par ses sept cas et sa sonde de régression) ; les gates se jouent sur la tête du lot avant le push.
