# Solde #1688 — 2026-09-09

VERIFIE: l'attendu du ticket confronté à l'arbre — (1) champ `taille` (catégorie de Taille du LDB 85 l.346-354, nœud `sizeCategorySchema` de la grammaire) EXIGÉ sur `StructureData` (`defs/structures.ts`), posé sur les 24 entrées de `structures.json` comme VALEUR MAISON éditable au Codex : le RAW dit « Le MJ doit déterminer la Taille de la Structure attaquée » (AA 10 l.98) — cas 1 du credo, chaque entrée porte la règle dite dans son `maison` (mur d'enceinte/rempart/terrassement/porte de ville/herse/navire = Énorme, l'exemple RAW étant un mur de pierre Énorme ; porte, clôture, palissade, mantelet, mur léger, chariot, chaloupe = Grande), et le refine du def refuse une `taille` sans `maison` ; (2) `woundsFromHit` compte le BE d'une Structure `1 + écart positif de catégories` (`structureEnduranceMult`, `src/engine/structures.ts`, source unique lue par le calcul ET par le journal), armes à Atout Siège exemptes (`weaponHasCap(w,'siege')`), avant les PA ; la Taille de l'attaquant est passée par les 11 sites de production, absente elle se rabat sur Moyenne (`effectiveSize`, LDB 14 l.128) ; `siegeMultiplier` (ADE II 08) et `sizeDamageMultiplier` (LDB 85 l.361) intouchés ; (3) bancs : mur de château (Énorme, BE 6) vs humain à l'épée → BE 18 (l'exemple RAW), arme de siège → 6, Taille égale → 6, Structure Grande vs Moyen → ×2, journal portant le terme ; multiplicateur débranché → 6 rouges nommés ; (4) journal de combat : la ligne de Dégâts d'`applyHit` dit « BE 6 × 3 (Taille Énorme contre Moyenne) = 18 » ; (5) `docs/raw/combat.md` `Implémente` régénéré (`AA 10 l.98` porté par le code), aucune paraphrase de règle en commentaire (réfs nues), les deux commentaires-pointeurs « non implémentée (#1688) » d'`ai.ts`/`targetingModes.ts` morts ; aucune entrée `raw.manifest.json` nécessaire (livré en un train).

## Restes
- Le codeur avait posé `size` sur le `Combatant` d'une Structure : le juge a mesuré CINQ lecteurs que le RAW ne soutient pas (`SIZE_RANGED_MOD` +40 au tir, `footprintN` 1 → 3 cases, `sizeDamageMultiplier`, `forceOpposedOutcome`, `peurTerreurFromSize` Terreur 2) et la doctrine `footprint.ts:29-32` (empreinte découplée de la Taille) — AA 10 l.98 détermine la Taille POUR CE COMPTE-LÀ (l.96 : le corps à corps touche automatiquement ; LDB 85 l.344 : le Trait Taille « représente les créatures ») -> corrigé dans ce commit : src/engine/structures.ts:120 (`structureEnduranceMult` lit `StructureData.taille` par `findStructureById(target.refId)`, aucun `Combatant` de structure ne porte `size` — garde dérivée du dataset, sondes du juge promues)
- `SIZE_LABEL` (`src/engine/size.ts`, Record au stock gelé de #1714) écrivait « Très Petite » là où le LDB 85 l.349 imprime « Très petite » ; `sizeCategorySchema` nommé ici porte la graphie RAW, et la divergence préexistante est rétablie ici -> corrigé dans ce commit : src/engine/size.ts:54
- `woundsAtCritLocation` (`combat.ts`) appelle `woundsFromHit` sans attaquant : inatteignable par une Structure (aucune Localisation ni Critique de structure, ADE II 8) -> RAS : le multiplicateur y vaut 1 par construction (`isStructure` faux)
- Un profil de dégâts de fixture `{plusBF:false, flat:0}` improvisé PERD l'Atout Siège (LDB 62 l.135) — piège de test nommé -> RAS : les bancs de ce train construisent leurs armes depuis le catalogue

## Réfutation
verdict: CONFIRMÉ — grounding (lecteur, lecture seule) : règle absente du moteur, socle de Taille existant (`sizeGap`), point d'injection unique dans `woundsFromHit`, table des Tailles et distinction avec le multiplicateur de DÉGÂTS du LDB 85 l.361 et l'Atout Siège ; codeur : RAW re-vérifié au Source, exemple « 6 fois 3 » reproduit par le banc ; juge de diff : ACCEPTE-SOUS-CORRECTIONS — formule et ordre BE × N avant PA conformes à l'exemple RAW « 6 fois 3 », effet de bord de `Combatant.size` RÉFUTÉ et corrigé (5 lecteurs mesurés), 21 `maison` copiées-collées → une raison par entrée, `attackerSize` requis avec garde nommée sur `woundsAtCritLocation` (AA 10 l.105), tombale d'`inanimate.ts` corrigée (angle mort de `comment-poison-guard` nommé), graphie RAW « Très petite » rétablie dans `SIZE_LABEL` ; les trois anciens tests verrouillaient bien l'ABSENCE de la règle.
