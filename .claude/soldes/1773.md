# Solde #1773 — 2026-09-16

VERIFIE: le DoD AMENDÉ du ticket (commentaire du 2026-09-15 : l'hypothèse (1) « armer l'empreinte sur une SOURCE stagée » est RÉFUTÉE — 59,3 % des commits auraient exigé un `docs:build` avant de committer, alors que l'étape docs de `ops:publier` re-signe une fois par train) confronté au diff sur `.wt-1772` (base origin/main 9c8da035a) — (1) `build-all.mjs --check` juge le PIED des 4 cibles `check: false` (`piedsDesNonVerifiables`, même calcul que `--empreinte` via `refusDuPiedAuCommit`, un seul message et un seul remède pour la classe) : pied périmé → non vert, cible et générateur nommés ; pied juste → vert sans rejouer le script ; cible hors index → rien à juger ; générateur jamais mesuré → refus nommé ; surcoût mesuré 0,33 s (`indexGit` 0,03 s, lu une fois) ; les pieds périmés s'impriment AVANT la boucle des générateurs ; (2) `touchesDocSources` (`scripts/git-hooks/docs-rebuild.mjs`, consommé par l'étape docs de `ops:publier` et par les hooks post-merge/post-rewrite) DÉRIVE de `docs/.sources-lues.json` au lieu d'une regex de préfixes qui ratait 49 sources mesurées : source lue ou cible signée, parent listé, frère d'une source, tout `docs/` ; fail-closed (lot inconnu ou mesure illisible → régénérer) ; mesuré sur la mesure réelle : `.claude/memory/user-x.md` → true, `.github/workflows/ci.yml` → true, `tsconfig.json` → true, `README.md` → false, `Source/…/13 - Combat.md` (non lu) → false ; (3) le refus du pre-commit NOMME les cibles à régénérer et la commande (`ciblesDesArmes`) ; `generateursArmes` reste à l'armement par CIBLE stagée, son test d'origine restauré à l'octet ; (4) commentaires « DÉFAUT CONNU » réécrits au régime réel, sans tombale, sans réf par numéro de ligne. Rejoué par l'orchestrateur : `node --test` sur `build-all-check`, `enregistreur-lectures`, `docs-rebuild`, `merge-docs` → 53/53 ; eslint 0 sur les 9 fichiers ; `build-all --check` réel sur l'arbre : nomme les deux pieds périmés par le lot lui-même (`reprise-apres-pause.md`, `consommateurs-de-champs.md`) en 60 s ; mutations du codeur : `piedsPerimes = []` → câblage rouge ; regex remise dans `touchesDocSources` → 3 tests rouges dont la classe `.claude/memory/user-x.md`.

## Restes
- Le câblage de `piedsDesNonVerifiables` sous `--check` n'est prouvé que par des assertions de regex sur la source de `build-all.mjs` ; le comportement l'est en contexte forgé sur la fonction seule ; forme exacte mesurée par le codeur (`executer({ argv, cwd, generateurs })` + scripts fictifs réels dans le dépôt fixture, 30-40 min) portée par le ticket -> #1775
- `merge-docs.test.mjs:171-178` attendait `Source/Warhammer v4 - LDB/13 - Combat.md` → true ; la nouvelle porte rend false (ce fichier n'est lu par aucun générateur, 312 autres fichiers de `Source/` le sont) -> RAS : un Source non lu ne périme aucun pied — l'ancienne regex disait « tout `Source/` » par prudence, la mesure dit le vrai
- La nouvelle porte rend `true` plus souvent (`docs/`, `.gitignore`, `tsconfig.json`, tout frère d'une source) -> RAS : le surcoût est un `build-all --check` qui saute les générateurs frais (~60 s sur cet arbre), jamais un `docs:build` complet
- `scripts/ops/board.test.mjs` n'était pas une source lue de `reconcile.mjs` (inexactitude du constat du ticket) ; seul `src/data/scene-prose-graphie-guard.test.ts` expliquait le pied périmé -> RAS : le mécanisme et la correction sont les mêmes

## Réfutation
verdict: CONFIRMÉ — juge de diff (2026-09-15, lecture seule, `.wt-1772`, 7 lentilles sur le cumul) : lentille 4 TIENT (`piedsDesNonVerifiables` : cibles à motif déjà résolues par la mesure, cible hors index → `continue`, entrée absente → refus nommé, coût 0,33 s ; pas de boucle refus→build→refus : la passe complète joue les `check: false` et re-pose leur pied, `publier` committe) ; lentille « décision » FRAGILE puis corrigée : la regex `touchesDocSources` laissait l'étape docs sortir verte SANS jouer `--check` sur 53 sources mesurées hors préfixes — corrigé dans le même lot (dérivation de la mesure, test de la classe, mutation) ; lentille 5 : le test supprimé l.262-274 est RESTAURÉ (l'assertion « une source stagée seule n'arme rien » a repris sa place), aucune tombale ni excuse ; lentille 7 : une seule rédaction du jugement de pied (`refusDuPiedAuCommit`), filtre `--only` sans appelant retiré, réfs par ligne remplacées par des noms ; lentille 6 FRAGILE → reste #1775 (câblage par regex de source). Push SANS `ops:publier` : le pied périmé est nommé par `docs:check` au résumé des gates, après le mur complet — arbitrage de #1772, dit là.
