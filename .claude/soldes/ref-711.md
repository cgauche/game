# ref-711 — Lot 1 : algèbre Condition étendue (skill/career/species/status, PARTY-level)

## Juge
Juge moteur adversarial (6 lentilles) — verdict initial PARTIEL, griefs comblés. CONFIRMÉ : `matchParty` (`flowCore.ts:264`) correct (`all` sur groupe vide → false via `length>0 && every`, `any` → `some`, pas de vacuously-true) ; `skill` (seuil `(s.advances ?? 0) >= (cond.advances ?? 0)`, `spec` optionnel, id inconnu → false propre) ; extraction `statusOf(career, careerLevel)` behavior-preserving (`actorStatus` délègue à l'identique, mêmes replis « Bronze 1 »), `statusMeets` compare palier PUIS standing, `TIER_RANK`/`parseStatus` RÉUTILISÉS non dupliqués ; chemin dialogue peuplé (`conditionCtx` recopie `party`, `Combatant` satisfait structurellement le type élargi), aucun call-site changé ; cases UI d'exhaustivité minimales et correctes ; tests positifs réels (status via vrai `createHero`). Trois griefs relevés et CORRIGÉS par l'orchestrateur en direct : (1) fuite d'id BRUT au joueur dans `humanize.ts` (« dresser »/« soldat »/« humains-reiklander » au lieu des libellés — violait la doctrine du fichier) → résolus via `refLabel('skills'/'careers'/'races', …)` (⚠ catégorie species = `'races'`, pas `'species'`, vérifié dans `data/index.ts:2255`) ; (2) membres `dead` non filtrés → `matchParty` gate désormais sur les VIVANTS (un héros tombé ne prête pas sa compétence, documenté) ; (3) test `'all'` sur groupe vide + exclusion du mort → ajouté.

## Juge-Vision
Aucun ÉCRAN rendu n'est modifié par ce lot. Le `src/ui` touché est de la LOGIQUE non-visuelle : `humanize.ts` (résolution d'id→libellé des 4 nouveaux kinds, chemin de texte) et `ConditionEditor.tsx` (cases `recast`/`condSummary` minimales d'exhaustivité, AUCUN `KIND_OPTIONS`/formulaire ajouté — reporté au Lot 2). Ces changements sont couverts par le juge moteur (lentille 5, cases UI correctes/minimales) et la suite ; il n'y a pas de surface visuelle à juger sur captures. Les formulaires de saisie (vraie surface d'auteur) et leur recette viennent au Lot 2 (`corrige #711`).

## Réfutation
verdict: CONFIRMÉ
Réfutation adversariale résolue : le moteur (`flowCore`/`social`) est solide et behavior-preserving (statut extrait sans changement, comparateur canonique réutilisé, zéro cycle d'import vérifié), les 4 kinds party-level sont corrects sur les cas limites (groupe vide, seuil non atteint, id inconnu, membre mort), et les 3 griefs du juge (fuite d'id, filtre `dead`, test manquant) sont comblés et re-vérifiés au diff par l'orchestrateur. Gates relancés par moi : typecheck 0 imputable (hors `_tmp-*`), suite complète verte, `flow.test` 28/28. Ce lot ne livre que le MOTEUR + l'exhaustivité ; l'éditeur de saisie des 4 kinds + les docs authoring + la recette dialogue ferment #711 (Lot 2).

2026-07-23
