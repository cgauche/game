# Solde #1834 — 2026-09-20

VERIFIE: (geste publié par `09880b160`, entré dans main le 2026-09-20 ; ce commit de clôture porte le solde et la revue de palier que le garde exigeait) le contrat du ticket (« `combat-ui.css` ne porte plus aucune règle de la couche partagée ») est tenu : `.panel h3`, `.panel label` et `.radio` vivent dans `src/ui/styles/components.css` avec `.panel`, la tranche ≤700 de `.panel h3` a quitté `base.css` pour le `@media` de la même feuille, le stock est recalé (`regen --check` vert, identité 1382 / espacement 628). L'orchestrateur a relu le diff, rejoué `scripts/ui/css-conservation.mjs` (12 inversions, les 8 du ticket + 4, relues une à une) et comparé AU PIXEL le dossier de navire — le seul écran atteignable où `.panel h3` s'applique sans rivale gagnante — à la capture d'avant : 0 pixel différent sur l'écran ENTIER à 1440 (1 296 000 px) et à 360 (266 400 px), console à 0 erreur.

## Restes
- La règle générale `.panel label` (tout `<label>` sous une surface est mis en colonne) est elle-même le défaut : `.radio` et `.coop-assign-row` la combattaient à coups de `!important`, et l'item « Importer JSON… » du menu Fichier de l'éditeur (`src/ui/editor/EditorToolbar.tsx:111`) en hérite à tort ; neutralisée par CSSOM sur 16 états, elle a ce dépendant vivant, donc elle DÉMÉNAGE ici au lieu de mourir -> #1838
- DoD « captures à 1440 et 360 des quatre écrans concernés » (inspecteur, sélecteur de dé forcé, zones et bandes du créateur) : le juge a relu poseur par poseur que AUCUNE des rivales n'a d'ancêtre `.panel` (`InspectPanel.tsx:42` pose `modal inspect-panel`, `CharacterCreator.tsx` ne pose aucun token `panel`, les 11 hôtes de `Band` non plus) — les 8 bascules ne visent aucun élément réel -> RAS : rien ne peut bouger sur ces quatre écrans ; la preuve au pixel est portée sur l'écran où les règles s'appliquent (dossier de navire), et les valeurs calculées de `.zone-section > h3` (11.5px), `.creator-band-head h3` (16.5px), `.rm-die-pick > label` (gap 2px) ont été relevées identiques avant / après par le codeur
- Le tableau du ticket attribuait aux bascules un effet visible ; la table du codeur attribuait à `.panel h3` le `font-size: 15px` des sections de port, qui vient de `.port-section h3` (`world-meta.css:264`), et comptait « 0 site sur massBattle » alors que `MassBattleView.tsx:255-256` en pose -> RAS : erreurs de MESURE du plan, relevées par le juge et sans effet sur le geste — ces sites vivent dans des feuilles importées après `combat-ui.css`, leurs règles gagnaient déjà et gagnent encore
- Sans ses 4 `!important`, `.radio` (0-1-0) aurait PERDU contre `.panel label` (0-1-1) sous un panneau, alors que le catalogue affirme qu'elle en renverse l'empilement -> corrigé par 09880b160 src/ui/styles/components.css:68
- Le contrat de `label.radio` (renverser `.panel label`) n'a aucune instance aujourd'hui : aucun de ses 4 poseurs n'est sous `.panel` -> RAS : vrai par poids égal et ordre de déclaration dans la même feuille ; le contrat disparaît avec `.panel label` (#1838), lui donner un spécimen serait outiller une règle condamnée

## Recette visuelle
capture: public/qc/soldes/1806-2d-g2-1834-navire-dossier-1440.png — kit CDP du 2026-09-20 (port 5202) : `scenario('voyage-maritime', 42)`, clic réel sur le bouton du dossier de navire ; les quatre titres de section (`.panel h3`) rendent à l'identique de la capture d'avant, 0 pixel différent sur l'écran entier.
capture: public/qc/soldes/1806-2d-g2-1834-navire-dossier-360.png — même route à 360 × 740 : la tranche ≤700 de `.panel h3` (enroulement, gouttière 6px), fusionnée dans le `@media` de `components.css`, s'applique comme avant ; 0 pixel différent.

## Réfutation
verdict: CONFIRMÉ — deux passes de juge adversarial en lecture seule (2026-09-20). Sur le PLAN : a réfuté la PREUVE du codeur (« 0 site hors navire / port » est faux au code : `MassBattleView`, `InterludeScreen`, `LandMarketView`, `ShipSheet`, `SeaActivitiesModal`, l'éditeur posent des `h3` / `label` sous `.panel`) tout en confirmant la conclusion par une autre voie : ces rivales vivent dans des feuilles importées APRÈS `combat-ui.css` et gagnaient déjà ; seules les feuilles intermédiaires pouvaient basculer, et aucune de leurs rivales n'a d'ancêtre `.panel`. Sur le DIFF : n'a réfuté ni le recul de `.panel h3` / `.panel label` (rivales énumérées feuille par feuille), ni le retrait des `!important` de `.radio` (`.ed-dim label`, `.money-fields label`, `.enemy-row label`, `creator.css:138` : aucune ne matche aux 4 sites), a validé `label.radio` ; a exigé un commentaire détaché de son `@media` (`components.css`), corrigé par `09880b160`.
