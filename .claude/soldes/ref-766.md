# ref-766 — Lot B (bibliothèque de projets migrée localStorage → IndexedDB)

## Juge
Juge adversarial d'intégrité (enjeu : perte de projets de campagne éditeur, 7 lentilles) : zéro perte à la migration (localStorage `KEY` UNIQUEMENT lue, jamais effacée = filet ; flag `MIGRATED_KEY` posé APRÈS la copie ; `cache=legacy` en retour), idempotence (double garde `stored>0` + flag, purge respectée sans résurrection), lecteurs SYNC intacts (`cache ?? readLocalStorage`, `main.tsx` awaite l'init avant render), API sync inchangée (3 consommateurs Editor/ProjectModals/PartyScreen non cassés), zéro poison — SAIN sur les chemins réalistes de préservation. Quatre réserves FRAGILE (aucune régression vs la baseline localStorage qui échouait déjà au quota) ticketées #776 ; l'overclaim du commentaire `main.tsx` (« aucun risque de hang ») CORRIGÉ dans ce commit.

## Réfutation
verdict: CONFIRMÉ
Réfutation adversariale confirmée : la migration localStorage→IndexedDB ne peut PAS perdre de données sur les chemins réalistes (localStorage jamais `removeItem`, flag après copie réussie, migration idempotente gardée) ; la garde `HAS_IDB` fait NO-OP proprement en jsdom (le cache reste servi) ; `initLibrary` ne rejette jamais (try/catch → repli localStorage) donc pas d'écran blanc par erreur. Les 4 FRAGILE (persistance best-effort, stranding de migration partielle, hang edge d'`indexedDB.open`, chemin idb non testé) sont des DURCISSEMENTS de robustesse, pas des régressions → ticket #776. Gates relancés par l'orchestrateur : typecheck 0 (hors `_tmp-*` d'autres sessions), suite complète 12877 verte, `projectLibrary.test.ts` 10/10.
