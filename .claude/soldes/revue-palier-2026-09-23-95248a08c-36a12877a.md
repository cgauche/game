# Revue de palier — fenêtre 95248a08c..36a12877a — 2026-09-23

verdict: PARTIEL

Fenêtre `95248a08c..36a12877a` : **58 commits, dont 32 de substance** au sens de l'instrument, et **2 fermetures**. Jugée en LECTURE SEULE.

Épinglage, fait avant toute mesure :
- `git rev-parse 36a12877a` → `36a12877a3a579e6879cce02f794dc5dd0c0127e`, égal à `origin/main` ;
- `git merge-base --is-ancestor 95248a08c 36a12877a` → `True` : chaînage confirmé.

Le worktree d'exécution `.wt-1343-lot-b` porte `cd00ad09c`, qui est POSTÉRIEUR à la fenêtre. Toute mesure passe donc par `git show <sha>:<chemin>`, `git diff` ou `git log`, jamais par le disque. Seule exception : `scripts/guards/lib/stocksNominatifs.mjs` et `stock.mjs` sont importés depuis le disque, après avoir vérifié que `git diff 36a12877a HEAD --stat` est vide sur ces deux fichiers.

Contrôle POSITIF que le travail jugé est bien là :
- `scripts/raw/source-puces-stock.json` : 466 entrées à la tête, 0 à la base ;
- `src/ui/styles/combat-ui.css` : ABSENT à la tête, 165 déclarations à la base ;
- `combat-start-splash.css` : 26 déclarations ;
- `src/data/primitives.manifest.json` : 93 → 97 entrées ;
- `CLAUDE.md:70-74` porte « ET en HAUTEUR ».

**Côté livraison, le cumul est le plus propre des huit paliers.**
- **CI** : 31 courses CI sur `main`, 31 `success`, `run_attempt = 1` partout. S'y ajoute un Canari vert (`c32f01982`, 2026-09-21T12:29), qui referme #1815.
- **Annotations** : `JUGE:` et `REFUTATION:` sur 32 commits de substance sur 32. Le palier précédent était à 15/22 : SOLDÉ.
- **Cliquets** : 21 lignes `CLIQUET:` sur 11 commits. Rejouées par le lecteur de la porte, 20 sont exactes. La 21ᵉ est hors du lecteur mais exacte à la main (`raw.manifest` 11 → 12).
- **Fermetures** : deux, chacune avec son solde DANS L'INDEX du commit de fermeture. Le reste routé est OPEN (#1838). Aucune fermeture hors commit, hors Canari.
- **Stock CSS** : sa décrue n'est plus un déménagement à 94 %. Le stock baisse de 115 sites pour 79 déclarations réellement disparues. Chaque lot CSS VENTILE au message SOLDÉ / DÉPLACÉ / EXEMPTÉ (`d25652e73`, `cf4c2ee7b`, `09880b160`).

**Ce qui le maintient en PARTIEL :**
1. **Stock CSS (grief 1 hérité, TENU sur l'outillage).** Le partage entre soldé et déménagé vit dans la PROSE, pas dans l'instrument. `components.css`, qui est exempte, gagne +29 déclarations (747 → 776). L'organisme neuf `combatStartSplash` sort 26 déclarations du stock par simple déclaration. `hud.css` passe de 13 sites à 0 alors que ses déclarations restent à 84.
2. **Cardinal vivant recalé à la main** sur tout le corpus de données : `src/data/structures-contrat.test.ts:1699` `toBe(2280)`, sous un journal de recalages en commentaire. Coût : 5 rouges de CI de branche (`56d7f36c9`).
3. **`src/state/combatManeuvers.ts:440` intact** pour le 3ᵉ palier (#1817 OPEN).
4. **Porte des stocks toujours aveugle aux jetons à espace** : à la tête, elle lit 21 entrées sur les 24 de `REPEINTS_STOCK` (#1840 OPEN).
5. **`Source/` ne compte pas comme substance** pour l'instrument (`revuePalier.mjs:168`). Deux réécritures de la vérité citable échappent ainsi au compte de palier et à la porte du ticket.
6. **Les trois réserves d'outillage** du grief 6 sont intactes.
7. **`Claude-Session` à 14/32 (44 %)**, deuxième palier de baisse.

## Sortie brute de l'instrument

`npm run ops:faits-de-palier -- --base 95248a08c --tete 36a12877a` — `C:\Users\gauch\AppData\Local\Temp\wfrp-faits-de-palier\faits-95248a08c-36a12877a.json`. Digest SANS valeur réécrite :

```
== base :: "95248a08c"   == tete :: "36a12877a"   == depuis :: "2026-09-20"   == chainage :: "vérifié"
== commits :: 58   (substance:true = 32)
== fermetures :: [ #1826 → 44879e009 (solde:true) · #1834 → cae05c51a (solde:true) ]
== stocks :: {"refus":[],"notes":[],"commits":58,"plage":"95248a08c..36a12877a","indisponible":null}
== fermeturesHorsCommit :: { "disponible": true, "valeur":
     "[fermetures] fenêtre depuis 2026-09-20 · baseline 12 entrée(s)
      ### Fermetures hors commit depuis le 2026-09-20
      - #1815 [canari (exempté)] 2026-09-21T12:40:12Z par github-actions[bot] · completed · canari · Canari rouge — environnement ou suite cassés
      **Aucun écart à la baseline.**" }
== auditStock :: "[audit-stock] stock 5 entrée(s), observé 5 paquet(s) >= high … aucun écart au stock daté"
                 (JAUNE racine:vite, racine:vitest, server:miniflare, server:undici, server:ws — routées à #1726)
== coursesCi :: 58 shas → 31 portent "CI" completed/success, 1 "Canari" success (c32f01982), 27 → "courses":[]
== provenance :: commits/fermetures/stocks/chainage = script · fermeturesHorsCommit/coursesCi = gh
                 · auditStock = npm audit · revuePrecedente = git
```

Comptage des annotations (`annot.cjs`) : sur `j.commits` `substance:true`, les lignes `^JUGE:`, `^REFUTATION:`, `^Claude-Session:`, `^JUGE-VISION:` et `^CLIQUET:`. Légende des colonnes : J/R/S/V = JUGE / REFUTATION / Session / VISION.

```
36a12877a J R - - cliquet=0 feat(source) #1887        |  1bbf9c5c8 J R S V cliquet=0 refactor(combat) #1858
cf5b12bde J R - - cliquet=3 feat(atlas) #1873 #1825   |  6d98c59b0 J R - - cliquet=2 feat(atlas) #1825 F1-0
85a8237bc J R - - cliquet=1 feat(atlas) #1824         |  b7c288c68 J R S V cliquet=1 feat(ui) #1847
07d9f850d J R S V cliquet=0 fix(ui/frise) #1867       |  3a43b3709 J R - - cliquet=2 fix(source) #1820 R4
c32f01982 J R - - cliquet=0 fix(source) #1739 S1      |  8fe2a96eb J R S V cliquet=0 feat(hud) #1848
9502ddc2f J R - - cliquet=3 refactor(source) #1739    |  daa4b82cd J R S V cliquet=0 test(combat) #1852
1504175ba J R - V cliquet=0 refactor(source) #1739 S0 |  24cd5262c J R S V cliquet=0 fix(combat) #1852
7e8def34c J R - - cliquet=0 fix(source) #1820 R5      |  4be659d8e J R - - cliquet=1 fix(source) #1820 R3c
7702ccb01 J R - - cliquet=0 fix(source) #1820 R3b     |  95ece188e J R - - cliquet=4 feat(atlas) #1825 F0
760eb94a3 J R - - cliquet=0 feat(atlas) #1825 F0b     |  56d7f36c9 J R S V cliquet=1 fix(données/tests) #1678
36cd9e36b J R S V cliquet=0 fix(gardes) #1806         |  a671aa278 J R S V cliquet=0 feat(combat) #1678
77c2b633b J R S V cliquet=0 test(ui) #1806            |  1597da5cb J R - - cliquet=0 fix(source) #1820 R2
07c3834bd J R - - cliquet=0 fix(source) #1820 R3      |  d25652e73 J R S V cliquet=0 refactor(ui) #1806 #1835
53e50ddac J R - - cliquet=0 fix(source) #1820 R1      |  44879e009 J R - - cliquet=2 feat(gardes) corrige #1826
cf4c2ee7b J R S V cliquet=0 refactor(ui) #1806        |  7f98bebfc J R S V cliquet=0 refactor(ui) #1806 γ2
244a9ed05 J R - - cliquet=1 feat(raw) #1825 E2        |  09880b160 J R S V cliquet=0 refactor(ui) #1806 #1834
{ substance: 32, JR: 32, session: 14, avecCliquet: 11, total: 58 }
```

- **`JUGE:` + `REFUTATION:` : 32/32.** Trajectoire : 6/9 → 10/11 → 10/10 → 15/22 → **32/32**.
- **`Claude-Session` : 14/32 (44 %).** Trajectoire : 2/11 → 7/10 → 11/22 → **14/32**.
- Les 18 commits sans trailer sont TOUS des lots `source`, `atlas` ou `raw` (#1820, #1825, #1739, #1824, #1873, #1887). Tous les lots `ui` et `combat` le portent.

Autres sorties de `vis.cjs` :
- **Commit de substance qui touche `src/ui` ou `src/gameIso` (hors tests) SANS `JUGE-VISION:` : 1.** C'est `44879e009` (`InterludeScreen.tsx`, `LandMarketView.tsx`, `creator/CharacterCreator.tsx`, `jetProps/useTestJetProps.tsx`).
- **Commits classés `substance:false` qui touchent de la matière : 2.** `d2e9a77a9` et `86a0c76e1` ne touchent que `Source/…5e Core Rulebook/*.md` et `docs/raw/coverage.md`.

## Fermetures

| # | commit qui ferme | solde DANS L'INDEX | restes | cible | verdict du solde | fermeture |
|---|---|---|---|---|---|---|
| #1826 | `44879e009` | **OUI** — `.claude/soldes/1826.md`, 1er des 43 chemins de `git diff-tree` | 5 « corrigé dans ce commit » + 4 `RAS` argumentés, aucun renvoi | — | PARTIEL (juge vu AVANT 4 corrections, prouvées par bancs sans 2ᵉ passe) | `github-actions[bot]` 2026-09-20T16:37:09Z |
| #1834 | `cae05c51a` | **OUI** — `.claude/soldes/1834.md` + la revue précédente, seuls 2 chemins | 1 `-> #1838` ; 1 « corrigé par 09880b160 src/ui/styles/components.css:68 » ; 3 `RAS` | #1838 **OPEN** | CONFIRMÉ (deux passes de juge) | `github-actions[bot]` 2026-09-20T10:04:36Z |

```
$ gh api "search/issues?q=repo:cgauche/game+is:issue+closed:>=2026-09-20"  → total_count 3
  1834 completed · 1826 completed · 1815 completed (Canari rouge)
$ gh api repos/cgauche/game/issues/<N>/events (event=closed) → 1826, 1834, 1815 : closed_by=github-actions[bot]
$ gh issue view 1838 → OPEN « Zéro `!important` : 28 dans les feuilles, dont 11 posés par #1792 … »
```

Les deux fermetures tiennent :
- aucun `gh issue close` ;
- le renvoi « corrigé par 09880b160 » vise un commit publié, ancêtre de `cae05c51a`.

## Stocks et cliquets

### Les 21 lignes `CLIQUET:` rejouées par le lecteur de la porte (`cliq.mjs`, code en annexe)

```
cf5b12bde src/data/raw.manifest.json                        +1    0 -> 0        ECART (hors lecteur ; à la main 11 -> 12 = +1 OK)
cf5b12bde scripts/gates/ecrivainsAtteints.test.mjs          +2    137 -> 139   OK
cf5b12bde scripts/migrations/lib/1825-…-par-coeur.test.mjs  +2    0 -> 2       OK
85a8237bc scripts/gates/ecrivainsAtteints.test.mjs          +1    136 -> 137   OK
9502ddc2f scripts/raw/source-format-stock.json              +20   58 -> 78     OK
9502ddc2f scripts/gates/ecrivainsAtteints.test.mjs          +1    135 -> 136   OK
9502ddc2f scripts/raw/check-source-tables.test.mjs          +1    0 -> 1       OK
6d98c59b0 scripts/gates/ecrivainsAtteints.test.mjs          +2    133 -> 135   OK
6d98c59b0 scripts/hooks/registres-porteurs.json             +2    0 -> 2       OK
b7c288c68 scripts/guards/budget-contexte.mjs                +150  26808 -> 26958 OK
3a43b3709 scripts/raw/source-puces-stock.json               +466  0 -> 466     OK
3a43b3709 scripts/gates/ecrivainsAtteints.test.mjs          +2    131 -> 133   OK
4be659d8e scripts/raw/source-tables-stock.json              +36   679 -> 715   OK
95ece188e scripts/gates/ecrivainsAtteints.test.mjs          +7    124 -> 131   OK
95ece188e scripts/guards/lib/depotGabarit.test.mjs          +4    1 -> 5       OK
95ece188e scripts/raw/_lib.test.mjs                         +6    0 -> 6       OK
95ece188e scripts/raw/check-refs.test.mjs                   +1    0 -> 1       OK
56d7f36c9 scripts/guards/lib/horsStrateStock.mjs            +2    1176 -> 1178 OK
44879e009 scripts/guards/lib/livresRecopies.mjs             +1    0 -> 1       OK
44879e009 scripts/guards/budget-contexte.mjs                +1    26807 -> 26808 OK
244a9ed05 scripts/gates/ecrivainsAtteints.test.mjs          +4    120 -> 124   OK
```

**21 comptes justes sur 21.** C'est la première fenêtre sans un seul compte faux.

### Bilan net au lecteur de la porte (`stk.mjs`, tous les fichiers de `git diff --name-only 95248a08c 36a12877a`)

```
source-puces-stock.json +466 · source-format-stock.json +20 · ecrivainsAtteints.test.mjs +19 · toutes.mjs +8 (registre de portes)
depotGabarit.test.mjs +4 · primitives.manifest.json +4 (catalogue) · _lib.test.mjs +6 · (+1..+3 : 10 autres)
cssCouchesStock.mjs -115 · source-tables-stock.json -96 (811 -> 715) · ecrans-ui.json -2 · build-implemente -1 · check-entity-in-chapter -1
{ hausse: 542, baisse: 215 }
```

La hausse est à 86 % une NAISSANCE. `source-puces-stock.json +466` est la dette 4e que la garde neuve `raw:check-source-puces` mesure à sa naissance : environ 1 900 items de livres VF dont la puce imprimée est lue « 0 ». Ce n'est pas une dette neuve, c'est de la matière qui préexistait, rendue visible. Grief E.

### Le stock CSS, re-mesuré (grief 1 hérité) — `css.cjs`, `css2.cjs`

Méthode :
- feuilles `.css` de `src/ui` et de `src/gameIso`, listées par `git ls-tree` ;
- commentaires retirés ;
- déclarations comptées par la regex `[{;] prop: valeur`.

Cette méthode diffère de celle de la revue précédente : **seuls les DELTAS sont comparables**, pas les valeurs absolues.

```
95248a08c stock {IDENTITE:1400, ESPACEMENT:637, INLINE:97} = 2134 · 70 feuilles · 8072 décl · !important 34 {base 9, combat-console 7, combat-ui 6, editor 1, initiative-strip 7, party-dock 4}
36a12877a stock {IDENTITE:1317, ESPACEMENT:606, INLINE:96} = 2019 · 72 feuilles · 7993 décl · !important 26 {base 9, combat-console 5, editor 1, initiative-strip 7, party-dock 4}
  combat-ui.css        165 -> ABSENTE  stock 81 -> 0
  hud.css               84 -> 84       stock 13 -> 0     <- 13 sites sortis, matière à compte égal
  components.css       747 -> 776      stock  0 -> 0     <- feuille EXEMPTE, +29
  initiative-strip.css 165 -> 195 · portrait-tile.css 80 -> 90 · coop-panels.css -> 27 · coop-lobby.css -> 1
  combat-start-splash.css  -> 26       stock  0          <- organisme NEUF
  world-meta.css 712 -> 702 (stock 366 -> 357) · compendium.css 261 -> 256 (153 -> 148) · creator-presentation 81 -> 76 (33 -> 27)
  organismes du manifeste : [combatConsole, inspectPanel, equipmentPanel, gameStage] -> + combatStartSplash
```

Lecture :
- **Stock −115, corpus −79.** Au palier précédent : stock −1215 pour −71 au corpus.
- **La ventilation est désormais au message**, lot par lot :
  - `d25652e73` : « SOLDÉ 18 · DÉPLACÉ 6 (…`components.css`, feuille exempte) · EXEMPTÉ EN PLACE 7 » ;
  - `cf4c2ee7b` : « La moitié de la décrue du stock est donc un déménagement ».
- **`!important` : 34 → 26 (−8).**
  - Les 6 de `combat-ui.css` meurent : `.radio` ×4 retirés par `09880b160`, `.coop-assign-row` ×2 par `7f98bebfc`.
  - `combat-console.css` en perd 2 (7 → 5).
  - Les 9 de `base.css` (#1792) et les 11 relogés dans `initiative-strip.css` et `party-dock.css` sont intacts. #1838 est OPEN.

## Griefs de la revue précédente — suivi, un par un

| grief du 2026-09-20 | état | mesure |
|---|---|---|
| **1 — décrue CSS = déménagement, outil aveugle** | **TENU sur l'instrument, ATTÉNUÉ sur la matière** | Stock −115 contre corpus −79, et la ventilation est écrite dans 3 messages. MAIS `cssCouchesStock.mjs` rend toujours 3 collections par CLASSE, jamais par nature de baisse. `scripts/guards/lib/cssCouches.mjs:18-23` (`FEUILLES_PARTAGEES`) exempte toujours `base.css` et `components.css`, et `components.css` gagne +29. `hud.css` : stock 13 → 0 pour des déclarations 84 → 84. Un `git grep` de reclass / RECLASS / EXEMPT à `36a12877a` sur `scripts/guards`, `scripts/ui` et `scripts/hooks` ne rend aucune garde CSS |
| **2 — pas de Canari vert, #1815 OPEN, `--online` hors `ci.yml`** | **EFFET SOLDÉ, CAUSE TENUE** | Canari `success` sur `c32f01982` (2026-09-21T12:29:26Z, schedule), puis #1815 refermée par le bot à 12:40:12Z. MAIS `git show 36a12877a:.github/workflows/ci.yml`, filtré par `grep -n plans`, sort en exit 1. Le volet `--online` ne vit donc toujours qu'à `canari.yml:115`, dont le cron est hebdomadaire (`canari.yml:5`, lundi 06:00). La fenêtre modifie pourtant `ci.yml` : elle y ajoute `raw:check-ancres` et `raw:check-source-puces`, pas ce volet |
| **3 — stock né à 26 sous `CLIQUET: +22`** | **INSTRUIT, TICKETÉ, NON CORRIGÉ** | #1840 OPEN, cause prouvée : une entrée fichier + sélecteur à ESPACE n'est pas lue. Rejouée à la tête (`g3.mjs`) : le littéral compte **24** entrées, `entreesDeStock` en rend **21**, et 17 des entrées ont un jeton à espace. `stocksNominatifs.mjs` n'est pas touché dans la fenêtre |
| **4 — annotations 15/22, Session 11/22** | **SOLDÉ / AGGRAVÉ** | `JUGE:` + `REFUTATION:` passent à 32/32. `Claude-Session` tombe à 14/32 |
| **5 — `combatManeuvers.ts:440`** | **TENU — 3ᵉ palier** | La ligne 440 `const dur = Math.max(1, bonus(effectiveChar(attacker, 'endurance')));` est identique à la base et à la tête ; idem pour `disease.ts:193`. #1817 OPEN, et aucun commit de la fenêtre ne le cite |
| **6 — trois réserves d'outillage** | **TENU, les trois** | `scripts/ops/fermetures-non-citees.mjs:45` (`TITRE_CANARI`) et `:47` (`startsWith`) : #1815 est passée par cette porte, comme prédit. `git grep -n run_attempt 36a12877a -- scripts/ops/` sort en exit 1. `scripts/guards/lib/revuePalier.mjs:118` ne regarde que `--diff-filter=A`. #1793 et #1777 OPEN |
| **7 — budget +348 o, un cran déclaré deux fois** | **NON RÉCIDIVÉ** | `PLAFOND_OCTETS` 26807 → 26958 (+151) en 2 crans, chacun déclaré une seule fois et au compte exact. Aucun rouge de budget dans les 58 corps. Le cran de +150 cite le consentement utilisateur du 2026-09-20 : « S'il faut modifier claude, faisons ca » |

## Griefs de ce palier

**Grief A — un cardinal VIVANT sur tout le corpus de données est recalé à la main, sous un journal de recalages, et coûte 5 rouges de CI.**

```
git show 36a12877a:src/data/structures-contrat.test.ts   (lignes 1691, 1697-1699)
    // #1612 (2026-09-06) : 2272 → 2279 — +7 objets à `op`, tous posés par Mendier et sa table MAISON.
    // #1678 (2026-09-20) : 2279 → 2280 — le verrou de TYPE d'À Terre (`LDB 18 l.15`) descend en DONNÉE
    // (`etats.json › lockedUntil`) : c'est un `compare`, et son `op` compte ici.
    expect(scan.totalConditionsAvecOp + scan.totalOps, «objets portant un op = …»).toBe(2280);
git log -1 --format=%B 56d7f36c9
  « La publication de `f556c04bd` a été REFUSÉE par la CI (course 35528483050, 5 tests rouges sur 23 517 …) »
  « CARDINAUX recalés à l'idiome de chaque fichier : 30 `compare` authorés (`compare-reel.test.ts`), 2280 objets à `op` … 194 Conditions sans `op` »
```

Le test compte TOUTE la donnée : chaque ajout de contenu le fait rougir.

Son commentaire est un journal daté de ses propres recalages, c'est-à-dire une pierre tombale au sens du credo.

`ad6ba48e9` avait remplacé la même classe par une PROPRIÉTÉ dans `scripts/migrations` au palier précédent. Elle vit toujours dans `src/data`, sur trois cardinaux au moins (2280, 194, 30).

Hors fenêtre, l'histoire du worktree montre la récidive à `05e545af9` : « cardinaux recalés : regles.json 85→86, total 836→837 ».

→ attendu : les trois cardinaux deviennent des propriétés, et le journal part au git. `fichier:ligne` : `src/data/structures-contrat.test.ts:1691,1697,1699` ; `compare-reel.test.ts`. **MOYEN, RÉCIDIVE DE CLASSE.**

**Grief B — l'instrument ne voit pas `Source/`.**

`scripts/guards/lib/revuePalier.mjs:168` porte `export const DOSSIERS_DE_SUBSTANCE = ['src', 'scripts']`. Son commentaire (`:164-167`) en fait la source UNIQUE de `commitsDeSubstanceDepuis` ET de `evaluatePorteDuTicket`.

`d2e9a77a9` et `86a0c76e1` ne touchent que `Source/…5e Core Rulebook/*.md` et `docs/raw/coverage.md` : les deux sont `substance:false`. Ils portent `JUGE:` + `REFUTATION:` par discipline, pas parce qu'une porte l'exige. Or `Source/` est « la vérité citable » (règle stricte 1).

→ attendu : `Source/`, et `docs/raw/` (l'Atlas, écrit à la main), entrent dans `DOSSIERS_DE_SUBSTANCE`, ou une porte exige `JUGE:` sur tout commit qui touche `Source/`. **MOYEN, trou de couverture.**

**Grief C — la décrue CSS se lit au message, jamais à la garde, et la feuille exempte grossit encore.**

Mesures au § Stocks :
- `components.css` : +29 déclarations ;
- `hud.css` : stock 13 → 0, déclarations 84 → 84 ;
- `combatStartSplash`, organisme neuf, sort 26 déclarations du stock.

Les messages disent vrai (« La moitié de la décrue du stock est donc un déménagement », `cf4c2ee7b`). Mais cette vérité tient au scrupule de l'orchestrateur, pas à un dépôt décroissant.

→ attendu, inchangé depuis le grief 1 hérité : trois nombres dans l'instrument (disparu / migré / reclassé), et un compte des feuilles partagées au cliquet. `fichier:ligne` : `scripts/guards/lib/cssCouches.mjs:18-23` ; `scripts/guards/lib/cssCouchesStock.mjs` ; `src/ui/styles/components.css`. **MOYEN, RÉCIDIVE (2ᵉ palier).**

**Grief D — `Claude-Session` : 14/32, 2ᵉ palier de baisse.** Les 18 absents sont TOUS des lots `source` / `atlas` / `raw` :

`36a12877a`, `cf5b12bde`, `85a8237bc`, `c32f01982`, `9502ddc2f`, `1504175ba`, `7e8def34c`, `6d98c59b0`, `3a43b3709`, `4be659d8e`, `7702ccb01`, `760eb94a3`, `07c3834bd`, `95ece188e`, `1597da5cb`, `53e50ddac`, `44879e009`, `244a9ed05`.

Ce n'est donc pas un oubli au hasard : c'est un canal de session entier qui ne pose pas le trailer.

→ attendu : le trailer est posé par `ops:publier`. **MINEUR, RÉGRESSION.**

**Grief E — 466 sites de dette naissent au stock, et la ligne `CLIQUET:` ne nomme aucun ticket.**

- `3a43b3709` : `CLIQUET: scripts/raw/source-puces-stock.json +466 — stock NEUF … dette des livres 4e mesurée à sa naissance, zéro site sur le Core 5e`.
- `4be659d8e` : `source-tables-stock.json +36`, même forme.

Le routage n'existe qu'en commentaire d'épique :
- #1820, 2026-09-21T01:32 : « 466 sites sur quinze livres » ;
- #1816, 2026-09-21T05:37 : « sentier des sources, #1388 / #1739 ».

Contre-modèle dans la même fenêtre : `9502ddc2f` écrit `+20 … décroît d un par livre mis au grain (#1739 lot 6)`. **MINEUR.**

**Grief F — `44879e009` touche 4 fichiers `src/ui` sans ligne `JUGE-VISION:`.** La recette est bien décrite au corps et 4 captures `public/qc/soldes/1826-*.png` sont à l'index. La porte d'annotation ne l'a pas exigée. **MINEUR, FORME.**

**Grief G — #1880 ouverte sans dépendances.** Sur les 52 issues #1837 à #1888, 51 portent « Bloqué par » / « Débloque » / « Prérequis partagé » au corps (sonde `gh api` issue par issue). #1880 n'en porte aucun. **MINEUR.**

## Ce qui résiste

- **La CI de publication.** 31 courses, 31 `success`, `run_attempt = 1` partout. Le seul rouge cité (course 35528483050) est un rouge de BRANCHE : « rien n'est entré dans `main` » (`56d7f36c9`).
- **Les annotations.** 32 commits sur 32 portent `JUGE:` + `REFUTATION:`. Les 21 lignes `CLIQUET:` sont toutes au compte juste.
- **La boucle revue → geste.**
  - #1834 est fermée par le commit même qui archive la revue précédente.
  - Le grief 3 hérité est instruit jusqu'à sa cause et ticketé : #1840 cite la revue en tête de corps.
  - Le grief 2 est soldé sur l'effet, et la régression d'annotation du grief 4 est soldée.
- **La décrue CSS est majoritairement réelle** : −79 déclarations pour −115 sites, −8 `!important`, et une ventilation écrite lot par lot.
- **Architecture à rebours, sondée sur `PendingDefense.suite` (`1bbf9c5c8`).**
  - Le conflit supposé : #1854 (OPEN) veut tuer le slot singleton `pendingDefense` au profit de la donnée portée par l'étape.
  - Pourquoi `suite` survit à cette cible : c'est un champ de la CHARGE (`SuiteDeDefense`, union typée), pas du slot. Il migrerait tel quel sur l'étape. **NON RÉFUTÉ.**
  - La dualité avec `pendingCascade.maneuverResume` est DITE au corps (« = lot 2 ») et reste portée par #1858, OPEN.
- **Balayage poison des lignes AJOUTÉES de la fenêtre.**
  - Commande : `git diff -U0 95248a08c 36a12877a -- src scripts server`, JSON exclu, lignes `+` seulement.
  - Motif : pour l'instant, temporaire, TODO, FIXME, déplacé vers, anciennement, legacy, deprecated, rétro-compat, fallback, à terme, plus tard.
  - Résultat : 6 touches, 0 poison. Ce sont un dossier « temporaire » jetable, un « plus tard » qui glose un Round, et un « déplacé » géométrique.

## Non couvert par ce jugement

(a) **Aucune suite, aucun typecheck, aucune gate n'a été rejouée** : tout est pris sur la foi des messages et des 31 courses CI vertes.

(b) **Le visuel n'est que partiellement couvert.** Une seule capture a été ouverte : `public/qc/soldes/1806-hud-apres-combat-360.png`, extraite par `git show 36a12877a:…`.
- Observation NON instruite, à 360×740 : la console occupe environ 55 % de la hauteur et la bande de monde visible fait environ 70 px.
- Libellés tronqués : « Armes… », « Mouve… », « Défens… », « Immu… », « Ignore… », « Relinda Feuill… ». « Fin du tour » reste visible.
- Ce qui trancherait : la même vue AVANT `cf4c2ee7b` (aucune capture 360 « avant » au dépôt), et `scripts/recette/hauteur-reelle.mjs` rejoué.
- Les `JUGE-VISION:` de `07d9f850d`, `8fe2a96eb` et `b7c288c68` annoncent des captures ABSENTES du dépôt : les 56 ajouts de `public/qc` ne couvrent ni #1867, ni #1848, ni #1847. Elles n'ont donc pas été vues.

(c) **Les mutations annoncées ne sont pas rejouées** : neuf sur `44879e009`, « 56 008 écarts » sur `36a12877a`.

(d) **Le RAW n'est pas rouvert au `Source/`** : `LDB 18 l.15`, `LDB 16 l.76`, `CRB 024`, `CRB 071`.

(e) **`24cd5262c` écrit** que sa recette joueur « est due AVANT le commit de solde de ce ticket et n'est PAS faite ». #1852 étant OPEN, ce n'est pas un défaut de la fenêtre ; c'est à reprendre au solde de #1852.

(f) **Mon comptage des déclarations CSS diffère** de celui de `css-conservation.mjs` : 8072 chez moi, contre 8121 ou 8145 dans les messages. Seuls les deltas sont comparés.

## Sondes jouées (code exact et sortie)

`g3.mjs` : la porte des stocks aveugle aux jetons à espace. Grief 3 hérité, à promouvoir en test de #1840 :

```js
import { execFileSync } from 'node:child_process'
const cwd='C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b'
const { entreesDeStock } = await import('file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b/scripts/guards/lib/stocksNominatifs.mjs')
const chemin='src/ui/css-modules-guard.test.ts'
const src=execFileSync('git',['show','36a12877a:'+chemin],{cwd,encoding:'utf8'})
const bloc=src.slice(src.indexOf('REPEINTS_STOCK')); const lit=(bloc.slice(0,bloc.indexOf('\n]')).match(/^\s*['"]src\//mg)||[]).length
const e=entreesDeStock(src,chemin)
console.log('tete 36a12877a — litteral REPEINTS_STOCK:',lit,' entreesDeStock (tout le fichier):',e.length)
const avecEsp=(bloc.slice(0,bloc.indexOf('\n]')).match(/^\s*['"][^'"]*\|[^'"]* [^'"]*['"]/mg)||[]).length
console.log('entrees du litteral dont le jeton porte un espace:',avecEsp)
```
```
tete 36a12877a — litteral REPEINTS_STOCK: 24  entreesDeStock (tout le fichier): 21
entrees du litteral dont le jeton porte un espace: 17
```

`cliq.mjs` : compte déclaré contre compte mesuré de chaque ligne `CLIQUET:`, sortie au § Stocks :

```js
import { execFileSync } from 'node:child_process'
const cwd='C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b'
const { entreesDeStock } = await import('file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b/scripts/guards/lib/stocksNominatifs.mjs')
const show=(r,p)=>{try{return execFileSync('git',['show',`${r}:${p}`],{cwd,encoding:'utf8',maxBuffer:1<<28,stdio:['ignore','pipe','ignore']})}catch{return null}}
const shas=execFileSync('git',['log','--format=%h','95248a08c..36a12877a'],{cwd,encoding:'utf8'}).trim().split('\n')
for(const h of shas){ const b=execFileSync('git',['log','-1','--format=%B',h],{cwd,encoding:'utf8'})
  for(const m of b.matchAll(/^CLIQUET: (\S+) \+(\d+)/mg)){ const p=m[1], decl=+m[2]
    const a=show(h+'^',p), c=show(h,p); let na, nc, how='entreesDeStock'
    try{ na=a==null?0:entreesDeStock(a,p).length; nc=c==null?0:entreesDeStock(c,p).length }catch(e){how='err '+e.message.slice(0,40)}
    if(p.endsWith('budget-contexte.mjs')){ const f=t=>+(/PLAFOND_OCTETS = (\d+)/.exec(t??'')||[0,0])[1]; na=f(a); nc=f(c); how='PLAFOND' }
    console.log(h,p.padEnd(62),'declare +'+decl,' mesure',na,'->',nc,'=',(nc-na)>=0?'+'+(nc-na):(nc-na), (nc-na)===decl?'OK':'ECART', how)
  }}
```

`stk.mjs` : bilan net des stocks au lecteur de la porte, sortie au § Stocks :

```js
import { execFileSync } from 'node:child_process'
const cwd='C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b'
const { entreesDeStock } = await import('file:///C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b/scripts/guards/lib/stocksNominatifs.mjs')
const show=(r,p)=>{try{return execFileSync('git',['show',`${r}:${p}`],{cwd,encoding:'utf8',maxBuffer:1<<28,stdio:['ignore','pipe','ignore']})}catch{return null}}
const files=execFileSync('git',['diff','--name-only','95248a08c','36a12877a'],{cwd,encoding:'utf8'}).trim().split('\n')
let up=0,down=0
for(const p of files){ if(!/\.(mjs|ts|mts|json)$/.test(p)) continue; const a=show('95248a08c',p), c=show('36a12877a',p)
  let na=0,nc=0; try{ na=a?entreesDeStock(a,p).length:0; nc=c?entreesDeStock(c,p).length:0 }catch{continue}
  if(na!==nc){ console.log(p.padEnd(70), na,'->',nc, (nc-na>0?'+':'')+(nc-na)); if(nc>na) up+=nc-na; else down+=na-nc } }
console.log({hausse:up, baisse:down})
const man=t=>{const j=JSON.parse(t); const a=Array.isArray(j)?j:(j.entries||j.dette||j.topics||Object.values(j).find(Array.isArray)||[]); return a.length}
console.log('raw.manifest cf5b12bde^ -> cf5b12bde', man(show('cf5b12bde^','src/data/raw.manifest.json')), man(show('cf5b12bde','src/data/raw.manifest.json')), '| base->tete', man(show('95248a08c','src/data/raw.manifest.json')), man(show('36a12877a','src/data/raw.manifest.json')))
```

`css2.cjs` : déclarations et stock CSS par feuille, sortie au § Stocks :

```js
const {execFileSync}=require('child_process');const cwd='C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1343-lot-b';
const g=(...a)=>execFileSync('git',a,{cwd,encoding:'utf8',maxBuffer:1<<28});
const per={};
for(const r of ['95248a08c','36a12877a']){ per[r]={};
  for(const f of g('ls-tree','-r','--name-only',r,'--','src/ui','src/gameIso').split('\n').filter(f=>f.endsWith('.css'))){
    const t=g('show',r+':'+f).replace(/\/\*[\s\S]*?\*\//g,''); per[r][f]=(t.match(/[{;]\s*-?-?[a-zA-Z-]+\s*:[^;{}]+(?=;|\})/g)||[]).length; }
  const st=g('show',r+':scripts/guards/lib/cssCouchesStock.mjs'); per[r].__stock={};
  for(const m of st.matchAll(/fichier: '([^']+)'/g)) per[r].__stock[m[1]]=(per[r].__stock[m[1]]||0)+1;
  const man=JSON.parse(g('show',r+':src/data/primitives.manifest.json')); const arr=Array.isArray(man)?man:(man.primitives||man.entrees||Object.values(man));
  per[r].__org=arr.filter(e=>e&&e.nature==='organisme').map(e=>e.id||e.nom); per[r].__n=arr.length;
}
const A=per['95248a08c'],B=per['36a12877a'];
const keys=new Set([...Object.keys(A),...Object.keys(B)].filter(k=>!k.startsWith('__')));
for(const k of keys){const a=A[k]??'-',b=B[k]??'-'; const sa=A.__stock[k]||0, sb=B.__stock[k]||0; if(a!==b||sa!==sb) console.log(k.replace('src/ui/styles/',''),'decl',a,'->',b,' stock',sa,'->',sb);}
console.log('manifest n',A.__n,'->',B.__n,'organismes',JSON.stringify(A.__org),'->',JSON.stringify(B.__org));
```

Courses `main` de la fenêtre :

```
gh api "repos/cgauche/game/actions/runs?branch=main&per_page=100&created=>=2026-09-20"
09880b160 CI 1 completed success 2026-09-20T09:47:19Z push
cae05c51a CI 1 completed success 2026-09-20T10:02:11Z push
  … 27 lignes CI, toutes « 1 completed success » …
c32f01982 Canari 1 completed success 2026-09-21T12:29:26Z schedule
cf5b12bde CI 1 completed success 2026-09-22T19:52:30Z push
36a12877a CI 1 completed success 2026-09-22T22:09:42Z push
```

États des cibles, par `gh issue view <N> --json state` :

```
CLOSED : 1826 (2026-09-20T16:37:09Z) · 1834 (2026-09-20T10:04:36Z) · 1815 (2026-09-21T12:40:12Z)
OPEN   : 1838 · 1840 · 1817 · 1793 · 1777 · 1796 · 1854 · 1858 · 1865 · 1824 · 1873 · 1887 · 1739 · 1820 · 1825 · 1726 · 1880
```

## Verdict

verdict: PARTIEL

**Côté livraison, c'est le meilleur palier mesuré :**
- 31 courses CI sur `main`, 31 vertes, zéro relance ;
- un Canari revenu au vert, qui referme #1815 ;
- 32 commits de substance sur 32 annotés `JUGE:` + `REFUTATION:` ;
- 21 comptes `CLIQUET:` justes sur 21 ;
- deux fermetures, chacune avec son solde dans l'index de son commit, et un reste routé OPEN ;
- une décrue CSS enfin majoritairement réelle : −79 déclarations pour −115 sites, et −8 `!important` ;
- trois griefs hérités soldés, ou instruits jusqu'à leur ticket.

**Il reste PARTIEL parce que la vérité tient au scrupule de l'orchestrateur, pas aux gardes :**
1. **(A) Cardinal vivant.** `structures-contrat.test.ts:1699` (`toBe(2280)`) compte tout le corpus de données. Il est recalé à la main sous un journal de recalages, et a coûté 5 rouges de CI de branche.
2. **(B) `Source/` hors substance.** L'instrument de palier et la porte du ticket ne comptent pas `Source/` (`revuePalier.mjs:168`) : deux réécritures de la vérité citable échappent à la porte.
3. **(C) Soldé et déménagé ne se distinguent qu'en prose.** `components.css`, exempte, gagne +29 déclarations ; `hud.css` sort 13 sites à matière constante ; un organisme neuf sort 26 déclarations par simple déclaration.
4. **Grief 3 hérité.** Prouvé et ticketé, mais la porte reste aveugle à la tête : 21 entrées lues sur 24.
5. **Griefs 5 et 6 hérités.** `combatManeuvers.ts:440` et les trois réserves de l'instrument sont intacts pour le 3ᵉ palier.
6. **`Claude-Session`.** 14/32, deuxième palier de baisse.

## Routage des griefs neufs (orchestrateur, 2026-09-23)

Contre-vérifiés sur `36a12877a` avant routage (`git grep` des cardinaux `:1699`/`:1711`, des lecteurs de `DOSSIERS_DE_SUBSTANCE`) ; dédupliqués par recherche d'issues ouvertes.
- A → #1889 (cardinaux de corpus figés, journal de recalage ; prérequis partagé avec #1648).
- B → #1890 (critère de substance aveugle à `Source/` et `docs/raw/`).
- C → #1806, qui possède la famille CSS : commentaire du 2026-09-23 (disparu / migré / reclassé).
- D, E, F, G (MINEURS) : consignés ici, sans ticket propre. D rejoint le chantier outillé #1750 (trailer posé par `ops:publier`) ; E, F, G sont des défauts de forme de commits et de ticket déjà publiés.
