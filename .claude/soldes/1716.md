# Solde #1716 « Semences d'une scène neuve (ambiance, mètres par case, lumière, terrain, relief, toiture) et plage de pente en DONNÉE config éditable — plus aucun littéral de matière ni de terrain dans src/state » — 2026-09-18

VERIFIE: le DoD relu clause par clause sur l'arbre final `chantier/1716` (`9d66de88f`) — (1) « les semences d'`emptyScene` vivent dans UN dataset de famille `config` » : `src/data/semences-de-scene.json` (train 1 `d286914ff` ; conformité aux cliquets de donnée et éclairage absent = `auto`, `26628b417`), lu par `emptyScene` ET par les migrations de projet 7/8 (`poseSurChaqueScene`), exposé au Codex (« Semences de scène ») ; sonde indépendante du train 1 : `emptyScene(8,8)` byte-identique hors `id`, ordre des clés compris, attendu reconstruit depuis `git show 6a1e0ebcf:src/state/scene.ts` et non depuis le diff ; (2) « typées par les portes de la grammaire » : `idDe('terrain')`, `idDe('material','relief'|'roof')`, enum nommé d'ambiance, `PENTE_TOIT_DEG` partagé — un défaut hors registre est refusé au parse (`defs/semences-de-scene.ts`) ; (3) « `src/state/scene.ts` ne porte plus aucune matière ni aucun id de terrain en littéral, et la garde étend son périmètre sans exemption » : tenu AU-DELÀ de la clause par le fan-out #1789 (cinq trains sur cette branche, `5b9937852`, `bc2bdb749`, `608b43965`, `6334e651a`, `9d66de88f`) — les deux rôles (`absence` sur `vide`, `bordDuMonde` sur `mur`) sont des propriétés de `terrains.json` (exactement un porteur, refine de collection), le seam `src/state/terrain/index.ts` les demande au dataset, l'alphabet ASCII est un champ `ascii` par terrain, les choix de compilation vivent dans `src/data/defauts-de-compilation.json`, et le bras TERRAIN de `matieres-en-donnee.test.ts` scanne les CINQ couches (`gameIso/builders`, `authoring/*Svg`, `catalog`, `state`, `ui/editor`) à ZÉRO, sans site toléré ni nom de fichier : neutraliseurs par FORME (vocabulaire d'union déclaré, union en place à membre hors registre, UNE entrée par nom de champ `key`/`kind`/`cargoId`/`weather`, semence gelée), chacun tenu par un contrat de vie au grain du champ — la mesure par champ a fait tomber un mort du bras matières (`kind`), retiré. Mutations collées : `defautsDeCompilation.cheminDeRonde → 'pierre'` (`state/mapSpec.ts:728`) et une émission dans `floors.ts` → rouge nommant les deux sites, remise prouvée par empreinte ; site `kind` retiré du corpus → le contrat de vie rougit sur « champ `kind` ». Chaque train jugé (juge de diff, corrections portées avant commit), `npm run typecheck` 0 sur l'arbre final, périmètre vitest vert (arbre combiné : 755 fichiers node / 41 jsdom), `map:check opera` 0 défaut, `docs:check` 0.

## Restes
- Le checker ne refuse pas un littéral de terrain ou de matière posé dans un type brandé (`Id<T>` érasé par `defs-scenes/scene.ts:244,250,253`, `state/scene.ts:318`, `grammaire/document.ts:282` ; 31 casts `as Terrain`) : la garde regex reste la seule porte, un id FABRIQUÉ (`${p}eau`, `'terrain:eau'`) lui échappe par construction ; programme D0→D4 mesuré (418 diagnostics / 372 sites, `MaterialRef` polymorphe) -> #1794
- `src/state/sceneRules.ts:47` récite `'Neige épaisse'` en libellé et `gameIso/catalog/ambiance.ts:63` récite l'union météo (`WeatherFxId`) — homonymes hors classe terrain (vocabulaire météo, libellé d'affichage), la source du libellé est `sceneWeatherSchema` -> inventaire #1680 : consigné au commentaire d'inventaire du 2026-09-18, cible `WeatherFxId` dérivé de `valeursDe(sceneWeatherSchema)`
- `docs/plans/combat-naval-modele.md:59-62` mentionne `shipDeck.ts` supprimé au train C -> RAS : plan DATÉ sous `docs/plans/`, hors `docs:check`, historique par construction

## Réfutation
verdict: CONFIRMÉ — passe adverse (juge, 2026-09-18, arbre `9d66de88f`, lecture seule) sur les trois clauses du DoD, pas sur le solde : grep des sept ids (`vide|mur|sol|herbe|eau|pierre|planches`) sur les cinq couches hors tests → sept lignes, chacune expliquée par une FORME nommée de la garde (deux commentaires, deux unions en place à membre hors registre, l'union déclarée `DismissResult` lue et déclarée dans le même fichier, la semence gelée `Fige<…Defaults>` de `worldMap.ts:600`), aucune émission ; garde rejouée 11/11 puis `'mur'` planté dans `src/ui/editor/Inspector.tsx` → rouge nommant `Inspector.tsx:2219`, arbre restauré ; identité byte-à-byte d'`emptyScene` reconstruite depuis `git show 6a1e0ebcf:src/state/scene.ts` et confrontée à `semences-de-scene.json` (ordre des clés, six valeurs) ; prémisse du split #1789 tenue au sens du TITRE (la garde scanne `src/state` entier et récursif, deux sanity assertions le tiennent). Réserve nommée sans grief : `scene.ts:818` `ambientLight ?? 'auto'` est la sentinelle d'absence documentée au schéma, hors classe gardée — pas « zéro littéral » au sens naïf. Ont CÉDÉ deux points de DISPOSITION, corrigés avant fermeture : la dette de libellé météo routée « inventaire #1680 » n'y était consignée nulle part → commentaire d'inventaire posé sur #1680 ; le corps de #1794 disait « seul reste routé des soldes #1716 et #1789 » pendant que #1795 disait « seul reste routé du solde #1789 » → #1794 reformulé (reste du solde #1716, partagé avec #1789). Design D écarté : sonde `id-brand.ts` retypecheckée (`No errors`), le brand accepte un littéral, programme et non train ; D0 seul n'était pas impossible mais hors DoD et sans invariant observable — dit, pas prétendu impossible.
