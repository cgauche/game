# Solde #1540 — 2026-09-05

VERIFIE: la question du ticket (« entrée `riser` de `reliefMaterials.json` : MORTE ou face MANQUANTE du volumique ? ») est tranchée par la mesure : grep indépendant du juge de diff (`src/`, `src/scenes/**/*.json`, `scripts/`, `public/`, `docs/`, `.claude/`) = 0 consommateur de production — seuls la déclaration du dataset, `_ids.generated.ts` et `catalog/relief/relief.test.ts` (existence + couleur) la citaient ; aucun builder n'assigne `material.id = 'riser'` à une Face, le POV ne rend aucune contremarche. Verdict : MORTE. Geste posé dans ce commit (lot 1 de #1686) : purge de `riser` (et de `sol-inconnu`, mort de la même façon depuis #877) du dataset, de `_ids.generated.ts` (`npm run gen`), du banc `relief.test.ts` (réécrit en contrat POSITIF sur les quatre ids vivants et le repli visible), du stock `structuresStock.mjs` (6 → 4), des migrations historiques `l1b-10c` (table de labels) et `l1b-11a` (cardinal 146 → 144) ; identité de rendu prouvée par la sonde `FaceSurface` (65 scènes, 94 817 faces, hash identique).

## Restes
- Face manquante du volumique (habiller les contremarches d'escalier/dénivelé POV) -> RAS : aucun builder ni escalier POV n'en demande (0 site d'assignation mesuré deux fois) ; si un besoin naît, l'entrée naîtra avec son site d'assignation et sa preuve visuelle, jamais par avance.
- Label « Contremarche » posé par V-P0d (#1467) sur une entrée que personne ne consommait -> corrigé dans ce commit (scripts/migrations/2026-08-28-l1b-10c-relief-roof-labels.mjs:44 : la table de labels ne le porte plus).
- Fiche mémoire `game-pov-first-person-view.md` qui affirmait des « faces verticales de relief `riser` aux marches » -> RAS : corrigée dans la mémoire de session (fichier de l'arbre principal, commité avec les fiches de la session), le .md porteur du faux ne le porte plus.

## Recette visuelle
capture: public/qc/1686-couverture/inspector-couvertures.png
capture: public/qc/1686-couverture/validate-plan-refuse.png

## Réfutation
verdict: CONFIRMÉ — la fermeture repose sur un compte de consommateurs à zéro mesuré deux fois (codeur, puis juge de diff par grep indépendant), sur la purge effective dans le même commit (dataset, registre, banc, stock, migrations rejouées vertes sur corpus intact et renversé) et sur l'invariance du rendu ; le DoD « purge + stock » est tenu à la lettre.
