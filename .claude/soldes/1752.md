# Solde #1752 — 2026-09-17

VERIFIE: le contrat du ticket (Échap depuis un sous-écran du menu système remonte d'UN échelon au lieu de fermer tout le menu ; la pile `dismissStack` reste cohérente, aucune couche fantôme ni surface sans couche ; le ☰ reste cliquable menu ouvert sans passer au-dessus des modales) confronté au code, à deux juges de diff et à une recette navigateur PILOTÉE EN JOUEUR. Socle : `src/state/dismissStack.ts:39` — le verdict d'un appui est `'ferme' | 'reste' | 'vide'`, `'reste'` (`:75-76`) couvrant la couche bloquante ET le congédiement PARTIEL (un `onDismiss` qui rend `false` garde SA couche) ; `GameMenu.back` (`src/ui/GameMenu.tsx:46`) rend `false` depuis un sous-écran (retour à la carte racine) et ferme depuis la racine ; `useModalA11y(boxRef, onClose?: OnDismiss, …)` (`src/ui/Modal.tsx`) transmet le verdict sans branche par porteur. Focus clavier (défaut trouvé en recette, pas 9) : les deux effets d'`useModalA11y` sortaient sur `if (!box) return` avec deps `[boxRef]` — une boîte montée en permanence (`GameMenu`, `VictoryScreen`) n'a pas d'élément au premier rendu et l'objet ref ne change jamais d'identité ; correction générale `if (!actif || !box) return` + deps `[boxRef, actif]` (`Modal.tsx:77`, `:91`), les onze consommateurs relus par le juge (cinq à `actif` constant : identiques ; trois montés conditionnellement : identiques ; deux montés en permanence : gagnent la prise clavier, testés). Empilement du ☰ : `.hud-topbar` crée son contexte (`position: absolute` + `z-index: 60`, `src/ui/styles/hud.css:834`), le ☰ (`.gm-btn` 131) passe devant le voile (`.game-menu-overlay` 130) DANS ce contexte, jamais devant les modales (`.modal-overlay` 130 au rang racine) ; le token `isolation: isolate` posé au premier train, mesuré NO-OP par le second juge, est retiré. Recette navigateur (Chrome headless CDP, clavier et clics réels, 26 captures, console 0 erreur) : pas 1-8 et 10 OK — Échap depuis Options revient à la carte racine, second Échap ferme, réouverture, ☰ cliquable menu ouvert, ☰ sous une vraie modale de jet ne prend pas le clic, menu principal sans régression ; pas 9 (focus clavier) rouge puis corrigé (ci-dessus). Tests : `src/state/dismissStack.test.ts` (4 cas : `vide`, `ferme` + dépilage, congédiement PARTIEL, retrait hors-ordre), `src/ui/echap-pile-lifo.test.tsx` (7, dont bloquante `:109-115` et « la couche RESTE » `:117-129` sous porte clavier réelle), `src/ui/echap-couches-fantomes.test.tsx` (7 : Échap un échelon, pile cohérente, focus à l'ouverture et au retour de sous-écran `:149`, écran de victoire monté en permanence `:164`), `src/ui/GameMenu.test.tsx` (11, dont contexte d'empilement `:108` mesuré sur le CSS sans ses commentaires, rang du ☰ > voile `:116`), mutations rouges collées (deps `[boxRef]` → 1 rouge ; `z-index: 60` retiré → 1 rouge, après correction de la garde qui lisait la prose du commentaire ; `isolation` retiré au train 1 → rouge, garde remplacée). Prothèse de layout jsdom mutualisée en `src/ui/layoutJsdom.testkit.ts` (cinq copies migrées, `grep "getClientRects = " src` ne rend plus que le helper). `docs/recette-navigateur.md:140/:147` (manuscrite) : deux observations datées d'outillage (`shot` neutralise le focus ; `Enter` sur un bouton focalisé n'a pas activé le bouton en CDP, `Space` oui). Vitest joués : 9 fichiers, 283/283, plus les six consommateurs du hook a11y 60/60 ; `comment-poison-guard` 73/73, `ui-ratchets` 30/30. Gates locales limitées au périmètre (consigne machine de l'utilisateur) : typecheck, lint, suite complète et docs par la CI de branche.

## Restes
- empilement du ☰ reposant sur un `z-index: 60` implicite, sans invariant déclaré ni garde (juge 1) -> corrigé dans ce commit (src/ui/GameMenu.test.tsx:108)
- « refus » employé comme NOM du verdict (juge 1) -> corrigé dans ce commit (src/ui/useDismissLayer.ts:87)
- deux cas de `dismissStack.test.ts` redisant `echap-pile-lifo.test.tsx:109-129` (juge 1) -> corrigé dans ce commit (src/state/dismissStack.test.ts:8)
- focus clavier jamais dans la carte du menu, `activeElement` = `<body>` (recette pas 9) -> corrigé dans ce commit (src/ui/Modal.tsx:77)
- `isolation: isolate` posé au train 1 alors que `position` + `z-index` créaient déjà le contexte : garde de FORME (juge 2 G1) -> corrigé dans ce commit (src/ui/styles/hud.css:834)
- `VictoryScreen` (monté en permanence) change de comportement sans test (juge 2 G3) -> corrigé dans ce commit (src/ui/echap-couches-fantomes.test.tsx:164)
- prothèse `getClientRects` copiée cinq fois dans les bancs (juge 2 G5) -> corrigé dans ce commit (src/ui/layoutJsdom.testkit.ts:1)
- Codex en mode atelier : Échap depuis l'édition ferme TOUT le Codex au lieu de revenir à la fiche (hors périmètre, pile cohérente) -> #1786
- « le ☰ ne passe pas au-dessus des modales » n'est pas mesurable en jsdom (juge 2, incertain) -> RAS : mesuré en recette navigateur, clic réel sur le ☰ sous une vraie modale de jet non pris, captures au dossier de recette

## Recette visuelle
Recette navigateur en joueur (Chrome headless CDP, clavier et clics réels, console 0 erreur), 2026-09-17, 26 captures dont quatre portées ici :
- capture: public/qc/soldes/1752-menu-options.png — sous-écran Options ouvert depuis le menu système (clavier).
- capture: public/qc/soldes/1752-echap-depuis-options-retour-racine.png — après UN Échap : carte racine du menu, le menu n'est pas fermé.
- capture: public/qc/soldes/1752-retour-racine-clavier.png — même retour au clavier, le focus est dans la carte (pas 9, après correction).
- capture: public/qc/soldes/1752-hamburger-sous-modale-de-jet.png — modale de jet ouverte : le clic réel sur la zone du ☰ ne l'atteint pas, la modale reste devant.

## Réfutation
verdict: PARTIEL — juge 1 (2026-09-16, lecture seule, diff du lot) : fond TIENT (un échelon par Échap, pile cohérente, verdict `'reste'` sans branche par porteur), trois griefs (invariant d'empilement non déclaré ni gardé ; « refus » comme nom de verdict ; deux cas de socle dupliqués) → corrigés. Juge 2 (2026-09-17, lecture seule, cumul + addendum focus, mutations par transform Vite en mémoire) : L1 hook partagé TIENT (onze consommateurs relus, collatéral `VictoryScreen` mesuré : le focus entre désormais sur `.victory-continue`) ; L2 restore du focus TIENT (revient au ☰, garde `body.contains` sur déclencheur démonté) ; L3 test focus TIENT (chemin réel, mutation rouge) ; L4 garde CSS TIENT (un seul bloc par sélecteur, aucune redéfinition en media query) ; L5 `isolation: isolate` RÉFUTÉ comme porteur de l'invariant (no-op, contexte déjà créé par `position` + `z-index`) → retiré, garde d'invariant ; L6 vocabulaire TIENT ; L7 cas retirés couverts à force supérieure ; L8 doc manuscrite, claim CDP non prouvée → reformulée en observation datée ; G5 duplication → mutualisée. Fermeture PARTIELLE en l'état → tous les griefs corrigés dans ce commit, mutations rejouées ; le grief Codex hors périmètre porté à #1786.
