# Solde #1682 — 2026-09-02 (train unique #1657 B2a + #1682, épic #1463)

VERIFIE: `src/data/aa-criticals.json`, `src/data/schemas/defs/aa-criticals.ts`, `src/engine/aaCritical.ts` et `src/engine/aa-critical.test.ts` sont MORTS (absents de l'arbre, `git grep aaCritical` ne rend que les 4 clés de catégorie Codex et un commentaire d'`overrides.ts`) ; `src/data/criticals.json` = 8 documents-tables `{id, jeu, localisation, entries[]}` ; sonde du juge rangée par rangée contre `HEAD:criticals.json` + `HEAD:aa-criticals.json` = 0 écart sur 160 rangées ; un seul lecteur `resolveCritique` (`src/engine/critical.ts`), aucun `if (jeu === …)` hors sévérité/journal/clé Codex ; invariance RNG 480 cas byte-égale ; recette Codex 8 catégories = 160 entrées et atelier PASS 4/4 après correctif du `CritTestField`.

## Restes
- Défaut de fidélité `critResistValue` hors `testValue` (`critical.ts:51-53`), préexistant, influence RNG/Chance -> #1685
- Forme des 3 porteurs de Test hors nœud (`amputation.difficulty`+`loss`, `handGate`, `escalation.onNextCritWhileCondition`) -> RAS : Tests différés (fin de rencontre) ou par Action, modélisés explicitement, mesurés 7/45 par le codeur ; relèvent du patron B2b–B2e de #1657
- `field-consumers.test.ts:230-231` `CountSpec.fixed/roll` sous-décrit (1 site nommé, 4 mesurés) -> RAS : hors périmètre, inventorié au ticket #1657
- `docs/superpowers/plans/2026-06-05-socle-blessures-critiques-mort.md` (+6) plans datés exécutés en place -> RAS : hors périmètre, inventorié au ticket #1657 (politique `docs/`)
- Frictions de recette (téléchargement natif d'Enregistrer ferme la session MCP ; recherche du Codex muette sur « Perte d'équilibre ») -> RAS : outillage/UX hors périmètre, inventoriées au ticket #1657

## Réfutation
verdict: CONFIRMÉ — juge de design (B2a × #1682 : 3 réfutations du brief, RAW au `Source/`, verdict UN train validé par l'utilisateur) ; codeur ARRÊTÉ sur la contradiction 8 documents / 4 catégories (arbitrage option A, `exposition-derivee.ts:47-60`) ; juge de diff sur 12 claims : 12 CONFIRMÉS (fidélité 0 écart/160, fixture RNG prouvée antérieure au refacto, stocks à occurrences constantes 303 = 303), COMMITTABLE sous 2 réserves soldées (morsure `difficulteRequise` promue, recette jouée) ; recette : 1 bug d'atelier trouvé (racine `FlowEditor` → `{kind:'seq',steps}`) et corrigé à la source avec contrat positif jsdom + mutation.
