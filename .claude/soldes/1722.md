# Solde #1722 — 2026-09-11

VERIFIE: l'attendu du ticket (une passe unique sur les quatre tests et les quatre JSDoc encore couplés à la scène livrée, preuve de l'arbitrage à la clé) confronté à l'arbre du worktree `gates/T4c` — classement par `it` lu dans le rendu et contre-lu au diff : un `it` supprimé (doublon de loi sur cartes vivantes, couverture réelle par l'`it` voisin sur fixtures, prose d'en-tête recalée), une fixture des seuils construite qui COUVRE ce que l'ancien sujet couvrait (seuils au rez soulevé d'un pas, seuils à l'étage z1, portes et passages sans porte : 7 portails, non-vacuité par z et par genre), deux fichiers laissés en ENTRÉE à attentes dérivées et dits, quatre JSDoc de production au présent sans id/coordonnée/compte et zéro ligne de code changée ; mutation « inversion à plat » rejouée par le juge puis par le codeur (rouges nommés, y compris les seuils z1 quand les deux étages de surface sont cassés) ; preuve de l'arbitrage (`diligence-salle-table-murale-1` déplacée d'une case → verts, fichier remis, md5 identique) ; `vitest` 5 fichiers / 171 tests verts, `typecheck:fast` 0, `eslint` 0, pre-commit 0, `docs:check` 0 ; gates locales complètes jouées en série avant le push.

## Restes
- Huit occurrences de la Diligence subsistent dans `bake-compte`, `sceneMeshes` et `worldTris` -> RAS : entrées à attentes DÉRIVÉES (identité d'index, oracle recalculé, hausse bornée), licites au critère D0c jugé — et le verbatim utilisateur du 2026-08-21 (#1447) cité par `worldTris:724-725` est une citation d'arbitrage, pas un contenu asserté ; l'en-tête de `bake-compte` ne le cite plus mais s'adosse à l'arbitrage du 2026-09-07 qui l'étend à toute carte livrée (juge : aucune décision orpheline)
- Un seuil à DÉNIVELÉ (case basse vers pièce haute d'un pas) résout la case haute, une BRÈCHE sans mur tombe sur la frontière exacte de deux cases -> RAS : comportements conformes au contrat écrit (« le lift le plus haut gagne », le `.5` du JSDoc de `screenToTileAtLift`), hors du contrat de ce banc ; la fixture porte d'ailleurs un portail à dénivelé z-conscient (la Diligence n'en portait aucun : gain mesuré par le juge)
- Un seuil z1 est tenu par DEUX étages de surface redondants (`caseMarchable` au lift métrique, `caseAuSol` à l'index de couche) : une seule inversion cassée ne le rougit pas -> RAS : redondance de production dite au JSDoc de `pickResolve.ts`, le banc rougit quand les deux cassent ; ce n'est pas un trou du banc mais une propriété du socle

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 20 min) : l'`it` supprimé était un doublon de loi (l'`it` restant préexistait), la fixture mesure le moteur (deux mutations rouges nommées), le commentaire « à un doigt de plus le corps se détache » vérifié (h = 1,01 → 0 seuil extérieur), `pickResolve.ts` sans ligne de code changée, les huit occurrences restantes sont des entrées dérivées, 0 mot d'histoire ; deux réfutations (prose de couverture morte dans `bake-compte:26-27`, couverture z1/`passage` perdue sans le dire) corrigées — la seconde par COUVERTURE, re-mesurée 7 portails.
