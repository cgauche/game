# Solde #1686 — 2026-09-09 (fermeture de l'épic après ses quatre descendants #1691, #1692, #1694, #1693)

VERIFIE: le DoD du ticket confronté à l'arbre — (1) UN SEUL dataset de matière : `src/data/materials.json` (16 entrées : 8 `prop`, 4 `roof` dont `plan`, 4 `relief`), `git ls-files src/data | grep -i material` ne rend que `materials.json`, ses types, son def et ses deux gardes (identité dérivée intra/croisée, fraîcheur par `setDataset`) — `propMaterials`/`roofMaterials`/`reliefMaterials` sont morts (lots 1-3a, 2026-09-05) ; `structureAppearance.json` est un COMPOSITE qui référence des matières, pas une forme parallèle (`materials-identite.test.ts:16`) ; (2) UN SEUL schéma : `src/data/schemas/defs/materials.ts`, forme PLATE + refine ⟺ par domaine (`CHARGE_PAR_DOMAINE`), `domain` = valeurs de `MaterialRef.domain` sous `satisfies`, Codex « Matières » par domaine avec formulaire = charge du domaine seule ; (3) ZÉRO catalogue TS de matière : `TERRAIN_DEFS` → `src/data/terrains.json` (#1690, 25 entrées, Codex « Terrains ») et la matière de relief en donnée (`terrains.json › matiere` + `scene.reliefDefaults` exigé, #1691) ; (4) `docs/structures-donnees.md`/`docs/donnees.md:168` sans forme de matière parallèle (mesuré) ; (5) gates vertes à chaque train, CI verte à chaque push ; (6) planche QC : l'identité de rendu a été prouvée à l'octet par sonde `FaceSurface` sur 65 scènes / 94 817 faces (hash identique, codeur puis juge, lots 2 et 3a) et par la sonde d'identité des ids de relief (#1691), l'homonyme `ardoise` tranché par l'utilisateur en ids composés (`prop-ardoise`/`toit-ardoise`, arbitrage 2026-09-05) — aucune planche avant/après à pixels divergents n'a eu à être arbitrée. Les quatre descendants nés du chantier sont fermés par publication : #1691 (2026-09-07), #1692 (2026-09-08), #1694 (2026-09-09), #1693 (2026-09-09).

## Restes
- Toiture et catalogue TS des bâtiments hors du dataset : 7 defs `roofMaterial: '<id>'` en dur (`catalog/buildings/defs/*.ts:7`, hors du périmètre de la garde de #1691 qui ne scanne que `builders/**`), `roofDefaults` optionnel à repli en CODE (`DEFAULT_ROOF_DEFAULTS`, 5 sites, 0 scène du corpus ne le pose — à trancher : parité avec `reliefDefaults` ou défaut en donnée de projet), entrée `plafond` du domaine relief sans émetteur (affirmée par cardinal par deux migrations datées) -> #1715
- Ids de matière de la campagne (scènes livrées) : portés par la donnée de scène et validés par le schéma (`idDe('material', …)`) -> RAS : c'est le chemin canonique
- `catalog/roofs/types.ts` sans `type` alors que le JSON le porte, `roofMaterials` discriminé par présence de champ (`Inspector.tsx:42`) — poison nommé au ticket -> RAS : morts avec `roofMaterials.json` (lot 2) et la forme PLATE + refine par domaine

## Réfutation
verdict: CONFIRMÉ — épic tenu OUVERT par régime jusqu'à la fermeture de ses quatre descendants (doctrine « un épic ne se ferme jamais sur des tickets ouverts », tenue) ; DoD relu ligne à ligne contre l'arbre (lecteur en lecture seule, 2026-09-09 : dataset, schéma, catalogues TS, docs vivants) ; les trois restes du solde #1691 routés vers cet épic ont été re-mesurés (7 sites, 5 replis, 0 lecteur de `plafond`) et versés en UN ticket avec leur coût — le plus petit (7 defs) n'a pas été corrigé ici parce que la forme cible (le catalogue des bâtiments par la donnée) est un chantier de la même famille que #1690, pas un geste de fermeture.
