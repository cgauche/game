# Solde #900 — le déréférencement devient général, les sondes deviennent des tests

date: 2026-07-27

VERIFIE: j'ai relu le diff des 7 fichiers moi-même (les 4 définitions de processus, `src/data/tables.test.ts`, les 2 fichiers de stock créés) ; j'ai relancé les 10 `grep -c` de contrôle du ticket — tous à `1`, aucun `0` (geste raté) ni `2` (insertion dupliquée) ; j'ai relancé le typecheck en sortie COMPLÈTE (0 `error TS`) et la suite COMPLÈTE (1087 fichiers, 14305 tests verts, 2 de plus qu'avant le lot = la garde neuve) ; et j'ai exigé puis lu la preuve par MUTATION de la garde de consommation — rouge avec `vdm-symboles-augure` retiré du stock et le message « table(s) NEUVE(s) sans consommateur », vert après remise à l'identique à la main.

## Restes

- L'automatisation du `git blame` des justifications de cliquet, sixième recommandation de l'audit, a été RETIRÉE du ticket par son auteur : détecter « une permission citée » dans du texte libre demanderait une heuristique qui produirait les premiers faux positifs d'un dispositif qui n'en a produit aucun en 7 blocages -> RAS : la règle survit en red flag (geste 2.2) et dans le périmètre de la passe de fermeture (geste 2.1), au poste qui a le discernement
- Les 2 tables orphelines qui peuplent le stock initial (`vdm-siphonnage-de-sort`, `vdm-symboles-augure`) ne sont pas câblées par ce lot — le stock les gèle, il ne les résout pas -> #901
- La règle du Lot 1 exige la sonde pour un BLOCAGE externe déclaré, mais mes trois erreurs du jour n'étaient pas des blocages : c'étaient des affirmations d'absence ordinaires, écrites dans un commentaire de ticket et dans deux briefs, chacune adossée à une sonde plus étroite que sa conclusion -> ce ticket RESTE OUVERT après ce commit (la généralisation « toute affirmation d'absence porte sa sonde ET son périmètre énoncé » n'est pas encore écrite)
- Le défaut de DOCUMENTATION que ces mêmes erreurs ont révélé — un mécanisme en service depuis trois livres et 13 cases, exposé par aucune référence vivante, donc cherché au mot-clef et déclaré absent -> #902

## Réfutation

verdict: PARTIEL — les 5 lots livrés tiennent et sont prouvés (greps à 1, mutation rouge/vert, suite complète verte), mais la passe n'a pas été menée par un juge adversarial sur ce diff-ci, et surtout le lot est INCOMPLET au regard de ce qu'il visait : il durcit le pointeur et le blocage déclaré, or les trois erreurs de la session portaient sur des affirmations d'absence qui n'étaient ni l'un ni l'autre. Le ticket reste ouvert sur ce reste, qui est le coeur du défaut plutôt que sa périphérie.
