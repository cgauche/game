# Solde #1687 — 2026-09-11

VERIFIE: l'attendu du ticket (« Décor STATIQUE ou UTILISABLE — identité, nom au survol, surbrillance et ACTIONS du décor ») confronté à l'arbre, lot par lot, chaque lot jugé sur le diff et recetté au navigateur avant push — (1) IDENTITÉ : `state/usable.ts` est LA source de l'offre (`actionsDe`/`estUtilisable`, table `CAPACITES` keyée par `CapaciteId`, drapeaux en paramètre d'un module pur) ; `SceneEntity.usable = { assise?, actions? }`, `interact` mort et migré 10 → 11 par script daté + `PROJECT_MIGRATIONS[10]` (une fonction partagée), 32 fouilles et 5 assises sur 4 projets, aucune chaîne FR figée (`label` absent partout, vérifié par le juge) ; (2) ACTIONS : exécuteur UNIQUE `store.jouerAction` (table `JOUER_CAPACITE` TOTALE au même `CapaciteId` — mutation : `commercer` retiré → TS2741), onze lecteurs d'`interact` migrés dans le même commit (grep = 0 hors migration), le ramassage en combat n'écrase plus le document de scène (`grep "\.flow = |flow\.steps = "` = 0), pastille « Que faire ? » hors combat pour tout utilisable À PORTÉE (régime du combat, un hôte `PastilleEntite`/`PanneauParametre`, `OffreRendue` à deux producteurs), portée « adjacent » UNIQUE avec l'étage (`aPorteeDe`, cinq sites + deux en combat, garde par origine dans `jouerAction`), éditeur : liste d'actions par décor, id renommé par `EntryRename` sans doublon ; (3) NOM AU SURVOL : `CodexRef tooltipOnly` autour du DOM de la pastille (aucune prop d'ancre), raison de refus au survol (assise pleine) ; (4) SURBRILLANCE : socle clavier « une touche-modificateur est son propre modificateur » + relâchement par la prise (fuite existante des cinq gestes maintenus corrigée), binding `decor.reveler` au registre unique, état `reveler` exclu de la save par nom, `interactionHalos(tokenEls, propEls, …, { survol, reveler })` → une liste, `etatDe` seul point de décision, rien de permanent, muet sous le brouillard, décor ET PNJ, plaques de nom = peintre de monde (`PlaquesDeNom`, `CodexTitre` extrait de `CodexRef`) ; (5) RACCOURCIS COHÉRENTS : porte, escalade, chute, structure de siège par l'étage `arete` unique de `resoudrePixel` (`DoorOverlays`/`ClimbOverlays`/`FallOverlays`/`SiegeHitAreas` supprimés), `frapper` par la porte partagée `combatantClickActs`, le relief de la couche 0 compte (`hauteurDe`/`liftDe`, cinq gardes fausses mortes). Trois recettes en joueur sur 3-I (deux rouges qui ont RÉFUTÉ le régime « pastille = entité survolée », la troisième verte, tactile compris), une sur 3-II-b (plaques DOM = utilisables visibles, frame sans surcoût, save réelle sans `reveler`, écran des raccourcis) ; CI verte sur chaque push sondé.

## Restes
- Voie affine du picker (`spritePicker.ts:targetUnderPointer` → `elementFromPoint`) : en POV + combat elle rend toujours `null` par construction (`SurcoucheIso` non monté en POV, `tireLeRayon` vrai en combat sans `hasSpritePicker()`) — sa purge passe par `tireLeRayon` et se juge sur le design -> inventaire #1680 : déposé en commentaire à la fermeture, à instruire avec le socle du picking
- Porteurs d'offre NON-entités (combattant, monture, coque servie) : pastille de geste intacte mais ni halo ni plaque sous Alt (`estUtilisable` n'accepte qu'une `SceneEntity`) -> inventaire #1680 : déposé en commentaire, à trancher avec le régime des gestes de combat
- Instrument des structures dérivées : `choixDeclares` (`zod-introspect.mts`, `PROFONDEUR_MEMO = 12`, la borne mord : 452 clés à 12, 488 à 20) classe une forme selon la PROFONDEUR de son chemin de schéma — le cliquet L3 a bougé (394 → 391) sans que la donnée change -> inventaire #1709 : déposé en commentaire, train propre de l'outillage des docs dérivés
- `HALO_SURVOL_MAX = 8` alors que le survol est borné à 1 (sur-allocation héritée, deux pools) ; `realKeyDown` n'émet pas `modifiers: 1` sur le keydown de la touche elle-même (sans effet : la touche est son propre mod) -> RAS : dits dans le message de commit du lot 3-II-b, sans effet à l'écran ni sur le contrat
- `combatFlow.ts` mesure l'adjacence entre deux COMBATTANTS (z-aveugle) — un autre concept qu'`aPorteeDe(pos, ent)`, hors signature -> RAS : nommé dans le message du lot 3-I, périmètre du combat
- Outillage de recette : `__wfrp.screen('editor')` charge la scène-fixture et non la scène active ; un `querySelector` de pastille reste vrai sous la modale d'intro ; `realKey` prend une chaîne et `realKeyDown`/`realKeyUp` un objet ; `__wfrp.scenario` peut rendre une scène vide sans exception -> corrigé dans ce commit : docs/recette-navigateur.md:147 (les deux pièges et Alt maintenu documentés ; l'asymétrie des helpers et le silence de `scenario` restent nommés ici, sans ticket)

## Recette visuelle
capture: public/qc/1687-raccourcis/l3II-03-alt.png

## Réfutation
verdict: CONFIRMÉ — juge de design lot 1b (2026-09-10) puis juges de diff sur chaque lot 1b-2 → 1b-5 (features au rayon cassaient #1297 : retirées ; ancrage de structure = frappeur → repli mort et prise décalée : ancrage = case du mur ; `liftOf` z-aveugle mesuré 96 px → socle `hauteurDe`) ; juge de design lot 3 (invariant v2 NON TENU, 8 corrections : registre fermé au hors-combat → mutualiser le RENDU, onze lecteurs d'`interact`, `jouer` affordance morte, label figé, `usable` = deux faits, brouillard au builder, Alt inexprimable au registre, pas de demi-migration visible) ; juge de diff sur le cumul 3-I (cliquet de strate baissé sur une justification FAUSSE → re-mesuré deux fois, `if` de champ survivant, affordance sans drapeaux, contrat de type) ; trois recettes 3-I dont deux ROUGES (la pastille « entité survolée » démontait à ~19 px du trajet vers son bouton : régime réfuté, remplacé par « à portée » = le régime du combat, seul qui existe au tactile) ; juges de confirmation (portée redite à quatre sites dont deux z-AVEUGLES sur le chemin du clic ; l'éditeur écrivait deux actions homonymes ; la main était le curseur de REPOS du stage par CSS, jamais effacée) ; juge de design 3-II (point 5 RÉFUTÉ deux fois au code, plaque = peintre de monde ; Alt non exprimable → socle clavier ; découpage socle seul puis binding avec son lecteur) ; juge de diff 3-II-a (une loi, relâchement par la prise, auto-repeat inchangé) ; juge de diff 3-II-b (`docs/systemes.md` périmé → régénéré dans le commit ; PNJ réunis par `CorpsPoste`, mutation « jetons débranchés » rejouée 9/31 rouges) ; recette 3-II-b rouge sur un attendu FAUX de mon brief (la plaque du survolé est celle de l'invariant), verte sur le contrat jugé.
