# Solde #1244 — 2026-09-06

VERIFIE: les trois gestes du ticket confrontés à l'arbre du train L3b A de #1679 — (1) tri STABLE : la prémisse « ordre des SYMBOLES non déterministe » était morte depuis 7f9c6a44d (recentrage mesuré au ticket, `build-implemente.mjs:459-470` tri total des symboles) ; ce qui restait, le départage entre puces de même (livre, chapitre) porté par l'ordre de la marche du système de fichiers (`:419`/`:483` sans départage, marche `:319` non triée), est fermé par construction : `ordreDesPuces` (`scripts/raw/build-implemente.mjs:485`) est une fonction PURE d'ordre total, la marche passe par `listerArbre` (ordre total des chemins), test nominatif `scripts/raw/build-implemente.test.mjs:25` (deux puces même livre+chapitre, deux fichiers, entrée à l'endroit et à l'envers → même rendu ; mutation « départage retiré » = rouge) ; (2) bruit `fr` du scanner : `grep` des champs `Implémente` de `docs/raw/*.md` = 0 occurrence du symbole `fr` ; (3) régénération de normalisation : `npm run docs:build` dans ce commit, 5 fiches RAW (`combat`, `combat-naval`, `deplacement`, `tests`, `traumatisme`) réordonnées une fois, `docs:check` vert.

## Restes
- Ordre entre puces de même (livre, chapitre) porté par le système de fichiers -> corrigé dans ce commit : scripts/raw/build-implemente.mjs:485
- Bruit du scanner (symbole `fr` dans les listes) -> RAS : mesuré 0 occurrence dans les champs `Implémente` sur cet arbre (la liste réelle des faux symboles est vide, rien à filtrer)
- Test de déterminisme « deux passes = même octets » (verbatim recopié de #1483) -> RAS : porté par la CI (`docs:check` régénère et compare sur ubuntu ce qui a été généré sous Windows) et par la garde structurelle `scripts/guards/lib/lister.test.mjs` (aucun listing brut dans la clôture des générateurs, mur lint) ; préchargeur inversant les listings : 0 générateur sur 26 dépendant de l'ordre du fs

## Réfutation
verdict: CONFIRMÉ — juge de diff (un agent, lecture seule) sur le train A : bloquant « départage (book, ch) » FERMÉ (`build-implemente.mjs:485 ordreDesPuces`, `build-implemente.test.mjs:25`, mutation n°6 rouge rejouée par le codeur, rendu `A-mutations.txt`) ; la prémisse d'ordre des symboles reste morte (`--check` ×2 identique, juge de design 2026-09-05) ; les 5 fiches RAW changées le sont par l'ordre total de la marche et le départage, aucune par un tri retiré à tort (les 3 tris à risque re-mesurés, `build-systemes.mjs:56` et `build-registre-jets.mjs:424` conservés). Ce que le juge n'a pas joué : la CI Linux elle-même — le substitut est le préchargeur « envers » (26/26 verts).
