# Solde #1723 « labelLogic : un stock de la moitie RATCHET cesse d'etre juge en silence si le corpus est ampute (non-vacuite par gisement absente) » — 2026-09-12

VERIFIE: l'attendu du ticket (non-vacuité PAR GISEMENT, patron B3 `2639287cd` : chaque dossier de `RATCHET_DIRS` et `STRICT_DIRS` a produit au moins un fichier mesuré, chaque fichier du stock est PRÉSENT dans `measured`, un absent = rouge nommé ; mutation corpus amputé → rouge nommant le gisement muet) confronté au diff du worktree `.wt-gates-A` sur `b7d5853a1` — le trou est mesuré avant le geste (mutation : `corpusTotal` ramené à `corpus(STRICT_DIRS)` → dérive `[]` alors que deux gisements ont disparu, parce que `literalFindings` ne pose une entrée que si `n > 0 || rel in stock` : un fichier de stock hors corpus n'entre jamais dans `measured`) ; la couverture est rendue par le SOCLE générique des gardes à stock `scripts/guards/lib/stock.mjs` (`couvertureDuBalayage({ nom, stock, balayes, gisements })`, quatrième calcul de la lib — ni plafond, ni verdict, ni disque, ni mémo — deux listes NOMMÉES : gisements MUETS, entrées de stock ABSENTES du balayage), déclarée au `stock.d.mts`, mesurée sur collections FORGÉES dans `src/stock-primitive.test.ts` (peuplé/muet, présent/absent, piège du préfixe `src/uix` ≠ `src/ui`, appariement exact), consommée EN TÊTE des `it` de cliquet de `src/state/label-logic-guard.test.ts` par `attendCouverture` (forme B3 : `toEqual([])`, message = la liste, aucun cardinal) sur les CINQ volets du fichier — `LABEL_LITERAL_STOCK` sur `ALL_DIRS`, `LABEL_CALL_LITERAL_STOCK`, `LABEL_KEYED_INDEX_STOCK`, `LABEL_RESOLVER_CALL_STOCK` et le volet #142 TOLÉRANCE ZÉRO sur `STRICT_DIRS` seuls (les gisements sont ceux que CE cliquet balaie, dit au site), sur la MÊME collection que la boucle itère ; le commentaire de `labelLiteralStockDrift` qui justifiait le trou par un « scan partiel du hook pre-commit » était FAUX (la fonction n'a aucun appelant hors ce test, contre-grep du juge ; `pre-commit.mjs:151-156` refait sa propre comparaison sur le STAGÉ) — réécrit ; mutations rejouées et remises à l'octet (hash) : corpus amputé de `src/gameIso` + `src/ui` → « src/gameIso : gisement MUET — aucun fichier balayé, le cliquet LABEL_LITERAL_STOCK ne juge plus ce dossier (il rendrait vert par vacuité) » + `src/ui` ; `CarnetScreen.tsx` exclu → « entrée de LABEL_LITERAL_STOCK ABSENTE du balayage — son compte n'est plus mesuré » ; `combatFlow.ts` exclu → volet #1694 rouge nommé ; volets #909 et résolveurs balayés sur `src/engine` seul → « src/state : gisement MUET » ; `vitest run` des deux fichiers verts, eslint 0, `typecheck:fast` 0, rejoués par l'orchestratrice ; juge de diff : TIENT SOUS CORRECTIONS, deux bloquants et quatre souhaitables portés.

## Restes
- Le hook `pre-commit.mjs:151-156` compare le stagé seul (un commit ne touche qu'une partie du corpus) -> RAS : hors couverture par construction, dit au JSDoc du socle ; la porte de vérité est la suite complète, qui porte désormais la non-vacuité
- Les autres gardes à stock du dépôt ont-elles le même trou ? -> RAS : CLASSÉES sur pièces par le juge — le trou n'existe que là où la collection observée est bâtie par `if (n > 0 || rel in STOCK)` (`git grep` : 4 occurrences, toutes dans `label-logic-guard.test.ts`, toutes couvertes ici) ; `manualDocsStock` (`manual-docs-ratchet.test.ts:58-73`), `grammaireStock` (`grammaire-guard.test.ts:249-272`), `slotsStock` (`slots-contrat.test.ts:283-296`), `rigViewStock` (`rig-part-views.test.ts:59-80`) assertent `perimees` (un corpus amputé rend les entrées périmées → rouge), `structuresStock` (`structures-contrat.test.ts:412`) est une égalité dans les deux sens ; réserve dite : la non-vacuité de `grammaireStock` porte sur le total, pas par gisement
- La couverture prouve qu'un fichier a été PRÉSENTÉ, jamais lu utilement (gisement d'un seul fichier non représentatif ; détecteur neutralisé + stock mis à jour dans le même commit = vert) -> RAS : angle mort DIT au JSDoc du socle ; le second cas est le « détecteur modifié dans le même commit » du credo, qui se juge au message de commit

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 2026-09-12) : claims (a)→(e) tous re-mesurés vrais (sonde par copie hors arbre : corpus sans `src/ui` → gisement muet + deux entrées absentes nommées ; volets `STRICT_DIRS` verts légitimement ; `src/engine/activities.ts` hors corpus → entrée absente nommée ; piège du préfixe mesuré) ; contre-grep du commentaire faux ; DEUX BLOQUANTS — le 4ᵉ cliquet `LABEL_RESOLVER_CALL_STOCK` non couvert (corpus vide → vert mesuré) et le socle neuf sans test forgé avec deux en-têtes « trois calculs » devenus faux — portés par le codeur, avec les quatre souhaitables (volet #142 STRICT couvert, même collection itérée, type `Record<string, unknown>`, JSDoc graphie des gisements + angle mort) ; les autres gardes à stock classées hors classe une à une. Non joué par le juge : suite complète, gates, typecheck complet — les gates se jouent sur la tête du lot avant le push.
