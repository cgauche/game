# Solde #1711 « scripts/raw/*-baseline.json : un cliquet de COMPTE se relève à la main en CI verte — porteur de stock ou stock nominatif ? » — 2026-09-13 (trains jugés le 2026-09-12, commit du 2026-09-13)

VERIFIE: la question du ticket (« (a) faire entrer la famille `*-baseline.json` dans les porteurs en nommant la limite ; ou (b) convertir ces baselines de compte en stocks NOMINATIFS pour que tout relèvement soit une entrée à déclarer ») TRANCHÉE par famille sur mesure, design jugé (juge de design lecture seule, TIENT SOUS CONDITIONS, conditions portées, design v2 sur le ticket) : la réfutation décisive du juge — convertir en nominal ne ferme le trou QUE si l'entrée nomme un fichier que `croissanceDesStocks` reconnaît ; relever `2 → 3` sous (a) rend `[]` (net 0), sous (b) avec `occurrence` dans la clé rend `net +1` avec exemple — et quatre baselines étaient vides ou absentes (le trou y était la création du fichier). Quatre trains, chacun jugé sur le diff puis corrigé : T1 `6ae10e3bb` (filet (a) : `PORTEURS` gagne `scripts/raw/[^/]+-baseline\.json`, `scripts/guards/[^/]+\.json`, `scripts/guards/lib/[^/]+\.json`, `knip-exports-baseline\.json` ; le test de périmètre CONFRONTE `PORTEURS` à une DÉRIVATION ; la limite de (a) dite et testée : clé neuve → `+1`, nombre relevé → net 0, entrée sans fichier → invisible) ; T2 `6ae10e3bb` ((b) `empty-line-code-refs` 22 sites, `graphy` 52 sites, `dead-code-refs` absent = tolérance zéro : stocks GÉNÉRÉS depuis les détecteurs, égalité des comptes prouvée fichier par fichier, clé `famille :: fichier :: ref :: occurrence`, gardes sur `ecartsDeStock` dans les deux sens) ; T3 `5b4fe9b6c` (module partagé `scripts/raw/stockNominatif.mjs`, `reanchor.mjs` capture la FICHE, `reanchor-low` 21 sites, `dead-refs` tolérance zéro, JSDoc de la clé recalé par le juge) ; T4 = ce commit (socle `CHEMIN_SOURCE` avec apostrophe — la première version aveuglait 36 des 76 entrées, RÉFUTÉE par le juge de diff, corrigée à 76/76 —, `folio-gaps` 76 sites, `entity-in-chapter` et `raw-blind-refs` tolérance zéro par absence, fin des quatre copies d'`assertAgainstBaseline`, exemple de la porte = ligne nommante des deux côtés, fixture décalée du T2 corrigée, trois tests neufs dont l'invariant transverse « la porte voit CHAQUE entrée déclarée » par glob) ; vérifié par l'orchestratrice sur l'arbre : sondes du juge rejouées (76/76, 22/22, 52/52, 21/21), `node --test` 1028/1028, eslint 0, neuf gates `raw:*`/`test:raw`/`test:hooks` exit 0 ; les gates locales jouées sur la tête du lot avant le push.

## Restes
- `empty-folios-baseline.json` : déjà nominal, hors classe (`assertEmptyFoliosAgainstStock` refuse toute entrée inconnue ou sans mesure), mais son `seuil` (200) est un NOMBRE en donnée qui reclasse `perdues↔benignes` sans porte, ses 51 entrées (nom nu à espaces) restent hors de vue de la porte de plage, et `reconciliation-stock.json` écrit plusieurs sites par ligne (13 trous vus pour 25 sites) ; ticket ouvert « Cliquets à NOMBRE hors porte : le seuil d'empty-folios-baseline.json reclasse perdues/benignes sans déclaration (51 entrées VUES = 0), et 8 plafonds numériques de tests de même classe — reste de #1711 », rattaché à la vague outillage, `## Invariant` verbatim, inventaire mesuré par le juge -> #1727
- L'échange EN PLACE à total constant (réécrire une entrée pour couvrir un site neuf pendant qu'un autre est soldé, même commit) rend `[]` à la porte de plage -> RAS : la dette ne peut pas croître ainsi et la garde le voit toujours (la clé change des deux côtés), dit au JSDoc du module partagé — c'est la suite qui tient ce cas
- Ré-rangement d'ordinal (deux sites homonymes dans un fichier, le premier corrigé → une périmée + une neuve) et ré-ordinalisation par tri -> RAS : dits au JSDoc avec leur portée mesurée (max d'occurrence 2 sur `empty-line`, 8 sur `graphy`, 1 sur `reanchor-low` et `folio-gaps`) ; le cliquet reste juste, sa phrase est plus bavarde
- Une ré-extraction Marker renouvelle en BLOC les stocks à réf citée (N périmées + N neuves pour zéro dette) -> RAS : c'est l'événement pour lequel `reanchor.mjs` existe, dit au JSDoc de la clé ; il se déclare comme tel au message du commit qui ré-extrait ; `folio-gaps-stock.json` se régénère par `sitesEnEntrees(sitesDeSauts(scanAllBooks()))`, la fonction que son `quoi` et son test d'égalité ordonnée nomment
- `raw:check-entity-in-chapter` n'est pas un step de `ci.yml` -> RAS : sa garde est jouée par son test dans `test:raw` (mesuré par le juge de design), la conversion n'y change aucun verdict de CI

## Réfutation
verdict: CONFIRMÉ — réfutation de fermeture à UN juge, lecture seule, clause par clause, deux passages le 2026-09-12. Premier passage (diff T4 tel que livré par la session précédente) : RÉFUTÉ sur deux clauses — `CHEMIN_SOURCE` excluait l'apostrophe ASCII et 36 des 76 entrées de `folio-gaps-stock.json` (ADE I, ADE II, EDOC, ACE, VDM 14) restaient invisibles aux deux portes (sonde `p4-apostrophe.mjs` : `croissanceDesStocks` → `[]`, 40/76 vues), le vice même du ticket sur la famille que T4 convertit ; et le reste `seuil: 200` d'`empty-folios-baseline.json` n'avait pas de ticket destinataire (le brouillon le routait vers #1726, ticket de dépendances npm, sans rapport de domaine). Les deux clauses LEVÉES : codeur T4b (apostrophe admise par une seconde branche de `JETON`, 76/76, trois tests neufs, mutation rouge nommée « 40 vue(s) / 76 déclarée(s) ») et ticket #1727 ouvert avec `## Invariant` verbatim. Second passage sur le cumul T4+T4b : TIENT — B1 levé et la classe attrapée par un test à corpus GLOB ; reste unique et routé ; porte de plage sans régression HEAD/WIP sur huit gestes forgés ; zéro faux porteur sur 1 859 fichiers par une méthode indépendante ; titre et DoD relus mot à mot (plus aucun `*-baseline.json` de compte sous `scripts/raw`/`scripts/guards`). Un bloquant de MESSAGE porté (l'assertion du T2 était non discriminante, née d'une fixture décalée d'une ligne — dit au message de ce commit) ; souhaitables portés par le codeur T4c et rejoués par l'orchestratrice. Non joué par les juges : la suite complète et les gates — jouées sur la tête du lot avant le push.
