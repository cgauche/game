# ref-670 — Carnet d'enquête : cœur (état + effets + coop + save) + éditeur (hors écran Carnet joueur)

## Juge
Deux juges adversariaux, deux lentilles.
- **Cœur (moteur)** — verdict initial PARTIEL : immutabilité/pureté CONFIRMÉES, persistance cross-scène CONFIRMÉE (assertion positive : un indice révélé survit à `transitionTo`, `startScene` remet `{}`), save additif CONFIRMÉ (`clues` clé non-fonction de `getInitialState()` → capturée par `snapshotSave` ; save v15 antérieure sans `clues` → `{}` via `{...base,...data}` ; goldens #766 non cassées), coop CONFIRMÉE (`toggleCluePin` ∈ `PARTY_INTENTS` = action réelle). Trois griefs réels trouvés et CORRIGÉS dans la passe de durcissement : (1) `revealClue` avait 3 comportements sur une fausse piste selon le stade visé (no-op muet possible = effet silencieux) → sémantique UNIFORME décidée (« un revealClue résulte toujours en `révélé` au stade cible ; seul vrai no-op = déjà révélé au stade cible ») ; (2) journal de ré-activation trompeur → 3e branche `eff.clueReactivate` ; (3) tests save simulaient le merge inline → réécrits sur le VRAI `saveGame`→`loadGame`, couverture mono-stade ajoutée, assertion tautologique retirée. `discreditClue` rendu vrai-idempotent.
- **Éditeur (vision)** — verdict initial RÉFUTÉ : DÉFAUT bloquant `addIndice` sur 0 affaire produisait `affaireId:''` (crash `validateNarratif` au réimport) → CORRIGÉ (bouton `disabled` + garde de fonction, garantit un indice valide au round-trip) ; RÉSERVE collision d'id inter-catégories au rename (comportement de la référence `renamePreset`) → DURCIE au-delà de la référence (`idUsedElsewhere` dans les 3 gardes) ; Lentille « aucun test fonctionnel » → 4 tests de round-trip ajoutés (`NarratifEditor.test.tsx`). Immutabilité, sélection par onglet, primitives réutilisées, baseline ratchet honnête (1→3), zéro poison : CLEAN.

## Réfutation
verdict: CONFIRMÉ
Les deux verdicts adversariaux (PARTIEL + RÉFUTÉ) ont été RÉSOLUS par deux passes de durcissement, chacune re-vérifiée par l'orchestrateur en LISANT le diff final (contre-grep : `revealClue` l.43-49 sémantique uniforme, `discreditClue` l.69 idempotent, handler 3 branches l.661 + guard l.659/672, 4 clés `fr.ts`, `idUsedElsewhere` câblé dans les 3 renames l.110/139/180, garde `addIndice` l.155 + bouton `disabled` l.267) — jamais sur la foi des rapports d'agent. Gates relancés PAR MOI sur l'état final combiné : typecheck 0 imputable (hors `_tmp-*` d'autres sessions), suite complète `success:true` 12912 passed / 0 failed / 9 pending (3370/3370 suites), i18n-narration-guard + ui-ratchets verts. Aucune contamination LITM dans mes fichiers (chaque hunk vérifié clue/enquête ; `_registry.generated.ts` n'ajoute que `97-enquete-carnet`). L'écran Carnet JOUEUR (surface de lecture) n'est PAS dans ce commit — il ferme #670 avec validation de goût utilisateur + recette + revue de palier.

2026-07-23
