# Solde #1803 — Conteneur cloud non conforme au canon : `gh` absent (ops:publier / pre-push bouclent sur ENOENT) et `core.hooksPath` vide (aucune garde de commit) — hook SessionStart de mise en conformité — 2026-09-18

VERIFIE: les trois manques du ticket mesurés puis REFERMÉS dans le conteneur `container_01YEJY6RviMqDR2Mzkndr876` (Ubuntu 24.04, `CLAUDE_CODE_REMOTE=true`), chacun avec sa sonde avant/après — (1) `git rev-parse --is-shallow-repository` répondait `true` et `npm run test:hooks` rendait `# pass 1000 / # fail 10`, les dix refus nommant eux-mêmes « dépôt SUPERFICIEL : cette mesure lit l'HISTOIRE » (`scripts/hooks/fermetures-sans-solde.test.mjs:68`, `soldes-stock`, `stocks-nominatifs`, `segments-profonds`) ; après `git fetch --unshallow origin` (22,9 s, 50 → 4884 commits) la même gate rend `# pass 1014 / # fail 0` ; (2) `git config core.hooksPath` était VIDE, donc `pre-commit`/`commit-msg`/`pre-push` morts (`docs/reprise-apres-pause.md` § 5) — `npm install` (7,5 s, cache npm chaud dans l'image) pose `scripts/git-hooks` par le `postinstall` de `package.json:11` ; (3) `which gh` ne rendait rien alors que `scripts/ops/publier.mjs:725` et `scripts/git-hooks/pre-push.mjs:77` en dépendent — `apt-get install -y gh` pose `2.45.0-1ubuntu0.3` (le dépôt officiel `cli.github.com` est REFUSÉ par la politique de sortie, `curl` → 403 au proxy), et la capacité VISÉE répond : `gh run list --commit <sha> --json status,conclusion` → `[{"conclusion":"success","status":"completed"},…]`. Le hook lui-même joué pour de vrai dans les deux sens : `CLAUDE_CODE_REMOTE=true node scripts/hooks/bootstrap-conteneur.mjs` → sortie VIDE, rc 0 (conteneur désormais conforme, silence voulu) ; `env -u CLAUDE_CODE_REMOTE` → idem, aucune commande lancée. Gates locales sur l'arbre final : `docs:check` rc 0, `agents:check` rc 0, `lint` rc 0, `test:hooks` 1014/1014, `test:docs` 83/83, `test:ops` 258/258, `test:agents` 24/24, `bootstrap-conteneur.test.mjs` 13/13.

## Restes
- `gh issue view --json` / `gh issue comment` de l'étape de PILOTAGE (`scripts/ops/publier.mjs:1040,1070`) passent par GraphQL, refusé HTTP 403 aux sessions Claude Code — sonde : `gh issue view 1803 --repo cgauche/game --json state,comments` → « GitHub GraphQL is not available from Claude Code sessions; use the REST API » ; ticket « ops:publier — l'étape de pilotage passe par GraphQL, refusé HTTP 403 en session Claude Code : le commentaire de ticket est posé à la main » -> #1804
- `server/node_modules` (portes `npm --prefix server ci` et `server:typecheck` du canari) n'est pas posé par le hook -> RAS : nommé « hors périmètre » au corps du ticket, aucune porte du TRAIN de publication ne le lit — le canari tourne sur un runner propre qui joue son propre `npm ci`
- `docs/raw/reconciliation.md` se réécrit à chaque run POSIX (chemins Windows committés) : sorti du commit par `git checkout --`, jamais poussé -> RAS : défaut PRÉ-EXISTANT et déjà porté par #1801, dont le titre le décrit à l'octet ; l'y router une seconde fois créerait un doublon
- L'empreinte de sources de `docs/consommateurs-de-champs.md` est REGÉNÉRÉE dans ce commit (pied seul, `corps:` inchangé à `0f7a3f86…`) -> RAS : légitime et obligatoire — `docs/.sources-lues.json` liste `scripts/hooks` parmi les dossiers lus par `scripts/docs/build-field-consumers.mts`, deux fichiers neufs y changent donc le listing hashé ; `docs:check` refuse le pied périmé, il ne s'agit pas du flottement de #1769
- Le `SessionStart` est déclaré SANS matcher, donc rejoué sur `resume` et `compact` -> RAS : `scripts/hooks/settings-guard-canaux.test.mjs:132` exige que tout segment de matcher soit un NOM D'OUTIL réel (`startup` n'en est pas un) ; le coût d'un conteneur conforme est de trois constats silencieux, et la correction du bloc B1 ci-dessous retire le faux positif qui aurait fait de ce rejeu un `npm install` par compaction

## Réfutation
verdict: CONFIRMÉ après réfutation — juge adversarial de DIFF (2026-09-18, lecture seule, arbre épinglé `7193fa723`) : **deux BLOQUANTS vérifiés, tous deux corrigés avant commit**. B1 — `lancer` concaténait `stdout` et `stderr` dans la valeur COMPARÉE à l'octet près par les deux constats (`bootstrap-conteneur.mjs:27` d'alors) : un `warning:` de git sur stderr donnait un faux positif sur `core.hooksPath` (`npm install` complet à CHAQUE démarrage, l'idempotence annoncée au docblock devenant fausse) et un faux négatif sur `--is-shallow-repository` (dépôt superficiel conservé EN SILENCE, exactement le défaut que le ticket prétend fermer) ; le juge l'a prouvé par une sonde exécutée rendant `sortie = "scripts/git-hooks\nwarning: bruit du conteneur"`. Correction : `lancer` rend désormais `{ ok, valeur, rapport }`, `valeur` ne lit que `stdout`, `rapport` garde les deux flux pour NOMMER un échec ; verrouillé par le test « la VALEUR mesurée ne lit que stdout ». B2 — budget incohérent : 150 s par commande × 4 commandes possibles contre un `timeout` déclaré de 180 s, et le test verrouillait cette justification fausse. Correction : chaque prérequis déclare son `budget`, `BUDGET_TOTAL` en est la SOMME et devient la source unique que le `timeout` de la surface doit couvrir (300 ≥ 300), assertion calculée et non recopiée. Mineurs retenus du même rendu : le `geste` rapporté ne nommait pas la commande jouée (`apt-get update` tu) ; `lancer` jetait la sortie partielle sur `ETIMEDOUT` ; le rapport n'était borné qu'en LIGNES, pas en octets (50 000 caractères de stderr entraient au contexte — désormais 400) ; le hook était câblé sur `.codex/hooks.json` où sa garde `CLAUDE_CODE_REMOTE` ne peut que rendre une liste vide, donc porté en `HOOKS_MONO_SURFACE` côté `SURFACE_CLAUDE`, et les fixtures de `scripts/agents/compat.test.mjs` se DÉRIVENT maintenant de cette table (un mono-surface de plus n'oblige plus à toucher un cas) ; `docs/reprise-apres-pause.md` annonçait « 17 scripts déclarés dans `.claude/settings.json` » en comptant des FICHIERS — il en déclare 15, une lib importée et le hook propre à Codex n'en étant pas, le compteur mesure désormais les DÉCLARÉS. Griefs explicitement essayés par le juge et ABANDONNÉS, cités ici pour ne pas les rejuger : la garde de module principal sans `resolve()` (sondée, aucune divergence de comportement avec le patron canonique, y compris par symlink), le pied d'empreinte de `docs/consommateurs-de-champs.md` (légitime, preuve au `docs/.sources-lues.json`), et la parité des deux surfaces (clef `phase|matcher|script|timeout` identique, `forbiddenEverywhere` et `forbiddenOnCodex` non déclenchés).
