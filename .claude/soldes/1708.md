# Solde #1708 « gates --serie : l'ordre de ci.yml n'est pas un ordre de coût — docs:empreinte (14 s) et server:typecheck (3 s) rougissent APRÈS la suite de 8 min, trois runs pour rien (2026-09-06) » — 2026-09-12

VERIFIE: le ticket demandait trois gestes (un ordre de COÛT en `--serie`, un refus de `server:typecheck` qui nomme `server/node_modules` absent et la commande qui le pose, et « sur un arbre à `docs:empreinte` rouge, `--serie` s'arrête en moins de 2 min ») ; confrontés à l'arbre `32a078c21` (= `origin/main`, CI verte) par la seconde réfutation de fermeture de #1709 (UN juge, lecture seule, 2026-09-12) — (1) l'ordre de coût est REFUSÉ par la mesure, refus posté sur le ticket le 2026-09-11 et re-vérifié chiffre par chiffre par le juge : en série le total est la somme des gates quel que soit l'ordre (run de référence `b84ea74ff`, 22 gates, `gates-final.log:73` « total 479.8 s · série équivalente 479.8 s », machine calme) ; le mode réel est celui des LANES (train E `1e7f4f830`), borné par le mur `test` 155,3 s contre `typecheck` 81,8 + `lint` 69,4 = 151,2 s — équilibré à 4 s près ; un ordre croissant placerait `test` (la seule gate rougie deux fois sur neuf runs le 2026-09-11) en DERNIER et retarderait le rouge de ~45 s à ~325 s (elle est cinquième dans `ci.yml`, `scripts/gates/toutes.mjs:385`) ; l'ordre de `ci.yml` garde le résumé comparable ligne à ligne à la CI ; (2) le geste 2, que le juge a trouvé MUET (ni au code — `package.json:87` restait le `tsc` brut — ni refusé), est LEVÉ dans ce commit : un champ optionnel `prerequis: [{ chemin, pose }]` de la table par gate `ECRIT_LU` (`scripts/gates/toutes.mjs:352` pour `server:typecheck` : `server/node_modules`, posé par `npm --prefix server ci` — la commande de `.github/workflows/ci.yml:86`, vérifiée à l'octet), et le pilote (`jouerUneFois`, `toutes.mjs:845`) refuse SANS spawner quand un chemin manque sous la racine : verdict ROUGE, fichier de sortie « prérequis absent : `server/node_modules` (le pose : `npm --prefix server ci`) », aucun justificatif écrit (seul `justifie.mjs` en écrit, il n'est pas appelé) ; générique, aucun nom de gate dans le pilote ; tests `node --test scripts/gates/toutes.test.mjs` rejoués par l'orchestratrice sur l'arbre principal : 29/29 verts, eslint 0 (sorties en fichiers) ; mutation (contrôle débranché) → le contrat positif rougit en nommant le chemin ET le code du lanceur passe de 1 à 0 ; juge de diff : TIENT SOUS CORRECTIONS, aucun bloquant, quatre souhaitables portés (`.gitignore` du dépôt jetable rendant le code du lanceur assertable, angle mort `existsSync` nommé au JSDoc `toutes.mjs:93`, portée du refus dite au site et au ticket, `assert.match` homogène) ; (3) le geste 3 est tenu par la SÉQUENCE et non par un tri : `docs:build` avant tout lancement (fiche `feedback-gates-docs-build-avant-lancement-sinon-25-min-perdues`, hooks `post-merge`/`post-rewrite`) et `docs:empreinte` jouée dans sa lane de lecteurs sans attendre `test` (table `LANES` de `toutes.mjs`) — sur un arbre à empreinte rouge, le rouge tombe au temps de sa lane, pas après la suite.

## Restes
- Ordre de coût en `--serie` (geste 1) -> RAS : refus MESURÉ posté sur #1708 le 2026-09-11 et re-vérifié par le juge du 2026-09-12 (somme invariante en série, mur des lanes à 4 s près, fail-fast meilleur avec l'ordre de `ci.yml`, résumé comparable à la CI)
- `server:typecheck` muet sur `server/node_modules` (geste 2) -> corrigé par f4a9fa5da scripts/gates/toutes.mjs:845
- Portée du refus de prérequis : un justificatif VERT antérieur reste valable après suppression de `server/node_modules` (clés = `git ls-tree`, `scripts/guards/lib/justificatif.mjs:47,77`, aveugles à un dossier gitignoré) -> RAS : propriété PRÉEXISTANTE du régime de justificatif, dite au site (`toutes.mjs:837-843`) et au ticket (commentaire du 2026-09-12) ; le refus mord sur tout contenu non encore justifié, qui est le cas du ticket (arbre frais)
- Arrêt < 2 min sur `docs:empreinte` rouge en `--serie` (geste 3) -> RAS : `--serie` est le mode DIAGNOSTIC (« même verdict, coût plein ») ; le mode réel joue `docs:empreinte` dans une lane de lecteurs sans attendre la suite, et `docs:build` précède tout lancement (hooks + fiche mémoire)

## Réfutation
verdict: CONFIRMÉ — seconde réfutation de fermeture de #1709 (UN juge, lecture seule, arbre `32a078c21`, 2026-09-12), clause 1 « ordre de coût en `--serie` (#1708) » : le refus du geste 1 est un FAIT (les trois chiffres re-vérifiés à `gates-final.log:59-61,73` et `toutes.mjs:385`) et TRACÉ au ticket ; le geste 2 était MUET (paire question/réponse : le refus posté ne répondait qu'au geste 1) → clause bloquante, LEVÉE par le code de ce commit ; juge de DIFF sur ce code (lecture seule, 2026-09-12) : TIENT SOUS CORRECTIONS, aucun bloquant, mutation par copie hors arbre → 28/29 avec le contrat positif rouge nommé, sonde `.gitignore` promue dans `depotDeGates`, quatre souhaitables portés par le codeur puis rejoués (29/29, eslint 0) et relus par l'orchestratrice sur l'arbre principal.
