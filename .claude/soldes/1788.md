# Solde #1788 « CI : sceneTint.test.ts « BUDGET » mesure une horloge (ratio bake/teinte) — rouge non déterministe sur main, vert sur la branche du même sha » — 2026-09-18

VERIFIE: le DoD relu clause par clause — (1) `sceneTint.test.ts` ne contient plus `performance.now()` : le contrat « repeindre la visibilité n'écrit que `color` » se lit sur la géométrie (tampons, `count`, `itemSize`, `version` de chaque attribut relevé par lecture, index intouché, `color` à exactement +1 par passe) ; mutation : `position` recréé dans `applyVisibilityTint` → rouge nommant l'attribut (rendu du codeur, sortie collée) ; même traitement pour `sceneCutaway.test.ts` (index ré-alloué → rouge) ; (2) `git grep -n -E "(performance|Date)\.now\(" -- "src/**/*.test.ts" "src/**/*.test.tsx" src/test-setup.ts` : 5 lignes, toutes prose ou fixture-chaîne (`banc-volumique.test.ts:258`, `flipbook-frames.test.tsx:373,733`, `dual-wield.test.ts:51`, `useGamepad.test.tsx:44`), zéro code — la CLASSE est traitée : 23 sites dans 12 fichiers + le harnais `src/test-setup.ts` (graine fixe), chacun réécrit selon son contrat (attributs de géométrie, compteur de touches par `Proxy` pour `wallIndex` — 669 à la construction puis 0 sur 10 000 questions contre 6 047 133 au balayage naïf —, `vi.useFakeTimers` avec bornes exactes à ±1 ms pour le plafond d'entrée en scène, `performance` fakée sans rAF pour le flipbook, `vi.waitFor` pour la convergence chokidar, graines RNG fixes, log de durée retiré) ; les mesures de COÛT RELATIF (`versionDataset`, `arete-dans-la-chaine`) sont des bancs `*.bench.ts` joués par `npm run bench`, hors `npm test` (`vite.config.ts` `include`, `npx vitest list --filesOnly | grep -c bench` = 0) ; (3) garde `src/tests-sans-horloge-guard.test.ts` : baseline ZÉRO, aucune liste de sites tolérés, périmètre = le corpus de `npm test` (câblage vérifié contre `vite.config.ts`) plus le harnais, vue CODE SEUL par la primitive partagée `scripts/guards/lib/codeSeul.mjs` (commentaires et chaînes blanchis, lignes et colonnes préservées, testée en `node --test`), contrat de vie, mutation `performance.now()` planté → rouge nommant le site. Gates sur l'arbre final : typecheck 0, 13 fichiers / 316 tests du périmètre verts, 208 fichiers ensemençant ou lisant `battleRng` rejoués verts sous la graine fixe (2761 tests), `codeSeul`/`sourceCorpus`/`battleRngEngineLeak` en `node --test` verts, eslint 0 sur les fichiers neufs, `docs:check` 0. Juges : design FRAGILE (4 corrections mesurées : `wallIndex` en compteur et non en banc, `performance` fakée pour le seul flipbook et jamais rAF, `codeSeul` hors de `sourceCorpus.mjs`, périmètre = corpus de `npm test`), diff cumulé TIENT (2 bloquants levés : commentaire faux sur la reproductibilité, harnais à horloge fermé ; 3 souhaitables levés : tombales, prémisse de vacuité du bake, ligne vide). Aucun écran touché.

## Restes
- ~20 copies locales de `stripComments`/`sansCommentaires` dans `scripts/guards/lib/*.mjs` (`hardcode.mjs`, `labelLogic.mjs`, `tableLookup.mjs`, `revealKindEmission.mjs`, `bindingsVifs.mjs:29`, `charKeyLegacy.mjs:12`…) et une dizaine inline dans `src/**/*.test.ts` — contrats voisins mais non identiques (lignes non préservées, imports retirés), la classe que `codeSeul` a vocation à absorber -> #1790
- `src/state/battleRng.ts:8` ensemence le RNG de combat à `Date.now()` à l'IMPORT : code de PRODUCTION, hors classe (une partie solo tire sa graine de l'horloge, c'est le sens d'une partie) ; les tests posent leur graine eux-mêmes (`src/test-setup.ts`, graine fixe) -> RAS : la garde exclut la production par construction, le harnais est dans son corpus
- Le brief de reprise portait « 0 lecture d'horloge dans `src/**/*.test.ts(x)` » sur la foi d'un `ctx_search` qui n'avait scanné aucun fichier (23 sites réels) -> RAS : fiche mémoire `game-ctx-search-skips-large-files-use-grep` complétée (un « scanned 0 » est une non-mesure)

## Réfutation
verdict: CONFIRMÉ — Tentée sur pièces : (a) « la garde à zéro cache une liste » — le fichier ne porte aucun nom de site, le périmètre est dérivé des `include` de `vite.config.ts` et verrouillé par un test de câblage, sonde du juge : 1688 fichiers, 0 site, un `Date.now()` planté dans `scripts/map/check.test.ts` ressort ; (b) « les bancs font perdre un contrat que la CI tenait » — `wallIndex` n'est PAS parti au banc (compteur de touches, contrat plus fort que le ratio ≥5×) ; `arete-dans-la-chaine` garde son verdict fonctionnel au test et son risque O(n²) est déjà tenu par `toHaveBeenCalledTimes(1)` ; `versionDataset` garde ses deux contrats purs et sa prémisse de vacuité du bake au test ; (c) « les faux timers pendent ou mentent » — flipbook et entrée en scène joués 3× chacun par le juge (19987/20403/19862 ms, 20234/17602/17822 ms), `performance` fakée seulement où la production la lit (`animNow`), rAF jamais faké, `useRealTimers()` rendu ; (d) « la graine fixe globale casse un test qui vivait de l'aléa » — 208 fichiers rejoués verts ; (e) « `codeSeul` desserre `battleRngEngineLeak` en silence » — le contrat présent est dit au JSDoc et couvert par deux cas positifs (appel en chaîne ≠ appel, voisin en code = appel).
