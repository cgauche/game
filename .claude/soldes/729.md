# Solde #729 — sorts des 8 domaines + Sorts d'Arcane — 2026-07-26

VERIFIE: diff exhaustif corpus↔donnée sur les 136 titres à `**NI :**` des chapitres 02/04-11/15 — un seul sort sans lien VDM (`configuration-du-terrain`, porté par #860) ; sonde OFF→ON des 18 variantes sur le chemin réel `findSpellById → effectiveEntry` (18/18, zéro champ inerte) ; HEAD était ROUGE et ne l'est plus (`8ce9d5c0` : la garde d'intégrité ne connaissait que `talents.json`, elle dérive désormais des defs) ; les 4 contradictions desc/op corrigées et vérifiées par comparaison de la desc effective à l'op effective (`c60efdfe`) ; portes relancées par moi : tsc 0 erreur, 14129 tests verts, 6 gardes documentaires vertes.

## Restes

- 11 divergences par OMISSION entre desc variantisée et op non republiée -> #880 (4 des 11 sont inscrites dans une liste de reste-à-traiter qui ne peut que décroître ; les 7 autres sont hors de portée de la garde, angle mort déclaré)
- `configuration-du-terrain` non curé faute d'unité « milles » sur `SpellRange` -> #860 (vérifié : ligne 4 de son tableau)
- `windModifiers` des 8 Vents fidèles mais DORMANTS (aucun producteur de circonstances de monde) -> #844 (vérifié : son corps tabule les 8 Vents et nomme `windModifiers[].when`)
- 52 sorts VDM à op `narrative` seule -> #838 (recensement réancré sur les chiffres réels le même jour)
- 308 sorts portant « arbitrage MJ » en donnée, trois familles mélangées -> #838 (tri par famille demandé, un cliquet global ne mesurerait aucune des trois)

## Réfutation

verdict: PARTIEL — un audit de fermeture a rendu NON FERMABLE et avait raison sur deux points durs : HEAD était rouge (mesures « suite verte » faites sur l'arbre, jamais sur le dépôt) et le champ `variants` des sorts n'était pas éditable, alors que tout le delta VDM y vit. Les deux sont corrigés depuis. Le verdict reste PARTIEL et non CONFIRMÉ parce que 11 divergences desc/op subsistent : la fermeture est justifiée par le fait qu'elles sont mesurées, nommées une par une et portées par #880 — pas parce qu'elles seraient résolues. Le DoD du ticket (curation, variantes gatées, éditabilité, coverage ⬜0) est lui intégralement satisfait, chaque point vérifié sur pièces.
