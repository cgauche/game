# Solde #1691 — 2026-09-07

VERIFIE: l'attendu du ticket confronté à l'arbre après le lot 2 — (1) la matière de relief est une DONNÉE : `terrains.json › matiere` (`idDe('material','relief')`, exigée ⟺ `solidHeightM > 0`, `defs/terrains.ts:101-112`, `mur → pierre`) et `scene.reliefDefaults` (record EXIGÉ au schéma de scène, clés dérivées du vocabulaire ÉMIS `cliff`/`ramp`/`deck`/`pilier`, `defs-scenes/scene.ts:205-211`, défaut posé par `emptyScene` donc hérité par `mapSpec.buildScene` et les 38 scénarios) ; (2) `floors.ts:166,201` LISENT (`scene.reliefDefaults.pilier`, `solidBlock ? terrainMatiere(terrain) : scene.reliefDefaults[part]`), les termes `deck ||` et `z > 0` — que 0 face du corpus exerçait (sonde du juge de design : 3 856 faces relief, `deck` à z=0 : 0, cliff/ramp non solides à z>0 : 0) — ont disparu ; garde dérivée `src/gameIso/builders/matieres-en-donnee.test.ts` (les ids de `materials.json` grep-és dans `builders/**` = 0, aucune exception) ; (3) migrations datées rejouables `scripts/migrations/2026-09-07-1691-relief-en-donnee.mjs` (data : `mur.matiere`, mort de `plafond`, `plan.vueDeDessus`) et `2026-09-07-1691-relief-defaults-scenes.mjs` (28 scènes × `reliefDefaults`, 0 suppression dans les projets, générateurs byte-stables 6/6), portes `scripts/migrations/lib/1691-relief-portes.test.mjs` 10/10, no-op au second passage ; identité de rendu : sonde `FaceSurface` 66 scènes / 95 121 faces, hash `8b4c2d024bad922c…` identique, et comptage par id `pierre 2813 / pilier 808 / terre 235` identique à l'avant-lot. Complément du ticket (`roofsSvg.ts` `roofMaterial('plan')`) : `plan` porte le marqueur POSITIF `vueDeDessus` (exclusif de `couverture`, « exactement une » au dataset), lu par `matierePlan()` (`src/data/index.ts`) — plus aucun id littéral. Éditable : Inspector (panneau de scène, un sélecteur par partie filtré `relief`, à côté de la Couverture) et Codex « Terrains » (sélecteur `matiere` filtré `relief`, premier usager de `REF_FIELD.filter`).

## Restes
- `floors.ts:165` `'pilier'` en dur -> corrigé dans ce commit : src/gameIso/builders/floors.ts:166
- `floors.ts:198` `deck || z > 0 || solidBlock ? 'pierre' : 'terre'` en dur -> corrigé dans ce commit : src/gameIso/builders/floors.ts:201
- `roofsSvg.ts:298` `roofMaterial('plan')` par id littéral (complément du ticket) -> corrigé dans ce commit : src/gameIso/authoring/roofsSvg.ts:299
- « Masse de relief » à laquelle accrocher la matière (attendu du ticket) -> RAS : aucune entité de masse n'existe, la matière est choisie par FACE ; le juge de design a réfuté une surcharge par couche (aucun panneau de couche, `putLayer` la détruirait, 0 face ne l'exercerait) — la donnée vit sur la SCÈNE (`reliefDefaults`, patron `roofDefaults`) et sur le TERRAIN à bloc plein
- Entrée `plafond` du domaine relief : 0 émetteur (le « dessous de tablier » est nommé dans `mapSpec.ts:733` sans face émise), seul lecteur un test à cardinal vivant réécrit en contrat positif par entrée -> inventaire #1686 : l'entrée RESTE — deux migrations datées antérieures l'affirment par cardinal (`2026-08-28-l1b-10c-relief-roof-labels.mjs`, `2026-09-05-1686-materials.mjs` : relief 4 / total 16) et leur rejeu est une porte du pre-push ; sa mort viendra avec son émetteur ou avec un régime de rejeu tolérant aux morts postérieures
- Cliquet des champs ÉTRANGERS de l'atelier `+ terrains: 2` (`codex-edit-charge-discriminee.test.tsx:174`) : le ⟺ `solidHeightM`/`matiere` partitionne par PRÉSENCE de champ, que `chargeParDiscriminant` ne sait pas exprimer — un auteur qui pose une hauteur de bloc sur `herbe` est refusé au save, nominativement -> inventaire #1463 : même famille que `activities`/`oups`/`reglesOptionnelles`/`structures` (4 → 5 documents), le socle « charge par présence de champ » soldera les cinq lignes d'un coup
- Ids de `materials.json` en dur dans le catalogue TS des bâtiments (`src/gameIso/catalog/buildings/defs/*.ts:7` `roofMaterial: 'tuile'`/`'chaume'`/`'toit-ardoise'`, hors `builders/**`) -> inventaire #1686 : même défaut que les 25 defs de terrain avant #1690 (un catalogue TS qui porte de la donnée), relevé par le juge de diff ; la garde dérivée de ce commit ne couvre que `builders/**`
- Fiche Codex de `mur` sans sa matière en lecture (recette, étape 4a) -> corrigé dans ce commit : src/ui/compendium/registry.ts:2065
- `matierePlan()` avec un `!` sur un invariant tenu par le schéma seul (juge de diff, S2) -> corrigé dans ce commit : src/data/index.ts:2442
- Refus d'une matière sur un terrain sans bloc plein au clic « Enregistrer », pas à la sélection (recette, étape 4c) -> RAS : régime de tout l'atelier (validation transactionnelle au save, `CodexEdit.tsx:595-605`), le refus est nominatif et nomme le terrain et le champ
- Le plan 2D de l'éditeur ne repeint pas une matière de relief éditée (recette, étape 2) -> RAS : le plan schématique ne peint pas les faces de relief (couleurs par terrain, `floorsSvg.ts`), le repeint se voit en vue jeu par `worldBakeDeps` (`sceneMeshes.ts:335`) ; dit au brief des prochaines recettes
- `reliefDefaults` EXIGÉ au schéma refusait les projets de schéma antérieur et les fixtures de test (29 rouges de la suite complète) -> corrigé dans ce commit : src/state/worldMap.ts:695 (bump de version de projet 7 → 8, `PROJECT_MIGRATIONS[7]` pose `DEFAULT_RELIEF_DEFAULTS` ; la migration datée devient la dernière de la chaîne)
- `<projet>.json | reliefDefaults` entre au stock des champs sans slot déclaré (4 lignes, `DETTE_ADOPTION_MAX` 339 → 343) alors que le slot est déclaré et résout 28/28 : `champDuPath` projette `scenes[].reliefDefaults.cliff` sur `cliff`, jamais sur le champ porteur -> inventaire #1463 : angle mort déjà déclaré du scan des slots (précédent `props.json | light`), soldé par le dériveur d'un niveau, pas par une adoption au champ
- Porte `scripts/migrations/lib/1691-relief-portes.test.mjs` posée sans être branchée (garde morte) -> corrigé dans ce commit : package.json:25
- `flipbook-frames.test.tsx` rouge dans la suite complète sous charge (mémoire à 100 %) -> RAS : 3 passes seul, 30/30 vert, aucun lien avec le lot
- Cinq replis `?? DEFAULT_ROOF_DEFAULTS` (`sceneEdit.ts:1047,1150,1151`, `roofs.ts:1188`, `sceneMeshes.ts:383`, `Inspector.tsx:261`) : `roofDefaults` optionnel au schéma avec un défaut en code, non copié pour le relief (champ EXIGÉ + migration) -> inventaire #1686 : règle en dur préexistante, nommée par le codeur et confirmée par le juge

## Recette visuelle
capture: public/qc/1691-relief/d-01-inspector-relief.png
capture: public/qc/1691-relief/d-02-codex-mur.png
Les deux captures ci-dessus sont postérieures à la DERNIÈRE retouche d'écran du lot (fait « Matière » sur la fiche Codex). La recette n°1 (`recette-1691-L2.md`, 9 images `0x-*.png` sous `public/qc/1691-relief/`) documente le parcours complet : Inspector, repeint, vue jeu sans repli, atelier, refus nominatif, mobile 360 px.

## Réfutation
verdict: CONFIRMÉ — juge de DESIGN (lecture seule) sur l'invariant v1 : RÉFUTÉ sur deux clauses (surcharge par couche ; migration des scénarios TS) et corrigé en v2 par sonde (3 856 faces, 0 face n'exerçant `z > 0`, un seul terrain à bloc plein) ; juge de DIFF (lecture seule, `juge-1691-L2`) : ACCEPTE, 10 lentilles sans réfutation — builder sans littéral, vocabulaire des clés ⟺ émetteur (`Grade` + `pilier`), `emptyScene` seul constructeur hors spread, ⟺ nominatif, `plan`/`plafond`, migrations no-op au rejeu et 0 suppression dans les projets, sonde rejouée (hash + cardinaux par id), Inspector = widget de la Couverture, 0 poison, cliquets mesurés (bancs : 23 + 7 + 2 + 3 fichiers, 76 + 223 + 74 + 8 tests, portes 10/10, byte-stables 6/6) ; suggestions portées dans ce commit : `matierePlan()` sans `!` (anomalie nommée), sonde d'identité des ids de relief PROMUE en test ; suggestions routées à l'inventaire ci-dessus. Recette navigateur (`recette-1691-L2`, captures `public/qc/1691-relief/`) sur l'Inspector, le rendu 3D et le Codex. Ce que les juges n'ont pas joué : la suite complète et les gates — jouées avant le push.
