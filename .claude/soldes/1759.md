# Solde #1759 — les tests `scripts/**` se découvrent par répertoire, jamais par liste — 2026-09-14

VERIFIE: l'orchestrateur a lu `scripts/gates/testsParGate.mjs`, `scripts/test/node-tests.mjs` et le diff de `package.json` (7 scripts `test:*` de 51 caractères, plus aucun nom de test), contre-grepé les sorties du codeur (7 gates vertes via le lanceur : hooks 935/935 avant suivi du test neuf, docs 71, raw 379, ops 197, runner 52, agents 24, recette 44 ; `toutes`/`ecrivainsAtteints`/`testsParGate` 41/41 ; eslint 0 ; comment-poison 73/73 ; docs:check OK ; empreinte OK sur l'index), et vérifié que les 2 tests jamais joués (`spawnResilient.test.mjs`, `canauxMecaniques.test.mjs`) entrent verts sans retouche ; `couverture()` = 0 orphelin, 0 doublon sur les 101 tests suivis.

## Restes
- `package.json` porte encore deux lignes > 200 caractères hors périmètre (`postinstall` 729, `docs:check:brut` 244) -> RAS : le ticket visait les listes de tests ; ces deux scripts ne sont pas des registres, ils ne grossissent pas par accrétion d'entrées.
- `docs/superpowers/plans/2026-07-23-compatibilite-claude-codex.md:631` cite l'ancienne valeur de `test:hooks` -> RAS : plan daté sous `docs/superpowers/plans/` (archive de 45 plans exécutés, hors `docs:check`), la politique de suppression du CLAUDE.md porte sur `docs/plans/`.
- Famille de tombale « passé(e)(s) à/vers » non mécanisable (98 sites nus, 8/14 contrats au présent avec auxiliaire) -> corrigé dans ce commit (scripts/guards/lib/commentPoison.mjs:254) : angle mort MESURÉ écrit à côté des deux existants.

## Réfutation
verdict: CONFIRMÉ — juge adversarial (Opus, 2026-09-14) a attaqué le diff sur 7 lentilles (source unique, découverte, garde de couverture, runner, lanes ECRIT_LU, stock déplacé, tombales) : 6 défauts rendus (parité table/package.json/ci.yml absente, tombales « passé à », découverte disque vs git, doublon muet, table mutable patchée par le test, drapeaux avalés), tous corrigés puis prouvés par mutation (rouge nommé / vert rebranché, fichier remis à l'identique) ; griefs tombés : détection par chaîne du lanceur (ATTENDU non vide par gate rougit), symlinks/node_modules/casse (listerArbre puis git ls-files), conflit de lane sur `public/`/`server/` (aucune gate n'y écrit).
