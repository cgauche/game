# Solde #1702 — 2026-09-16

VERIFIE: le contrat du ticket (« `learnSpell` consulte le même `eligibleTalent` que le repli, héros nommé ou non (UNE garde, pas deux) ; refus NOMMÉ au journal, jamais silencieux ») confronté au code et au Source : `src/state/combatEffects.ts:1457-1468` n'a plus qu'un prédicat, `spellCost(h, sp) != null` (`src/engine/grimoire.ts:140-143`, la garde de `buySpell`), pour le `mutate` du héros nommé, le `pick` du repli et le journal ; le RAW est cité verbatim au Source (`Source/Warhammer v4 - Livre de base version corrigee/46 - Les regles magiques.md:14` : « Les Sorts de Domaine sont ceux que vous pouvez apprendre seulement si vous connaissez ce Domaine »). Juge de diff (lecture seule, huit lentilles) : sonde de mutation hors arbre — implémentation de `ba8aaaae6` rejouée par surcharge de `EFFECT_HANDLERS.learnSpell.apply` : `MUTANT spells=["cauteriser"] journal=["… apprend Cautériser."]` contre `ARBRE spells=[] journal=["… ne peut pas apprendre Cautériser (déjà connu ou Talent manquant)."]` ; et la prémisse « déjà connu restait silencieux » RÉFUTÉE par mesure : à `ba8aaaae6` le journal MENTAIT (« Wilhelmina Faust apprend Éblouissant. » sans écriture) — le lot supprime un faux positif de journal. Quatre cas de câblage sondés (nommé déjà connu, `heroId` inconnu, repli Arène, repli sans lanceur) : aucun journal faux. Seul site authoré (`src/scenes/arene/arene-projet.json:16983`, `eblouissant`, sans `heroId`) reste vert : le repli retient la sorcière (`PREGEN.sorcier`, Talent `mineure`). Tests : `buy-spell.test.ts` § learnSpell 6 cas (nommé sans Talent → refus ; nommé avec `magie-des-arcanes` spec `feu` → `cauteriser` appris ; Talent d'un autre Domaine `bete` → refus ; nommé déjà connu → aucun doublon, refus, jamais « apprend » ; repli vide → refus de groupe ; `heroId` inexistant → `eff.heroUnknown`), assertions au message EXACT (`t(...)`) ; `devtools.test.ts` : le `soldat` sans Talent REFUSÉ puis appris avec `magie-mineure` (le cas qui passait par le trou). Mutations (garde débranchée : 5 rouges ; clause « déjà connu » neutralisée : 1 rouge ; branche `heroUnknown` retirée : 1 rouge), empreintes blake3 restaurées à l'identique. Gates du périmètre : buy-spell/devtools/i18n 69/69, + comment-poison 142/142, typecheck (sortie vide), lint, docs:check OK ; `docs/campagne-effects.md:60` et `docs/raw/magie.md` régénérés.

## Restes
- `spec: 'Feu'` (LABEL du Domaine à la place de l'id `feu`) dans deux fixtures du fichier du lot -> corrigé dans ce commit (src/state/buy-spell.test.ts:66)
- même poison dans trois fixtures HORS lot (`src/state/l9-souffle.test.ts:34`, `src/state/triggered-effects.test.ts:171`, `:189`) et aucune garde de la classe -> #1782
- `heroId` authoré inexistant journalisait le refus de GROUPE (motif faux) -> corrigé dans ce commit (src/state/combatEffects.ts:1466)
- deux écritures de `spells` subsistent (`partyFlow.ts:551` et `combatEffects.ts:1463`) -> RAS : la GARDE est mutualisée (`spellCost`), un `apprendreUnSort` partagé n'économiserait qu'une ligne quand `buySpell` débite en plus les PX et porte trois messages

## Réfutation
verdict: PARTIEL — juge de diff (2026-09-16, lecture seule, huit lentilles, sonde de mutation hors arbre) : L1 fidélité RAW TIENT (`spellCost` ≡ « pas déjà connu ET Talent éligible », aucune branche ne rend `null` par ailleurs : rituel `xp` requis, Béni → 0 ; Béni/Invocation/Chaos par culte conformes) ; L2 TIENT et RÉFUTE la prémisse du brief (le « déjà connu » n'était pas silencieux mais MENSONGER) ; L3 câblage TIENT avec réserve (héros inconnu → message de groupe) → corrigée ; L4 tests FRAGILE (poison `spec: 'Feu'` non corrigé dans le fichier du lot, cas « déjà connu » sans contrat) → corrigés ; L5 données TIENT ; L6 docs TIENT ; L7 console TIENT (`__wfrp.log` expose le journal, `devtools.ts:1066-1074`) ; L8 architecture TIENT (la garde ne pouvait pas vivre dans `mutateHero`, générique à 8 appelants). Fermeture PARTIELLE en l'état → les deux griefs et la réserve corrigés dans ce commit, mutations collées.
