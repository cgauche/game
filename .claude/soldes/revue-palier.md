# Revue adversariale de PALIER — 19 commits de substance, 6 fermetures (#828 #584 #1483 #1269 #1655 #1682), 2026-09-02

verdict: PARTIEL

Revue rendue par un juge en lecture seule (mandat : réfuter) sur la fenêtre `2205fde51..9aee19448` — arbre ÉPINGLÉ à `9aee1944873ecb873cc5945151c4a574fd8a99b9` (= `origin/main`), lu par `git show <sha>:<chemin>`, jamais par le working tree (WIP de sessions voisines). Compteur PARTAGÉ mesuré à **19**, pas 18 : `git log --format=%h 2205fde51..HEAD -- src scripts` = 19 (14 non-merges + 5 merges), conforme au critère de `scripts/git-hooks/post-commit` (« un commit = +1 » sur tout diff touchant `src/**`/`scripts/**`).

Rien n'est RÉFUTÉ sur le fond. Les 6 fermetures portent toutes leur solde VERSIONNÉ **dans le commit de fermeture** (`8a2807134 --stat` : 1269/1483/1655/584/828.md créés ; `ae2fa5d58 --stat -- .claude/soldes` : 1682.md créé), au format `VERIFIE` / `## Restes` / `## Réfutation`, chacun sous le plafond d'UN reste routé (`584.md` route 0 ticket : `-> inventaire #1679` est non routante et `-> corrigé par 4d6e1ff78` est vérifiable ; `1682.md` route le seul `#1685`). Les 5 destinataires de restes sont OUVERTS (#1685, #1679, #1617, #996, #1244). Aucun commentaire de `src/**`/`scripts/**` ne cite l'un des 6 numéros avec un motif de dette (`git grep -Ei "(dette|todo|fixme|non implément|reste ouvert|déplacé vers)[^\n]{0,80}#(828|584|1483|1269|1655|1682)" HEAD -- src scripts` = 0 ; 36 mentions brutes, toutes de provenance). Aucun des 6 n'apparaît dans le stock figé de `scripts/hooks/fermetures-sans-solde.test.mjs`.

Quatre commits de substance vérifiés au hasard (2 par session voisine) tiennent leurs annonces à l'octet : ae2fa5d58 (`aa-criticals.json`/`aaCritical.ts`/`aa-critical.test.ts` ABSENTS de `git ls-tree HEAD`, `criticals.json` = 8 documents, lecteur unique `critical.ts:305 resolveCritique`) ; ea1cd7361 (`grammaire/mecanique.ts:337 noeudTest`, et « zéro octet de donnée » vérifié : `git show ea1cd7361 --stat -- 'src/data/*.json' 'src/scenes/*.json'` = vide) ; ccbdbac04 (`builders/types.ts:55 oriented: boolean` requis, `modele-forme.test.ts:112 Math.fround`) ; 270fe58a4 (`saves.ts:72 SAVE_VERSION = 39` au commit, 40 à HEAD après le conflit résolu par 2058ca5b6 ; `capIdentite` littéral au schéma `defs/props.ts:38` ; tests neufs `planche-qc-cap-identite.test.ts` et `decor-cover-atteignable.test.ts` présents). Gate mesurée, pas lue : `npm run test:hooks` = **exit 0, 429/429 pass, 0 fail** — la valeur annoncée par HEAD est exacte.

Ce qui fait basculer en PARTIEL est le MÉCANISME, comme au palier précédent :
1. **Deux fermetures de la fenêtre échappent entièrement au régime** : #1659 (2026-09-02T04:50:41Z) et #1673 (2026-09-02T05:17:57Z), closes par l'acteur `cgauche` **sans `commit_id`** (timeline REST) — aucun `.claude/soldes/1659.md` ni `1673.md` dans `git ls-tree -r HEAD .claude/soldes`, et le cliquet a posteriori ne les verra JAMAIS : `fermetures-sans-solde.test.mjs` ne mesure que les fermetures **par message de commit** (`FERMETURE_RE` sur `git log --since`). Le trou nommé au palier précédent (« gh issue close n'exige ni solde ») est colmaté côté HOOK par 586aa7cbe pour les commandes d'agent, mais reste ouvert pour la fermeture humaine par l'interface web — et le détecteur a posteriori, lui, n'a pas été étendu.
2. **2 rouges CI sur 10 runs de la fenêtre, et les 2 sont `npm run lint`** — que `scripts/git-hooks/pre-commit.mjs` NE JOUE PAS (il joue docs:check, plans-anchors, raw:implemente, test:raw, test:recette, agents:check ; `grep -n "lint\|eslint"` = 0 hit). c1a67400a (run 33593091337) : `seam-assise-guard.test.ts:257` deux U+00A0 introduites PAR ce commit (`git show c1a67400a:… | sed -n 257p | cat -A` → `M-BM-+M-BM- 0 erreurM-BM- M-BM-;`), alors que son message annonce « npm test … 1089 fichiers / 16 161 tests verts » et AUCUN eslint. Récidive de classe : 68055fe99, juste avant la fenêtre, est rouge sur le même step.
3. **Un commit poussé sur main ROUGE** : 26be12347 (06:58:29Z) hérite exactement des 2 mêmes erreurs `257:91`/`257:100` (run 33601299152) alors que main était rouge depuis 05:02 ; le vert n'est revenu qu'à 07:24 (ae127be1d). Régime « pas de push sur CI rouge » non tenu une fois sur la fenêtre.
4. **Quatre commits de substance n'annoncent aucune gate chiffrée** : 270fe58a4, ccbdbac04, ae127be1d, 4d6e1ff78 (`git log --format=%B` intégral relu — ni suite, ni tsc, ni eslint). c1a67400a en annonce une (suite) mais tait celle qui a rougi.
5. Fan-out : **5 tickets créés / 6 fermés** sur la journée du 2026-09-02 (#1681-#1685 vs #828 #584 #1483 #1269 #1655 #1682) — net **−1**, régime « fan-out ≤ 1 » TENU. Le +45 net visible sur `created:>=2026-09-01` appartient au palier précédent (inventaire de l'audit de dérive).

Classe dominante du cumul : le CONTENU est solide et instrumenté (mutations rouges nominatives, sondes promues, cliquets nominatifs décroissants) ; ce qui fuit reste la PORTE — un lint que le pre-commit ignore, et une fermeture humaine hors commit qu'aucun détecteur ne compte. Actions routées en inventaire de #1679 : (a) `npm run lint` sur le diff stagé dans `pre-commit.mjs`, mutation prouvée → L1b ; (b) le canari hebdomadaire mesure aussi les issues CLOSED par l'API (état GitHub) et non le seul journal git — sinon le cliquet ne mesure que sa propre couverture → L2 ; (c) toute fermeture hors commit exige son solde a posteriori — #1659 et #1673 en premiers cas → L2 ; (d) les gates annoncées au message de commit deviennent un justificatif daté du sha au pre-push → L2. Rien à rouvrir sur le fond : aucune des 6 fermetures n'est réfutée au contenu.
