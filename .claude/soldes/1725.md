# Solde #1725 « audit-stock rouge sur deux advisories neuves (vitest GHSA-82fw-gwwq-j7x9, sharp GHSA-rgj7-g3m4-5g8c) : sans porteur depuis deux paliers » — 2026-09-12

VERIFIE: l'attendu du ticket (chaque advisory instruite — mise à jour si un correctif existe, sinon entrée au stock avec sa raison datée et son horizon —, `audit-stock` vert, le fait `auditStock` de la revue de palier disponible) confronté au diff du worktree `.wt-gates-A` sur `2945e9a60` — `sharp` 0.34.5 (transitif dans `server/`, épinglé à l'exact par `wrangler@4.100.0 → miniflare@4.20260611.0`), GHSA-rgj7-g3m4-5g8c haute (`<0.35.4`, publiée le 2026-09-08) : le premier jet la classait au stock (« aucune montée de sharp seul n'est possible » : `npm audit fix` rend ERESOLVE parce que wrangler ≥ 4.108 exige `@cloudflare/workers-types@^5`), affirmation RÉFUTÉE par le juge sur une copie hors arbre — un `overrides` npm surclasse l'épinglage exact transitif ; CORRIGÉ : `server/package.json` porte `"overrides": { "sharp": "0.35.4" }`, `server/package-lock.json` régénéré depuis `server/` avec npm 10.9.3 (`install --package-lock-only --ignore-scripts`, version collée ; seuls sharp, ses `@img/sharp-*` et `@img/sharp-libvips-*`, `@emnapi/runtime` et `semver` changent — vérifié par sonde sur le lock ; un premier essai depuis la racine avait lié le dépôt parent comme dépendance, remis à l'octet depuis `HEAD` avant la régénération propre, lock final sans `..` ni `link`), `npm --prefix server ci` et `server:typecheck` verts ; l'advisory s'éteint et `wrangler` sort avec elle des ≥ high : la mesure nomme `server:sharp` et `server:wrangler` PÉRIMÉES, les deux entrées sont retirées, `server:miniflare.viaPaquets` recalé sur l'observé — le stock DÉCROÎT de 7 à 5, et le cliquet du test (`scripts/ops/audit-stock.test.mjs`) devient une ÉGALITÉ (`PLAFOND = 5`, un plafond au-dessus du compte est un cliquet mou : mutation plafond 7 → rouge nommé « à 5 entrées pour un compte déclaré de 7 ») ; la doctrine de l'override est écrite dans l'`_entete` du stock, lieu vivant de sa doctrine ; `vitest` 2.1.9 (direct, racine), GHSA-82fw-gwwq-j7x9 modérée (`>=2.1.0 <4.1.11`, publiée le 2026-09-08) : le seul correctif est au-delà de la frontière majeure (4.1.11, `npm audit` propose 5.0.0) → déclarée dans le `via` de l'entrée existante (la forme du stock — une entrée par paquet, verdict par advisory sur `via`, plafond dans le test — l'impose), plage observée `<=4.1.10`, motif daté (advisory publiée le 2026-09-08, entrée au stock le 2026-09-12), valeurs re-mesurées par les fonctions de production ; le ticket de CLASSE que les échéances citaient sans numéro depuis le 2026-09-04 est ouvert (#1726) et les cinq échéances le citent ; `node scripts/ops/audit-stock.mjs` exit 1 (deux ROUGE) avant, exit 0 après (« aucun écart au stock daté », 5 JAUNE) ; mutation (advisory retirée du `via`) → rouge nommé, remise à l'octet ; `audit-stock.test.mjs` 9/9, eslint 0, `faits-de-palier` → `auditStock.disponible: true` ; audit, test et `server:typecheck` rejoués par l'orchestratrice ; juge de diff : TIENT SOUS CORRECTIONS, la correction portée par la voie de la mise à jour.

## Restes
- La frontière majeure `@cloudflare/workers-types` 4 → 5 (miniflare, undici, ws) et `vite` 8 / `vitest` ≥ 4.1.11 -> #1726
- `server/package-lock.json` n'est couvert par aucune garde #528 (`pre-commit.mjs:215` est nominatif sur le lock racine) -> RAS : le lock server a été régénéré avec npm 10.9.3, version collée au rendu ; l'extension de la garde au lock `server/` est écrite au geste 1 de #1726 qui rouvrira ce lock
- L'override `sharp: 0.35.4` impose à l'émulateur local (`wrangler dev`) un binaire natif d'une ligne que miniflare 4.x n'épingle pas -> RAS : hors chemin du Worker déployé, `server:typecheck` vert, la voie et sa mesure sont écrites dans l'`_entete` du stock ; l'override tombe avec la montée de #1726
- Le fait `auditStock` de la revue de palier n'a pas été rejoué après le second geste -> RAS : il ne dépend que du code de sortie de `audit-stock.mjs`, rejoué à 0 par l'orchestratrice

## Réfutation
verdict: CONFIRMÉ — juge de diff (lecture seule, 2026-09-12) : claims re-mesurés (observation par les fonctions réelles du comparateur : `plage`, `severite`, `via`, `viaPaquets` identiques à `npm audit --json` ; frontière au registre confirmée : aucune wrangler sans peer workers-types 5 ne tire sharp ≥ 0.35.4 ; plafond mordant ; #1726 porteur) ; UN BLOQUANT — le stock affirmait une impossibilité que la sonde du juge réfute à l'octet (`overrides: { sharp: "0.35.4" }` résout sous npm 10.9.3 et éteint l'advisory) — porté non par une raison de refus mais par la MISE À JOUR elle-même, conformément à l'attendu du ticket ; souhaitables portés (dates d'advisory et d'entrée distinguées). Non joué par le juge : les gates — jouées sur la tête du lot avant le push.
