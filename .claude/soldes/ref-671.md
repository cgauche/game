# ref-671 — Lot A (mécanisme presets PNJ)

## Juge
Juge adversarial 7 lentilles sur le diff du mécanisme (merge base+surcharges, routage spawn `campaignData`-free, cross-ref au parse, portrait de dialogue par preset, double garde authoring, test de câblage, cycle d'import) : toutes RÉEL / confirmées. Le seul flanc FRAGILE (`validateNarratif` ne gardait pas la branche base-absente → un profil ad hoc partiel produisait un TypeError au spawn) a été CORRIGÉ dans le MÊME lot (garde profil-sans-base ⇒ `char`+`traits` requis, + 2 cas de test), avant ce commit.

## Réfutation
verdict: CONFIRMÉ
Réfutation adversariale confirmée sur pièces : `spawn.ts` n'importe PAS le store/`campaignData` (contre-grep des imports) → moteur pur préservé (règle 3) ; `mergeCreatureProfile` ne mute jamais la base globale partagée (reconstruction par spread) ; `validateScenePresetRefs` couvre les entités compilées ET les encounters terse (aucun presetId mort n'échappe au parse) ; test de câblage genuine par le chemin store réel `startCombat→spawnEnemy` (base `brigand` CC 40→99 surchargée + label du preset ; « échoue sans la clé » sans la couche chargée). Gates relancés par l'orchestrateur : typecheck complet 0 erreur (hors `_tmp` d'autres sessions), suite complète 12861 verte.
