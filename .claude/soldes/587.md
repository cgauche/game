# Solde #587 — Hook solde-ticket-guard : lectures git dans le dépôt du COMMIT (2026-07-17)

VERIFIE: le fix vit dans le driver du hook (fonction PURE exportée extractTargetDir(command, cwd) — premier `cd <path> && … git commit` du pipeline, chemins quotés simples/doubles et relatifs résolus, motif `git -C <path>`, défaut = cwd inchangé) ; readStagedManifestFile et readStagedDiffStat prennent désormais un dir passé en cwd d'execSync — les DEUX seules lectures d'état git du fichier (audit complet rendu : aucune autre exec git ni lecture relative au commit) ; les lectures de soldes/.claude restent côté SESSION par design (distinction commentée en réf nue #587) ; 7 cas de test ajoutés sur extractTargetDir (cd relatif, sans cd, commande vide, quotes avec espaces, git -C, absolu) ; suite hooks 94/94 verte, lint 0 ; suite complète du worktree verte (11 858 / 0).

## Restes
- Le hook ACTIF de la session courante est chargé depuis l'arbre PRINCIPAL — le fix ne prend effet qu'au pull de la session parallèle (ou au prochain redémarrage sur un arbre à jour) ; les fermetures de CE chantier passent encore par gh + hash si l'ancienne version refuse -> RAS : limite de déploiement inhérente aux hooks fichiers, constatée et contournée par le canal de fermeture documenté (gh + hash + solde), le fix est committé/poussé pour toutes les sessions suivantes.

## Réfutation
verdict: CONFIRMÉ
Le bug avait été DÉMONTRÉ empiriquement avant le ticket (manifest stagé du worktree propre — 0 entrée — pendant que le même `git show :` depuis le cwd de session rendait 13 entrées de l'arbre principal) ; le fix cible exactement les deux lectures fautives, prouvé par tests injectés (le cwd passé à l'exec est vérifié par cas), sans toucher au design volontaire des lectures de soldes côté session. Aucune autre lecture d'état git n'existe dans le fichier (audit).
