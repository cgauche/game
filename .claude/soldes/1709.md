# Solde #1709 « Gates locales avant push : 23-27 min contre 10 min en CI — la facture est dans les TESTS (doublons de gate, fixtures hors de proportion, scans de src/ réécrits, tests de processus rejoués à chaque push) » — 2026-09-12

VERIFIE: le DoD de l'épic (lots A doublons de gate, B1 fixtures de `test:hooks`, B2 fixtures de la suite, C corpus partagé et fusions, D cardinaux vivants et gates menteuses, E placement, F build sans `tsc -b`, plus l'invariant « régime des 22 gates avant push INCHANGÉ, jamais un cardinal vivant ni un magasin partagé ») confronté MOT À MOT à l'arbre `32a078c21` (= `origin/main`, CI verte le 2026-09-11T14:23Z) par DEUX réfutations de fermeture à un juge chacune : la première (arbre `b84ea74ff`, 2026-09-11) a rendu RÉFUTÉ sur huit clauses (sept items de B2/C/E sans exécution ni refus tracé, #1717 vivant) ; depuis, B4 `e1ae5618c` a joué les cinq restes de B2 (durées d'`it` 85,8 → 43,3 s sur six fichiers, 222 tests inchangés), F2 `32a078c21` a nommé et reproduit la cause de #1717 et l'a verrouillée par construction (garde du seam d'écriture en passe AST à 48 résolveurs DÉRIVÉS du source — mesuré à la tête, le message de F2 dit 32 —, six sites par la porte `setDataset`), et les trois refus de E/C sont MESURÉS et tracés (ordre de coût sur #1708 ; huit gardes de discipline et fusions `menace-fk`/CSS au pilotage v20) ; la seconde réfutation (arbre `32a078c21`, 2026-09-12, brief rejoué tel quel) a rendu RÉFUTÉ sur UNE clause neuve — le geste 2 de #1708, muet — LEVÉE dans ce commit (`scripts/gates/toutes.mjs:845`, champ `prerequis` de `ECRIT_LU`, juge de diff TIENT SOUS CORRECTIONS sans bloquant, corrections portées, 29/29 verts rejoués sur l'arbre principal), les huit autres étant TENUES sur pièces. Mesure de référence FINALE (`npm run gates -- --serie --tout`, 22 gates, machine calme : 14,3 Gio libres, aucune autre suite, log `gates-final.log`) : 479,8 s contre 1 633 s au diagnostic du 2026-09-06 (−71 %), gate par gate : `test:hooks` 564 → 36,1 s, `test` 438 → 155,3 s, `build` 136 → 21,1 s, `typecheck` 107 → 81,8 s, `docs:check` 105 → 53,6 s, `lint` 80 → 69,4 s, `deps:unused` 43 → 14,9 s — le local est passé SOUS la CI (≈ 10,5 min) ; régime inchangé (CLAUDE.md § Commandes), aucun cache ajouté. Les gates locales du commit de fermeture jouées sur l'arbre principal avant le push.

## Restes
- E — ordre de coût en `--serie` (#1708, absorbé par E) -> RAS : refus MESURÉ posté sur #1708 le 2026-09-11, chiffres re-vérifiés par le juge du 2026-09-12 (somme invariante en série, mur des lanes `test` 155,3 s vs `typecheck`+`lint` 151,2 s, ordre croissant retarderait le rouge, résumé comparable à `ci.yml`) ; #1708 fermé par ce commit avec son solde
- E — geste 2 de #1708 (`server:typecheck` muet sur `server/node_modules`), clause bloquante de la seconde réfutation -> corrigé dans ce commit scripts/gates/toutes.mjs:845
- E — les 8 gardes de discipline et `test:recette` sortent de la suite de push -> RAS : refus MESURÉS — gardes : 37,06 s cumulés dans le worker au run de référence, gain ≤ 4 s de mur en lanes, contre la couverture de l'ARBRE que le pre-commit (scan du STAGÉ seul, `scripts/git-hooks/pre-commit.mjs:85,115`) ne donne pas (pilotage v20) ; `test:recette` : clé nominative « outillage » refusée par le train E `1e7f4f830` (quatre gates éligibles = 10,8 s dans une lane de 198 s, espérance 4 s sur 1 020)
- C — `menace-fk` et les 4 gardes CSS fondues -> RAS : refus MESURÉ (pilotage v20, chiffres re-vérifiés sur les durées d'`it` : 609 + 153 + 217 + 31 + 18 ms), invariants DISJOINTS, même verdict que le juge de design C3b sur les quatre autres fusions
- E — clé de justificatif dérivée de `ECRIT_LU[gate].lit` -> RAS : design E RÉFUTÉ par un juge (faux vert : 9 gates sur 22 lisent hors `lit`), alternative minimale livrée par le train E `1e7f4f830` (`test`/`test:ops`/`deps:unused` sous clé complète, `ECRIT_LU` re-mesuré)
- B2 — `worldTris.test.ts` reste 9 s après mémo -> RAS : le coût restant est l'ORACLE O(n²) et la fonction jugée (STOP mesuré au message de `e1ae5618c`, dit au JSDoc `worldTris.test.ts:64`) ; le toucher changerait ce que le contrat prouve
- B2 — `structures-contrat.test.ts` garde une sonde à spawn séparé (`diff-strate-document`) -> RAS : pilote DIFFÉRENT qui ampute `lexique.CONCEPTS` en mémoire (`PILOTE_DIFF_STRATE:1616-1624`, vérifié au code par le juge) ; la batcher couplerait deux contrats par l'ordre (STOP mesuré, 1,7 s, `e1ae5618c`)
- INVARIANT — seconde occurrence de #1717 (`skills-screen-attention`, React « Should not already be working » sous `act()` au montage, sous charge) -> RAS : tranché par le juge du 2026-09-12 sur pièces — ce n'est PAS un magasin partagé (la cause de #1717 est nommée, reproduite et corrigée ; hypothèse distincte : racine react-dom / `act()` en vol sous `isolate: false`, sonde écrite sur #1724, ticket ADJACENT hors clause)

## Réfutation
verdict: CONFIRMÉ — deux réfutations de fermeture à UN juge chacune, lecture seule, clause par clause du DoD. Première (arbre `b84ea74ff`, 2026-09-11) : RÉFUTÉ, huit clauses non tenues (sept items de B2/C/E sans exécution ni refus tracé, #1717 vivant). Seconde (arbre `32a078c21`, 2026-09-12, brief rejoué tel quel, 6 min) : les huit clauses TENUES — cinq par le code de B4/F2 (`de-monde-surface.test.ts:939-953` profondeur de pile constante mesurée, `disease-noeud-test.test.ts:199-206`, `sceneCutaway.test.ts:20,38` + `worldTris.test.ts:52,69-81` mémoïsés, `structures-contrat.test.ts:2107` un pilote), trois par des refus mesurés dont les chiffres ont été re-vérifiés au log de référence (`gates-final.log:73` 479,8 s ; mur `test` 155,3 s vs `typecheck`+`lint` 151,2 s ; 8 gardes = 37,06 s recalculés ; `pre-commit.mjs:85,115` scanne le STAGÉ seul) ; le verrou de #1717 VIVANT (sonde adversariale : cinq formes de mutation d'entrée vive rougissent, trois témoins négatifs muets, `seam-ecriture-guard.test.ts` 6/6 verts) ; RÉFUTÉ sur UNE clause neuve bloquante — le geste 2 de #1708 (« `server:typecheck` : dire dans son refus que `server/node_modules` manque ») n'était ni au code ni refusé — LEVÉE dans ce commit : prérequis déclaré par gate dans `ECRIT_LU`, pilote qui refuse sans spawner, quatre tests node --test, mutation rejouée rouge, juge de DIFF (lecture seule, 2026-09-12) TIENT SOUS CORRECTIONS sans bloquant, quatre souhaitables portés et rejoués. Non bloquant relevé : le message de F2 dit « 32 résolveurs dérivés », la mesure à la tête en rend 48 — le chiffre juste est 48. Tickets classés : #1708 reste du DoD (fermé par ce commit) ; #1711, #1718, #1719, #1720, #1721, #1723, #1724, #1725 ADJACENTS (#1724 n'est pas un magasin partagé : hypothèse distincte, sonde au ticket). Non joué par les juges : la suite complète et les gates — jouées sur l'arbre principal avant le push de ce commit.
