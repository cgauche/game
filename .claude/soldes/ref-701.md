# ref-701 — Lot 1 : Effects moveEntity + playSfx (mise en scène scriptée)

## Juge
Juge moteur adversarial (6 lentilles) — verdict CONFIRMÉ. Vérifié : AUCUN artefact d'édition node (les 2 variantes de l'union `Effect` et les 2 handlers du registre `EFFECT_HANDLERS` syntaxiquement complets, commentaires entiers, imports `removeEntity`/`playSfx` propres) ; `moveEntity` correct (no-op propre si scène/entité absente, repositionnement IMMUABLE, `to` PUIS `remove` via le point unique `removeEntity`, les 3 cas testés) ; `playSfx` appelle `playSfx(e.id)`, coop hôte-autoritaire DÉCLARÉE (commentaire nu, pas une excuse), id inconnu sans crash ; `effectSummary` 3 branches FR correctes, icônes existantes (`travel/foot`/`audio/volume`) ; test 9/9 avec nettoyage isolate:false CORRECT (reset `useGame` en afterEach + restauration `Audio`, aucun `vi.mock`/`vi.spyOn`) ; zéro poison, `anim` non détourné.

## Juge-Vision
Aucun ÉCRAN rendu n'est modifié par ce lot. Le `src/ui` touché est de la LOGIQUE non-visuelle : `EffectList.tsx` (2 `case` de `effectSummary` — texte de résumé humain des 2 nouveaux Effects, chemin de chaîne, icônes DÉRIVÉES du registre). Pas de surface visuelle à juger sur captures ; couvert par le juge moteur (effectSummary correct/non-vide) et la suite. L'éditeur d'Effects (GameOpEditor/FlowEditor) expose les 2 kinds via le registre existant, sans nouveau composant.

## Réfutation
verdict: CONFIRMÉ
Réfutation adversariale confirmée : les 2 Effects (`moveEntity` repositionne/retire une entité par le point unique `removeEntity`, capturant décor consommé ET mort combat ; `playSfx` son ponctuel branché sur `audio/engine`) sont data-driven (registre `EFFECT_HANDLERS`, aucun god-switch), immuables, et sans artefact malgré l'édition contrainte via scripts node (deny-rule Read/Edit). Le test est isolate:false-propre (le flake qui a retardé ce commit était #777, pré-existant et désormais résolu — plusieurs runs complets verts). Gates relancés par l'orchestrateur : typecheck 0 imputable, suite complète verte. Ce lot livre le MOTEUR ; le scénario démo (figurant qui fuit / cloche) + la carte des Effects régénérée + la recette ferment #701 (Lot 2).

2026-07-23
