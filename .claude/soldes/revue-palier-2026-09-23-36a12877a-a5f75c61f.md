# Revue de palier — fenêtre 36a12877a..a5f75c61f — 2026-09-23

verdict: PARTIEL

Épinglage :
- `git rev-parse a5f75c61f` = `origin/main` = `a5f75c61f3e2de23…`.
- `merge-base --is-ancestor 36a12877a a5f75c61f` → 0 ; `a5f75c61f HEAD` → 0.
- Worktree `.wt-1739-onglets` à `e069e7e8e`, un commit hors fenêtre. Il diffère de la tête sur 7 fichiers ; aucun n'est lu depuis le disque. `git diff --quiet a5f75c61f HEAD` rend 0 sur `_lib.mjs`, `pdfHorsCouture.mjs`, `stocksNominatifs.mjs` et `books.json`.

Contrôle positif :
- `scripts/raw/pdf-de.mjs` et `scripts/guards/lib/pdfHorsCouture.mjs` sont présents (A dans `6c764b041`).
- `.claude/soldes/1317.md` est dans l'index de `42ff9b2b4`.
- `PLAFOND_OCTETS` 26958 → 26951.
- 55 captures ajoutées sous `public/qc/soldes` ; les 10 citées par un `JUGE-VISION:` sont présentes.
- La fenêtre compte 12 commits, dont 9 de substance.

**L1 — CI sur `main`.** 3 pushes, 3 courses « CI », toutes `run_attempt 1 completed success` : `209290c36` (qui porte `3bbecd03f..42ff9b2b4`), `a2e6cf29b` et `a5f75c61f` (qui porte `6c764b041`). Aucun rouge sur `main`.

**L2 — Annotations** :
- `JUGE:` + `REFUTATION:` : 8/9. Manque `a2e6cf29b`, qui n'a ni l'un ni l'autre.
- `JUGE-VISION:` : 7/7 sur les commits qui touchent `src/ui` ou `src/gameIso` hors tests. Le grief F est soldé.
- `Claude-Session` : 0/9.
- `CLIQUET:` : 5 lignes rejouées.
  - 3 sont exactes au lecteur de la porte : `pdfHorsCouture` 0→14, `ecrivainsAtteints` 139→140→141.
  - Les 2 lignes « +125 » de `90b9aa6c0` sont en ÉCART au lecteur (348→348 et 1076→1076). Elles sont exactes en occurrences : `props.json | primitives` 172→297.
  - `34a974a34` monte des occurrences de +1 et +2 sans ligne `CLIQUET:`.

**L3 — Fermetures.**
- #1317 : fermée par `github-actions[bot]` le 2026-09-23T08:21, avec son solde dans l'index. Ses 3 restes : 2 `RAS`, 1 routé → #1343, qui est OPEN.
- Fermetures hors commit : aucune.
- Restant ouverts comme cité : #877, #1588, #1882, #1884, #1883, #1739, #1887, #1891, #1769.

**L4 — Griefs hérités.**

| Grief | État | Mesure |
|---|---|---|
| A — cardinaux | TENU, et RÉCIDIVE DE CLASSE | `structures-contrat.test.ts:1699` `toBe(2280)` et `:1711` `toBe(194)` inchangés (#1889 OPEN). Voir grief I. |
| B — Source/ hors substance | TENU | `revuePalier.mjs:168` `['src','scripts']` (#1890 OPEN). Voir grief H. |
| C — stock CSS | NEUTRE | 0 fichier `.css` touché dans la fenêtre. |
| 3 — jetons à espace | TENU | 24 entrées au littéral, 21 lues (#1840 OPEN), alors que `209290c36` touche `stocksNominatifs.mjs`. |
| 5 — `combatManeuvers.ts:440` | TENU, 4ᵉ palier | Ligne intacte (#1817 OPEN). |
| 6 — réserves d'outillage | TENU | `revuePalier.mjs:118` : `--diff-filter=A`. `git grep run_attempt -- scripts/ops/` → exit 1. |
| D — `Claude-Session` | AGGRAVÉ | 14/32 → 0/9 (#1750 OPEN). |
| G — dépendances | RÉCIDIVE | #1880 ne porte toujours aucune dépendance. #1891, née de `6c764b041`, non plus. En regard, #1889 et #1890 en portent. |

**L5 — Substance et prémisses.**
- **#1739, couture `pdfDe`.**
  - `pdfDe` résout 17/17 livres ; `sortieMarkerDe` échoue sur 17/17.
  - Dans l'arbre principal, `_marker/full/` ne contient que `au1`, `les Vents de Magie`, `sondes-ldb` et `wfrp5`. Les renommages « laissés à l'utilisateur » ne sont pas faits : les découpeurs sont à l'arrêt, ce qui est consigné à #1739 (comment 5792103077:32-39).
  - Tests : `node --test` sur pdf-de, pdfHorsCouture et le banc 877 → 22/22, exit 0.
- **#1591, consignes shell.** La prémisse tient : l'outil PowerShell a été bloqué deux fois par l'allowlist lean-ctx. Mais la consigne retirée a quand même été servie : l'arbre principal est sur `main@3113daad1`, et `merge-base --is-ancestor a2e6cf29b HEAD` y rend 1. Son `juge.md` porte encore « PowerShell pour TOUT ».
- **#877, #1588, #1882, porte de projet.** Aucun des 11 documents de sonde n'a été rejoué. Rien de réfuté ; c'est non couvert, pas établi.
- **#1343, décor.** La dette de signature `divergente` monte de +128 occurrences dans des stocks dont la porte ne compte que les entrées (grief I).

**Ce qui empêche CONFIRMÉ :**

**H — `a2e6cf29b` réécrit les consignes de tous les agents sans aucun `JUGE:` / `REFUTATION:`.** C'est de la substance au sens de l'instrument (`scripts/guards/budget-contexte.mjs`), mais la porte ne la voit pas : `solde-ticket-guard.mjs:1787` pose `touchesSrc: /^src\//`, alors que `revuePalier.mjs:168` inclut `scripts`. Deux définitions de la substance coexistent.
→ Correction exacte : `touchesSrc` dérive de `estCheminDeSubstance`, déjà importé en `:72`. Commentaire sur #1890 (même classe). **MOYEN.**

**I — Dette divergente +125 (`90b9aa6c0`) et +3 (`34a974a34`), invisible à la porte, et journal de recalage en commentaire.**
- Stocks : `slotsStock.mjs` et `structuresStock.mjs`, ligne `props.json | primitives`, avec le commentaire « 89 → 172 … ; 172 → 297 (#1343 lot B, 2026-09-21) ».
- Même chose aux lignes `barge-du-sel` et `loup-et-saumure` : « 5→6 », « 10→12 ».
- C'est la classe A (pierre tombale datée), récidivée dans les stocks.
→ Dette : #1463 et #1473 OPEN. Journal : commentaire sur #1889. **MOYEN.**

**J — « DEUX MAISONS, UNE DÉFINITION ».**
- `src/data/schemas/grammaire/livres-extraits.ts:10-12` et `_lib.mjs:45` sont deux définitions tenues égales par un test : c'est une garde de synchronisation, que le credo appelle un smell.
- `estExtrait` lit le `books.json` statique. `DescRefField.tsx:147` filtre désormais les `books` vivants (éditables) par ce prédicat statique.
→ Ticket à ouvrir après dédup par label. **MOYEN.**

**K — Forme de `6c764b041`.**
- Le corps commence au milieu de la citation du bloquant (« chemins en dur, `--pdf`…) ») : la citation est mutilée.
- Les captures « 04/05/07 » de son `JUGE-VISION:` sont absentes du dépôt : la vision n'est pas vérifiable.
- Le `--pdf` explicite survit dans `marker-split.mjs:24-34` et `:69` comme SURCHARGE de la couture, alors que le bloquant listait `--pdf` parmi les localisations ad hoc.

**L — `pdfHorsCouture` est lexicale.** 4 formes passent sur 7 (extension en variable dans un gabarit JS, `['p','d','f'].join('')`, glob `*.[pP][dD][fF]`, f-string `{EXT}`). La garde elle-même construit l'extension par `String.fromCharCode` (`:15`).
→ Commentaire sur #1739. **MINEUR.**

**Non couvert :**
- Suites, typecheck et gates non rejoués au-delà des 22 tests ci-dessus.
- Mutations annoncées non rejouées.
- Aucune capture ouverte : le « BON » visuel n'est pas prononcé.
- RAW non rouvert.
