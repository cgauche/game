# Solde A POSTERIORI #1659 — 2026-09-04 (L2 T2, programme #1679)

Ce ticket a été fermé À LA MAIN sur GitHub le 2026-09-02T04:50:41Z par `cgauche` (`state_reason: completed`, `closed_by.login = cgauche`, `commit_id` NUL sur l'événement `closed` — mesuré par `gh api repos/cgauche/game/issues/1659`), donc SANS solde et hors de tout commit fermant : ni le garde PreToolUse, ni le cliquet `fermetures-sans-solde.test.mjs` (qui ne lit que les messages de commit) ne pouvaient le voir. Ce solde est écrit APRÈS coup, point de DoD par point de DoD, contre l'arbre à HEAD.

VERIFIE: les trois points du « À faire » du ticket, confrontés à l'arbre du worktree `.wt-1679-L2` (HEAD `da3acf95c`). **(1) « Déclarer l'angle mort dans `ANGLES_MORTS` et dans l'en-tête de `plage-bornes-contrat.test.ts` ; promouvoir l'inventaire des tuples en stock DÉCROISSANT (99, nominatif par dataset/champ) » : PROUVÉ.** `scripts/docs/lib/structures-lexique.mts:116` porte l'entrée d'angle mort qui nomme la forme (« Une paire de bornes encodée en TUPLE `[min,max]` n'est mesurée par AUCUN concept ») et sa mesure datée (16 tuples sur 8 sites, contre 99 sur 18 à l'ouverture) ; `src/data/plage-bornes-contrat.test.ts:16-24` porte la section « COUVERTURE RÉELLE, et ses deux bords (#1659, 2026-09-01) » qui dit ce que le fichier ne mesurait pas, et son volet F (`:10-12`) est l'inventaire ; le stock nominatif vit à `src/data/plage-bornes-contrat.test.ts:157` (`TUPLES_STOCK`, site → compte + raison d'exclusion), confronté à l'observé à `:224-228` et plafonné à `:232` (`toBeLessThanOrEqual(16)`, message « stock des tuples en HAUSSE : la liste ne fait que décroître (#1659) »). **(2) « Migrer les 72 `avail` (et les 27 autres tuples) vers `plageSchema` `{min, max}`, cardinal asserté » : PROUVÉ.** `src/data/schemas/grammaire/valeurs.ts:631` définit `dispoSaisonniereSchema = parSaison(plageSchema)` — une seule forme de fourchette ; `src/data/schemas/defs/sea-cargo.ts:29` et `src/data/schemas/defs/land-cargo.ts:51` déclarent `avail: dispoSaisonniereSchema` (le `seasonRange = z.tuple(...)` que le ticket citait n'existe plus dans ces deux defs) ; la DONNÉE suit — `src/data/sea-cargo.json` porte `"avail": { "printemps": { "min": 1, "max": 5 }, … }`. Le commit porteur est `fa678e5e4` (« la DISPONIBILITÉ SAISONNIÈRE des cargaisons (72 cellules, sea + land) cesse d'être un tuple `[min,max]` … couverture 1–100 de chaque colonne GARDÉE par un refine nominatif »). Les « 27 autres » ont été inventoriés et traités ou EXCLUS nommément : 7 `ship-construction.json › standard[].lengthM` et 4 `stars.json › [].sub` migrés (lot L-1659-3), les 16 restants ne sont PAS des plages et le disent site par site (14 paramètres de recette de rendu, `qualities.json › capabilities.fumbleDigits` = un ensemble de chiffres, `structureAppearance.json › door.herse.traverseFracs` = des positions fractionnaires) — `structures-lexique.mts:116`. **(3) « `cargo.ts:78` passe à `findTableEntry` — plus de repli silencieux » : PROUVÉ.** `src/engine/cargo.ts:82` lit `findTableEntryIndex(cargoes.map((c) => c.avail[season]), r)` et `src/engine/cargo.ts:83-87` LÈVE une erreur nommée quand aucune plage ne couvre le jet (« le jet ${r} ne tombe dans aucune plage de disponibilité de la saison … la colonne ne couvre pas le d100 ») — le `?? cargoes[cargoes.length-1]` que le ticket citait a disparu, et la JSDoc `src/engine/cargo.ts:76-79` nomme le refine qui garde la couverture 1–100 au parse.

## Restes
- Le ticket a été fermé sans solde ni commit fermant : c'est la classe que ce train ferme, et #1659 en est l'un des deux cas fondateurs -> corrigé dans ce commit (scripts/ops/fermetures-non-citees.mjs:41)
- Réouverture : AUCUNE demandée — les 3 points du DoD sont prouvés à HEAD en `fichier:ligne`, aucun n'est « NON PROUVÉ » -> RAS : la fermeture était juste sur le fond ; seule sa FORME (hors commit, sans solde) était en défaut, et ce solde a posteriori la répare
- Les 16 tuples restants ne décroissent plus (ils ne sont pas des plages) : le cliquet les tient à « ne pas repousser » plutôt qu'à « décroître » -> RAS : dit à `src/data/plage-bornes-contrat.test.ts:145` et dans l'angle mort du lexique, avec l'exclusion NOMMÉE de chacun
- HORS du DoD de #1659, non re-mesuré par ce solde : (a) la fidélité au RAW MDG 15 des 72 cellules migrées (revendiquée par le message de `fa678e5e4`, non re-confrontée) ; (b) le cardinal « 99 à l'ouverture » (repris du ticket) ; (c) l'existence d'un autre lecteur que `rollSeasonalCargo` portant le même repli silencieux -> RAS : aucun des trois n'est une clause du « À faire » du ticket ; (a) relève du catalogue `cargoes` (#1684, ouvert, épic #1463), (b) et (c) sont des mesures d'audit, pas des livrables de #1659

## Réfutation
verdict: CONFIRMÉ — juge de diff L2 T2 (2026-09-04), sur pièces : les trois points du « À faire » vérifiés un par un à HEAD (`structures-lexique.mts:116` + `plage-bornes-contrat.test.ts:15-20,157,223-232` ; `valeurs.ts:631` + `sea-cargo.ts:29` + `land-cargo.ts:51`, aucun `z.tuple` résiduel, 72 = 44+28 et 27 = 11 migrés + 16 exclus nommément ; `cargo.ts:82-87` lève une erreur nommée, le repli a disparu). Ce solde est écrit deux jours APRÈS la fermeture par une session qui n'a pas fait le travail : son autorité est la lecture de l'arbre, chaque point cite son `fichier:ligne`. Ce qu'il ne prouve pas est HORS DoD et porté aux Restes.
