# Solde #1715 — 2026-09-09

VERIFIE: l'attendu du ticket confronté à l'arbre — (1) les 7 types de bâtiment sont la DONNÉE `src/data/buildings.json` (document `entite`, `SANS_LIVRE` + `maison` par entrée, `roofMaterial: idDe('material','roof')`, ornements `ref('prop', { anchor })` à enum nommé), le registre TypeScript `catalog/buildings/defs/*.ts` et sa génération sont supprimés, la façade VIVE (`memoParVersion`) vit en `src/state/buildings.ts` (domicile de #1690), `ArchitectureBody.style` est une référence typée `idDe('building')` optionnelle (deux corps composites d'arène sans type), Codex « Bâtiments » sous Monde (parité Terrains, édition au Compendium) ; la garde `matieres-en-donnee.test.ts` couvre les couches ÉMETTRICES (`builders/**`, `authoring/*Svg.ts`, `src/state/**`) avec neutralisation STRUCTURELLE des homonymes (`part === '…'`, `: Terrain`, `scope:`), zéro exemption nominative — un scan naïf rendait 50 hits dont 43 faux positifs ; (2) `roofDefaults` TRANCHÉ en parité avec `reliefDefaults` (#1691) : `scene.roofDefaults` EXIGÉ `{material, pitchDeg (référence 5–75°), riseMaxStoreys}` (pas de `profile` : un profil non posé se choisit par PORTÉE, le socle résout), `ArchitectureBody.roofDefaults` = surcharge PARTIELLE, résolution UNIQUE `toitureEffective(scene, body)` corps > type de bâtiment > scène pour la matière, corps > scène pour la borne ; `DEFAULT_ROOF_DEFAULTS` n'est plus qu'une SEMENCE d'authoring de `scene.ts` (lue par `emptyScene` et la migration), les cinq replis en code sont morts, la masse posée à l'éditeur prend la toiture effective ; bump `SCHEMA_PROJET` 8 → 9 avec les trois artefacts (`PROJECT_MIGRATIONS[8]`, script daté `2026-09-09-1715-roof-defaults-scenes.mjs` à banc de portes câblé, parité `projet-migration-8-vers-9.test.ts`), 28 scènes des 4 paquets migrées, rejeu vert ; panneau de SCÈNE « Toiture par défaut » et panneau de CORPS qui affiche l'effectif et n'écrit que le champ touché (case vide sans mot) ; (3) `plafond` MORT assumé : 0 émetteur mesuré (vocabulaire des faces `cliff`/`ramp`/`deck`/`pilier`, le dessous d'un surplomb est une dalle `deck`), entrée purgée, cardinaux des trois migrations datées RECALÉS dans le train au régime de `11a` (précédent écrit dans `10c`) — régime local, rien à porter à #1463.

## Restes
- La SEMENCE d'une scène neuve (`emptyScene` : ambiance, mètres par case, lumière, terrain de remplissage, `DEFAULT_RELIEF_DEFAULTS`, `DEFAULT_ROOF_DEFAULTS`) et la plage de pente `[5,75]` de `validateScene` restent des littéraux de `src/state` : le défaut d'AUTHORING lui-même n'est pas éditable au Codex (doctrine « rien en dur, tout éditable » ; le document de projet n'a aucun slot de défauts partagés) -> #1716
- `RefField` ne sait pas adresser un tableau d'objets imbriqués (`buildings.json › features[].id` édité en champ texte générique à l'Atelier — même classe que `structures.json › traits[].id`, `vehicles.json › traits[].id`, ancres `src/ui/compendium/RefField.tsx:71-72`, `CodexEdit.tsx:858-863`), et la matière d'une fiche du groupe Monde s'affiche en texte nu sans lien codex (hérité de Terrains) -> inventaire #1689 : UNE primitive picker pour les références imbriquées et le renvoi codex des matières
- `src/state/sceneEdit.ts` importait `../gameIso/catalog/buildings` (premier import `state → gameIso` du dépôt, en-tête réécrit pour le tolérer) : la façade a déménagé en `src/state/buildings.ts` et une garde structurelle refuse désormais tout import de `src/gameIso` depuis `src/state` (règle 3) -> corrigé dans ce commit : src/state/frontiere-state-gameiso.test.ts:56
- Le commit 13450351a (volet a) avait laissé `src/comment-poison-guard.test.ts` rouge (revendication d'autorité sans citation dans un commentaire de `bundled-projects.test.ts`) -> corrigé dans ce commit : src/scenes/bundled-projects.test.ts:323
- `docs/superpowers/plans/2026-06-04-editeur-tilesets-batiments*.md` décrivent encore `defaultFoot` et des champs jamais nés — plans DATÉS exécutés -> RAS : hors `docs/` vivant, git porte l'historique

## Recette visuelle
capture: public/qc/1715-batiments/b-03-corps-suit-la-scene.png
capture: public/qc/1715-batiments/b-04-corps-type-forge.png

## Réfutation
verdict: CONFIRMÉ — grounding (lecteur, 30 min) : 7 littéraux purs, `roofDefaults` par corps à 0 usage livré, `plafond` sans émetteur ; vérificateur : faces de relief `cliff`/`ramp`/`deck`/`pilier` (0 émetteur de `plafond`), régime de rejeu à cardinal recalé dans le train ; juge de DESIGN sur l'invariant (25 min) : `ref()` avec `anchor`, `SANS_LIVRE` + `maison`, Codex dû, `styleRoofMaterial` mort, périmètre de garde structurel (43 faux positifs mesurés), semence d'authoring conservée comme #1691, pente de référence inerte sans le site `fittedPitchDeg` ; juge de DIFF (a) : COMMIT-ABLE APRÈS CORRECTIONS (hints faux → `defaultFoot` purgé sans lecteur, clause `maison` dupliquée, 3 exports morts, repli de libellé, fixtures fantômes, test sur paquets livrés) ; juge de DIFF (b) : NON COMMIT-ABLE TEL QUEL — frontière `state ↛ gameIso` relâchée (corrigée par déménagement + garde), garde des matières réduite à son allowlist (périmètre entier + neutralisation structurelle), JSDoc mentant sur la pente de référence, plage de pente absente du schéma, réducteur pur domicilié dans un composant — toutes corrigées et re-bancées ; recette navigateur en joueur sur les deux lots (captures ci-dessus), vision CONFORME.
