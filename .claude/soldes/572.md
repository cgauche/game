# Solde #572 — Pipeline ESPÈCE→TRAITS : le PJ ogre hérite de sa règle raciale (2026-07-17)

VERIFIE: `SpeciesData.traits?: TraitInstance[]` (schéma partagé traitInstanceSchema dédupliqué) appliqué à `createHero` → un PJ ogre porte le trait `ogre` (Encombrement/consommation ×2, ADE II 02 l.708) — Encombrement MAX (BF+BE)×2 VU EN RECETTE (3/18 pour BF5+BE4), taille Grande, `hero.traits` contient {ogre} ; TAILLE rendue DATA-DRIVEN (arbitrage user « GameOp passif ou matching ? ») : capability `TalentData.size` déclarée en donnée (massif→grande, petit→petite), dérivation générique `sizeFromTalents` — PLUS AUCUN id de talent hardcodé dans le moteur (contre-lu par l'orchestrateur : character.ts:383) ; `small` (doublon du talent petit) RETIRÉ (mort, 0 occurrence — contre-vérifié) ; rubrique Traits portée sur la Fiche complète (CharacterSheet/FicheBody) que le joueur consulte réellement ; poison `/ogre/i.test(sp.label)` supprimé (0 occurrence moteur/store) ; gate cumulé autoritatif relancé (952 fichiers / 11 903 tests, tsc 0, garde label-logic 4/4, docs:check OK) ; JUGE archi CONFIRMÉ ; après rebase sur la refonte « fiche v4 » (CharacterSheet compose désormais HeroSheet), la rubrique Traits a été portée sur la HeroSheet de fiche v4 (idiome déclaratif HERO_SHEET_SECTIONS + TraitChips, CharacterSheet:320 inclut 'traits'), preuve navigateur refaite (Ogre voit sa rubrique, humain non) ; JUGE VISION (orchestrateur, capture fiche v4 lue) BON — rubrique « Traits » visible entre dérivées et Compétences, chip « Ogre » stylé comme les chips Talents (libellé résolu), Encombrement 3/16 = (BF+BE)×2 visible, humain sans rubrique.

## Restes
- Chip décoratif « Taille Petite » du créateur (CharacterCreator.tsx) — migré de `sp.small` vers la détection du talent petit -> corrigé dans ce commit
- L'invariant « héros sans trait de créature » est désormais assoupli (un PJ ogre porte le trait ogre) -> RAS : le juge archi a réfuté tout leak de machinerie créature (le trait ogre est capabilities-only, aucun effects/passive/aura ; tous les chemins itérant c.traits filtrent par présence et le skippent) — feature scopée, pas de fuite.
- Outillage recette (chemin fiche complète, helper trait) -> #557

## Réfutation
verdict: CONFIRMÉ
Triple réfaction sous réfutation. (1) L'utilisateur a réfuté deux fois l'approche : d'abord « la taille c'est les talents Massif/Petit, pas un trait » (corrigé : dérivation par talent id, plus par label ni trait taille), puis « c'est du matching ou un GameOp passif ? » (corrigé : capability size déclarée en donnée, dérivation générique — plus aucun id nommé dans le moteur). (2) Le juge architecture a rendu CONFIRMÉ en réfutant le risque majeur (leak de machinerie créature vers un PJ — trait ogre capabilities-only, chemins filtrés) et a signalé la dette `small` (tuée dans le geste). (3) La RECETTE navigateur a réfuté la visibilité : le chip Traits était greffé sur `HeroSheet`, primitive JAMAIS montée dans le parcours joueur réel (Fiche complète/Présentation utilisent leur propre corps) → invisible pour un PJ ogre ; corrigé (rubrique portée sur CharacterSheet/FicheBody) et re-prouvé au navigateur (Ogre voit sa rubrique, humain non). La taille data-driven a été verrouillée par les tests préexistants (un premier jet « plus grande taille » cassait `petite < moyenne` → corrigé en « première non-défaut »).
