# Revue de palier — fenêtre 993107240..95248a08c — 2026-09-20

verdict: PARTIEL

Fenêtre `993107240..95248a08c` (41 commits, **22 de substance** au sens de l'instrument, **2 fermetures**) jugée en LECTURE SEULE, l'arbre épinglé AVANT toute mesure : `git rev-parse 95248a08c` → `95248a08cf530a15c978df1b0a529dd392347d2c`, `git merge-base --is-ancestor 993107240 95248a08c` → **exit 0** (chaînage confirmé). Le worktree d'exécution `C:/Users/gauch/PhpstormProjects/Foundry/Game/.wt-1806` porte `09880b160` — un commit **POSTÉRIEUR à la fenêtre** (`2d γ2`) : **aucune** mesure ci-dessous n'est prise sur le disque, toutes par `git show 95248a08c:<chemin>` / `git show 993107240:<chemin>`. Contrôle POSITIF que le travail jugé est là, pas une absence de marqueur : `src/ui/RewardRecap.tsx` et `src/ui/styles/reward-recap.css` **blob** ; `src/ui/styles/combat-modals.css` **n'existe plus** (`fatal: path … does not exist in '95248a08c'`) ; `scripts/raw/chapitres.json` **22 479 octets** ; `scripts/guards/lib/ticketsGh.mjs` exporte `DEPOT`, `appelGhRunner`, `pagesRest`, `lireTicket` ; `src/data/primitives.manifest.json` **93 entrées** ; les feuilles de `src/ui/styles` passent de **36 à 68**. **Le cumul est le plus productif des sept paliers sur les axes de livraison** : **16 courses CI sur `main` dans la fenêtre, 16 `success`, 0 relance** (`run_attempt = 1` partout) ; deux fermetures, deux soldes DANS L'INDEX du commit de fermeture, deux cibles de routage OPEN ; l'Atlas RAW cesse de nommer un livre (`BOOK_ORDER`, `SCENARIO_BOOKS`, `SECTION_LEVEL`, `PIVOT_ABBR`, `ldbRe` : **0 occurrence** dans `scripts/`) ; les quatre cardinaux vivants que le palier précédent dénonçait sont **ÉTEINTS** ; et le grief 6, porté par quatre revues consécutives, reçoit son geste au caractère (`scripts/gates/toutes.mjs:226`). Ce qui le fait basculer en PARTIEL est d'un autre ordre et c'est le grief de l'UTILISATEUR : **le stock CSS annoncé en baisse de 1215 sites ne correspond qu'à 71 déclarations réellement disparues du corpus** (6491 → 6420), **265 sites sortent du cliquet par une DÉCLARATION d'« organisme » sans que la matière bouge** (`combat-console.css` 655 → 644 décl.), **les feuilles EXEMPTES grossissent de +147 déclarations** (`base.css` 510→537, `components.css` 475→595), et **les `!important` ne bougent pas d'une unité** : 36 avant, 36 après, les 11 de `hud.css` simplement relogés dans deux modules neufs. S'y ajoutent trois annotations en RÉGRESSION nette (`JUGE:`/`REFUTATION:` 22/22 → **15/22**, `Claude-Session` → **11/22**), un `REPEINTS_STOCK` né à **26** pour un `CLIQUET:` qui en déclare **22**, et trois griefs hérités intacts au caractère près.

## Sortie brute de l'instrument

`npm run ops:faits-de-palier` — `C:\Users\gauch\AppData\Local\Temp\wfrp-faits-de-palier\faits-993107240-95248a08c.json`, digest sans valeur réécrite :

```
== base :: "993107240"   == tete :: "95248a08c"   == depuis :: "2026-09-18"   == chainage :: "vérifié"
== commits :: 41   (substance:true = 22)
== fermetures :: [ #1818 → 6afd6c7a2 (solde:true) · #1812 → ad6ba48e9 (solde:true) ]
== stocks :: {"refus":[],"notes":[],"commits":41,"plage":"993107240..95248a08c","indisponible":null}
== fermeturesHorsCommit :: { "disponible": true, "valeur":
     "[fermetures] fenêtre depuis 2026-09-18 · baseline 12 entrée(s)
      - aucune fermeture non citée dans la fenêtre
      **Aucun écart à la baseline.**" }
== auditStock :: "stock 5 entrée(s), observé 5 paquet(s) >= high … aucun écart au stock daté"
                 (les 5 mêmes JAUNES du 2026-09-04 : vite, vitest, miniflare, undici, ws — routées à #1726)
== coursesCi :: 41 shas photographiés → 16 portent une course "CI" completed/success, 25 → "courses":[]
== provenance :: commits/fermetures/stocks/chainage = script · fermeturesHorsCommit/coursesCi = gh
                 · auditStock = npm audit · revuePrecedente = git
```

Sondes d'annotation, mesurées commit par commit (`git log -1 --format=%B`, comptage `^JUGE:` / `^REFUTATION:` / `^JUGE-VISION:` / `^CLIQUET:` / `^Claude-Session:` sur les 22 `substance:true`) :

```
95248a08c - - -  G  fix(gardes) #1816 plafond budget recalé            cliquet=1
3dcf69f6b J R S  G  #1806 2d γ1 RewardRecap                            cliquet=1  VISION
d46e6ab01 J R -  G  #1825 lot E1a l'Atlas ne nomme plus un livre       cliquet=0
0645a8886 J R S  G  #1806 2d α+β quatre primitives                     cliquet=1  VISION
41aa406d5 J R S  G  #1806 2b console en organisme                      cliquet=0  VISION
e65606591 - - S  -  #1806 verrou DrBar exempte la vitrine              cliquet=0
9e6fffcdd J R S  G  #1806 2a identité du HUD                           cliquet=2  VISION
c623f5062 J R -  G  #1825 lot D dette de niveau FICHE                  cliquet=2
95726daa7 J R -  G  #1825 lot C champ coeur, PIVOT meurt               cliquet=0
9a0819852 - - -  G  #1825 lot B deux gardes rouges en CI               cliquet=0
f331ed800 J R -  G  #1825 lot B BOOK_ORDER meurt                       cliquet=1
d380aa72f J R S  -  #1813 ops:board par REST                           cliquet=1
c251f46af J R S  G  #1806 2c identité du JET                           cliquet=2  VISION
208733587 - - S  G  #1750 lane test déclare scripts/                   cliquet=0
cdb264449 J R -  G  #1825 lot A refRe                                  cliquet=1
a0c68642c J R S  G  #1813 UNE couture REST                             cliquet=4
28210fd5b - - -  -  #1820 gel des CLÉS re-gelé                         cliquet=0
cfa3bfbac J R -  G  #1820 Core Rulebook 5e citable                     cliquet=6
0b43420fb - - -  G  #1818 consignes vivantes nomment l'exception       cliquet=0
27c9f248a - - -  G  #1818 plafond relevé à la mesure                   cliquet=1
218a19735 J R S  G  #1811 #1815 plan daté ré-ancré                     cliquet=0
ad6ba48e9 J R S  G  corrige #1812 cardinaux vivants éteints            cliquet=1
```

**`JUGE:` ET `REFUTATION:` : 15/22 (68 %)** — trajectoire 6/9 → 10/11 → 10/10 → **15/22**. **`Claude-Session` : 11/22 (50 %)** — trajectoire 2/11 → 7/10 → **11/22**. **Trois commits de substance n'annoncent AUCUNE porte** (`e65606591`, `d380aa72f`, `28210fd5b`).

## Fermetures

| # | commit qui ferme | solde DANS L'INDEX du commit | restes routés | cible | verdict du solde | fermeture posée |
|---|---|---|---|---|---|---|
| #1818 | `6afd6c7a2` | **OUI** — `git diff-tree --name-only -r 6afd6c7a2` rend `.claude/soldes/1818.md`, SEUL fichier | 1 `inventaire #1816` + 12 lignes `RAS` | `-> inventaire #1816` **open** | PARTIEL (9 corrections du juge de fermeture, 8 prouvées au site, 1 écart assumé) | bot 2026-09-18T20:55:33Z |
| #1812 | `ad6ba48e9` | **OUI** — `.claude/soldes/1812.md` présent dans les 65 chemins de l'index | 1 (`MDG 12 l.122-129` absente de l'Atlas) | `-> #1814` **open** | PARTIEL (3 passes de juge, 5 majeurs soldés dans le commit) | bot 2026-09-18T19:25:31Z |

Sondes collées :

```
$ git diff-tree --no-commit-id --name-only -r 6afd6c7a29f0f2d3d7d33b047befbcace75b1895
.claude/soldes/1818.md

$ gh api repos/cgauche/game/issues/N --jq [.number,.state,.state_reason,.closed_at,closed_by]
1818  closed  completed  2026-09-18T20:55:33Z  github-actions[bot]
1812  closed  completed  2026-09-18T19:25:31Z  github-actions[bot]
1814  open    -          -                     -    [Atlas] Table des tailles de coque (MDG 12 l.122-129)…
1816  open    -          -                     -    ÉPIQUE — WFRP 5e : édition SÉLECTIONNABLE…

$ gh api search/issues?q=repo:cgauche/game+is:issue+closed:>=2026-09-18
  → 11 items, dont 9 fermés AVANT 993107240 (fenêtre précédente) ; les deux de CETTE fenêtre
    sont #1818 et #1812. AUCUNE fermeture sans commit.

$ instrument, fermeturesHorsCommit → « aucune fermeture non citée dans la fenêtre ».
```

**Les deux fermetures tiennent sur la forme et sur le fond** : solde suivi, `verdict:` nommé, **zéro `gh issue close`** (les deux posées par `github-actions[bot]`, `state_reason=completed`), reste unique par ticket, cible OPEN. Le reste de #1812 est réellement routé et VIVANT (#1814, ouvert, « Table des tailles de coque absente de `docs/raw/combat-naval.md` »). Le reste de #1818 passe par la porte de service `-> inventaire #N`, la même qu'aux deux paliers précédents — **elle n'a toujours pas de plafond** (grief 8 du 2026-09-18, réserve reconduite).

**Point mesuré en faveur du palier** : `ad6ba48e9` ne se contente pas de router son reste, il **éteint la classe** que le palier précédent dénonçait. Contre-grep à la tête :

```
$ git grep -n "TOTAL_ATTENDU|attendu: 66|attendu: 62" 95248a08c -- scripts/migrations   → AUCUNE LIGNE
$ git grep -c "entrees:" 95248a08c -- scripts/guards/lib/structuresStock.mjs            → AUCUNE LIGNE
$ git grep -n "primitives.manifest.json#0" 95248a08c -- src/ui/compendium/registry-enveloppe.test.ts
  459:  "primitives.manifest.json#0",        ← le cardinal « (62) » a disparu de la clé
```

## Stocks et cliquets

`refus: []`, `notes: []` sur 41 commits ; `auditStock` : 5 entrées, 5 paquets `>= high`, **aucun écart au stock daté** (mêmes cinq JAUNES du 2026-09-04, routées à #1726).

### Ce qui a bougé, et sous quelle ligne `CLIQUET:`

| porteur | base 993107240 | tête 95248a08c | ligne `CLIQUET:` ? |
|---|---|---|---|
| `scripts/guards/lib/cssCouchesStock.mjs` | 3349 sites (2339/910/100) | **2134** (1400/637/97) | n/a (BAISSE) |
| `scripts/guards/budget-contexte.mjs` `PLAFOND_OCTETS` | **26459** | **26807** (+348) | OUI ×3 (`27c9f248a +288`, `cfa3bfbac +2`, `95248a08c +58`) |
| `scripts/raw/source-tables-stock.json` | 5318 L | **6494 L** (+147 entrées) | OUI (`cfa3bfbac +147`) |
| `scripts/raw/source-format-stock.json` | 462 L | **470 L** (+1) | OUI (`cfa3bfbac +1`) |
| `scripts/raw/graphy-stock.json` | 422 L | **438 L** (+2) | OUI (`cdb264449 +2`) |
| `src/ui/ui-ratchets.test.ts` | 1379 L | **1599 L** | OUI (`9e6fffcdd +13`, `3dcf69f6b +2`) |
| `src/ui/css-modules-guard.test.ts` (`REPEINTS_STOCK`) | **ABSENT** | **24** | OUI mais **compte FAUX**, voir grief 3 |
| `src/ui/primitive-owners-guard.test.ts` (`POSEUR_ABSENT_STOCK`) | ABSENT | **13** | OUI (`c251f46af +13`) |
| `scripts/guards/lib/sitesDeFermeture.mjs` | ABSENT | 132 L | OUI (`a0c68642c +6`) |
| `scripts/guards/lib/modulesFeuilles.mjs` | ABSENT | 133 L | OUI (`a0c68642c +1`) |
| `.claude/memory/` | 147 fiches, 168 732 o | **150 fiches, 177 428 o** (+8696) | couvert par le budget de contexte |

Sur le CANAL, le palier est **exemplaire** : **13 des 22 commits de substance portent au moins une ligne `CLIQUET:`**, tous les porteurs qui naissent ou grandissent en portent une, et le dépôt a désormais une PORTE — `scripts/hooks/solde-ticket-guard.mjs:51` (« `evaluateStocksQuiGrandissent` stock nominatif qui naît ou grandit sans `CLIQUET:` au message »), avec le contrôle du compte (`scripts/hooks/stocks-nominatifs.test.mjs:513` : « un compte FAUX ou un motif de tampon ne couvre rien »). C'est l'attendu du grief 4 du palier précédent, **posé**.

### Le grief de l'utilisateur, instruit et chiffré

> « L'application c'est un dépot de stock decroissant. Surtout que c'est toi qui a mis ces !important il y a quelques jours » — utilisateur, 2026-09-20.

**(a) Les `!important` sont HORS fenêtre, et ils n'ont pas bougé d'une unité.** Le commit accusé est `74b952861` (`fix(ui): corrige #1792 — la boîte des cases/radios custom est immune aux règles de module (base.css)`) :

```
$ git merge-base --is-ancestor 74b952861 993107240   →  exit 0
     ← 74b952861 est un ANCÊTRE de la base : fenêtre PRÉCÉDENTE (9d66de88f..993107240), pas celle-ci.
$ git log -1 --oneline -S"!important" -- src/ui/styles/base.css
74b952861 fix(ui): corrige #1792 — la boîte des cases/radios custom est immune aux règles de module
$ git show 95248a08c:src/ui/styles/base.css | grep -c "!important"       → 11   (inchangé)
$ comptage sur TOUT src/ui/styles :
  993107240  TOTAL 36 | base.css:11 combat-console.css:7 combat-ui.css:6 editor.css:1 hud.css:11
  95248a08c  TOTAL 36 | base.css:11 combat-console.css:7 combat-ui.css:6 editor.css:1
                        initiative-strip.css:7 party-dock.css:4
```

Le total est **36 → 36**. Les 11 de `hud.css` n'ont pas été soldés : ils ont **déménagé** dans deux modules de primitive NEUFS (`initiative-strip.css` 7, `party-dock.css` 4). Et la garde qui les EXIGE est toujours là, à la tête :

```
src/ui/ui-ratchets.test.ts:1367
  it((xx) base.css : chaque propriété de boîte de la règle case/radio est `!important`, () => {
:1370  expect(manquantes, propriété(s) de boîte de la case/radio sans `!important` — une règle de
       module ou le `min-height` tactile la déformerait).toEqual([])
```

Les neuf déclarations vivent à `src/ui/styles/base.css:353-361`. **Ce n'est pas un grief de CETTE fenêtre** — c'est un grief que la revue du 2026-09-18, qui jugeait `74b952861`, n'a pas relevé : elle a lu le solde #1792 « PARTIEL » et compté son routage `-> #1796`, sans jamais mesurer qu'un lot d'assainissement CSS avait ajouté **9 `!important` et une garde qui les rend obligatoires**. Le grief de l'utilisateur porte : la classe est intacte, rien dans cette fenêtre ne l'a ni aggravée ni résorbée. **#1796 est OPEN.**

**(b) La décrue du stock CSS est, pour 94 %, un déménagement.** Mesure par sonde `git show` sur les DEUX arbres, jamais sur le disque :

```
stock cssCouchesStock.mjs : 3349 → 2134 sites        (−1215, ce que les messages annoncent)
corpus CSS réel (src/ui/styles + src/gameIso, fichiers .css) :
  993107240 : 37 feuilles · 13 681 lignes · 6491 déclarations
  95248a08c : 69 feuilles · 13 880 lignes · 6420 déclarations     (−71 déclarations, +199 lignes)
```

**Seules 71 déclarations ont cessé d'exister.** Les 1144 autres sites sortis du cliquet sont dans des périmètres que le cliquet ne mesure pas : 32 feuilles de primitive neuves, plus les feuilles EXEMPTES, qui GROSSISSENT :

```
base.css        510 → 537 déclarations   (+27)    ← « n a aucune entrée au stock »  (0645a8886)
components.css  475 → 595 déclarations   (+120)   ← « exempte du stock »            (3dcf69f6b)
                                          +147 déclarations hors de toute mesure
```

**(c) 265 sites quittent le cliquet par une DÉCLARATION, la matière ne bouge pas.** `41aa406d5` (sous-lot 2b) déclare `combatConsole` organisme au manifeste (`src/data/primitives.manifest.json:731`, `"nature": "organisme"`). Effet mesuré :

```
stock combat-console.css   : identité 214 → 0 · espacement 51 → 0      (265 sites)
matière combat-console.css : 1407 → 1402 lignes · 655 → 644 décl.      (−11)
REPEINTS_STOCK : les QUATRE entrées combat-console.css|… retirées 27 → 23, sans correctif
   "src/ui/styles/combat-console.css|.cc-phase .btn"
   "src/ui/styles/combat-console.css|.cc-arch .ptile.team-enemy"
   "src/ui/styles/combat-console.css|.cc-dock [data-medaillon] .ptile"
   "src/ui/styles/combat-console.css|.cc-dock [data-medaillon] .ptile-face"
```

**265 sites d'un cliquet et 4 d'un autre s'éteignent pour 11 déclarations réellement touchées.** À l'honneur du lot : **il le DIT, en toutes lettres et en tête de sa rubrique** — « déclarer l'organisme RECLASSE 265 sites […] il ne les solde pas […] Seules les 6 unités de `exploration-dock.css` absorbées par la peau sont réellement soldées ». Même honnêteté à `0645a8886` (« `base.css` n a aucune entrée au stock, cette part de la baisse est un transfert vers une feuille exempte, pas une primitivisation ») et à `3dcf69f6b` (« ces 2 unités d espacement sont un transfert, pas un solde »). **Le défaut n'est donc pas le mensonge : c'est que l'INSTRUMENT ne porte pas la distinction.** Le nombre qu'un palier lit — « identité 2339 → 1400 » — additionne du SOLDÉ, du PRIMITIVISÉ (la cure arbitrée A1) et du RECLASSÉ-par-déclaration, sans qu'aucun des trois ne soit séparable mécaniquement. Un dépôt de stock décroissant se prouve par un compte qui ne peut pas baisser par déménagement.

**(d) Stocks OUVERTS ou crûs dans la fenêtre, comptés.** 2 stocks NAISSENT (`REPEINTS_STOCK` 26 sites, `POSEUR_ABSENT_STOCK` 13) ; 5 GRANDISSENT (`source-tables-stock.json` +147, `source-format-stock.json` +1, `graphy-stock.json` +2, `ui-ratchets` +15, `budget-contexte` +348 octets) ; 2 naissent chez `a0c68642c` (`sitesDeFermeture` 6, `modulesFeuilles` 1). **Toutes ces hausses portent leur ligne `CLIQUET:` et une contrepartie nommée** — les 147 de `source-tables` sont l'inventaire d'un livre NEUF au corpus, le CRB, avec son ticket de tri (#1820, reste déclaré) ; les 13 de `POSEUR_ABSENT_STOCK` sont un stock de NAISSANCE routé à l'épic #1811. Une seule est FAUSSE au compte : grief 3.

## Griefs de la revue précédente — suivi, un par un

| grief du 2026-09-18 (`9d66de88f..993107240`) | état | mesure |
|---|---|---|
| **1 — `main` laissé avec un Canari ROUGE, plan daté de #1800 survivant à #1800** | **GESTE POSÉ, EFFET NON PROUVÉ** | `git show 95248a08c:docs/plans/2026-09-18-1800-couche-layout.md` ligne 3 rend **`Ticket: #1811`** (OPEN) : le plan est ré-ancré par `218a19735`, commit DÉDIÉ qui cite la revue. MAIS le Canari rend **`993107240 1 completed failure 2026-09-18T18:36:21Z`** puis **plus AUCUNE course** en deux jours, et **#1815 est TOUJOURS OPEN**. L'attendu « porte jouée AVANT la publication » n'est pas tenu : voir grief 2 |
| **2 — `docs/doctrines.md` datait du 2026-09-04 un arbitrage du 2026-09-18** | **RÉSORBÉ, cause générale corrigée** | `docs/doctrines.md:39` rend `**user-doctrine-ui-coherente-par-primitives-comme-les-donnees** (**2026-09-18**, 7 verbatims)`. Corrigé par `218a19735` avec son banc (`dateDe` lit la LIGNE du verbatim ; `build-doctrines.test.mjs` 21/21) |
| **3 — le grep donné POUR PREUVE par `fcaa6ef8d` (#1797) réfuté à deux sites** | **NON TENU** | `src/state/combatManeuvers.ts:440` rend toujours `const dur = Math.max(1, bonus(effectiveChar(attacker, 'endurance')))`, posé `rounds: dur` à la ligne 446, sous un commentaire (`:438`) qui dit « BE Rounds » sans réf de minimum. Intact au caractère. `src/engine/disease.ts:193` idem |
| **4 — quatre cliquets déplacés sans ligne `CLIQUET:`** | **RÉSORBÉ AU-DELÀ DE L'ATTENDU** | les quatre cardinaux sont **ÉTEINTS** (contre-greps au § Fermetures), et la porte demandée EXISTE (`solde-ticket-guard.mjs:51` ; `stocks-nominatifs.test.mjs:505,513`). Réserve : elle a laissé passer un compte faux (grief 3 de ce palier) |
| **5 — dette de synchro de #1800 absente de tout solde, #1812 sous-comptant** | **SOLDÉ SUR LE FOND** | #1812 fermée le 2026-09-18T19:25:31Z sur 41 migrations et une garde de PROPRIÉTÉ ; le titre du ticket dit encore « trois recalages » là où la revue en mesurait quatre — sans conséquence, le lot a traité les quatre |
| **6 — `ECRIT_LU['test'].lit` omet `scripts/`, 4ᵉ palier** | **RÉSORBÉ** | `scripts/gates/toutes.mjs:226` rend `lit: ['src/', 'server/src/', 'scripts/', 'docs/', 'Source/', '.gitattributes', 'vite.config.ts']`, avec sa raison citant la sonde (`:228-230`). Commit dédié `2087335874`, qui NOMME la revue |
| **7 — trois réserves d'outillage de l'instrument** | **PERSISTANT, les TROIS intactes** | `scripts/ops/fermetures-non-citees.mjs:45` rend `export const TITRE_CANARI = 'Canari rouge'` (exemption clouée au TITRE, #1793 OPEN) ; `git grep run_attempt 95248a08c -- scripts/ops/faits-de-palier.mjs` rend **aucune ligne** ; `scripts/guards/lib/revuePalier.mjs:118` rend `--diff-filter=A` seul (#1777 OPEN) |
| **8 — trailer `Claude-Session` manquant sur 3/10** | **RÉGRESSION** | **11/22** (50 %) contre 7/10 (70 %). Les absents couvrent les six lots #1825, les lots #1818/#1820 et trois correctifs de CI |

**Point remarquable** : la boucle revue → geste, ouverte au palier précédent par `da0a35c73`, se referme **trois fois** ici — `218a19735` (griefs 1 et 2), `2087335874` (grief 6), `ad6ba48e9` (grief 4) : trois commits qui NOMMENT la revue dans leur corps.

## Griefs de ce palier

**Grief 1 — la décrue du stock CSS est un DÉMÉNAGEMENT à 94 %, et rien dans l'outillage ne permet de le lire.** Mesures au § Stocks : stock `3349 → 2134` (−1215) contre corpus réel `6491 → 6420` déclarations (**−71**), feuilles `37 → 69`, feuilles EXEMPTES **+147** déclarations, `combat-console.css` **265 sites retirés du cliquet pour 11 déclarations touchées**. Les trois lots DISENT chacun leur part de transfert dans leur message — c'est l'honneur du palier — mais la ligne que le palier suivant lira est « identité 2339 → 1400 », et cette ligne additionne trois natures incomparables : le SOLDÉ (la matière disparaît), le PRIMITIVISÉ (la cure arbitrée A1 : l'identité passe dans un module de primitive) et le RECLASSÉ (une déclaration `nature: organisme`, ou une feuille déclarée exempte, retire des sites sans toucher au CSS). Un cliquet dont une partie de la baisse s'obtient en déplaçant la frontière de sa propre mesure n'est pas un cliquet : c'est un compte.

→ attendu : `cssCouchesStock.mjs` rend TROIS nombres et non un — sites DISPARUS du corpus, sites MIGRÉS vers un module de primitive, sites RECLASSÉS hors périmètre (organisme, feuille exempte) — et la garde refuse qu'une baisse soit majoritairement du troisième sans son ticket ; symétriquement, un compte des déclarations de `base.css` et `components.css` entre au cliquet, faute de quoi « feuille exempte » est un trou par lequel toute identité peut sortir. `fichier:ligne` : `scripts/guards/lib/cssCouchesStock.mjs` ; `src/data/primitives.manifest.json:731` ; `src/ui/styles/components.css` (475 → 595 décl.). **MAJEUR — c'est le grief nommé par l'utilisateur le 2026-09-20, et il se mesure.**

**Grief 2 — la tête publiée n'a AUCUN Canari vert : le dernier a échoué il y a deux jours, #1815 est toujours OPEN, et la seule porte qui voit une ancre fermée reste hors du chemin de publication.** Mesures :

```
$ gh api "repos/cgauche/game/actions/runs?branch=main&per_page=100"  (filtre name == Canari)
993107240   1  completed  failure  2026-09-18T18:36:21Z
75692d80c   1  completed  success  2026-09-16T10:14:30Z
3dc9500a7   1  completed  success  2026-09-14T12:22:03Z
   ← AUCUNE course depuis le rouge. 41 commits et 16 courses CI ont été publiés par-dessus.

$ gh api repos/cgauche/game/issues/1815  →  open
   ← « #1815 se referme par le Canari redevenu vert, pas par ce commit » (218a19735) : jamais arrivé.

$ git show 95248a08c:package.json              | grep -n plans
  35: "docs:check": "node scripts/docs/check-doc-refs.mjs && node scripts/docs/check-plans-anchors.mjs && …"
$ git show 95248a08c:.github/workflows/canari.yml | grep -n plans
  107: id: plans-dates
  115: node scripts/docs/check-plans-anchors.mjs --online /tmp/issues-ouvertes.json
$ git show 95248a08c:.github/workflows/ci.yml  | grep -n plans   →  aucune ligne
```

Le volet HORS LIGNE de la porte est bien dans `docs:check`, donc dans le chemin de publication ; **le volet `--online` — le seul qui sache qu'une ancre `Ticket:` est FERMÉE, exactement la cause du rouge du 18 — ne vit qu'au Canari**, et le Canari ne tourne plus. Conséquence double : le geste de `218a19735` n'est prouvé par aucune course, et une issue de bruit reste ouverte deux jours, avec précisément le titre que #1793 décrit comme empoisonnant l'instrument de palier.

→ attendu : le volet `--online` de `check-plans-anchors` entre au job de publication de `ci.yml` (la liste des issues ouvertes est une lecture REST, la couture existe : `scripts/guards/lib/ticketsGh.mjs:118 pagesRest`) ; et un Canari muet sur `main` pendant deux jours est en soi un défaut à instruire. `fichier:ligne` : `.github/workflows/canari.yml:107,115` ; `.github/workflows/ci.yml` (aucune ligne) ; `package.json:35`. **MAJEUR.**

**Grief 3 — un stock NAÎT à 26 entrées sous une ligne `CLIQUET:` qui en déclare 22, et la porte qui vérifie les comptes ne l’a pas vu.** `c251f46af` crée `src/ui/css-modules-guard.test.ts` (ABSENT à `993107240`) et porte : `CLIQUET: src/ui/css-modules-guard.test.ts +22 — stock de NAISSANCE de la garde §5.3 : 22 repeints…`. Comptage des entrées du littéral `REPEINTS_STOCK` sur l’arbre, sha par sha :

```
993107240  ABSENT      c251f46af  26      9e6fffcdd  27      41aa406d5  23
0645a8886  24          3dcf69f6b  24      95248a08c  24
entrées présentes à la naissance et absentes après 2b (les QUATRE que 41aa406d5 retire) :
  "src/ui/styles/combat-console.css|.cc-phase .btn"
  "src/ui/styles/combat-console.css|.cc-arch .ptile.team-enemy"
  "src/ui/styles/combat-console.css|.cc-dock [data-medaillon] .ptile"
  "src/ui/styles/combat-console.css|.cc-dock [data-medaillon] .ptile-face"
```

26 − 4 = 22 : la prose du `CLIQUET:` décrit une SOUS-FAMILLE (les repeints par un module d’écran) et ignore les quatre repeints par ce qui deviendra un organisme. Le nombre écrit au message ne mesure donc pas ce que le commit EMPORTE — or c’est exactement ce que la porte prétend tenir : `scripts/hooks/solde-ticket-guard.mjs:2168` (« la mesure vient de ce que le commit EMPORTE ») et `scripts/hooks/stocks-nominatifs.test.mjs:513` (« un compte FAUX ou un motif de tampon ne couvre rien »). Quatre entrées d’exemption sont entrées sans jamais être déclarées — et elles sont ressorties deux lots plus tard **par déclaration d’organisme**, sans correctif (grief 1). Un juge de diff ne pouvait pas le voir : il faut les six shas côte à côte.

→ attendu : instruire pourquoi la porte n’a pas mordu — soit `src/ui/*.test.ts` sort du périmètre de `scripts/guards/lib/stocksNominatifs.mjs` (auquel cas le périmètre se corrige : le fichier est bien un « test de `src/**` », famille nommée en tête de la lib), soit l’appariement du compte est trop lâche. **La vérification qui tranche, en lecture seule** : appeler `evaluateStocksQuiGrandissent` sur le diff de `c251f46af` avec son message, et lire le refus ou son absence. **MOYEN — un détecteur ne mesure que sa couverture, et celle-ci a un trou nommé.**

**Grief 4 — `JUGE:`/`REFUTATION:` retombe de 22/22 à 15/22, `Claude-Session` de 70 % à 50 %, et trois commits de substance n’annoncent AUCUNE porte.** Mesuré au § Instrument. Les commits de substance SANS passe de réfutation sont `95248a08c`, `e65606591`, `9a0819852`, `208733587`, `28210fd5b`, `0b43420fb`, `27c9f248a` — la famille est lisible : **ce sont les correctifs de CI rouge et les commits de suite**. Trois ne citent même pas une porte jouée (`e65606591`, `d380aa72f`, `28210fd5b`). Or `e65606591` MODIFIE un verrou (`src/state/travel-carto.test.ts`) en lui ajoutant une exemption par CLASSE (préfixe de dossier `src/ui/gallery/`) : c’est précisément le geste qu’une passe adverse doit regarder — une exemption par préfixe est plus large qu’une exception nommée, et rien au message ne mesure combien de fichiers elle absout aujourd’hui, ni combien elle en absoudra demain.

→ attendu : la porte de commit exige `JUGE:` + `REFUTATION:` sur tout commit qui touche un VERROU ou une exemption (le corpus est celui de `stocksNominatifs.mjs`), et le trailer `Claude-Session` se pose par `ops:publier` comme `Co-Authored-By` — attendu déjà formulé à trois paliers. `fichier:ligne` : table collée au § Instrument ; `src/state/travel-carto.test.ts` (exemption par préfixe posée par `e65606591`). **MOYEN, RÉGRESSION.**

**Grief 5 — `combatManeuvers.ts:440` : un plancher de durée en ROUNDS sans réf survit un palier de plus, dans le périmètre littéral de la preuve sur laquelle #1797 a été FERMÉE.** Sonde rejouée sur la tête, inchangée au caractère depuis le 2026-09-18 :

```
$ git show 95248a08c:src/state/combatManeuvers.ts   (lignes 438-447)
438:  // Fumée (souffle-fumee) : la zone bloque les Lignes de vue pendant BE Rounds — GÉOMÉTRIE moteur…
440:      const dur = Math.max(1, bonus(effectiveChar(attacker, «endurance»)))
446:      const zones = [...(get().battle!.zones ?? []), { label: t(«manv.smokeZone»), tiles,
                         rounds: dur, blocksLoS: true }]
```

Un BE de 0 rend **1 Round** là où le commentaire du site annonce « BE Rounds » et où aucune ligne de livre n’est citée pour un minimum : c’est le jumeau exact de la règle inventée que la passe de réfutation de #1797 avait fait corriger ailleurs. Le grief a été rendu le 2026-09-18 avec son `fichier:ligne` ; la fenêtre suivante compte 41 commits, aucun ne le touche, et **aucun ticket ne le porte** — vérifié un par un sur les neuf tickets ouverts dans la fenêtre (#1827, #1829 à #1836).

→ attendu : la borne migre dans la FORMULE (`{minimum, of}`, grammaire posée par le lot qui a fermé #1797) avec sa réf, ou le plancher tombe ; et un grief de palier NON traité reçoit son ticket au palier suivant, sinon la boucle revue → geste ne se ferme que sur ce qui est facile. `fichier:ligne` : `src/state/combatManeuvers.ts:438,440,446` ; `src/engine/disease.ts:193`. **MOYEN, RÉCIDIVE (2ᵉ palier).**

**Grief 6 — trois réserves d’outillage de l’instrument sont intactes pour le 2ᵉ palier consécutif, et l’une va rendre le prochain palier faussement vert.** Contre-greps collés au § Suivi : `fermetures-non-citees.mjs:45` cloue son exemption au TITRE `Canari rouge` (#1793 OPEN) ; `faits-de-palier.mjs` ne lit toujours pas `run_attempt` (sans conséquence ici — vérifié à l’API, les 16 courses sont à `run_attempt = 1`) ; `revuePalier.mjs:118` ne regarde que `--diff-filter=A` (#1777 OPEN). **#1815, l’issue de bruit ouverte par le Canari rouge du 18 et toujours OPEN, se fermera un jour par le bot** — et comme elle porte exactement le titre cloué, elle traversera l’exemption : le détecteur restera vert par chance, non parce qu’il sait ce qu’est un signaleur. La bonne mesure — l’exemption dérivée du registre des workflows — est celle de #1793, ouvert depuis trois paliers.

→ attendu : #1793 au prochain lot d’outillage de palier ; `coursesCi` remonte `run_attempt` ; `revuePalier` regarde aussi `D`. `fichier:ligne` : `scripts/ops/fermetures-non-citees.mjs:45,47` ; `scripts/ops/faits-de-palier.mjs` ; `scripts/guards/lib/revuePalier.mjs:118`. **MINEUR pour la livraison, MOYEN pour l’instrument.**

**Grief 7 — le budget de contexte monte de +348 octets en une fenêtre, et le même cran est déclaré DEUX FOIS après un rouge mécaniquement évitable.** `PLAFOND_OCTETS` : **26459 → 26807**, en trois hausses toutes portées par un `CLIQUET:` (`27c9f248a +288`, `cfa3bfbac +2`, `95248a08c +58`). Mais `96b76f1eff` porte lui aussi `CLIQUET: scripts/guards/budget-contexte.mjs +58 — une ligne d index pour la fiche de conduite entre chantiers` **sans relever le porteur** : la CI de branche a rougi (course 35498598122, reconnue au corps de `95248a08c`), et le cran a dû être re-déclaré. C’est la deuxième fois en deux paliers que la séquence « déclarer la montée, oublier le porteur » rougit une CI (`f3674009e` puis `27c9f248a` le 18 ; `96b76f1ef` puis `95248a08c` le 20). Rien n’a été fait de cette classe.

→ attendu : la porte de commit qui lit déjà `CLIQUET: scripts/guards/budget-contexte.mjs +N` VÉRIFIE que `PLAFOND_OCTETS` du même index vaut la mesure — les deux sont dans le même commit, aucune lecture réseau, le rouge est mécaniquement évitable et il a coûté deux courses. `fichier:ligne` : `scripts/guards/budget-contexte.mjs` (`PLAFOND_OCTETS` 26459 → 26807) ; corps de `96b76f1eff` et de `95248a08c`. **MINEUR, RÉCIDIVE.**

## Ce qui résiste

**La CI de publication, sans réserve.** `gh run list --workflow=ci.yml --branch main --limit 40` : les 40 dernières courses sont `success`. Dans la fenêtre : **16 courses, 16 `success`, `run_attempt = 1` partout, zéro relance** (agrégat `gh api` collé). 41 commits publiés en 38 heures sans un rouge sur `main`. Les trois rouges cités par les messages (35475955188, 35431802036, 35498598122) sont des CI de BRANCHE, et chacun est corrigé par un commit qui le NOMME et dit ce qui est entré dans `main` — « rien n’était entré dans main » (`9a0819852`, `95248a08c`) : la discipline de branche tient.

**Les deux fermetures.** Solde DANS L’INDEX du commit de fermeture (`6afd6c7a2` ne porte QUE `1818.md`), `verdict:` nommé sur les deux, restes uniques, cibles OPEN et vivantes, zéro `gh issue close`, `state_reason=completed` posé par le bot. Les deux soldes portent des restes `RAS` argumentés au site, pas des renvois : #1818 refuse d’amender la règle stricte 1 de CLAUDE.md avec son motif mesuré (budget de contexte), et #1812 justifie ses 6 documents `SANS_CROISSANCE` par une porte `fichier:ligne` chacun.

**L’Atlas cesse de nommer un livre, et c’est contre-grepé.** L’invariant de #1825 se vérifie en une commande :

```
$ git grep -c <token> 95248a08c -- scripts/
SCENARIO_BOOKS · SCENARIO_PUR · SECTION_LEVEL · BOOK_ORDER · PIVOT_BOOK_ID · PIVOT_ABBR
ldbRe · otherAbbrAlternation                                 → TOUS : aucune occurrence
HORS_REGLE → 3 occurrences, toutes dérivées de la DONNÉE :
  scripts/raw/_lib.mjs:107  const HORS_REGLE = horsRegleDe(chapitresData)
  scripts/raw/_lib.mjs:108-109  motifHorsRegle / estHorsRegle, table injectable
$ git cat-file -s 95248a08c:scripts/raw/chapitres.json  → 22479 octets
$ src/data/books.json : entrée core-rulebook-5e { abbr: "CRB", language: "VO", coeur: "5e",
                                                  niveauDeSection: 3, dir: "Source/…5e Core Rulebook" }
```

Six lots en série (A, B, B-fix, C, D, E1a), chacun avec sa passe de réfutation, chacun prouvant la non-régression par une méthode DIFFÉRENTE de celle du codeur : sonde différentielle sur 1 049 855 lignes ; comparaison des rapports générés ; lot F simulé de bout en bout ; tables de HEAD reconstruites et comparées ensemblistement (6/6 teneurs, 12/12 niveaux de section, 54/54 chapitres hors règle, 126/126 appartenances de catalogue, 0 écart). C’est la chaîne la mieux prouvée du palier.

**Les quatre cardinaux vivants du palier précédent sont morts, pas déplacés.** Contre-greps au § Fermetures : `TOTAL_ATTENDU` et `attendu: 66` n’existent plus dans `scripts/migrations`, `entrees:` n’existe plus dans `structuresStock.mjs`, et `registry-enveloppe.test.ts:459` porte `"primitives.manifest.json#0"` NU. La garde qui remplace n’est pas une graphie mais une PROPRIÉTÉ (`migrations:replay:croissance` : +1 entrée clonée dans 79 documents, rejeu de toutes les migrations), prouvée par mutation dans les deux sens (17 rouges sans le lot, 0 avec).

**Les surfaces neuves sont cataloguées, et le catalogue dit vrai.** `src/data/primitives.manifest.json` rend **93 entrées**, exactement le compte annoncé par `3dcf69f6b` ; `docs/charte-ui.md:302` catalogue `combat-console.css` en nommant son statut d’ORGANISME et sa peau `.skin-pont` ; `:307` catalogue `reward-recap.css` avec ses neuf classes et son slot de sortie ; `:44` et `:345-349` décrivent la borne de l’organisme. Aucune primitive neuve n’entre sans sa ligne de catalogue.

**Les tickets ouverts portent leurs dépendances, sans exception.** Les neuf tickets ouverts dans la fenêtre (#1827, #1829, #1830, #1831, #1832, #1833, #1834, #1835, #1836) portent tous « Bloqué par » / « Débloque » / « Prérequis partagé avec » dans le CORPS (`gh api …/issues/<N>` sur le champ `body` → **9/9 DEPS**). #1833 est le modèle du genre : un défaut trouvé PENDANT la recette de #1806 (`console-pont-formes.mjs` rouge à 1100 / 900 / 800), **mesuré comme ANTÉRIEUR au lot** (`combat-console.css` n’avait pas bougé depuis le 2026-08-24), instruit et routé plutôt qu’absorbé en silence.

**La mémoire n’est pas une poubelle.** `.claude/memory/` : 147 → 150 fiches (+8696 octets), **trois ajouts seulement**, chacun causé et daté — `user-doctrine-edition-5e-coeur-remplace-ldb-raw-sauf-errata` (10 verbatims utilisateur, dont le juge de fermeture a retrouvé les 13 fragments à l’identique au transcript), `reference-doc-differences-4e-5e-carte-jamais-source`, `feedback-sentier-voisin-des-faits`. Et `67bf023af` fait le geste inverse du réflexe : il RETIRE d’une fiche `user-*` une hypothèse d’ingénierie de l’orchestrateur qu’un juge avait réfutée sur mesure (1739 entrées n’ont que le LDB pour source), au motif qu’« une fiche `user-*` ne porte pas une idée que personne n’a arbitrée ».

**Et la boucle revue → geste se ferme trois fois.** `218a19735` (griefs 1 et 2), `2087335874` (grief 6, « porté par quatre revues de palier consécutives »), `ad6ba48e9` (grief 4). Trois commits qui citent la revue dans leur corps, contre un seul au palier précédent — qui était une première.

## Non couvert par ce jugement

(a) **Aucune suite, aucun build, aucun typecheck rejoué** (consigne machine) : `npm test`, `npm run gates`, `npm run typecheck`, `docs:build`, `docs:check` sont pris pour vrais sur la foi des messages et des 16 courses CI vertes. (b) **Aucun contrôle visuel** : les cinq `JUGE-VISION:` du palier (#1806 2a / 2b / 2c / 2d α+β / 2d γ1) annoncent des captures `public/qc/soldes/1806-2*.png` et une PREUVE ISO AU PIXEL sur l’écran de victoire (0 pixel différent à 1440 comme à 360) — **jamais ouvertes**. Un rendu non regardé n’est pas un rendu jugé. (c) Les **mutations annoncées** (12+8 sur #1825 E1a, 11 sur le lot D, la garde retirée qui fait rougir le test du beat d’ouverture) ne sont pas rejouées. (d) La sonde `scripts/ui/css-conservation.mjs`, dont trois lots tirent leur preuve de conservation, n’est **pas rejouée** : les comptes de disparues / apparues / bascules sont pris au message. (e) Le **contenu RAW** n’est pas rouvert au `Source/` : `LDB 59 l.41`, `MDG 12 l.122-129`, `CRB 037 l.9` (`#### **Gaining Momentum**`) sont lues comme des graphies, jamais résolues. (f) Le contenu GitHub des tickets n’est recroisé que sur `number` / `state` / `state_reason` / `closed_at` / `closed_by` / `title` / `body`. (g) Le commit `09880b160` (2d γ2) du worktree d’exécution est **postérieur à la fenêtre et écarté** ; aucune mesure n’a été prise sur le disque. (h) Le grief 3 laisse une question OUVERTE que seule l’exécution de `evaluateStocksQuiGrandissent` sur le diff de `c251f46af` tranchera.

## Verdict

verdict: PARTIEL

**Seize courses CI sur `main`, seize vertes, zéro relance ; deux fermetures, deux soldes dans l’index de leur propre commit, deux cibles de routage OPEN et vivantes ; six lots RAW en série qui retirent d’un dépôt tout nom de livre en dur, chacun prouvé par une méthode différente de celle du codeur ; quatre cardinaux vivants ÉTEINTS et remplacés par une PROPRIÉTÉ mécanisée, prouvée par mutation dans les deux sens ; treize commits sur vingt-deux portant une ligne `CLIQUET:`, et une PORTE de pré-commit qui en exige désormais la déclaration ; neuf tickets neufs portant tous leurs dépendances dans les deux sens ; trois commits qui NOMMENT la revue précédente et soldent trois de ses huit griefs** : sur l’axe de la livraison, le cumul de la fenêtre `993107240..95248a08c` est le plus propre des sept paliers. Ce qui le fait basculer en PARTIEL est le grief que l’utilisateur a nommé le 2026-09-20, et il se mesure : **(1)** le stock CSS annoncé en baisse de **1215 sites** ne correspond qu’à **71 déclarations réellement disparues** du corpus (6491 → 6420) — **265 sites quittent le cliquet par la seule déclaration `"nature": "organisme"` pour 11 déclarations touchées** (`combat-console.css` 655 → 644), les feuilles déclarées exemptes grossissent de **+147 déclarations** (`base.css` 510 → 537, `components.css` 475 → 595), et les lots le DISENT honnêtement au message pendant que l’INSTRUMENT, lui, ne sait pas séparer le soldé du déménagé ; **(2)** les `!important` sont à **36 avant, 36 après**, les 11 de `hud.css` simplement relogés dans `initiative-strip.css` et `party-dock.css`, et les 9 de `base.css:353-361` posés par `74b952861` — **ancêtre de la base, donc fenêtre PRÉCÉDENTE**, grief que la revue du 2026-09-18 a manqué — restent verrouillés par une garde qui les EXIGE (`src/ui/ui-ratchets.test.ts:1367`), #1796 OPEN ; **(3)** la tête publiée n’a **AUCUN Canari vert** : le dernier a échoué le 2026-09-18T18:36:21Z, aucun n’a tourné depuis, **#1815 reste OPEN** et le volet `--online` de `check-plans-anchors` — seul capable de voir une ancre fermée — ne vit qu’au Canari (`canari.yml:115`), jamais dans `ci.yml` ; **(4)** un stock NAÎT à **26 entrées** sous un `CLIQUET:` qui en déclare **22**, les quatre non déclarées étant précisément celles que le lot 2b retirera deux commits plus tard par la déclaration d’organisme — la porte qui refuse « un compte FAUX » ne l’a pas vu ; **(5)** `JUGE:`/`REFUTATION:` retombe de **22/22 à 15/22** et `Claude-Session` à **11/22**, la famille manquante étant celle des correctifs de CI rouge et des commits de suite — dont `e65606591`, qui élargit un verrou à une exemption par PRÉFIXE sans une ligne de mesure. S’y ajoutent deux récidives exactes : `src/state/combatManeuvers.ts:440` (`Math.max(1, BE)` en Rounds sans réf, dans le périmètre littéral de la preuve qui a FERMÉ #1797) intact au caractère pour le 2ᵉ palier et sans ticket, et le budget de contexte qui monte de **+348 octets** en trois crans dont un déclaré deux fois après un rouge de CI mécaniquement évitable. Les trois réserves d’outillage de l’instrument (#1793 clouée au titre, `run_attempt` illisible, `--diff-filter=A` seul) sont intactes pour le 2ᵉ palier — et #1815, toujours ouverte, porte exactement le titre que l’exemption cloue : le prochain palier sera vert par chance.

## Note de l'orchestrateur à l'archivage (2026-09-20)

Contre-grep d'UNE affirmation de la revue, sans toucher à son texte : le grief 5 dit qu'« aucun des 9 tickets ouverts de la fenêtre » ne porte `src/state/combatManeuvers.ts:440` / `src/engine/disease.ts:193`. Le ticket existe hors de cette liste : **#1817** « [RAW/moyenne] Planchers de durée sans réf : zone de fumée Math.max(1, BE) et durée de maladie — #1797 fermée sur un grep réfuté », OPEN (`gh api search/issues … combatManeuvers`). Le grief tient sur le fond (aucun GESTE depuis deux paliers) ; il n'appelle pas de ticket neuf, il appelle que #1817 soit pris.
