# Solde #1826 — 2026-09-20

VERIFIE: `BOOK_REF` de `src/ui/book-ref-guard.test.ts` n'écrit plus aucun sigle — il dérive de `alternationDuRegistre()` (`scripts/raw/_lib.mjs`, la fabrique `alternationDe` PARAMÉTRÉE sur tout livre du registre porteur d'un `abbr`, extrait ou non), servie à `src/**` par la couture typée `scripts/guards/lib/rawRefIntegrity.mjs` ; le banc asserte que CHAQUE `abbr` du registre est couvert (le CRB compris) et qu'aucun livre n'est sans `abbr` ; les deux fantômes `ADE2?` et `T2C` sont morts ; les 11 réfs de livre rendues sur surface de jeu sont retirées (remède 2 du ticket), chaque ancre RAW contre-vérifiée vivante côté moteur ou flux par le juge du diff ; garde verte à zéro tolérance, sans baseline.

## Restes
- Le ticket attendait l'import de `allAbbrAlternation()` : cette alternation ne couvre que les livres EXTRAITS (à `dir`), soit une partie seulement des sigles que le constat du ticket nomme (juge du diff, point 3) -> corrigé dans ce commit scripts/raw/_lib.mjs:151
- `T2C` n'était pas un livre manquant mais une graphie périmée du livre déjà au registre sous `MSRC` (quatre concordances de chapitre au `Source/`) ; le commentaire qui la portait a d'abord été réécrit avec un numéro de chapitre IMPRIMÉ au lieu du préfixe de fichier (juge du diff, point 6) -> corrigé dans ce commit src/engine/possession.ts:90
- `T2C` survivait au § Sources VF du contexte permanent -> corrigé dans ce commit CLAUDE.md:97
- Réf de LIGNE nue rendue sur surface de jeu (« , l.131 »), même classe, invisible à la garde dont le motif exige un sigle -> corrigé dans ce commit src/ui/LandMarketView.tsx:149
- Douzième réf rendue, trouvée par la recette : un sigle SEUL entre parenthèses, sans numéro, que le motif ne voyait pas -> corrigé dans ce commit src/ui/InterludeScreen.tsx:1043
- La classe de cette douzième réf (apposition parenthésée sans chapitre) échappait à la garde -> corrigé dans ce commit src/ui/book-ref-guard.test.ts:49
- DoD « recette navigateur des 4 écrans » : Interlude (bandeau et volet de préparation), marché terrestre (six libellés) et écran de l'Étoile déroulés en joueur à deux largeurs, console à 0 erreur ; l'infobulle « Capricieux » de la modale de Test n'est atteignable par AUCUN contenu jouable (seule la Naïade porte le Trait, aucune scène ne la référence, `vsCapricieux` n'est posé que par un test unitaire) -> RAS : site prouvé par lecture du nœud rendu et par la garde, qui lit aussi les attributs `title` ; l'absence de scénario pour ce modulateur est un fait de testabilité consigné au compte rendu du ticket 1825.
- Le « ADE II 8 » retiré du bandeau de bataille était de surcroît mal attribué : le plafond cité vit à `src/state/interludeFlow.ts:212` sous une autre réf (juge du diff, point 4) -> RAS : la réf fautive n'existe plus nulle part, l'ancre du flux est juste.
- DoD « mutation `(CRB 08 l.1)` rendue → rouge » -> RAS : tenu par deux bancs conjoints — la MORSURE (une réf rendue dont le sigle est pris au registre rougit, la même en commentaire passe) et la couverture (`BOOK_REF.test` vrai pour l'`abbr` de chaque livre du registre).
- Ce que la garde ne voit toujours pas, écrit dans son en-tête : un livre nommé par son TITRE, une réf sans numéro, un nœud non rendu -> RAS : limites de FORME déclarées au site, aucune occurrence mesurée sur `src/ui`.

## Recette visuelle
capture: public/qc/soldes/1826-interlude-bandeau.png — recetteur du 2026-09-20 (worktree `.wt-1825-atlas-second-coeur`, port 5189, kit CDP) : scénario de bataille de masse, les quatre dés d'Événement résolus et « Passer aux Activités » par CLICS réels ; le bandeau « Bataille imminente » se lit « …Sabotage… (max 3). « Engager la bataille » clôt l'interlude et lance les Rounds. », sans réf de livre ni ponctuation cassée.
capture: public/qc/soldes/1826-interlude-preparation.png — Activité « Discours inspirant » ouverte au clic : la note du volet se lit « 1 Activité d'interlude — l'issue porte sur l'armée. ».
capture: public/qc/soldes/1826-marche-terrestre.png — scénario de commerce fluvial, étal ouvert par le jet de Ragot réel : libellés et infobulles `title` d'Évaluer, Acheter, Vendre et Brader relus sans réf (« Estimation avant Marchandage (+10 % si lot partiel) », « Brader à la moitié du prix de base »).
capture: public/qc/soldes/1826-createur-etoile.png — créateur avancé jusqu'à l'étape « Signe astral » : « …et le garder rapporte +25 PX de création. L'Étoile du Sorcier ouvre quatre destins… » et « Choisissez ou tirez votre signe astral — son sens apparaîtra ici. ». Les quatre écrans repassés à 360 px sans débordement dû au lot ; console à 0 erreur sur tout le parcours.

## Réfutation
verdict: PARTIEL — juge adversarial en lecture seule sur le diff (2026-09-20, tête `2e213446a`, neuf points) : lot jugé FRAGILE, dont pour ce ticket la population de l'alternation (livres extraits au lieu du registre entier), un commentaire de garde qui mentait sur cette population, une sonde de banc découpée dans une alternation déjà échappée, et la réf `MSRC 10` fausse au `Source/`. Les quatre sont soldés dans le commit et relus par l'orchestrateur ; les onze ancres RAW ont été contre-vérifiées une par une, aucune n'est perdue. PARTIEL parce que le juge a vu le diff AVANT ces corrections : elles sont prouvées par les bancs et les portes, pas par une seconde passe adverse.
