# ref-707 — Lot 1 : persistance d'état des scènes au revisit (couche `sceneInstances`)

## Juge
Juge moteur adversarial (6 lentilles) — verdict CONFIRMÉ, aucun défaut bloquant. Vérifié : `captureMutation` (delta = ids authored absents de current + flags dont la valeur runtime diffère de l'authored — capte AUSSI un flag authored:true passé à false, `undefined` si vide, `authored.flags?.` géré) ; `applyMutation` PUR (filtre les retirées + fusion mut-gagne, nouvelle Scene, ne mute ni le clone ni le registry, no-op par référence si absent) ; couture `transitionTo` (capture la scène QUITTÉE vs `sceneRegistry[leaving.id]`, applique l'instance de la CIBLE au clone frais, `scene`+`sceneInstances` dans le MÊME set, ordre `suspendActiveCascade`/`resetFields('scene')`/`autoSave` préservé, cible jamais visitée = no-op propre) ; revisit A→B→A préservé (ni doublé ni perdu, testé avec `removeEntity`+`setDoorOpen` réels) ; save/load RÉEL (`saveGame`/`loadGame`, save antérieure sans clé → `{}` sans crash) ; `startScene` remet `{}` (reset total) ; `resetOn:[]` empêche `resetFields('scene')` de vider ; save mid-scène SÛR (la scène courante porte sa mutation dans `state.scene`, aucun chemin lossy). Alias `applyMutation as applySceneMutation` propre (le `applyMutation` de corruptionFlow inchangé). Une fragilité latente relevée par le juge et CORRIGÉE par l'orchestrateur : `initialFields()` ne clonait que les arrays → `sceneInstances:{init:{}}` (1er champ à init objet) partageait le `{}` du manifeste → `initialFields` clone désormais AUSSI les objets (footgun fermé).

## Réfutation
verdict: CONFIRMÉ
Réfutation adversariale confirmée : la couche `sceneInstances` (capture-au-départ/applique-à-l'entrée, une SEULE couture dans `transitionTo`, zéro instrumentation de `removeEntity`/`setDoorOpen`) fait bien persister au revisit les entités RETIRÉES (décor consommé ET PNJ tué en combat, tous deux via le point unique `removeEntity`/`removeEntities`) et les flags de portes/structures/tuiles, sans jamais muter le document authored. Save + coop gratis (patron `explored`/`campaignDoc`). Le trou de scope « entité SPAWNÉE au runtime non persistée » est une frontière ASSUMÉE de #707 (périmètre = retraits + flags), à documenter en Lot 2 — pas un défaut du lot. La fragilité `init:{}` partagé est comblée. Gates relancés par moi : typecheck 0 imputable, suite complète verte, `sceneInstance.test` 11/11. Ce lot livre le MOTEUR ; le scénario démo de revisit + la doc campagne-authoring + la recette clavier ferment #707.

2026-07-23
