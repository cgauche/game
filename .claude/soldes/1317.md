# Solde #1317 « Filtre de couche : les PROPS d'étage (z1) sont supprimés à l'écran de jeu pendant que leur plancher est peint (les deux voies) » — 2026-09-21, clôturé le 2026-09-23

VERIFIE: le DoD verbatim (« l'opéra montre son lustre et ses loges meublées en jeu, jugé sur capture, aucune régression du filtre pour les étages réellement masqués »), sur l'arbre `34a974a34` — le défaut re-mesuré aux règles du jour n'était plus « un filtre » mais DEUX lois d'étage pour un même écran : `buildProps` portait la sienne (`z > activeZ`, trois sites : overlays de terrain, entités, features de façade) pendant que la masse cuite et le décor volumique obéissaient à `loiDeDégagement`/`keepEl` (`src/gameIso/stage/MondeDeCampagne.tsx`). Geste : `buildProps` ne porte plus AUCUNE loi d'étage (il ne garde que l'isolement demandé `viewZ`) ; l'écran de jeu filtre le décor billboard par `keepEl`, la MÊME expression que la masse (iso → couvercle retiré au-dessus du groupe, plan → `z === activeZ`, POV → toutes les couches) ; l'éditeur passe `buildProps` nu par son prédicat unique `layerHidden` (rien ne bouge à son écran). Émission et filtre en deux mémos : un cran de rotation de caméra ne rebâtit aucun décor (trouvaille du juge de diff, mutation rouge). Picking : aucun geste de socle — le rayon ne parcourt que les groupes que le masque laisse à l'index, un quad de décor occulte sans fuir d'identité, la portée d'interaction tient l'étage (`exploreNav.aPorteeDe`) ; parité iso/plan/couvercle/picking tenue par `monde-de-campagne.test.tsx`, `props.test.ts`, `editeur-monde-volumique.test.tsx`. En-tête de `state/viewLevel.ts` et JSDoc de `buildProps` réécrites (elles disaient un rendu « un étage à la fois », faux en iso). `npm run typecheck` 0 ; `npm test -- src/state src/data src/scenes src/ui` vert ; `docs:check` 0. Recette en joueur à l'opéra (kit CDP, neuf sessions, console 0 erreur) : groupe au rez, la galerie de l'étage montre balustrades, fauteuil de loge, plante et lustre ; sous la galerie le couvercle est retiré et aucun meuble ne flotte sans sol ; depuis l'étage le parterre et son décor restent visibles en contrebas ; lustre et fauteuils ne répondent ni au survol ni au clic (décor sans `usable`).

## Restes
- Vue du dessus, décor des DEUX étages au même x,y -> RAS : recettée en joueur le 2026-09-22 (kit CDP, touche V `toggle-view`, montée par la rampe à pied, console 0 erreur). Groupe au rez, sous le lustre (10,8) : rangées de sièges et colonnes du parterre visibles, AUCUN meuble d'étage (lustre, fauteuil de loge, balustrade) ; groupe à l'étage (`partyPos.z` passe à 1 en franchissant la rampe (2,13)→(2,12)) : fauteuils de loge et plante visibles, AUCUNE rangée du parterre ; aucun meuble sans plancher, aucun plancher sans ses meubles, sur la moitié gauche de la galerie et le centre du parterre (la loge droite x=17-19 non visitée)
- Voile de brouillard sur un décor d'étage inexploré -> RAS : l'opéra porte une zone voilée (`__wfrp.visibleCount()` : 219 cases jamais explorées sur 714) ; la recette n'y a pas cadré de décor identifié, le décor voilé n'est donc PAS vu à l'écran : tenu par les tests de parité iso, plan et couvercle sur le chemin réel de `MondeDeCampagne` (`monde-de-campagne.test.tsx`)
- En vue du dessus un décor BILLBOARD est vu de chant et se réduit à un trait (constaté sur `statue`, recette du lot B) : c'est la propriété du billboard, dont le remède structurel est le passage en volume (lot B : 17 defs ; restes nommés au commentaire de #1343 et #1883) -> #1343

## Réfutation
verdict: CONFIRMÉ — passe de réfutation de la vague #1343 lot B (un juge, lecture seule, arbre `bff4352e9`) : DoD TIENT (`buildProps` ne porte plus que `viewZ`, `builders/props.ts:137,157,189` ; balustrades, fauteuil, plante et lustre vus sur `1317-opera-rez-rot1.png` et `-etage.png` ; lustre à 5 m, sonde `h(z)`). Reste « vue du dessus → RAS » FRAGILE : la clause « aucune régression du filtre pour les étages réellement masqués » est exactement le cas du plan (`z === activeZ`), jamais vu à l'écran — soldé par la recette en vue du dessus du 2026-09-22 (restes ci-dessus, captures `1317-plan-rez.png`, `1317-plan-etage.png`). Ce que la recette n'a pas vu : un décor DANS la zone voilée, et la loge droite.

## Recette visuelle
capture: public/qc/soldes/1317-opera-rez-rot1.png
capture: public/qc/soldes/1317-opera-rez-rot3.png
capture: public/qc/soldes/1317-opera-sous-galerie-vue-large.png
capture: public/qc/soldes/1317-opera-etage.png
capture: public/qc/soldes/1317-plan-rez.png
capture: public/qc/soldes/1317-plan-etage.png
