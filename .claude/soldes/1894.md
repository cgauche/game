# Solde #1894 — le juge ne mute jamais ; aucune suppression récursive hors des cibles jetables du hook

date: 2026-09-23

VERIFIE: j'ai relu le diff des 7 fichiers (définitions juge et codeur, Claude et Codex ; workflow `juge-design-socle` ; skill `orchestrer-des-agents` et son miroir `.agents/` produit par `npm run agents:sync`) ; j'ai confronté la consigne au hook qui émettait la confirmation (`scripts/hooks/git-destructive-guard.mjs`, `CIBLES_JETABLES` l.117-118) ; les deux `developer_instructions` Codex se relisent en TOML (`tomllib`, antislashs doublés) ; `npm run agents:check` exit 0, `npm run test:agents` exit 0, `node --test scripts/agents/compat.test.mjs scripts/guards/budget-contexte.test.mjs scripts/ops/workflows.test.mjs` 51/51.

## Restes

- Le workflow `juge-design-socle` autorisait une sonde « dans un dépôt jetable », donc une copie mutée puis supprimée -> corrigé dans ce commit (.claude/workflows/juge-design-socle.js:36)
- Aucune consigne ne disait au juge de REFUSER un brief qui lui ordonne une mutation, alors que l'origine mesurée est le brief -> corrigé dans ce commit (.claude/agents/juge.md:30)
- Le skill d'orchestration, qui rédige ces briefs, ne l'interdisait pas -> corrigé dans ce commit (.claude/skills/orchestrer-des-agents/SKILL.md:66)
- Le juge devait vérifier des empreintes que le codeur n'avait pas à fournir, et `codeur.toml` ne portait pas la preuve par mutation -> corrigé dans ce commit (.codex/agents/codeur.toml:27)
- La recette Codex écrit ses captures sous `%TEMP%\recette-<scenario>\`, hors des cibles jetables -> RAS : elle ne prescrit aucune suppression (`.codex/agents/recetteur.toml:36-38`), et une écriture ne déclenche pas la confirmation

## Réfutation

verdict: CONFIRMÉ — un juge adversarial a attaqué la première version du diff, verdict FRAGILE en 6 griefs : classe non couverte (workflow `juge-design-socle`), cause au brief laissée ouverte, remède « chemin littéral » qui ne répondait pas au hook (seules les `CIBLES_JETABLES` sont exemptées, la variable n'est qu'un cas), empreintes exigées du juge mais pas du codeur, ambiguïté sonde/mutation, fins de ligne. Les griefs 1 à 5 sont corrigés un par un dans ce commit ; le 6 est écarté sur pièce (`git ls-files --eol` : index `i/lf`, `eol=lf`). La version corrigée n'a pas été re-jugée : je l'ai vérifiée grief par grief contre le hook et les gates.
