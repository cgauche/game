# Solde #540 — Vue LECTURE du Compendium : projection des op rollTable (2026-07-17)

VERIFIE: gates authoritatifs verts (suite COMPLÈTE, tsc 0, lint 0, docs:check OK) ; `opRows`/`GameOpChips` projettent `rollTable` GÉNÉRIQUEMENT (inline `rows` ET variante `tableId` résolue via `findEffectTableById`) via la nouvelle fonction UNIQUE `tableRows` — la catégorie « Tables d'effets » de #541 (qui dupliquait la projection à la main) refactorée dessus, source unique ; récursion bornée `MAX_TABLE_DEPTH=1` (une table imbriquée → chip codex-lié cliquable, jamais d'expansion infinie ni A→B→A) ; gate `flowHasRollTable` (les rangées structurées ne s'ajoutent qu'aux sorts portant un rollTable — zéro régression sur les ~200 autres, walker walkFlow couvrant toutes les branches) ; RECETTE navigateur en joueur réel : vers-de-carie → Cycle quotidien = 8 rangées humanisées VISIBLES (capture), allure-demoniaque → Effet mécanique = 4 branches par Domaine + 4 chips Mutation codex-liés comptés conformes au RAW (Nurgle 1/Slaanesh 1/Tzeentch 2/Indivisible 0), console 0 erreur ; juge vision BON (tables lisibles comme de vraies tables de règle, chips distincts, pas de casse).

## Restes
- Prose d'effet répétitive des sorts multi-branches (allure-demoniaque : la formule commune répétée 4×) + densité des tables de lecture perfectible -> #594
- `opRow` rollMutation retombe sur le libellé brut `?? o.table` sans fail-fast (contrairement à findEffectTableById qui throw) — latent, toutes les données actuelles résolvent -> RAS : défensif à durcir si un id de table de mutation devient invalide un jour ; aucune donnée fautive aujourd'hui, le contrat positif est vérifié.
- Effets DÉCLENCHÉS (traits/qualités/talents via effectRows) non étendus à rollTable -> RAS : contre-grep confirme qu'aucun rollTable ne vit dans un TriggeredEffect aujourd'hui (trou réel-mais-vide, à ouvrir si un rollTable y apparaît un jour).

## Réfutation
verdict: CONFIRMÉ
Juge primitives dédié rendu CONFIRMÉ sur les 6 angles : source unique établie (le duplicata #541 supprimé, aucune 2e projection min/max/label, aucun opSummary d'atelier en lecture), récursion bornée SANS perte (chip ref résolu et cliquable), gate flowHasRollTable sans régression (sort sans table = rendu strictement inchangé), row-type `sub` PRÉEXISTANT réutilisé (classe .codex-rowsub, pas d'invention), couverture honnête (contre-grep : rollTable ne vit qu'en sorts/symptômes/tables, tous couverts), zéro orphelin après refonte. Preuve navigateur + juge vision confirment le rendu à l'écran.
