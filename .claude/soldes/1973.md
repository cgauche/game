# Solde #1973 — « Hooks : retrait des ask git destructif et suppression (régime worktree) ; silence des hooks Write/Edit hors dépôt »

date: 2026-09-26

VERIFIE: publié sur main en deux trains (`f4b01419d` puis `54a60d4f8`), CI de branche verte aux deux (courses 36237732295 et 36242170730) ; quatre juges de diff successifs, chaque défaut confirmé corrigé avec son test et sa preuve par mutation ; typecheck complet exit 0 ; `node --test scripts/hooks/*.test.mjs scripts/docs/lib/*.test.mjs scripts/gates/*.test.mjs` 735/735 ; `npm run docs:check` exit 0. Mesuré dans l'arbre principal avancé à `0be32dfdb` : un `git restore` y passe sans confirmation, un `rm -rf "$S/…"` par variable dans le scratchpad aussi, un Write portant un tag entériné au scratchpad et sous `node_modules/.cache` ne déclenche aucun hook.

## Restes

- Écriture par la jonction mémoire `~/.claude/projects/…/memory` jugée hors de `.claude/memory` et non gardée -> corrigé par f4b01419d scripts/hooks/solde-ticket-guard.mjs:2132
- Graphie MSYS `/c/…` lue au mauvais endroit par poison-postcheck et enterine -> corrigé par f4b01419d scripts/hooks/solde-ticket-guard.mjs:2130
- Graphie `\c\…` non convertie en `C:\…` -> RAS : un Write de l'outil sur `\c\Users\…\scratchpad\sonde-antislash.txt` crée `C:\c\Users\…` (mesuré 2026-09-26), le hook juge donc l'endroit réel
- Fichier neuf sous un dossier ignoré (ex. `public/qc/*.json`, `.gitignore:29`) rendu muet -> RAS : il ne se committe qu'avec `git add -f`, aucun script ni doc du dépôt n'en use (`git grep -nE "add (-f|--force)"`, 0 résultat) ; les quatre fichiers suivis sous motif restent gardés (`check-ignore` sans `--no-index`)
- Worktree d'agent vivant dont le `.gitignore` précède les exceptions `soldes` -> RAS : ce worktree ne peut pas committer ces fichiers non plus, le verdict suit son propre `.gitignore`
- Dossier de worktree mort qui garde un `.git` fichier cassé -> RAS : git y rend indisponible, `ignoreParGit` rend false et le hook garde (repli conservateur, aucun cas sur disque au 2026-09-26)

## Réfutation

verdict: CONFIRMÉ — juge 1 : 5 défauts (dont un chemin MSYS DANS le dépôt jugé hors dépôt) ; juge 2 : lecture disque au chemin brut (D1, D2) et notes gitignorées jugées suivies ; juge 3 : double décision hors dépôt, repli muet de git indisponible ; juge 4 (élargissement aux fichiers ignorés) : aucun défaut confirmé, 132 chemins sondés sur l'arbre réel, aucune surface versionnée muette. Tous les défauts confirmés sont corrigés dans les deux trains publiés.
