# Solde #877 « Un ref de décor inconnu se rend en TONNEAU (fallback en dur) et validateScene ne vérifie aucun ref d'entité » — 2026-09-21, clôturé le 2026-09-24

VERIFIE: re-mesuré le 2026-09-24 par la passe de réfutation de clôture, sur l'arbre du train #1882 (branche `chantier/1882`, `ab288804c`) : les quatre clauses du DoD TIENNENT (`git grep` des replis d'id → 0 ligne ; sonde des 4 paquets `{propEnts:314, morts:0, sansType:0}` contre `props.json` ; `decor.test.ts`, `props.test.ts`, `projet-migration-11-vers-12.test.ts` : 55 tests verts). La PRÉSENTATION d'un refus de schéma dans la modale « Avancé » (chemin positionnel `dialogues:`) n'est pas un reste de ce DoD : elle appartient à #1588, ticket préexistant (2026-08-30). La LANGUE y est livrée par ce chantier (train D, capture `877-C-avance-refuse-schema.png` : « Entrée invalide : tableau attendu, nombre reçu »). Mesure d'origine, le DoD clause par clause, sur l'arbre `c3157a1f4`, ticket RÉ-INSTRUIT aux règles du jour (le site d'origine `decor/index.ts:22` avait disparu ; le repli vivait dans `REF_DECOR_DEFAUT`, `propSprite` et douze `findPropById(x ?? '')`) — (1) « aucun repli sur un id en dur » : `REF_DECOR_DEFAUT` et `propSprite` MORTS, les douze coercitions `?? ''` retirées, `?? 'baliste'` de la palette remplacé par `premierOffert(catalogue, quoi)`, l'union `Tool` de l'éditeur scindée (`{mode:'entity';kind:'prop';ref:string}`) — un décor sans type n'est plus constructible par le typage ; schéma de scène : `kind:'prop'` ⇒ `ref` REQUISE et `idDe('prop')` (`defs-scenes/scene.ts`, message nommé) ; (2) « `validateScene` signale un `ref` inconnu » : « décor sans type… » et « → décor inexistant « x » », test à mutation ; (3) « un `ref` inconnu ne se rend plus comme un objet valide » : silhouette d'erreur `[sans type]` (`sceneMeshes.ts` `REF_NON_NOMMEE`), inspecteur à état NOMMÉ (option désactivée + `.chip.tone-danger` `role="alert"`, plus de `<select value={undefined}>` qui affichait « Abreuvoir ») ; (4) « zéro `ref` mort dans les projets embarqués » : migration de projet `PROJECT_MIGRATIONS[11]` → v12 (`REF_DU_RENDU_AVANT_877 = 'tonneau'`, l'ancien rendu NOMMÉ, jamais un défaut vivant), script de dépôt pour les artefacts manuscrits, et les trois points d'appareillage de `barge-du-sel` / `loup-et-saumure` typés à la SOURCE (générateurs, byte-stables). Étendu dans le lot à la classe entière — tout geste de l'éditeur qui couche, charge ou lance un projet passe `parseProject` : « Enregistrer » (`3bbecd03f`), « Exporter JSON », « Importer JSON… », « ▶ Tester », modale « Avancé » au schéma de ses blocs (`c3157a1f4`) ; refus LU par l'auteur (`refusDeLaPorteDuProjet`, table fermée verbe + conséquence par geste), zéro `alert()` sous `src/ui/`, un seul constructeur de l'enveloppe (`documentDeProjet`, `state/worldMap.ts` ; les générateurs y DÉLÈGUENT depuis `63a7ede96`, `scripts/campagne/lib.mjs` `projectDoc`, byte-stable). Durci par le train E2 (`63a7ede96`) : `parseProject` n'altère plus son entrée et lève `ProjetRefuse` (cause `mal-forme` | `version` | `schema` + fautes structurées), `migrateDoc` nomme sa raison de refus (`RaisonDeRefus`), UN traducteur d'écran pour les CINQ gestes (ouverture comprise) qui classe par chemin et par cause, jamais par le texte du rapport ; la bibliothèque de campagnes (joueur) rattrape le refus à « Jouer » / « Exporter » et l'affiche dans le panneau de l'entrée, près des boutons ; réf de port au schéma (`idDe('navalPort')`). Outillage de recette : `__wfrp.editorPatchEntity`, `__wfrp.editorEntities`. `npm run typecheck` 0 ; `npm test -- src/ui src/state src/scenes src/data/schemas` vert (888 fichiers node, 133 jsdom) ; `docs:check` 0. Recettes en auteur (kit CDP, trois passages, console 0 erreur) : palette, inspecteur aux trois états, validation, enregistrement / export / import / essai refusés en nommant scène et entité, lisibles à 360 px, focus initial sur « Fermer ».

## Restes
- Même doctrine côté PERSONNAGE : un `kind:'personnage'` sans `ref` ni `statblock` ni `presetId` recevait en silence le profil en dur `{label:'Ennemi', char:{B:10}}` (`spawn.ts:392-396`), lu comme fiche d'opposant par `tavernFlow` -> RAS : soldé dans la MÊME publication par #1882 (T2a : schéma 13, `typeNonNomme` source unique du schéma et de `validateScene`, migration 12 → 13 ; T2b : `PorteurDeFiche`, la branche `!ref` de `spawnEnemy` est morte ; T2c : la Palette pose une fiche, sentinelle `id:''` morte)
- Les trois points d'appareillage des quais se dessinent en `tonneau` par accident historique (c'était le repli) : question d'ART, pas de règle -> inventaire #1343 : consignée au commentaire du lot B, le passage du décor en volume la reprend avec les autres définitions billboard
- Filet d'autosave : `restore` rechargeait un brouillon du stockage local sans passer AUCUNE migration (`useEditorAutosave`, `normalizeScene` seul) -> RAS : soldé par #1882 T2c, dans la même publication. L'autosave porte désormais son `schema`, sa lecture traverse la chaîne canonique `PROJECT_MIGRATIONS` (`sceneAuSchemaCourant`, `state/worldMap.ts`), et un brouillon SANS version est écarté et DIT à l'auteur (politique `saves.ts:17-24`). Le filet reste le recours d'un enregistrement refusé : la migration n'est pas la porte de validation
- Un `ref` de décor peut encore MOURIR par un geste d'auteur : renommer ou retirer l'id au Codex (`CodexEdit.tsx:1854`, champ libre, aucune garde des références entrantes), après quoi la porte REFUSE le projet porteur au chargement (la silhouette d'erreur reste donc inatteignable en jeu, tenue par test) ; la classe entière (tout dataset, renommage et retrait), trouvée par la passe de réfutation -> #1884

## Réfutation
verdict: CONFIRMÉ — passe de réfutation de CLÔTURE du 2026-09-24 (un juge, lecture seule, arbre `ab288804c`, train #1882 T2a-c compris), sous deux corrections du solde, faites avant commit. DoD TIENT sur ses quatre clauses, re-mesurées sur l'arbre du jour. Reste « personnage sans fiche » TIENT à la lettre : la branche `!ref` est morte (`spawn.ts`, `PorteurDeFiche`), et `'Ennemi'` n'a plus aucune occurrence dans `spawn.ts` ni dans `Inspector.tsx`. La classe VOISINE (`ref: ''` qui joue le mannequin `RÉF ?`, adversaire de poursuite semé vide, le mannequin lui-même) est dite à #1882, qui reste OUVERT et l'absorbe en T2d. Reste autosave TIENT (`autosaveLoad` → `sceneAuSchemaCourant` → `migrateDoc`, 19 tests verts). RÉFUTÉ le constat du reste #1588 (« reste en anglais » faux depuis `bff4352e9`) : retiré des restes, il n'en est pas un (VERIFIE). FRAGILE VERIFIE épinglé sur un arbre antérieur au train : ré-épinglé. Passe précédente, vague #1343 lot B (un juge, lecture seule, arbre `bff4352e9`), attaquant le DoD clause par clause, les restes et l'architecture « à rebours » : DoD TIENT sur ses quatre clauses (`git grep REF_DECOR_DEFAUT|propSprite` = 0 ; `validateScene.ts:171-174` et ses tests ; `props.test.ts:128`, `decor.test.ts:41-47` ; sonde des paquets `{files:4, propEntities:314, morts:0, sansType:0}`), reste autosave TIENT. FRAGILE « un seul constructeur » (second constructeur à la main dans `scripts/campagne/lib.mjs:198`) et « la porte mute son entrée » (`structuredClone` de l'appelant) : soldés par E2 (`63a7ede96`). RÉFUTÉ « table fermée des gestes » (quatre gestes, un traducteur-sœur pour l'ouverture, deux regex relisant un texte que le dépôt formate lui-même) : soldé par E2, un traducteur pour cinq gestes nourri des fautes structurées. RÉFUTÉ l'en-tête de `export-passe-la-porte.test.tsx` (« Avancé » ne passe pas `parseProject`) : réécrit. FRAGILE « silhouette d'erreur inatteignable » (renommer un id au Codex tue les réfs) : routé à #1884. Deux passes de juge de diff sur E2 (RÉFUTÉ ×2 : TypeError de migration relancée, cause devinée, `try` accusant l'auteur) soldées en remontant la raison à `migrateDoc`. Recette en auteur et en joueur des refus E2 (kit CDP, console 0 erreur).

## Recette visuelle
capture: public/qc/soldes/E2-import-mal-forme.png
capture: public/qc/soldes/E2-import-mal-forme-detail.png
capture: public/qc/soldes/E2-import-version.png
capture: public/qc/soldes/E2-import-mal-forme-360.png
capture: public/qc/soldes/E2-ouvrir-sans-scene.png
capture: public/qc/soldes/E2-bibliotheque-refus.png
capture: public/qc/soldes/877-palette-decor.png
capture: public/qc/soldes/877-palette-emplacement.png
capture: public/qc/soldes/877-inspecteur-sans-type.png
capture: public/qc/soldes/877-inspecteur-type-disparu.png
capture: public/qc/soldes/877-inspecteur-type-elu.png
capture: public/qc/soldes/877-validation-sans-type.png
capture: public/qc/soldes/877-enregistrement-refuse.png
capture: public/qc/soldes/877-C-export-refuse.png
capture: public/qc/soldes/877-C-export-refuse-360.png
capture: public/qc/soldes/877-C-import-non-json.png
capture: public/qc/soldes/877-C-import-refuse.png
capture: public/qc/soldes/877-C-tester-refuse.png
capture: public/qc/soldes/877-C-tester-refuse-faute.png
capture: public/qc/soldes/877-C-avance-refuse.png
capture: public/qc/soldes/877-C-avance-refuse-schema.png
