# Solde #1812 — 2026-09-18

VERIFIE: le contrat du ticket (un cardinal VIVANT gelé dans une migration déjà jouée, dans `registry-enveloppe.test.ts` et dans `structuresStock.mjs` fait payer un recalage hors sujet à chaque entrée neuve — 62→66 au #1800, 85→86 au #1392) est corrigé pour la CLASSE entière : 41 migrations datées remplacent leurs comptes par des portes de FORME en lecture seule avant toute écriture (forme SOURCE ou forme CIBLE, sinon « ARBITRAGE REQUIS », rien d'écrit) ; les cardinaux IMPOSÉS par le livre restent, et leur refus PRONONCE sa réf nue ; la propriété du DoD (« +1 entrée → aucun rouge ») est mécanisée par `npm run migrations:replay:croissance` (job `migrations` de la CI) : +1 entrée clonée dans 79 documents et +1 Scène par projet, puis rejeu de toutes les migrations — rejoué par l'orchestrateur sur l'arbre final (`--arbre`) : 0 rouge, 4 refus à référence, 567 fichiers du périmètre inchangés ; le juge l'a joué sur la tête SANS le lot : 17 rouges nominatifs, exactement la classe du ticket ; mutation (accumulateur `total !== 841` réinjecté dans `l1b-11b`) → rouge nominatif, retrait → vert, fichier restauré à l'octet ; `test:hooks` 1043/1043, `typecheck:fast` 0 erreur, 41 migrations rejouées à la main exit 0 sans réécriture ; après rebase sur `origin/main`, `migrations:replay:head` et `migrations:replay:croissance` rejoués en mode SHA sur le commit du lot : verts ; diff relu par l'orchestrateur, jugé trois fois.

## Restes
- Garde de classe par GRAPHIE (`/attendu: \d+|TOTAL_ATTENDU/`), verte par construction puis aveugle aux deux graphies d'origine — table indexée par variable de boucle, total accumulé (juge, passes 1 et 2) -> corrigé dans ce commit (scripts/migrations/lib/croissance.mjs:133) : le détecteur statique est supprimé, la garde fabrique l'ajout et regarde qui proteste.
- Exemption évaluée sur la sortie ENTIÈRE d'une migration : une seule réf exemptait tous ses refus (juge, passe 3) -> corrigé dans ce commit (scripts/migrations/lib/croissance.mjs:210) : chaque ligne de refus porte SA réf, banc « refus double dont une seule ligne porte une réf ».
- Porte nominative de `stars.json › [].sub` exemptée par raccroc (juge, passe 3) -> corrigé dans ce commit (scripts/migrations/2026-09-01-1659-sub-lengthm-plage.mjs:157) : la table nominative ne gouverne que la forme SOURCE, un sous-tirage neuf traverse.
- `materials.json` exclu de la croissance par une table d'arbitrage id→label tenue dans une migration jouée, même péage (juge, passe 3) -> corrigé dans ce commit (scripts/migrations/2026-08-28-l1b-10c-relief-roof-labels.mjs:83) : une entrée portant son `label` traverse, `materials.json` est exercé par la croissance.
- Péage dormant sur le chemin « arbre non migré » de `1657-b2b-maladies-noeud-test` (juge, passe 3) -> corrigé dans ce commit (scripts/migrations/2026-09-02-1657-1682-critiques-unifiees.mjs:143) : table `CARDINAUX` de `b2b` supprimée au profit de relations mesurées, et la réf des Critiques passe au MESSAGE de refus en forme canonique `LDB 18 l.53`.
- `testsParGate` ne listait que les tests SUIVIS : un test neuf non indexé passait vert par ABSENCE, faux vert de classe (codeur, confirmé par le juge) -> corrigé dans ce commit (scripts/gates/testsParGate.mjs:81) : découverte `--cached --others --exclude-standard`, banc aux deux bords (neuf pris, ignoré laissé).
- `structuresStock.d.mts` déclarait `entrees: number` qu'aucune ligne ne porte plus (juge, passe 1) -> corrigé dans ce commit (scripts/guards/lib/structuresStock.mjs:867) : le stock gèle l'identité de la divergence, le poids reste mesuré par `build-structures.mts`.
- `ReferenceError` introduite en cours de lot dans `1691-relief-en-donnee` (`reliefApres` supprimé), invisible au rejeu sur arbre migré, attrapée par le banc `1691-relief-portes (a)` -> corrigé dans ce commit (scripts/migrations/2026-09-07-1691-relief-en-donnee.mjs:108).
- `expect(apparies.length).toBe(111)` puis partition tautologique (juge, passes 1 et 2) -> corrigé dans ce commit (src/ui/compendium/registry-enveloppe.test.ts:565) : restent les contrats qui mordent, `toEqual(PARTIELS)` / `toEqual(ORPHELINS)` keyés `fichier#rang`.
- La preuve « 0 rouge » n'existait sur aucun arbre mesurable, le script n'exportant qu'un sha (juge, passe 3) -> corrigé dans ce commit (scripts/migrations/replay-croissance.mjs:45) : mode `--arbre`, copie jetable de l'arbre de travail, fichiers non suivis versionnables compris.
- Deux variables mortes laissées par l'extinction (`assert` importé, `SOUS_FAMILLES`) et un commentaire à affirmation RAW non ancrée, refusés par le lint du pré-commit -> corrigé dans ce commit (scripts/migrations/2026-09-01-1659-sub-lengthm-plage.mjs:156) : variables supprimées, commentaire ramené à la table imprimée ; les trois migrations rejouées, exit 0.
- Table des tailles de coque absente de l'Atlas : il a fallu rouvrir `Source/` pour confirmer `MDG 12 l.122-129` -> #1814
- `SANS_CROISSANCE` : 6 documents qu'un clone ne peut pas faire croître (`structures`, `careerLevels`, `names`, `raceAppearance`, `props`, `raw.manifest`) -> RAS : vraies intégrités référentielles ou identités dérivées vérifiées au site par le juge, chaque entrée cite sa porte `fichier:ligne` et un banc vérifie qu'elle existe ; un ajout réel apporte son jumeau.
- 43 documents à racine non-tableau : leurs tables NICHÉES ne sont pas exercées par la croissance -> RAS : les deux sites connus (`plage-borne-haute`, `1463-gram-seuils-prix-offre`) sont des tables closes du livre et portent leur réf nue au site ; aucun cardinal nichée vivant trouvé en trois passes de juge.
- Le clone est à la forme CIBLE : la croissance ne prouve rien des portes de forme -> RAS : périmètre écrit en tête de `croissance.mjs`, les portes de forme sont tenues par `idempotence-ordre-des-cles.test.mjs` famille 4 et les bancs `*-portes.test.mjs`, prouvés par mutation.
- `CARDINAL_RESHAPES = 2` de `1463-cost-rendu-a-son-type` laissé -> RAS : compte des deux gestes que ce passage a faits lui-même, aucune entrée neuve ne l'incrémente.

## Réfutation
verdict: PARTIEL — juge du diff, trois passes (2026-09-18, lecture seule, sondes `node`) : passe 1, 5 majeurs (garde verte par construction, 13 cardinaux vivants restants, type `.d.mts` menteur, test neuf invisible de `test:hooks`, preuve `replay:head` jouée sur l'ancien code) ; passe 2, la garde statique rate encore les deux graphies d'origine → changement de niveau, la garde devient la propriété ; passe 3, 4 corrections (exemption à la ligne, `stars.json`, `materials.json`, péage dormant) → toutes corrigées dans ce commit. Résiste : la croissance mord sur la tête sans le lot (17 rouges nominatifs) et rend 0 rouge avec lui ; les trois reclassements RAW→vivant du codeur sont fondés (`skills.json` porte 11 livres, maladies 16→18 par EDOC 08, `spells.json` grandit) ; les réfs au site se résolvent à l'Atlas, sauf les tailles de coque (#1814) ; 0 poison de commentaire sur les 60 fichiers (`scanExcuses`, `scanTombstones`, `scanLegacyVocab`).
