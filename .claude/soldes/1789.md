# Solde #1789 « Ids de terrain récités en code hors semence : alphabets d'authoring, choix de compilation, sentinelles et défauts d'IU → donnée » — 2026-09-18

VERIFIE: les cinq fix du ticket et le DoD, clause par clause, sur l'arbre final `9d66de88f` — (1) alphabets d'authoring : `terrains.json › ascii` (glyphe UNIQUE par terrain, hors `GLYPHES_RESERVES` de `grammaire/carte-ascii.ts`), `BASE_LEGEND` mort, légende et export `devtools`/`sceneToAscii` DÉRIVÉS avec un allocateur unique partagé (legend/wallLegend/zoneLegend), round-trip vert, `__wfrp.ascii(z)` rend grille murée + trois légendes + relief (train B2 `608b43965`) ; (2) choix de compilation : `src/data/defauts-de-compilation.json` (`cheminDeRonde: pierre`, `masse: mur`, `pont: planches`, trois `idDe('terrain')`, Codex « Défauts de compilation » vérifié à l'écran, jumelle des semences) — `CELL_WALKWAY`, `CELL_MASS` et le `'planches'` du pont morts, `state/shipDeck.ts` sans importeur supprimé (train C `6334e651a`) ; (3) sentinelles : ce ne sont pas des constantes mais des RÔLES de donnée — `absence:true` sur `vide`, `bordDuMonde:true` sur `mur`, exactement un porteur chacun (refine de COLLECTION), demandés par `terrainAbsent()`/`terrainHorsGrille()`/`porteurDuRole` (`state/terrain/index.ts`, mémo par version) ; `spawn.ts` ne replie plus sur `'sol'` ; cardinal `tiles/height/crenellated === w·h` en `superRefine`, `putLayer` refuse hors cardinal, `resizeGrid` primitive pure qui conserve les merlons et sème PAR COUCHE (base = sol de départ, étage = absent : agrandir `siege-enceinte` 30×46 → 31×47 posait 77 « Étage sans appui », mesuré 0 après, aller et retour — trains B1 `bc2bdb749`, C, E) ; (4) IU : options météo dérivées de `valeursDe(sceneWeatherSchema)` (`enumNomme(valeurs, hints)`, hints `LDB 14 l.75/76/82` en `title`, « Non spécifiée » = absence — lus à l'écran : 6 options, 3 hints exacts, `value=""` jamais `'clair'`), `offTerrainMod` avec son éditeur DÉDIÉ (sélecteur dérivé de `terrainsElectifs()` — le registre MOINS les porteurs de rôle —, libellé FR à l'écran, id en valeur, défaut = premier non franchissable des électifs = `bois`, résumé « hors Eau … , suffoque »), payload strict sorti d'`OPS_NON_TYPEES` ; (5) garde : bras terrain sur les cinq couches à zéro, sans site toléré, mutation `cheminDeRonde → 'pierre'` rouge nommant `state/mapSpec.ts:728` (train D' `9d66de88f`) ; écart au verbatim § Fix 5 (`capacite:` non ajouté, couvert par le vocabulaire d'union `type CapaciteArete`) consigné au ticket avec la sonde. `map:check opera` 0 défaut ; registre des scénarios vert ; `npm run typecheck` 0 ; `docs:check` 0. Recette navigateur en joueur (trois passes, 2026-09-18, console 0 erreur) : météo conforme (6 options, hints en `title`, absence = `""`) ; Codex « Défauts de compilation » identique à sa jumelle ; éditeur dédié `offTerrainMod` : toutes les électives — 25 terrains moins les deux porteurs de rôle, soit 23 options, sans « Vide (étage) » ni « Mur », défaut « Sous-bois », résumé « hors Eau : M 1, -2 DR aux Tests, suffoque » après gestes réels ; `siege-enceinte` 31×47 puis 29×45 : Validation « Aucun défaut détecté » (avant le train E : 77 « Étage sans appui »).

## Restes
- `SchemaDef.chargeParDiscriminant` ne sait exprimer ni un RÔLE à porteur UNIQUE ni une partition par PRÉSENCE : l'atelier offre `absence`/`bordDuMonde`/`ascii` (nés ici) et `vueDeDessus` sur toutes les entrées, le save refuse ; trouvé en corrigeant un FAUX VERT de `codex-edit-charge-discriminee.test.tsx` (sonde de faisabilité qui parsait un singleton contre des refines de cardinalité globale : quatre catégories muettes), stock passé du compte aux clés, ligne pointée -> #1795
- Le message du train C affirmait « défaut `offTerrainMod` inchangé à l'écran : `eau` » ; mesuré `bois` (premier non franchissable dans l'ordre authoré) -> RAS : claim faux corrigé au message du train E, le défaut est une valeur de départ dérivée, l'auteur élit dans la liste
- La case « suffoque » de l'éditeur dédié est la 17ᵉ instance du sélecteur non typé `editor.css:709` (`.dr input { width: 44px }`), sujet du ticket ouvert #1792 dont le cardinal (« 14 ») est recalé par commentaire -> RAS : classe CSS déjà ticketée, la case est fonctionnelle, hors périmètre du lot
- Observation d'une recette au volet : « M imposé » vidé (triple-clic, `Delete`, `Tab`) retombe à `0` — non reproduite par le kit CDP ni par l'orchestrateur (outillage), test jsdom « vider ÔTE la clé » vert ; seul chemin de code rendant `0` : `NumberField` en mode `frappe`, `Number(v) || 0` sur une saisie transitoire non numérique -> RAS : hypothèse et gestes de re-mesure consignés sur #1792 (même champs `.dr` de `GameOpEditor`, reprise qui les recette)
- Design D (garde au checker, brand `Id<'terrain'>`) jugé FRAGILE et non exécuté : le brand est érasé à trois coutures et n'empêche pas un cast ; programme séparé, routé par le solde #1716 -> inventaire #1680 : #1794 ouvert, seul reste routé du lot pour ce socle

## Recette visuelle
capture: public/qc/soldes/1789-offterrainmod-dedie.png
capture: public/qc/soldes/1789-resize-agrandi-0.png
capture: public/qc/soldes/1789-resize-retreci.png
capture: public/qc/soldes/1789-meteo-champ.png
capture: public/qc/soldes/1789-codex-defauts-de-compilation.png
capture: public/qc/soldes/1789-resize-validation-erreurs.png

## Réfutation
verdict: CONFIRMÉ — passe adverse (juge, 2026-09-18, arbre `9d66de88f`) sur les cinq fix et les trois clauses de DoD, sur pièces : `terrains.json` mesuré (25 entrées, un `absence` = `vide`, un `bordDuMonde` = `mur`, cinq `ascii` uniques `~ _ # = D` = le cardinal exact de l'ancien `BASE_LEGEND`, zéro doublon) ; garde 11/11 et mutation `'mur'` dans `Inspector.tsx` → rouge nominatif ; `map:check opera` 0 ; `check-doc-refs` 0 ; les huit docs régénérés du dernier commit ne changent que leur empreinte (hash de corps identique) ; le claim faux du train C (« défaut `eau` ») est bien dans `6334e651a` et sa correction explicite dans `9d66de88f` ; #1795 porte invariant, constat mesuré, DoD à mutation et dépendances. Ont CÉDÉ deux points de disposition, corrigés avant fermeture (rédaction de #1794 sur le partage des restes, item de dette météo consigné sur #1680). Écart au verbatim § Fix 5 (`capacite`) consigné au ticket avec la sonde du juge de diff. Architecture à rebours : `defauts-de-compilation.json` séparé de `semences-de-scene.json` jugé JUSTE (les semences se déversent telles quelles sur la scène, les défauts de compilation ne sont des champs d'aucune scène).
