# Solde #1644 « Recettes volumiques — LOT A contenants & mobilier de base (tonneau, caisse, coffre, étagère, chaises/bancs, tables, comptoir…) — vague 2 de #1343, ~116 usages » — 2026-09-15

VERIFIE: le DoD relu clause par clause — (1) les 12 refs du lot A portent leur recette `volume` dans `src/data/props.json` et la garde dérivée `src/gameIso/catalog/props-volumiques.test.ts` (4 contrats), `props-integrity`, `label-parite` sont vertes (livrées par les trains précédents de l'épique #1680, suite complète 90 fichiers / 1363 tests verte sur `61ad56a64`) ; (2) aucun code nouveau pour les recettes, les stocks structures/slots nommés à leurs commits ; (3) PORTE — le reste ouvert du ticket, « vérification à l'écran des meubles d'intérieur de l'opéra », était IMPOSSIBLE par construction : `src/scenes/opera/furnished.ts` (`scenarioEntities`, 443 entités) n'était consommé par aucune scène jouable depuis `ecbf4e124` (sonde `grep -rn "furnished\|scenarioEntities" src scripts` : un script QC et deux gardes de population) ; `61ad56a64` pose la scène de test DÉDIÉE `opera-plan` (arbitrage utilisateur 2026-09-07, verbatim : « faites les [tests] sur des scenes créé spécialement pour ces tests, pas sur des scénes qui sont utilisés »), catégorie « Rendu », et la recette en joueur du 2026-09-15 (recetteur, worktree `.wt-1478-1644-recette-opera`, port 5179, `fog(false)` en setup, déplacements `goto` puis clavier réel) résout les 27 cibles du brief au `pickTileAt` — billetterie (`billet-g`/`billet-d` comptoir-droit), foyer (`foy-banc-g/-d`, `foy-urne-g/-d`), salle verte (`sv-table` 2×1, `sv-etagere`, `sv-chaise-1/2`, `sv-banc-1`), coulisses et réserves (`c25-table-1/2`, `c25-tab-1/2`, `c24-armoire-1`, `s20-tonneaux`, `r27-tonneaux-1/2`), étage (`bar-g-*`, `bar-d-*`, `salon-d-*`, `salon-s-*`) — chacune sur SON `entId`, `via:'decor'` (volume dessiné sous le pixel) ou `'meuble'` ; À L'ÉCRAN, les volumes du foyer (comptoir de billetterie, bancs), des réserves (tonneaux) et de l'étage (tonneau, comptoir et étagère du bar D) se voient et se lisent (captures versionnées) ; console à 0 erreur sur tout le parcours ; (4) GOÛT — parqué en absence (le DoD le dit : « ne bloque pas le commit — la donnée est éditable au Codex ») : captures jointes ci-dessous, question consignée au ticket #1771 pour les pièces du rez.

## Restes
- Sous la galerie de l'étage, le REZ de `opera-plan` reste COUVERT : en salle verte `(3,4)` et aux coulisses `c25`, le groupe se rend en silhouette bleue et le mobilier est invisible bien que `pickTileAt` résolve ses `entId` — mesuré de première main le 2026-09-15 : `walls()` → `{"(sans structure)":983}`, `src/scenes/opera/floorplan.ts` ne déclare AUCUNE `architecture` (la galerie est un `relief` à 4 m sur toute la grille, `:46`), `clearedSpace` (`src/gameIso/builders/roofs.ts`) ne dégage donc ni emprise ni couvercle, et `viewLevel(0)` n'a aucun effet visible en iso ; ce n'est pas un défaut des recettes volumiques (les mêmes refs se rendent au foyer et à l'étage) mais de la SCÈNE de recette et de la loi de dégagement d'un niveau sans masse — deux questions de design (masse émise par le générateur ASCII ou plancher de niveau dégagé par lui-même ; emprise à dégager sous une galerie continue, question de goût) -> #1771
- Validation de GOÛT des meubles du rez (cap du comptoir face à la salle, étagère plaquée au mur, table 2×1 sans blocage d'un passage) : instruite au foyer et à l'étage (comptoir et bancs bien posés, aucun flottement vu), NON instruite en salle verte/coulisses puisque rien ne s'y voit encore -> RAS : la donnée est éditable au Codex (DoD), les captures sont jointes ici et au ticket #1771 qui porte la question au même geste que le découvrement du rez
- Le doublon de concept `comptoir` vs `comptoir-droit`/`comptoir-angle` nommé au ticket -> RAS : tranché par les trains précédents de #1680 (le plan meublé n'emploie que `comptoir-droit`, `billet-g`/`billet-d` et les bars le prouvent à l'écran), la ref `comptoir` porte sa recette propre dans `props.json` et la garde de population l'atteste

## Recette visuelle
capture: public/qc/soldes/1644-opera-plan-foyer.png — recetteur du 2026-09-15 (worktree `.wt-1478-1644-recette-opera`, port 5179) : scénario `opera-plan`, groupe au foyer (21,54) après `ready()` et `fog(false)` — le comptoir de billetterie (`comptoir-droit`, volume) et un banc du foyer sont dessinés en volumes, le héros non occulté. Seconde capture versionnée : `public/qc/soldes/1644-opera-plan-etage-bar.png` — étage z=1 depuis (36,45) après marche au clavier : tonneau (`bar-d-tonneau`), bloc de comptoir et étagère du bar D en volumes. Troisième, le DÉFAUT routé : `public/qc/soldes/1644-opera-plan-salle-verte-couverte.png` — salle verte (3,4), héros en silhouette bleue, pièce « vide » sous la galerie (#1771). Console à 0 erreur.

## Réfutation
verdict: PARTIEL — passe de réfutation de la fermeture sur pièces (orchestratrice, 2026-09-15) : la clause PORTE du DoD (« les scènes réelles qui posent ces refs rendent les volumes ») TIENT sur le foyer, les réserves et l'étage de la scène dédiée et sur les 27 `pickTileAt`, mais elle est CONTREDITE à l'écran pour les pièces du rez couvertes par la galerie — le défaut est dit, mesuré (sondes collées au reste) et routé vers UN ticket (#1771) rattaché à la vague #1343/#1680, il n'est pas enjolivé ; le juge de la revue de palier `abedb5fd4..61ad56a64` a attaqué « `opera-plan` double-compte le mobilier dans les gardes de population » et l'a écarté par re-mesure (`CORPUS_SCENES` ne lit que le `.json`, les gardes importent `scenarioEntities` directement) ; la prémisse « le reste du ticket était vérifiable sur une scène existante » a été réfutée par la sonde de consommateurs de `furnished.ts` (commentaire de pilotage du 2026-09-15 sur le ticket).
