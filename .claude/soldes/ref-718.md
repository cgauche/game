# ref-718 — Lot 1 : archive d'historique de dialogue (état) + doc du journal persistant

## Juge
Juge moteur adversarial (5 lentilles) — verdict CONFIRMÉ. Vérifié : (1) le hook d'archivage dans `chooseDialogue` enregistre un tour UNE seule fois, au bon moment — APRÈS la garde de coût (un choix bloqué faute d'argent n'archive rien), et une seule fois même quand `choice.flow` suspend sur un Test différé (`resolveTest` ne rappelle jamais `recordTurn` ; `set` fonctionnel = état frais, pas de lecture périmée) ; (2) `dialogueHistory` campagne-scopé — absent de `stateFields.ts` donc survit à `transitionTo`, remis à `[]` par le spread de `getInitialState()` dans `startScene`, prouvé par assertions positives ; (3) `speakerLabel` correct (override `node.speakerId` prime), et la correction du lookup partiel historique (ex-`store.ts:2109`, qui ignorait l'override) est réelle et sémantiquement équivalente à `DialogueBox` ; (4) `recordTurn` pur, borné (fenêtre glissante, cap 500 = arbitrage MAISON commenté) ; (5) save round-trip exercé par le VRAI `saveGame`/`loadGame`. Deux griefs mineurs relevés par le juge et CORRIGÉS par l'orchestrateur en direct : `speakerLabel` calculé deux fois → extrait en une const unique ; test de rétrocompat manquant (save antérieure sans `dialogueHistory` → `[]` sans crash) → ajouté et vert.

## Réfutation
verdict: CONFIRMÉ
Réfutation adversariale confirmée : l'archive de dialogue n'introduit ni double-compte, ni fuite d'état, ni régression du flux `chooseDialogue` (garde de coût et Test différé préservés) ; la prémisse périmée du ticket (« journal reset par scène ») a été vérifiée sur pièces (l'Effect `transition` route vers `transitionTo` qui ne touche pas `journal`) et commentée sur #718 ; le journal est désormais documenté (cycle de vie nu). Les deux griefs mineurs du juge sont comblés et re-vérifiés par l'orchestrateur (const unique, test de rétrocompat additive). Gates relancés par moi : typecheck 0 imputable (hors `_tmp-*`), suite complète verte, `dialogueHistory` 14/14. L'écran de relecture JOUEUR (surface verbatim) est le Lot 2 (à venir, avec goût utilisateur) — ce commit ne livre que l'ÉTAT.

2026-07-23
