# Solde #1699 — chemins ASCII sous `Source/` (Lot G de l'épique #1388) — 2026-09-14

VERIFIE: l'orchestrateur a lu le diff (`git status` 462 lignes : 336 renames purs R100, 6 index de livres renommés R066-R096, 1 paire A/D, 39 `00 - Index.md` modifiés, ~95 fichiers hors `Source/`, 4 créés) et contre-mesuré lui-même sur `Game-1388` : `git -c core.quotePath=false diff HEAD -M --numstat -- Source` ne montre aucun fichier à +/- non nul hors `00 - Index.md` (0), `git ls-files -- Source` = 0 chemin non ASCII sur 1 353, `12 - Sans titre.md` présent (One Shots), `croissanceDesStocks` sur le diff = +2 (`ecrivainsAtteints.test.mjs`, déclarés par `CLIQUET:`), `books.json` `dir` ×4 + `extractionDir` recalés, `docs/raw/talents.md:11` déjà juste ; contre-grep des 6 dossiers accentués hors `Source/` : deux chemins nus dans `docs/raw/sources.md:20,24` trouvés par l'orchestrateur et couverts ensuite par la classe « nom nu » (206 couples, 0 résidu vivant) ; codeur : migration dry/apply/apply² (0 collision, 276 → 0 gestes), `node --test` migration 9/9 et `nom-ascii` 10/10, `test:raw` 337/337, `test:hooks` 850/850, vitest 17 fichiers / 309 tests, `raw:check-folio-continuity` (76 sites, 23 perdues + 28 bénignes alignées), 6 gates raw exit 0, `docs:build` + `check-doc-refs` OK, `migrations:replay` OK, `typecheck` complet 0, eslint 24 fichiers 0, `--dry` sur l'arbre principal : 6 PDF à renommer (pas 3, rejoué après le push) ; DoD du ticket : « contenu des fichiers byte-identique (seul le nom change : `git diff -M --stat` = 100 % renames) » tenu pour les chapitres, l'exception mesurée des 39 index (cibles de liens, +198/−198, 0 ligne hors lien, 1 194 liens vivants) qualifiée de décision d'ingénierie dans l'en-tête de la migration.

## Restes
- Les 6 PDF gitignorés homonymes de l'arbre PRINCIPAL (`Source/*.pdf`, absents d'un worktree lié) restent à renommer par le pas 3 de la migration (`--apply` sur l'arbre principal, rejouable, indépendant du plan git) -> RAS : le pas 3 existe dans ce commit (scripts/migrations/2026-09-14-1699-source-chemins-ascii.mjs:236, lu sur le disque, indépendant du plan git) et l'orchestrateur le joue sur l'arbre principal juste après le push — des fichiers gitignorés n'entrent dans aucun commit
- `chantier/1456-choix` (`772a217cc`) cite `Boîte d'Initiation` dans `refs-migrated.test.ts:563` et `ab/phanes-c` / `essai/phaneslight-rejeu` suppriment `.claude/workflows/audit-poison.js` que ce lot réécrit -> RAS : WIP d'autres sessions, préavis posté sur #1699 après le push (jamais touchés d'ici)
- `tsconfig.tsbuildinfo` SUIVI porte encore `source-eol-guard.test.ts` (cache de build committé qu'aucune gate ne régénère, `typecheck` complet ne le modifie pas) -> RAS : rien ne le lit comme vérité de nommage ; qu'il soit versionné est un défaut hors périmètre de ce lot, à mesurer avant tout ticket
- `estMajuscule`/point fixe : la garde d'hygiène juge l'ASCII, pas le point fixe de `nomAscii` (un `12 - .md` ASCII passerait) -> RAS : critère tenu par la migration (`aBouger = nomAscii(nom) !== nom`), dit dans l'en-tête de la garde

## Réfutation
verdict: CONFIRMÉ — juge de diff Opus (lecture seule, 2026-09-14) a attaqué l'invariant à l'octet (numstat, paire A/D comparée, 93 lignes de 5 index sans divergence hors cible de lien), `nomAscii` hors table (`ß ø € U+00A0 U+202F „ ﬁ ł þ µ ° ×` lèvent), l'idempotence et les collisions sur 1 278 segments, l'export hors dépôt, les chemins d'histoire, `.claude/memory`, `.gitattributes`, la garde EOL (identique à l'octet) et la coordination des worktrees ; RÉFUTÉ puis corrigé : troisième angle mort (PDF composé par segments dans deux `.py`), croissance de stocks +5 → +2 déclarés, `12.md` hors forme canonique → `Sans titre`, 43 basenames nus en commentaires, collision de dossiers, court-circuit `estAscii`, nom vide, « arbitré » sans citation ; l'orchestrateur a réfuté « 0 résidu hors Source/ » (deux dossiers nus dans `docs/raw/sources.md`) → classe couverte, 0 résidu vivant re-mesuré. Non joué par l'orchestrateur : les gates complètes, jouées par le train sur la tête poussée.
