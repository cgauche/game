# Solde #419 — WorldMapEditor : 820 lignes, 6 vagues sédimentées

date: 2026-07-26

VERIFIE: j'ai compté les lignes après découpe (277 + 380 + 195 + 93 au lieu de 848 en un bloc), lancé `npx tsc --noEmit` (0 erreur) et les tests ciblés (197 verts sur `src/ui/editor`), et regardé MOI-MÊME la capture à 360 px — les onglets « Trajet / Péripéties » fonctionnent et le panneau reste utilisable à cette largeur, ce qui prouve le responsive plutôt que de l'affirmer.

## Restes

- `MapRoute.river` / `riverPerils` / `riverExposure` n'ont AUCUN contrôle dans le panneau de route : lacune PRÉEXISTANTE (0 avant comme après le refactor), donc pas une régression -> #841
- `PlaceService.label` et `.rest` : le panneau Lieu n'expose qu'une case à cocher basculant `{kind}`, sans surcharge de libellé ni offre de couchage -> #841
- Une référence périmée ou invalide retombe sur le placeholder « — choisir — » dans `RefSelect`, alors que `IconField`/`BackdropField` signalent explicitement « héritée » : la référence cassée devient invisible -> RAS : divergence mineure d'affordance, sans perte de donnée, relevée par le juge comme réserve et non comme grief

## Réfutation

verdict: CONFIRMÉ — un juge adversarial a comparé `git show HEAD:` (848 lignes) aux 4 fichiers issus, occurrence par occurrence sur 12 familles de champs (`prices` 2→2, `perilDie` 5→5, `ambush` 11→11, `market` 12→12, `richesse` 14→14…) : aucun champ perdu, la restructuration est fidèle. Il a en revanche RÉFUTÉ la valeur du test de non-régression — en-tête qui survendait (« chaque champ » pour un échantillon) et une assertion VACANTE qu'un composant débranché aurait passée. Corrigé : couverture étendue aux 18 champs manquants, et non-vacance prouvée en cassant délibérément l'assertion.
