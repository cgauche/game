---
name: project-1680-socle-volumique-etat-2026-09-03
description: "État du chantier SOCLE VOLUMIQUE #1680 (+ #1507, #1509) au 2026-09-04 — ce qui est posé sur main, le lot 15-B commité local en attente de validation d'écran, ce qui reste, où sont les briefs/passation"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4407a64f-b0ad-4d3d-b30f-ffca252025d6
  modified: 2026-09-04T19:00:05.330Z
---

Chantier #1680 (socle des décors volumiques, 18 lignes), mené en worktree `.wt-1624` (branche `chantier/1624-decors-ancres`), un seul codeur à la fois, juge de design AVANT brief + juge adversarial sur chaque diff, `npm run gates` (22 gates de `ci.yml`, en lanes depuis T1d) avant push. Régime user : « ne fais pas d'autre tickets, juste fini de bout en bout » (2026-09-04) → toute trouvaille hors lot = commentaire d'inventaire I-n sur #1680, jamais un ticket.

**Posé sur main (2026-09-02 → 04)** : lot 1a/1b, lot 2 `99405cab5`, lot 3 `9967d06b4`, #1507 `30dd38716`, #1509 L1 `17926d5de`+`61d815dc7`, #1509 A/B/C `73dc49eb6`/`68b3a0cd2`/`529d0405b`, I-10 `d0b44a384`/`f72992bfd`, **15-A `49654eb3f`** (Pénalité de Couvert des structures AA ch. 10 en jeu, `src/engine/cover.ts`, `couvertDArete` même étage), **banc météo `7a3595755`** (cause = `restoreMocks` + collecteur rAF en `beforeAll` + `halos-interaction:287` ; primitive `brancherImagesPilotees()` composée par 5 bancs + garde « aucun `beforeAll` ne pose rAF »), **migrations idempotentes `0ce333203`** (no-op = cardinal du geste POSSÉDÉ, 89 migrations × corpus renversé = 0 réécriture, banc `scripts/migrations/lib/idempotence-ordre-des-cles.test.mjs` dans `test:hooks`), cliquet écrivains `f3d23dfed` (CI 33870828452 verte).

**15-B POSÉ sur main — `55e69ce7d` (+ perf IA `cf50eee98`, docs `3575a23b9`, poussé 2026-09-04 soir, CI 33908750067 VERTE)** : opacité DÉCLARÉE par structure (`occulte: z.literal(false)` + `maison`, clayonnage AA 10 l.65 et herse l.70), `areteOcculte`/`areteOcculteEntre` (`scene.ts`), migration `2026-09-04-1680-cloison-basse-en-clayonnage.mjs` (6 séparations de box des Écuries → clayonnage, 4 deltas AA mesurés et mordus), la barrière d'entrée de la cour (19,0-5 E) était déjà en clayonnage ; scénario de test `ecuries-clayonnage` + `__wfrp.visibleCount()`/`walls()` ; recette Chrome (a)(b)(c)(e)(f) conformes. **Le couvert des cloisons de box (+0 clayonnage vs −10 maison) est un sujet que le user NE TRAITE PAS pour le moment — pas un arbitrage** (« Ce n'est pas un arbitrage, vire ça », 2026-09-04) : ne rien consigner comme décision. **Perf IA** (rencontrée en jouant : tour IA bloquant ~2 min) : index d'arêtes `state/wallIndex.ts` (mémo par `scene.walls`, composé par `scene.ts` et `roofs.ts`, verrou au parse `refuseAretesDupliquees`), `makeLosMemo` par décision + invariants hissés dans `ai.ts` → 57 s → 3,2 s / 3 tours, décisions identiques (58/58 contre le `ai.ts` d'avant). Multiplicateur restant TRAITÉ le 2026-09-04 soir (lot IA siège, commit `12a2be8b1`, poussé `9fc4d4af9`, CI 33923435024 verte) : `EncounterDef.siege` (drapeau de rencontre éditable, défaut littéral false), gate unique `buildAiInput` via `siegeActif`, l’IA ne cible plus une structure hors siège (archer : tire sur Aelindra au lieu de marcher vers un clayonnage, 142 → 17 ms/tour) ; cause racine RAW non implémentée = AA 10 l.98 → #1688 (entrée `raw.manifest` impossible : défaut d’Atlas, topic trop gros). **#1624 FERMÉ par commit `15a7b4975` (fermeture à la publication confirmée, 2026-09-05)** (lot 3 recetté, validation user « La planche me va, tu peux valider #1624 ») ; #1644 reste ouvert (non nommé par le user). #1507 fermé (`d608418ad`). Nouveaux tickets du soir : #1686 matières, #1687 décor utilisable, #1688 règle AA 10 l.98.

**Reste** : #13 identité de picking (décision produit) ; fusion des matières (ligne 10, hors ticket) ; validation de GOÛT (#1624/#1644) ; échelle Diligence (#1507 option A). Inventaire ouvert sur #1680 : I-11 (encounter par `setState` → 672 combattants), I-16 (rendu : la teinte de brouillard ne masque rien, intérieur lisible à 0,15 — question user 2026-09-04), I-17 (IA tire sur un MUR en combat non-siège), I-18 (`bodyPlan` sur une structure-combattant), I-13/I-19 (doc de recette).

**Pièges appris (2026-09-04)** : tout ajout à une liste MESURÉE (cliquet `ecrivainsAtteints.test.mjs`, `scenarios-contrat.test.ts`) exige une ligne `CLIQUET: <fichier> +N — motif` dans le commit ; `-F <fichier>` de commit refusé par le hook si le fichier est créé dans la même commande ; `server/node_modules` par worktree (`npm --prefix server ci`) sinon `server:typecheck` rouge ; docs dérivés à régénérer APRÈS le stage des sources (empreinte) ; le `key` du volet Claude_Browser n'émet pas `event.code` (WASD morts) → Playwright `browser_press_key`.

**Passation** : cette fiche + les commentaires de pilotage/inventaire sur #1680 (2026-09-04) ; les tickets ouverts par les décisions du soir : #1686 (matières), #1687 (décor utilisable) ; le lot « IA cible des structures en siège seul » est en cours dans `.wt-1624` (le scratchpad de session s’est révélé instable — fichiers de travail sous `.wt-1624/node_modules/.cache/`).

**Why:** reprise sans relire 15 commits ni 5 tickets. **How to apply:** lire ce résumé, puis `gh issue view 1680 --comments` (derniers pilotages/inventaire) et `TODO-vague-1624.md`. Liens : [[user-regime-une-session-par-chantier-2026-09-01]], [[env-recette-worktree-preview-kit]], [[env-recette-jamais-en-parallele-dun-juge-qui-teste]].
