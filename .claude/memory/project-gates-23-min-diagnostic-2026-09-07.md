---
name: project-gates-23-min-diagnostic-2026-09-07
description: "Chantier 2026-09-07 : les 22 gates locales avant push coûtent 23-27 min (CI : 10 min sur 4 vCPU Linux) — profil mesuré gate par gate et fichier par fichier ; verbatims user : le régime reste, la réponse attendue porte sur les TESTS eux-mêmes, pas sur des caches (« cacher la misère »)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1c0e1f9a-0a0e-4b10-ab45-a52c36d41f83
  modified: 2026-09-07T20:38:28.994Z
---

**Verbatims utilisateur (2026-09-07)** : « AUjourd'hui il faut 23min pour passer les gates permettant de push son travail, c'est beaucoup trop. Et paraléliser ca ne fait de déplacer la charge » · « Tu as le droit de remettre la doute la pertinence de chacun de ses tests et de ce qui la compose » · (après une proposition clés-par-gate / eslint --cache / mémo de hash) « rien sur les tests en eux meme ? ce que tu me propose c 'est cacher la misere » · régime « 22 gates locales avant push » : **inchangé** (réponse à AskUserQuestion).

**Mesures (run série de référence 2026-09-06, worktree, 22 gates = 1 633 s ; CI même contenu = 10,5 min `npm ci` compris ; 10-14 pushes/jour)**
- test:hooks 564 s (384 s seul, fichier par fichier) vs CI 32 s ; test 438 s à 4 cœurs (le lanceur détaché de la fiche garde-mémoire pose `WFRP_TEST_COEURS=4`) / 233 s à 10 cœurs vs CI 316 s ; build 136 s (dont `tsc -b` ≈ 100 s = doublon strict de typecheck, même tsconfig sans `references` ; vite build 31 s) ; typecheck 107 s ; docs:check 105 s (build-all --check hache 21 562 fichiers pour 4 146 uniques, aucun mémo ; check-doc-refs 16 s, check-plans-anchors 14 s) ; lint 80 s (règles 11 s, dont no-unused-vars 5,9 s ; le reste = parsing) ; 8 petites gates node --test ≈ 110 s vs CI 15 s.
- Suite : 1 651 fichiers, 22 526 tests ; CPU tests 1 112 s, setup 234 s (`src/test-setup.ts` : parse de l'état pristine + signature des registres d'art + barrière DOM à CHAQUE test), collect 308 s, transform 96 s ; 41 fichiers = 56 % du CPU, 87 fichiers (>2 s) = 74 % ; 1 125 fichiers < 100 ms, 362 fichiers ≤ 3 tests. Top : scene-field-editability-guard 67 s (ts.Program complet ×5), field-consumers 57 s (2 Programs complets), de-monde-surface 27 s, props-volumiques 27 s, entity-orphans 21 s (spawn du générateur déjà joué par docs:check), structures-contrat 21 s (3 spawns tsx).
- test:hooks par fichier (seul) : pre-push.test 87 s/23 tests, idempotence-ordre-des-cles 48 s (copie `Source/` = 4,7 Go de PDF gitignorés, la CI n'a que 1 009 .md ; les migrations ne lisent que des .md), justificatif.test 42 s, solde-ticket-guard 35 s + driver 28 s, toutes.test 21 s, enregistreur-lectures 21 s (rejoue 3 vrais générateurs), empreinteRejeu 20 s. Fixture typique = `git init` + commits en tmp + spawns node par test ; node démarre en 0,45 s.
- Contexte machine pendant les mesures : 2 autres sessions en vitest (`.wt-1624`, `.wt-1388-A`) + tsserver 3,7 Go, 4,4 Go libres/32 ; exclusions Defender illisibles sans admin.
- Ratio local/CI ×17-25 sur les gates à spawn/git-tmp, ×1,4-2,5 sur le reste.

**Verdicts des 3 juges (2026-09-07, arbre 599979762, lecture seule)** :
- DOUBLONS de gate à supprimer (≈ 80 s) : `field-consumers.test.ts:82` (fraîcheur du .md = docs:check, 2 Programs complets), `entity-orphans.test.ts:138` (spawn du générateur), `migrations-format-projet` 4 `it` NO-OP + `migrations-type-enveloppe` cas B (= `migrations:replay`), `check-code-refs.test.mjs:137,144` (= gate `raw:check-code-refs`), 8 gardes de discipline déjà tenues par `pre-commit.mjs:24-36`/`eslint.config.js:139` (comment-poison, label-logic, combat-hardcode, no-emoji, npm-lock, portable-paths, ownership-lint, roll-seam-exclusivity).
- FIXTURES à réduire (≈ 200 s) : `scene-field-editability-guard` 4 `ts.Program` complets pour `fossileAudit` qui ne lit que 2 fichiers (−25-32 s) ; 52 `git init` par test dans test:hooks → gabarit + `cpSync` (820 → 31 ms, −40 s) ; `idempotence` copie 4,7 Go de PDF jamais lus (−14 s) ; `runner-fast-reminder` 33 spawns pour des prédicats non exportés (−10 s) ; drivers à batcher (−12 s) ; `bakeWorldGeometry` recuit par 15 tests (−25 s) ; `banc-volumique` relit `src/**` 5× (−10 s) ; `props-volumiques` cuit 5× chaque prop (−12 s).
- SCANS : ~130 walkers `readdirSync` écrits à la main dans les tests vs 11 `readCorpus` ; ≈ 136 000 ouvertures de fichier par run pour 2 081 fichiers ; mémo interdit par le commentaire FAUX de `sourceCorpus.mjs:6-8` (« un cache survivrait au worker » — c'est ce que `isolate:false` cherche) → −78 s ; AST partagé (`registry-id-branch-guard` 6,2 s de parse) ; fusions : 4 gardes de pureté → `no-restricted-imports`, 7 quarantaines d'import → une table, 4 gardes CSS, night-stakes ×2, grid+unions-canon, menace-fk, pregen-by-label.
- CARDINAUX VIVANTS : `effets.test.ts:94` `toBe(1118)`, `prop-foot-migration:208` `toHaveLength(18)` sur une campagne ÉDITABLE, `justificatif.test.mjs:290` `noms.length === 22` gates, `solde-ticket-guard.test.mjs:299-328` lit le `.claude/soldes` VIVANT + sha littéral, `field-consumers` asserte des NUMÉROS DE LIGNE, 15 autres listés par les juges.
- PLACEMENT : 690/745 tests de test:hooks tiennent le PROCESSUS (hooks, gates, soldes), pas le produit ; `test:recette` garde un outil d'agent ; 2 tests testent git lui-même.
- GATES QUI MENTENT : `raw:reconcile` ne peut jamais rougir (`reconcile.mjs:365-377`, aucun exitCode) ; `citation-graphy-guard.mjs` a un `main()` fail-closed câblé à rien.
- Total plausible : suite −250 s CPU sur 895 (+ −100 s tsc en double dans build, −14 s PDF, −80 s test:hooks) ; les tests seuls rendent ~6-7 min sur 27 — le reste est structure (4 cœurs, hashing docs:check, spawn Windows).

**Épique #1709 (2026-09-07)**, lots A-F ; train A (doublons de gate) dans `.wt-gates-A` (branche `gates/A`). ⚠ L'ARBRE PRINCIPAL était 74 commits derrière `origin/main` (2998720aa) avec 1 commit local (599979762, mémoire) : tout worktree se crée sur `origin/main`, jamais sur `main` local — vécu : le train A a dû être rebasé avant ses gates.
**Design E jugé (RÉFUTÉ tel quel, alternative minimale retenue)** : la clé par gate dérivée de `ECRIT_LU.lit` est un FAUX VERT (la suite lit `Source/`, `.claude/memory/`, `.gitattributes` sans les déclarer ; 9 gates sur 22 lisent hors `lit` ; les gardes de vérité statique/dynamique sont aveugles aux lectures médiées par git et aux listes dérivées). Faux vert DÉJÀ VIVANT : la gate `test` est sous clé PARTIELLE (hors `.claude/`) alors que `src/memory-links-guard.test.ts:79-82` scanne l'arbre réel de `.claude/memory/` (sonde : deux commits mémoire seuls ont la même clé que leur parent). Retenu : (1) rebaser sur origin/main (le socle `justificatif.mjs`/`pre-push.mjs`/`toutes.mjs` a été réécrit : `clesDeContenu`, `RAISON_CLE_COMPLETE`, nom de fichier `<gate>.<cle>.<propre|sale>.json`, verrou machine sur tout `npm run gates`) ; (2) sortir le scan `.claude/memory/` de la suite vers `test:hooks` ; (3) scinder `test:hooks` — `idempotence-ordre-des-cles` + `empreinteRejeu` → job `migrations` (déjà hors justificatif, joué au pre-push sur export), `enregistreur-lectures` → `test:docs` ; (4) 3ᵉ valeur de clé NOMINATIVE « outillage » (`scripts/` + `.claude/` + socle) pour test:hooks, test:runner, test:agents, test:recette, agents:check, avec une garde `lit ⊆ scripts/ ∪ .claude/ ∪ SOCLE` ; (5) corriger `ECRIT_LU.lit` là où la sonde l'a prise en défaut (`test` : Source/, .claude/memory/, .gitattributes ; raw:check-refs/folio/reanchor : src/data/books.json ; docs:empreinte : l'index de ses sources ; test:ops : .claude/soldes). Ne PAS généraliser la clé par `lit`. Lentille manquée : le MUR par lane (en lanes seule test:hooks compte ; en `--serie` chaque gate compte).

**CONTENTION = premier facteur (mesuré 2026-09-07 ~10 h 30)** : gates série du train A dans `.wt-gates-A` sur une machine CALME (11,7 Go libres, aucune autre suite) — `test:hooks` **28 s** (contre 564 s dans le run de référence et 384 s fichier par fichier le matin, avec deux sessions en vitest + tsserver 3,7 Go), deps:unused 18 s (43 s), test:docs 3,5 s (25 s). Les tests à spawn/git-tmp sont multipliés ×15-20 par la charge d'autres sessions — le « 23 min » est d'abord un problème de sessions PARALLÈLES sur une seule machine (l'utilisateur l'avait dit : « paralléliser ça ne fait que déplacer la charge »). Conséquence : toute mesure de gate se note AVEC l'état de la machine ; les gains des lots B/C se re-mesurent sur machine calme avant d'être annoncés.

**État 2026-09-08 ~20 h 40** : seize trains POUSSÉS, CI verte jusqu'à D0d `26340a52a` (D2 `5756d2d2c`, B3 `2639287cd`, D0c `fd660c8b8`, D0d `26340a52a` — le stock des tests-photographies de scènes utilisées est ÉPUISÉ hors STOP nommés : les trois scénarios de test bâtis sur la Diligence, `ecuries-clayonnage:101 > 600`). C3b-2 (pureté de couche → `eslint.config.js`, trois tests vitest supprimés, filet `src/eslint-purete-de-couche.test.ts`) COMMITTÉ `c4a0854cf` sur 26340a52a dans `.wt-gates-B1` (branche `gates/C3b2`), gates en attente du verrou de suite (autre session `.wt-1624`). PIÈGE VÉCU — deadlock des noms de revue de palier : la revue de PALIER (13 commits de substance depuis 6382c792d, verdict PARTIEL, actions α-ε) est écrite sous `.wt-gates-B1/.claude/soldes/revue-palier-2026-09-08-6382c792d-fd660c8b8.md` (NON TRACKÉE, à committer) ; le hook PreToolUse du harnais exécute `solde-ticket-guard.mjs` de l'ARBRE PRINCIPAL (3fa2878d8, ancien : nom `<date>-<base>`) alors que le pre-commit du worktree exige `<date>-<base>-<tête>` → toute fermeture est refusée par l'un ou l'autre. CONTOURNEMENT appliqué : `corrige #996` → `refs #996` (la garde ne mesure le palier que sur une FERMETURE, `issues.length === 0 → null`) ; la fermeture de #996 et la revue partent dans un commit dédié dès que l'arbre principal est à jour. DÉCISION USER EN ATTENTE : l'arbre principal `main` porte 2 commits locaux non poussés d'autres sessions (3fa2878d8 « fix(coop): resynchroniser… », 599979762 mémoire) et ne peut pas avancer en ff — à rebaser/pousser ou abandonner ; tant qu'il reste là, aucun commit qui FERME un ticket ne passe depuis un worktree. Autres décisions en attente : #1713 (export des issues refusé par le pre-push), #1712 (fenêtre opaque), défauts de plan Diligence/échoppe Arène, hors-bornes bloquant ?, spawn marchable ?. SUITE : push C3b2 → CI → pilotage v11 → train E (`brief-E.md` prêt) → C3c (54 walkers) → T3 → #925 → actions du palier.
**État 2026-09-07 22 h** : POUSSÉS (CI verte) C1 `41ed2c93a`, C2 `6382c792d`, C3a `8aee8a926` (walkers restants 72 → 54), F `a474ca986` (`build` = `gen && vite build`, 78 → 20 s, `deploy.yml` joue `typecheck` avant), D1 `c5bfa9996` (détecteur de stock : un argument n'est exempt que s'il ne nomme AUCUN fichier — la première frontière « argument ≠ porteur » a été RÉFUTÉE par 7 façades d'une ligne ; 3 variantes mesurées sur l'inventaire 2 679 entrées), C3s `056d287b8` (`readCorpus` refuse le corpus vide PAR BASE — un total agrégé laissait une base vide se cacher ; `viderCorpus()` en `finally` REFUSÉ sur mesure : relâchement total + identité d'AST perdue, bénéfice nul). D0 POUSSÉ `c9606d110` (6 cardinaux → propriétés ; preuve = le SUJET dévié rougit, l'ajout légitime reste vert — « donnée amputée → rouge » est FAUX et c'est correct ; `.claude/soldes` : les 4 archives réelles et `revues.length >= 10` supprimés, l'écrivain valide à l'écriture ; hors lot même classe : `solde-ticket-guard.test.mjs:2038` lit `584.md`). D0b après l'arbitrage user [[user-arbitrage-tests-sur-scenes-dediees-jamais-sur-scenes-utilisees]] — design jugé : le socle de connectivité EXISTAIT (`mapQC.ts` #778, câblage dans `validateScene`), `planDefects` = porte orpheline seule, `scenePlanDefects` non mémoïsé (68 ms à chaud) ; T1 POUSSÉ `0ae41122f` (`spawnEnemy` consomme `refEntiteResolue`, cascade réordonnée avec équivalence prouvée par le juge sur 9 entrées ; « départ absent » = niveau PROJET `warn` sur `places[].scene` sans `entry` ∪ `poi.sceneId`, le runtime replie) ; T2 POUSSÉ `56182764e` (les deux tests nominatifs supprimés, garde générique `bundled-projects.test.ts` : 0 error + prose sourcée non blanche ; skill de campagne recalé — un paquet neuf n'écrit AUCUN test) ; D2 POUSSÉ `5756d2d2c` (`raw:reconcile` rougit : stock nominatif +13, famille `*-stock.json` en porteurs, LDB tolérance zéro dans la gate ; #925 commenté : 19 s = re-scan regex ×6 ; #1711 ouvert : baselines de compte) ; B3 POUSSÉ `2639287cd` (props 12,6 → 2,1 s — cause = échelle de l'Opéra par entité ; bake mémo canonique ; plancher chiffré RÉFUTÉ → non-vacuité par gisement ; gabarit de dépôt REFUSÉ à la mesure 374 → 688 ms) ; D0c POUSSÉ `fd660c8b8` (mobilier-spatial, barge, loup supprimés ; fixtures + 8 gardes génériques ; preuve édition anodine 501 verts ; #1712 : fenêtre dessinée mais opaque, décision produit) ; C3b-2 en cours (`.wt-gates-B1`, gates/C3b2) ; reste D0d (bake-compte/worldTris rouges sous édition anodine, places-ancre-decor, worldMap-gating, vision MPT_MER, ecuries > 600, 3 test-scenarios, 27 fichiers à trier), T3, E, 54 walkers ; questions de design nommées : hors-bornes bloquant ? case de spawn marchable ? ; **PRÉMISSE FAUSSE du juge de design** : les « 3 heroStart non marchables » étaient les 3 scènes d'échelle MER (`isMerScene`, 10 m/case, grille d'eau) — garde d'échelle posée, sinon 28 avertissements faux ; défauts RÉELS livrés : Diligence 8 défauts de plan (`etage-sur-exterior` ×4, `etage-sans-appui` ×4), Arène `arene-hub`/`piece-echoppe` inatteignable — à instruire avec l'auteur, jamais par un agent. Briefs prêts : `brief-D0b-T2.md` (garde générique dans `bundled-projects.test.ts`, suppression des `it` (C)/(D), prose au Source généralisée sans numéros de ligne, table de couverture des vitrines → T3). Pilotage v10 posté sur #1709 (quinze trains poussés). Juge de design C3b : fusions quarantaines/night-stakes/grid+unions/pregen→label RÉFUTÉES ; pureté → lint TIENT sous conditions (engine/state/gameIso, pas data hors périmètre eslint ; doubler par `no-restricted-syntax ImportExpression` ; filet de config résolue) ; vrai doublon `chapterLines` ×2 + 10 tests `data-folio` → primitive `chapitreVerbatim`. Mesure suite (run C2) : CPU tests 837 s / collect 196 / setup 166 / transform 55, wall 125 s → coût marginal d'un fichier ≈ 0,25 s + ses tests. Trois worktrees : `.wt-gates-A` (gates/F), `.wt-gates-B1` (gates/C3s), `.wt-gates-D` (gates/D1). Pièges vécus : `sed -i 's|…|…\\U…|'` uppercase le remplacement (GNU sed `\U`) ; `docs:build` AVANT les gates sinon run perdu ; origin/main bouge par d'autres sessions → re-fetch avant chaque lancement de gates. Rebase avec conflit : `git -c core.editor=true rebase --continue` (le `$env:GIT_EDITOR=` est bloqué par l'allowlist).
**État antérieur 17 h 30** : B1 POUSSÉ `429d96cc3` (gates 22/22 en 450 s ; 126 fabrications → 32 gabarits). Piège vécu : le garde pre-commit `stocks-nominatifs` et le détecteur de PLAGE (`croissancesDeLaPlage`, test:hooks + pre-push) comptent DIFFÉREMMENT le même diff (+3 vs +2, et 3 fichiers vus par la plage seule) → écrire les `CLIQUET:` au compte de la PLAGE, sinon un run de gates perdu ; les `fichiers: {…}` de fixtures sont classés « stock » par forme.
**État antérieur 15 h 45** : train A POUSSÉ `4c07daf16` (349,8 s), train B2 POUSSÉ `1e63ea51c` (392 s, −28 s sur 3 fichiers ; le juge a RÉFUTÉ le retrait de la régénération dans quad-harnais : écriture conditionnelle de `compile-dessin-quad.mts:189`). B1 (gabarit `depotGabarit.mjs`, 126 fabrications → 32 gabarits, un seul `git init` restant) : cumul jugé, corrections B1c en cours dans `.wt-gates-B1` (fuite sur exception/SIGTERM, hooksPath relatif, refus nominatif refs+commit:false, ECRIT_LU test:ops « sept », principal de fixture propre, assertion vacue refs/stash → `--git-common-dir`). Restent C, D (+ `fossileGate` inerte : `scene-field-editability-guard.test.ts:400`), E alternative minimale, F. Worktrees : `.wt-gates-A` (gates/B2 poussée ; branches gates/A, gates/A2 à supprimer), `.wt-gates-B1`. Leçons : `git commit` par `-F <fichier>` (les `-m` à parenthèses cassent le parseur PowerShell) ; le garde de solde exige `JUGE:` et `REFUTATION:` sur tout commit `src/**` ≥ 10 lignes ; le poison-guard refuse « shim retiré » ; un rebase régénère les pieds d'empreinte → `--amend` sur la branche de worktree non poussée.

**How to apply :** la réponse attendue est un audit de PERTINENCE test par test (nature produit/doctrine/outillage, où part le coût, régression réelle attrapée, redondance avec docs:check/typecheck/hooks/CI migrations, verdict garder/réduire/fusionner/supprimer) — pas des caches. Trois juges dispatchés le 2026-09-07 (vitest >5 s ; gardes scannant le dépôt ; outillage node --test) ; résultats à consigner ici puis ticket. Mesures brutes : scratchpad de la session (mesures.log) et `node_modules/.cache/gates/` du worktree `agent-a56ccbe6a53e71ecb`.

Lié : [[env-garde-memoire-harnais-gates-serie-detachees]], [[feedback-train-court-codeur-rend-apres-gates]], [[feedback-gates-docs-build-avant-lancement-sinon-25-min-perdues]], [[feedback-test-jamais-un-cardinal-vivant-ni-un-magasin-partage]], [[feedback-perf-profiler-avant-design-cache]].
