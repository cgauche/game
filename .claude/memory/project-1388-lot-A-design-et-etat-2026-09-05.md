---
name: project-1388-lot-a-design-et-etat-2026-09-05
description: "Épique #1388 « Un texte, trois fenêtres » reprise le 2026-09-04 (« Ok, je te laisse prendre cet épique ») : design du Lot A #1389 jugé deux fois — la prose adressée (`descRef`) est MATÉRIALISÉE AU BUILD par un plugin Vite (114 lecteurs synchrones de `desc`, moteur pur compris, rendent toute résolution au runtime impossible sans casser N+1 = une ligne) ; parseur TS pur `src/data/source/decoupe.ts` chargeable par Node nu ; stock de campagne par type avec comptes ; état des commits C0-C6 et pièges mesurés"
metadata: 
  node_type: memory
  type: project
  originSessionId: 604f0acf-d510-47bf-9ed4-b120e221c214
  modified: 2026-09-07T01:51:33.418Z
---

**Reprise** : 2026-09-04 soir, verbatim utilisateur « Ok, je te laisse prendre cet épique » (après « On a un ticket ou une épic lié a reformater la source entre autre non ? »). Session unique, worktree `.wt-1388-A` (détaché sur `origin/main`), pilotage réécrit sur #1388 à chaque étape (v1 = reprise + métriques, v2 = design). RE-MESURE postée sur #1389 #1384 #1390 #1391 #1392 #1393 #1228 #566 le 2026-09-04.

**Design du Lot A (jugé v1 « À CORRIGER » 9 points, v2 « À CORRIGER » 8 points locaux, v3 = amendements ; fichiers de travail du scratchpad `design-lotA-v2.md` + `design-lotA-v3.md`, résumé au pilotage v2 de #1388)** :
1. Adresse `descRef = { book, ch, parts: 1..3 × ({kind:'blocs', sec, secOcc, b0, b1, sum} | {kind:'cellule', sec, secOcc, row, col, sum}) }`, `sum` = 64 bits FNV-1a à DEUX sels (un sel unique rend deux moitiés identiques), verrous structurels au parse (plafond, même chapitre, clés jamais des indices).
2. UN helper de grammaire `grammaire/prose.ts` (`champsProse`, `refineProse` V1 exclusivité · V2 résolubilité `descRef.book ∈ EXTRAITS` (dérivé de `books.json.dir`, aucun champ « sans-extraction ») · V2b cohérence `source.book === descRef.book` · V3 non-régression (desc inline sur livre extrait admise seulement si le type est au stock) · V4 obligation) composé par `enveloppe()` de `document()` AVANT l'étape `variantes` (`document.ts:403-406`, sinon spells/talents/traits lèvent) ; `exiges: ['desc']` garde son sens « prose obligatoire quel qu'en soit le porteur » ; `descRef` ∈ `CLES_ENVELOPPE` et `NON_EXIGIBLES`.
3. Stock `PROSE_INLINE_TOLEREE` (type → compte), patron `structuresStock`/`structures-contrat` (PAS `sans-livre.ts` : lui n'a pas de comptes), masque « desc non vide dont la source EFFECTIVE (propre sinon ancêtre) est un livre à `dir`, maison inclus, deux racines, toute profondeur » — baseline mesurée 2 550/49 types sans maison ni héritée (+32 maison, +43 source héritée dont rencontres-edoc 26) ; le refine V3 ne mord au parse que sur les nœuds à source PROPRE, le contrat compte tout.
4. **MATÉRIALISATION AU BUILD** : plugin Vite `proseSource` `enforce:'pre'`, `transform` sur `/src/data/*.json` (hors `?raw`) injecte `desc` résolu, `this.error` fail-closed ; HMR par `configureServer` + `watcher` + `moduleGraph.invalidateModule` + `full-reload` (`addWatchFile` est INOPÉRANT sur un module JSON : `vite:import-analysis` saute les `.json`, mesuré) ; assets `source/<livre>/<NN>.md` + manifest (312 chapitres, 11 Mo) pour l'éditeur et la fenêtre LIVRE ; `versDisque` DANS `datasetSerializeRoot` (`overrides.ts:415`) avant `validateDataset` de `CodexEdit.tsx:577` ; `dev-validate.ts` en `?raw` ; `src/scenes` HORS transform au Lot A (`parseProject` au boot). Invariant « hors Vite = forme disque » : tout consommateur Node de prose passe par `resoudreProse`/`materialiser`.
5. Gardes : `prose-resolution.test.ts` (A résout, B empreinte, D montage, E maison-à-livre-extrait non EXACT) ; la preuve folio reste `folioIntegrity` nourri du texte résolu (`citedEntriesOf`), plafonds 109/776/0 à MARGE ZÉRO invariants ; volet « folio de la découpe valide source.page » REPORTÉ au Lot C (explicitement, ticket) ; `psychology-verbatim.test.ts` meurt en C5.

**Pourquoi la matérialisation** : le juge v1 a compté 114 lecteurs synchrones de `.desc` dans 46 fichiers (`src/engine/trauma.ts:39` au niveau module, `qualities/describe.ts:14`, `critical.ts:347`, `items.ts:273`, `tavernGame.ts:160`, UI sync) — une résolution au runtime (fetch + états) casserait le moteur pur famille par famille ; le design de l'instruction du 18/08 (fetch paresseux + `resolveProse` async) était donc faux sur ce point.

**Pièges mesurés** : Node 22.20 importe un `.ts` nu si ses imports relatifs portent leur extension (`allowImportingTsExtensions: true`, STDERR vide) ; `scripts/migrations/replay.mjs:180` rejoue sous `process.execPath` sur un export sans `node_modules` (pas de tsx) ; `eslint.config.js:50` ignorait tout `src/data/**` — dé-ignorer exige `'src/data/**/*'` + négations (`'src/data/**'` + négation ne marche pas) ; `_lib.mjs` ré-exporte `normalize` depuis `src/data/source/normalize.ts` ; `hash32` vit en `src/data/hash.ts` (12 sites) ; 769 blocs dupliqués au LDB (pas 773).

**État (2026-09-06 ~02:00)** : C0 spike + **C1 parseur TS POUSSÉS (b37bf8520)** · **C2 POUSSÉ (main = 714df53da)** · **C3 plugin Vite POUSSÉ (main = de8424de5 : 7e94518cb feat + 2 commits de docs post-rewrite, gates 22/22)** — le push avait été refusé une fois par main ROUGE sur 1658c946a (#1686 : `grammaire.test.ts:1038` mutait `IDS_PAR_DATASET`, shadowé par `idsVivants` selon l'ordre des tests ; diagnostic transmis, correctif porté par audit-drift en 627451282) · **C4 POUSSÉ : main = 2998720aa (2026-09-07 ~03:45, 49 fichiers, +2 901/−140, gates 22/22 en 425 s ; pilotage v6 posté #1388 issuecomment-5563884416 ; #1689 et #1698 commentés — « seg → T4 » livré, angle mort `scanRefusMuet`)** — garde `prose-resolution` (A/B/D, règle D = fragments `blocs` seulement, `fragments-chevauchants`), `citedEntriesOf` sur texte résolu (14 verdicts folio identiques), `DescRefField` (5 recettes, 7 tours codeur, 6 juges), `OptionChooser` seg à refus en 3e instance de l'`OptionBouton` de #1689 T2 (rebase, 6 sites migrés, `REFUS_MUET_EXEMPT_SITES` 13 → 7), `datasetObjectSerializeRoot`, kit de recette enrichi. **FAIL-CLOSED décidé par le juge final : aucun asset `source/**` au build de production** (les 16 livres VF, 11 Mo, auraient été publiés sur Pages) — chapitres et manifeste servis en DEV seulement ; **QUESTION à l'utilisateur au pilotage v6 : publier le corpus sur le site public ?** Le hook de commit exige désormais une ligne `REFUTATION: <qui a attaqué quoi, ≥ 40 c>` pour tout `refs #N` touchant `src/**` (sinon `.claude/soldes/ref-<N>.md`) · C5 pilote psychology · C6 `reparer-adresses` + solde → `corrige #1389`. Pilotage v5 posté sur #1388 (issuecomment-5557389374). Leçon de train : committer les docs post-rewrite APRÈS le dernier rebase seulement (deux rebases = deux commits de docs).

**Lot G #1699 (2026-09-06, demande utilisateur relayée par game-30)** : « Tu sais nos fichiers aujourd'hui peuvent etre déplacé, par contre les caracteres accentués c'est un soucis » — 348/1 503 fichiers de `Source/` non ASCII cassent `git ls-files` (quotePath) et les outils tiers → renommage en slugs ASCII, train dédié APRÈS le Lot A et AVANT le Lot B, garde tolérance zéro ; les adresses de prose et les réfs RAW ne citent aucun chemin.

**C4 en boucle recette/juge (2026-09-06)** : 5 tours codeur (brief-C4 → corrections → recette-corrections → ter → quater → quinquies), 4 juges de delta et 4 recettes. Ce que la boucle a appris : (1) un champ neuf se RECETTE avant tout juge de « fini » — la première recette a trouvé le 360 px, les codes moteur bruts, le fragment né en erreur ; (2) deux passes sur la même classe = trou de PRIMITIVE (`OptionChooser` seg sans `refus` → composer `GatedAction bare`, migrer les 6 sites, régime muet refusé par le type) ; (3) `display: contents` sur l'enveloppe `CodexRef` casse le placement de l'infobulle (rect 0×0) ; (4) une heuristique de texte (« jeton minuscule ») se remplace par la structure (ligne brute du heading) ; (5) un `poser` qui rescelle tous les fragments efface une divergence en silence — ne sceller que le fragment muté ; (6) `min-width: auto` n'est un plancher que sur l'axe principal — mesurer le CSS aux deux hôtes (galerie ET Codex) avant d'écrire un commentaire ; (7) `vi.resetModules` + réimport de React = 65 s / 17 cas et flaky — préférer `vi.mock` du module de cache. Kit de recette enrichi au fil des recettes (`selectOption`, `clickButtonByText {exact, dans}`, `openApp {timeoutMs}`, `champParLibelle`, `verdictDebordement`, `evaluerFn`, `attendreSelecteur`, `shot {ancre}`).

**Volet E ABANDONNÉ (C4, 2026-09-06, juge de diff)** : le design v3 §Am.4 posait « maison à livre extrait, desc jamais retrouvée EXACT, baseline 0 » — mesuré FAUX : 32 nœuds au périmètre, 14 retrouvés EXACT (activities ×6 VDM, arcane-phenomena testMods ×1, naval-traits ×2 MSRC, talents ×3, traits ×2). Ma décision « stock nominatif décroissant des 14 dans la garde » a été RÉFUTÉE : (a) ces 32 sont DÉJÀ au dénominateur décroissant de `PROSE_INLINE_TOLEREE` (« `maison` ne dispense pas ») → second stock = doublon ; (b) l'en-tête de `prose-resolution.test.ts` déclare tolérance zéro sans stock ; (c) le défaut réel est une ÉTIQUETTE `maison` fausse sur du verbatim (credo house-rule ≠ lacune) → correction = retirer `maison` ou adresser, à la migration de la famille (Lot C #1390, liste nominative au ticket). Masque mesuré : 174 nœuds `maison`, 50 avec desc, 32 à livre extrait (0 hérités), 1 livre non extrait, 17 sans livre = 18 invérifiables. Leçon : avant d'ajouter un stock, vérifier qu'un stock existant ne compte pas déjà les mêmes nœuds.

**Décision YAGNI C3 (juge de diff, 2026-09-06)** : l'infrastructure « l'adresse EST l'URL » (assets `source/<livre>/<NN>.md` + manifeste = 12 Mo qui doubleraient `dist/`, middleware de dev, `chargerChapitre`) n'avait AUCUN consommateur → retirée de C3, reposée en C4 AVEC `DescRefField` (patch `scratchpad/patch-C4-adresse-url.diff` ; manifeste en TABLEAU ordonné — un objet à clés de chiffres est réordonné par JS). Autres corrections C3 : `materialiser` ignore un `desc` porté par un nœud adressé (symétrique de `versDisque`) ; test de forme disque sur racine SYNTHÉTIQUE (jamais `setDataset`) ; `resoudre.d.mts` supprimé (convention unique `@ts-expect-error` de `vite.config.ts`) ; `oublierChapitre` exporté par `lecteur-fs.mjs` (aucun cache dupliqué) ; `cheminChapitre` rend un chemin RELATIF (clé de l'index HMR, forme que chokidar émet). Écarts portés : 2e porte de save `datasetObject` (`CodexEdit.tsx:576`, fail-closed) → C4 ; `entreePartielle` + scènes hors transform (adresse posée en scène = prose absente en silence) → #1390.

**Régime de push appris (L3 #1679, 2026-09-05)** : la porte `stocks-nominatifs` juge la PLAGE POUSSÉE commit par commit — toute croissance d'un « stock » (littéral de module nommant des chemins dans `scripts/guards/lib/**`, tests, JSON de hooks — les descripteurs de configuration et l'en-tête GARDE d'un test comptent) se DÉCLARE au message du commit fautif par `CLIQUET: <fichier> +N — motif` avec le N EXACT (mesuré : +2 refusé quand +3 déclaré) ; un stock hors périmètre (`src/data/schemas/grammaire/prose-inline.ts`) n'est pas vu — écart consigné chez #1679 L3b. Historique privé d'un worktree : regrouper par `git reset --soft origin/main` + recommit pour porter les déclarations (l'arbre identique conserve les 21 autres justificatifs, keyés par contenu). Script de train `train-pousse.mjs` (scratchpad) : fetch → rebase → docs post-rewrite → gates → push, second essai si le tronc bouge. Message de commit dès C3 (porte `evaluateJuge`, `solde-ticket-guard.mjs:1317-1364`) : ligne `JUGE: <verdict + corrections, ≥ 40 c>` pour tout commit `refs #N` touchant `src/**` de ≥ 10 lignes, et `JUGE-VISION: …` si un fichier d'écran est touché (« aucun rendu modifié — import seul » plutôt qu'omis). Session SUSPENDUE ~5 h 42 le 2026-09-05 matin (tour figé « Effecting »), débloquée par Échap injecté depuis la session voisine.

**Détecteur #1508 corrigé dans ce train (6e37b75ab)** : `scanDesHorsPorte`/`engineDiceRollers` comptaient tout `.int(` comme `rng.int` → 14 `z.number().int()` de schéma baselinés comme dette « F → #1508 T6 » (lot sans matière) + les 4 bornes de `descRefSchema` rouges. Critère structurel `estAppelDeDe` (≥ 1 argument non-chaîne, `RNG.int(min, max)`), stock (X) 319 → 305, feu vert du propriétaire (#1508 T2 en juge, aucune ligne commune), consigné sur #1508. Briefs C1-C6 dans le scratchpad (`brief-C*.md`, volatils — le pilotage #1388 v3 porte l'essentiel). Puis Lot B #1384 (garde de tables suspectes : 117/217 sous définition écrite, jamais 84) et Lot E #1392.

**Leçons du train C1** : `export { x } from '…'` ne lie PAS le nom local (un module qui utilise `x` lui-même doit l'importer puis le ré-exporter — `_lib.mjs` plantait sur `findAnchor`, chemin hors des 311 tests de `test:raw`) ; gates en LANES : les fixtures de `lintStage.test.mjs` (lane types) salissent le justificatif de `build` (lane docs) → pre-push refusé → rejouer `npm run gates -- --serie` (seules les gates manquantes rejouent, ~2 min) — défaut pris par #1679 L3 T5 ; le harnais tue les enveloppes Bash de fond « low on memory » pendant une suite (le job ctx_shell survit) → attendre par un script node via `Monitor`.

**Why :** une reprise doit retrouver le design jugé et ses raisons sans rejouer 2 M tokens de juges. **How to apply :** relire le pilotage de #1388 (source de vérité), ce fichier pour les raisons, les briefs du scratchpad s'ils survivent.
