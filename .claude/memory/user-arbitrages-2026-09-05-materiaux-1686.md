---
name: user-arbitrages-2026-09-05-materiaux-1686
description: "Quatre arbitrages user du 2026-09-05 (+ quatre décisions d'ingénierie du lot 3, déduites des doctrines, NON user) sur le chantier #1686 (matières du monde en UN dataset) : terrain HORS matières (ticket TS → terrains.json), structureAppearance HORS (composite qui référence des matières), homonyme ardoise = ids COMPOSÉS, onglet Codex « Matières » par domaine ; puis (lot 3) recalcul des ids en mémoire dans #1686, socle MetaChamp.valeurs + migration des Records, pastille sous Monde + 3 groupes, purge du champ mort structureAppearance.material. Le discriminant est `domain` de MaterialRef, pas un `domaine` neuf."
metadata:
  type: user
  originSessionId: 4407a64f-b0ad-4d3d-b30f-ffca252025d6
  modified: 2026-09-05T05:58:27.653Z
---

Questions posées par AskUserQuestion le 2026-09-05 après le juge de design (options = assertions chiffrées), réponses verbatim :

1. **Terrain** — « un terrain est une ENTITÉ de jeu (marchable, opaque, priorité, cible de clés étrangères des scènes) qui A une matière ; le scinder ferait coûter deux fichiers à l'élément N+1 » → **« Hors matières, ticket TS → terrains.json »** : le registre TS de 25 terrains (`src/state/terrain/defs/<id>.ts` — 24 est FAUX : mesuré 25 fichiers, #1690 lot 2, 2026-09-06) passe en donnée dans son propre chantier (entité = une entrée, règle + rendu ensemble) ; #1686 ne fusionne que les catalogues de matière pure.
2. **structureAppearance** (18 entrées : herse, parapet, fenêtre, 16 saillies, `material` = bois|pierre) → **« Hors du dataset, il RÉFÉRENCE des matières »** : `materials.json` = 3 domaines (prop, roof, relief), `structureAppearance.json` survit comme composition et consomme des matières.
3. **Homonyme `ardoise`** (prop : couleur + PBR ; toit : 4 faces + assises ; aucune garde ne l'attrape, tie-breaks divergents mesurés) → **« Ids composés, une seule graphie »** : `prop-ardoise` / `toit-ardoise`, grammaire inchangée, migration des références de scène.
4. **Éditabilité** → **« Onglet Codex « Matières » par domaine »** : les 38 champs de charge reçoivent leurs libellés FR (garde des libellés mesurée verte) ; une matière s'ajoute et se retouche au Codex.

Faits du juge de design qui portent ces choix : le discriminant existe déjà (`MaterialRef.domain`, `src/gameIso/builders/types.ts:33-37`, 5 valeurs `terrain|relief|structure|roof|prop`, `faceColors.ts:88-118` switche dessus) — le dataset fusionné est la CIBLE de cette référence ; charge commune = ∅ (seuls `id/type/label` d'enveloppe ; `detail` déjà mutualisé en grammaire `valeurs.ts:208`) ; forme = entrée PLATE + refine par domaine (patron `oups.ts:7-11`), pas une union ; aucun renommage de couleur (`color`/`face`/`N,E,S,O` ne sont pas la même forme, `faceColors.ts:14-26`) ; `plan` n'est pas une couverture (`faceColors.ts:56-62`, `validateScene.ts:22` l'admet à tort) ; `riser` et `sol-inconnu` sont morts (0 consommateur de production).

5. **Rappel transversal (2026-09-05, deux verbatims)** : « Malgres mes choix, ne pas oublier que rien ne doit etre en dure, tout doit etre éditable. » puis « datadriven » → aucun des quatre choix n'autorise du dur : le terrain sorti des matières va en JSON éditable (c'est son ticket), `structureAppearance.material` (énumération fermée `bois|pierre`) — glose du rédacteur RÉVISÉE le soir même : le champ a 0 lecteur au runtime, le user a tranché sa PURGE (point 9), `plan` reste une donnée (domaine propre, jamais une constante), les 38 champs portent leurs libellés/métas au Codex. Règle stricte 2 et credo « rien de hardcodé » priment sur toute lecture restrictive d'un arbitrage.

**Points 6-9 : DÉCISIONS D'INGÉNIERIE, pas des arbitrages user** (2026-09-05, AskUserQuestion après le juge de design du lot 3 — le user a cliqué « Recommandé » et a dit, verbatim : « Je ne fais pas vraiment de choix, j'ai juste cliqué sur "Recommander", tu connais les grands principes de l application »). Elles se déduisent des doctrines écrites (rien en dur/tout éditable ; jamais de demi-migration ; primitives existantes avant socle neuf ; zéro code mort) et restent RÉVISABLES par la mesure — cf. [[feedback-ne-pas-faire-arbitrer-un-fait]] corollaire 3 :

6. **Référence vivante** — « `IDS_PAR_DATASET` figé au `npm run gen`, aucun recalcul en mémoire câblé » → **« Recalcul en mémoire, dans #1686 »** : le 2ᵉ régime décrit en tête de `_ids.generated.ts` (listes d'ids recalculées depuis les datasets en mémoire au `validateDataset`) se câble DANS #1686, ~50 lignes de socle, pour les 80 datasets et les 107 sites `ref` — une matière créée au Codex est référençable tout de suite.
7. **Libellés FR des valeurs d'un enum** (`MetaChamp` ne porte que label/hint/widget/ordre ; ~20 `Record`/ternaires côté UI) → **« Socle `MetaChamp.valeurs` + migration des ~20 Records »** : un champ de socle dans la grammaire, lu par le groupe Codex ET par le `select` du formulaire dérivé ; les ~20 `Record` existants MIGRENT dans le même train (jamais de demi-migration).
8. **Forme de l'« onglet »** → **« Pastille « Matières » sous « Monde », 3 groupes titrés »** : catégorie `materials` dans le `CodexGroup` existant, items groupés Décor/Toiture/Relief par `CodexItem.group` (patron `mutations`), 0 changement de socle d'onglets.
9. **`structureAppearance.material`** (0 lecteur runtime, 18 entrées 10 bois / 8 pierre, aucun id `bois` au dataset, `pierre` = matière de RELIEF) → **« Purger le champ mort »** : 1 champ de schéma, 18 entrées, 2 lignes de type ; le composite garde sa palette propre ; « il RÉFÉRENCE des matières » se fera quand un consommateur existera. La lecture (b) « le domaine `structure` entre dans `materials.json` » est MORTE : elle contredit le verbatim « Hors du dataset ».

**Why:** ces choix fixent le périmètre de #1686 (3 domaines, 16 entrées à l'arrivée) et ouvrent un ticket terrain. **How to apply:** ne jamais reposer les quatre arbitrages (1-4) ; les décisions 6-9 se réévaluent aux doctrines, pas à l'utilisateur ; tout brief de #1686 cite `MaterialRef.domain` comme source des valeurs de `domain`. Liens : [[user-direction-fusion-json-par-type]], [[feedback-finalite-1463-mutualiser-les-divergences]], [[game-vague-de-stock-lecons-2026-09-01]], [[project-1680-socle-volumique-etat-2026-09-03]].
