---
name: game-refonte-rendu-builders-backends
description: "La couche rendu a été refondue en « une scène, une apparence, N projections » — pivot + builders purs + 2 backends ; IsoStage n'est plus un monolithe."
metadata: 
  node_type: memory
  type: project
  originSessionId: bf8496da-2edb-4edf-94d4-2db7303b331d
---

> ⚠ **PÉRIMÉ EN PARTIE (2026-08-14, #1176 C5a/C5b)** : la voie AFFINE décrite ici est MORTE — le moteur de jeu est three.js volumique (`backends/webgl/`), le POV SVG est supprimé, et les peintres SVG survivants sont requalifiés `src/gameIso/authoring/` (aperçu éditeur, plan, oracles de parité). Voir [[game-audit-moteur-rendu-2026-08-09]]. Les principes DONNÉE (apparence JSON, builders purs, pivot `SceneEl`/`Face`) restent la loi ; les sections backends/QC ci-dessous sont de l'HISTOIRE. Refonte TOTALE de la couche rendu (2026-07-02, 8 lots ; la spec datée a été exécutée puis supprimée — git porte l'historique). Motif : après empilement de features (verticalité, murs d'arête, multi-niveaux, edge-on, POV, apparence JSON), l'utilisateur n'était satisfait NI du visuel NI du code.

**Architecture cible atteinte — « une scène, une apparence, N projections » :**
- **Pivot** `src/gameIso/builders/types.ts` : `SceneEl` (floor/wall/roof/prop/token), `Face{poly: GP[], plane, material, detail?}` en coordonnées MONDE (grille continue + h en MÈTRES) — jamais de rotation ni d'écran. Les builders n'importent NI `Dims` NI caméra.
- **Builders purs** `builders/{floors,walls,roofs,props,tokens,highlights}.ts` : dérivent la scène en éléments sémantiques + états de SCÈNE (visible/down/open/overhang/roofOccupied). UNE dérivation par préoccupation (relief, arêtes `wallEnds` 4→1, occlusion, pans de toit).
- **2 backends** : `backends/affine*.ts` (iso losange / edge-on / top — projette via [[game-iso-depth-z-secondary]], tri `depth()`) ; `pov/*` (perspective pinhole `camera.ts`). Les DEUX dessinent les mêmes faces/recettes, chacun à SA résolution (pas de parité pixel).
- **Stage fin** : `IsoStage.tsx` tient en ~350 lignes de COQUILLE (état→builders memoïsés→backends→fusion→JSX) + `stage/*` (14 modules + hooks `useStageCamera`/`useStagePointer`/`useHoverTargeting` + overlays d'INTERACTION en sous-composants). Fix re-tri 60 Hz : couches statiques triées 1× par memo, insertion dichotomique des dynamiques (`mergeByDepth`).

**Apparence 100 % DONNÉE** : `src/data/{structureAppearance,reliefMaterials,roofMaterials,ambiance,decorPalette}.json` + terrain defs + `shade.ts` (SEULE source de lumière ; identité = JSON) — API : `shade(hex,k)` (luminance ×k, clampé ; `var(--x)` passe tel quel), `mix`, facteurs de lumière (`SIDE_N=0.86`, `POST_CAP/BASE`), voiles `ao/spec/warm` ; calibré pour reproduire l'existant au lancement de la migration (bois iso ±3/canal). Recettes de détail « dessiné main » : `detail/{types,expand}.ts` (`DetailRecipe`→primitives UV) consommées par `backends/affineDetail.ts` (règle **pattern = joints partagés par matériau×orientation, fill = couleur teintable** ; accents seedés `hash32(identité monde)` ; LOD par zoom). Décor : 97 defs consomment `decorPalette` (tons nommés), ZÉRO hex. Garde-fou `renderer-no-hardcoded-color.test.ts` couvre tous les renderers d'environnement.

**Gains visuels clés** : toits en PANS CONTINUS (fin de la mosaïque/zigzag — `roofFromCells` supprimé, fusion coplanaire + selles triangulées) ; pierre/bois appareillés ; POV profond (portée 32, brume smoothstep, grille au point de fuite, silhouettes de brume pour le hors-vue — cf. retour user « manque de profondeur ») ; défaut de chargement corrigé (`camEdge:false` = losange lisible, plus l'edge-on illisible) ; POV spawn orienté vers le contenu (`spawnFacing`).

**Instrument de non-régression** : `npm run qc:env` → planches contact `public/qc/env-*.png` (4 scènes × iso 4 rot / edge 4 rot / top / POV 2 caps). Comparer avant/après à CHAQUE lot (SVG affine byte-identique pour les refactors iso-visuels).

**L'interdit qui en découle** (re-cadré post-#1176) : le rendu ne se re-monolithise pas. Aucun module de rendu à plat ne se rouvre à la RACINE de `src/gameIso/` (⚠ `src/gameIso/builders/walls.ts` est le builder PUR, vivant : c'est le module racine homonyme qui est proscrit). Une préoccupation de rendu vit soit dans un builder PUR (`builders/`, coordonnées MONDE, aucun import `Dims`/caméra), soit dans le backend de JEU (`backends/webgl/`), soit dans un peintre d'AUTHORING (`authoring/` — aperçu/plan/oracles, jamais importé par le monde du jeu). Aucun composant ne pose un sprite directement sur une tuile en court-circuitant le pivot `SceneEl`/`Face`. Aucun sous-système « bâtiment » codé ne se recrée à côté du catalogue `src/gameIso/catalog/buildings/defs/` : une identité de bâtiment est une `BuildingFeature` en DONNÉE, une matière est une entrée de `structureAppearance.json`/`roofMaterials.json`, jamais une charpente ni un hex en dur.

Relié : [[game-pov-first-person-view]], [[game-mapspec-unified-authoring]].

**Parité POV soldée (2026-07-02, post-refonte, 3 lots `a8caf66b`→`89993335`)** : les différés « hors POV v1 » repérés en playtest sont comblés. (A) **Overlays de terrain en DONNÉE** : `TerrainDef.overlayProp` (`bois→'arbre'`, billboard rendu par le chemin partagé dans les 2 backends) + `solidHeightM` (`mur→bloc plein` via le relief existant : hauteur d'AFFICHAGE `heightAt+solidHeightM` dans `buildFloors`, `heightAt`/combat INTOUCHÉ) → arbres et murs pleins enfin en POV ; legacy `sprites.ts` (`tree`/`wallBlock`/`TERRAIN_OVERLAYS`) SUPPRIMÉ. (B) **Détail de terrain POV** : `tintVar`+`tufts`+`speckle` (l'herbe vit) via `expandRecipe` (émet déjà les tufts) + même seed monde que l'iso. (C) **Animations POV** : `anim.css` est GLOBAL → les classes d'ambiance (`warm`/`sway` + `PropEl.fx`) jouent sur les billboards ; `smoke` en `-100%` (fill-box) ; idle bob des persos via `useRigClip`. INVARIANT : combat (LdV/vision/walkability) prouvé intouché (fichiers hors diff, tests verts).

**Bâtiments LISIBLES soldés (2026-07-02, 3 lots `3a3f249e`→`2cb08378`, post-parité)** — retour user « c'est des bâtiments quoi » (boîtes plates). Tout 100 % DONNÉE, rendu par les 2 backends via le pivot. (1) **Fenêtres + portes** : `WallSeg.window` DÉCORATIF STRICT (mur PLEIN — AUCUNE règle combat ne le lit : ni `wallIsOpen`/`vision`/`isWalkable`), croisée 4 carreaux + vantail de porte fermée, vitre ambrée émissive la nuit ; apparence dans `structureAppearance.json` (window.{glass,lit,frame,mullion}+door.{leaf,plank,handle}) ; posées auto par `sceneEdit.addBuilding` (périmètre, saut coins+porte). (2) **Identité par TYPE** : `BuildingDef.features: BuildingFeature[]` (`catalog/buildings/defs/<id>.ts`) résolu depuis `Roof.style` AU RENDU par `buildProps` → billboards d'ornement (chapelle=clocheton/forge=cheminée fumante/taverne=enseigne/échoppe=étal). Nouveau primitif GÉNÉRAL `PropEl.liftM` (billboard surélevé en mètres, honoré aux 3 SEULS points : `stage/tokens` iso, `pov/billboardCore`, `qc/env-panels`). Ancrage : faîte partage empreinte+profondeur du toit (se peint par-dessus), façade/étal ancrés HORS mur (case sortante). `source:'ornament'` 3e origine. Cutaway : faîteau masqué avec le toit levé (`roofHidden` partagé, `allies` sur `FloorView` DÉCOUPLÉ du tri par couche → POV ne culle pas ses props d'étage). (3) **Toits VOLUMIQUES** : géométrie ADDITIVE dans `roofPans` par égout — soffite (débord coplanaire, continue la pente) + fascia verticale sombre (planche de rive qui ombre l'arase) + faîtage (trait renforcé) ; `RoofMaterialDef.{eaveOverhangM,soffite,fasciaDropM,fascia,ridgeCap}` dans `roofMaterials.json` (chaume sans fascia dure). INVARIANT tenu : zéro règle inventée, combat intouché, anti-hex+serialize verts, commits curés (WIP siège `floors.ts`/naval `combatFlow`/`shipCrew` JAMAIS embarqués). Vérif = planches headless `qc:env` + close-ups dédiés (nav live indispo — rafraîchi en boucle par une lane //).

**Coordination** : chantier mené EN PARALLÈLE de 2-3 autres lanes (naval MDG, UI grimdark Jalon 9, systèmes-LDB) sur la MÊME branche `feat/wfrp4-rpg-foundation` — commits curés chemin par chemin (`git commit -- <fichiers>`), agents bornés aux fichiers rendu (cf. [[git-commits-propres-wip-parallele]]). Piège intercepté : `engine/combat.ts` modifié dans l'arbre = WIP lane LDB (défense « social »), PAS le mien → exclu du commit. Reset de limite de session a coupé 2 agents mid-vol → repris par SendMessage avec état des lieux (cf. [[feedback-background-agent-not-done-until-notified]]).
