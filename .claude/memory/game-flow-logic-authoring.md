---
name: game-flow-logic-authoring
description: "Système FLOW — logique authorée unifiée (conditions/effets/branches) vers des sorts créés dans l'éditeur ; refonte des briques POC"
metadata: 
  node_type: memory
  type: project
  originSessionId: 95ed0488-96d5-4512-939e-3cf27b47cc12
---

**Vision (user, 2026-06-15)** : UNE couche de logique authorée (conditions → effets → branches, façon liste de blocs imbriqués RPG Maker/ink — **PAS** un graphe à fils, tranché par l'user) qui subsume `Effect[]` + branches test/dialogue + conditions de trigger, et culmine en **sorts créés dans l'éditeur**. Consignes dures : gutter le POC, **pas de rétro-compat / code mort / déprécié / dupliqué / dette** ; approche adversariale pour éprouver l'idée.

**Garde-fous adversariaux actés** : ensembles CLOS de Conditions/nœuds (puissance par composition, pas par croissance) ; **PAS de boucle** (arbre jamais cyclique) ; UNE représentation (`Flow` EST exécuté, pas compilé vers un 2ᵉ format) ; **un sort custom = (ops) × (archétype de résolution fourni par le CODE : self-buff/heal/missile/zone/opposé/summon)** — les sorts flow-lourds restent code, ceil d'expressivité assumé.

**Livré** :
- **Noyau `src/engine/flowCore.ts`** (brique 1, `50d0b88`) : `Condition` (algèbre close flag/time/compare/all/any/not), `Flow<E>` (`seq`/`do`/`if`/`test`), `FlowTest` (tout le métier du Test), `flowFromEffects`/`flattenFlow`/`walkFlow`/`EMPTY_FLOW`. Pur, testé — il vit en `engine/` parce que le moteur n'importe JAMAIS `state` (#8, règle 3). `src/state/flow.ts` en est la fine instanciation : `export *` du noyau + les alias `Flow`/`TriggeredEffect` fixés sur l'union `Effect` de SCÈNE (transition/startCombat/startDialogue…, irréductiblement state) + les deux builders `flowFromEffects`/`testFlow` fixés sur `Effect`. Tous les consommateurs state/ui/scenes/data importent depuis `./flow`.
- `evalCondition` = SEUL évaluateur de conditions (brique 3, `5b195db`) ; `checkTriggers`/`DialogueBox` l'appellent. Le DATA des triggers/choix garde `condition:string`/`temporalCondition` (la migration de SHAPE vers `Condition` viendra avec celle de Trigger).
- `runFlow` (brique 2, `4b6cd07`, dans `combatEffects.ts`) : exécuteur UNIQUE — `do` en lot (applyEffectsLoot), `if` sur état VIVANT, `test` SUSPEND + reprend branche **+ continuation** (l'ancien `Effect.test` était terminal). `openSkillTest` = ouvreur de modale UNIQUE (nœud `test` ET `Effect.test` normalisé via `effectTestToFlowNode`). `PendingTest.onSuccess/onFailure` deviennent des `Flow` + `after` ; `resolveTest` rejoue via `runFlow`. Tests : `flow.test.ts`, `run-flow.test.ts` (209 verts incl. store.test 160 + scénarios Opéra).

- **Trigger migré (brique 4, `51fa822`)** : `Trigger { condition, temporalCondition, effects }` → `{ when: Condition, flow: Flow }`, anciens champs SUPPRIMÉS. checkTriggers = `evalCondition(when)` + `runFlow(flow)`. Adaptateurs partagés `editorState`: `whenFlag/whenWindow/buildWhen/flowEffects`.
- **Choix de dialogue migré (`29bf8ab`)** : `DialogueChoice { condition, effects }` → `{ when: Condition, flow: Flow }`. store.chooseDialogue=runFlow(choice.flow), DialogueBox filtre via evalCondition(c.when), DialogueDetail/validateScene lisent flowEffects(c.flow). Générateur+JSON régénéré.
- **Éditeur de Flow en blocs imbriqués LIVRÉ (`ccb1201`)** : `FlowEditor.tsx` récursif — blocs do/si/test, `TestFields` (FlowTest), menu « + Bloc ». Monté dans LogicDock (trigger) + DialogueDetail (choix), REMPLACE le round-trip plat flowEffects/flowFromEffects (qui perdait les branches). `EffectFields` exporté.
- **`Effect.test` SUPPRIMÉ (`5eca178`)** : le branchement par jet = nœud Flow `test` UNIQUE (plus de `effectTestToFlowNode`/cas applyEffects). Conséquence — les payloads BRANCHABLES deviennent `Flow` : `SceneEntity.interact {effects}`→`{flow}` (fouille à risque=test ; butin ramassable=feuilles `do` via flowEffects, battlePickup adapté) ; `delayedEffect {effects}`→`{flow}` (event programmé branchable : pétards Opéra→Test Perception) ; `ScheduledEffect.flow`. flow.ts gagne `testFlow`+`walkFlow` (validation récursive). Producteurs runtime (récolte ZI, `planJump`) émettent `testFlow`. Générateur=`testNode`, scènes Opéra migrées, JSON régénéré (0 Effect.test, 9 `kind:test`). **`onVictory` reste `Effect[]`** (récompenses-feuilles, aucun test → pas de churn).
- **`ConditionEditor` récursif LIVRÉ (`b11b96e` + polish `959ffc4`)** : source UNIQUE d'édition des Conditions (algèbre close flag/horaire/ET/OU/NON), montée dans le `si` d'un Flow + le `when` des triggers (LogicDock+Inspector) et choix de dialogue (DialogueDetail) ; `WhenEditor` = variante optionnelle (« Toujours »↔`undefined`) ; `condSummary` partagé. Aucun adaptateur plat à côté : un « flag ET créneau » s'édite comme toute autre Condition. On peut authorer un OU/NON imbriqué. CSS chartée (accents si=or/test=vert ; OU=or/NON=danger). **→ l'authoring de la logique (conditions→effets→branches) est COMPLET pour les pièges/scénarios.**
- **Conditions d'ÉTAT VIVANT LIVRÉ (`c5ab9b1`, direction « contenu/authoring sûr » choisie par user)** : 3 feuilles `Condition` que les flags ne peuvent suivre — `hasItem {trapping, count?}` (inventaire groupe), `money {atLeast}` (bourse ≥, en sous de bronze), `partyDead {who:any|all}` (mort). `ConditionCtx` gagne `party?`/`money?` (absents→false) ; `conditionCtx(state)` = builder UNIQUE câblé aux 3 sites runtime (checkTriggers, `if` de runFlow, filtrage DialogueBox) → tous lisent le MÊME état. Authorables (ConditionEditor). Démo en contenu : scénario vitrine **« Le Caveau piégé »** (`b8d564b`+`bc9575f`, registry e25 / menu 🧪) — herse = (hasItem « Clé en fer » OU levier) ET NON alarme, dalle = Test à branches.

- **EffectOp + sorts sous Flow LIVRÉ (user a acté le risque engine ; cadrage = PREUVE de complétude)** :
  - **Brique 1 (`50621d4`)** : `Effect.ops { ops: GameOp[]; on?: party|hero|caster|target }` = pont UNIQUE Flow↔moteur mécanique (applique des GameOp via `applyOps`). `inflictDamage`→`wounds`, `applyCondition`→`condition` SUPPRIMÉS/migrés. `GameOpEditor` réutilisable (sous-ensemble curé wounds/heal/condition/removeCondition/charMod/corruption). Les ops de CONTRÔLE (test/perRound/gates) NE sont PAS authorées dans EffectOp : le branchement vit dans le Flow ; le `test` de GameOp = jet de sauvegarde de la cible (feuille mécanique, couche ≠ du `test` interactif du Flow → PAS une duplication).
  - **Brique 2 (`8376329`)** : chaque sort porte ses EFFETS dans `SpellData.effects` — un `Flow` dont les feuilles `do` sont des `EffectOp` (`{type:'ops', on, ops}`), donnée app-owned éditable au Compendium ; la spec engine ne garde que les MÉTADONNÉES de résolution. Lecture par les primitives du noyau `spellOps(flow, on)` / `spellEffectOps` / `spellFlowFor` (`engine/flowCore.ts`) ; exécution par `runPureFlowLines` (combatFlow) — chemin UNIQUE des effets de sort. **PREUVE `src/state/spell-flow-completeness.test.ts`** : tout sort de la base porte un Flow valide à feuilles `ops` (≥ 220 sorts curés) ET s'exécute réellement → le système Flow/EffectOp est DÉMONTRABLEMENT complet (un sort qui n'y rentrerait pas casserait ce gate → généraliser, cf. consigne user « généralise le vocabulaire »).
  - **PIÈGE noté** : `zoneBlast`/`ZoneEffect` = dégâts MITIGÉS (BE+PA) ≠ `GameOp.wounds` NON mitigé → pas migré (design à part, op de dégâts de zone mitigés à généraliser si besoin).

- **Effets « lourds » de sort remontés en ops du Flow (Phase 1, commit `46a4264`, 2026-06-15)** : les 4 derniers effets vivant en métadonnée engine (`SpellSpec.summon/persistentZone/polymorph/lifeSteal` — invisibles/inéditables, d'où « Hurlement du loup n'affiche qu'un narratif ») → ops GÉNÉRIQUES `summon`/`zone`/`polymorph`/`lifeSteal` de `SpellData.effects` (16 sorts, ref/forme LIBRES). Moteur PUR préservé : `applyOps` expanse polymorph (polymorphOps) + applique lifeSteal (`ctx.woundsDealt`) ; summon/zone (impurs : grille) restent INERTES dans applyOps, résolus par combatFlow (applySummon/placeSpellZone) **lus depuis le Flow** (plus de spec). 4 champs SpellSpec + 4 sites combatFlow SUPPRIMÉS. `GameOpEditor` COMPLET (`ace8044`) : lit/modifie/ajoute TOUTE op (~45) + repli JSON, fix corruption Formula `{dice}`→0. `spell-impure-ops.test.ts` prouve la résolution depuis le Flow.
- **Effets DÉCLENCHÉS génériques — Traits + Atouts d'arme (Phase 2, commit `0bd3a6b`)** : but user = « modifier traits/atouts comme les sorts depuis Codex+Édition », puis « c'est générique ? un Atout 1d10+immobilise à la touche ? ». `TriggeredEffect { trigger: EffectTrigger(onHit/onWoundLoss/onRoundStart/onStartled/onKill); on: self|victim|engaged; flow }` (flowCore) PARTAGÉ par `TraitData.effects` ET `QualityData.effects` (= pendant « sur événement » des sorts « au lancement »). **ÉDITEUR** : `CodexEdit` rend `TriggeredEffectsField` (déclencheur+cible+`FlowEditor`) pour catégories `traits` ET `qualities` → un Atout « à la touche : wounds 1d10 + condition Empêtré » s'édite en données. **RUNTIME** : dispatcher UNIQUE `state/triggeredEffects.fireTriggers` (réutilise `runPureFlowLines` — le MÊME vocabulaire d'ops que les sorts, zéro duplication) au point de touche, Traits de l'attaquant + Atouts de l'arme à l'identique. **Migré+nettoyé** : Toile (trait) + Immobilisante (Atout) en `effects` — aucun flag ni handler en dur par entité, les defs se réduisent à la clé canonique (parsing/affichage). `triggered-effects.test.ts` prouve trait+atout+l'exemple user. `traitByLabel`/`qualityByLabel` index data.

**Reste** :
0. **Balayage Phase 2 (commits `5e48a0d`/`42c2877`/`1092221`/`6cfb1ea`)** : 6 traits/atouts CONVERTIS en `effects` data, handlers en dur supprimés — Toile, Immobilisante (atout), Nerveux (onStartled), Sang corrosif (onWoundLoss/engaged = TOUS les Engagés RAW), Affamé (onKill : op `test` carac → `loseTurn`), **Régénération** (onRoundStart, le cas le + riche : 1 d10 → heal=valeur + crit-heal sur 10 + branche PB). **Vocabulaire neuf, tout réutilisable + éditable au Codex** : `wounds.min`, `test.characteristic?`, op `loseTurn`, op `rollThreshold` (1 dé → ops par palier cumulatif), Formula `{rolled}` (réutilise le jet), et surtout **Condition `compare` GÉNÉRIQUE** = `who`(cible|lanceur) × (donnée fixe `woundsCurrent/Max` OU valeur/stacks d'un État) × `op`(≥≤=<>) × `value` — a ABSORBÉ woundsAtLeast/AtMost + selfHasCondition, lit LES DEUX acteurs du Flow (ConditionCtx `target`/`caster`, `actorView`). Op-doublon `gainFortune`+`gainFate` → `gainResource {resource}`. **Déstabilisante** (Atout onHit INTERACTIF, AA p.89) est elle aussi EN DONNÉE : `qualities.json#destabilisante.effects` — dépense de 2 Avantages (op `spendAdvantage`) + Test opposé Force/Athlétisme → À Terre, surfacé comme une étape de CHOIX de la cascade (le porteur décide, la branche vise la VICTIME : `state/combat/triggeredTest.ts` ; `modalArbiter` n'a PAS d'entrée propre pour le Renversement) ; garde `engine/qualities/dispatch.test.ts`. Restent des FLAGS (légitimement distincts, ne PAS forcer) : traits passifs/structurels (Taille/Armure/fly/magicalAttacks/psych) + `perturbingAura` (modificateur passif lu) ; Conditions de GROUPE `hasItem`/`money`/`partyDead` (saisie friendly : nom d'objet, CO/pa/sc, un/tous) ; les 4 wards + exemptions (sous-systèmes ≠).
1. ✅ Les DONNÉES portent le Flow : `SpellData.effects` (app-owned, commité) est le Flow lui-même — les sorts existants s'éditent dans le FlowEditor/Compendium, la spec engine ne garde que les métadonnées de résolution.
2. **Sorts CUSTOM dans l'éditeur** (le but user) : écran Compendium = archétype de résolution (missile/soutien/ZdE/soi…) + effets via `GameOpEditor` (déjà fait) + méta (durée/portée) + dataset app-owned. Réutilise tout Brique 1-2.
3. Recette navigateur (différée : arbre foreign-cassé ; couvert par tests unitaires).

Migration de SHAPE = éditer données en repo + régénérer l'arène (`node`/`tsx scripts/arene/generate.mjs`) ; pas de shim retro-compat (consigne user).

Prolonge [[credo-exemples-calibrants]], [[feedback-adversaire-creatif]], [[game-existant-poc-refactor-libre]], [[game-jet-modale-exhaustif]].
