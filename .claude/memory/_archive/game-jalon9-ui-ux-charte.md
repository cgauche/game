---
name: game-jalon9-ui-ux-charte
description: "Jalon 9 (refonte UI/UX produit + charte Warhammer) — arbitrages utilisateur, charte VALIDÉE, état des phases"
metadata: 
  node_type: memory
  type: project
  originSessionId: df22e358-4438-4cca-b8e3-ad83ea327a2e
---

Jalon 9 ajouté 2026-06-11 (ROADMAP) : tous les écrans passent de POC à **produit final** — pas qu'une charte, une refonte UI/UX (hiérarchie, parcours, wording, états vides porteurs d'action).

**Arbitrages utilisateur (2026-06-11)** : éditeur INCLUS dans l'objectif mobile 360px (« Mobile complet », malgré mon avertissement de coût) ; direction « identité Warhammer marquée » ; pilote menu+groupe. **Charte VALIDÉE** (« Sinon la charte graphique me va ») ; il a aussi exigé : retrait de la mention campagne Ennemi Intérieur du menu, purge du texte tuto (cf. [[feedback-pas-de-texte-tuto-ui]]).

**Charte (Phase 0, livrée `419b3d8`)** : tokens `:root` = charbon chaud `--bg #16120d`, **rouge sang** `--accent #8e2418` (primaire), or vieilli `--gold #c9a227`/`--gold2`, `--parchment`/`--ink`/`--blood`, `--font-display` = **Grenze Gotisch** (fontsource, embarquée, graisses 600/800) appliquée à h1-h3 partout. Boutons : primaire sang, hover or ; barre d'écran à filet or ; séparateur `.rule-fleur` (⚜).

**VISER LE BEAU, pas que le cohérent** *(signal fort 2026-06-11)* : sur la carte du monde j'avais d'abord fait un re-skin (tokens + fix bug) ; l'utilisateur a répondu « la word map est vraiment un POC, donc hésite pas » + « c'est juste fonctionnel mais pas très beau ». Leçon : pour un écran jugé POC, ne pas se contenter de cohérence/tokens — faire une **vraie refonte visuelle** (texture, ornements, identité dessinée). Ex. livré : carte SVG transformée en carte ancienne (grain feTurbulence + vignettage + cadre à fleurons + médaillons + routes courbes + rose des vents, commit `84e4fbd`). « Hésite pas » = budget visuel ambitieux assumé. Prolonge [[game-visual-direction]].

**État Phase 2** : Phase 1 pilote menu+groupe LIVRÉE (commits `317982e`/`2bfaba7`). Écrans Phase 2 livrés : **Victoire** (cartouche cérémonial + butin par portraits, `59ee018`), **Interlude** (`f87c6bd`), **Marchand** (header charté, `b2bcae4`), **boutons HUD** (fin du bleu-gris POC #1c2230 → charbon/or, accent-color doré global, `150f98c`), **Carte du monde** (fix bug `btn primary`→`btn-primary` `27c6981` + refonte parchemin `84e4fbd`), **fiche perso** (sidebar+onglets, last session), **Éditeur produit final** (édition modale + Ouvrir/Enregistrer/Publier, cf. [[game-editeur-produit-final]]), **Coop lobby** (`ccd344e` : carte centrée charte, fin de la zone morte des 2/3 d'écran ; 3 états sur un `CoopShell`). Tests-scénarios = déjà on-charter (vérifié écran). **Coop lobby** (`ccd344e`) + **Dialogue in-game** (`a0c1190` : portrait interlocuteur via pickBackend + `dialogue.speakerId` store, cartouche gothique, choix ▸ + coût or à droite — façon BG3) **livrés**. **Reste Phase 2** : save (déjà `Modal`, hérite) — Phase 2 close en pratique. **Balayage 360px fait** (`d767aa1`, via `__wfrp`) : marchand corrigé (table d'achat → cartes empilées en ≤560px, fini le scroll horizontal pour « + Ajouter », colonnes secondaires via ▸) ; interlude propre ; victoire responsive par CSS (max-width 520/92%). Méthode rapide désormais : `__wfrp.talk('id')` / `store.getState().startInterlude()` pour atteindre l'écran, puis mesurer `scrollWidth>clientWidth`. **Phase 3 ENTAMÉE** (`6f524a4`) : éditeur 360px — barre d'outils enroulée (fin du débordement horizontal) + **Palette/Inspecteur en TIROIRS glissants** (≤900px : canvas plein écran, barre tactile basse 🗺️/🔍 + voile, grille 3-col intacte en grand écran). Reste Phase 3 : pose de tuiles/drag au doigt (le canvas est déjà `touch-action:none`+pannable), affinage tactile. Méthode par écran : audit parcours → hiérarchie/wording → charte/**beauté** → recette 360/1440 ; chaque écran a révélé une vraie trouvaille (restes POC, bug de classe, texte verbeux).
