---
name: game-temps-voyage-pivot
description: "Jalon 1.6 COMPLET : Qualité ✓ + #T1 Horloge & #T1c Jour/Nuit ✓ + Marchand ✓ + #T2 Voyage ✓ + #T3 Cascade ✓ (intégré 2026-06-10, bundle Jalon 2.6)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 36f0db2e-763e-4a5c-9856-c28192f92316
---

**Re-séquencement du Jalon 1.6** (2026-06-07) : d'abord Qualité → Temps & Voyage → Marchand → Arène. **Puis 2e re-séquencement (2026-06-07, décision utilisateur)** : seul **#T1 Horloge** (+ **#T1c Jour/Nuit**) était nécessaire au Marchand (il est *time-ready*, l'horloge est livrée) → **#T2 Voyage et #T3 Cascade sont MIS EN SUSPENS**, le **Marchand est REMONTÉ et devient le prochain livrable**. Ordre actuel : **Qualité ✓ → Temps #T1 + #T1c ✓ → MARCHAND v1 ✓LIVRÉ ([[game-marchand-v1]]) → (#T2/#T3 en suspens) → Arène (next)**. ⚠️ Conséquence : le **re-stock du Marchand dans le temps attendra #T3** (le cœur transactionnel achat/vente/Disponibilité/Marchandage, lui, ne dépend pas de #T2/#T3).

- **#1 Qualité d'objet LIVRÉ** (Phase 0+A+B+C1+C2, poussé) — cf. [[game-qualities-registry]].
- **Temps & Voyage = nouveau sous-projet #T**, décomposé : **#T1 Horloge & Calendrier impérial** (fondation) → **#T2 Voyage** (graphe de lieux + coût-temps + rencontres) → **#T3 Cascade RAW** (guérison LDB 18, Fatigue, maladies LDB 20, Corruption, re-stock marchand). Spec de cadrage : `docs/superpowers/specs/2026-06-07-temps-voyage-design.md` (design #T1 validé).
- **#T1 Horloge & Calendrier impérial = LIVRÉ + poussé** (commits 856c63b clock.ts, 144db2c gameTime/advanceTime, 96e774f TIME_COST+horodatage, 56c92b8 HUD). Détails :
  - `src/engine/clock.ts` PUR : 12 mois + 6 intercalaires + semaine de 8 jours ; base interne = **minutes depuis l'époque = Hexenstag 2512 00:00** ; `toDate`/`fromDate` (round-trip), `formatImperial` FR (« 33 Jahrdrung 2512 CI · 08:00 »). **CAMPAIGN_START = fin Jahrdrung 2512, 08:00** (jour 33 = totalDays 65, inchangé). **Calendrier corrigé après vérif canon web** (commit af0a8ba, EiS Annexe 3 l.20/68 « notre monde, 400 » + Fandom/Lexicanum) : **année = 400 j** (pas 401 ; les 6 intercalaires sont INCLUS → mois = 394 = **2×32** (Nachhexen+Nachgeheim) **+ 10×33**, consensus communautaire car canon ne publie pas la table) ; **jours intercalaires HORS du cycle hebdomadaire** (`weekday=null`, la semaine de 8 j « enjambe » les fêtes) — l'ancien calcul `totalDays%8` comptait tout = faux ; ancre 1 Nachhexen 2512=Wellentag (convention). **Piège plan** : le test `formatImperial` du plan avait un off-by-one (Hexenstag = jour 0 déjà compté par l'époque) — corrigé `(32+30)` pas `(1+32+30)`.
  - Store `gameTime: CAMPAIGN_START` + `advanceTime(min)` (no-op si ≤0) → émet **`EVT.TIME_ADVANCED`** (seam #T3 cascade, pas encore de consommateur). **« tout est horodaté »** (invariante) : table `src/engine/timeCost.ts` `TIME_COST` (combatRound=1/search=10/dialogue=5/sceneTransition=0, **paramétrable**, canon muet) branchée : Round franchi (`advanceTurn`), fouille (`interactEntity`), clôture dialogue (chooseDialogue/closeDialogue/Effect endDialogue, **idempotent** via `if(get().dialogue)`), seam transition (sceneTransition=0 → no-op, pour #T2 extérieur). HUD `🕓` dans CampaignView.
  - 14 tests T1 ; suite complète verte (1217). golden-combat intact. Recette navigateur non faite (browser verrouillé par session //).
- **#T1c Cycle jour/nuit piloté par l'horloge = LIVRÉ + poussé** (spec `2026-06-07-cycle-jour-nuit-design.md`, plan `2026-06-07-cycle-jour-nuit.md`). Décision clé : **le jour/nuit ne vient PLUS de la scène** — `Scene.ambiance` réduit à **`'interieur'` (éclairé) vs `'exterieur'` (piloté par l'heure)** ; legacy `'jour'/'nuit'/'foret'` normalisés `'exterieur'` (`normalizeAmbiance`/`isIndoor`, zéro migration). `clock.ts` : **7 phases d'affichage** (`DAY_PHASES` aube/matin/midi/apresmidi/crepuscule/soir/nuit + icône) ; **obscurité mécanique = `isNight` via `NIGHT_WINDOW` paramétrable (22:00–05:00), DÉCOUPLÉE des phases** ; `minutesUntilNext` (saut monotone). Pont unique **`sceneRules.sceneIsDark(scene, gameTime)` = !isIndoor && isNight**, consommé par le combat (`sceneCombatModifiers(scene, gameTime)` → −20 tir obscurité LDB 14 l.107, 3 appelants threadés) ET le rendu (`IsoStage`). **Pour forcer une scène de nuit : Effet `setTime { phase|hour }`** (saut EN AVANT, jamais en arrière) — PAS de tag de scène. HUD = icône phase + jour-de-semaine + date+heure. **Exposition éditeur AUSSI LIVRÉE** (plan 2 `2026-06-07-cycle-jour-nuit-editeur.md`) : menu `ambiance` Intérieur/Extérieur (2 options, legacy→Extérieur), Effet `setTime` éditable dans `EffectList` (sélecteur des 7 phases ; variante `hour` reste data) ; aperçu éditeur de jour. Suite verte (1323). Détails reset/horloge : cf. [[game-newgame-reset-pattern]].
- **Marchand v1 CADRÉ mais PARQUÉ** (annexe de la spec ci-dessus, « à ne pas perdre ») : transactionnel #2a+#2b+#2f ; **resaleRate défaut 10 %** paramétrable (pas de règle RAW de rachat — LDB 59 « achat/vente optionnels ») ; **Disponibilité RAW complète** (Commune/Limitée-Rare-Test/Exotique, quantités Village 1/Ville 1d10/Cité, révélations) ; taille d'agglo **sur l'entité** paramétrable ; **scopé par catégorie** (herboriste ≠ arquebuses) ; archétype = 6ᵉ famille `defs/` ; monnaie à réconcilier `price.bronze`↔`Money.brass` (1 CO=20 SC=240 PA). Conçu **time-ready** (seam re-stock branché une fois #T en place).

- **#T2 Voyage & Nourriture = LIVRÉ + intégré** (2026-06-10, bundle session parallèle `feat/t2-voyage-nourriture` f1e27d1, merge e5c709c poussé, suite verte 2366) : carte du monde (`src/state/worldMap.ts` + `WorldMapView` + éditeur `WorldMapEditor`), voyage RAW (`src/engine/travel.ts` + `travelFlow.ts`), rations & faim (`src/engine/provisions.ts` + `state/upkeep.ts`), péripéties (`src/data/peripeties.ts`), scénario test `15-voyage`, scène `tome1-carte`.

- **#T3 Cascade = LIVRÉ + intégré** (2026-06-10, bundle ultraplan « Jalon 2.6 », merge 51a5d5e poussé) : maladies/convalescence aux **jours CALENDAIRES** + purge horloge sur tous les chemins (bc3528b), **buffs de sort à durée d'horloge** (`untilTime`, LDB 47, f0cdb03), + le même bundle livre les ops magie Jalon 2.6 (grantTrait/grantTalent/enchantWeapon/soins/Téléportation+Poussée), Béni→6 Bénédictions, et des tests de verrouillage création × features. **Jalon 1.6 COMPLET.** Re-stock marchand : vérifier s'il est couvert avant de le réclamer.

- **Refonte UX du voyage (playtest 2026-06-12, 94aaa58, arbitrages utilisateur)** : le voyage se
  vit en RAPPORT JOURNALIER — la halte du soir (modale de Repos, `pendingRest.travelDay`) affiche
  la « Journée de route » (km + péripéties + jets en MultiRollList) ; le recap final ne couvre que
  le DERNIER segment (`recapDays` supprimé de TravelPlan). **Embuscade DIFFÉRÉE derrière le récit**
  (`TravelRecap.then`, déclenchée par `dismissTravelRecap` — péripéties d'auteur à
  startCombat/transition ET « Attaqués ! » d10) : recap d'abord (« ⚔️ Faire face », pas d'esquive,
  resumeTravel refuse tant que pending). « Attaqués ! » sans `route.ambush` configurée → le texte
  ne promet plus d'embuscade (mention « alerte sans suite ») ; l'éditeur de carte expose déjà
  scène/rencontre/entrée d'embuscade par route. Jets de route STRUCTURÉS (forcedMarchTest renvoie
  `{line, gained, d}`).

État : Jalon 1.6 clos. Lié à [[game-qualities-registry]]. Commits propres malgré WIP // : cf. [[git-commits-propres-wip-parallele]] (combatFlow.ts/store.ts partagés → temp-index + reverse-apply des hunks étrangers).
