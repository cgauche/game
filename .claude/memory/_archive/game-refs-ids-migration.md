---
name: game-refs-ids-migration
description: "Migration refs structurées par id + gods.json éditable LIVRÉE ; compétences/talents d'instance en id ; traits PAS encore migrés (prochain)"
metadata: 
  node_type: memory
  type: project
  originSessionId: e636cc63-4e05-4723-bc40-1452e878c0af
---

Chantier `structured.refs-gods-editable` **LIVRÉ et vert** (typecheck 0 erreur refs ; suite 5187 pass, seuls 6 échecs = WIP // rig/armes/caméra, hors périmètre).

- **Données**: `id` (slug du libellé) ajouté à `trappings`/`qualities`/`spells` ; tous les `string[]` de référence migrés en refs structurées par `id` (TrappingRef/QualityRef/Ref/AdvancementRef) ; `gods.json` = dataset éditable (registre culte codegen `engine/cults/defs` SUPPRIMÉ). Script `scripts/migrate-refs.mts`.
- **Runtime ids (sous-systèmes Qualités, Sorts, puis Compétences/Talents)**: `SkillInstance{skillId,spec?,characteristic,advances}` + `TalentInstance{talentId,spec?,times}` (ex-`name`). Helpers `src/data`: `skillInstanceLabel`/`talentConcrete` (label concret), `findSkillById`/`findTalentById`, `refLabel`. Matching partout par `s.skillId === '<slug>'` (ex. `corps-a-corps`/`resistance`/`calme`/`guerison`/`focalisation`/`langue`/`priere`/`projectiles`/`esquive`/`metier`) ; talents par `t.talentId` (+ `t.spec`). **« Initiative » n'est PAS une compétence** (caractéristique `I`) — supprimé un lookup mort.
- **`slugId(label)`** dans `src/data/slug.ts` : NFD, strip diacritiques, lowercase, œ→oe, `[^a-z0-9]+`→`-`. C'est la SOURCE des id (data + runtime). Authoring/test : helper de fabrique peut sluguer un libellé (ergonomie), pas une dette.
- **Piège**: des fixtures de test construisaient `{name:'X'}` derrière des casts `as never`/`as any`/`as Combatant` → invisibles au typecheck, cassent au runtime (id `undefined`). Toujours grep `name:` dans `skills:`/`talents:` après ce genre de migration.

**Traits LIVRÉS aussi (#9, atomique)**: `id` slug sur `traits.json`(84)+`frenchy-traits.json`(5) + `TraitData.id`/`traitById`/`findTraitById` ; `TraitInstance.key`→`id` ; **`TraitList = TraitInstance[]`** (plus de `string |` — `asTrait` SUPPRIMÉ) ; registre `TRAITS` keyé par `slugId(def.key)` (aucun fichier `defs/` édité — `def.key`=libellé reste pour l'affichage) ; `CANON_BY_LOWER` (label→id) + `LABEL_BY_ID`/`traitLabelById` (id→label) dans dispatch ; `hasTraitKey`/`hasTrait`/`parseTrait` par id ; tous les `hasTraitKey(t,'Frénésie')` → slug. **DONNÉES destringifiées** : `creatures.json` (2619 traits → `{id,value?,arg?}`, `scripts/migrate-trait-instances.mts`) + `mutations.json` (`Mutation.traits`→`TraitInstance[]`). `parseTraitInstance` = authoring-only. **Couche AUTHORING des TRAITS aussi en id** : `CustomStatblock.traits` / `SceneEntity.combat.optionals` / `SpawnExtras.optionals` → `TraitInstance[]` (pickers éditeur « parse-on-edge » : affiche `formatTrait`, stocke `parseTraitInstance` à l'édition) ; op `grantTrait{trait:string}` → `{traitId, arg?, indice?}`, `grantTrait`/`removeGrantedTrait`/`ActiveEffect.grantedTrait` prennent un `TraitInstance` ; DONNÉES migrées : `arene-projet.json` (optionals+statblock traits, `scripts/migrate-arene-authoring-traits.mts`) + `spells.json` (30 ops grantTrait, `scripts/migrate-spell-granttrait.mts`). ZÉRO string trait nulle part (runtime/données/authoring).

**Couche authoring skills/talents/spells de statbloc AUSSI en id (LIVRÉ)** : `CustomStatblock.skills→SkillRef[]`, `.talents→TalentRef[]`, `.spells→string[] d'IDS` ; `SceneEntity.combat.spells→string[] d'IDS` ; `skillsFromStatblock`/`talentsFromStatblock` SUPPRIMÉS (statblocs routés via `skillsFromBook`/`talentsFromBook`) ; pickers éditeur parse-on-edge (`skillRefLabel`/`talentRefLabel`/`refLabel` display, `parseStatEntry`+`findX` store) ; `arene-projet.json` (combat.spells) + fixtures migrés (`scripts/migrate-arene-statblock-refs.mts`). Spawn filtre les spells par `findSpellById`.

**Gaps spell-id TRAITÉS** : (1) grimoire : shim `spellIdOf`/`?? findSpell(x)` (repli libellé) SUPPRIMÉ → `spellCost`/`canCastFromGrimoire`/`knownCount` comparent `c.spells` par id strict ; **BUGS RUNTIME corrigés** : `ActionBar` faisait `findSpell(id)`→undefined (casters ne voyaient plus leurs sorts !) → `findSpellById(id)` + pont id→libellé pour le flux de cast ; `CharacterSheet` repli libellé retiré ; `registry` `c.spells` (ids) → `refLabel('spells',{id})`. Fixtures spell→ids (grimoire/store/zde-cast/encounterAuthoring/InspectPanel). (2) `traits-lot-c` n'était PAS un flake : la session // avait reverté mon `.map(parseTraitInstance)` ligne 116 — re-corrigé, vert. **Tous les producteurs de `Combatant.spells` = ids** (spawn/draft/pregens/buySpell). Suite : 5193 pass, 13 fail = WIP // rig (mutations/espèce vide/golden/armes AA/caméra), zéro de cette migration.

**RESTE (pureté, prochain lot)** : le flux de cast ÉPHÉMÈRE (`battle.selectedSpell`, `pendingCast.spellLabel`, `focus.spell`) identifie le sort par LIBELLÉ (findSpell), pas par id — interne-cohérent et fonctionnel (ActionBar fait le pont id→libellé), mais pour la pureté multilangue ces champs d'état devraient être des ids. Pas cassé, juste pas pur. Étend [[feedback-zero-retrocompat-briques-solides]].
