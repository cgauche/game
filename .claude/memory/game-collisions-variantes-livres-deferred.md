---
name: game-collisions-variantes-livres-deferred
description: Collisions/règles alternatives entre livres (même talent/trait/mutation aux desc+source+page+effets différents) — trois cas ; le Cas B reste DIFFÉRÉ par décision utilisateur citée, socle engine/variants.ts jamais joué
metadata: 
  node_type: memory
  type: project
  originSessionId: 79086e8f-2b86-464f-8a9e-6f2bc67f4515
---

Plusieurs talents/traits/mutations existent en **versions différentes selon le livre/la règle optionnelle active**, et la règle de l'user est : **« les 2 sont valides »** (garder les deux, ne pas réconcilier de force).

Trois cas distincts :
- **Cas A — variante choisie par le CONTEXTE (table)** : ex. mutations. La table d100 diffère de toute façon → **deux entités « both valid »** + la table (gouvernée par une règle optionnelle) route vers la bonne. RÉSOLU proprement. Fait pour EDOC : « Écailles épineuses » LDB (+1 PA) vs EDOC (-10 Dex/-10 Soc/+1 PA) = deux entités ; règle `corruption-tables-edoc` choisit la table. Voir [[game-mutation-creature-id-migration]].
- **Cas B — variante de l'ENTITÉ attachée directement** : ex. **certains talents AA ont des règles différentes sous le système d'Avantage AA**. Un perso « a Fusilier » : l'attache est agnostique à la règle, la résolution est **live** (toggle en cours de partie). Et ce n'est **pas que l'effet** qui change : **desc, source, page et d'autres éléments** aussi. **DIFFÉRÉ** — un socle `variants` existe, son extension est sous décision utilisateur (voir *How to apply*).
- **Cas C — même ENTITÉ NOMMÉE (créature), profil différent selon le livre, NON gaté par une règle** : ex. **Brochet du Stir** ZI (Amphibie, Attaque caudale, harvest « prédire l'avenir », 3 Points de Chance) vs T2C ch.13 (Aquatique, Foulée, Belliqueux). ✅ **RÉSOLU (user 2026-07-03 : « les 2 ont le droit d'exister »)** : **DEUX entrées distinctes** (`brochet-du-stir` + `brochet-du-stir-fluvial`), MÊME libellé, **ids distincts**, `folder`/`source` distincts, `appearance.species` partagé (même rig). L'auteur choisit par **id** à l'authoring d'une rencontre — le jeu référence par id ([[game-label-id-migration-complete]]), donc `id-collisions.test.ts` confirme « inoffensives à l'exécution ». **Pas besoin du mécanisme `variants` du Cas B** (aucun toggle de règle). Piège de PROCESS à éviter : réconcilier/écraser (un agent avait écrasé ZI→T2C ; garder les DEUX). Ne JAMAIS détruire la curation d'une version (harvest, capacités) en « fusionnant ».

**Why:** notre gating d'op (`onlyIfCondition`/`unlessCondition`) ne référence que des **États**, pas l'état d'une règle optionnelle ([[game-data-driven-architecture]] `policy.ts`/`rule(id)`).

**How to apply:** le Cas B relève des **variantes d'entité**, et la seule DÉCISION au dossier est CITÉE : utilisateur 2026-07-03, verbatim « enfin bon continue EDOC » — le mécanisme est jugé trop compliqué en data-driven et **DIFFÉRÉ** ; on ne l'étend pas sans relancer la discussion avec l'utilisateur. La formule « jamais 2 talents différents », étiquetée « doctrine user 2026-07-17 » dans le dépôt (`src/data/sourceRefs.ts:3`, `src/data/schemas/common.ts:57`, `src/data/schemas/defs/traits.ts:99`, `src/engine/variants.ts:2`, `src/ui/compendium/CodexEdit.tsx:1101`, `docs/donnees.md:183`), ne porte AUCUNE citation et n'apparaît **ni dans #563 ni dans #564** (mesuré 2026-07-26, corps + commentaires des deux issues) : elle vaut comme **évaluation d'ingénierie révisable**, jamais comme arbitrage utilisateur, et ne renverse donc pas la décision citée ci-dessus.

**État MESURÉ du mécanisme (2026-07-26)** : primitive UNIQUE `src/engine/variants.ts` — `activeVariant(variants)` (première variante dont `rule(when.rule) === (when.equals ?? true)`, `when.rule` référençant LE registre `OPTIONAL_RULES` de `engine/policy.ts`, jamais un flag parallèle) et `effectiveEntry<T>()` (REPLACE par champ DÉCLARÉ au 1er niveau — le livre republie l'entrée entière, aucune fusion profonde ; aucun consommateur ne réimplémente la fusion, mécanique comme affichage). Forme en donnée : `variants?: [{ when:{rule,equals}, …surcharge complète }]`. Porteurs : **12 talents** de `talents.json` (artilleur, battement, cavalier-emerite, concocter, coude-a-coude, distraire, frappe-blessante, fuite, impitoyable, porte-bouclier, rechargement-rapide, renversement) ; garde `src/data/variants-integrity.test.ts` (deux variantes actives à la fois = défaut de DONNÉE que le `find` ne peut pas départager). ⚠ Ce socle n'a **jamais été joué** — le vérifier bout-en-bout avant d'y fonder un lot ([[game-socle-variantes-avantages-groupe-non-valide]]). Le Cas A reste sur deux entités « both valid » (la TABLE route).
