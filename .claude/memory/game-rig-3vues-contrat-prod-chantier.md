---
name: game-rig-3vues-contrat-prod-chantier
description: Chantier prod — le rig bipède passe à un contrat de vues TOTAL (front/back/profile), corps de base garanti (crâne+cou), fin du front plaqué
metadata:
  type: project
---

Le système de VUES du rig humanoïde bipède est déclaré POC → refonte niveau PRODUCTION.
Verbatim utilisateur (2026-07-21) : « Bon l'existant est un POC, il est temps de passer au
niveau prod. Demande a Fable 5 un plan pour amener ce changement, même si cela doit casser
l'existant. » Plan rédigé par Fable 5, claims porteurs contre-vérifiés au code.

**2 défauts de fond** (une vue à l'autre : bras détachés, ceinture décalée, tête flottante) :
1. Pas de CORPS DE BASE garanti par vue — crâne arrière / cou / avant-bras n'existent que si une
   coiffure/tenue/moignon les simule → hacks (nuque reroutée `composeRig.tsx:204` z=torse.z-0.5,
   calottes `BACK_TETE`/`PROFILE_TETE`, moignon `WRIST` dans `HAND`). Chauve de dos = tête absente
   (`cosmeticPart` renvoie `back: BACK_NAPE`, jamais un crâne ; cf. la preuve `crane-rase-m`).
2. Résolution de vue = cas-par-cas RUNTIME (`pickView` `art[view] ?? art.front` silencieux
   `parts/types.ts:13` ; silhouettes génériques dans `resolve.ts` ; `dominantCloth`).

**Ironie clé** : `rig/viewArt.ts` (navires/engins/terrestres) porte DÉJÀ le bon contrat
(`ViewArt` + `declaredViews` + `foldView` + `MISSING_ART` bruyant #223). Le bipède est en retard
sur ses propres objets inertes. Cliquet existant #551 `part-view-format.test.ts` (`MAX_FORMAT=103`,
`MAX_ALIAS=3`, descendu de 171).

**Cible** : type TOTAL `ViewSet {front,back,profile}` pour slots de corps (repli front silencieux
= erreur de COMPILE) ; parts système `crane`+`cou` en chair `@peau` 3 vues (os `cou` z 6→4.5, sous
torse → tout col couvre le cou sans patch par tenue) ; silhouettes génériques → helpers `derive*()`
dans `parts/derive.ts` APPELÉS PAR les defs (greppable, cliquetable) ; `resolve.ts` = pure table de
priorité. 3 vues × miroir = 8 dir via `project(dir8,camRot)` (`facing.ts`), inchangé.

**Décisions utilisateur TRANCHÉES (2026-07-21)** : D1 = OUI scinder les bras au coude (part
`avantBras` réelle) ; D2 = 3 VRAIES vues obligatoires pour les 90 armes + 4 boucliers (pas de
repli) ; D3 = vues dérivées matérialisées dans les defs (décision d'ingénierie orchestrateur) ;
D4 = OUI pas de visage de dos (crâne+cou+cheveux portent le dos).

**Phasage** (chaque phase mergeable + porte ; goldens régénérés SEULEMENT aux portes, après rebase
— collision session goldens) : P0 gel+contrat v2 · P1 types+pipeline aplati (shim de chargement) ·
P2 corps de base crâne+cou + mort du hack (1er gain VISIBLE) · P3 migration defs (codemod, mort du
shim, cliquet successeur `derive*`) · P4 équipement tenu (armes 3 vues, dos de boucliers) ·
P5 vague d'art dos/profil (parcelable, pilotée par `mesure-volume --views back,profile` + juge) ·
P6 resserrage types familles annexes. Plan détaillé archivé en scratchpad de session.

**État 2026-07-21** : P1 (`ddfeded6`) + P2 (`c51da3e3`) + P3-squelette (`ce6b368f`, refonte proportions/canon, proportions VALIDÉES user, panel vision + juge diff = TIENT, canon gardé par `skeletons.test.ts`) + suppression des 8 TENUES DE CLASSE (fallback→Nu, palette préservée, Choses→mendiant, Gnome→artisan, garde `tenue-refs-resolvable`, cliquet #551→86 ; juge TIENT) + CIBLE #1 CORPS `Nu` PROD (`4f6a66f0` : torse front=dos 0 %/global 2,6 %, jambes 3 vues, `BodyDef.jambe`→ViewSet, nu-ogre migré, cliquet→79 ; outil `scripts/qc/silhouette-coherence.mts` ; mesure + panel vision + juge diff = TIENT). Menés en worktree, rebasés, puis posés sur `main`. Jugés TIENT / FRAGILE-levé ; suite complète verte hors 2 rouges `effect-rule-anchor` = ENGINE hérités de la base (autre session), hors des 18 fichiers rig. Reste P3→P6 + les défauts ci-dessous.

**État 2026-07-22 — jalon « corps Nu PROD » (`21f5b1a9`)** : B-P0 (bras scindé au coude, part `avantBras` réelle + vraie MAIN au poignet → fin du « double bras » en profil, P0 juge ATTEINT) + finition prod du corps Nu : dalle pelvienne éliminée, poche pubienne dégonflée (aine en V), dos = **FESSES** (fin du bas-ventre recopié), épaules arrondies (invariant silhouette torse front↔dos **0.0 %**), **occiput lisse** `BACK_CRANE` → **défaut #6/D4 (visage de dos) RÉSOLU** (+ 2 points faciaux retirés de la vue back de `crane-rase-M`), **pied nu LISSE** (`PLAINFOOT`) → **défaut TROLL/#48 (bottes) RÉSOLU** par la vraie racine : un **bug de DIVERGENCE de fallback** (le corps retombait sur Nu mais le pied était keyé sur la clé BRUTE → botte) ; fix = le pied se keye sur la tenue RÉSOLUE (`resolveWardrobeId`, miroir de `tenueFor`, `parts/career.ts`) — décision user 2026-07-22 : « Nu générique → pied lisse, monstres → griffe via leur propre def ». Offset de ceinture (hanche+épaule) **conscient de la carrure** (`applyBuild`, `pivot.x`×k, garde de monotonie) → débord de cuisse du build MINCE réglé. **AUDIT SQUELETTE (juge opus) — la racine des défauts structurels N'EST PAS le squelette** : emboîtement tête/jambes OK depuis P3 (menton +2 sur col, ourlet 8 sous genou), z-order cuisse/torse déjà symétrisé AU REPOS (`composeRig.tsx:357-361`, z=6 réservé au PROFIL où c'est voulu) → **défaut #50 (enfoncement) et l'hypothèse z-order RÉFUTÉS ; PAS de refonte squelette**. Mains = moufle sans doigts CONSERVÉE (décision user, suffisant à l'échelle). Pieds des monstres : #722 (routage) puis #736 (art des 3 types), voir plus bas. **RESTE : B-P1** (étalons Soldat armuré — armure RECOUPÉE au coude pour la part `avantBras` — + Sorcier robe), B-P2/P3 (recalibration ANIM via galerie animée `scripts/gen-clip-anim-gallery.mts` = le risque), B-P4 (~art defs dos/profil), B-P5 (recette combat).

**État 2026-07-22 — B-P1 LIVRÉ (`17e773c5`)**. Migration du membre supérieur en 2 segments par **DÉRIVE** (voie C tranchée par Fable — décision d'archi) au lieu de réécrire 101 defs : `splitBrasSvg` (`derive.ts`) découpe l'art `bras` PLEINE LONGUEUR au coude (y=18, clips partagés `fxGradients.ts`) → `.haut`→os épaule, `.bas` rebasé→os avant-bras ; `resolveUpperLimb` (`resolve.ts`) résout le membre en UNITÉ + couvre l'avant-bras jusqu'au poignet de la **matière dominante** du bras (`avantBrasBase`+`dominantCloth`) — **décision user 2026-07-22 « couverture pleine partout »** (sauf chair Nu/monstre = reste peau). Armure front-only : pilotage sur l'art RAW @tokens + `matterResolve` (palette d'armure) en fin sur bras haut ET avant-bras → 3 vues en matière d'armure (fin de l'incohérence front↔profil). Écoutille override `TenueSet.avantBras` (0 def l'utilise). **COULEUR ARMURE** : `Plaque.ts`+`Maille.ts` avaient les tokens INVERSÉS (fond=ombre `@metalO` presque noir) → corrigé (fond=`@metal` acier clair) — réponse au constat user « pourquoi la plaque si sombre » ; Cuir/Rembourré déjà OK. Garde Lot 5 : `resolve-membre`/`split-bras` (comportement), cliquet `avantbras-override-ratchet` (MAX=0), rapport `scripts/qc/bras-coverage-report.mts`. **B-P4 ABSORBÉ par la dérive** : queue = ~6 defs à art `bras` atypique seulement (prophete-gris, chevaucheur-de-blaireau, charlatan, ingenieur, chevalier-errant, intendant), pas 101. 2 juges positifs (diff TIENT, vision bloquant-levé). **RESTE : B-P2/P3 = recalibration ANIM** sur le bras scindé (le vrai risque, galerie animée `scripts/gen-clip-anim-gallery.mts`) ; B-P4 = polir ~6 defs atypiques (piloté par le rapport) ; B-P5 recette combat. **Pieds = chantier séparé #722 (routage) + #736 (art des 3 types) — CLOS.**

**État 2026-07-22 — B-P2/P3 LIVRÉ (`9756ce7a`)** (anim du bras scindé, LÉGER — 30/39 clips posaient déjà `avantBras*`). POISON purgé dans `mountedRig.ts` (commentaire périmé « avant-bras NON dessinés → membres droits », faux depuis D1) + `seatedBody` enrichi (pli de coude assis). 2 clips recalibrés (`STAFF_BLOCK`, `arme_feu` monté) pour plier le coude. Juge vision : aucun bloquant, assise+marche INTÈGRES. **Polish anim ouvert (ticket)** : amplitude staff-block, prise 2-mains (2e main sur hampe), arme qui clippe le sol en marche, balancement de bras. **RESTE du chantier : B-P4** = polir ~6 défs à art `bras` atypique (prophete-gris, chevaucheur-de-blaireau, charlatan, ingenieur, chevalier-errant, intendant — piloté par `scripts/qc/bras-coverage-report.mts` ; RENDRE d'abord pour voir lesquels ont VRAIMENT besoin d'un override avant d'en peindre) ; **B-P5** recette combat navigateur.

**État 2026-07-22 — B-P4 RÉSOLU SANS OVERRIDE (rendu diagnostic des 6 défs signalés).** Sur les 6 candidats du rapport `bras-coverage-report`, seuls **3 ont un avant-bras réellement NU** (`prophete-gris`, `charlatan`, `intendant` — manche à revers + peau dessous, fidèle à leur art d'origine) ; les 3 autres (`chevalier-errant` plaque acier, `ingenieur` manche brune, `chevaucheur-de-blaireau` manche crème) sont DÉJÀ couverts par la dérive (le flag « large au coude » = la spallière, faux positif de couverture). **Arbitrage user 2026-07-22 (verbatim : « Les laisser nus — c'est un look d'époque assumé (manches à crevés/bouffantes) »)** : les 3 avant-bras nus RESTENT nus (mode Renaissance, avant-bras découvert) — la règle « couverture pleine partout » ne visait que les cas FAUX (armure), PAS ce style. Donc B-P4 n'ajoute AUCUN override, cliquet `MAX_AVANTBRAS_OVERRIDES` reste 0. **La DÉRIVE seule couvre tout le roster — B-P4 est CLOS sans peindre un seul def.** **Leçon d'ORDONNANCEMENT** : une recette in-game ne peut PAS tourner depuis un worktree (`node_modules` VIDE, ne jamais le junctionner cf. [[game-worktree-node-modules-junction-hazard]]) — elle se joue APRÈS le merge sur `main`, et la régénération des goldens se fait à la porte. La validation statique (renders + juges) reste faisable sur la branche, mais elle ne remplace pas la recette.

**État 2026-07-22 — REFONTE JAMBE EN DÉRIVE ouverte (arbitrage user « refonte tout de suite »).** Constat : les tenues HABILLÉES **dupliquent** leur art de jambe inline (~195 fois), recopiant le défaut de galbe → même smell que le bras avant B-P1. Plan Fable : voie **(A) composition d'authoring** — un builder partagé `jambeVetue({tissu, surfaces, deborde?, galbe?, botte?})` que les defs APPELLENT (pas de nouveau chemin runtime, conforme D3). Mesure : 74 % suivent la jambe / 22 % débord léger / 5 % override (budget 8). LIVRÉ : **jambe Lot 0** (`b08a4fd3`, gabarit `parts/bodies/jambe-gabarit.ts` — contour+galbe PARTAGÉS déménagés de nu.ts, clips `rigJambeClip*`, Nu 1re instance byte-identique, cliquet `JAMBE_LEGACY`=103→cible 0 + `JAMBE_SILHOUETTE_OVERRIDES` MAX=8) + **jambe Lot 1 pilote** (`3acf63d3`, 6 defs migrés : Soldat/Sorcier/Chevaucheur/Charlatan/Chevalier/Villageois ; cliquet 103→97 ; 0 override ; **budget `deborde` calibré ~2,0 < 2,5** ; juge vision OUI). **⚠ LEÇON PALETTE pour la masse** : `@{tissu}O` n'est PAS toujours une ombre (Chevalier `vet1O`=ROUGE) → LIRE la palette, hériter le bon token d'ombre. **Masse restante ~97 defs (Lots 2-3-4) → ticket #759** (`domaine:art`+`sev:smell`), passe DÉDIÉE (arbitrage user 2026-07-22 « figer ici, masse en ticket »). Front-only → dos/profil générique = résiduel connu (vrai fix 3-vues = lot futur).

**Tickets de la session** (label `domaine:art`) : #722 (routage pieds monstres→griffe) et #736 (art des 3 pieds botte/nu/griffe) CLOS ; #745 (polish anim : staff-block, prise 2-mains, arme qui clippe le sol) et #759 (migration masse jambe) OUVERTS. **Le chantier est sur `main`** : P1 `ddfeded6` → P2 `c51da3e3` → P3 `ce6b368f` → suppression des tenues de classe → corps Nu `4f6a66f0` → B-P0 `21f5b1a9` → B-P1 `17e773c5` → B-P2/P3 `9756ce7a` → jambe Lot 0 `b08a4fd3` → jambe Lot 1 `3acf63d3` ; porte de merge `7b51eaa6` (jambières FRONT jointives à la cheville sur 4 armures, garde de portée, goldens régénérés).

**Vérif à DURCIR (user 2026-07-21, « tu utilises un agent pour vérifier tes images ? j'ai des doutes ») :** mon œil sur-valide (double-bras dit « occulté » alors qu'il doublait ; troll en bottes raté ; panels aussi). → l'ACCEPTATION visuelle passe par un agent vision DÉDIÉ à check-list ÉNUMÉRÉE stricte (par vue × entité), pas un coup d'œil global. Deux checks à AJOUTER : (i) **hauteur + proportions du PROFIL == front/dos** (l'outil silhouette ne compare que front↔dos ; le profil, même projection différente en largeur, doit avoir la MÊME hauteur/ratios — jamais vérifié) ; (ii) **un MONSTRE ne porte pas de bottes** (voir la règle de résolution ci-dessous).

**Un MONSTRE ne porte pas de bottes (constat user 2026-07-21) :** le pied est une ZONE résolue comme la tête, le torse ou les jambes — table de priorité de `parts/resolve.ts` (override → armure → tenue → repli de chair d'espèce) : `out.pied = equipWinner('pied', …) ?? PIED_NU[extremites]`. Une BOTTE est un HABIT porté (`tenue.pied`, gabarit `tenues/botte-gabarit.ts`), JAMAIS un repli ; une espèce nue retombe sur SA chair — `CLAWFOOT` (serres `@griffe`) pour les monstres, `PLAINFOOT` (lisse, #481) pour les civilisés va-nu-pieds (`bodies/extremites.ts`). Le check de recette reste : par vue × entité, vérifier qu'aucun monstre nu ne rend une botte.

**Défauts profil/dos/proportions repérés par l'utilisateur (2026-07-21, POUR PLUS TARD — il l'a dit explicitement : « je te donne ces informations pour plus tard ») :**
1. **SQUELETTE — proportions** : tête ENFONCÉE dans le corps + jambes ENFONCÉES dans le corps, sur les 3 vues → pivots/ancrages/longueurs du gabarit bipède (`skeletons.ts`) à REFAIRE. Phase distincte du contrat de vues (« faudra sûrement refaire le squelette du bipède »).
2. **Pied nu en profil** : `PLAINFOOT` (civilisé va-pieds, #481 — PAS `CLAWFOOT`) — ses « orteils suggérés » en profil se lisent comme des GRIFFES à l'échelle de jeu → art de pied nu profil POC. (vague d'art / vues `pied`).
3. **Bras en profil** : « doubles bras » — les deux épaules-bras rendues en silhouette latérale se dédoublent visuellement → connexité bras + scission avant-bras (D1) + vraies vues `bras` back/profile (P5).
4. **Casque/coiffe ne couvre pas l'arrière du crâne** (question user « le casque est le même front/dos ? ») : coiffe/heaume sans vraie vue `back` qui ENVELOPPE le crâne → révélé par le crâne plein de P2 (chair nue derrière un heaume trop court). Relève des vues `tete`-équipement (coiffe/equipment 3-vues). ⚠ à élucider : le def `Chevalier-du-loup-blanc` déclare « tête nue » (pas de slot `tete`) — d'où vient le heaume rendu ? (art d'emblème Loup Blanc vs vraie coiffe).
5. **Cou un peu long en vue de DOS** (légère sur-correction du canon squelette, panel vision 2026-07-21) — tuning à surveiller, non bloquant.
7. **CORPS `Nu` (`parts/bodies/defs/nu.ts`) = POC à REFAIRE prod** (user 2026-07-21, « refaire la tenue Nue… suffit de voir le profil et son code ») : `torseFront` largeur ±13 ≠ `torseBack` ±8.5 (torse de FACE plus large que le DOS = la divergence de silhouette mesurée #1) ; haut −28 vs −27 et ourlets différents (2 formes, pas 2 vues) ; `jambe` = UN seul path pour les 3 vues (pas de jambe de profil dédiée → profil bizarre) ; paths à nombres magiques. → **CIBLE #1 silhouette = refonte prod du corps Nu** (torse front=dos, vraies jambes/bras 3-vues, code propre), confiée à Fable, gardée par `scripts/qc/silhouette-coherence.mts` (front↔dos ≈0) + panel + œil user. Fondation (tout repose dessus).
6. **`BACK_CRANE` (crâne arrière P2) lit comme un VISAGE de dos** (sourcils + ligne de sourire dus au galbe/reflet du dôme) → contredit D4 (pas de visage de dos) VISUELLEMENT. Fixe d'ART ciblé : simplifier le modelé du dôme `BACK_CRANE` (cosmetic.ts) pour un occiput lisse. NON lié au squelette. Priorité : contredit une décision utilisateur (pas juste du polish).

Fichiers clés : `parts/resolve.ts`, `composeRig.tsx`, `parts/types.ts`, `bones.ts`,
`parts/cosmetic.ts`, +`parts/derive.ts` (nouveau), `skeletons.ts`, `viewArt.ts` (modèle).
Lié : [[game-rig-socle-audit-2026-07-16]] (41 % des vues fabriquées, même racine),
[[game-doctrine-une-tenue-nhabille-pas-le-porteur]] (chair au personnage — préservée),
[[game-contrat-art-verdict-harnais-refute-jamais-certifie]] (harnais QC #635 survit),
[[game-orientation-monde-facing]] (Dir8), [[game-rig-static-3-views-direction]].
