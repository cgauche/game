---
name: project-1686-materiaux-etat-2026-09-05
description: "État du chantier #1686 (matières du monde en UN dataset materials.json, vague #1463) au 2026-09-05 — périmètre tranché (3 domaines), invariant reformulé par le juge de design, lots 1-2 POUSSÉS (946ccfe84 ; 39989b6d3+3999e8a2a+56af53b19, materials.json en place), lot 3a-1 POUSSÉ (224aa8964 : matières vivantes, re-cuisson roof/relief, idDe('material',<domaine>) par discriminant déclaré), lot 3a-2 CODÉ + JUGÉ + RECETTÉ (Codex OK, re-cuisson en jeu OK, mode création corrigé et re-recetté au DOM) et COMMITTÉ en `refs #1686` — #1686 RESTE OUVERT : la revue de PALIER du 2026-09-05 (`.claude/soldes/revue-palier-2026-09-05-f0f9436f5.md`, PARTIEL) a mesuré qu'une fermeture routerait 5 tickets neufs (plafond 1, « fan-out ≤ 1 »), que `-> inventaire #épic` ne masque pas un ticket ouvert, et que la ligne de DoD terrain vit dans #1690 ; le solde `1686.md` a été RETIRÉ de l'arbre et s'écrira à la fermeture (après #1690), UN seul reste routé, dispositions à la grammaire exacte (`-> #N` sans prose après, `-> RAS : …` avec deux-points, jamais de point final) ; tickets frères ouverts : #1690 terrains, #1691 relief en dur, #1692 index figés ARRAYS, #1693 bodyPlan console, #1694 libellés d'enums nichés, tickets frères #1690 (terrains TS→JSON), #1687, #1688"
metadata:
  type: project
  originSessionId: 4407a64f-b0ad-4d3d-b30f-ffca252025d6
  modified: 2026-09-05T06:02:11.525Z
---

Chantier #1686 démarré le 2026-09-05 dans la session du socle volumique (choix user « #1686 — fusion des matières »), worktree `.wt-1624` (= main 9fc4d4af9), fichiers de travail sous `.wt-1624/node_modules/.cache/` (le scratchpad de session est instable) : `invariant-1686.md`, `brief-1686-L1.md`, sondes du juge `juge1686/`.

**Invariant (juge de design, reformulé)** : une matière = une entrée de `src/data/materials.json`, champ `domain` = valeurs de `MaterialRef.domain` (`builders/types.ts:33-37`), périmètre `prop|roof|relief` (16 entrées à l'arrivée), forme PLATE + refine par domaine (patron `oups.ts`), charge commune ∅, aucun renommage de couleur, identité (domain, id) GARDÉE avec ids composés pour l'homonyme (`prop-ardoise`/`toit-ardoise`), façades par domaine sur `catalogEntry`, `plan` hors couverture (critère en DONNÉE), terrain hors (#1690), `structureAppearance` hors (composite qui référence des matières — son `material` bois|pierre devient une référence), Codex « Matières » par domaine, migration datée, cardinal sur le RÉSULTAT, sonde d'identité de rendu (hash des `FaceSurface` de toutes les scènes livrées).

**Lots** : L1 (POUSSÉ 946ccfe84 le 2026-09-05, rebasé sur af8873c42 : 4 conflits de CHIFFRES avec le tronc — `raw.manifest` 9→10 côté #1653, `TOTAL_ATTENDU` 11a 147→145, stock/registre relief 4, `field-consumers` lignes 294/3488 — résolus par MESURE dans l'arbre fusionné, jamais en choisissant un côté ; le post-rewrite régénère les docs mais `field-consumers.test.ts` reste à rejouer avant `rebase --continue`) — garde d'identité, ids composés + migration des références, purge `riser`/`sol-inconnu`, `type` dans `roofs/types.ts` et `structures/types.ts`, parse zod au chargement (les 4 datasets étaient CASTÉS `src/data/index.ts:2383-2683`), `plan` hors couverture (`validateScene.ts:22`, `Inspector.tsx:43`), `validateScene` ne lit plus un JSON en direct ; L2 (POUSSÉ 56af53b19 : `materials.json` 16 = 8/4/4, forme plate + refine `CHARGE_PAR_DOMAINE`, `DOMAINES_MATIERE` sous `satisfies`, vues dérivées figées à l'import, exemption Codex en DETTE `#1686` dans `exposition-contrats.test.ts`, orphelin `materials.json#0 (16)` dans `registry-enveloppe`, banc de portes de migration AUTONOME par projection inverse — leçon : un banc ne lit jamais `git show HEAD:`, ça meurt au commit suivant) — dataset fusionné + migration + TOUS les lecteurs (façades, `src/data/index.ts`, éditeur, scripts docs/guards, `_ids`, `_registry`) + docs dérivés, jamais une demi-migration ; L3a-1 (POUSSÉ 224aa8964 le 2026-09-05 ; leçons : un banc ne lit jamais `git show HEAD:` ; `ref.ts` ne peut pas importer le registre généré — cycle registre → defs/props → idDe à l'init ; docs:empreinte se régénère APRÈS le dernier commit de sources ; knip voit mort un export lu textuellement — le registre doit le consommer via `optionalFields`) puis L3a-2 — Codex « Matières » UNE catégorie groupée par `domain` (patron `mutations`), `edit.dataset` + `ARRAYS`, fraîcheur (fin des `MAP` figés, patron `lightTones`), référence par DISCRIMINANT dans la grammaire `ref.ts` (`ref('material','roof')`, sites `props.ts:29-34`, `scene.ts:205,232`), `MaterialRef` en union discriminée ; L3b — `structureAppearance.material` est MORT (0 lecteur runtime, mesuré 2026-09-05) : purge vs domaine `structure` dans le dataset = question user. Trouvailles : `props.json` n'est PAS au Codex (palette de l'éditeur), `PROP_BY_ID` figé (`index.ts:2680`) à vérifier contre `setDataset('props')` ; #1691 ouvert (relief : ids de matière choisis en code par `floors.ts`).

**Pièges nommés** : un banc ne lit jamais `git show HEAD:` (meurt au commit suivant) ; `docs:empreinte` se régénère APRÈS le dernier stage de sources ; la porte de stock (image) compte +1 sur une entrée à virgule interne (faux positif nommé au CLIQUET) et sur un fichier de stock CRÉÉ ; le solde-guard est un hook de session, pas git — ses refus ne se voient qu'à la revue de palier ; `npm run galleries` ne rend AUCUNE planche de scène (rigs/armes/props) — la preuve de rendu est la sonde `FaceSurface`, pas une galerie ; `structuresStock.mjs` : 4 lignes « source clé absente » → 1, mais 36 ENTRÉES inchangées (le dire, ne pas le vendre comme décroissance) ; `dataset-id-parity.test.ts` ne couvre pas les matières.

**Why:** reprise sans relire le ticket et ses 3 commentaires. **How to apply:** lire cette fiche, `gh issue view 1686 --comments` (inventaire + décisions du 2026-09-05), puis le brief du lot en cours. Liens : [[user-arbitrages-2026-09-05-materiaux-1686]], [[user-direction-fusion-json-par-type]], [[project-1680-socle-volumique-etat-2026-09-03]].
