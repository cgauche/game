---
name: game-audit-moteur-rendu-2026-08-09
description: "Audit complet du moteur de rendu (6 lecteurs, 2026-08-09) — verdict par symptôme user + trouvailles hors symptômes, chiffré fichier:ligne ; base du choix d'ambition (consolider / WebGL / modèle de contenu)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5d129c4b-c665-4e81-9389-8f4edf55ae2a
  modified: 2026-08-10T00:06:21.337Z
---

Audit déclenché par le constat user (2026-08-09, verbatim) : « Le moteur semble etre le défaut de mon application […] la vue du dessus, iso et pov semblent chacun avoir une logique différente […] les murs, sols, plafonds, toitures, semblent se mener une guerre d'occlusion […] comptoir de bar […] impossible car ils sont en 1 et au maximum 4 directions […] les rampes qu'on ne voit pas en iso […] On a un problème fondamental » — puis « Tu t'es limité qu'à mes quelques symptômes » → audit élargi.

**Verdict consolidé** : le pivot géométrique ([[game-refonte-rendu-builders-backends]]) tient (builders sans caméra/store, top confiné aux backends). Ce qui n'a JAMAIS été unifié : (a) la SÉMANTIQUE de rendu — occlusion/tri/visibilité vivent N fois à la main par backend (~40 sites de règles spéciales ; 2 tris indépendants : 9 offsets iso vs FLOOR_BIAS/FLOOR_ZLIFT+6 biais POV ; 2 cutaways parallèles roofHidden-bbox vs clearedSpace ; même bug « dalle enterrée » corrigé 2× — une fois par moteur) ; (b) le MODÈLE DE CONTENU (grain « une case, un sprite, 4 m »).

**POV = demi-moteur parallèle** : contourne `buildTokens` (re-scan `scene.entities`, kind `personnage` seul → montures/non-bipèdes invisibles), neutralise la visibilité du pivot (`buildFloors(scene, undefined)` → viewTruth « tout visible ») et recalcule la sienne, ignore `state.explored` (zéro brouillard persistant), zéro overlay joueur, importe des internes d'affine (`COURSED`, `TINT_SPREAD` — frontière trouée).

**Props** : 116 defs, 5 directionnelles ; plafond STRUCTUREL 4 silhouettes (`ViewArt` = front/profile/back + miroir) ; aucun ancrage arête/fraction de case ; escalier = meuble-pictogramme sans géométrie (POV : 0/11 fichiers).

**Modèle de contenu — impossibles mesurés** : étage habité simultané (activeZ unique), mur hauteur variable (`WallSeg` sans champ hauteur, `WALL_H_M=4` en dur → pas de muret/garde-corps/comptoir-cloison), escalier 1-2 cases pour 4 m (`minFlightCells` exige ≥4 ; ni colimaçon ni trappe), fenêtre traversable/source de jour (décorative stricte), lumière par pièce (scalaire global), toit-terrasse/dôme, eau graduée. Contenu réel plafonne à 2 couches (18 scènes campagne toutes z0). Porte-à-faux vertical aligné = OK (`pont-vitrine`) ; LATÉRAL = non gardé (cf. [[game-iso-depth-z-secondary]] corrigée).

**Santé/perf** : `roofPans` cc=149, `buildComposition` cc=127, 15+ fonctions cc≥20 toutes couches ; `IsoStage.tsx` churn 210 commits/10 semaines (393 L). Opéra meublé ≈ 5266 SceneEl / ~13 900 faces avant culling SANS toits ; postes plein-carte au pas : `portalsForParty` (BFS/porte), `clearedSpace`/`lidCutaway`, `ClimbOverlays` (tout `scene.walls` par frame, ni memo ni culling) ; rAF par bipède même hors écran ; éditeur duplique le culling de `CulledScene` sans ses caches.

**Tickets ouverts par l'audit** : #1157 (poison JSDoc depth : offsets escalier morts), #1158 (`qc:env` ROUGE — vit-taverne case hors masse → instrument de non-régression visuelle cassé, passe vision bloquée).

**Rapports complets** : transcripts session 5d129c4b (6 lecteurs). **SPIKE WebGL LIVRÉ le jour même** (user : « je te laisse faire la preuve […] je jugerai sur pièces ») — commits `8d366ee4` (spike : `src/gameIso/backends/webgl/` 16 fichiers + écran dev `webglSpike` + capture CDP), `ce52632b` (state : dérivation bornée/couverture par étage/complétude de scène), `9c39b403` (arène : espèces #1165). **PROUVÉ par tests+mutations** : fidélité pixel three↔`tileCenter`/`project` à 1e-6 px (pitch DÉRIVÉ `atan2(pxPerM·TH/TW, LEVEL_H/MPL)` = 25,24° — ni 30° ni 26,57°, l'iso SVG n'est PAS une ortho, étirement vertical 1,17 dans la MATRICE), palette identique à l'octet en unlit, z-fighting dissous (biais coplanaire par rang d'émission, montants compris), rotation libre + zoom continus propres, rigs SVG rasterisés nets aux 3 zooms. **Verdicts juges VISION : « prototype prometteur »** — `siege-pov-lit`/`arene-pov-lit` « font un jeu » ; défauts de RÉGLAGE consignés (shadow casters des blocs terrain absents → rempart sans ombre 0,29 % vs 35 % arène ; vue top morte — murs sans coiffe ; ciel POV ; enceinte opéra disjointe — murs diagonaux ; billboards enfoncés dans la géométrie) ; VRAI coût du chantier = la couche d'ORNEMENT (colombages/croisillons/motifs = recettes de détail → textures/shaders). **Taille** : héros héroïque 3,6 m PLUS HAUT que les portes (81 vs 70 px) → recommandation juge « héroïque modéré ~2,2-2,4 m » (arbitrage #1160 EN ATTENTE user). Planches : `public/qc/spike-webgl.html` (62 PNG) ; écran live : `__wfrp.screen('webglSpike')`. Tickets nés du chantier : #1157/#1158(fermé)/#1159/#1160/#1161/#1162/#1163/#1165(fermé)/#1170/#1172 ; #1142 ROUVERT (revue de palier RÉFUTÉ). Design restant nommé : l'attribution corps↔case des masses est IMPLICITE (feet+ordre) — 2 passes de juge sur la même classe = trou de design, #1172. DÉCISION user en attente : GO/NO-GO chantier WebGL sur pièces.

**DÉCISION USER (2026-08-09, verbatims)** : après test live « Ah oui c'est 100x mieux ... », puis la Diligence ajoutée au sélecteur : **« Ok, on part sur ce moteur. »** — GO FORMEL du chantier WebGL. Le moteur de rendu CIBLE du jeu est le backend three.js sur les builders existants ; les backends SVG (affine + pov) sont en sursis de migration. Reste à trancher : convention de taille (#1160, question posée dans la foulée). Chantier à programmer : Phase 0 réglages spike (shadow casters blocs, coiffe vue top, ciel POV, murs diagonaux, ancrage billboards), Phase 1 couche d'ORNEMENT (recettes de détail → textures — le devis principal), Phase 2 intégration jeu (picking raycast, overlays, brouillard/explored/cutaway, perf), Phase 3 unification POV/top + retrait des backends SVG + migration QC. L'autre moitié de l'audit (MODÈLE DE CONTENU : murs hauteur variable, ancrage arête, vrais escaliers, lumière par pièce) se déplombe APRÈS — chaque ajout ne coûtera plus qu'une géométrie.

**Direction PRODUIT complémentaire (2026-08-09, verbatim)** : « Je compte bien me débarasser des billboards par des vrais objets 3d, et ne garder que les creatures en face/arriere/profil. » — le DÉCOR migre vers de la vraie géométrie (voie recommandée : vocabulaire PROCÉDURAL en donnée — primitives composables par def, opt-in par def, set taverne d'abord ; prérequis de schéma : `facing` libre en degrés pour les props + poignée de rotation à l'éditeur) ; seules les CRÉATURES restent en billboards 3 vues. Taille tranchée : convention `'jeu'` = 2,3 m (#1160, implémentée `billboardMath.JEU_ENT_H_M`). Consigné sur #1176.

**PHASE 0 SOLDÉE le jour même** (commits `819fcdba` webgl + `a1badc7b` #1181, suite 16 412/0, point d'étape complet au commentaire #1176 du 2026-08-09 soir). Verdict vision de sortie : **« maquette jouable »**. Acquis : boîtes de murs à coiffe (top 3→2329 couleurs), soleil fixe 38°/SO + lumières neutres (ombre rempart 0,29→6,8 %, bois re-dans-famille), polygonOffset billboards (zéro déplacement monde), ciel POV, fit-to-content, convention 'jeu' 2,3 m par défaut, `scripts/qc/spike-checks.mjs` = les mesures des juges vision devenues GARDES d'échec du pipeline de capture. #1181 fermé par la RÈGLE (adoption des colonnes à sommet ouvert au DÉBOUCHÉ — 2 réfutations latentes du juge corrigées avant commit). Restes ticketés : #1179/#1180/#1182/#1185/#1186. ⚠ DESIGN dû avant tout nouveau code sur les masses : 3 passes de juge sur la classe → #1172+#1186 (fusion des masses contiguës, toit CONNEXE = l'intention de maquette user au verbatim) exigent un invariant jugé AVANT le premier codeur. Prochaine phase : Phase 1 ornements (recettes de détail → textures). Leçon d'outillage consignée par un juge : en PowerShell, `-shl` sur un byte tronque au type gauche (packing RGB faux) — `([int]$b)*65536`.

**CHANTIER MASSES SOLDÉ le soir même** (commits `e11f1728` #1186 + `b5e42445` #1172 + `52dcb8b6` normes webgl + `589ce4ab` clause skill ; suite 16 474/0 ; étape complète au commentaire #1176). Verdict vision : **« la maquette est satisfaite »** — champ de hauteur PARTAGÉ par groupe de nappe (corps+z+égout ; noues automatiques, joint Diligence 8,00→11,10 m sans marche, chevron mort à 99,97 % de coïncidence d'arêtes, pignons 2/2). Règle « la carte décide » gravée : masses DÉRIVÉES groupent librement, AUTHORÉES à réglages identiques seulement, flat/shed jamais (`mayShareNappe`). Ordre-invariance : `validateArchitectureResiduals` (2 non-bornés en concurrence = erreur nommée), éditeur qui borne à la naissance, `architecture-0` purgé. Design tenu par la réfutation : invariant v1 réfuté (3 pts) → v2 ; sortie re-réfutée (pente/profil authorés écrasés) → M3 ; UN claim de juge réfuté PAR le codeur (`RoofEl.profile` avait des lecteurs). REGISTRE DES FOSSILES tenu à jour sur #1176 (+ [[feedback-registre-fossiles-transition]]). Reste du grief #1186 (rives de pignons, couronnement au larmier, faîteaux) → Phase 1 ornements, dont le GROUNDING est lancé (inventaire recettes de détail → textures).

**PHASE 1 ORNEMENTS SOLDÉE (2026-08-10)** — 6 commits : `ce9c51f8` (fondation detail/courses + faceSurface + UV doubles), `cf9a1d95` (textures de période), `60a03170` (colombage), `908d004e` (#1196 capture résiliente), `81efc349` (P1-D accents de sol en InstancedMesh), `2b228e4e` (P1-E relief mince). 8 gardes permanentes (7 de planche + gardes d'ombre PAR SCÈNE côté vitest). Causes profondes trouvées par MESURE : jointFactor clampé, teinteRatio sRGB→linéaire (−42 % contraste), cache nié par seed d'arête (1 572 cuissons/56 Mo), needsFaceBake aveugle à la part, budget d'accents en densité/m² (mpt² réfutait la borne par tuile), biais d'ombre du pire du registre (opéra 0,2118 m, chemin worldShadowBox) > défauts initiaux → 0,26 m gardé par scène. P1-E : boîtes CENTRÉES sur le plan médian (le contrat `Face` n'a PAS de notion de joue int/ext), deux familles saillie/traversant (9 parts sont la MATIÈRE de leur ouverture, pas un décor — design initial réfuté par juge AVANT codeur, 5 angles sur 6), profondeurs = DONNÉE d'apparence (`StructureAppearanceDef.relief`, `fasciaThickM`), jamais une table en dur. Tickets restants : #1198 (canal accents muraux), #1201 (coiffes uv1). Observation : façade diligence « matelassée » = cadence authorée un-cadre-par-case, pas le calibrage. Suivant : Phase 2 intégration jeu.
