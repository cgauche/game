---
name: game-audit-moteur-rendu-2026-08-09
description: "Audit complet du moteur de rendu (6 lecteurs, 2026-08-09) — verdict par symptôme user + trouvailles hors symptômes, chiffré fichier:ligne ; base du choix d'ambition (consolider / WebGL / modèle de contenu)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 5d129c4b-c665-4e81-9389-8f4edf55ae2a
  modified: 2026-08-16T20:35:48.483Z
---

Audit déclenché par le constat user (2026-08-09, verbatim) : « Le moteur semble etre le défaut de mon application […] la vue du dessus, iso et pov semblent chacun avoir une logique différente […] les murs, sols, plafonds, toitures, semblent se mener une guerre d'occlusion […] comptoir de bar […] impossible car ils sont en 1 et au maximum 4 directions […] les rampes qu'on ne voit pas en iso […] On a un problème fondamental » — puis « Tu t'es limité qu'à mes quelques symptômes » → audit élargi.

**Verdict consolidé** : le pivot géométrique ([[game-refonte-rendu-builders-backends]]) tient (builders sans caméra/store, top confiné aux backends). Ce qui n'a JAMAIS été unifié : (a) la SÉMANTIQUE de rendu — occlusion/tri/visibilité vivent N fois à la main par backend (~40 sites de règles spéciales ; 2 tris indépendants : 9 offsets iso vs FLOOR_BIAS/FLOOR_ZLIFT+6 biais POV ; 2 cutaways parallèles roofHidden-bbox vs clearedSpace ; même bug « dalle enterrée » corrigé 2× — une fois par moteur) ; (b) le MODÈLE DE CONTENU (grain « une case, un sprite, 4 m »).

**POV = demi-moteur parallèle** : contourne `buildTokens` (re-scan `scene.entities`, kind `personnage` seul → montures/non-bipèdes invisibles), neutralise la visibilité du pivot (`buildFloors(scene, undefined)` → viewTruth « tout visible ») et recalcule la sienne, ignore `state.explored` (zéro brouillard persistant), zéro overlay joueur, importe des internes d'affine (`COURSED`, `TINT_SPREAD` — frontière trouée).

**Props** : 116 defs, 5 directionnelles ; plafond STRUCTUREL 4 silhouettes (`ViewArt` = front/profile/back + miroir) ; aucun ancrage arête/fraction de case ; escalier = meuble-pictogramme sans géométrie (POV : 0/11 fichiers).

**Modèle de contenu — impossibles mesurés** : étage habité simultané (activeZ unique), mur hauteur variable (`WallSeg` sans champ hauteur, `WALL_H_M=4` en dur → pas de muret/garde-corps/comptoir-cloison), escalier 1-2 cases pour 4 m (`minFlightCells` exige ≥4 ; ni colimaçon ni trappe), fenêtre traversable/source de jour (décorative stricte), lumière par pièce (scalaire global), toit-terrasse/dôme, eau graduée. Contenu réel plafonne à 2 couches (18 scènes campagne toutes z0). Porte-à-faux vertical aligné = OK (`pont-vitrine`) ; LATÉRAL = non gardé (cf. [[game-iso-depth-z-secondary]] corrigée).

**Santé/perf** : `roofPans` cc=149, `buildComposition` cc=127, 15+ fonctions cc≥20 toutes couches ; `IsoStage.tsx` churn 210 commits/10 semaines (393 L). Opéra meublé ≈ 5266 SceneEl / ~13 900 faces avant culling SANS toits ; postes plein-carte au pas : `portalsForParty` (BFS/porte), `clearedSpace`/`lidCutaway`, `ClimbOverlays` (tout `scene.walls` par frame, ni memo ni culling) ; rAF par bipède même hors écran ; éditeur duplique le culling de `CulledScene` sans ses caches.

**Tickets ouverts par l'audit** : #1157 (poison JSDoc depth : offsets escalier morts), #1158 (`qc:env` ROUGE — vit-taverne case hors masse → instrument de non-régression visuelle cassé, passe vision bloquée).

**Rapports complets** : transcripts session 5d129c4b (6 lecteurs). **SPIKE WebGL LIVRÉ le jour même** (user : « je te laisse faire la preuve […] je jugerai sur pièces ») — commits `8d366ee4` (spike : `src/gameIso/backends/webgl/` 16 fichiers + écran dev `webglSpike` + capture CDP), `ce52632b` (state : dérivation bornée/couverture par étage/complétude de scène), `9c39b403` (arène : espèces #1165). **PROUVÉ par tests+mutations** : fidélité pixel three↔`tileCenter`/`project` à 1e-6 px (pitch DÉRIVÉ `atan2(pxPerM·TH/TW, LEVEL_H/MPL)` = 25,24° — ni 30° ni 26,57°, l'iso SVG n'est PAS une ortho, étirement vertical 1,17 dans la MATRICE), palette identique à l'octet en unlit, z-fighting dissous (biais coplanaire par rang d'émission, montants compris), rotation libre + zoom continus propres, rigs SVG rasterisés nets aux 3 zooms. **Verdicts juges VISION : « prototype prometteur »** — `siege-pov-lit`/`arene-pov-lit` « font un jeu » ; défauts de RÉGLAGE consignés (shadow casters des blocs terrain absents → rempart sans ombre 0,29 % vs 35 % arène ; vue top morte — murs sans coiffe ; ciel POV ; enceinte opéra disjointe — murs diagonaux ; billboards enfoncés dans la géométrie) ; VRAI coût du chantier = la couche d'ORNEMENT (colombages/croisillons/motifs = recettes de détail → textures/shaders). **Taille** : héros héroïque 3,6 m PLUS HAUT que les portes (81 vs 70 px) → recommandation juge « héroïque modéré ~2,2-2,4 m » (arbitrage #1160 EN ATTENTE user). Planches : `public/qc/spike-webgl.html` (62 PNG) ; écran live : `__wfrp.screen('webglSpike')`. Tickets nés du chantier : #1157/#1158(fermé)/#1159/#1160/#1161/#1162/#1163/#1165(fermé)/#1170/#1172 ; #1142 ROUVERT (revue de palier RÉFUTÉ). Design restant nommé : l'attribution corps↔case des masses est IMPLICITE (feet+ordre) — 2 passes de juge sur la même classe = trou de design, #1172. DÉCISION user en attente : GO/NO-GO chantier WebGL sur pièces.

**DÉCISION USER (2026-08-09, verbatims)** : après test live « Ah oui c'est 100x mieux ... », puis la Diligence ajoutée au sélecteur : **« Ok, on part sur ce moteur. »** — GO FORMEL du chantier WebGL. Le moteur de rendu CIBLE du jeu est le backend three.js sur les builders existants ; les backends SVG (affine + pov) sont en sursis de migration. Reste à trancher : convention de taille (#1160, question posée dans la foulée). Chantier à programmer : Phase 0 réglages spike (shadow casters blocs, coiffe vue top, ciel POV, murs diagonaux, ancrage billboards), Phase 1 couche d'ORNEMENT (recettes de détail → textures — le devis principal), Phase 2 intégration jeu (picking raycast, overlays, brouillard/explored/cutaway, perf), Phase 3 unification POV/top + retrait des backends SVG + migration QC. L'autre moitié de l'audit (MODÈLE DE CONTENU : murs hauteur variable, ancrage arête, vrais escaliers, lumière par pièce) se déplombe APRÈS — chaque ajout ne coûtera plus qu'une géométrie.

**Direction PRODUIT complémentaire (2026-08-09, verbatim)** : « Je compte bien me débarasser des billboards par des vrais objets 3d, et ne garder que les creatures en face/arriere/profil. » — le DÉCOR migre vers de la vraie géométrie (voie recommandée : vocabulaire PROCÉDURAL en donnée — primitives composables par def, opt-in par def, set taverne d'abord ; prérequis de schéma : `facing` libre en degrés pour les props + poignée de rotation à l'éditeur) ; seules les CRÉATURES restent en billboards 3 vues. Taille tranchée : convention `'jeu'` = 2,3 m (#1160, implémentée `billboardMath.JEU_ENT_H_M`). Consigné sur #1176.

**PHASE 0 SOLDÉE le jour même** (commits `819fcdba` webgl + `a1badc7b` #1181, suite 16 412/0, point d'étape complet au commentaire #1176 du 2026-08-09 soir). Verdict vision de sortie : **« maquette jouable »**. Acquis : boîtes de murs à coiffe (top 3→2329 couleurs), soleil fixe 38°/SO + lumières neutres (ombre rempart 0,29→6,8 %, bois re-dans-famille), polygonOffset billboards (zéro déplacement monde), ciel POV, fit-to-content, convention 'jeu' 2,3 m par défaut, `scripts/qc/spike-checks.mjs` = les mesures des juges vision devenues GARDES d'échec du pipeline de capture. #1181 fermé par la RÈGLE (adoption des colonnes à sommet ouvert au DÉBOUCHÉ — 2 réfutations latentes du juge corrigées avant commit). Restes ticketés : #1179/#1180/#1182/#1185/#1186. ⚠ DESIGN dû avant tout nouveau code sur les masses : 3 passes de juge sur la classe → #1172+#1186 (fusion des masses contiguës, toit CONNEXE = l'intention de maquette user au verbatim) exigent un invariant jugé AVANT le premier codeur. Prochaine phase : Phase 1 ornements (recettes de détail → textures). Leçon d'outillage consignée par un juge : en PowerShell, `-shl` sur un byte tronque au type gauche (packing RGB faux) — `([int]$b)*65536`.

**CHANTIER MASSES SOLDÉ le soir même** (commits `e11f1728` #1186 + `b5e42445` #1172 + `52dcb8b6` normes webgl + `589ce4ab` clause skill ; suite 16 474/0 ; étape complète au commentaire #1176). Verdict vision : **« la maquette est satisfaite »** — champ de hauteur PARTAGÉ par groupe de nappe (corps+z+égout ; noues automatiques, joint Diligence 8,00→11,10 m sans marche, chevron mort à 99,97 % de coïncidence d'arêtes, pignons 2/2). Règle « la carte décide » gravée : masses DÉRIVÉES groupent librement, AUTHORÉES à réglages identiques seulement, flat/shed jamais (`mayShareNappe`). Ordre-invariance : `validateArchitectureResiduals` (2 non-bornés en concurrence = erreur nommée), éditeur qui borne à la naissance, `architecture-0` purgé. Design tenu par la réfutation : invariant v1 réfuté (3 pts) → v2 ; sortie re-réfutée (pente/profil authorés écrasés) → M3 ; UN claim de juge réfuté PAR le codeur (`RoofEl.profile` avait des lecteurs). REGISTRE DES FOSSILES tenu à jour sur #1176 (+ [[feedback-registre-fossiles-transition]]). Reste du grief #1186 (rives de pignons, couronnement au larmier, faîteaux) → Phase 1 ornements, dont le GROUNDING est lancé (inventaire recettes de détail → textures).

**PHASE 1 ORNEMENTS SOLDÉE (2026-08-10)** — 6 commits : `ce9c51f8` (fondation detail/courses + faceSurface + UV doubles), `cf9a1d95` (textures de période), `60a03170` (colombage), `908d004e` (#1196 capture résiliente), `81efc349` (P1-D accents de sol en InstancedMesh), `2b228e4e` (P1-E relief mince). 8 gardes permanentes (7 de planche + gardes d'ombre PAR SCÈNE côté vitest). Causes profondes trouvées par MESURE : jointFactor clampé, teinteRatio sRGB→linéaire (−42 % contraste), cache nié par seed d'arête (1 572 cuissons/56 Mo), needsFaceBake aveugle à la part, budget d'accents en densité/m² (mpt² réfutait la borne par tuile), biais d'ombre du pire du registre (opéra 0,2118 m, chemin worldShadowBox) > défauts initiaux → 0,26 m gardé par scène. P1-E : boîtes CENTRÉES sur le plan médian (le contrat `Face` n'a PAS de notion de joue int/ext), deux familles saillie/traversant (9 parts sont la MATIÈRE de leur ouverture, pas un décor — design initial réfuté par juge AVANT codeur, 5 angles sur 6), profondeurs = DONNÉE d'apparence (`StructureAppearanceDef.relief`, `fasciaThickM`), jamais une table en dur. Tickets restants : #1198 (canal accents muraux), #1201 (coiffes uv1). Observation : façade diligence « matelassée » = cadence authorée un-cadre-par-case, pas le calibrage. Suivant : Phase 2 intégration jeu.

**PHASE 2 SOLDÉE (2026-08-11)** puis **PHASE 3 OUVERTE ET LARGEMENT AVANCÉE (2026-08-12/13)** — état complet aux commentaires #1176. Lot P3-0 « surcouches d'arène » **SOLDÉ sur pièces** (2026-08-13, 3 juges vision indépendants, chaque famille vue à l'écran) : 18+ commits — catalogue `highlightTints` (source unique TS des teintes, gardes catalogue↔CSS + relais), marques de cases en InstancedMesh (écriture GPU en place prouvée, grille DÉLINÉÉE par inset — la couture affine était un ARTEFACT de composition alpha), marques dynamiques par frame (tether opacité 1 rang au sommet, LineDashedMaterial réfuté sur mesure, pas du pointillé = `hypot(TW/2,TH/2)` via `projectStep`), anneaux d'équipe (ringDash pré-compensé en ARC ÉCRAN, uniformes à tout lacet, géométrie PAR KIND — top = discR), chrome d'écran (peintre UNIQUE `TokenChromeMarks`, ancre = TOISE `bodyTopFrac`/`bodyHeight`, uAllureAlpha APRÈS alphatest — le fantôme était DISCARD total), halos d'interaction (keyframes CSS→fonctions de frame, capacités couplées), couple monté composite (`seatRiderOnMount` pur, parité 26/26 transforms, boîte adaptée au débord, fuite de brouillard `mtd:` fermée aux DEUX voies). Assainissements de bord : zones bornées à l'ÉCRITURE (`inMapBounds`/`clampZoneTiles`), garde #842 à zéro, suite DÉTERMINISTE (factices en bac à sable + lecture-tolérante des 3 gardes scanneuses). Lot P3-1 POV volumique **CODÉ de bout en bout** (`b2d9a4e1`/`e4f9c2a3`/`c25710fe`) : PovStage monte GameStage3D en perspective (union StageFrame — JAMAIS un kind dans ProjKind), caméra glissante à cote lissée (`groundUnderM` bilinéaire, saut d'œil 1 m→0,1 m), billboards par le CAP Dir8 (anti-recuisson testé), biais anti-tranchage par `z_view` (pas la distance euclidienne — 0,300 m ±1 % jusqu'au coin), brume = fog natif + UNE surcharge de chunk gamma (#define, UN programme), `outdoorFog` réconcilié, 2 couleurs, affordances NON embrumées, `fogGamma` borné au schéma. Jalon d'écran P3-1 atteint (ciel/brume/entité orientée mesurés) — le SOLDE attend sa recette dédiée (liste au commentaire du 2026-08-13). ARBITRAGES user consignés : vue top tactique tabletop ([[game-arbitrage-vue-top-tactique-tabletop]]), caméra de jeu : **AIMANTS = les 4 diagonales seulement** (#1289 — l'arbitrage RESTREINT les crans-aimants de 8 à 4 pour tuer l'empilement des vues de face ; ⚠ il ne SUPPRIME PAS la promesse Phase 3 du programme #1176 « rotation/zoom CONTINUS au jeu, les crans deviennent des aimants » — la rotation continue aimantée reste un DÛ du chantier, relevé par le user le 2026-08-16 « tu mens, relis le ticket » : seule la moitié aimant fut livrée à b80502e3), focus de pièce = RIEN (cutaway+lumières suffisent, #1284), taille 2,3 m = référence (#1288 FERMÉ), **silhouette à travers les murs** pour les jetons occlus (#1297 — prochain lot de rendu, design à juger avant codeur). Tickets vivants : #1292 (miroir rig simple), #1293 (glisse verticale jetons + partyLeaderOf inline), #1295 (ombre affine vole-clic), #1296 (harnais recette : screenPos webgl, confirm ambigu, killEnemies, dynamicMarks() suggéré). Leçons d'outillage : le pane navigateur MCP ne composite pas les screenshots (2 sessions sur 2) — chaîne qui MARCHE : recetteur capture par CDP (`scripts/recette/lib.mjs`) dans le SCRATCHPAD → juge vision lit par `ctx_read(mode=auto)` ; les recettes 1-3 ont échoué sur pièges d'instrumentation (captures pré-combat, modales ouvertes, baseline « Chargement », 7 sessions mélangées) — le script `recette4/5.mjs` du scratchpad les désamorce tous et se réutilise. Prochains lots de phase : P3-3 éditeur (grounding lancé 2026-08-13) · P3-4 bascule+mort de l'affine (cliquet `double-voie-ratchet`) · P3-5 top tabletop.

**☠️ C5a COMMITTÉ (`c2b700b4`, 2026-08-14) — LA VOIE DE JEU AFFINE EST MORTE (point de non-retour franchi, validation utilisateur verbatim « Je valide. Pas parfait, mais rien d'irrémédiable »)** : 69 fichiers +1076/−2688. 7 morts importeurs-à-zéro (CulledScene, FogLayer, affineHighlights, MountedToken, WeatherVeil, tests, rempart-crest-depth) ; scissions (layers→wallLayerObjs pour TopoScene ; tokens→WalkPos/TokenCtx/propLayerObjs ; highlightLayer→combatHighlightsView ; Flies SURVIT hors garde !webgl — réfutation du brief) ; backends affine* RESTENT jusqu'à C5b (éditeur/QC/plan) ; MESSAGE sans-WebGL (webglSupport latché + SansWebgl role=alert, reset au harnais) remplace le repli mort ; VUE TOP restaurée par keepEl (tranché par le juge — la loi voyageait dans le viewZ des builders morts) ; 2 sauvetages (TapPreview mort en prod depuis C4 → MoveOverlays ; garde capsule #907 → actorCapsule) ; faux vert chrome-jeton débusqué ; 5 oracles re-fondés À PROVENANCE (règle C1 : au moment où la référence meurt) ; useMoveBlockedBump supprimé (trace = #1319) ; toggles UI morts, store stage3d survit jusqu'à C5b (POV SVG en vit) ; cliquet jeu à 3 (TopoScene/layers/tokens) avec inventaire C5b ÉCRIT (dont les 13 JSDoc des backends vivants) ; LEÇON MAJEURE : l'INDEX était pollué par 12 fichiers stagés du voisin — le juge l'a intercepté, commit par chemins stricts, index voisin intact. Suite 18312/18313, tsc 0, build 0. EN COURS : recette de non-régression 14. RESTENT : C5b (POV SVG dégraissé/renommé, requalification authoring, spike/devtools/docs, JSDoc, cliquets restants) · P3-5 top tabletop · passe de goût user (ouverte à tout moment, liste au ticket).

**🧹 C5b COMMITTÉ (`65de00d4`, 2026-08-14) — P3-4 SOLDÉ, LE MÉNAGE FINAL** : 123 fichiers +388/−8700. Requalification AUTHORING par git mv réel zéro ré-export (`backends/affine*`→`src/gameIso/authoring/{floors,walls,roofs,detail}Svg+project`, frontière JSDoc 3 consommateurs ; `backends/` = `webgl/` seul) ; POV SVG MORT (geometry 838L, billboards, usePovIdle — PovStage garde son NOM, il nomme la vue, 186→115L) ; store `stage3d` MORT ENTIER (devtool compris — RÉFUTATION du brief : le catch GameStage3D était déjà purgé à C5a) ; `stage/tokens` mort avec sauvetage WalkPos→`fx/walkPose.ts` ; spike mort (SpikeScreen, écran, 13 scripts qc) ; les 2 cliquets double-voie morts À ZÉRO sans tombale ; correctif du juge : 2 scripts de recette vivants appelaient `__wfrp.stage3d` (dont capture-jeu.mjs, l'instrument du chantier) — la grille « tout retiré est purgé PARTOUT » l'a attrapé ; ATTRIBUTION tranchée par la MESURE contre codeur ET juge (le rouge de lot = WIP voisin, byte-identique/`git show HEAD:` — le voisin a COMMITTÉ en vol `2b694d94`, HEAD avance sous les agents) ; résidus arbitrés : public/ hors commit (galeries non déterministes → #1321), 'webglSpike' orphelin dans store.ts voisin → #1320, layers garde ses paramètres LOD (capacité P3-5, pas du code mort). Tickets nés : #1320-#1325.

**🔍 AUDIT DES HÉRITAGES SVG (2026-08-14, juge frais arbre 2b694d94 — déclenché par le rappel user « ne pas se baser sur le cadavre de l'ancien moteur » + sa question « éclairages par case et par mur entier ? »)** — le mort qui dicte la forme du vivant, 7 zones, sondes mesurées : **G4 teinte de visibilité PAR ÉLÉMENT D'ANCRAGE = BUG DE FIDÉLITÉ** (pas « par case » : `tintAt(span.cellKey)` teinte jusqu'à 17 CASES d'un bloc — arène 13,3 % de clés multi-cases, vitrine 20,1 % ; le « un élément = un fill » SVG transposé ; canal par sommet EXISTE (#1300), perf sans surcoût mesuré ; vision par case = règle de jeu LÉGITIME ; cutaway franc = arbitrage tabletop LÉGITIME). **Lot C6 avant fermeture #1176** : teinte per-face + cliquet sonde, `UPRIGHT_PX`+`wallThicknessM` (largeurs de trait SVG→donnée authorée, fossile daté), capsule d'occlusion en pixels `TW*0.37`→métrique, `WalkPose.sortPt` MORT (force un faux Dims au POV, verrouillé par ses propres tests), renommage (15 en-têtes « SPIKE » de prod mensongers, `mode:'affine'`→vue 'plateau', doublon AffineKind/StageKind ; NE PAS renommer pxPerM tant que G1 vit — ils disent la vérité du couplage), JSDoc « Node-safe » de sceneMeshes réfuté à l'exécution. **Tickets gros œuvre** : #1322 affordances au MONDE (la caméra three = OTAGE de 16 overlays SVG au viewBox — StretchedOrthographicCamera correcte TANT QUE le lot vit), #1323 le pivot émet des SOLIDES, #1324 cutaway par raycast (clause de pureté réelle), #1325 picking de tuile par rayon, PROP_H_M→#1311. **LÉGITIMES attaqués et tenus** : renderRanks, stageLights/PointLights, fusion une géométrie, spriteRaycast, TopoScene/layers (coiffes 0,35-1,59 px mesurées), iso.ts (meurt avec G1).

**⚖️ C6 COMMITTÉ (`6f65a420`, 2026-08-14) — LES HÉRITAGES « AVANT FERMETURE » PAYÉS ; ARRÊT AVANT P3-5 (consigne user verbatim « Tu t'arreteras avant de commencer P3-5 »)** : 65 fichiers +2913/−397. G4 exécuté À LA CIBLE REHAUSSÉE par l'arbitrage [[game-arbitrage-grille-systeme-rendu-libre]] : champ de visibilité CONTINU par sommet (TintAt (x,y,z) continu, `visibilityField` Float64 par étage — le 32 bits arrondissait la valeur discrète, bilinéaire clampé ; fondu de frontière = VOULU ; corps/instances à l'échantillon de case, coordonnées entières = valeur discrète exacte ; ne tourne PAS par frame — site unique GameStage3D, deps stables) ; G2 : le monde ne convertit plus AUCUN px de trait SVG en mètres (WALL_MATTER_M & co au catalogue + relief.wallM, canal FaceDepth réutilisé ; mpt=2 inchangé MESURÉ par le juge, mpt≠2 corrigé) ; G3 capsule métrique (0,272 AVOUÉ résolution-à-l'envers conservant 23,68 px + clause-témoin, remesure rig = #1324) ; G6 sortPt mort ; G7 renommage (8 « SPIKE » réfutant le ~15 du brief, 'affine'→'plateau', ProjKind unique, interdit pxPerM tenu). GARDE NEUVE `bake-compte.test.ts` (compteurs de bake par scène étalon, politique de mise à jour au JSDoc). RÉFUTATIONS de la passe : la contradiction des triangles du rapport codeur tranchée PAR LA MESURE du juge (géométrie inchangée — la paire comparait le témoin RELIEF MINCE pré-#1300, provenance fausse) ; la 1re sonde FRONTIÈRE ne mordait PAS (NN passait) → contrat STRICT « teintes INTERMÉDIAIRES entre paliers » sur couleurs RENDUES, mutation 3 rouges ; oracle non circulaire (3 discriminants disjoints : loi du champ / câblage / fondu peint). ⚠ RÉPARATION D'ARBRE : `src/gameIso/authoring/` ABSENT du commit C5b (déplacement par FS + commit par pathspec n'embarque pas les non-suivis → 65de00d4..f65b0108 ne compilaient pas sur clone frais) — réparé ici ; LEÇON : après tout commit par chemins, `git ls-tree` des DOSSIERS NEUFS du lot. Suite 18260/18270 (unique rouge = editor.css voisin). RESTE À #1176 : P3-5 top tabletop (EN ATTENTE de la main utilisateur), solde/fermeture, passe de goût (+ le FONDU DE BROUILLARD de C6 à voir en jeu).

**🎲 P3-5a+5b COMMITTÉS (`d0505d30`·`6590eeb2`·`f0597104`, 2026-08-16) — LA VUE TOP EST TABLETOP, PROUVÉE À L'ÉCRAN** (reprise sur « Tu peux reprendre » ; design 3 passes de juge + arbitrages). **`viewPolicy`** = LA politique de style par vue en UN lieu (8 verdicts, frontière JSDoc « style = ce qu'on choisit de montrer, géométrie = ce que la projection rend possible » ; les 3 gates éparpillés migrés ; un verdict de plus = une ligne). Toits DÉCOUVERT PERMANENT en top ; régime SANS-SOLEIL gaté AUX LAMPES en une ligne (fade=0/lit=false/sun=null/ambiante pleine — tous les consommateurs suivent : modelé plein, parité monde/pions, disque de contact des pions REVENU = effet VOULU idiome VTT) — RÉFUTATION en chemin : le codeur a réfuté le juge par la mesure (« modelé plein sans soleil » vrai qu'à fade=0 ; à midi fade=1 EFFAÇAIT le modelé), le juge a ACTÉ et poussé le gate à la SOURCE (jamais au call site) ; précipitations ÉTEINTES en top (particule à pitch 90° = point immobile — l'écrêtage n'a plus de cas). **COMPOSITION UNE-LOI** : « three ne peint que les SOLS de l'étage actif ; murs/grille/portes/escaliers = surcouches SVG » — la vue top de JEU et le PLAN DE STATION convergent (`wallTraitObjs` de layers.tsx consommé par les DEUX, structuralObjs de TopoScene MORT) ; murs au trait ÉLÉMENTS **ET TUILES solides** (bloquant du juge vision : la loi ignorait les tuiles `mur` — contour de GROUPE via terrainSolidHeightM, 10 segments pour le muret 4 cases, sonde promue en 4 contrats) ; grille tactique = `authorGrid` DÉPLACÉ en foyer pur `src/geometry/grid.ts` (encre unique `--iso-grid`) ; **ÉPREUVE DE VÉRITÉ RÉUSSIE** : planSnapshot cesse de MENTIR SUR L'HEURE (SANS_SOLEIL mort → verdict de la politique + banc d'invariance 0/midi/23h). ÉCART DATÉ au registre : murs/grille SVG au-dessus des pions billboards (SVG>canvas), mort par construction en 5c. PREUVES : suite 18401/18410 ZÉRO rouge · recette 5/5 console 0 (13 captures) · juge vision DoD CONFIRMÉ (SHA256 identique cran 0 = retour des 4 rotations) + regard libre routé (pions décalés d'UNE case = l'ancrage billboard que 5c supprime ; anneaux/props → design 5c ; HUD-sur-modale + plan de station nu = PRÉ-EXISTANTS #1316 avec coordonnées ; joints de traits/semis d'herbe/cadrage → goût). GATE-ENTORSE consignée puis PAYÉE (2 runs de suite tués par la machine occupée → arbitrage user « Committer sur gates ciblés » pour 5a, la suite complète a couvert le cumul à 5b). **RESTE À #1176 : P3-5c les pions VTT** (suppression billboards top, disque+CAP wedgePath+discR par empreinte — EN ATTENTE : BodyToken/billboardMath occupés par la session d'ART) · solde/fermeture · passe de goût.

**🕳️ ARC « VOIR-À-TRAVERS » (2026-08-16, constat user sur capture réelle « on voit a travers les murs et le planché ») — SOLDÉ (L1/L2/M1/M2/câblage/pompe/M3, voir l'entrée M3 plus bas)** : diagnostic par juge frais (capture lue AU PIXEL : le seul vrai trou = la herse en bord de carte, 6656 px de clear color enclavés ; le reste = lidCutaway qui ÉVENTRAIT l'étage PLANCHER COMPRIS depuis la cour — 174-422/422 dalles, cause ANCIENNE fffd9e8c — + portes ouvertes en plaques noires opaques 0.19 + aggravant C6 rim −32/−48 %). **L1 `435b7627`** : ClearedSpace SCINDÉ (overheadCells=couvercle surplomb monde tout-tombe / liftedCells=levée d'écran LE SOL RESTE). **L2 `0b317fda`** : porte ouverte = TROU RÉEL (le 0,42 translucide du SVG = artefact de peintre, PURGE totale du vocabulaire embrasure — builder partagé, les 2 voies d'un coup) + fond hors-carte en donnée (iso.stageBg ; → #1331 il ne suit pas le palier de nuit). **Prototype silhouette naïf REJETÉ SUR MESURE** (worktree, 6 captures : la cour = héros perdu sur mer de tuiles, 361 cases ; « se lit SUR le toit, pas derrière ») → **DIRECTIVE USER [[user-doctrine-etat-de-lart-avant-invention]]** → étude sourcée (BG3 = découpe locale par occlusion : spherecast+sphere mask+dither+click-through, COMBINÉE au peeling de pièce à l'entrée + silhouette en filet). **M1 `4f0b04af`** (canal : aPercable par sommet sol-exclu-structurel, chunks #ifdef, garde de profondeur par vPercClip — gl_FragCoord RÉFUTÉ, sous la passe d'ombre c'est le raster de la LUMIÈRE —, cloneUniforms RÉFUTE ShaderLib → onBeforeCompile à 3 réfs partagées UN programme, materiauProfondeurPerce pour l'ombre, molettes 96px/0.6/300ms/4). **M2 `6b44a586`** (verdict : occludesActor RÉUTILISÉ — même loi que la levée, divergence impossible par construction, le Raycaster reste #1324 ; cadence événementielle clePercage). ⚠ **CÂBLAGE DIFFÉRÉ À ÉCHÉANCE (ticket)** : 5 lignes d'hôte dans GameStage3D occupé par la session d'art (+475L) — dès son commit : câbler, puis **M3** = mort de la branche occlusion de lidCutaway + jumeau en filet = ARBITRAGE USER sur 4 situations (cour/derrière/dedans/cour intérieure entourée). Rouges de suite attribués par mesure (built-brand +90L WIP voisin, player-text-ratchet=#1318 voisin, field-consumers=dérive de SON index.ts).

**🧭 ROTATION CONTINUE LIBRE COMMITTÉE (`a5c6129c`, 2026-08-16) — LE DÛ DU PROGRAMME LIVRÉ, RECETTE VERTE** : l'arbitrage « 4 diagonales » #1289 ANNULÉ par l'utilisateur (question VICIÉE — [[feedback-question-viciee-arbitrage-nul]] : elle ré-arbitrait le programme, périmètre ambigu, recommandation contre le programme ; « J'ai pris ton choix recommandé et c'était une grave erreur ») → refonte au modèle état-de-l'art (BG3 : rotation libre totale). LIVRÉ : tap = PAS FIN 2° exact (verbatim « 1-2 degrés » ; le MAINTIEN est la réponse à « 2° c'est lent », jamais un pas plus gros), maintien = continu 100°/s (seuil 250 ms, tap à l'ENFONCEMENT — aucune zone morte), drag bouton MILIEU pose absolue 0,35°/px (preventDefault anti-auto-scroll ; le droit = attaque, le gauche = pan), AUCUN ré-aimantage (repos libre, diagonales = angle initial seulement) ; stageYaw mode repos|libre|approche UNE boucle, DT_MAX_MS=100 (ajout codeur ACTÉ — onglet caché), Alt-Tab sûr par le REGISTRE des bindings (blur+visibilitychange → runUp de TOUT geste maintenu, socle) ; MORTS snapYawTarget/CRAN_JEU_DEG/AVANCE_MAX_CRANS + camera-quatre-crans.test (le nom portait l'arbitrage annulé) → lacet-libre.test ; viewRot INTOUCHÉ (quantification au quart = protection des memos ; sonde : artRot plancherise au QUART, atlasKey sans degré — un lacet continu ne recuit RIEN) ; minuterie via scheduleFlowTimer (garde #415, écart forcé, JSDoc). 5 mutations md5 remis ; UN commit fusionné (bisectabilité) ; entorse de suite consignée (machine occupée, arbitrage user P3-5a réappliqué). RECETTE VERTE 5/5 console 0 (tap 2° exact, maintien fluide arrêt persistant, drag 70,0° exact, Alt-Tab net, ON JOUE à 194° — clic de case OK). **POINT DE GOÛT OUVERT** : en VUE TOP la grille reste AXÉE CARDINAL (le lacet libre ne s'y applique pas — quantifiée au cran) : lecture VTT-plan-droit vs programme-partout, à trancher par l'utilisateur à la passe de goût, aucune des deux n'est un bug. Frictions harnais → #1296 (piège MODULE-FANTÔME Vite : import() nu = copie isolée quand HMR versionne ?t= — résoudre l'URL par performance.getEntriesByType('resource') ; exposer __wfrp.stageYaw() quand devtools.ts sera libre). HOOK SubagentStop installé (settings user) : fermeture auto des navigateurs headless de la session au retour de chaque agent (ciblage par session_id dans la cmdline, journal ~/.claude/hooks/browser-cleanup.log).

**🕳️✅ M3 COMMITTÉ (`3ac5b6f4`, 2026-08-16) — LA LEVÉE D'ÉCRAN EST MORTE, L'ARC VOIR-À-TRAVERS EST SOLDÉ** (arbitrage user verbatim « Le trou BG3 » sur la page 4-situations, câblage `a2373567` + pompe `dcb06311` au préalable) : `lidCutaway` morte ENTIÈRE + `cutawayLifted`/`liftedCells`/`liftedSections` en cascade (greps de mort à zéro) ; l'abri réel (`overheadCells`) mesuré bit-identique par sonde indépendante du juge (cour 0 nappe cacheuse retirée, sous-abri 178 cellules) ; reprojection des centres du trou AU PILOTE (`Percage.avancer(dtMs,camera,w,h)`, positions monde par référence VIVE `memoirePercée.monde` mutée par frame — pas une copie figée) ; `majVerdict` amaigri. Juge : « le lot TIENT », 7 lentilles — son seul grief structurel (angle mort : le banc mesurait la LOI, pas le `keepEl` monté) payé dans la foulée : contrat-LOI sur 356 postes de cour balayés (« une masse qui cache sans abriter ne perd rien », prémisses assertées, plancher de population >50 + ancres nommées — une mutation étroite avait fait rétrécir la population sans rougir) + contrat `keepEl` MONTÉ (espion classant sur `applyCutawayMask`, le seul qui morde une levée rebranchée dans l'hôte — démontré au rouge croisé). 4 mutations mordantes débranchées à la main (md5 = préimage). SUITE COMPLÈTE sur CE commit (1383 fichiers/18670 verts — l'entorse « gates ciblés » ×3 est SOLDÉE ; unique rouge = timeout 5 s `cascade-consequence-guard` sous contention, re-mesuré vert isolé ; leçon : la suite détachée via `Start-Process` + sentinelle EXIT survit au plafond 10 min de l'outil qui tuait les 5 tentatives précédentes). Vérifié au code du hook : le gate `docs:check` du pre-commit est DIFF-SCOPÉ sur les fichiers stagés (le rouge `docs/raw` du WIP voisin ne mord pas un commit purement `src/gameIso`). Observation consignée au ticket (non corrigée, vraisemblablement voulue — mémoire de brouillard) : `IsoStage.tsx:299` alimente `clearedSpace` avec l'EXPLORÉ, pas la vision vive ; premier rendu → nappes écartées une frame (#950) puis stables. RESTE À #1176 : P3-5c pions VTT (BodyToken/billboardMath LIBÉRÉS par le commit de la session d'art) puis solde/fermeture + passe de goût.

**✅ DOSSIER C4 COMPLET (2026-08-14) — LA BASCULE EST PROUVÉE SUR TOUS SES TERRAINS, la main est à l'UTILISATEUR** : verdict d'ARCHITECTURE « le volumique DÉPASSE l'affine sur son meilleur terrain » (A/B voie-contre-voie au même cadrage — colombage réel, tuiles en relief, créneaux ombrés vs panneaux plats ; RÉFUTATION DE MÉTHODE : la baseline env de C3 ne compare pas l'écran de jeu, la sonde `ab-affine.mjs` est le patron du comparatif licite) ; ciel nocturne POV corrigé `75e969df` (skyTexture×ambianceLum en LINÉAIRE — l'octet sRGB du brief réfuté : le monde s'atténue en linéaire ; povFog atteinte du même défaut, corrigée ; prouvé au pixel #5f6b74) ; capture-jeu ferme le briefing `18ca8a38` (signature du calque statique 29,4 % mesurée). Restes routés : #1315 (console repli), #1316 (plan de station indigne, sev:majeur), #1317 (props z1 supprimés pendant que le plancher est peint — bug PRÉEXISTANT aux 2 voies débusqué par la fausse piste du juge), cadrage planches → #1296 ; LISTE DE GOÛT LOT D consolidée au ticket (silhouette bleue sans traits #1301, nappes #1305, teintes de portée, faceShade nuit, texture de sol, billboards #1311, grain de bois, dégagement frangé, halo merlons, herse rot2, cadrage cran 3). **CONDITIONS DE C5a : (1) session de jeu utilisateur sur le nouveau défaut, (2) passe de goût LOT D — les deux sont des actes UTILISATEUR, rien ne se dispatche avant.**

**⚡ LA BASCULE C4 EST COMMITTÉE (`efa63ba7`, 2026-08-14, GO utilisateur explicite)** — LE MOTEUR VOLUMIQUE EST LE MOTEUR DU JEU : défaut `webgl` au store, gates DEV levées aux QUATRE sites (le 4e — TopoScene — trouvé par le codeur : le laisser = demi-bascule), chunk `three` séparé au vite.config (517 kB/130 gz HORS préchargement d'entrée — le lazy prescrit réfuté par la mesure du modulepreload de vendor), REPLI SANS WEBGL câblé (bloquant du juge : échec de création de contexte ⇒ setStageBackend('affine'), CulledScene remonte — le repli meurt à C5a, message joueur alors obligatoire, inscrit à l'inventaire du cliquet ; angle mort : webglcontextlost en cours de partie), 6 fichiers de tests débusqués qui vivaient sur le canevas vierge (migrés au BancRenderer). Commits du découpage : C1 `8ab863e5` · C2 `ab03c861` · caméra `b80502e3` (4 crans diagonaux, snap directionnel — la formule Math.round du juge réfutée par le codeur) · C3 `763a19be` (QC trié, baseline affine FIGÉE 6 planches 9,11 Mo — « non régénérable » réfuté, checkout documenté ; capture-jeu.mjs sur l'écran réel ; cartes orphelines → #1314) · C4 `efa63ba7`. EN COURS : recette de bascule (défaut webgl, DoD caméra empilement, plan+éditeur en prod, REPLI vu de l'écran, POV+météo) puis juge vision COMPARATIF contre la baseline. ⚠ C5a (mort de la voie de jeu affine, POINT DE NON-RETOUR) n'ira qu'APRÈS la session de jeu réelle de l'utilisateur sur le nouveau défaut — engagement explicite. Restes C5a inscrits au cliquet : affineHighlights/FogLayer/MountedToken orphelins à cet instant, 6 re-fondations de tests, test de chargement stage3d à RETIRER (pas retourner), message sans-WebGL. C5b : POV SVG dégraissé, requalification authoring, spike/devtools/docs.

**P3-4 DÉCOUPAGE INITIAL (2026-08-13/14) — historique du plan** : C1 ✔ `8ab863e5` (découplages : actorCapsule/occluders extraits, cliquet POV SVG liste ['pov/PovStage.tsx'] — l'HÔTE des deux voies jamais compté, regex imports RELATIFS trouvée par mutation ; C0 « ménage » RÉFUTÉ par le codeur : la sonde d'orphelins du juge s'AUTO-CONTREDISAIT, affineHighlights/FogLayer VIVANTS jusqu'à C5a ; requalification hors backends/affine* IMPOSSIBLE avant C5 — floorSvg enjambe la frontière ; règle généralisée : un test de référence croisée se re-fonde AU MOMENT où sa référence meurt) · C2 ✔ `ab03c861` (plan de station VOLUMIQUE par arbitrage user « Portage volumique » — INSTANTANÉ ÉPHÉMÈRE : renderer jetable 1 frame→blit 2D→dispose+forceContextLoss en finally, rétention deps+boîte de pixels, murs/marqueurs SVG — coiffes sous le pixel à 180 px mesurées ; worldSurfaceMaterials 3 sites→1 ; recette 12 : blit prouvé, 0 accumulation de contexte) · **#1300 SOLDÉ/FERMÉ ✔ `64d70280`** (modelé par famille d'orientation cuit PAR SOMMET — 3 réfutations du codeur TENUES : per-span impossible (boîtes multi-familles 71 %), famille lue sur la normale PRÉSENTÉE par versLExterieur (avant retournement = tous les sols en soffite, Newell sans convention), ambientLight = ID DE PALIER ; piste directionnelle MORTE par mesure (jamais >2 familles sur 4, jumelles adjacentes) ; vision : DoD TENU par preuve SUBSTITUÉE — le recetteur avait certifié sur un PROP BILLBOARD (leçon : sonde props∩spans disjoints à promouvoir), le juge a prouvé sur la maçonnerie ×4,6 ; restes #1310 authoring ambientLight, #1311 billboards enrichi) · **EN COURS : lot CAMÉRA #1289-2** (design au juge — 4 crans diagonaux arbitrés, aimant = la cible du lacet toujours multiple de 90° en jeu, nudge±45 reste DEV ; volets flou/limites durs HORS lot) · restent C3 (QC+baseline FIGÉE avant mort — env-panels tient pov/geometry en vie), C4 bascule (portes : caméra seule — #1300 ✔ #1284 ✔ fermé par arbitrage ; checklist plinthe/appareillage au ticket ; three ~150-180 Ko gz, lazy à trancher ; App.tsx:37 = spike screen à TUER pas dé-gater), C5a mort voie de jeu (POINT DE NON-RETOUR — planches SVG à figer avant, TopoScene/layers/affineHighlights/FogLayer/MountedToken deviennent orphelins À cet instant, 6 re-fondations de tests), C5b (POV SVG dégraissé/renommé, requalification authoring, spike/devtools/docs). LEÇONS : staging par HUNK quand 2 codeurs partagent un fichier (8ab863e5 avait embarqué le lecteur faceShade sans sa donnée — non-bisectable, réparé par 64d70280) ; backticks gh --body TOUJOURS par fichier (2 récidives).

**P3-3 ÉDITEUR SOLDÉ (2026-08-13, DoD vision CONFIRMÉ)** — 5 commits (`a7f8ac0a` vague A socle · `6ccc7144` vague B canaux · `328e4e5c` toggle découvrable · `48ece489` correctifs vision · `9f50a842` création au survol). Acquis structurels : EditorCanvas monte GameStage3D (MÊME composant, toggle stage3d), surcouches d'authoring en calque SVG au-dessus (contexte d'empilement posé), CADRE de caméra GÉNÉRALISÉ (`StageScreen` — jeu = cas particulier viewBox fixe, éditeur = viewBox mobile à échelle MESURÉE clientWidth, loi meet vs slice, pixel ≤1e-6 + letterboxé), CUISSON RETENUE PAR READ-SET gardée AU COMPILATEUR (`worldBakeDeps`, `Record<keyof Scene>` — trait 6385→47,5 ms, cuisson au relâché, gel restreint aux outils géométriques), gabarit de couche par canaux EXISTANTS (keepEl/tintAt, bascule isolé sous seuil 0,15 — un scalaire de sommet à 0 peint du NOIR), grille d'authoring PURE `authorGrid` (en affine = effet de bord du stroke des sols), cliquet frère `double-voie-editeur-ratchet` (mort affine éditeur inscrite P3-4). RÉFUTATIONS marquantes : aperçu soustractif de gomme = MENSONGE (eraseAt ne retire que des entités), canal chromeAt/dim continu MORT-NÉ retiré (aucun sujet d'éditeur ne porte de cid — la teinte couvrait déjà), zoom menteur = l'HUD (les 2 voies rendent le même pas au centième). Bugs réels trouvés par recettes : embusqué invisible (marque dans la seule branche isCombat), `createArchitecturePart`/`createRoofSection` posaient à (0,0) EN DUR (le test existant verrouillait l'origine PAR ACCIDENT — jamais de survol). Trou acté : preuve d'écran des toits en plan (l'éditeur ne charge pas de scène existante → #1296 ; contrat en positif sur fixture à toiture au montage réel). Restes : #1308 (désélection), #1309 (arête d'empreinte 2 px + échelle en infobulle), goût (touffes d'herbe, matière d'aperçu).

**P3-2 MÉTÉO SOLDÉ, #1247 FERMÉ (2026-08-13, commit `198b08fa`)** — design jugé 2 passes AVANT codeur : `THREE.Fog` en vue plateau RÉFUTÉ par sonde (rampe de profondeur caméra, 102 m d'écart entre coins, tourne avec le lacet) → NAPPES DE BRUME MONDE sur colonnes non couvertes du PLAN (même vérité `shelterField` que la pluie, retenues par clé de contenu), éteintes en top (pitch 90° = voile dégénéré, arbitrage tabletop) ; dérivation `dim=1−α` RÉFUTÉE (inversait le sens de neige/brouillard qui ÉCLAIRCISSENT en affine) → APPARIEMENT EN LUMINANCE `dim=(1−a)+a·L(teinte)/albédo-réf` (test de SENS sur les 4 météos — leçon : sous mutation le banc de MONTAGE restait vert, seul le test PUR refuse l'inversion) ; POV module l'existant (`povDepth(indoor,tightenK)` source unique des 2 consommateurs de portée, gate par FRAME `povIndoor`) ; écrêtage pluie au verdict de vue (`ShelterColumn.sectionId` = l'égout qui ARRÊTE, prédicat `sheltered`, 0 re-semis par identité d'objet) ; REGISTRE D'ORDRE DE RENDU né du lot (`renderRanks.ts`, 6 rangs, 10 sites — le tri pluie⇄nappe cessait d'être l'ordre de montage React). Recette 8 (4/4, console 0) + juge vision : brouillard/orage PROUVÉS à l'écran, écrêtage prouvé par SONDE DE COLONNE committée (l'image fixe est INDÉCIDABLE — projection oblique + 4,9 faux brins/1000px² au témoin) ; « cutaway actif hors bâtiment » = comportement CONÇU de `lidCutaway` #907 (levée par capsule en projection). Restes : #1305 (MATIÈRE des nappes : dalles crénelées, arête franche sur surfaces, éclaircissement local sous nappe nocturne −29 % de contraste de flaque, facteur dilué en neige, brouillard≡neige en top — avec goût LOT D et #1300), #1296 enrichi (setWeather, waitReady — délai de montage VARIABLE 600→1500 ms, heroStart zones-pieces au seuil). Page de preuves : artifact meteo-p32 (5 météos, même scène).

**P3-1 SOLDÉ + CHANTIER SILHOUETTE #1297 SOLDÉ ET FERMÉ (2026-08-13)** — P3-1 : recettes 5/6 + 3 juges vision (intérieur SANS ciel 0 px, fond = `pov.fogIndoor` exact, œil constant <1 cm, brume mesurée f=0,296 ; trouvaille → #1300 modelé intérieur NUL, chantier d'éclairage partagé, design à juger). Silhouette (arbitrage user) : 4 lots jugés sur design — A anneau traversant (jumeau de pool GreaterDepth/renderOrder −1, buffers PARTAGÉS, opacité 0,65) + B picker « jeton touché gagne » (occlusion sprite-contre-sprite CONSERVÉE, divergence affine S/E actée par table 4 arêtes mesurée — l'affine refusait DÉJÀ le clic derrière ses crêtes) = `1834f70f` ; C corps (jumeau ENFANT du quad = zéro écriture/frame, APLAT PLEIN couleur d'équipe — arbitrage user 2026-08-13 révisant le « contour teinté » initial, verbatim au ticket ; aplat = `diffuseColor.rgb = diffuse` après map_fragment ; alpha = constante × uniforme d'allure DU CORPS partagé ; D1 grave du juge : le couple MONTÉ se silhouettait en rouge ennemi → teinte dérivée du CAVALIER `teamRingDecor(rider ?? c)` + ordinal propagé) = `59fea43f`. Recette 7 + juge vision : DoD prouvé (aplat au pixel EXACT `#37c07a`, découpe anatomique, frontière = arête du mur, clic sur silhouette ⇒ bonne cible). Restes : #1301 (doser ENSEMBLE corps α=1 / anneau 0,65 — avec le LOT D goût), #1296 enrichi (readback webgl impossible sans `preserveDrawingBuffer` → capture CDP obligatoire ; `screenPos` inopérant en volumique ; `heroIndex` non exposé par `__wfrp.battle()`). Leçon de discipline : une formulation de commentaire choisie POUR esquiver une famille de la garde poison = contournement à rejeter — le commentaire porte le FAIT + le POINTEUR vers la trace (ticket), jamais la revendication d'autorité.
