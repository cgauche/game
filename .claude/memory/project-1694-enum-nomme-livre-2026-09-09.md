---
name: project-1694-enum-nomme-livre-2026-09-09
description: "#1694 LIVRÉ le 2026-09-09 (libellé d'une valeur d'enum SUR LE NŒUD : `enumNomme` = `z.enum(...).meta({ valeurs })`, lecture `valeursDe`/`libelleDeValeur`, socle 1er niveau supprimé, 40 Records UI morts, deux stocks décroissants) — leçons : le juge de design a réécrit la JUSTIFICATION (forme, pas compte de copies) ; agrégation par vocabulaire en OU = jumeaux muets ; les nœuds « à créer » existaient ; libellé fabriqué dans engine (`ModLine`) = contrat de socle ; recette qui édite une valeur refusée par le schéma conclut rouge à tort"
metadata:
  type: project
  originSessionId: 4407a64f-b0ad-4d3d-b30f-ffca252025d6
---

**#1694 livré (2026-09-09, wt-1624, trains A / B1 / B2 / B3 poussés séparément)** — troisième des quatre descendants de #1686 (#1691 → #1692 → **#1694** → #1693). Forme : `enumNomme(valeurs)` dans `grammaire/valeurs.ts` (zod 4.4.3 `.meta()` natif, survit à `.optional/.default/.array/.refine`, perdu par `.extract/.exclude` → garde structurelle), lecture unique `valeursDe(noeud)`/`libelleDeValeur(noeud, v)` dans la feuille `grammaire/meta.ts` (déroulé unique `noyauEnum` composé par `optionsEnum`), `noeudDuChamp(file, champ)` ; `MetaChamp.valeurs`/`verifieValeurs`/`ENUMS_DE_DOCUMENT` SUPPRIMÉS ; gardes `valeurs-de-champ.test.ts` (par VOCABULAIRE et PAR NŒUD, `muets === 0` dès qu'un jumeau est nommé) et `records-de-libelles.test.ts` (détecteur structurel des Records UI, formes `{clé: 'texte'}` et `{ id, label }[]`, stock GELÉ 47 → 9, restes → #1714).

**Leçons (mesurées)** :
- **La justification d'un design se mesure** : « une méta par chemin recopierait le libellé » était FAUX pour 14 des 16 Records (1 chemin chacun) ; la vraie raison est la FORME (`MetaDesChamps<C>` keyée par clé de 1er niveau ne peut pas nommer `[].outcomes[].on`). Le juge de design (`juge-design-1694.md`) a aussi tranché `.meta()` natif > WeakMap, `specsSource` en grammaire (jamais `data/index.ts` : cycle), stock par vocabulaire jamais par nœud (78 % des nœuds étaient des enums INLINE clonés par le `lazy` — hissés en consts : 2 275 → 210).
- **Agrégation en OU = jumeaux muets blanchis** : un vocabulaire « nommé » cachait 2 nœuds muets (`physique|mentale`) → un enum = UNE const partagée par ses hôtes, garde par nœud.
- **Les « nœuds à créer » existaient** (3/3 en B3) : mesurer avant de briefer ; un brief qui nomme des sites inexistants (« Codex Critiques », « Fiche héros État ») coûte 15 appels de recette.
- **Verbatim prouvé mécaniquement** : Records de `git show HEAD:` évalués contre `valeursDe(nœud)`, texte ET ordre — un test qui compare à une table recopiée n'est pas cette preuve.
- **Engine ne lit jamais un libellé de grammaire** (`mecanique.ts` dépend d'engine) : un Record dans `src/engine` migre en DESCENDANT son lecteur au site d'affichage ; quand le libellé alimente une `ModLine` fabriquée dans engine, c'est un contrat de socle (id résolu à l'affichage) → #1714.
- **Logique par libellé FR invisible à la garde** : `combat.ts:1302` `rangeBandName(...) === 'Bout portant'` — la garde ne jugeait que `.label` ; étendue aux retours d'appel comparés à un littéral FR (AST), 1 préexistant gelé (`combatFlow.ts:5926`, argument du trait `corruption`).
- **Recette** : une édition Codex REFUSÉE par le schéma (trou d100) conclut « pas de propagation » à tort → diagnostiquer par le chemin réel du `save` ; les onglets de catégorie et sous-onglets de fiche sont homonymes (`role="tab"`) ; aucune inspection des combattants en jeu ; « Atelier » est un toggle.
- **Régime** : budget d'horloge dans chaque brief tenu (juges 5-10 min, codeurs 20-35 min) ; la revue de palier n°8 exige `args.faits` INLINE (117 KB → compactés à 44 KB : corps de commit tronqués, texte complet dans `faitsChemin`) et son archive porte la fenêtre en SHAS (`fd660c8b8..34e3cbb6c`, jamais `HEAD`).

**Restes routés** : #1714 (9 Records : second axe, `ModLine`, homonymie, `<option>` JSX) ; #1463 (argument du trait `corruption` en libellé, colonne de classe dans `structures-donnees`) ; #1689 (`[bodyPlan]` à l'Atelier, groupes d'onglets, inspection en jeu, manœuvre invisible sous le seuil d'Avantage) ; #1679 (outillage de recette : `pendingCascade` hors `roll/confirm`, `fight()` sans acquittement, revue n°8 actions α/γ/δ/ε).

**Why:** reprendre #1693 puis fermer #1686 sans relire les rendus. **How to apply:** avant tout brief de migration, mesurer les nœuds existants et les sites réels ; toute garde structurelle déclare ce qu'elle ne voit pas avec cas mesuré ; un rouge de recette se diagnostique avant d'être cru. Liens : [[project-1692-index-vif-livre-2026-09-08]], [[project-1691-relief-en-donnee-livre-2026-09-07]], [[project-1686-materiaux-etat-2026-09-05]], [[env-budget-horloge-agents-2026-09-08]], [[feedback-un-detecteur-ne-mesure-que-sa-couverture]].
