refactor(data+engine): #1440 convergence — StakeForm NOMMÉ (14 recopies 'verbatim|descripteur' → 1 source, les 6 zod dérivent, l'optionalité divergente de flow-stakes tranchée et documentée), Availability dérivée du canon engine/types (4 schémas — harvestRarity qualifié), memoByRef composé par rig/races (12ᵉ réinvention purgée), chebyshev importé par les 5 tests qui le recopiaient (corrige #1440)

FOYER choisi = `src/engine/types.ts` par MESURE, pas par goût : c'est le seul module FEUILLE que les
deux couches importent déjà (`src/data/index.ts:100`, `src/data/schemas/defs/trappings.ts:10`), et
`Availability` — le concept jumeau du même ticket — y est canon depuis toujours. Poser le tuple dans
`src/data` aurait créé un cycle (`schemas/common.ts` importe `engine/menace` au runtime) ou forcé
`engine/activities.ts` à importer `src/data`. Patron du dépôt réutilisé tel quel : tuple `as const` +
type dérivé `(typeof X)[number]` (l'étalon `NIGHT_TEST_KINDS`/`NightTestKind`), les `z.enum` prennent
le tuple. Résultat : le lien zod↔type n'est pas vérifié, il est IMPOSSIBLE à rompre par construction.

TRANCHE de l'optionalité divergente (flow-stakes seul NON-optionnel) : c'est un CHOIX, pas un oubli,
mesuré en donnée — `night-stakes` 13/15 entrées SANS `form` (son type déclare `verbatim` par DÉFAUT),
`combat-stakes` 3/35 sans `form`, et ces 3 sont EXACTEMENT les 3 sans `template` (rien à qualifier),
`flow-stakes` 33/33 avec `form` et 33/33 avec `template` REQUIS. La raison est désormais écrite AU
SITE (`schemas/defs/flow-stakes.ts`). Aucune donnée touchée, aucun régime de garde déplacé.

harvestRarity QUALIFIÉ au Source, pas au brief : le 5ᵉ palier `Unique` est RAW — la table « COÛT DE
BASE POUR 1 ENC DE PIÈCES DE CRÉATURE BRUTES » imprime cinq colonnes (`ZI 13 l.286`, vérifié dans
`Source/WH - V4 - Le zoo impérial/13 - Sirène.md`). Il DÉRIVE donc du canon (`[...AVAILABILITIES,
'Unique']`) au lieu d'être une 5ᵉ liste tapée à la main.

REFUTATION : le commentaire de `data/index.ts:262` affirmait « `src/data` ne dépend jamais de
`src/engine` » — FAUX depuis longtemps (`data/index.ts:100` importe `engine/types`) ; corrigé en la
vérité mesurable (`src/data` n'importe pas `src/engine/activities`, seul le module FEUILLE
`engine/types` l'est). Le brief attendait qu'une 3ᵉ valeur ajoutée « d'un seul côté » rougisse : elle
ne PEUT plus être ajoutée d'un seul côté (une seule source) — ce qui reste falsifiable, c'est la
DÉRIVATION elle-même, et c'est ce que les deux gardes neuves mesurent.

JUGE (mutations, rouge remis à la main, jamais par git restore) :
 · StakeForm — littéral ré-inliné dans `defs/night-stakes.ts:26` → ROUGE
   « defs/night-stakes.ts:26 — StakeForm recopiée » (`unions-canon.test.ts`) ; rebranché → 5/5 verts.
 · Availability — littéral ré-inliné dans `defs/vehicles.ts:51` → ROUGE
   « defs/vehicles.ts:51 — Availability recopiée » ; rebranché → 5/5 verts.
 · Dérivation coupée — `stakeFormSchema = z.enum(['verbatim'])` → `npm run typecheck` passe de 0 à 2
   erreurs, dont `unions-canon.test.ts(23,7): error TS2322` (l'égalité de types `StakeForm` ⇄
   `z.infer<typeof stakeFormSchema>`) ; rebranché → 0.
 · memoByRef — WeakMap maison réintroduite dans `rig/races/index.ts` → ROUGE « cache maison réinventé
   — composer `memoByRef` » (`memo-canon.test.ts`) ; rebranché → 9/9 verts sur `rig/races`.
 · chebyshev — primitive `state/path.ts` mutée +1 → 3 des 5 fichiers rougissent (ai-cast, ai-threat,
   ai-zde-telegraph : 4 tests) ; mutée ×100 → ai-golden rougit aussi. `path.ts` remis à l'identique
   (`git diff src/state/path.ts` vide). ANGLE MORT HONNÊTE consigné : `broken-restriction.test.ts:66`
   reste VERT même à ×100 — son assertion (`> 1`) ne discrimine aucune métrique ; l'import de la
   primitive y est de l'hygiène, pas une preuve de mesure. Idem pour `ai-golden.test.ts:290` (±1 ne
   la bouge pas). Ces deux assertions faibles sont un défaut préexistant, signalé, non corrigé ici.

JUGE-VISION : aucun écran modifié — types, schémas zod et tests seuls ; zéro donnée `.json` touchée,
zéro composant, zéro CSS. Rien à recetter au navigateur.

Portes : `npm run typecheck` → 0 erreur TS. `npx vitest run src/data src/engine
src/state/night-stake-form src/gameIso/rig/races` → 330 fichiers / 4556 tests verts. Les 5 tests
touchés + `comment-poison-guard` → 6 fichiers / 107 verts. `npm run docs:check` → OK ; 4 rapports
GÉNÉRÉS régénérés dans le MÊME commit (`consommateurs-de-champs`, `index-moteur`, et les fiches RAW
`combat.md`/`combat-naval.md` dont l'ordre des symboles suit les lignes d'`engine/types.ts`).
`docs/donnees.md` régénéré aussi : son écart (structureAppearance 17→18) était DÉJÀ présent sur la
base 2d086cf7, il n'est pas de ce lot mais la garde l'exigeait.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013aEme3NYi7FgV4hYu3EX5Z
