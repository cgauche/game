# Marchand v1 (transactionnel + Disponibilité RAW) — Plan d'implémentation (#2)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans (exécution INLINE — `store.ts`/`scene.ts`/`combatFlow.ts`/`CampaignView.tsx` sont des fichiers CHAUDS édités par d'autres sessions //). Étapes en `- [ ]`.
> ⚠️ **Fichiers chauds** → relire l'ancre avant chaque edit ; committer **uniquement mes hunks** (index temporaire seedé sur HEAD + reverse-apply des hunks étrangers ; marqueurs : `merchant`/`Money`/`disponibilite`/`MERCHANT`/`Marchand`). Vérifier `git diff --cached`/`git show --stat` avant commit. Terminer les commits par `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
> **VF** : tout texte affiché en français ; RAW depuis le **Livre de Base FR** uniquement. ⚠️ Décision coin-label en attente (cf. « Note VF monnaie » en fin de plan) — ce plan suit la convention HUD existante **CO/SC/PA** par défaut.

**Goal:** Un marchand jouable : cliquer un PNJ marchand ouvre un panneau acheter/vendre, avec prix RAW (catalogue × qualité) et stock = Disponibilité RAW (Commune/Limitée/Rare/Exotique selon l'agglo). Paramétrable par archétype (`defs/`) + override par entité.

**Architecture:** Moteur pur (`money.ts`, `disponibilite.ts`) → données (`merchants/` 6ᵉ famille du registre `defs/`) → état (slice marchand dans le store) → UI (`MerchantPanel`). Réutilise `craftPriceFactor`, `itemFromTrapping`/`recomputeLoadout`, `findTrapping`/`trappings`, `makeRNG`.

**Tech Stack:** TypeScript pur, Zustand, Vitest, React/SVG.

**Spec:** `docs/superpowers/specs/2026-06-08-marchand-v1-design.md`. **RAW déjà extrait du LDB FR** (table dans Task 2 — `59 - Faire son marché.md` p.292 ; monnaie `57 - La monnaie.md` p.290), pas d'extraction à faire.

**Runners:** `npx vitest`/`tsc` via Bash natif (RTK). Sur échec JSON tronqué → `npx vitest run X --reporter=json --outputFile=vt.json` puis parser au node.

---

## Task 1 : `money.ts` — monnaie (pur, testé)

**Files:** Create `src/engine/money.ts` ; Create `src/engine/money.test.ts`. *(à moi, commit direct.)*

RAW (LDB 57 « La monnaie » p.290, l.14/27/31) : **1 CO = 20 pistoles d'argent = 240 sous de cuivre ; 1 pistole = 12 sous**. Le `Money` du store est `{gold,silver,brass}` (= couronne d'or / pistole d'argent / sou de cuivre) ; `money.ts` définit sa propre interface `Money` structurellement identique (pas d'import du store → moteur pur).

- [ ] **Step 1 : Tests qui échouent** — `src/engine/money.test.ts` :
```ts
import { describe, it, expect } from 'vitest';
import { toBrass, fromBrass, add, subtract, canAfford, formatMoney, priceToMoney, type Money } from './money';

describe('money — monnaie impériale (LDB 57 : 1 CO=20 SC=240 PA, 1 SC=12 PA)', () => {
  it('toBrass : convertit en sous de cuivre', () => {
    expect(toBrass({ gold: 1, silver: 0, brass: 0 })).toBe(240);
    expect(toBrass({ gold: 0, silver: 1, brass: 0 })).toBe(12);
    expect(toBrass({ gold: 2, silver: 3, brass: 5 })).toBe(480 + 36 + 5);
  });
  it('fromBrass : normalise (greedy CO puis SC puis PA)', () => {
    expect(fromBrass(521)).toEqual({ gold: 2, silver: 3, brass: 5 }); // 480+36+5
    expect(fromBrass(0)).toEqual({ gold: 0, silver: 0, brass: 0 });
  });
  it('add/subtract via le total en PA', () => {
    expect(add({ gold: 1, silver: 0, brass: 0 }, { gold: 0, silver: 0, brass: 13 })).toEqual({ gold: 1, silver: 1, brass: 1 });
    expect(subtract({ gold: 1, silver: 0, brass: 0 }, { gold: 0, silver: 5, brass: 0 })).toEqual({ gold: 0, silver: 15, brass: 0 });
    expect(subtract({ gold: 0, silver: 0, brass: 5 }, { gold: 0, silver: 1, brass: 0 })).toBeNull(); // insuffisant
  });
  it('canAfford', () => {
    expect(canAfford({ gold: 1, silver: 0, brass: 0 }, { gold: 0, silver: 19, brass: 11 })).toBe(true); // 240 ≥ 239
    expect(canAfford({ gold: 0, silver: 0, brass: 5 }, { gold: 0, silver: 1, brass: 0 })).toBe(false);
  });
  it('priceToMoney : data {gold,silver,bronze} → Money (bronze = brass = sou de cuivre)', () => {
    expect(priceToMoney({ gold: 2, silver: 0, bronze: 0 })).toEqual({ gold: 2, silver: 0, brass: 0 });
    expect(priceToMoney({ silver: 3 })).toEqual({ gold: 0, silver: 3, brass: 0 });
  });
  it('formatMoney : convention HUD existante (CO/SC/PA), omet les zéros', () => {
    expect(formatMoney({ gold: 2, silver: 3, brass: 0 })).toBe('2 CO 3 SC');
    expect(formatMoney({ gold: 0, silver: 0, brass: 0 })).toBe('0 PA');
  });
});
```

- [ ] **Step 2 : Lancer → échoue** : `npx vitest run src/engine/money.test.ts`.

- [ ] **Step 3 : Implémenter `src/engine/money.ts`** :
```ts
/**
 * Monnaie impériale (pur). RAW LDB 57 « La monnaie » p.290 : 1 couronne d'or (CO) = 20 pistoles
 * d'argent = 240 sous de cuivre ; 1 pistole = 12 sous. Le champ `brass` du store = le sou de cuivre
 * (= `price.bronze` des données). Interface structurellement identique au `Money` du store (pas
 * d'import → moteur pur). Convention d'affichage : CO/SC/PA (alignée sur le HUD existant). */
export interface Money { gold: number; silver: number; brass: number; }
export const PA_PER_SC = 12;
export const PA_PER_CO = 240; // 20 × 12

export function toBrass(m: Money): number {
  return m.gold * PA_PER_CO + m.silver * PA_PER_SC + m.brass;
}
export function fromBrass(pa: number): Money {
  let r = Math.max(0, Math.round(pa));
  const gold = Math.floor(r / PA_PER_CO); r -= gold * PA_PER_CO;
  const silver = Math.floor(r / PA_PER_SC); r -= silver * PA_PER_SC;
  return { gold, silver, brass: r };
}
export function add(a: Money, b: Money): Money { return fromBrass(toBrass(a) + toBrass(b)); }
/** a − b, ou null si insuffisant. */
export function subtract(a: Money, b: Money): Money | null {
  const d = toBrass(a) - toBrass(b);
  return d < 0 ? null : fromBrass(d);
}
export function canAfford(purse: Money, cost: Money): boolean { return toBrass(purse) >= toBrass(cost); }
export function priceToMoney(p: { gold?: number; silver?: number; bronze?: number }): Money {
  return { gold: p.gold ?? 0, silver: p.silver ?? 0, brass: p.bronze ?? 0 };
}
/** « 2 CO 3 SC », zéros omis (sauf 0 PA si tout est nul). Convention HUD existante (cf. Note VF monnaie). */
export function formatMoney(m: Money): string {
  const parts = [m.gold && `${m.gold} CO`, m.silver && `${m.silver} SC`, m.brass && `${m.brass} PA`].filter(Boolean);
  return parts.length ? parts.join(' ') : '0 PA';
}
```

- [ ] **Step 4 : Lancer → passent** ; `npm run typecheck`.
- [ ] **Step 5 : Commit** : `git commit -- src/engine/money.ts src/engine/money.test.ts -m "feat(marchand): money.ts -- monnaie imperiale (LDB 57, CO/SC/PA, conversions) (#2)"`

---

## Task 2 : `disponibilite.ts` — Disponibilité RAW (pur, seedé, testé)

**Files:** Create `src/engine/disponibilite.ts` ; Create `src/engine/disponibilite.test.ts`. *(à moi.)*

**Table RAW (LDB 59 « Faire son marché » p.292, l.26-35)** — déjà extraite, FR, **ne rien inventer** :
- Test de Disponibilité (réussite si d100 ≤ %) : **Commune** = toujours ; **Limitée** Village 30 / Ville 60 / Cité 90 ; **Rare** Village 15 / Ville 30 / Cité 45 ; **Exotique** = jamais (sauf curaté).
- Quantité de base si en stock : Village **1**, Ville **1d10**, Cité **illimité** (MJ → on modélise « illimité », paramétrable). Puis **×2 pour Commune, ÷2 pour Rare (arrondi sup.)** ; Limitée = base.

- [ ] **Step 1 : Tests qui échouent** — `src/engine/disponibilite.test.ts` :
```ts
import { describe, it, expect } from 'vitest';
import { makeRNG } from './dice';
import { rollAvailability, rollStock, DISPO_PCT, type CatalogItem } from './disponibilite';

describe('disponibilite — Disponibilité RAW (LDB 59 p.292)', () => {
  it('table RAW : Limitée 30/60/90, Rare 15/30/45', () => {
    expect(DISPO_PCT.Limitée).toEqual({ village: 30, ville: 60, cite: 90 });
    expect(DISPO_PCT.Rare).toEqual({ village: 15, ville: 30, cite: 45 });
  });
  it('Commune → toujours en stock (sans Test)', () => {
    const r = rollAvailability('Commune', 'village', makeRNG(1));
    expect(r.inStock).toBe(true);
    expect(r.test).toBeUndefined();
    expect(r.qty).toBe(2); // base 1 × 2 (Commune)
  });
  it('Exotique → jamais en stock', () => {
    expect(rollAvailability('Exotique', 'cite', makeRNG(1)).inStock).toBe(false);
  });
  it('Limitée à la Ville : un seed réussit, un autre échoue ; Test exposé', () => {
    // cherche deux seeds donnant des issues opposées (target 60 → roll ≤60 réussit)
    let win = -1, lose = -1;
    for (let s = 1; s < 50 && (win < 0 || lose < 0); s++) {
      const r = rollAvailability('Limitée', 'ville', makeRNG(s));
      if (r.inStock && win < 0) win = s;
      if (!r.inStock && lose < 0) lose = s;
    }
    expect(win).toBeGreaterThan(0);
    expect(lose).toBeGreaterThan(0);
    const r = rollAvailability('Limitée', 'ville', makeRNG(win));
    expect(r.test).toMatchObject({ target: 60 });
    expect(r.qty).toBeGreaterThanOrEqual(1); // Ville 1d10, Limitée = base
  });
  it('Rare en stock : quantité = ceil(base/2) (÷2 arrondi sup.)', () => {
    // au village, base=1 → Rare ceil(1/2)=1 quand en stock
    let s = 1; while (!rollAvailability('Rare', 'village', makeRNG(s)).inStock && s < 200) s++;
    expect(rollAvailability('Rare', 'village', makeRNG(s)).qty).toBe(1);
  });
  it('rollStock : déterministe (même seed → même stock), filtre Exotique, curaté forcé', () => {
    const cat: CatalogItem[] = [
      { label: 'Épée', availability: 'Commune' },
      { label: 'Arquebuse', availability: 'Rare' },
      { label: 'Clavecin', availability: 'Exotique' },
    ];
    const a = rollStock(cat, 'ville', makeRNG(7));
    const b = rollStock(cat, 'ville', makeRNG(7));
    expect(a).toEqual(b); // déterministe
    expect(a.find((l) => l.label === 'Épée')!.qty).toBeGreaterThan(0); // Commune toujours
    expect(a.find((l) => l.label === 'Clavecin')).toBeUndefined(); // Exotique exclu
    const withCurated = rollStock(cat, 'ville', makeRNG(7), ['Clavecin']);
    expect(withCurated.find((l) => l.label === 'Clavecin')!.qty).toBeGreaterThan(0); // curaté forcé
  });
});
```

- [ ] **Step 2 : Lancer → échoue.**

- [ ] **Step 3 : Implémenter `src/engine/disponibilite.ts`** :
```ts
/**
 * Disponibilité RAW (pur, seedé). Source : LDB 59 « Faire son marché » p.292 (l.26-35).
 * Ne rien inventer : la table % et les quantités viennent directement du Livre de Base FR.
 */
import { makeRNG, d100, d10, type RNG } from './dice';

export type Availability = 'Commune' | 'Limitée' | 'Rare' | 'Exotique';
export type Settlement = 'village' | 'ville' | 'cite';
export interface CatalogItem { label: string; availability: Availability | null; }
export interface StockLine { label: string; qty: number; test?: { roll: number; target: number } }

/** % de Disponibilité (réussite si d100 ≤ %). RAW LDB 59 p.292. */
export const DISPO_PCT: Record<'Limitée' | 'Rare', Record<Settlement, number>> = {
  'Limitée': { village: 30, ville: 60, cite: 90 },
  'Rare': { village: 15, ville: 30, cite: 45 },
};

/** Quantité de base par agglo si en stock (Village 1 / Ville 1d10 / Cité illimité). */
const CITE_UNLIMITED = 99;
function baseQty(settlement: Settlement, rng: RNG): number {
  if (settlement === 'village') return 1;
  if (settlement === 'ville') return d10(rng);
  return CITE_UNLIMITED; // Cité : « autant que le MJ » → illimité (paramétrable)
}
/** Modulation par classe : ×2 Commune, ÷2 Rare (arrondi sup.), Limitée = base. */
function classQty(av: Availability, base: number): number {
  if (av === 'Commune') return base * 2;
  if (av === 'Rare') return Math.ceil(base / 2);
  return base;
}

export function rollAvailability(av: Availability, settlement: Settlement, rng: RNG): { inStock: boolean; qty: number; test?: { roll: number; target: number } } {
  if (av === 'Commune') return { inStock: true, qty: classQty('Commune', baseQty(settlement, rng)) };
  if (av === 'Exotique') return { inStock: false, qty: 0 };
  const target = DISPO_PCT[av][settlement];
  const roll = d100(rng);
  if (roll > target) return { inStock: false, qty: 0, test: { roll, target } };
  return { inStock: true, qty: Math.max(1, classQty(av, baseQty(settlement, rng))), test: { roll, target } };
}

/** Instantané de stock : pour chaque article du catalogue, Test de Disponibilité (sauf Commune/curaté).
 *  Exotique exclu sauf curaté ; `availability` nulle/inconnue → exclu. Déterministe pour un seed donné. */
export function rollStock(catalog: CatalogItem[], settlement: Settlement, rng: RNG, curated: string[] = []): StockLine[] {
  const out: StockLine[] = [];
  for (const it of catalog) {
    const av = it.availability;
    if (curated.includes(it.label)) { out.push({ label: it.label, qty: Math.max(1, classQty(av ?? 'Commune', baseQty(settlement, rng))) }); continue; }
    if (av !== 'Commune' && av !== 'Limitée' && av !== 'Rare' && av !== 'Exotique') continue; // ND/null exclus
    const r = rollAvailability(av, settlement, rng);
    if (r.inStock) out.push({ label: it.label, qty: r.qty, test: r.test });
  }
  return out;
}
```
*(Note : `rollStock` consomme le `rng` séquentiellement → un seul `makeRNG(seed)` par instantané donne un résultat déterministe.)*

- [ ] **Step 4 : Lancer → passent** ; typecheck.
- [ ] **Step 5 : Commit** : `git commit -- src/engine/disponibilite.ts src/engine/disponibilite.test.ts -m "feat(marchand): disponibilite.ts -- Disponibilite RAW (LDB 59 p.292, seede) (#2)"`

---

## Task 3 : Archétype marchand — 6ᵉ famille du registre `defs/`

**Files:** Create `src/state/merchants/types.ts`, `src/state/merchants/defs/Armurier.ts`, `src/state/merchants/defs/Herboriste.ts`, `src/state/merchants/index.ts` ; Modify `scripts/gen-registry.mjs` (config) ; (généré) `src/state/merchants/_registry.generated.ts` ; Create `src/state/merchants/merchants.test.ts`.

Pattern (confirmé) : 1 fichier `defs/` = 1 entrée (`export const merchantArchetype: MerchantArchetypeDef = {...}`), index dérivé par `npm run gen` ; config dans `gen-registry.mjs::REGISTRIES`.

- [ ] **Step 1 : `types.ts`** :
```ts
import type { Settlement } from '../../engine/disponibilite';

export interface MerchantArchetypeDef {
  /** Clé unique (référencée par l'entité de scène). */
  name: string;
  /** Libellé FR (UI). */
  label: string;
  /** Familles vendues : filtre le catalogue par `type` et/ou `subType` de trapping. */
  category: { types?: string[]; subTypes?: string[] };
  /** Taille d'agglo par défaut (overridable par l'entité). */
  settlement: Settlement;
  /** Taux de rachat (défaut 0.10 — non RAW, LDB 59 « achat/vente optionnels »). */
  resaleRate: number;
  /** Articles garantis en stock (labels exacts), Disponibilité ignorée. */
  curated?: string[];
}
```

- [ ] **Step 2 : Deux entrées `defs/`** :

`src/state/merchants/defs/Armurier.ts` :
```ts
import type { MerchantArchetypeDef } from '../types';
export const merchantArchetype: MerchantArchetypeDef = {
  name: 'armurier', label: 'Armurier',
  category: { types: ['melee', 'ranged', 'armor', 'ammunition'] },
  settlement: 'ville', resaleRate: 0.10,
};
```
`src/state/merchants/defs/Herboriste.ts` :
```ts
import type { MerchantArchetypeDef } from '../types';
export const merchantArchetype: MerchantArchetypeDef = {
  name: 'herboriste', label: 'Herboriste',
  category: { subTypes: ['Herbes et potions', 'Drogues et poisons'] },
  settlement: 'village', resaleRate: 0.10,
};
```
*(Step ultérieur de recette : ajuster `subTypes` aux valeurs réelles de `trappings.json` — `node -e "console.log([...new Set(require('./src/data/trappings.json').map(t=>t.subType))])"`.)*

- [ ] **Step 3 : `index.ts`** (wrapper public dérivé) :
```ts
import { MERCHANT_ARCHETYPES } from './_registry.generated';
import type { MerchantArchetypeDef } from './types';
export type { MerchantArchetypeDef } from './types';
export { MERCHANT_ARCHETYPES };
/** Lookup par clé `name`. */
export const MERCHANTS: Record<string, MerchantArchetypeDef> = Object.fromEntries(
  MERCHANT_ARCHETYPES.map((m) => [m.name, m]),
);
```

- [ ] **Step 4 : Config `gen-registry.mjs`** — ajouter une entrée dans le tableau `REGISTRIES` (cf. `scripts/gen-registry.mjs:21-77`) :
```js
  {
    dir: 'src/state/merchants/defs',
    out: 'src/state/merchants/_registry.generated.ts',
    exportName: 'merchantArchetype',
    arrayName: 'MERCHANT_ARCHETYPES',
    type: 'MerchantArchetypeDef',
    typeFrom: './types',
  },
```

- [ ] **Step 5 : Générer** : `npm run gen` → crée `src/state/merchants/_registry.generated.ts`.

- [ ] **Step 6 : Test** — `src/state/merchants/merchants.test.ts` :
```ts
import { describe, it, expect } from 'vitest';
import { MERCHANTS } from './index';
describe('registre archétypes marchands (#2)', () => {
  it('charge les archétypes depuis defs/ (clé = name)', () => {
    expect(MERCHANTS.armurier).toMatchObject({ label: 'Armurier', settlement: 'ville', resaleRate: 0.10 });
    expect(MERCHANTS.herboriste.category.subTypes).toContain('Herbes et potions');
  });
});
```

- [ ] **Step 7 : Lancer → passe ; typecheck ; commit** (fichiers à moi + le généré) :
```bash
git commit -- src/state/merchants/ scripts/gen-registry.mjs -m "feat(marchand): archetype = 6e famille du registre defs/ (#2)"
```

---

## Task 4 : État — slice marchand (openMerchant / buyItem / sellItem)

**Files:** Modify `src/state/scene.ts` (**CHAUD** — `SceneEntity.merchant?`) ; Modify `src/state/store.ts` (**CHAUD** — état + actions + branche interactEntity) ; Modify `src/state/store.test.ts` (**CHAUD**).

- [ ] **Step 1 : Tests qui échouent** — `store.test.ts` (à la fin) :
```ts
import { priceToMoney, toBrass } from '../engine/money';

describe('Marchand — openMerchant / buyItem / sellItem (#2)', () => {
  beforeEach(() => reset());
  const hero = (): Combatant => ({ id: 'h', name: 'H', items: [], characteristics: {}, wounds: { current: 10, max: 10 }, conditions: [], weapons: [], armour: {} } as unknown as Combatant);

  it('openMerchant : crée un stock (Commune toujours présente) + état merchant', () => {
    const sc = emptyScene(4, 4); sc.id = 'm';
    sc.entities.push({ id: 'pnj', kind: 'personnage', pos: { x: 0, y: 0 }, merchant: { archetype: 'armurier' } });
    useGame.setState({ party: [hero()], scene: sc });
    useGame.getState().openMerchant('pnj');
    const m = useGame.getState().merchant!;
    expect(m.entityId).toBe('pnj');
    expect(m.stock.length).toBeGreaterThan(0); // au moins les Communes de la catégorie
    expect(m.resaleRate).toBe(0.10);
  });

  it('buyItem : débite la Bourse, donne l’objet à stats au héros, décrémente la qty', () => {
    const sc = emptyScene(4, 4); sc.id = 'm';
    sc.entities.push({ id: 'pnj', kind: 'personnage', pos: { x: 0, y: 0 }, merchant: { archetype: 'armurier' } });
    useGame.setState({ party: [hero()], scene: sc, money: { gold: 5, silver: 0, brass: 0 } });
    useGame.getState().openMerchant('pnj');
    const line = useGame.getState().merchant!.stock[0];
    const before = toBrass(useGame.getState().money);
    useGame.getState().buyItem(line.label, 'h');
    const st = useGame.getState();
    expect(st.party[0].items!.some((i) => i.name === line.label)).toBe(true); // objet sur le héros
    expect(toBrass(st.money)).toBeLessThan(before); // Bourse débitée
    expect(st.merchant!.stock.find((l) => l.label === line.label)?.qty ?? 0).toBe(line.qty - 1);
  });

  it('buyItem refuse si Bourse insuffisante (objet inchangé)', () => {
    const sc = emptyScene(4, 4); sc.id = 'm';
    sc.entities.push({ id: 'pnj', kind: 'personnage', pos: { x: 0, y: 0 }, merchant: { archetype: 'armurier' } });
    useGame.setState({ party: [hero()], scene: sc, money: { gold: 0, silver: 0, brass: 0 } });
    useGame.getState().openMerchant('pnj');
    const line = useGame.getState().merchant!.stock[0];
    useGame.getState().buyItem(line.label, 'h');
    expect(useGame.getState().party[0].items!.length).toBe(0); // rien acheté
  });

  it('sellItem : crédite resaleRate × prix et retire l’objet du héros', () => {
    const sc = emptyScene(4, 4); sc.id = 'm';
    sc.entities.push({ id: 'pnj', kind: 'personnage', pos: { x: 0, y: 0 }, merchant: { archetype: 'armurier' } });
    const h = hero(); h.items = [{ uid: 'x', name: 'Hallebarde', kind: 'melee', qualities: [], enc: 3, equipped: false }] as any;
    useGame.setState({ party: [h], scene: sc, money: { gold: 0, silver: 0, brass: 0 } });
    useGame.getState().openMerchant('pnj');
    useGame.getState().sellItem('x', 'h');
    const st = useGame.getState();
    expect(st.party[0].items!.find((i) => i.uid === 'x')).toBeUndefined(); // retiré
    expect(toBrass(st.money)).toBeGreaterThan(0); // crédité (Hallebarde 2 CO × 10 %)
  });
});
```

- [ ] **Step 2 : Lancer → échoue** (`merchant`/`openMerchant`/`buyItem`/`sellItem` absents).

- [ ] **Step 3 : `scene.ts`** — ajouter `merchant?` à `SceneEntity` (relire l'ancre ; après `weapon?`) :
```ts
  /** Marchand : ce PNJ ouvre un panneau d'achat/vente (référence un archétype de `state/merchants`). */
  merchant?: { archetype: string; settlement?: import('../engine/disponibilite').Settlement; resaleRate?: number };
```

- [ ] **Step 4 : `store.ts`** (relire chaque ancre — fichier chaud) :

4a. Imports (avec les autres imports moteur/état) :
```ts
import { makeRNG } from '../engine/dice';
import { rollStock, type Settlement, type CatalogItem } from '../engine/disponibilite';
import { priceToMoney, subtract as moneySub, add as moneyAdd, canAfford, fromBrass, toBrass } from '../engine/money';
import { craftPriceFactor } from '../engine/qualities/craftEconomy';
import { findTrapping, trappings } from '../data/index';
import { MERCHANTS } from './merchants/index';
```

4b. `GameState` : ajouter l'état + les actions (près de `dialogue`) :
```ts
  merchant: { entityId: string; archetype: string; settlement: Settlement; resaleRate: number; stock: { label: string; qty: number }[] } | null;
  openMerchant: (entityId: string) => void;
  closeMerchant: () => void;
  buyItem: (label: string, heroId: string) => void;
  sellItem: (uid: string, heroId: string) => void;
```

4c. Init du store : `merchant: null,` (ne PAS oublier : il est réinitialisé gratis par `startScene` via `getInitialState()`, cf. [[game-newgame-reset-pattern]]).

4d. Branche dans `interactEntity` (parallèle à `ent.dialogueId`, cf. store.ts ~885) :
```ts
    if (ent.merchant) { get().openMerchant(ent.id); return; }
```

4e. Actions (près de `closeDialogue`) :
```ts
  openMerchant: (entityId) => {
    const ent = get().scene?.entities.find((e) => e.id === entityId);
    if (!ent?.merchant) return;
    const arch = MERCHANTS[ent.merchant.archetype];
    if (!arch) { get().log(`Archétype marchand inconnu : « ${ent.merchant.archetype} ».`); return; }
    const settlement: Settlement = ent.merchant.settlement ?? arch.settlement;
    const resaleRate = ent.merchant.resaleRate ?? arch.resaleRate;
    // Catalogue filtré par catégorie (type/subType).
    const cat: CatalogItem[] = trappings
      .filter((t) => (!arch.category.types || arch.category.types.includes(t.type)) && (!arch.category.subTypes || (t.subType != null && arch.category.subTypes.includes(t.subType))))
      .map((t) => ({ label: t.label, availability: (t.availability as any) ?? null }));
    // Instantané déterministe (seed dérivé de l'entité — stable pour la visite ; #T3 re-seedera au temps écoulé).
    const seed = [...entityId].reduce((a, c) => (a * 31 + c.charCodeAt(0)) | 0, 7) >>> 0;
    const lines = rollStock(cat, settlement, makeRNG(seed), arch.curated);
    // Disponibilité montrée au joueur (journal) — VF, hors combat (pas de pendingReveals).
    const tested = lines.filter((l) => l.test);
    if (tested.length) get().log(`Marché (${settlement}) : ${tested.map((l) => `${l.label} ✔×${l.qty}`).join(', ')}.`);
    set({ merchant: { entityId, archetype: ent.merchant.archetype, settlement, resaleRate, stock: lines.map((l) => ({ label: l.label, qty: l.qty })) } });
  },
  closeMerchant: () => set({ merchant: null }),
  buyItem: (label, heroId) => {
    const m = get().merchant; if (!m) return;
    const line = m.stock.find((l) => l.label === label); if (!line || line.qty <= 0) return;
    const t = findTrapping(label); if (!t) return;
    const cost = fromBrass(Math.round(toBrass(priceToMoney(t.price)) * craftPriceFactor({ qualities: t.qualities })));
    if (!canAfford(get().money, cost)) { get().log(`Bourse insuffisante pour ${label}.`); return; }
    const it = itemFromTrapping(label); if (!it) return;
    set((s) => ({
      money: moneySub(s.money, cost)!,
      party: s.party.map((h) => {
        if (h.id !== heroId) return h;
        const clone: Combatant = JSON.parse(JSON.stringify(h));
        clone.items = [...(clone.items ?? []), it];
        recomputeLoadout(clone);
        return clone;
      }),
      merchant: { ...s.merchant!, stock: s.merchant!.stock.map((l) => (l.label === label ? { ...l, qty: l.qty - 1 } : l)) },
    }));
    get().log(`Achat : ${label}.`);
  },
  sellItem: (uid, heroId) => {
    const m = get().merchant; if (!m) return;
    const hero = get().party.find((h) => h.id === heroId);
    const item = hero?.items?.find((i) => i.uid === uid); if (!item) return;
    const t = findTrapping(item.name);
    const base = t ? toBrass(priceToMoney(t.price)) * craftPriceFactor(item) : 0;
    const gain = fromBrass(Math.round(base * m.resaleRate));
    set((s) => ({
      money: moneyAdd(s.money, gain),
      party: s.party.map((h) => {
        if (h.id !== heroId) return h;
        const clone: Combatant = JSON.parse(JSON.stringify(h));
        clone.items = (clone.items ?? []).filter((i: { uid: string }) => i.uid !== uid);
        recomputeLoadout(clone);
        return clone;
      }),
    }));
    get().log(`Vente : ${item.name}.`);
  },
```
*(Note : `itemFromTrapping`/`recomputeLoadout` sont déjà importés dans store.ts — sinon les ajouter depuis `../engine/items`.)*

- [ ] **Step 5 : Lancer → passent** ; `npm test` (golden-combat intact) ; typecheck.
- [ ] **Step 6 : Commit isolé** (scene.ts/store.ts/store.test.ts chauds ; marqueurs `merchant`/`openMerchant`/`buyItem`/`Disponibilité`) :
```bash
# commit isolé -m "feat(marchand): slice store openMerchant/buyItem/sellItem + entite PNJ merchant (#2)"
```

---

## Task 5 : UI — `MerchantPanel`

**Files:** Create `src/ui/MerchantPanel.tsx` ; Create `src/ui/MerchantPanel.test.tsx` ; Modify `src/ui/CampaignView.tsx` (**CHAUD** — montage).

- [ ] **Step 1 : Test qui échoue** — `MerchantPanel.test.tsx` (rendu statique, comme `EffectList.test.tsx`) :
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderToStaticMarkup } from 'react-dom/server';
import { useGame } from '../state/store';
import { MerchantPanel } from './MerchantPanel';

describe('MerchantPanel (#2)', () => {
  it('affiche le stock + la Bourse + boutons Acheter/Vendre', () => {
    useGame.setState({
      party: [{ id: 'h', name: 'H', items: [{ uid: 'x', name: 'Dague', kind: 'melee', qualities: [], enc: 0, equipped: false }] } as any],
      money: { gold: 1, silver: 0, brass: 0 },
      merchant: { entityId: 'p', archetype: 'armurier', settlement: 'ville', resaleRate: 0.1, stock: [{ label: 'Hallebarde', qty: 2 }] },
    });
    const html = renderToStaticMarkup(<MerchantPanel />);
    expect(html).toContain('Hallebarde');
    expect(html).toMatch(/Acheter/);
    expect(html).toMatch(/Vendre/);
    expect(html).toContain('Dague'); // vendable depuis les items du héros
  });
});
```

- [ ] **Step 2 : Lancer → échoue.**

- [ ] **Step 3 : Implémenter `src/ui/MerchantPanel.tsx`** :
```tsx
import { useState } from 'react';
import { useGame } from '../state/store';
import { findTrapping } from '../data/index';
import { priceToMoney, fromBrass, toBrass, formatMoney } from '../engine/money';
import { craftPriceFactor } from '../engine/qualities/craftEconomy';

/** Prix d'achat d'un article du catalogue (catalogue × facteur qualité d'artisanat). */
function buyPrice(label: string): string {
  const t = findTrapping(label);
  if (!t) return '—';
  return formatMoney(fromBrass(Math.round(toBrass(priceToMoney(t.price)) * craftPriceFactor({ qualities: t.qualities }))));
}

export function MerchantPanel() {
  const merchant = useGame((s) => s.merchant);
  const party = useGame((s) => s.party);
  const money = useGame((s) => s.money);
  const buyItem = useGame((s) => s.buyItem);
  const sellItem = useGame((s) => s.sellItem);
  const closeMerchant = useGame((s) => s.closeMerchant);
  const [heroId, setHeroId] = useState(party[0]?.id ?? '');
  if (!merchant) return null;

  return (
    <div className="merchant-panel modal-overlay">
      <div className="merchant-box">
        <div className="merchant-head">
          <strong>Marchand</strong>
          <span className="purse">Bourse : {formatMoney(money)}</span>
          <button className="btn small" onClick={closeMerchant}>Fermer</button>
        </div>
        <div className="merchant-cols">
          <div className="merchant-stock">
            <div className="mini-title">En vente</div>
            <label className="hero-sel">Donner à
              <select value={heroId} onChange={(e) => setHeroId(e.target.value)}>
                {party.map((h) => <option key={h.id} value={h.id}>{h.name}</option>)}
              </select>
            </label>
            {merchant.stock.filter((l) => l.qty > 0).map((l) => (
              <div className="merch-row" key={l.label}>
                <span>{l.label} ×{l.qty}</span>
                <span className="price">{buyPrice(l.label)}</span>
                <button className="btn small" onClick={() => buyItem(l.label, heroId)}>Acheter</button>
              </div>
            ))}
            {merchant.stock.every((l) => l.qty <= 0) && <p className="empty">— rien en stock —</p>}
          </div>
          <div className="merchant-sell">
            <div className="mini-title">Vendre (équipement du groupe)</div>
            {party.flatMap((h) => (h.items ?? []).map((it) => (
              <div className="merch-row" key={it.uid}>
                <span>{h.name} : {it.name}</span>
                <span className="price">{formatMoney(fromBrass(Math.round(toBrass(priceToMoney(findTrapping(it.name)?.price ?? {})) * craftPriceFactor(it) * merchant.resaleRate)))}</span>
                <button className="btn small" onClick={() => sellItem(it.uid, h.id)}>Vendre</button>
              </div>
            )))}
            {party.every((h) => !(h.items ?? []).length) && <p className="empty">— rien à vendre —</p>}
          </div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4 : Monter dans `CampaignView.tsx`** (relire — chaud) : sélecteur + rendu conditionnel près de `{dialogue && <DialogueBox />}` :
```tsx
  const merchant = useGame((s) => s.merchant);
  // …
        {dialogue && <DialogueBox />}
        {merchant && <MerchantPanel />}
```
+ import `import { MerchantPanel } from './MerchantPanel';`.

- [ ] **Step 5 : Lancer → passe ; typecheck ; commit** (MerchantPanel à moi ; CampaignView chaud → isolé) :
```bash
git commit -- src/ui/MerchantPanel.tsx src/ui/MerchantPanel.test.tsx   # à moi
# CampaignView.tsx : commit isolé -m "feat(marchand): MerchantPanel (achat/vente, Bourse FR) + montage (#2)"
```

---

## Task 6 : Éditeur — champ « Marchand » sur l'entité PNJ

**Files:** Modify `src/ui/editor/Editor.tsx` (**CHAUD**) — un champ pour l'entité sélectionnée (kind `personnage`) : sélecteur d'archétype (depuis `MERCHANTS`) + override agglo/resaleRate.

- [ ] **Step 1 : Implémenter** (relire l'ancre du panneau d'édition d'entité, là où `dialogueId` est édité) — ajouter, pour une entité `personnage`, un contrôle :
```tsx
{selected?.kind === 'personnage' && (
  <label className="ed-field">Marchand (archétype)
    <select value={selected.merchant?.archetype ?? ''} onChange={(e) => updateEntity(selected.id, { merchant: e.target.value ? { archetype: e.target.value } : undefined })}>
      <option value="">— aucun —</option>
      {Object.values(MERCHANTS).map((a) => <option key={a.name} value={a.name}>{a.label}</option>)}
    </select>
  </label>
)}
```
(adapter `updateEntity`/`selected` aux noms réels de l'éditeur ; import `import { MERCHANTS } from '../../state/merchants/index';`).

- [ ] **Step 2 : Typecheck ; commit isolé** (Editor.tsx chaud, marqueur `merchant`/`MERCHANTS`) :
```bash
# Editor.tsx : -m "feat(marchand): champ Marchand (archetype) sur l'entite PNJ dans l'editeur (#2)"
```

---

## Task 7 : Vérification

- [ ] `npm test` + `npm run typecheck` verts ; golden-combat intact.
- [ ] **Recette** (si browser dispo) : placer un PNJ + lui donner l'archétype « Armurier » dans l'éditeur → tester la scène → cliquer le PNJ → le panneau s'ouvre, montre un stock (Communes + Limitée/Rare selon Disponibilité), acheter un objet débite la Bourse et l'ajoute à la fiche du héros, vendre un objet le retire et crédite ~10 %.
- [ ] Ajuster les `subTypes` des archétypes aux valeurs réelles de `trappings.json` (recette).

## Note VF monnaie (DÉCISION en attente)
Le HUD existant affiche **CO / SC / PA** où **SC = pistole d'argent** et **PA = sou de cuivre (brass)**. Or, en canon FR (LDB 57), l'abréviation **« sc » = sou de cuivre** (le cuivre), pas l'argent. Ce plan **suit la convention HUD existante** (CO/SC/PA) pour rester cohérent. **À trancher avec l'utilisateur** : (a) garder CO/SC/PA partout (statu quo) ; (b) corriger globalement vers les noms canon (Couronne d'or / Pistole d'argent / Sou de cuivre) — fix transverse HUD + marchand. Non bloquant pour la v1.

## Hors périmètre (lots suivants)
Marchandage (#2c), Réparation d'armure (#2d), Évaluation (#2e), re-stock dans le temps (#T3, seam : re-seed `rollStock` sur `EVT.TIME_ADVANCED`), éditeur d'archétypes `defs/` complet (création d'archétypes via l'UI).

## Self-review
- **Couverture spec** : money (T1), disponibilite (T2), archétype defs/ (T3), store buy/sell + entité (T4), MerchantPanel (T5), éditeur entité (T6), vérif (T7). ✓
- **Pas de placeholder** : signatures et corps complets (money, disponibilite, archétype, openMerchant/buyItem/sellItem, MerchantPanel) ; la table Disponibilité et la monnaie sont **RAW FR cités** (LDB 59 p.292 / LDB 57 p.290), pas à extraire.
- **Cohérence types** : `Settlement`/`Availability`/`CatalogItem`/`StockLine` (disponibilite) réutilisés par l'archétype + le store ; `Money`/`priceToMoney`/`fromBrass`/`toBrass`/`craftPriceFactor` cohérents T1→T4→T5 ; `itemFromTrapping`/`recomputeLoadout`/`findTrapping`/`trappings` = signatures réelles vérifiées.
- **Discipline** : `money.ts`/`disponibilite.ts` purs (seedés) ; archétype = donnée (registre `defs/`) ; store en état ; UI en React. Réutilise `craftPriceFactor`, le pattern `giveTrapping`, le pattern dialogue→panel. `merchant` réinitialisé gratis par le reset zéro-maintenance ([[game-newgame-reset-pattern]]).
- **Pièges** : 4 fichiers chauds (commits isolés) ; Disponibilité montrée en **journal** (hors combat) plutôt que `pendingReveals` (machinerie combat) — refine la spec, intention « montrer les jets » préservée ; Cité = quantité « illimitée » modélisée (99, paramétrable) car le canon défère au MJ ; décision VF coin-label en attente.
