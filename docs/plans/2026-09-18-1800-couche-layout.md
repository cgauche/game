# #1800 — La couche LAYOUT : trois couches CSS comme une bibliothèque de style (design)

Ticket: #1811

> **Plan DATÉ du 2026-09-18** (politique `docs/`, `docs/architecture.md:9-13`) — à SUPPRIMER une fois
> exécuté ; git porte l'historique. Ce document ne contient AUCUN code livré : il mesure, tranche,
> nomme les primitives, les gardes et les lots. Jugé une première fois (PARTIEL, 9 griefs majeurs,
> 14 mineurs), corrigé en place ; les deux arbitrages utilisateur du §7 sont RENDUS (2026-09-18).
> **Provenance des mesures** : lecteur sur `ed9f7d7e2` (worktree `.wt-1797`, SUPPRIMÉ depuis) ;
> aucun `src/ui/styles/*.css` ne diffère entre `ed9f7d7e2` et la tête `7193fa723` de `.wt-1800`
> (re-sondé par le juge : 92 sites `style={{`, 521 `gap:`). Scripts et sorties dans le scratchpad
> `mesures-1800/` — leur constante `ROOT` pointe sur `.wt-1797` : **repointer `ROOT` sur `.wt-1800`
> avant tout rejeu**.

---

## 1. Cap, verbatims et constat mesuré

### 1.1 Verbatims (utilisateur)

Cap du 2026-09-18 (ticket #1800, trois messages) :

> « Ca serait vraiment cool que les écrans n'aient qu'a utiliser que les primitives et les classes, sans styles inline. C'est comme ca que fonctionnent les bibliotheque de style non ? »

> « Je te laisse me faire un plan de design sur Fable 5 sur le sujet. »

> « Tu es orchestrator, utilise un agent Fable 5 pour concevoir un plan qui sera jugé puis executé par un codeur »

Doctrine du 2026-07-12 (#373, fiche `.claude/memory/user-doctrine-ui-coherente-par-primitives-comme-les-donnees.md`, corollaire — SEUL verbatim de cette date porté par la fiche) :

> « J'y crois pas une seule seconde à des classes mono-écrans personnellement, c'est une excuse à la dérive »

La phrase « si on ne sait pas faire, on ajoute de nouveaux génériques — on en a déjà pas mal, j'en doute » n'est PAS dans la fiche mémoire : sa seule source est un RENDU, `docs/charte-ui.md:44-45`. Ce plan la cite comme telle (rendu de la charte, non vérifiable au ticket #373 hors ligne) et ne s'y adosse pas.

Doctrine du 2026-09-04 (même fiche) :

> « N'oublie pas les concepts lié a l'UI et les primitives. Le but est toujours d'avoir une interface cohérente dans toute l'application, comme on fait avec notre structure de donnée. »

> « si on doit modifier l'interface, on n'a juste qu'a toucher aux primitives plutot que partir a la chasse de tous les écrans de l'application »

**Poison relevé (à corriger au lot 1)** : `docs/charte-ui.md:44` porte une PARAPHRASE (« classe mono-écran = excuse à la dérive ») là où un verbatim existe — la charte reçoit le verbatim complet à sa place ; `src/ui/ui-ratchets.test.ts:250` TRONQUE le verbatim (« personnellement » absent) — corrigé au même commit.

**#1689** (« un concept d'écran = une primitive ») n'est cité ni dans `docs/` ni en mémoire (`git grep 1689 -- docs .claude` = 0) : c'est l'épic UI, pas une doctrine ; ce plan ne s'y adosse pas.

### 1.2 Diagnostic

Le cliquet (xii) (`src/ui/ui-ratchets.test.ts:1122-1129`) gèle PAR MODULE le NOMBRE de noms de classes définis (`classNamesDefined`, `:898-917`) sur 33 modules (`DOMAIN_CSS_MODULES`, `:264-298`), baseline `CLASS_SELECTOR_BASELINE` (`:299-828`). Il compte la mauvaise quantité : une classe de PLACEMENT (grille, rangée, gap) est une composition légitime PAR ÉCRAN, alors que la vraie dérive — une classe d'écran qui redéclare une IDENTITÉ (couleur, bordure, police, rayon, ombre) — et le `style={{…}}` inline lui sont invisibles.

### 1.3 Constat mesuré

| Mesure | Valeur | Commande / script |
|---|---|---|
| Commits touchant `ui-ratchets.test.ts` sur 60 j ; hausses / baisses de (xii) | 55 ; 24 / 17 | ticket #1800 (mesuré sur `main`, non refait) |
| Modules gelés par (xii) ; noms de classes gelés | 33 ; **1 324** | `sed -n '299,846p' src/ui/ui-ratchets.test.ts \| grep -o -E "^\s*'styles/[a-z-]+\.css': [0-9]+"` (33 entrées, somme awk) |
| Déclarations de PLACEMENT vs d'IDENTITÉ dans les 33 modules | **3 905** vs **2 677** | `mesures-1800/section1-classes.mjs` → `section1-out.json`, sommes des colonnes `declLayout`/`declIdentity` |
| Classes PURES placement / PURES identité / MIXTES | **424 / 288 / 596** (1 308 classées ; 16 sans décl. des deux familles) | idem, colonnes `pureLayout`/`pureIdentity`/`mixed` |
| Top identité (classe : décl.) | hud `.ptile` 30 · combat-console `.cc-arch` 25, `.cc-cell` 24, `.cc-end` 21, `.cc-socle` 20 · creator `.dicewell` 22, `.talent-option` 20, `.step-chip` 15 · frames `.fig-tile` 22, `.fig-zone-badge` 17 · plaque-row `.plaque-row` 18, `.plaque-name` 18 · combat-ui `.fx-chip` 17 · hud `.ptile-states` 17 · sheet `.rm-roll` 16 | `section1-summary.mjs` (TOP15) |
| Sites `style={{` dans `src/ui` (hors tests) | **92** sites / 37 fichiers : STATIQUE 77 · DYNAMIQUE 11 · VAR-CSS 2 · VIDE 2 | `git grep -n "style={{" -- src/ui` ; `section2-inline.mjs` → `inline-styles-raw.txt` |
| Sites `style={expr}` (sans double accolade, NON couverts par le lecteur) | **30** (dont `CreatorSummary.tsx` ×6, `EditorCanvas.tsx` ×9, `NotchGauge.tsx` ×2) | `git grep -n -E "style=\{[^{]" -- 'src/ui/*.tsx' ':!src/ui/*.test.tsx'` |
| Sites `style=` dans `src/gameIso/*.tsx` | 21 | `git grep -c "style={" -- 'src/gameIso/*.tsx'` (HORS périmètre, voir §9) |
| Tokens `:root` de `base.css` ; tokens d'ESPACEMENT / RAYON | 217 ; **0 / 0** | `grep -c -E '^\s*--[a-z0-9-]+\s*:' src/ui/styles/base.css` ; `grep -n -E '^\s*--(sp\|space\|gap\|pad\|radius\|r-)'` → seul `--font-display` (l.190) |
| Histogramme `gap` (valeur:occurrences) | 6px:117 · 8px:97 · 4px:58 · 10px:49 · 2px:40 · 12px:31 · 5px:21 · 14px:17 · 3px:14 · 16px:10 | `section3-histo.mjs` (36 `.css`) ; total `gap:` = 521 (`git grep -o -E "\bgap: [^;]+" -- 'src/ui/styles/*.css' \| wc -l`) |
| Histogramme `padding` | 0:32 · 8px:16 · `8px 10px`:15 · 10px:14 · 2px:13 · 12px:11 · 6px:10 · 16px:8 · `6px 10px`:8 · 18px:7 | idem |
| Histogramme `margin` | 0:78 · auto:33 · 6px:20 · 8px:16 · 12px:13 · 14px:13 · `6px 0`:13 · `0 0 6px`:12 · `8px 0`:12 · 4px:12 | idem |
| Histogramme `border-radius` / `font-size` | 8:50 · 6:33 · 10:31 · 0:17 · 50%:16 · 12:11 · 9:10 · 4:9 · 999px:7 / 13:74 · 11:72 · 12:70 · 12.5:25 · 10:24 · 14:21 · 15:20 | idem |
| `flex-direction: column` dans les modules | 211 | `git grep -c "flex-direction: column" -- 'src/ui/styles/*.css'` (somme) |
| Blocs `@media` ; blocs NE contenant QUE du placement | **75 ; 40** | `section6-media.mjs` |
| Breakpoints hors canon rencontrés | 561/701/901 (miroirs `min-width`), 1100 (`creator-presentation`), 1440 (`test-scenarios`) | idem ; (xviii) les tolère (`:1436-1438`) |
| Classes de placement PARTAGÉES et consommateurs `.tsx` | `.stack` 8 · `.row-flex` 26 (REDÉFINIE en domaine : `creator.css:273-278` gap 10px, `editor.css:699` `.row-flex .dr`) · `.layout-sidebar` 3 · `.panel-grid` 8 (+`.span-2` 2) · `.bar` 9 · `.screen` 5 · `.screen-body` posée par `ScreenShell` · `.master-detail` posée par `MasterDetail` (11 écrans) · `.tf-row` 10 mais définie en DOMAINE (`editor.css:692`) · `.cols`/`.split`/`.grid` 0 | `section4-usage.mjs` ; `git grep -n "span-2" -- 'src/ui/*.tsx'` ; `git grep -n "row-flex" -- 'src/ui/styles/*.css'` |
| Composant React de layout existant | **aucun** | `ctx_glob src/ui/**/{Stack,Row,Grid,Split,Layout,Flex,Cluster,Pile}*.tsx` → 0 |
| Variable CSS posée inline et consommée par une classe (patron existant) | `.swatch` (`components.css:275-287`, `--swatch-*`) · `PartyDock.tsx:51` `--pd-hp` → `hud.css:1332` · `PartyScreen.tsx:453` `--i` → `party.css:209` | `git grep -n -E "\-\-pd-hp\|\-\-i\b" -- 'src/ui/styles/*.css'` |
| `line-clamp` dans les modules | 7 (combat-console 4, hud 2, test-scenarios 1) | `git grep -c "line-clamp" -- 'src/ui/styles/*.css'` |

Le nombre gelé par (xii) (1 324 noms) ne mesure NI les 2 677 déclarations d'identité posées en module de domaine, NI les 122 sites inline. C'est CETTE dette qu'il faut nommer, stocker et faire décroître.

**Angle mort assumé** : la densité placement/identité de `base.css`/`components.css` eux-mêmes n'est pas mesurée — la couche partagée porte l'identité PAR CONSTRUCTION ; aucune garde de ce plan ne la juge.

---

## 2. Architecture cible en trois couches

Comme une bibliothèque de style : (1) **tokens** — échelles fermées ; (2) **primitives d'identité** — ce qui a une matière (couleur, bordure, police, rayon, ombre) ; (3) **layout** — ce qui PLACE (pile, rangée, grille, split), sans matière. Un écran COMPOSE (2) et (3), et n'écrit ni CSS d'identité ni style inline (arbitrages A1 et A2, §7).

### 2.1 Couche 1 — tokens (`src/ui/styles/base.css`, bloc `:root`)

Échelle d'ESPACEMENT nommée, DÉRIVÉE de l'histogramme `gap` (les six valeurs dominantes 6/8/4/2/12/16 px = 353 des 521 `gap`, 68 %) :

```css
--sp-2xs: 2px;  --sp-xs: 4px;  --sp-sm: 6px;  --sp-md: 8px;  --sp-lg: 12px;  --sp-xl: 16px;
```

Décision d'ingénierie (révisable, non soumise — credo : la cérémonie est réservée aux règles de jeu) : **10 px (49 sites) et 14 px (17 sites) n'entrent PAS dans l'échelle**. Règle de recalage, la même pour tous les lots : 10 → `lg` si le conteneur est un `.panel`, `md` sinon ; 14 → `lg`. `0` et `auto` restent des valeurs légales sans token.

**Rayons et tailles de police** : l'identité (rayon, police) vit dans les PRIMITIVES (couche 2), inchangées par ce ticket (brief : « primitives d'identité (inchangées) »). Aucun token `--r-*`/`--fs-*` ici : ce serait une refonte de `components.css`, hors de la couche layout. Ce n'est pas un report : la garde (xxi) n'en a pas besoin, elle interdit l'identité en module d'écran quelle que soit sa valeur.

### 2.2 Couche 2 — primitives d'identité (inchangées)

`components.css` + composants React (`ScreenShell`, `MasterDetail`, `Tabs`, `Band`, `PlaqueRow`, `Modal`, `OptionChooser`, `StateChips`… — catalogue `docs/charte-ui.md` § « Couche atomique — catalogue », `docs/primitives.md`). Deux ajouts MINIMAUX, chacun mesuré :

- `.clamp` (components.css) : `display:-webkit-box; -webkit-box-orient:vertical; -webkit-line-clamp: var(--clamp, 3); overflow:hidden` — 7 sites de `line-clamp` recopiés dans 3 modules = un concept de TEXTE partagé (troncature à N lignes). Cataloguée § « Titres / texte ».
- `MenuCard` prop `large?: boolean` → classe `.menu-card-large` (components.css). `.menu-card` est définie en `base.css:601-613` (plafond 560 px) ; la prop `wide` existe DÉJÀ sur `MenuSubScreen` (`MenuCard.tsx:69-77`, → `.game-menu-sub-wide`, `components.css:958-963`, 720 px) — d'où le nom distinct `large`. `.menu-card-large` pose les DEUX plafonds repris de `test-scenarios.css:5-8` : `max-width: min(760px, 96vw)`, puis `@media (min-width: 1440px) { max-width: min(1600px, 96vw) }` (politique grand écran `charte-ui.md:324-343`). Cataloguée à la charte (sinon `check-doc-refs.mjs:178-195` 4b — classe canonique de `components.css` absente du catalogue — et (xiii) `'styles/components.css': 10` rougissent).

**Module de PRIMITIVE vs module d'ÉCRAN** (question posée par le lecteur : `band`, `plaque-row`, `frames`, `gauges`… portent une identité LÉGITIME). Tranché : la vérité dérive du CATALOGUE, jamais d'une liste (fiche `game-garde-exemption-au-site-jamais-au-fichier`). Le manifeste `src/data/primitives.manifest.json` reçoit un champ OPTIONNEL `css` (chemin du module que la primitive POSSÈDE ; `build-primitives.mjs` n'a pas de schéma fermé, `:22` n'exige que six champs). Un module est « de primitive » ssi ≥ 1 entrée du manifeste le nomme ; tout autre `src/ui/styles/*.css` hors `SHARED_CSS_FILES` est un module d'ÉCRAN, soumis à (xxi). Classification au lot 1 (à VÉRIFIER par le codeur contre l'`@import` commenté de `src/ui/styles.css:5-40` et les ids du manifeste) :

| Module | Entrée manifeste qui le possède (`css`) | Identité mesurée |
|---|---|---|
| `rose.css` | `roseAxes` | 2 |
| `hero-sheet.css` | `heroSheet` | 16 |
| `creator-step.css` | `creatorStepFrame` | 17 |
| `frames.css` | `figTile` | 55 |
| `plaque-row.css` | `plaqueRow` | 55 |
| `band.css` | `band` | 40 |
| `panneau-parametre.css` | `panneauParametre` | 11 |
| `gauges.css` | `lifeBar` + entrées `notchGauge`/`windRose` à DÉCLARER (`NotchGauge.tsx`, `WindRose.tsx` existent, le manifeste ne les nomme pas) | 58 |
| `ornaments.css` | entrée `ornaments` à DÉCLARER (`src/ui/Ornaments.tsx`, textures `.tx-*` composées par `ParchmentCard` et d'autres) | 19 |
| `layout.css` (neuf) | `layout` (§2.3) — et inscrit à `SHARED_CSS_FILES` | 0 par construction |

Quatre entrées de manifeste s'ajoutent (`layout`, `ornaments`, `notchGauge`, `windRose`) : le cardinal `entrees: 62` de `scripts/guards/lib/structuresStock.mjs:898` (`primitives.manifest.json`) est RECALÉ à 66 avec son commentaire daté (un relèvement de compte est net 0 pour la porte de plage — `stocksNominatifs.mjs` l'écrit — donc pas de ligne `CLIQUET:` pour ce fichier).

Tous les autres modules (creator, combat-ui, combat-modals, sheet, merchant, hud, world-meta, editor, compendium, codex-edit, house-rules, mass-battle, tavern, party, city-hub, voyage, gallery, creator-presentation, creator-shell, test-scenarios, combat-console, exploration-dock, postes-roster, celestial-wheel) sont des modules d'ÉCRAN : leur identité entre au stock (xxi). Stock initial attendu ≈ **2 400** sites (2 677 − 273 des neuf modules de primitive − 9 de l'écran de référence, à recalculer par le régénérateur). Une classe de primitive planquée en module d'écran (`.ptile*` dans `hud.css`, `.gpg-*` dans `creator.css:792-808`, `.rm-*` dans `combat-modals.css`) est exactement ce que le stock rend visible : elle se solde en la DÉPLAÇANT vers le module de sa primitive, jamais en effaçant son aspect.

### 2.3 Couche 3 — LAYOUT (couche absente, créée)

**Composants, pas utilitaires** (tranché §8) : un concept d'écran = une primitive. Quatre concepts, liste FERMÉE : **Stack** (pile), **Row** (rangée), **Grid** (grille), **Split** (deux colonnes dont une bornée). Fichier React unique `src/ui/Layout.tsx` (patron `MenuCard.tsx` : plusieurs exports d'une famille), feuille `src/ui/styles/layout.css`.

**Ordre de cascade (tranché)** : `layout.css` s'importe dans `src/ui/styles.css` APRÈS `components.css` (et `tabs.css`), AVANT les modules. Raison mesurée : `.panel { padding:16px }` (`components.css:26-32`) et `.panel.flush { padding:0 }` (`:40-42`) écraseraient tout `padding` d'une primitive de layout importée avant. Corollaire : **aucune primitive de layout ne déclare `padding` sans attribut** — la règle est `.stack[data-pad], .row[data-pad], .grid[data-pad], .split[data-pad] { padding: var(--pad) }` ; sans `pad`, un `<Stack className="panel">` garde le padding du panel ; avec `pad`, `[data-pad]` (0,2,0) bat `.panel` (0,1,0) et, à spécificité égale avec `.panel.flush` (0,2,0), gagne par l'ordre. Prouvé par un `it` du §4.3.

Les valeurs se posent en ATTRIBUTS `data-*`, pas en classes modificatrices : (xiii) compte des NOMS de classe (`classNamesDefined` ignore `[data-*]`), et une règle par pas d'échelle sert les quatre primitives.

```ts
// src/ui/Layout.tsx — signatures
export type Espace = '2xs' | 'xs' | 'sm' | 'md' | 'lg' | 'xl';          // = --sp-*
export type Cassure = 900 | 700 | 560;                                     // breakpoints canon (règle 4)
type Balise = 'div' | 'section' | 'article' | 'nav' | 'header' | 'footer' | 'form' | 'ul' | 'ol' | 'li' | 'fieldset';
// `style` INTERDIT à la compilation sur les quatre primitives (A2) ; le reste (id, role, aria-*, data-testid, onClick…) passe.
type Rest = Omit<HTMLAttributes<HTMLElement>, 'style' | 'className'> & { className?: string };

export function Stack(p: { gap?: Espace /* md */; pad?: Espace; align?: 'stretch' | 'start' | 'center' | 'end';
  rowBelow?: Cassure /* sous la cassure : devient une rangée qui s'enroule */; as?: Balise; children: ReactNode } & Rest): JSX.Element;
export function Row(p: { gap?: Espace /* md */; pad?: Espace; align?: 'center' | 'start' | 'end' | 'stretch' | 'baseline';
  justify?: 'start' | 'between' | 'end' | 'center'; wrap?: boolean /* true */;
  stackBelow?: Cassure /* sous la cassure : devient une pile */; as?: Balise; children: ReactNode } & Rest): JSX.Element;
export function Grid(p: { gap?: Espace /* xl */; pad?: Espace; min?: 'sm' | 'md' | 'lg' /* md — auto-fill minmax(min(240|340|400px,100%),1fr) */;
  cols?: 2 | 3 | 4 /* colonnes FIXES ; exclusif de min */; stackBelow?: Cassure /* 700 si cols */; as?: Balise; children: ReactNode } & Rest): JSX.Element;
export function Split(p: { gap?: Espace /* lg */; pad?: Espace; aside?: 'sm' | 'md' | 'lg' /* sm */; side?: 'start' | 'end' /* start */;
  sticky?: boolean /* la colonne bornée reste visible au défilement */; align?: 'start' | 'stretch' /* start */;
  stackBelow?: Cassure /* 700 */; as?: Balise; children: ReactNode } & Rest): JSX.Element;
// Modificateurs d'ENFANT (attributs typés, à étaler : <h2 {...grow}>) :
export const grow = { 'data-grow': '' } as const;        // prend la place (flex:1 1 auto ; min-width:0)
export const pushEnd = { 'data-push': 'end' } as const;  // se cale au bout de la rangée (margin-inline-start:auto)
export const spanFull = { 'data-span': 'full' } as const; // pleine largeur d'une Grid (grid-column:1/-1)
```

Les deux cassures portent des noms qui disent leur SENS : `stackBelow` (Row/Grid/Split deviennent une pile) et `rowBelow` (Stack devient une rangée) — attributs rendus `data-stack-below` / `data-row-below`.

Rendu et classes (`layout.css`) — tout ce qui suit est la LISTE FERMÉE des sélecteurs de la feuille :

| Primitive | Rendu | Règles `layout.css` |
|---|---|---|
| échelle | `data-gap` / `data-pad` sur les quatre | `[data-gap='2xs'] { --gap: var(--sp-2xs) }` … `xl` ; `[data-pad='…'] { --pad: var(--sp-…) }` (12 règles) ; `.stack[data-pad], .row[data-pad], .grid[data-pad], .split[data-pad] { padding: var(--pad) }` |
| `Stack` | `class="stack" data-gap data-pad data-align data-row-below` | `.stack { display:flex; flex-direction:column; gap:var(--gap, var(--sp-md)); min-width:0 }` · `.stack[data-align='center'\|'start'\|'end']` · `ul.stack, ol.stack, ul.row, ol.row, ul.grid, ol.grid { list-style:none; margin:0; padding:0 }` (le `padding:0` de la liste nue est battu par `[data-pad]`, plus spécifique) |
| `Row` | `class="row" data-gap data-pad data-align data-justify data-wrap data-stack-below` | `.row { display:flex; align-items:center; flex-wrap:wrap; gap:var(--gap, var(--sp-md)); min-width:0 }` · `.row[data-wrap='no'] { flex-wrap:nowrap }` · `.row[data-justify='between'\|'end'\|'center']` · `.row[data-align='start'\|'end'\|'stretch'\|'baseline']` |
| `Grid` | `class="grid" data-gap data-pad data-min \| data-cols data-stack-below` | `.grid { display:grid; gap:var(--gap, var(--sp-xl)); align-content:start; align-items:start; grid-template-columns:repeat(auto-fill, minmax(min(var(--grid-min, 340px), 100%), 1fr)) }` · `.grid[data-min='sm'] { --grid-min:240px }` `md` 340 (ex-`.panel-grid`, `base.css:466`) `lg` 400 (ex-`.ts-grid`, charte l.332-335) · `.grid[data-cols='2'\|'3'\|'4'] { grid-template-columns:repeat(N, minmax(0,1fr)) }`. `align-items:start` REMPLACE `.panel-grid > .panel { align-self:start }` (`base.css:470-472`) pour tout enfant. `auto-fill` remplace l'`auto-fit` de `.panel-grid` : à peu d'items les colonnes vides gardent leur largeur au lieu de s'étirer — conforme charte `:332-335`, à voir en recette |
| `Split` | `class="split" data-gap data-pad data-aside data-side data-sticky data-align data-stack-below` | `.split { display:grid; grid-template-columns:var(--aside) minmax(0,1fr); gap:var(--gap, var(--sp-lg)); align-items:start; min-width:0 }` · `.split[data-aside='sm'] { --aside:minmax(160px,240px) }` (ex-`.master-detail`, `components.css:590`) `md` `270px` (ex-`.layout-sidebar`, `base.css:450`) `lg` `minmax(240px,1.3fr)` (ex-`city-hub.css:37`) · `.split[data-side='end'] { grid-template-columns:minmax(0,1fr) var(--aside) }` · `.split[data-align='stretch'] { align-items:stretch }` · la colonne BORNÉE est désignée par sa position, que `side` décide : `.split:not([data-side='end'])[data-sticky] > :first-child, .split[data-side='end'][data-sticky] > :last-child { position:sticky; top:0; align-self:start }` |
| enfants | `data-grow`, `data-push`, `data-span` | `.stack > [data-grow], .row > [data-grow] { flex:1 1 auto; min-width:0 }` · `.row > [data-push='end'] { margin-inline-start:auto }` · `.grid > [data-span='full'] { grid-column:1 / -1 }` |
| cassures | `data-stack-below='900'\|'700'\|'560'`, `data-row-below='…'` | `@media (max-width: 900px) { .split[data-stack-below='900'], .grid[data-stack-below='900'] { grid-template-columns:1fr } .split[data-stack-below='900']:not([data-side='end'])[data-sticky] > :first-child, .split[data-stack-below='900'][data-side='end'][data-sticky] > :last-child { position:static } .row[data-stack-below='900'] { flex-direction:column; align-items:stretch } .stack[data-row-below='900'] { flex-direction:row; flex-wrap:wrap } }` — même bloc pour 700 et 560 (3 blocs, breakpoints canon, (xviii) vert) |
| coquilles historiques | `.screen` (5 consommateurs), `.screen-body`/`.screen-body-wide` (`ScreenShell`), `.screen-scroll` (neuf) | `.screen` DÉPLACÉ tel quel de `base.css:667-671` ; `.screen-body*` déplacé de `components.css:758-769` avec sa marge recalée sur l'échelle : `margin: var(--sp-sm) auto 0` (le `6px` littéral tomberait sous l'obligation de token de `layout.css`, §4.2) ; `.screen-scroll { flex:1; overflow-y:auto; width:100%; max-width:1480px; margin:0 auto }` = le corps défilant borné de `.layout-sidebar` (`base.css:453-457`), séparé de sa grille |
| `MasterDetail` | `.master-detail-list { max-height:min(60vh,520px); overflow-y:auto }` + `@media 700 { max-height:none; overflow:visible }` | seule règle propre à la primitive qui reste : sa géométrie de défilement (`components.css:600-601, 613-614`) ; le reste devient `<Split aside stackBelow={700}>` + `<Stack rowBelow={700}>`. Les classes `master-detail`/`master-detail-detail` DISPARAISSENT du DOM — leurs consommateurs (tests, stocks, CSS scopé) sont au périmètre §6.1 |

**Classes qui MEURENT au lot 1** (leurs consommateurs migrent dans le même commit, aucune rétro-compat) : `.stack` conserve son nom mais sa définition passe à `layout.css` ; `.row-flex` (26 consommateurs ; définitions `components.css:58-63`, `creator.css:273-278` — le créateur passe de `gap: 10px` à `md`, règle §2.1 —, `editor.css:699` `.row-flex .dr` → `.row .dr`, règle d'identité qui entre au stock (xxi) jusqu'au lot 4) → `<Row>` ; `.panel-grid` + `.span-2` (8 + 2) → `<Grid min="md" stackBelow={700}>` + `spanFull` ; `.layout-sidebar` (3) → `<Split aside="md" stackBelow={900} gap="xl" pad="xl" className="screen-scroll">` (aucun de ces écrans n'avait de colonne collante : `.layout-sidebar` ne posait pas de `position`) (sous 900, `base.css:476-485` passait `padding`/`gap` à 10 px : la cassure les recale à `lg`/`md` via `@media 900 { .split[data-stack-below='900'][data-pad='xl'] { --pad: var(--sp-lg) } … }` — deux règles de plus dans le bloc 900, listées ici) ; `.master-detail` (grille) → rendu de `MasterDetail` sur `Split`.

### 2.4 Comment un écran existant se réécrit — `TestScenariosScreen` (écran de référence du lot 1)

Choisi par la mesure : `test-scenarios.css` = 9 classes, 17 décl. de placement / 9 d'identité, 1 `@media` (1440), **0 inline** dans `TestScenariosScreen.tsx` ; test unitaire `TestScenariosScreen.test.tsx` (32 l., ne cible aucune classe) ; c'est la PORTE de toute recette navigateur (menu « Tests — scénarios »), donc couvert par construction. `city-hub` (18 classes, 44/23, 1 inline) est plus représentatif de `ScreenShell`+`MasterDetail` mais exige d'extraire un concept absent (« option de liste sélectionnable », §6 lot 3) : ce n'est pas un socle — le lot 1 le touche seulement pour `MasterDetail aside` (ci-dessous).

AVANT (`src/ui/TestScenariosScreen.tsx:46-80` + `src/ui/styles/test-scenarios.css:1-16`) :

```tsx
<div className="menu">
  <MenuCard className="test-scenarios" header={…}>
    {groupBySection(testScenarios).map((sec) => (
      <section className="ts-section" key={sec.section.key}>
        <h2 className="mini-title"><Icon id={sec.section.icon} size="sm" /> {sec.section.label}</h2>
        <div className="ts-grid">
          {sec.items.map((sc) => (
            <div className="ts-card" key={sc.id}>
              <div className="ts-head"><span className="ts-ico"><Icon id={sc.icon} size={20} /></span><strong>{sc.title}</strong></div>
              <p className="ts-tests" title={sc.tests}>{sc.tests}</p>
              <p className="ts-party">{sc.partyNote}</p>
              <button className="btn btn-primary" data-testid={`scenario-launch-${sc.id}`} onClick={() => launch(sc)}>Lancer</button>
            </div>))}
        </div>
      </section>))}
  </MenuCard>
</div>
```

APRÈS (zéro CSS écrit, zéro inline, `test-scenarios.css` SUPPRIMÉ) :

```tsx
<div className="menu">
  <MenuCard large header={…}>
    <Stack gap="xl">
      {groupBySection(testScenarios).map((sec) => (
        <Stack as="section" gap="md" key={sec.section.key}>
          <h2 className="mini-title"><Icon id={sec.section.icon} size="sm" /> {sec.section.label}</h2>
          <Grid min="lg" gap="lg">
            {sec.items.map((sc) => (
              <Stack className="panel sunken" gap="sm" key={sc.id}>
                <Row gap="md"><Icon id={sc.icon} size={20} /><strong>{sc.title}</strong></Row>
                <p className="hint clamp" title={sc.tests} {...grow}>{sc.tests}</p>
                <p className="hint">{sc.partyNote}</p>
                <button className="btn btn-primary" data-testid={`scenario-launch-${sc.id}`} onClick={() => launch(sc)}>Lancer</button>
              </Stack>))}
          </Grid>
        </Stack>))}
    </Stack>
  </MenuCard>
</div>
```

Correspondance, classe par classe (le juge vérifie qu'aucune n'est perdue en silence) : `.test-scenarios` max-width 760/1600 → `MenuCard large` (mêmes deux plafonds) · `.ts-section` margin-top 18 → `Stack gap="xl"` (16) · `.ts-section > .mini-title` margin-bottom 8 → `gap="md"` · `.ts-grid` → `Grid min="lg" gap="lg"` · `.ts-card` (bordure, rayon 8, padding 12, fond `--bg`, pile gap 6) → `.panel.sunken` + `Stack gap="sm"` (le panel impose SA matière : rayon 12, padding 16, fond `--bg2` — c'est le but ; sans `pad`, le padding du panel reste, §2.3) · `.ts-head` (rangée gap 8, 15 px) → `Row gap="md"` (la taille locale meurt : `strong` hérite) · `.ts-ico` font-size 20 → rien (`Icon size={20}` dimensionne déjà) · `.ts-tests` (12 px, `--muted`, clamp 3, `flex:1`) → `.hint.clamp` + `grow` — `.hint` est à 13 px (`base.css:655-659`), la carte gagne 1 px de corps · `.ts-party` (12 px, `--ok`) → `.hint` (13 px, `--muted`) — le vert n'a pas de sens sémantique ici (ce n'est pas un succès ; les tons sont réservés aux chips qui SIGNALENT, charte l.93), jugement de design à confirmer par le juge à l'écran.

Second cas (`city-hub.css`, touché au lot 1 pour `MasterDetail aside`, migré entièrement au lot 3) : `:36-38` (`@media (min-width:701px) { .city-hub-plan.master-detail { grid-template-columns:minmax(240px,1.3fr) 1fr } }`) scope sur une classe qui meurt au lot 1 → **le lot 1 migre `CityHubScreen.tsx:338` vers `<MasterDetail aside="lg">`** et supprime `city-hub.css:33-38` (une classe morte ne survit pas un lot). Au lot 3 : `:29-32` (`@media 700 { .city-hub-services { flex-direction:row; flex-wrap:wrap } }`) → `<Stack role="listbox" rowBelow={700} pad="md" gap="sm" className="panel flush">` (avec `pad`, `[data-pad]` bat `.panel.flush`, §2.3). Le troisième bloc (`:40-42`, `font-size` sous 560) est de l'identité : il ira au stock puis mourra avec `.city-hub-weather`.

---

## 3. Géométrie dynamique — règle unique (arbitrage A2 rendu)

**Une seule forme légale de `style=` dans `src/ui` : un littéral d'objet dont TOUTES les clés sont des variables CSS (`'--…'`), consommées par une classe.** Patron déjà canonique : `.swatch` (`components.css:279-287`), `--pd-hp` (`PartyDock.tsx:51` → `hud.css:1332`), `--i` (`PartyScreen.tsx:453` → `party.css:209`). Typage : `style={{ '--x': v } as CSSProperties}` (la forme des 2 sites existants ; la garde tolère le `as CSSProperties` final).

Le lot 1 migre les FICHIERS ENTIERS des cinq primitives ci-dessous (un fichier touché s'audite en entier — `NotchGauge.tsx` porte 3 sites `:82,:89,:98`, `CodexRef.tsx` en porte 2 `:354,:384`) :

| Site (avant) | Après (TSX) | Classe consommatrice (module de la primitive) |
|---|---|---|
| `src/ui/DrBar.tsx:13` `style={{ width: \`${pct}%\` }}` et `:16` `style={{ left: … }}` | `style={{ '--dr-pct': \`${pct}%\` } as CSSProperties}` / `{{ '--dr-at': … }}` | `.dr-bar-fill { width: var(--dr-pct) }`, `.dr-bar-notch { left: var(--dr-at) }` (`hud.css:1111-1112`, à déplacer avec la primitive DrBar au lot combat) |
| `src/ui/LifeBar.tsx:67` `style={{ width: …%, ...(color ? { background: color } : {}) }}` | `style={{ '--life-pct': …, '--life-color': color ?? 'inherit' } as CSSProperties}` | `.life-bar__fill { width: var(--life-pct); background: var(--life-color, …) }` — les tons `data-tone` (`gauges.css:44-46`) restent |
| `src/ui/PortraitTile.tsx:77,80` `style={{ width: s }}` / `{{ width: s, height: s }}` | `style={{ '--ptile-px': \`${s}px\` } as CSSProperties}` sur `.ptile` | `.ptile { width: var(--ptile-px) }`, `.ptile-face { width: var(--ptile-px); height: var(--ptile-px) }` |
| `src/ui/compendium/CodexRef.tsx:354` popover `top/bottom/left/maxWidth/maxHeight` + `pointerEvents` conditionnel ; `:384` `style={{ pointerEvents: 'auto' }}` | `style={{ '--pop-top': …, '--pop-bottom': …, '--pop-left': …, '--pop-w': …, '--pop-h': … }}` + `data-atteignable={boiteAtteignable \|\| undefined}` ; `:384` → attribut `data-atteignable` sur l'élément | `.codex-pop { top: var(--pop-top); … }`, `.codex-pop[data-atteignable], .codex-pop [data-atteignable] { pointer-events: auto }` — un BOOLÉEN est un attribut, jamais une variable |
| `src/ui/NotchGauge.tsx:82` `style={cellSize != null ? { width: … } : undefined}`, `:89` `{ flex: \`0 0 ${cellSize}px\` }`, `:98` `style={{ left: …% }}` | `style={{ '--notch-w': \`${…}px\` }}` (absent quand `cellSize` est nul : l'attribut n'est pas rendu), `'--cell-w'`, `'--mark-at'` | `.notch-gauge__track { width: var(--notch-w, auto) }`, `.notch-gauge__cell { flex: 0 0 var(--cell-w, auto) }`, `.notch-gauge__mark { left: var(--mark-at) }` (`gauges.css`, module de primitive) |

Règle de partage : une valeur CALCULÉE (pourcentage, pixel mesuré, coordonnée) → variable CSS ; un ÉTAT (actif, atteignable, ouvert) → attribut `data-*`/`aria-*` ; une COULEUR DE DONNÉE (teinte d'un jeton, `Palette.tsx:305`, `CodexEntry.tsx:54`) → variable CSS consommée par `.swatch`, la primitive existante. Aucune propriété CSS directe inline, jamais.

---

## 4. Gardes

Un audit se clôt par des GARDES. Toutes vivent dans `src/ui/ui-ratchets.test.ts` (aucune garde CSS n'existe dans `scripts/guards/lib/` — vérifié : `grep -l -i css scripts/guards/lib/*` ne rend que des libs génériques), MAIS la MESURE et le STOCK vivent dans `scripts/guards/lib/` parce que le stock nominatif y est attendu par la porte de plage (`stocksNominatifs.mjs`, périmètre `scripts/guards/lib/**`) et que le régénérateur (`regenStock.mts`) la partage avec la garde (patron `paletteLiteralAudit.ts` / `paletteLiteralStock.mjs` / `scripts/rig/regen-palette-literal-stock.mts`).

### 4.1 Fichiers

| Fichier | Rôle |
|---|---|
| `scripts/guards/lib/cssCouches.mjs` (+ `cssCouches.d.mts`, `cssCouches.test.mjs`) | PARSEUR pur, sans disque. `reglesCss(text)` REMPLACE la regex plate de `ui-ratchets.test.ts:1691-1700` (incapable de rendre le contexte `@media`) : c'est une RÉÉCRITURE — un lexer de blocs à profondeur (`{`/`}` comptés, commentaires et chaînes neutralisés, `@media`/`@supports`/`@container` empilés, `@keyframes`/`@font-face` sautés) — sous le CONTRAT : même sortie `{ selecteurs: string[]; corps: string }[]` pour (xx), enrichie de `media: string \| null` (le contexte n'est pas un abri). Puis `declarations(corps)` → `{ prop, valeur }[]` ; `PROPRIETES_DE_PLACEMENT` (Set fermé, §4.2) ; `estPlacement(prop)` ; `PROPRIETES_A_ECHELLE` = `gap`, `row-gap`, `column-gap`, `padding*`, `margin*` ; `valeurHorsEchelle(valeur)` = vrai ssi la valeur contient un littéral `<n>(px\|rem\|em)` avec n ≠ 0 |
| `scripts/guards/lib/cssCouchesAudit.ts` | MESURE (lit le disque via `readCorpus`) : `modulesDePrimitive()` (champ `css` du manifeste), `modulesDEcran()` (= `src/ui/styles/*.css` − `SHARED_CSS_FILES` − primitives), `sitesIdentiteEcran(fichiers)`, `sitesEspacementHorsEchelle(fichiers)`, `sitesStyleInline(fichiers)` — chacune PURE sur les fichiers reçus (les preuves par mutation leur passent des fixtures en mémoire), et `mesureCssCouches()` qui rend `{ identite, espacement, inline }`, trois `EntreeNominative[]` via `sitesEnEntrees(sites)` SANS `famille` : `regenStock.mts:35-36` n'écrit que `{ fichier, ref, occurrence }`, une `famille` mesurée mais jamais écrite rendrait tout neuf ET périmé à chaque run ; trois collections séparées portent la distinction |
| `scripts/guards/lib/cssCouchesStock.mjs` (+ `.d.mts`) | STOCK GÉNÉRÉ : `CSS_IDENTITE_ECRAN_RATCHET`, `CSS_ESPACEMENT_RATCHET`, `STYLE_INLINE_RATCHET` — une entrée par occurrence `{ fichier, ref, occurrence }`, ordre `ordreDeStock` |
| `scripts/guards/lib/regenStock.mts` | AMORÇAGE : `regenererStock(p)` (`:42-51`) appelle `refusDeCroissance` AVANT d'écrire, et `stock.mjs:151-156` refuse tout site hors stock → un stock VIDE face à ≈ 2 400 sites est un refus. Ajout d'une option `amorce?: boolean` : quand elle est vraie, le refus de croissance est sauté et le stock est écrit depuis la mesure ; le régénérateur l'expose par le drapeau `--amorce`, LÉGAL au seul commit de création du stock (le message le dit ; tout usage ultérieur est un contournement visible au diff, la porte de plage le compte) |
| `scripts/ui/regen-css-couches-stock.mts` | RÉGÉNÉRATEUR (`regenererStock`) : `npx tsx scripts/ui/regen-css-couches-stock.mts [--check] [--amorce]` ; hors amorce, refuse toute croissance site par site (`refusDeCroissance`), réécrit le stock DÉCROISSANT-SEULEMENT |
| `scripts/docs/build-primitives.mjs:22-28` | accepte le champ optionnel `css` : chemin qui EXISTE, sous `src/ui/styles/`, jamais dans `SHARED_CSS_FILES` ; nouvelle colonne « CSS possédé » du tableau généré |
| `src/ui/ui-ratchets.test.ts` | gardes (xxi)/(xxii), suppression (xii), adaptation (vii)/(xiv)/(xx) — §4.3 |

Clés nominatives (`cleDeSite`, `stock.mjs:78`) — jamais la ligne du fichier porteur :
- identité : `fichier` = `src/ui/styles/<mod>.css`, `ref` = `<sélecteur normalisé (espaces réduits)> :: <prop>` (ex. `.city-hub-service:hover :: border-color`), `occurrence` = ordinal parmi les homonymes ;
- espacement : `ref` = `<sélecteur> :: <prop> :: <valeur>` ;
- inline : `fichier` = le `.tsx`, `ref` = `<balise>[.<première classe littérale>] :: <clés triées>` (ex. `i.dr-bar-fill :: width`), `expr` pour une valeur non littérale (`style={s}`, `style={a ? {…} : undefined}`), `vide` pour `style={{}}`.

### 4.2 Garde de CONTENU — liste FERMÉE des propriétés de PLACEMENT

Autorisées en module d'ÉCRAN (tout le reste est de l'identité, donc au stock) :

`display` · `flex`, `flex-*` · `grid`, `grid-*` · `gap`, `row-gap`, `column-gap` · `place-*`, `align-*`, `justify-*` · `order` · `position`, `inset`, `inset-*`, `top`, `right`, `bottom`, `left`, `z-index` · `width`, `height`, `min-*`, `max-*`, `aspect-ratio`, `box-sizing` · `margin`, `margin-*`, `padding`, `padding-*` · `overflow`, `overflow-*`, `overscroll-*`, `scroll-*` · `float`, `clear`, `columns`, `column-*` · `transform`, `transform-origin`, `translate`, `rotate`, `scale` · `text-align`, `vertical-align`, `white-space`, `text-overflow`, `overflow-wrap`, `word-break`, `hyphens` · `visibility`, `pointer-events`, `touch-action`, `user-select`, `resize` · `isolation`, `contain`, `will-change` · `list-style`, `list-style-*` · `--*` (variable : paramètre d'une primitive, patron `.swatch`).

Écarts assumés avec le script du lecteur (`section1-classes.mjs:15-16`) : `text-align`, `white-space`, `text-overflow`, `word-*`, `list-style*` comptés PLACEMENT ici (ils ferrent, débordent ou retirent une puce, ils ne peignent pas — cf. `.prow`, charte l.228 ; 14 sites d'écran seraient insoldables sinon) ; `cursor` compté IDENTITÉ (affordance = matière d'un contrôle, elle appartient à `.btn`/`.chip`). Le chiffre 2 677 est donc une borne haute de ±quelques dizaines ; le régénérateur donne le compte exact au lot 1.

**Obligation de token** (tranché) : en module d'ÉCRAN et dans `layout.css`, toute valeur de `gap`/`padding`/`margin` est `0`, `auto`, un `%`/`vw`/`vh`, `env(…)`, ou `var(--sp-*)` — un littéral `px`/`rem`/`em` est un site du stock `CSS_ESPACEMENT_RATCHET`. Les modules de PRIMITIVE n'y sont pas soumis (leur densité interne est leur matière). Limite écrite : une variable `--x: 7px` posée par un écran échappe à l'obligation (c'est un paramètre) ; `--x: #fff` échappe à (xxi) mais pas à (iv).

### 4.3 Spécification des `it`

Nouveau `describe('#1800 — trois couches CSS : un module d’écran ne pose que du PLACEMENT')` :

| `it` | Entrée | Rougit si |
|---|---|---|
| `(xxi) identité en module d’écran : stock nominatif, décroissant` | `mesureCssCouches().identite` vs `CSS_IDENTITE_ECRAN_RATCHET`, `ecartsDeStock` | `neuves.length > 0` (site neuf) OU `perimees.length > 0` (site soldé encore stocké → relancer le régénérateur). **C'est cette garde qui est le `--check`** : rien ne joue les régénérateurs en CI (`git grep regen-palette-literal-stock -- .github package.json` = 0) |
| `(xxi) espacement hors échelle : stock nominatif, décroissant` | `…espacement` vs `CSS_ESPACEMENT_RATCHET` | idem |
| `(xxii) style inline hors variable CSS : stock nominatif, décroissant` | `…inline` vs `STYLE_INLINE_RATCHET` | idem |
| `(xxi) le manifeste classe chaque module : un css de primitive existe, vit sous styles/, et n’est pas partagé` | manifeste + `SHARED_CSS_FILES` | un `css` absent du disque, hors `src/ui/styles/`, ou listé dans `SHARED_CSS_FILES` |
| `(xxi) preuve par mutation — une couleur dans une classe MAL NOMMÉE d’un module d’écran rougit` | fixture `{ rel:'src/ui/styles/faux.css', text:'.layout-truc { color: var(--gold) }' }` | `sitesIdentiteEcran` ≠ `[faux.css :: .layout-truc :: color]` |
| `(xxi) preuve — une classe de placement NEUVE est verte` | `.x { display:flex; gap:var(--sp-md); min-width:0; grid-template-columns:1fr 2fr }` | ≠ 0 site |
| `(xxi) preuve — chaque famille d’identité est vue` | `border-radius`, `box-shadow`, `font-weight`, `opacity`, `background`, `border-color`, `cursor`, `transition` → 1 site chacune ; `--ma-var: 3px` → 0 ; `list-style: none`, `touch-action: none`, `user-select: none` → 0 | |
| `(xxi) preuve — le contexte @media n’abrite rien` | `@media (max-width:700px) { .x { font-size:12px } }` → 1 site, `ref` porte le sélecteur seul | |
| `(xxi) preuve — le lexer rend les blocs imbriqués et saute les @keyframes` | `@media (a) { @supports (b) { .x { color:red } } } @keyframes k { from { color:red } }` → `reglesCss` rend UNE règle, `media` = `@media (a) && @supports (b)` ; (xx) sur `base.css` rend le même compte de règles case/radio qu'avant la réécriture (mesuré au commit) | |
| `(xxi) preuve — le même texte en module de PRIMITIVE est vert` | même fixture sous un chemin cité par `css` du manifeste (fixture de manifeste injectée) → 0 | |
| `(xxi) preuve — l’échelle` | `gap: 10px` → 1 ; `gap: var(--sp-md)` → 0 ; `margin: 0 auto` → 0 ; `padding: 0 max(12px, env(safe-area-inset-left))` → 1 (le 12px littéral) | |
| `(xxi) preuve — l’exemption est au SITE : une 2ᵉ déclaration homonyme est une occurrence NEUVE` | deux `color` sous `.x` ; stock ne portant que l'occurrence 1 → `neuves = [occurrence 2]` | |
| `(xxi) preuve — la cascade : `.stack[data-pad]` n’est écrasé ni par `.panel` ni par `.panel.flush`` | rendu jsdom de `<div class="panel flush stack" data-pad="md">` avec les DEUX feuilles réelles (`components.css` puis `layout.css`, dans l'ordre d'`@import` de `styles.css`) injectées dans `<style>` → `getComputedStyle(el).paddingTop === 'var(--sp-md)'` résolu (jsdom ne résout pas `var()` : asserter alors sur la RÈGLE gagnante par calcul de spécificité + ordre sur les `reglesCss` des deux feuilles — le test dit laquelle des deux méthodes il emploie) ; et `<div class="panel stack">` sans `data-pad` → padding du panel (`16px`) | |
| `(xxii) preuve — formes` | `style={{ width: w }}` → 1 · `style={{ '--x': w }}` → 0 · `style={{ '--x': w } as CSSProperties}` → 0 · `style={{ '--x': 1, width: 2 }}` → 1 · `style={s}` → 1 (`expr`) · `style={c ? { top } : undefined}` → 1 (`expr`) · `style={{}}` → 1 (`vide`) · `style={{ ...base }}` → 1 (`expr`) | |
| `(xxii) preuve — un commentaire n’est pas du markup` | `{/* style={{ color }} */}` → 0 | |

Modifications des gardes existantes :
- **(xii) SUPPRIMÉ** : bloc de commentaire + `DOMAIN_CSS_MODULES` `:249-298`, `CLASS_SELECTOR_BASELINE` `:299-828`, `it('(xii)…')` `:1122-1129`. `classNamesDefined` (`:898-917`) RESTE (xiii s'en sert). Le commentaire `:95` (« voir (xii) ») est réécrit.
- **(xiii) CONSERVÉ** : `SHARED_CSS_FILES` (`:848`) reçoit `'styles/layout.css'`, `SHARED_LEAK_BASELINE` reçoit `'styles/layout.css': 0` — tenable parce que `catalogueClasses()` (`:871-878`) lit les backticks de TOUTE la charte et que `classNamesDefined` ignore `[data-*]` : `.stack`, `.row`, `.grid`, `.split`, `.screen`, `.screen-body`, `.screen-body-wide`, `.screen-scroll`, `.master-detail-list`, `.clamp`, `.menu-card-large` DOIVENT être backtiqués dans la charte (§5).
- **(xiv)** (`:1149-1160`) réécrit : « chaque `.css` de `src/ui` est partagé (`SHARED_CSS_FILES`), de primitive (manifeste `css`) ou d'écran — et un module n'est jamais à la fois partagé et de primitive » ; ne cite plus `DOMAIN_CSS_MODULES`.
- **(vii)** (`:1082-1089`) : exclut `layout.css` en plus de `components.css` (`.row` s'enroule par construction) ; la baseline `creator.css: 5` (`:96`) BAISSE de 1 (mort de `.row-flex` `creator.css:273-278`).
- **(xx)** (`:1689-1736`) : importe `reglesCss` depuis `scripts/guards/lib/cssCouches.mjs` au lieu de la définition locale ; `PROPRIETES_DE_BOITE`/`boiteImmune` inchangés ; son compte de règles avant/après réécriture est asserté (ligne ci-dessus).
- **HUD — matrice responsive** (`:1243-1380`) : INCHANGÉE au lot 1 (elle lit `hud.css`/`combat-console.css`, qui ne bougent pas avant le lot combat, §6).
- `docs/charte-ui.md:49` (« Cliquets CI : (x)… (xii)… ») → « (x) boutons nus, (xxi) identité et espacement en module d'écran, (xxii) style inline ».

### 4.4 Ce qu'une garde ne voit pas (écrit, jamais tu)

- Une identité portée par un `data-*` de primitive détourné (`data-tone`) — hors forme CSS ; la revue seule.
- `src/gameIso/**/*.tsx` (21 sites `style=`) — hors périmètre de (xxii), ticket à ouvrir au lot 1 (§9).
- La SUBSTITUTION à compte égal (un site fautif remplacé par un autre, même ordre) — limite connue de tout stock nominatif (`paletteLiteralStock.mjs:22-25`).
- La RÉ-ORDINALISATION (`stock.mjs:83-90`) : corriger la 1ʳᵉ de deux occurrences homonymes, ou insérer une règle homonyme AVANT une autre, rend une périmée + une neuve pour un seul geste — le cliquet reste juste, sa phrase est bavarde ; se solde par un `regen`.
- Le RENOMMAGE d'un sélecteur (`.a` → `.b`, mêmes déclarations) = faux « site neuf » + faux « périmé » : soldé par `regen` ; net 0 pour la porte de plage, donc invisible au message de commit.
- Une valeur d'espacement calculée `calc(3 * var(--sp-md))` passe l'échelle : c'est voulu (multiple d'un token).

---

## 5. Docs vivantes — même commit que le socle

| Doc | Nature | Changement |
|---|---|---|
| `docs/charte-ui.md` | manuscrite | § Anatomie l.21 : « `.panel`/`.panel-grid` » → « `.panel` en `Grid` ». § Architecture CSS (`:38-57`) : trois couches, ordre `base → components → tabs → layout → modules` ; l.44 : le VERBATIM complet du 2026-07-12 à la place de la paraphrase, la seconde phrase (l.44-45) gardée comme rendu de la charte ; l.49 nouveaux cliquets ; l.54-57 : composer `Stack`/`Row`/`Grid`/`Split` au lieu de `.stack`/`.row-flex`/`.layout-sidebar`/`.panel-grid`. § catalogue : lignes `.stack`/`.row-flex` (`:116-117`) SUPPRIMÉES ; ligne `.menu-card` (`:125`) mentionne `.menu-card-large`. § Layouts responsive (`:187-199`) : lignes `.layout-sidebar`/`.panel-grid` SUPPRIMÉES ; lignes AJOUTÉES pour `.stack`, `.row`, `.grid`, `.split`, `.screen-scroll`, les attributs `data-gap`/`data-pad`/`data-stack-below`/`data-row-below`/`data-grow`/`data-push`/`data-span`, l'échelle `--sp-*` ; ligne `.master-detail` : « composé sur `Split aside="sm"` ». § Titres/texte : `.clamp`. § Politique grand écran l.335 : « `.ts-grid` » → « `Grid min="lg"` ». § Densité : l'échelle. § Galerie (`:419-427`) : rien à changer (la règle s'applique, voir lot 1). |
| `docs/recette-navigateur.md:723` | manuscrite | la piste « `sticky` de l'aside » s'éteint autrement : la carte n'a JAMAIS eu de colonne collante (`.layout-sidebar` ne posait pas de `position` ; `world-meta.css` pose `position:static` ≥901px) |
| `docs/primitives.md` | GÉNÉRÉ (`npm run docs:primitives`) | régénéré après : entrée `layout` (`label` `Stack/Row/Grid/Split`, `fichier` `src/ui/Layout.tsx`, `css` `src/ui/styles/layout.css`, `concept` « placement d'écran : pile, rangée, grille, split — gap/pad sur l'échelle, cassure canon », `perimetre` « tout écran », `verrou` « ui-ratchets (xxi)/(xxii) »), champ `css` sur les 8 entrées du §2.2, entrées `ornaments`/`notchGauge`/`windRose` |
| `docs/systemes.md` | GÉNÉRÉ (`npm run docs:systemes`, matrice d'adoption dérivée du manifeste) | régénéré |
| `.claude/memory/user-doctrine-ui-coherente-par-primitives-comme-les-donnees.md` puis `docs/doctrines.md` (GÉNÉRÉ, `npm run docs:doctrines`) | fiche doctrine | reçoit les verbatims des arbitrages A1/A2 (2026-09-18, §7) — **par l'ORCHESTRATEUR, jamais par le codeur** ; le codeur ne touche pas `.claude/memory/` |
| `scripts/guards/lib/structuresStock.mjs:898` | cardinal | `entrees: 62` → `66` (§2.2) |
| `src/ui/styles.css:1-7` et `components.css:6-23` | commentaires d'inventaire | `layout.css` inséré après `tabs.css` ; `.stack / .row-flex` retiré de l'inventaire de `components.css` |
| `src/ui/ui-ratchets.test.ts:250` | commentaire | verbatim complet |
| `src/ui/gallery/registry.tsx` | galerie DEV (charte `:426-427`) | quatre spécimens `Stack`/`Row`/`Grid`/`Split` montés avec des données réelles, sans aucun `style=` |

`npm run docs:check` (`check-doc-refs`, `check-plans-anchors`, `build-all --check`) doit rester vert : ce plan porte son ancre `Ticket: #1800` ; les symboles backtiqués cités par la charte (`Stack`, `Row`, `Grid`, `Split`, `grow`, `pushEnd`, `spanFull`) sont des exports réels de `src/ui/Layout.tsx` ; `check-doc-refs` 4b exige `.clamp` et `.menu-card-large` au catalogue.

---

## 6. Lots

### 6.1 Lot 1 — SOCLE (#1800, `.wt-1800`)

**Périmètre de fichiers EXACT**

Créés : `src/ui/Layout.tsx` · `src/ui/Layout.test.tsx` · `src/ui/styles/layout.css` · `scripts/guards/lib/cssCouches.mjs` · `scripts/guards/lib/cssCouches.d.mts` · `scripts/guards/lib/cssCouches.test.mjs` · `scripts/guards/lib/cssCouchesAudit.ts` · `scripts/guards/lib/cssCouchesStock.mjs` · `scripts/guards/lib/cssCouchesStock.d.mts` · `scripts/ui/regen-css-couches-stock.mts`.

Supprimés : `src/ui/styles/test-scenarios.css`.

Modifiés :
- socle : `src/data/primitives.manifest.json` · `scripts/docs/build-primitives.mjs` · `scripts/guards/lib/regenStock.mts` (option `amorce`) · `scripts/guards/lib/structuresStock.mjs:898` · `src/ui/styles.css` · `src/ui/styles/base.css` (tokens `:root` ; retrait `:448-485` — `.layout-sidebar`, `.panel-grid`, `.span-2`, leur `@media 900` — et `:492-494` — `.panel-grid` sous 700 ; le commentaire canon `:438-447` (règle 4) est RÉÉCRIT pour nommer les quatre primitives ; `.bar` `:487-491` et `.panel h3` `:495-498` RESTENT ; retrait `:667-671` `.screen`) · `src/ui/styles/components.css` (retrait `:52-63` `.stack`/`.row-flex`, `:583-616` `.master-detail*`, `:755-769` `.screen-body*` ; ajout `.clamp`, `.menu-card-large`) · `src/ui/styles/creator.css:273-278` (mort de `.row-flex` redéfinie) · `src/ui/styles/editor.css:699` (`.row-flex .dr` → `.row .dr`) ;
- primitives : `src/ui/MasterDetail.tsx` (rend `Split`/`Stack`, prop `aside?: 'sm' \| 'lg'` passée) · `src/ui/MenuCard.tsx` (prop `large`) ;
- consommateurs de `MasterDetail` (la classe `master-detail`/`master-detail-detail` quitte le DOM) : `src/ui/primitive-owners-guard.test.ts:114-116` · `src/ui/creator/CharacterCreator.test.tsx:46-47,109-110` (`toContain('master-detail-detail')`) · `src/ui/MasterDetail-scroll-edge.test.tsx:47,102` · `src/ui/creator/creator-step-scroll-cue.test.tsx:37,49` · `src/ui/InterludeScreen.test.tsx` · `scripts/guards/lib/domResiduStock.mjs:64` · `src/ui/styles/compendium.css:76-81` · `src/ui/styles/creator.css:890-947` · `src/ui/styles/city-hub.css:33-38` + `src/ui/CityHubScreen.tsx:338` (`<MasterDetail aside="lg">`) — chaque sélecteur scopé sur `.master-detail*` se réécrit sur `.split`/`[role]`/la classe de l'écran, jamais sur une classe morte ;
- écran de référence et sites dynamiques : `src/ui/TestScenariosScreen.tsx` · `src/ui/DrBar.tsx` · `src/ui/LifeBar.tsx` · `src/ui/PortraitTile.tsx` · `src/ui/compendium/CodexRef.tsx` · `src/ui/NotchGauge.tsx` (fichiers ENTIERS) et leurs règles CSS (`hud.css:1111-1112`, `gauges.css`, `compendium.css` `.codex-pop`) ;
- gardes et docs : `src/ui/ui-ratchets.test.ts` · `src/ui/gallery/registry.tsx` · `docs/charte-ui.md` · `docs/recette-navigateur.md:723` · `docs/primitives.md` (regen) · `docs/systemes.md` (regen) ;
- consommateurs des classes mortes : liste obtenue par `git grep -l -E "\b(row-flex\|panel-grid\|span-2\|layout-sidebar\|stack)\b" -- 'src/**/*.tsx' ':!*.test.tsx'` (≈ 35 fichiers ; `.stack` garde son nom, ses consommateurs composent `<Stack>` pour ne laisser aucun `className="stack"` manuscrit dans le socle).

**Ordre des étapes du codeur**

1. Manifeste : entrée `layout` + champ `css` + entrées `ornaments`/`notchGauge`/`windRose` (§2.2) ; `build-primitives.mjs` valide `css` ; `structuresStock.mjs:898` → 66 ; `npm run docs:primitives && npm run docs:systemes`. (Le hook `scripts/hooks/new-src-file-guard.mjs` refuse tout `.tsx` neuf de `src/ui` non déclaré : l'entrée précède la création de `Layout.tsx`.)
2. `base.css` : tokens `--sp-*`. `layout.css` : la liste fermée du §2.3, y compris les classes DÉPLACÉES (`.screen`, `.screen-body*` avec `var(--sp-sm)`, `.screen-scroll`, `.master-detail-list`). `styles.css` : `@import './styles/layout.css'` APRÈS `tabs.css`. `components.css` : retraits + `.clamp` + `.menu-card-large`. `creator.css:273-278` et `editor.css:699`.
3. `Layout.tsx` + `Layout.test.tsx` (rendu : classe, attributs par prop, `as`, absence d'attribut quand la prop est absente, `grow`/`pushEnd`/`spanFull`, `style` refusé par le type — test `// @ts-expect-error`).
4. `MasterDetail.tsx` sur `Split` (prop `aside`) ; `MenuCard` `large` ; `CityHubScreen.tsx:338` + `city-hub.css:33-38` ; migration des consommateurs (tests, stock `domResiduStock.mjs:64`, CSS scopé `compendium.css:76-81`, `creator.css:890-947`) ; suppression des classes mortes ; `git grep` de contrôle (DoD).
5. Gardes : `cssCouches.mjs` (lexer, contrat (xx)) + tests du parseur ; `cssCouchesAudit.ts` ; `regenStock.mts` option `amorce` ; `ui-ratchets.test.ts` (suppression (xii), adaptation (vii)/(xiv)/(xx), (xxi)/(xxii) + preuves, dont la cascade) ; AMORÇAGE : `npx tsx scripts/ui/regen-css-couches-stock.mts --amorce` → stock initial.
6. Écran de référence (§2.4) ; suppression de `test-scenarios.css` ; migration des 5 fichiers dynamiques (§3) ; `npx tsx scripts/ui/regen-css-couches-stock.mts` (SANS `--amorce`) → les entrées de `test-scenarios.css` et des 5 fichiers ont DISPARU du stock, aucune neuve.
7. Docs (§5), galerie, commentaires, verbatim l.250.
8. Tests du PÉRIMÈTRE seuls (fiche `feedback-codeur-tests-du-perimetre-gates-une-fois-au-train`) : `npx vitest run src/ui/ui-ratchets.test.ts src/ui/Layout.test.tsx src/ui/TestScenariosScreen.test.tsx src/ui/MasterDetail-scroll-edge.test.tsx src/ui/primitive-owners-guard.test.ts src/ui/creator/CharacterCreator.test.tsx src/ui/creator/creator-step-scroll-cue.test.tsx src/ui/InterludeScreen.test.tsx src/ui/roll-display-contract.test.tsx scripts/guards/lib/cssCouches.test.mjs scripts/guards/lib/structuresStock.test.mjs` (si ce dernier existe ; sinon le test qui consomme `structuresStock.mjs`, à trouver par `git grep -l structuresStock -- 'src/**/*.test.ts'`) ; `npm run typecheck` en sortie COMPLÈTE ; `npm run docs:check` ; `npx tsx scripts/ui/regen-css-couches-stock.mts --check`.
9. Recette navigateur (`docs/recette-navigateur.md`), en JOUEUR : menu principal → « Tests — scénarios », aux largeurs 360 / 700 / 900 / 1440 (`browser_resize`), console à 0 erreur ; au clavier : `Tab` jusqu'à un bouton « Lancer », `Entrée` → le scénario s'ouvre ; à 360 : aucune barre de défilement horizontale (`document.documentElement.scrollWidth <= innerWidth`) ; à 1440 : ≥ 3 colonnes de cartes (`getComputedStyle(grid).gridTemplateColumns.split(' ').length >= 3`) ; puis hub de ville (scénario « Arène 2.0 — le Bourg », onglet Plan) à 700 et 900 pour `MasterDetail aside="lg"` ; capture d'écran par largeur jointe au ticket.

**Message de commit** (porte de plage `stocksNominatifs.mjs:595-620` : UNE ligne par fichier, `+N` = croissance nette de LIGNES d'entrées, motif ≥ 20 caractères) :

```
CLIQUET: scripts/guards/lib/cssCouchesStock.mjs +<N> — amorçage du stock des trois couches CSS (#1800), décroissant-seulement ensuite
```

où N = nombre total de lignes d'entrées des trois collections ; les trois comptes (identité, espacement, inline) vont dans le CORPS du message, pas sur la ligne `CLIQUET:`.

**DoD mesurable**

- `git grep -c -E "\b(row-flex\|panel-grid\|span-2\|layout-sidebar)\b" -- src` = 0 ; `git grep -c -E "master-detail(-detail)?\b" -- src scripts` = 0 (seul `master-detail-list` survit) ; `git grep -c "ts-(grid\|card\|head\|ico\|tests\|party\|section)" -- src` = 0 ; `test-scenarios.css` absent.
- `git grep -c -E "CLASS_SELECTOR_BASELINE\|DOMAIN_CSS_MODULES\|\(xii\)" -- src/ui/ui-ratchets.test.ts` = 0.
- `npx tsx scripts/ui/regen-css-couches-stock.mts --check` → 0 ; le corps du commit porte les trois COMPTES initiaux (identité ≈ 2 400, espacement, inline = 92 + 30 − sites migrés) — point de départ des lots 2-6.
- Toutes les preuves par mutation du §4.3 vertes (cascade comprise) ; `typecheck` 0 erreur ; `docs:check` vert ; hook de nouveau fichier passé.
- Recette : 4 largeurs, 0 erreur console, lancement clavier, pas de défilement horizontal à 360 ; hub de ville Plan à 700/900.
- `TestScenariosScreen.tsx` : 0 `style=`, 0 classe `ts-*`, 0 CSS propre — « zéro CSS écrit, zéro inline ».

### 6.2 Lots de migration (un ticket + un `.wt-<N>` chacun)

Ordre = poids d'identité MESURÉ par FAMILLE d'écrans, décroissant — une famille partage ses primitives manquantes, l'extraction se fait une fois. Chaque lot : (a) l'identité des modules de la famille MIGRE vers les modules de leurs primitives (déplacement, pas effacement) ou meurt si la primitive canonique existe déjà ; (b) les blocs `@media` « placement seul » (40/75) deviennent des props `stackBelow`/`rowBelow` ; (c) les `style=` des `.tsx` de la famille passent en variables CSS ; (d) DoD : 0 entrée des trois stocks pour les fichiers de la famille, recette de chaque écran à 360/700/900/1440.

| Lot | Famille | Modules (identité mesurée) | Primitives à extraire / relocaliser (mesure) | Dépendance |
|---|---|---|---|---|
| 2 | Combat (935) | hud 357 · combat-modals 217 · combat-console 216 · combat-ui 145 | `.ptile*` (30+17) → module `portraitTile` ; `.rm-*` → `rollShell`/`rollRow`/`optionChooser` ; `.cc-arch/.cc-cell/.cc-end/.cc-socle` (90) → primitive « alvéole » de la console à DÉCLARER ; `.fx-chip` (17) → `FxChip` ; `DrBar` | la matrice HUD (`ui-ratchets.test.ts:1243-1380`) se RÉÉCRIT depuis le design 2026-07-31 §12 : elle assertera les props `stackBelow` rendues, plus le texte des `@media` |
| 3 | Monde (455) | world-meta 221 · merchant 94 · voyage 48 · mass-battle 48 · city-hub 23 · tavern 14 · exploration-dock 7 | « option de liste sélectionnable » (`.city-hub-service`, `.gpg-*` de `creator.css:792-808` = primitive `groupedPickGrid` sans module) ; liste clé/valeur (`.city-hub-synth`) ; `.worldmap-btn` | lot 1 |
| 4 | Atelier (360) | editor 150 · compendium 126 · gallery 41 · codex-edit 25 · house-rules 9 | `.tf-row` (10 consommateurs) → `Row` ; `.row .dr` (`editor.css:699`) ; `registry.tsx` (20 `style=`) ; `EditorCanvas.tsx` (9 `style={expr}`, filtres de calque → `data-calque`) | lot 1 |
| 5 | Fiche (323) | sheet 170 · party 153 | `.rm-roll` (16), `.seg` (charte l.199, composé par `OptionChooser`) → module de la primitive ; `PartyScreen.tsx` | lot 2 (partage `.rm-*`) |
| 6 | Créateur (313) | creator 289 · creator-presentation 23 · celestial-wheel 11 · postes-roster 7 · creator-shell 1 | `.dicewell` (22) → `creatorDice` ; `.talent-option` (20), `.step-chip` (15) ; `CreatorSummary.tsx` (6 `style={DIM}` → `data-eteint`) ; `creator-presentation.css` breakpoint 1100 (exempté (xviii) `:1438`) à recaler sur le canon | lot 3 (`.gpg-*`) |

Compte de clôture attendu de chaque lot : les trois stocks DÉCROISSENT du nombre d'entrées de la famille (chiffré au ticket à l'ouverture depuis le stock du lot 1) ; l'épic ne se ferme qu'à stocks vides (fiche `feedback-epic-ne-se-ferme-jamais-sur-tickets-ouverts`).

---

## 7. Arbitrages UTILISATEUR — RENDUS le 2026-09-18 (question fermée, option = assertion)

**A1 — La doctrine « stock de classes gelé et décroissant » (#373, 2026-07-12) est REMPLACÉE.** Assertion retenue (a) : « Un module d'ÉCRAN ne déclare que du PLACEMENT ; toute IDENTITÉ vit dans une primitive ; le nombre de classes de placement n'est plus compté. » Options écartées : (b) garder le gel du nombre de classes ET ajouter la garde d'identité (rejetée par la mesure 24 hausses / 17 baisses) ; (c) zéro CSS de domaine, même de placement. Conséquences : (xii) supprimé, (xxi) créé, frontière primitive/écran par le manifeste (§2.2). La fiche `user-doctrine-ui-coherente-par-primitives-comme-les-donnees.md` reçoit l'assertion datée (orchestrateur, §5).

**A2 — « sans styles inline » : la variable CSS posée inline est l'unique exception LÉGALE.** Assertion retenue (a) : « la seule forme légale de `style=` est un objet dont toutes les clés sont des variables CSS consommées par une classe. » Options écartées : (b) aucun `style=` du tout (géométrie par paliers `data-*`/SVG) ; (c) `style=` toléré pour la géométrie calculée. Conséquences : §3, (xxii), `style` interdit par le type des quatre primitives de layout (§2.3).

Décisions d'INGÉNIERIE prises par le plan, révisables sans cérémonie (credo : la cérémonie est réservée aux règles de jeu) : l'échelle à six pas et sa règle de recalage (§2.1) ; le champ `css` du manifeste comme frontière primitive/écran (§2.2) ; composants plutôt qu'utilitaires (§8.1) ; attributs `data-*` plutôt que classes modificatrices (§2.3) ; `layout.css` après `components.css` et `padding` conditionné à `[data-pad]` (§2.3).

---

## 8. Pistes écartées (avec la raison mesurée)

1. **Utilitaires Tailwind-like** (`.gap-2`, `.flex`, `.p-4`…). Un utilitaire n'est pas un concept : « un concept d'UI = une primitive » (2026-09-04). Mesure : 10 valeurs de `gap` × 5 propriétés d'espacement = 50 classes à cataloguer pour (xiii), et chaque écran recompose sa grille à la main — c'est la liberté qui a produit 92 `style={{` et 596 classes mixtes. Les props typées sur l'échelle (`gap: Espace`) donnent la même expressivité avec une erreur de compilation au lieu d'une dérive.
2. **CSS-in-JS / styled-components**. Ajoute une dépendance, sort les tokens de `:root` (217 tokens, cliquet (iv)), rend aveugles les parseurs texte de (iv)/(vii)/(ix)/(xiii)/(xx) et de ce plan ; l'identité migrerait dans le TSX, exactement l'inverse du cap « primitives et classes ».
3. **Garder (xii) EN PLUS de (xxi)** (option A1-b, écartée par l'utilisateur). Mesure : 24 hausses / 17 baisses en 60 j = (xii) compte des rangées et des grilles ; avec la couche layout, une classe de placement neuve reste légitime et (xii) rougirait à tort. Deux gardes sur deux quantités différentes = une garde de synchronisation (credo).
4. **CSS Modules / `*.module.css` scopés par écran**. Institutionnalise la classe mono-écran, le mal nommé par la doctrine.
5. **Container queries à la place des cassures**. Le canon 900/700/560 est gardé par (xviii) et cité par la charte (règle 4) ; les props `stackBelow`/`rowBelow` le portent sans changer le contrat.
6. **Un module `.css` par primitive de layout** (`stack.css`, `row.css`…). Quatre fichiers de 15 lignes pour une seule couche ; `layout.css` est la couche, comme `components.css` est la couche d'identité.
7. **Tokens de rayon et de police dans ce ticket**. Identité = primitives inchangées (brief) ; ce serait une refonte de `components.css` sans effet sur (xxi).
8. **Garder `.row-flex`/`.panel-grid` comme alias rendus par les composants**. Rétro-compatibilité (credo : aucune) ; deux portes pour un concept.
9. **`Bornee`/`Center` comme 5ᵉ primitive** (max-width 640/760/960). Le bornage d'un écran est DÉJÀ un concept de `ScreenShell` (`body='centered'|'centered-wide'`), de `Modal` (`.wide`) et de `MenuCard` (`large`) ; `.city-hub-detail` 640 est un plafond local que la doctrine résorbe dans le canon au lot 3.
10. **`layout.css` AVANT `components.css` avec `padding` inconditionnel** (première version de ce plan). Cassé par la mesure de cascade : `.panel { padding:16px }` et `.panel.flush` (`components.css:26-42`) auraient écrasé le `pad` de tout `<Stack className="panel">`.
11. **Une seule prop `collapse` sur les quatre primitives**. Même nom, sens inverse entre `Stack` (devient rangée) et `Row`/`Grid`/`Split` (deviennent pile) — remplacée par `stackBelow`/`rowBelow`.

---

## 9. Risques et neutralisation

| Risque | Neutralisation |
|---|---|
| Le hook `new-src-file-guard.mjs` refuse `src/ui/Layout.tsx` | étape 1 du lot 1 (manifeste avant création) |
| `catalogueClasses()` ne voit pas une classe de `layout.css` → (xiii) rougit ; `check-doc-refs` 4b rougit sur `.clamp`/`.menu-card-large` | chaque sélecteur du §2.3 et les deux classes neuves de `components.css` sont backtiqués dans la charte (§5) ; (xiii) et `docs:check` le prouvent |
| (vii) rougit sur le `flex-wrap` de `.row` | (vii) exclut `layout.css` ; baseline `creator.css` abaissée (§4.3) |
| `.panel`/`.panel.flush` écrasent le `pad` d'une primitive de layout | ordre d'`@import` + `padding` conditionné à `[data-pad]` + `it` de cascade (§2.3, §4.3) |
| Amorçage du stock refusé par `refusDeCroissance` | option `amorce` de `regenererStock`, drapeau `--amorce` légal au seul commit de création (§4.1) |
| Porte de plage : le commit qui CRÉE le stock est une croissance | UNE ligne `CLIQUET: scripts/guards/lib/cssCouchesStock.mjs +N — …` (format `stocksNominatifs.mjs:595-620`), comptes par famille dans le corps (§6.1) |
| `famille` mesurée mais jamais écrite → stock jamais stable | trois collections, `sitesEnEntrees` sans `famille` (§4.1) |
| Régression visuelle de l'écran de référence (matière du `.panel.sunken`, `.hint` 13 px, perte du vert `.ts-party`, `auto-fill` à peu d'items) | recette à 4 largeurs + captures ; le juge tranche à l'écran (`feedback-fait-se-juge-a-lecran-contre-attendu`) |
| Stock initial non reproductible entre machines (ordre, EOL) | `ordreDeStock` par unités de code (`regenStock.mts`) ; la garde (xxi) est le `--check` en CI (rien ne joue les régénérateurs, §4.3) |
| Matrice HUD (`:1243-1380`) casse quand `hud.css` migre | dépendance EXPLICITE du lot 2 ; inchangée au lot 1 |
| `MasterDetail` sur `Split` change le DOM (`master-detail`/`master-detail-detail` disparaissent) — 6 tests, 1 stock, 3 modules CSS les ciblent | tous au périmètre §6.1 et à la liste de tests de l'étape 8 ; `git grep -n "master-detail" -- src scripts docs` = seul `master-detail-list` à la sortie |
| `city-hub.css:36-38` scope sur `.city-hub-plan.master-detail` | migré au lot 1 vers `<MasterDetail aside="lg">` (§2.4) |
| Le codeur « solde » une entrée du stock en supprimant l'aspect au lieu de le déplacer | règle §2.2 écrite au ticket de chaque lot ; le juge compare les captures avant/après |
| `src/gameIso` reste hors (xxii) | issue ouverte au lot 1 (gabarit #101, label existant `gh label list`) : « (xxii) étendue à `src/gameIso` (21 sites) » — dédupliquée par label |
| Scripts de mesure pointés sur un worktree supprimé | en-tête : `ROOT` → `.wt-1800` avant rejeu ; les chiffres du §1.3 sont re-sondés sur `7193fa723` |
| 10 px / 14 px recalés « au voisinage » de façon incohérente entre lots | règle unique écrite §2.1, consignée au ticket de chaque lot |
