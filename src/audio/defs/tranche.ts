import type { SoundDef } from '../types';

/** Lame qui tranche (touche d arme tranchante / projectile) — Kenney RPG Audio (CC0). */
export const sound: SoundDef = { id: 'tranche', files: ['tranche-1.ogg', 'tranche-2.ogg'] };
