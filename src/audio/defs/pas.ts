import type { SoundDef } from '../types';

/** Pas (déplacement d un token) — Kenney RPG Audio (CC0). */
export const sound: SoundDef = { id: 'pas', files: ['pas-1.ogg', 'pas-2.ogg', 'pas-3.ogg'], volume: 0.4 };
