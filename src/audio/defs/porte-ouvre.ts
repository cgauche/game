import type { SoundDef } from '../types';

/** Porte qui s ouvre / se ferme (interactions de scene) — Kenney RPG Audio (CC0). */
export const sound: SoundDef = { id: 'porte-ouvre', files: ['porte-ouvre.ogg'], volume: 0.7 };
