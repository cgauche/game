import type { SoundDef } from '../types';

/** Parade métal sur métal (défense réussie) — Kenney Impact Sounds (CC0). */
export const sound: SoundDef = { id: 'parade', files: ['parade-1.ogg', 'parade-2.ogg'] };
