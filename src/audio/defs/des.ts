import type { SoundDef } from '../types';

/** Jet de dés (clic « Lancer » des modales) — Kenney Casino Audio (CC0). */
export const sound: SoundDef = { id: 'des', files: ['des-1.ogg', 'des-2.ogg', 'des-3.ogg'], volume: 0.7 };
