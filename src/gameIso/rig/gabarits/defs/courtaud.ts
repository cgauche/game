// Carrure trapue et large : Nain (base) ; Halfelin musclé via override.
import type { GabaritDef } from '../types';
export const gabarit: GabaritDef = { id: 'courtaud', sl: 0.74, st: 1.25, legs: 0.62 };
