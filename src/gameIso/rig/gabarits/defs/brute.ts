// Carrure massive et large : Ogre / Fimir (base) ; Minotaure & Géant via override.
import type { GabaritDef } from '../types';
export const gabarit: GabaritDef = { id: 'brute', sl: 1.35, st: 1.7, legs: 0.8 };
