// Carrure élancée et voûtée : Skaven (base) ; Bête-Non-Morte via override.
import type { GabaritDef } from '../types';
export const gabarit: GabaritDef = { id: 'elance-voute', sl: 0.96, st: 0.84, legs: 0.92 };
