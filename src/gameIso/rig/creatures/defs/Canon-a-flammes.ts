import type { CreatureDef } from '../types';

/**
 * CANON À FLAMMES NAIN — engin de siège, rendu par le gabarit `engin` (corps statique).
 * Route l'apparence par l'id d'espèce `canon-a-flammes` : chaudière sous pression et tube-lance.
 */
export const creature: CreatureDef = {
  label: 'Canon à flammes nain',
  id: 'canon-a-flammes',
  plan: 'engin',
};
