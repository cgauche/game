/**
 * Fragments d'art PARTAGÉS par plusieurs defs de queue (patron `heads/kit.ts`).
 */

/** Vue de DOS des queues reptiliennes (traînante et enroulée) : même fuseau pendant au centre. */
export const QUEUE_BACK_REPTILE = `<path d="M-2.4 0 Q-3 18 -1 32 Q0 40 0 46 Q0 40 1 32 Q3 18 2.4 0 Z" fill="@corps" stroke="@corpsO" stroke-width="0.6"/>`;
