import type { AppearanceElement } from '../types';
export const element: AppearanceElement = {
  key: 'queue-rat', label: 'Queue de rat', category: 'trait',
  overlays: [{ bone: 'bassin', appendage: 'queue-rat', svg: '', scale: 'bone', layer: -2 }],
};
