import type { AppearanceElement } from '../types';

/** Difformité morpho (mutation « Longues jambes », EDOC 12 p.67) : jambes allongées (multiplicateur 1.3). */
export const element: AppearanceElement = {
  key: 'longues-jambes', label: 'Longues jambes', category: 'mutation', legs: 1.3,
};
