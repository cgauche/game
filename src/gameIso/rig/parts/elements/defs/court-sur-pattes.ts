import type { AppearanceElement } from '../types';

/** Difformité morpho (Tableau de Corruption LDB 19) : jambes raccourcies (multiplicateur 0.78). */
export const element: AppearanceElement = {
  key: 'court-sur-pattes', label: 'Court sur pattes', category: 'mutation', legs: 0.78,
};
