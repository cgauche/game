import type { AppearanceElement } from '../types';

/** Difformité morpho (Tableau de Corruption LDB 19) : carrure réduite (−0.2 build, clampé 0..1). */
export const element: AppearanceElement = {
  key: 'emacie', label: 'Émacié', category: 'mutation', build: -0.2,
};
