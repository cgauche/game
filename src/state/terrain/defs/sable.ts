import type { TerrainDef } from '../types';

export const terrain: TerrainDef = { id: 'sable', label: 'Sable', walkable: true, priority: 2, gradient: 'g_sable', swatch: '#bca264', stops: [{ off: '0%', color: '#cdb37a' }, { off: '100%', color: '#a88a4e' }] };
