import type { QualityDef } from '../types';

// socMod migré en donnée éditable (`qualities.json` → `passive: testMod{Soc,-10}`) ; def réduite à la clé.
export const quality: QualityDef = { key: 'Laid', type: 'Défaut', subType: 'Objet' };
