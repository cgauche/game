import type { TraitDef } from '../types';

// LDB 85 p.341 : dépense tous ses Avantages — min 1 → Haine de ses adversaires au contact ;
// min 3 → Frénésie.
export const trait: TraitDef = { key: 'Rage', rage: true };
