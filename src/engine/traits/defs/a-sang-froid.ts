import type { TraitDef } from '../types';

// LDB 85 p.338 : « Elle peut inverser tous ses Tests de Force Mentale échoués. »
export const trait: TraitDef = { key: 'À sang-froid', coldBlooded: true };
