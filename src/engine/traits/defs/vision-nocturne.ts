import type { TraitDef } from '../types';

// LDB 85 p.343 : « La créature possède le Talent Vision nocturne. » (Voit dans une faible lumière.)
export const trait: TraitDef = { key: 'Vision nocturne', seesInDark: true };
